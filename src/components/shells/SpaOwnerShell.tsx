// Cleave stub — legacy Agentiport Karen spa-owner shell. Refill uses
// RefillShell instead. Dead branch in app.tsx, deleted in Step 3.12.
export function SpaOwnerShell({ children }: { children?: React.ReactNode }) {
  return <>{children}</>;
}
