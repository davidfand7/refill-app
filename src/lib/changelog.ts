/**
 * Rolling product changelog for the version pill (rendered by PageHeader).
 *
 * Fresh start for the cleaved Refill app — openagenticv4's 3,691-line
 * history is not load-bearing here. Append a new entry to the top of
 * CHANGELOG for each Refill ship.
 */

export interface ChangelogEntry {
  version: string;
  date: string;
  items: string[];
}

export const CHANGELOG: ChangelogEntry[] = [
  {
    version: "v2.170.0",
    date: "June 2026",
    items: [
      "<strong>v2.170.0 &mdash; &ldquo;See &amp; recall these patients&rdquo; now lands pre-selected and one click from drafting.</strong> Closing the last gap between a rebate move and outreach going out. The Program tab&rsquo;s recall CTA now lands on the patient list with the <strong>overdue cohort already selected</strong> (the 201 checked, bulk bar primed) &mdash; and the bulk bar gains a <strong>&ldquo;Draft recall outreach&rdquo;</strong> action that reuses the <em>exact same</em> recall-draft engine the Rewards/Recall panel uses (<code>draftRecallOutreachFn</code>, trigger=lapsed): it emails ready-to-send iMessage drafts to your proxy inbox, which you paste into Claude Desktop and send per-patient from Messages.app (nothing goes to a patient without your tap). So the full arc is now: rebate move &rarr; <em>see &amp; recall these patients</em> &rarr; land on the 201, pre-checked &rarr; <em>Draft recall outreach</em> &rarr; review &amp; send. <strong>Why keep it on the patient book</strong> (not the Recall panel): the count came from <code>doListOverdue</code>; drafting from the same surface keeps the cohort, the count, and the drafts reading one source &mdash; no fork. Chunks at the engine&rsquo;s 200-target cap; skips no-phone with an honest tally; the plain <em>&ldquo;201 patients&rdquo;</em> number still links to a no-preselect &ldquo;just show me&rdquo; view. <strong>New</strong>: <code>preselect=overdue</code> search param + &ldquo;Draft recall outreach&rdquo; bulk action (Apply soft tag demoted to secondary). <strong>Touched</strong>: <code>app.refill.patients.index.tsx</code>, <code>app.refill.recognition.program.tsx</code>, <code>changelog.ts</code>. No migration. <em>Banked: optional true auto-fire-on-arrival (draft without the click) if wanted.</em>",
    ],
  },
  {
    version: "v2.169.0",
    date: "June 2026",
    items: [
      "<strong>v2.169.0 &mdash; The &ldquo;201 patients&rdquo; is now a click into the actual 201, ready to recall.</strong> The flywheel count was a claim you couldn&rsquo;t drill into &mdash; you tried to confirm it and couldn&rsquo;t find the list (exactly the right instinct: a number you can&rsquo;t open is a number you can&rsquo;t act on). Now the cohort count on the &ldquo;Your next move&rdquo; hero <em>and</em> each row of &ldquo;Your moves&rdquo; is a <strong>link straight to those patients</strong>, and the CTA reads <strong>&ldquo;See &amp; recall these patients.&rdquo;</strong> It lands on <code>/app/refill/patients</code> pre-filtered to <em>overdue + that treatment kind</em> &mdash; the same multi-select list where you draft recall outreach. The recall engine&rsquo;s server fn already accepted a <code>kind</code> filter; this wires it to the URL (new <code>kind</code> search param: toxin/filler/biostimulator) and shows a <strong>&ldquo;Filler cohort ✕&rdquo;</strong> chip on the list so you know why you&rsquo;re seeing those rows (and can clear it). The count and the list now read from the same <code>doListOverdue</code>, so what the move claims and what the list shows are the same number, by construction. <strong>Touched</strong>: <code>app.refill.patients.index.tsx</code> (kind search param + cohort chip + pass-through), <code>app.refill.recognition.program.tsx</code> (count + CTA deep-links), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.168.0",
    date: "June 2026",
    items: [
      "<strong>v2.168.0 &mdash; The rebate move now names the real patients to recall toward it (the flywheel join).</strong> Until now &ldquo;Your next move: treat 10 more Restylane Refyne &rarr; unlock the 3% rebate&rdquo; stopped at the buy. This closes the loop the product is built on: each move now carries <strong>how many of your patients are overdue for that treatment kind</strong> &mdash; <em>&ldquo;You have 14 filler patients due to come back &mdash; recall them and those visits count toward this rebate <u>and</u> recover the patient.&rdquo;</em> One recall, two wins. The count is <strong>real, not estimated</strong>: it reuses <code>doListOverdue</code> &mdash; the exact source the recall surface reads &mdash; so the number can never drift from your recall list. The move&rsquo;s product keyword (&ldquo;restylane&rdquo;) resolves to a treatment kind (filler) via <code>resolveProduct</code>, and we scan the overdue cohort once per distinct kind (&le;3), best-effort (any hiccup just leaves the count off; the move still renders). When a cohort exists the CTA sharpens from &ldquo;Set up a recall offer&rdquo; to <strong>&ldquo;Recall these patients.&rdquo;</strong> Shown on the &ldquo;Your next move&rdquo; hero and each row in &ldquo;Your moves.&rdquo; <strong>New</strong>: <code>ProgramIntel.moves</code> is now <code>FlywheelMove[]</code> (carries <code>lapsedInCohort</code>); <code>enrichMovesWithCohort</code> in program-intel.functions.ts (dynamic-imports the recall engine, server-only). <strong>Touched</strong>: <code>program-intel.functions.ts</code>, <code>app.refill.recognition.program.tsx</code>, <code>changelog.ts</code>. No migration. <em>Note: only fires on an in-progress unit-purchase rebate &mdash; Rejuv has none live today, so verify via a seeded in-progress snapshot (SQL provided).</em>",
    ],
  },
  {
    version: "v2.167.0",
    date: "June 2026",
    items: [
      "<strong>v2.167.0 &mdash; Program tab now shows the pricing it captured, and stops calling an in-progress rebate &ldquo;maxed.&rdquo;</strong> Two readout-honesty fixes on the captured snapshot. <strong>(1) Signature pricing surfaced.</strong> Vision reads the &ldquo;signature pricing&rdquo; panel off a rewards dashboard (your Galderma capture pulled 10 prices) &mdash; but the Program tab only showed a <em>count</em> in the capture modal and then dropped them. New <strong>Signature pricing card</strong> lists each product + price, framed honestly as <em>reference</em> (what the dashboard says you pay) vs the catalog&rsquo;s <em>verified cost</em> (what drives margin), with a cross-link to set them in the verified-pricing lane (the &divide;box GIGO check lives there). Hidden when a capture had no pricing panel. <strong>(2) &ldquo;Maxed&rdquo; no longer fires on a live rebate.</strong> A rebate that&rsquo;s genuinely <em>in-progress</em> but emits no buy-N-units move &mdash; e.g. a <em>points-maintenance</em> threshold like Allergan&rsquo;s &ldquo;maintain 20 pts&rdquo; &mdash; was reading the &ldquo;you&rsquo;re maxed, secured what&rsquo;s in reach&rdquo; state, which is false (it&rsquo;s live, not secured). Split out an <strong>&ldquo;On track &mdash; nothing to buy toward it right now&rdquo;</strong> state that shows the rebate&rsquo;s real in-progress status + note; only an all-secured/blocked program shows &ldquo;maxed.&rdquo; <strong>Touched</strong>: <code>app.refill.recognition.program.tsx</code> (PricingCard + in-progress callout branch), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.166.1",
    date: "June 2026",
    items: [
      "<strong>v2.166.1 &mdash; Don&rsquo;t say &ldquo;you&rsquo;re maxed&rdquo; when the rebate page just wasn&rsquo;t in the screenshot.</strong> Honesty refinement on the v2.166.0 Program callout: a capture that has your tier + pricing but <em>no rebate trackers</em> (they live on a different portal tab) was reading the &ldquo;no moves &rarr; you&rsquo;re maxed, you&rsquo;ve secured what&rsquo;s in reach&rdquo; state &mdash; which is false; you didn&rsquo;t max them, they weren&rsquo;t captured. Absence of data &ne; nothing to do. Split it: <strong>no rebate trackers at all</strong> &rarr; <em>&ldquo;One more capture unlocks your moves &mdash; capture your rebate/tracker tab&rdquo;</em> (deep-links to the capture modal); <strong>rebates captured but none actionable</strong> (genuinely all secured / blocked) &rarr; the real &ldquo;maxed for now &mdash; put your earned units to work.&rdquo; <strong>Touched</strong>: <code>app.refill.recognition.program.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.166.0",
    date: "June 2026",
    items: [
      "<strong>v2.166.0 &mdash; The Program tab now lands you on a next step, and stops dressing the seed as your own.</strong> Two fixes from capturing a real snapshot. <strong>(1) The capture landed in the wrong place.</strong> &ldquo;View your standing&rdquo; (was &ldquo;View in Incentives&rdquo;) pointed at the <em>units pool</em> &mdash; the one Incentives tab that doesn&rsquo;t show your captured standing &mdash; so the payoff was invisible. It now lands on <strong>Program</strong>, where your tier, rebates, and moves render. <strong>(2) A landing callout that always names the next step</strong>, adaptive across three states: ① a rebate is within reach &rarr; <em>&ldquo;Your next move: treat N more X &rarr; unlock the Y% rebate&rdquo;</em> with a door into Offers; ② captured but nothing to chase (all secured / blocked &mdash; Rejuv today) &rarr; <em>&ldquo;You&rsquo;re maxed for now &mdash; put your earned units to work&rdquo;</em> into the units pool; ③ <strong>no capture yet</strong> &rarr; the &ldquo;out&rdquo;: instead of silently showing the Rejuv <em>seed</em> as if it were yours (a Connection-Health honesty hole &mdash; placeholder wearing the real hat), it shows <em>&ldquo;No standing captured yet &mdash; capture your rewards dashboard to light this up,&rdquo;</em> deep-linking (<code>#capture</code>) straight into the capture modal. <strong>New</strong>: <code>ProgramIntel.captured</code> flag (real row vs seed fallback) so the UI can tell them apart. <strong>Touched</strong>: <code>program-intel.functions.ts</code> (loadSnapshot + type), <code>app.refill.recognition.program.tsx</code> (NextMoveCallout + NotCapturedState), <code>app.refill.settings.manufacturers.tsx</code> (link &rarr; Program, <code>#capture</code> auto-open), <code>changelog.ts</code>. No migration. <em>Honest fast-follow banked: the move callout shows the verified rebate move but not yet the lapsed-patient cohort count (needs the recall-engine join) &mdash; no fabricated patient numbers.</em>",
    ],
  },
  {
    version: "v2.165.1",
    date: "June 2026",
    items: [
      "<strong>v2.165.1 &mdash; Capture-snapshot button now tells you what it&rsquo;s waiting for.</strong> Quick UX polish on the v2.165.0 rewards-capture modal: before you&rsquo;ve added a screenshot, the primary action was a <em>silently greyed</em> &ldquo;Capture snapshot&rdquo; &mdash; correct (you can&rsquo;t read nothing) but it reads as <em>broken</em>. It now says <strong>&ldquo;Add a screenshot first&rdquo;</strong> while disabled, so the modal guides instead of stonewalling; it flips to &ldquo;Capture snapshot&rdquo; the moment an image is in the dropzone. <strong>Touched</strong>: <code>app.refill.settings.manufacturers.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.165.0",
    date: "June 2026",
    items: [
      "<strong>v2.165.0 &mdash; Capture your manufacturer rewards standing with a photo (Slice 2 of the capture &rarr; intelligence loop).</strong> Slice 1 built the persistence; this lands the <strong>capture path</strong> that fills it. On <strong>Account &rarr; Manufacturers</strong> there&rsquo;s a new <strong>&ldquo;Capture rewards snapshot&rdquo;</strong> button: snap or upload your rewards-dashboard screen &mdash; the tier / points / rebate-tracker page (Galderma ASPIRE Program Details, Allē for Business, Evolus Rewards) &mdash; and Claude vision reads where you <em>stand</em> (current tier, points, progress to next level, each rebate tracker&rsquo;s status, signature pricing) into a <code>ProgramSnapshot</code> and writes a real <code>program_snapshots</code> row. The moment it lands, <code>loadSnapshot</code> serves your <em>real</em> standing instead of the seed constant &mdash; <strong>Incentives &rarr; Program auto-upgrades off live data</strong>, and your next capture diffs against this one (the &ldquo;what changed&rdquo; detector). The modal shows an immediate readout of exactly what we read, so it&rsquo;s never a black box. <strong>Honest by design</strong>: an owner-snapped screenshot is labeled <code>manual_capture</code>, never implied to be an automated live pull. These are the auth-walled, per-spa numbers no CSV or scraper can reach &mdash; only a photo from inside your own account (the vision-ingestion moat). <strong>New</strong>: <code>program-snapshot-core.ts</code> (server-only vision engine, Anthropic SDK isolated behind a dynamic import &mdash; never in the client bundle), <code>captureProgramSnapshotFn</code>. <strong>Touched</strong>: <code>app.refill.settings.manufacturers.tsx</code> (capture button + modal + snapshot readout), <code>program-intel.functions.ts</code>, <code>changelog.ts</code>. Reuses the v2.164 table &mdash; no new migration. <em>Slice 3 (Account/Manufacturers as the polished capture front-door, Incentives/Program as the live readout) is next.</em>",
    ],
  },
  {
    version: "v2.164.0",
    date: "June 2026",
    items: [
      "<strong>v2.164.0 &mdash; Program Intelligence can now read a REAL per-tenant snapshot (Slice 1 of the capture &rarr; intelligence loop).</strong> The Incentives &rarr; Program surface (your manufacturer&rsquo;s tiers, rebates, and the moves to unlock them) was served entirely by a hardcoded constant &mdash; one captured Rejuv/Galderma snapshot, the same for everyone. This ship lands the <strong>persistence layer</strong>: a new <code>program_snapshots</code> table (per-tenant, per-manufacturer, append-only per pull) and a DB-first <code>loadSnapshot</code> that reads your <em>latest</em> snapshot plus the <em>prior</em> one of the same manufacturer (so the &ldquo;what changed&rdquo; diff lights up the moment you have history). <strong>Safe by construction</strong>: the read is wrapped defensively &mdash; until a real snapshot is captured (Slice 2: vision/AI parses your rewards-dashboard screenshot into a snapshot and writes a row here), it falls back to the seed constant, so nothing changes yet and the surface never breaks. The fallback is the only thing still serving hardcoded data; it retires automatically when captures land. No UI, type, derive, or diff logic moved &mdash; just the swap-point, exactly as designed. <strong>New</strong>: migration <code>program_snapshots</code> (RLS, service-role only). <strong>Touched</strong>: <code>program-intel.functions.ts</code> (async DB-first loadSnapshot, tenant-resolved), <code>types.ts</code>, <code>changelog.ts</code>. <em>Slices 2&ndash;3 (capture path + Account/Manufacturers as the front door) are the next arc.</em>",
    ],
  },
  {
    version: "v2.163.0",
    date: "June 2026",
    items: [
      "<strong>v2.163.0 &mdash; Reports stops showing a dollar number it never actually had.</strong> Closing the last money-board fast-follow from the island audit: an investigation into Account &rarr; Reports&rsquo; &ldquo;attributed revenue&rdquo; found its source column (<code>patient_outreach_state.attributed_revenue_usd</code>) is <strong>never written anywhere</strong> &mdash; a reserved-but-dead field. So the headline revenue stat read a <em>structural</em> $0, and the per-campaign Revenue column was permanently &ldquo;&mdash;&rdquo;. Showing a money number that can never be real is a trust hole, so we removed both: the top stat is now <strong>&ldquo;Closed-won&rdquo;</strong> (a real funnel outcome &mdash; won, with showed/booked underneath) and the dead Revenue column is gone. Reports is now honestly the <strong>conversion-funnel lens</strong> (sent &rarr; booked &rarr; won, no dollars); the verified, invoice-grade recovered $ lives in <strong>Recovery</strong>, where the on-board link points. <strong>Confirmed</strong> there&rsquo;s no double-count: every <code>attributed_revenue_usd</code> <em>write</em> in the codebase is on <code>recovery_events</code> (the single source of truth) &mdash; the outreach copy was only ever read. <strong>Touched</strong>: <code>app.refill.reports.tsx</code>, <code>changelog.ts</code>. No migration. <em>Future enhancement (banked): wire real campaign-attributed $ by slicing recovery_events per campaign &mdash; needs an attribution model (which campaign earns a given recovery).</em>",
    ],
  },
  {
    version: "v2.162.0",
    date: "June 2026",
    items: [
      "<strong>v2.162.0 &mdash; Fix the &ldquo;May 2026 &middot; closes Jun 30&rdquo; month label on the Recovery invoice card.</strong> The &ldquo;If this month closed today&rdquo; card showed a month <em>one behind</em> the close date (e.g. &ldquo;May 2026 &middot; closes Jun 30&rdquo; on June 23). The invoice period is a <em>date-only</em> calendar-month boundary built from <code>Date.UTC(year, month, 1)</code> &mdash; not a real moment &mdash; but the label rendered it in a negative-offset timezone (<code>America/Denver</code>), which shifts UTC-midnight back a day and pushed the start into the <em>previous</em> month. The close date (<code>periodEnd</code> = next-month start) happened to land right only by the same accidental shift. Now both render in <strong>UTC</strong> (month from <code>periodStart</code>; close = <code>periodEnd</code> &minus; 1 day) &mdash; so it reads &ldquo;<strong>June 2026 &middot; closes Jun 30</strong>.&rdquo; This is the exact exception the v1.4.6 TZ sweep carved out: real-moment timestamps (rescue/appointment times) stay spa-local; <code>Date.UTC</code>-constructed date-only values must render UTC. <strong>Touched</strong>: <code>app.refill.recovery.index.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.161.0",
    date: "June 2026",
    items: [
      "<strong>v2.161.0 &mdash; The Recovery headline now agrees with your invoice (tenant-scoped, like billing).</strong> Right after crowning <strong>Recovery &rarr; Overview</strong> as THE recovered-$ home (v2.160.0), an audit of the screen itself found a divided house: its <strong>headline cards</strong> (This month / Lifetime verified $) were computed <em>user-scoped</em> (<code>.eq user_id</code>) while the <strong>invoice preview</strong> sitting inches below &mdash; and actual billing &mdash; are <em>tenant-scoped</em> (<code>.in</code> across every member via <code>tenant_memberships</code>). So a spa with more than one login saw a headline <em>smaller</em> than its own invoice on the same screen, and billing settled on the bigger number. Now <code>getRecoveryStats</code> fans out across the whole tenant through the <em>same</em> resolver billing uses &mdash; one shared <code>getTenantUserIds</code> helper feeds both paths, so they can&rsquo;t drift again by construction. Single-login spas (incl. Rejuv today) see <em>identical</em> numbers &mdash; the fix is forward-looking for multi-user tenants and impersonated/cross-tenant views. All four reads (month, prior-month, lifetime, provisional count) now tenant-scoped. <strong>Touched</strong>: <code>auth-helpers.ts</code> (new <code>getTenantUserIds</code>), <code>billing-fee-core.ts</code> (reuses it &mdash; DRY parity), <code>emma-attribution.functions.ts</code>, <code>changelog.ts</code>. No migration. Closes one of two fast-follows from the island audit; Reports funnel-source reconcile still queued.",
    ],
  },
  {
    version: "v2.160.0",
    date: "June 2026",
    items: [
      "<strong>v2.160.0 &mdash; One canonical home for &ldquo;what SmartSpa recovered for me.&rdquo;</strong> A platform-wide island audit found <em>recovered $</em> showing up on several screens. The good news: the <strong>numbers can&rsquo;t drift</strong> &mdash; Recovery, the invoice, and the new Incentives scoreboard all settle on the <em>same</em> verified math (<code>aggregateMetricsForTenant</code>). The fix here is about <em>clarity</em>, not correctness: pick <strong>one authority</strong> and have the rest defer to it. <strong>Recovery &rarr; Overview</strong> stays THE recovered-$ home (verified, audit-trail, invoice-grade). The <strong>&ldquo;Incentives at work&rdquo;</strong> band is now framed as the <em>incentive-ROI lens</em> (&ldquo;incentive-driven&rdquo;) &mdash; a subset of recovered $, with a link straight to the full Recovery audit. <strong>Account &rarr; Reports</strong> relabels its number to <strong>&ldquo;Campaign-attributed&rdquo;</strong> (funnel, all-time &mdash; a different source than verified recoveries) and links to Recovery for the dollars that actually bill, so the two never read as competing authorities. <strong>Also fixed</strong>: a rep&rsquo;s <strong>Live earnings</strong> read wasn&rsquo;t paginated &mdash; a high-volume rep past PostgREST&rsquo;s 1,000-row cap would silently under-report lifetime earnings; now it pages the full ledger. <strong>Touched</strong>: <code>IncentivesAtWorkBand.tsx</code>, <code>app.refill.reports.tsx</code>, <code>rep-platform.ts</code>, <code>changelog.ts</code>. No migration. <em>Scoped as fast-follows: reconciling Reports&rsquo; funnel source vs verified recoveries, and unifying Recovery&rsquo;s two internal read paths.</em>",
    ],
  },
  {
    version: "v2.159.0",
    date: "June 2026",
    items: [
      "<strong>v2.159.0 &mdash; Allocation becomes a real ROI lane.</strong> Last ship&rsquo;s &ldquo;Incentives at work&rdquo; band showed <strong>Allocation</strong> as send-only with a &ldquo;conversion tracking soon&rdquo; tag &mdash; honest, but incomplete. Now it&rsquo;s <em>wired</em>: when a patient who received a <strong>deployed rebate allocation</strong> (a confirmed/sent suggestion) books a visit, SmartSpa settles it as a recovery event (<code>recovery_agent='allocation'</code>) and back-references the booking onto the suggestion &mdash; so the Allocation lane shows real <strong>bookings + dollars recovered</strong>, folded into the headline total alongside Recall and Cross-sell. <strong>Honest by construction</strong>: allocation is the <em>lowest-priority</em> agent &mdash; there&rsquo;s one win per booking, so if a more specific agent (recall / cross-sell / reschedule) already claimed that visit, allocation does <em>not</em> double-count the revenue; it only credits bookings nothing else claimed. Credit is bounded by a 120-day look-back and one-allocation-one-booking, and stale/pending suggestions never qualify. Until a patient actually books, the lane reads &ldquo;N sent &middot; awaiting first booking.&rdquo; <strong>Not billed</strong> &mdash; allocation deploys the spa&rsquo;s <em>own</em> rebate units, so it&rsquo;s scored for the scoreboard but charges no fee. <strong>New</strong>: <code>recordAllocationWinIfBooked</code> (wired into both booking flows, best-effort, never blocks a booking). <strong>Migration</strong>: widen the <code>recovery_agent</code> CHECK to accept <code>'allocation'</code>. <strong>Touched</strong>: <code>refill-recognition-allocation.functions.ts</code>, <code>scheduling.functions.ts</code>, <code>scheduling-owner.functions.ts</code>, <code>refill-incentive-scoreboard.functions.ts</code>, <code>IncentivesAtWorkBand.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.158.0",
    date: "June 2026",
    items: [
      "<strong>v2.158.0 &mdash; Incentives at work: one honest scoreboard for what your incentives actually do.</strong> The <strong>Incentives</strong> tab managed your rebate pool and promos &mdash; but never showed the <em>payoff</em>. New band atop the tab unifies all three incentive surfaces under one lens (trailing 12 months): <strong>Recall</strong> and <strong>Cross-sell offers</strong> show <em>verified</em> bookings + <strong>dollars recovered</strong> &mdash; and that dollar is <em>your</em> recovered treatment revenue (reconciled from your transactions), not SmartSpa&rsquo;s fee. The headline totals the two into one &ldquo;<strong>$X recovered across N bookings</strong>.&rdquo; <strong>Allocation</strong> sits beside them honestly as <em>send-only</em> &mdash; incentives + units you&rsquo;ve put to work for specific patients &mdash; tagged &ldquo;<strong>Conversion tracking soon</strong>&rdquo; rather than faking a dollar it can&rsquo;t yet prove (booking attribution is the fast-follow). Reuses billing&rsquo;s exact <code>aggregateMetricsForTenant</code> math, so the scoreboard and the invoice can&rsquo;t drift; paginated reads on both lanes (no silent under-count). Empty practice gets a plain &ldquo;shows up here as recalls and offers convert&rdquo; state. <strong>New</strong>: <code>refill-incentive-scoreboard.functions.ts</code>, <code>IncentivesAtWorkBand.tsx</code>. <strong>Touched</strong>: <code>recognition.inventory.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.157.0",
    date: "June 2026",
    items: [
      "<strong>v2.157.0 &mdash; Program Intelligence stops pretending it&rsquo;s live.</strong> The <strong>Program</strong> tab (your manufacturer&rsquo;s tiers/rebates/moves) is built on a <em>snapshot</em> — but the freshness line just showed a date, so a months-old capture read the same as a fresh one. That&rsquo;s the silent-stale trap. Now it runs through SmartSpa&rsquo;s <strong>Connection Health</strong> spine (snapshot tier): it shows the real <strong>age</strong> (&ldquo;captured 2 months ago&rdquo;) + an honest tier label (&ldquo;Snapshot · refreshed on import&rdquo;), and once a capture passes ~2 weeks it flips <strong>amber — &ldquo;May be out of date&rdquo;</strong> with a plain-English caution that your manufacturer may have moved a tier/threshold/price since, so you treat the numbers as last-known rather than current. Never hard-breaks (a snapshot aging isn&rsquo;t a failure), never implies live. Also fixed a stray &ldquo;Recognition&rdquo; eyebrow → &ldquo;Incentives&rdquo;. <strong>Touched</strong>: <code>recognition.program.tsx</code> (reuses <code>computeVerdict</code>/<code>formatAge</code>/<code>tierLabel</code>), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.156.0",
    date: "June 2026",
    items: [
      "<strong>v2.156.0 &mdash; Island cleanup: one home for manufacturer-promo delivery.</strong> After last change moved promo management to <strong>Incentives</strong>, a manufacturer promo&rsquo;s delivery (who/when/cap, on-Deals) could still <em>also</em> be edited from a &ldquo;Deliver a manufacturer promo&rdquo; tab inside the Offers composer &mdash; two places editing the same thing. Retired that composer tab so there&rsquo;s one edit home: <strong>Incentives</strong>. The <strong>Offers</strong> composer now stays focused on what only it does &mdash; <em>authoring</em> your own offers and the head-to-head <strong>Arena</strong> (the impartial A/B, untouched). Note: A/B creation was already unified into this one composer (back in v2.100), so &ldquo;Your offers / Your tests&rdquo; are just result lists &mdash; nothing duplicated there. <strong>Touched</strong>: <code>OfferComposer.tsx</code> (one tab removed; mfr-mode code kept but unreachable since the Arena still uses the promo data), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.155.0",
    date: "June 2026",
    items: [
      "<strong>v2.155.0 &mdash; Island cleanup: Incentives ↔ Brand economics, bridged.</strong> Two pages talk about the same rebates/samples/vouchers from different angles &mdash; <strong>Incentives</strong> is the pool you <em>deploy</em> to patients, <strong>Brand economics</strong> is the margin lens (what those incentives <em>cost</em> you and how they rank your high-margin substitute). They&rsquo;re deliberately separate tools, but nothing said so. Now each carries a one-line cross-link to the other (&ldquo;…to see what each incentive costs your margin, open Brand economics →&rdquo; / &ldquo;…to deploy them to specific patients, open Incentives →&rdquo;), so the relationship reads as one story instead of two disconnected islands. <strong>Touched</strong>: <code>recognition.inventory.tsx</code>, <code>catalog.brand-economics.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.154.0",
    date: "June 2026",
    items: [
      "<strong>v2.154.0 &mdash; Island cleanup, Tier 3: Rewards + Recall are one surface now.</strong> They were always the same data (your manufacturer reward signals) shown two ways &mdash; <strong>Rewards</strong> as a data explorer, <strong>Recall</strong> as the place to act &mdash; so you had to bounce between tabs. Merged into a single <strong>Rewards</strong> tab, <strong>action-first</strong>: open it and you land straight on who to recall and the money on the table (Expiring · Eligible &amp; idle · Becoming eligible · Lapsed), with <strong>Draft outreach</strong> and <strong>Book</strong> right there. Below that, an <strong>&ldquo;Imports &amp; sources&rdquo;</strong> section holds the plumbing &mdash; the manufacturer CSV uploads, attribution review, and raw signal list that feed it. The standalone <strong>Recall</strong> tab is gone (Recognition is now 5 tabs: Incentives · Rewards · Offers · Program · Allocation); old <code>/recall</code> links + the weekly-digest email now point straight at Rewards. The reward-expiry badge moved onto the Rewards tab. <strong>Touched</strong>: <code>recognition.recall.tsx</code> (now exports <code>RecallPanel</code> + redirects), <code>recognition.rewards.tsx</code> (renders the panel action-first), <code>RecognitionTabs.tsx</code>, <code>skill-catalog.ts</code>, <code>refill-recall.functions.ts</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.153.0",
    date: "June 2026",
    items: [
      "<strong>v2.153.0 &mdash; Island cleanup, Tier 2 (promos get one home).</strong> Manufacturer promos used to be managed in a card buried on the <strong>Rewards</strong> tab while everything else about them lived elsewhere &mdash; so &ldquo;where do I manage a promo?&rdquo; had two answers. Now there&rsquo;s one: the <strong>Incentives</strong> tab. The promo <em>manager</em> (the list, the on-Deals eye toggle, and the per-offer delivery editor — who it&rsquo;s for, which days, the cap) now lives on Incentives. The CSV <em>feed</em> stays on Rewards alongside your other manufacturer-export uploads, with a <strong>&ldquo;Manage in Incentives →&rdquo;</strong> link; Incentives shows an <strong>&ldquo;Import on Rewards →&rdquo;</strong> link back. Same data, same promos — just one obvious place to manage them. Under the hood the card moved into a shared <code>PromoIntelligenceCard</code> component with import/manage modes (no logic forked). <strong>Touched</strong>: new <code>components/refill/PromoIntelligenceCard.tsx</code>, <code>recognition.rewards.tsx</code> (import-only + dead-import cleanup), <code>recognition.inventory.tsx</code> (manage), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.152.0",
    date: "June 2026",
    items: [
      "<strong>v2.152.0 &mdash; Island cleanup, Tier 2 (rename): &ldquo;Promos / Inventory&rdquo; → &ldquo;Incentives.&rdquo;</strong> The section that holds your manufacturer-funded patient money was confusingly labeled three different ways &mdash; the top-nav chip said <em>Promos</em>, the first tab said <em>Inventory</em>, and breadcrumbs said <em>Promos</em>. It&rsquo;s all one idea now: <strong>Incentives</strong> &mdash; the manufacturer-funded units (portal-issued rebates + promo/samples) you deploy toward specific patients. Renamed everywhere it shows: the top nav chip + dashboard card, the tab, the page heading, and every breadcrumb across Rewards / Offers / Program / Recall / Allocation / Manufacturers. <em>(Same data, same routes &mdash; purely a clarity rename. Next step folds manufacturer-promo editing into this tab so there&rsquo;s one place to manage it.)</em> <strong>Touched</strong>: <code>RecognitionTabs.tsx</code>, <code>RefillNav.tsx</code>, <code>RefillHome.tsx</code>, the recognition routes + <code>settings.manufacturers.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.151.0",
    date: "June 2026",
    items: [
      "<strong>v2.151.0 &mdash; Island cleanup, Tier 1: the catalog stops contradicting itself.</strong> A sweep of stale and duplicated calls-to-action across the Catalog tabs. <strong>(1) Starter catalog is now state-aware</strong> &mdash; if your catalog is already loaded (it is, for any spa past signup), the big &ldquo;Load starter catalog&rdquo; pitch on <strong>Programs &amp; tiers</strong> shrinks to a quiet &ldquo;✓ Catalog loaded — N products&rdquo; with a &ldquo;Manage products →&rdquo; link and a low-key &ldquo;add more from the book&rdquo;; only a genuinely empty spa sees the full loader. <strong>(2) The Services toolbar de-clutters</strong> &mdash; the three one-time maintenance actions (Auto-categorize, Link products, Clean up duplicates) collapse into a single <strong>&ldquo;Data tools&rdquo;</strong> menu, so the row isn&rsquo;t a wall of seven buttons. <strong>(3) &ldquo;Recategorize&rdquo; &rarr; &ldquo;Auto-categorize&rdquo;</strong> on both tabs (clearer verb; tooltips spell out that Products also matches brand + manufacturer). <strong>(4) &ldquo;Import CSV&rdquo;</strong> on Services and Products now says, on hover, that it&rsquo;s <em>one</em> importer shared by both tabs &mdash; and points you to <strong>Verified pricing</strong> for manufacturer &ldquo;Your Price&rdquo; lists instead. <strong>(5) The empty Products state</strong> now offers CSV import + starter-catalog, matching Services. New shared <code>getCatalogStatusFn</code> (head-only counts) powers the state-aware bits. <strong>Touched</strong>: <code>refill-catalog.ts</code>, <code>programs.tsx</code>, <code>services.tsx</code>, <code>products.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.150.0",
    date: "June 2026",
    items: [
      "<strong>v2.150.0 &mdash; Rep Deal-Maker (slice 2.1): how far you are from your next break.</strong> Your verified ladder already shows your next volume break (&ldquo;buy 100+ vials &rarr; $379, save $10&rdquo;). Now it tells you whether that&rsquo;s actually within reach <em>at your pace</em> &mdash; e.g. <em>&ldquo;At your pace (~96/yr) you&rsquo;re ~4/yr from this break&rdquo;</em> or <em>&ldquo;...you already clear this &mdash; make sure your rep has you on the better tier.&rdquo;</em> And in the draft dialog, your &ldquo;Commit to ~&rdquo; ask shows how it sits against your pace (&ldquo;within your ~96/yr pace&rdquo;). <strong>The careful part &mdash; no fake math.</strong> Comparing &ldquo;how much you buy&rdquo; to a ladder threshold is a trap: a filler is priced per syringe (the unit you buy), but a toxin is priced per <em>dosing unit</em> (~$12), not per vial &mdash; so naively counting would silently mis-compare. SmartSpa only shows the distance when it can <em>prove</em> your unit matches the ladder&rsquo;s (your per-unit sales price lines up with the per-unit cost); for toxins priced in dosing units it stays quiet rather than guess. The distance is derived from reliable dollars (your spend &divide; your sales price), never from an ambiguous quantity field. <strong>Touched</strong>: <code>manufacturer-burn-rate.functions.ts</code> (per-product spend + catalog brand_family/sales-price match), <code>programs.tsx</code> (reachability line + same-unit gate + dialog readout), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.149.0",
    date: "June 2026",
    items: [
      "<strong>v2.149.0 &mdash; Rep Deal-Maker (slice 2): your own buying pace, armed for the ask.</strong> The Leverage window now knows how big an account you are. SmartSpa reads your own transaction history and surfaces your <strong>volume with each manufacturer</strong> right on the card &mdash; e.g. <em>&ldquo;Your volume: ~$8.4k/qtr · ~$34k/yr &mdash; you&rsquo;re a real account.&rdquo;</em> That spend figure is rock-solid (it&rsquo;s just the sum of your own purchases, no guesswork), and it&rsquo;s the kind of leverage a rep actually responds to. When you open <strong>&ldquo;Draft my Rep ask,&rdquo;</strong> the Ghostwriter now weaves that volume in as <em>your</em> credible leverage (&ldquo;the business I do with you&rdquo;) and pre-fills <strong>&ldquo;Your usual / quarter&rdquo;</strong> with your derived pace from your records &mdash; so you&rsquo;re not typing numbers, just confirming them. The unit pace is an estimate (toxin dosing-units aren&rsquo;t vials, and not every line records a quantity), so it&rsquo;s clearly labeled and editable &mdash; you have the final say. <strong>The guardrail holds</strong>: every number is <em>yours</em> &mdash; your spend, your pace &mdash; never a word about what anyone else pays. Trailing 12 months; nothing new is stored. <strong>Touched</strong>: new <code>manufacturer-burn-rate.functions.ts</code> (spend + unit-pace aggregation), <code>rep-proposal-core.ts</code> + <code>rep-dealmaker.functions.ts</code> (spend leverage), <code>programs.tsx</code> (volume line + auto-fill), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.148.0",
    date: "June 2026",
    items: [
      "<strong>v2.148.0 &mdash; The Rep Deal-Maker (slice 1): time the ask, ghostwrite it in your voice.</strong> The published volume ladder isn&rsquo;t the real floor &mdash; the deepest savings live in the off-the-record deals reps cut near a deadline. SmartSpa now institutionalizes the timing half of that game. On <strong>Programs &amp; tiers</strong>, every manufacturer now shows a <strong>Leverage window</strong>: how close the rep&rsquo;s quarter (and, at year-end, their fiscal year) is to closing &mdash; e.g. <em>&ldquo;Q2 closes in 7 days &mdash; your rep is chasing their number right now. Strongest ask window.&rdquo;</em> The badge heats up (open &rarr; warming &rarr; hot &rarr; closing) as the deadline nears. Tap <strong>&ldquo;Draft my Rep ask&rdquo;</strong> and the <strong>Ghostwriter</strong> composes a short, warm, discreet message <em>from you to your rep</em> &mdash; armed with your timing and your own volume commitment (pre-filled from your verified ladder&rsquo;s next break) &mdash; that you review, tweak, and send yourself. <strong>The guardrail is the whole point</strong>: it only ever asks <em>what&rsquo;s possible if you commit</em>, on <em>your</em> leverage &mdash; it never references what any other practice pays, so the confidentiality you promise every rep stays intact. Nothing is saved: SmartSpa is the strategist and the clock, never a party of record &mdash; the deal stays human and off the books (it resurfaces later only as a deeper verified cost when you import your new price). <strong>Touched</strong>: new <code>manufacturer-fiscal-calendar.ts</code> (pure leverage-window engine), new <code>rep-proposal-core.ts</code> (server-only Ghostwriter) + <code>rep-dealmaker.functions.ts</code>, <code>programs.tsx</code> (Leverage window card + draft dialog), <code>changelog.ts</code>. No migration, no new data stored.",
    ],
  },
  {
    version: "v2.147.0",
    date: "June 2026",
    items: [
      "<strong>v2.147.0 &mdash; Buy-optimization: your ladder now tells you how to save.</strong> Once you’ve imported a manufacturer’s real volume ladder (the green “Verified — your real portal ladder” on <strong>Programs &amp; tiers</strong>), SmartSpa now reads it and shows your <strong>next volume break</strong> right under it: e.g. <em>“Next volume break: buy 100+ vials → $379/vial · save $10.00/vial (3% lower).”</em> It finds the smallest quantity bump that drops your per-unit cost and spells out the saving — so a buying decision that used to mean squinting at a tier table is now a one-line nudge. If you’re already on the best rung, it says so (🎉). Uses the per-spa verified ladder you imported, so the numbers are <em>your</em> real costs, not estimates. <strong>Touched</strong>: <code>programs.tsx</code>, <code>changelog.ts</code>. No migration, no backend &mdash; pure read of the ladder you already captured.",
    ],
  },
  {
    version: "v2.146.0",
    date: "June 2026",
    items: [
      "<strong>v2.146.0 &mdash; The 1-click importer: drag once, click anywhere, no install.</strong> The browser-extension route works but nobody’s going to side-load an extension. So: on the <strong>Verified pricing</strong> tab there’s now a <strong>“Import to SmartSpa”</strong> button you <strong>drag to your bookmarks bar once</strong> — your spa token baked right in. After that, on <em>any</em> manufacturer-portal price page (Allergan APP, Galderma ASPIRE, Evolus, anything), just click it: SmartSpa reads the page and drops your prices into the review inbox. No screenshot, no upload, no extension, no password. The clever bits that make it actually work: it reads the page <strong>text</strong> (so no screenshot API needed) and Claude extracts the prices from it (ladders included); and it hands off through the URL <strong>fragment</strong> to a SmartSpa <code>/import</code> page rather than a direct request — so a portal’s security policy can’t block it. Reads only what you’re already logged in to see; no portal credential ever leaves your machine. <strong>Touched</strong>: <code>portal-import-core.ts</code> (<code>extractPortalPricesFromText</code> + <code>ingestPortalImportText</code>), <code>portal-import.functions.ts</code> (<code>createPortalImportFromTextFn</code>, token-authed), new <code>/import</code> bridge route, <code>verified-pricing.tsx</code> (draggable bookmarklet card), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.145.1",
    date: "June 2026",
    items: [
      "<strong>v2.145.1 &mdash; Verified ladder saves even when a product is loosely tagged.</strong> The v2.145.0 ladder persistence keyed strictly off a product’s <code>brand_family</code>, so a loosely-categorized or inline-added product (no family tag) would silently not save its ladder. Now it derives the family from the manufacturer’s program ladder names (matches the family inside the product name, e.g. “Jeuveau® 100U” → “Jeuveau”), so the verified ladder persists on real-world data and lands under the right tier selector. <strong>Touched</strong>: <code>portal-import.functions.ts</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.145.0",
    date: "June 2026",
    items: [
      "<strong>v2.145.0 &mdash; Your real volume ladder, wired into Programs &amp; tiers.</strong> When you confirm a portal import that carried a <strong>volume ladder</strong> (v2.144.0 reads them), SmartSpa now <em>saves your real per-rung prices</em> — and surfaces them on <strong>Programs &amp; tiers</strong>. Under each manufacturer’s volume-tier selector you’ll see a green <strong>“Verified — your real portal ladder”</strong> showing every quantity → $/unit pulled straight from your portal, with your current rung marked <em>← yours</em>. This matters because your real ladder often <strong>beats the generic estimate</strong>: e.g. Evolus showed 100 vials at <strong>$379</strong> and everything below at <strong>$389</strong>, where the built-in estimate had a steeper $399/$419/$449 curve — now you see <em>your</em> numbers, not the book’s. Stored strictly <strong>per-spa</strong> (never in the shared manufacturer program, so one spa’s pricing never leaks into another’s), and it never overwrites a portal-verified cost. Foundation for buy-optimization (“bump to 100 vials, save $10 each”). <strong>Touched</strong>: migration <code>v2_145_0_tenant_verified_ladders</code> (<code>verified_ladders</code> jsonb on <code>tenant_manufacturer_tiers</code>), <code>portal-import.functions.ts</code> (persist on apply, per-tenant merge), <code>refill-catalog-seed.ts</code> (getter returns ladders), <code>programs.tsx</code> (ladder display), Supabase types, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.144.0",
    date: "June 2026",
    items: [
      "<strong>v2.144.0 &mdash; Vision now reads volume-tier ladders.</strong> Some portal pages aren’t a price <em>list</em> — they’re a single product’s <strong>quantity ladder</strong> (e.g. Evolus Jeuveau: 250 / 150 / 100 / 50 / 20 / 1 vials, each at its own per-unit price). Until now vision read those as six unmatchable rows. Now it recognizes a ladder, collapses it to <strong>one matched product</strong> at the spa’s real current per-unit price (the “Your Price” rung), and <strong>captures every rung</strong>. In the review table, a matched ladder shows an expandable <strong>“Volume ladder · N tiers”</strong> with each quantity → $/unit and your current rung marked <em>← yours</em> — so you can see at a glance what the next volume break would save. Confirm applies your current price as a Verified cost, same as any import; the full ladder is captured with the batch for upcoming buy-optimization (writing it into the volume-tier program model is the next step). Normal multi-product price lists are unchanged (one row per product). <strong>Touched</strong>: <code>portal-import-core.ts</code> (extractor + <code>tiers</code> on parsed row/proposal), <code>verified-pricing.tsx</code> (ladder display), <code>changelog.ts</code>. No migration — ladder rides the batch jsonb.",
    ],
  },
  {
    version: "v2.143.1",
    date: "June 2026",
    items: [
      "<strong>v2.143.1 &mdash; Capture extension accepts your token <em>or</em> your drop-address.</strong> The capture endpoint now normalizes the pasted token — strips whitespace and any <code>@rewards.smartspa.app</code> suffix — so whether you paste the bare token (Option B) or the full email drop-address (Option A) out of habit, it just works instead of erroring <code>unknown_token</code>. <strong>Touched</strong>: <code>api.ingest.portal-capture.ts</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.143.0",
    date: "June 2026",
    items: [
      "<strong>v2.143.0 &mdash; Capture Lane (P1): one-click portal capture from your browser.</strong> A 4th way to get portal prices into SmartSpa, alongside upload, email, and the Agent. Install the new <strong>“Capture to SmartSpa”</strong> browser extension, paste your token once, and on any manufacturer-portal “Your Price” page just click it &mdash; it screenshots the page <em>you’re</em> logged into and sends it straight to your <strong>Verified pricing</strong> inbox. No screenshot-save-find-upload, no email. <strong>Credential-free by design:</strong> SmartSpa never logs into the manufacturer and never stores a portal password &mdash; the capture happens in your own authenticated tab, tagged to your spa by the same token the email/Agent lanes use. Backend is a thin new endpoint (<code>/api/ingest/portal-capture</code>) over the <em>existing</em> vision → match → review-inbox engine (~95% reuse); the extension is a small side-loadable Chrome/Edge add-on under <code>capture-extension/</code>. P1 = visible-tab capture, stages as a normal import for review (you still confirm before any cost is written). <strong>Touched</strong>: new route <code>api.ingest.portal-capture.ts</code> + <code>capture-extension/</code> (manifest + popup), <code>changelog.ts</code>. No migration. Realizes the vision-ingestion moat &mdash; see the Capture Lane spec.",
    ],
  },
  {
    version: "v2.142.3",
    date: "June 2026",
    items: [
      "<strong>v2.142.3 &mdash; Emailed screenshots stage exactly once (de-dupe).</strong> Resend can hand the same inbound email to more than one of our webhooks (we saw a forwarded screenshot hit both inbound endpoints), which staged the import <em>twice</em> — two identical batches in your review inbox. Fixed: each emailed batch is now stamped with the email’s message id, with a one-per-message database guard, so a double-delivery collapses to a single batch (the second is recognized and skipped, even when both arrive at the same instant). In-app uploads are unaffected (each is intentional). <strong>Touched</strong>: migration <code>v2_142_3_portal_import_dedup</code> (<code>source_message_id</code> + partial-unique index), <code>portal-import-core.ts</code> (pre-check + race-safe conflict handling), <code>portal-import-email.ts</code>, Supabase types, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.142.2",
    date: "June 2026",
    items: [
      "<strong>v2.142.2 &mdash; Email lane now fires wherever the mail lands.</strong> Live testing surfaced that Resend delivers a forwarded screenshot to the <em>general</em> inbound webhook (<code>/api/resend/inbound</code>, the reply handler), not the rewards endpoint where Ship 3 first put the image lane — so the screenshot hit a dead-end (&ldquo;no router matched&rdquo;). Fixed by extracting the portal email-import into a shared <code>portal-import-email.ts</code> and calling it from <strong>both</strong> inbound webhooks: after the reply routers miss, if the message is addressed to your rewards drop-address <em>and</em> carries an image, it stages a portal-import batch. So a forwarded portal screenshot now works no matter which endpoint Resend routes it to. Never collides with real replies (requires the rewards token + an image). <strong>Touched</strong>: new <code>portal-import-email.ts</code> (shared token-extract + image-fetch + ingest), <code>api.resend.inbound.ts</code> + <code>api.resend.inbound-rewards.ts</code> (both call it), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.142.1",
    date: "June 2026",
    items: [
      "<strong>v2.142.1 &mdash; Email lane: compose buttons that actually open (Gmail / Outlook / Mail app).</strong> The v2.142.0 “Open my email” button used a <code>mailto:</code> link, which only fires if you have a desktop mail app set as your system default &mdash; so webmail users (Gmail or Outlook in the browser) clicked it and nothing happened. Fixed: the card now offers <strong>Compose in: Gmail · Outlook · Mail app</strong> &mdash; Gmail and Outlook open a pre-filled compose <em>tab</em> directly (addressed to your drop-address, subject filled), and Mail app keeps the <code>mailto:</code> path for desktop-client users. Copy address is still there for anything else. <strong>Touched</strong>: <code>verified-pricing.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.142.0",
    date: "June 2026",
    items: [
      "<strong>v2.142.0 &mdash; Email lane, now guided right on the Verified pricing tab.</strong> Ship 3 made email work; this makes it <em>obvious</em>. The <strong>Verified pricing</strong> tab now has an <strong>“Or email it in”</strong> card directly under the upload box, with a numbered 1-2-3 and a one-tap <strong>Open my email</strong> button that opens a new message <em>already addressed to your private SmartSpa drop-address</em> (subject pre-filled too) — you just attach the screenshot and hit send. No hunting for an address on another page, no copy-paste dance. A <strong>Copy address</strong> fallback is right there for phone/forwarding setups, and a note reminds you a forward from any staff inbox works (it’s tied to your spa, not the sender). The address is your existing hands-free auto-import address — so email price screenshots and reward CSVs ride the same private inbox. <strong>Touched</strong>: <code>verified-pricing.tsx</code> (email-lane card + <code>mailto:</code> compose link, reuses <code>getOrCreateRewardIngestToken</code>), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.141.0",
    date: "June 2026",
    items: [
      "<strong>v2.141.0 &mdash; Verified pricing by email: just forward the screenshot (Ship 3 of 3 &mdash; the arc is complete).</strong> You already forward manufacturer emails to your SmartSpa drop-address for reward CSVs. Now that <em>same address</em> also takes <strong>price screenshots</strong>: email (or forward) a manufacturer-portal &ldquo;Your Price&rdquo; screen as an image attachment, and SmartSpa reads it with vision, matches it to your catalog, and stages it in the <strong>Verified pricing</strong> tab&rsquo;s review inbox &mdash; exactly where an in-app upload lands. You open the tab, confirm, done. No app, no login, no upload screen &mdash; the most effortless lane there is. SmartSpa routes by attachment type: a CSV still goes to your rewards/transactions ingest unchanged; an <strong>image</strong> (and only when there&rsquo;s no CSV) goes to portal-import. The token in your drop-address is the only credential (we never trust the sender, so a forward from any staff inbox works), and the subject line sharpens manufacturer matching when it names the brand. Two ingest mouths &mdash; in-app upload + email &mdash; now feed one shared engine and one review inbox. <strong>Touched</strong>: <code>api.resend.inbound-rewards.ts</code> (additive image lane: detect image attachments &rarr; resolve token&rarr;tenant &rarr; <code>ingestPortalImport(source='email')</code>; CSV path byte-for-byte unchanged), <code>changelog.ts</code>. No migration. <em>(Live email delivery needs the rewards inbound address configured in Resend; the lane is testable by POSTing a sample payload until then.)</em>",
    ],
  },
  {
    version: "v2.140.0",
    date: "June 2026",
    items: [
      "<strong>v2.140.0 &mdash; Verified pricing: add an unrecognized product right from the review (Ship 2.1).</strong> When a portal screenshot has a price for something not yet in your catalog, that row used to be a dead-end (&ldquo;add the product first&rdquo;). Now each unmatched price has a <strong>+ Add product</strong> button inline: it opens a tiny form pre-filled with the product name, you pick the two things a screenshot can&rsquo;t tell us (<strong>category</strong> + <strong>unit type</strong>, plus units-per-box if the price is per box, and an optional sales price), and the portal price comes straight in as a <strong>Verified</strong> cost. One click and the unrecognized product is in your catalog, already verified &mdash; no detour to the Products tab. The cost is read server-side from the stored portal price (you can&rsquo;t accidentally type a wrong number), and the per-box &divide; units conversion shows live so you see the per-unit cost before you save. <strong>Touched</strong>: <code>createProductFromPortalRowFn</code> (new server fn, stamps <code>cost_source='portal'</code>) in <code>portal-import.functions.ts</code> + inline add UI in <code>verified-pricing.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.139.0",
    date: "June 2026",
    items: [
      "<strong>v2.139.0 &mdash; Verified pricing: snap your portal, confirm, done (Ship 2 of 3).</strong> Ship 1 built the engine; this is the screen you actually use. New <strong>Verified pricing</strong> tab in the catalog: <strong>upload (or drag) a screenshot of your manufacturer portal&rsquo;s &ldquo;Your Price&rdquo; screen</strong> &mdash; Allergan APP, Galderma ASPIRE, Evolus, up to 8 images at once &mdash; and Claude vision reads the prices and matches them to your catalog. You get a <strong>review table</strong>: each row shows what the screenshot said (price + whether it&rsquo;s per box or per unit), the <strong>per-box &divide; units conversion</strong> spelled out (so a 2-syringe Allergan box can&rsquo;t secretly double your filler cost &mdash; the #1 import trap, now visible for a human eye-check), the per-unit cost we&rsquo;d write, and your current cost beside it. Tick the rows you trust (strong matches start pre-ticked), hit <strong>Verify</strong>, and those products flip from <em>Estimate</em> to the green <strong>Verified</strong> badge. Nothing is ever written without your confirm. Unmatched prices are shown but parked. The tab doubles as a <strong>review inbox</strong> &mdash; pending imports wait there, and Ship 3&rsquo;s email-a-photo lane will land in the same place. <strong>Touched</strong>: new route <code>verified-pricing.tsx</code> (upload + review table) + the catalog tab nav across all four sibling pages; split the vision engine into a server-only <code>portal-import-core.ts</code> (keeps the Anthropic SDK out of the browser bundle, reached via a dynamic import inside the server handlers), <code>changelog.ts</code>. No migration &mdash; rides the v2.138.0 <code>portal_import_batches</code> table.",
    ],
  },
  {
    version: "v2.138.0",
    date: "June 2026",
    items: [
      "<strong>v2.138.0 &mdash; Vision portal-import: the engine (Ship 1 of 3).</strong> Phase 3 boots your catalog with <em>estimate</em> costs; this is the path to flip them to <strong>Verified</strong> by reading your real manufacturer-portal &ldquo;Your Price&rdquo; &mdash; the auth-walled, per-spa numbers no spreadsheet or scraper can reach, only a photo from inside your account. This ship lays the backend: Claude vision reads a portal screenshot into <code>{product, price, unit-basis, sku}</code> rows, fuzzy-matches them to your catalog (SKU first, then name), and stages the proposals for your review &mdash; it <strong>never</strong> writes a cost without your confirm, and it does the per-box &divide; units conversion so a 2-syringe Allergan box doesn&rsquo;t double your filler cost. Confirmed rows become <code>cost_source='portal'</code> (the green Verified badge). Next ships wire the in-app upload screen + a review table (Ship 2) and an email-a-photo lane (Ship 3). <strong>Touched</strong>: new <code>portal-import.functions.ts</code> (extract + match + apply/dismiss server fns) + migration <code>v2_138_0_portal_import_staging</code> (<code>portal_import_batches</code>) + Supabase types. Backend only this ship &mdash; no UI yet.",
    ],
  },
  {
    version: "v2.137.0",
    date: "June 2026",
    items: [
      "<strong>v2.137.0 &mdash; New spas now boot <em>fully loaded</em> (Phase 3).</strong> Until now a new spa started with an empty catalog and had to click <em>Load starter catalog</em> to populate it. Now the moment you claim your URL, <strong>your catalog auto-loads from the manufacturer master book</strong> &mdash; real products, list prices, packaging and treatment areas already filled in (Allergan, Evolus, and more as they come online). The onboarding finish screen now tells you exactly what landed (&ldquo;<strong>We pre-loaded 67 real products</strong>&rdquo;) and points at the one next step: <strong>Pick my tiers</strong> &mdash; choose your loyalty tier with each manufacturer and every cost snaps to <em>your</em> pricing. It&rsquo;s best-effort and safe: if anything hiccups, your spa is still created and the manual <em>Load starter catalog</em> button is always there (it never duplicates). Costs start as estimates until you pick a tier or enter your real portal price. <strong>Touched</strong>: extracted a shared <code>seedTenantCatalog</code> core in <code>refill-catalog-seed.ts</code> (one engine for the button + onboarding), auto-seed wired into <code>claimSlug</code> (<code>refill-tenants.ts</code>, non-fatal), onboarding nudge in <code>onboard.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.136.0",
    date: "June 2026",
    items: [
      "<strong>v2.136.0 &mdash; &ldquo;Set price&rdquo; now reaches the product editor too.</strong> v2.133.0 made unpriced products read <em>&ldquo;Set price&rdquo;</em> instead of a scary negative in the <strong>Products</strong> list and <strong>Brand economics</strong> table &mdash; but one spot slipped through: opening a freshly-loaded product to <strong>edit</strong> it still flashed a green &ldquo;Margin per vial: &minus;$389.00&rdquo; box (cost is real, sell price is $0, so 0 &minus; cost looked like a loss). Now the editor shows a quiet <strong>&ldquo;Set a sales price to see your margin&rdquo;</strong> prompt until you actually price it. Important: this only hides the <em>unpriced</em> case &mdash; if you type a real price that&rsquo;s <em>below</em> cost, you still see the (negative) margin, because that&rsquo;s genuine &ldquo;selling at a loss&rdquo; signal you want. <strong>Touched</strong>: <code>products.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.135.0",
    date: "June 2026",
    items: [
      "<strong>v2.135.0 &mdash; Multi-manufacturer catalog: filter by vendor + know which costs are real (Phase 2.2).</strong> Now that your catalog spans Allergan, Galderma and Evolus, two upgrades on <strong>Products</strong> and <strong>Brand economics</strong>: <strong>(1) Manufacturer filter</strong> &mdash; chips to isolate one vendor&rsquo;s products on demand. The default view stays mixed and margin-ranked (so you can still compare Juvéderm vs Restylane vs Evolysse side-by-side in a category) &mdash; click <em>Galderma</em> or <em>Evolus</em> to focus, click again to clear. <strong>(2) Estimate-vs-verified badges</strong> &mdash; every cost now shows where it came from: <strong>Verified</strong> (your real portal &ldquo;Your Price&rdquo;), <strong>Estimate</strong> (computed from your loyalty tier &mdash; a nudge to confirm against your portal), or <strong>Set cost</strong> (a placeholder to fill in). Owner-entered costs stay clean with no badge. So you can trust the margin math at a glance and see exactly which numbers are real vs. estimated. <strong>Touched</strong>: new <code>CostSourceBadge.tsx</code>, <code>products.tsx</code>, <code>brand-economics.tsx</code> + <code>cost_source</code> threaded through <code>refill-catalog.ts</code> / <code>brand-economics.ts</code> / <code>refill-brand-economics.functions.ts</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.134.0",
    date: "June 2026",
    items: [
      "<strong>v2.134.0 &mdash; Evolus joins the catalog (a third pricing shape).</strong> Two manufacturers already ship loaded with very different loyalty math &mdash; Allergan&rsquo;s APP is a <em>formula</em> (portfolio × brand tier), Galderma&rsquo;s ASPIRE is a <em>price table</em>. <strong>Evolus is a third shape: a volume ladder</strong> &mdash; buy more cumulative units, unlock a lower price <em>per unit</em>. Each product carries its own ladder; you just sit at a tier. New on <strong>Programs &amp; tiers</strong>: an Evolus card where you pick your volume tier for <strong>Jeuveau</strong> (tox) and <strong>Evolysse</strong> (filler) and the cost snaps to that rung (e.g. Jeuveau Tier&nbsp;2 → $389/vial). <strong>Jeuveau</strong> and <strong>Evolysse</strong> added to the starter catalog. Resolver gained a <code>volume_tier</code> branch (reads the product&rsquo;s ladder at your tier &mdash; an absolute $/unit, not a % off list); estimate-priced rows only, verified portal costs untouched. Evolysse&rsquo;s treatment-area map isn&rsquo;t captured yet (forward the face-map and it joins cross-manufacturer filler substitution). <strong>Touched</strong>: <code>manufacturer-tier-resolver.ts</code>, <code>programs.tsx</code>, <code>changelog.ts</code> + migration <code>v2_134_0_evolus_volume_tier</code>.",
    ],
  },
  {
    version: "v2.133.0",
    date: "June 2026",
    items: [
      "<strong>v2.133.0 &mdash; Unpriced reads &ldquo;Set price,&rdquo; not a scary negative.</strong> A freshly-loaded product has a real <em>cost</em> but no <em>sell price</em> yet (you set that) &mdash; so margin = $0 − cost came out <em>negative</em> and looked broken. Now any product with no sell price shows a quiet <strong>&ldquo;Set price&rdquo;</strong> chip (Brand economics + Products) instead of a red negative number &mdash; unpriced ≠ unprofitable. Also tidied the Allergan tier selectors to a sensible order (<strong>Hylacross → Vycross → Skincare → Surgery</strong>; Postgres jsonb had alphabetized them). <strong>Touched</strong>: <code>brand-economics.tsx</code>, <code>products.tsx</code>, <code>programs.tsx</code>, <code>changelog.ts</code>. No migration. Phase 2 polish following the proven seed→tier end-to-end on demo.",
    ],
  },
  {
    version: "v2.132.0",
    date: "June 2026",
    items: [
      "<strong>v2.132.0 &mdash; Ships fully loaded (Phase 2: pick your tier, costs snap).</strong> New <strong>Programs &amp; tiers</strong> sub-tab in the Catalog. <strong>Load starter catalog</strong> boots your catalog fully populated with the manufacturer book (real products, list prices, packaging, treatment areas) instead of a blank slate &mdash; idempotent, never duplicates or overwrites a verified cost. Then per-manufacturer cards: <strong>Allergan</strong> &mdash; choose your <em>Portfolio Level</em> + <em>Brand Tiers</em> (Hylacross / Vycross / Skincare / Surgery) and hit <strong>Apply</strong>; the catalog cost layer re-resolves (<code>list × (1 − brandTier%) × (1 − portfolio%)</code>) for your estimate-priced products only &mdash; your real verified portal costs stay put. Galderma / Merz / Evolus / Revance show &ldquo;coming soon &mdash; forward your price list&rdquo; (the snap-a-photo growth hook). Estimates are clearly framed as estimates until your real &ldquo;Your Price&rdquo; overrides them. <strong>Touched</strong>: new <code>app.refill.catalog.programs.tsx</code> + 4th tab on services/products/brand-economics, <code>changelog.ts</code>. Wires the v2.131 seed engine. No migration.",
    ],
  },
  {
    version: "v2.131.0",
    date: "June 2026",
    items: [
      "<strong>v2.131.0 &mdash; Ships fully loaded (Phase 1b: the seed engine).</strong> The server logic that turns the master catalog into a tenant&rsquo;s living catalog. <code>seedTenantCatalogFromMaster</code> copies the master book into a spa&rsquo;s products, computing each cost from their selected loyalty tier (via the v2.130 resolver) &mdash; <strong>idempotent</strong> (reconciles by <code>catalog_ref_id</code>, never duplicates) and it <strong>never clobbers a real cost</strong> (only touches <code>tier_estimate</code>/<code>unset</code> rows, leaving <code>portal</code>/<code>manual</code> truth intact). <code>applyTenantTierSelection</code> is &ldquo;pick your tier &rarr; costs snap&rdquo;: saves the selection and re-resolves just the estimate rows. Plus reads for the tier-selector UI (<code>getManufacturerPrograms</code>, <code>getTenantTierSelections</code>). Backend only &mdash; the select-your-tier surface is Phase 2. <strong>Touched</strong>: new <code>refill-catalog-seed.ts</code>, <code>types.ts</code> (3 tables + 6 product cols), <code>changelog.ts</code>. No migration (uses v2.130).",
    ],
  },
  {
    version: "v2.130.0",
    date: "June 2026",
    items: [
      "<strong>v2.130.0 &mdash; Ships fully loaded (Phase 1: the master catalog).</strong> Backend substrate so a spa can boot into a <em>fully-populated</em> catalog with real manufacturer products + real numbers, then just pick their loyalty tier and the cost layer snaps to their economics &mdash; with their real portal &ldquo;Your Price&rdquo; overriding the estimate later. Ships three cross-tenant reference tables (<code>manufacturer_programs</code>, <code>manufacturer_catalog</code>, <code>tenant_manufacturer_tiers</code>) + 6 columns on <code>products</code> (<code>list_price</code>, <code>brand_family</code>, <code>sku</code>, <code>units_per_box</code>, <code>cost_source</code>, <code>catalog_ref_id</code>). Seeds the <strong>full Allergan Advantage book</strong> (~65 SKUs &mdash; BOTOX, the whole Juvéderm line in both fills, Kybella, the SkinMedica wall, Latisse, Diamond Glow), captured via vision, with packaging + brand-family + face-map sub-categories; plus the complete <strong>Allergan Partner Privileges</strong> tier matrix. Galderma / Merz / Evolus / Revance seeded as &ldquo;coming soon&rdquo; shells. New pure resolver <code>manufacturer-tier-resolver.ts</code> computes <code>cost = list × (1 − brandTier%) × (1 − portfolio%)</code> (verified 7/7). <strong>Multi-manufacturer but segregated</strong> &mdash; one catalog, programs never bleed. <strong>Touched</strong>: new migration <code>v2_130_0_manufacturer_master_catalog.sql</code>, <code>manufacturer-tier-resolver.ts</code>, <code>changelog.ts</code>. <strong>Migration required.</strong> Phase 2 (seed-into-tenant + select-your-tier UI) next.",
    ],
  },
  {
    version: "v2.129.0",
    date: "June 2026",
    items: [
      "<strong>v2.129.0 &mdash; A new treatment area, found by the brochure.</strong> Added <strong>&ldquo;Undereyes / Tear trough&rdquo;</strong> to the sub-category picklist &mdash; the one filler injection area your taxonomy was missing. It surfaced the way the best data does: Allergan&rsquo;s own Juvéderm treatment map lists <em>undereye hollows</em> as Volbella&rsquo;s lead indication, but there was no bucket for it, so the substitution recommender couldn&rsquo;t group on it. Now it can. One canonical value (both names on one chip, since they&rsquo;re used interchangeably &mdash; and a single bucket so undereye products actually compete with each other). Tag it on the Products tab like any other area; it feeds <code>Filler · Undereyes / Tear trough</code> recommendations on the patient profile. <strong>Touched</strong>: <code>product-subcategories.ts</code> (one canonical value), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.128.0",
    date: "June 2026",
    items: [
      "<strong>v2.128.0 &mdash; Deal-aware samples: cake, and eating it.</strong> A Sample now reads two ways and <em>you pick per entry</em> by filling one optional field &mdash; <strong>&ldquo;…on an order of N&rdquo;</strong>. <strong>Fill it</strong> (the real deal, e.g. buy 10 get 2 free) and SmartSpa drops your <em>effective per-unit cost</em> (cost &times; paid ÷ (paid+free)) and shows it as a genuine <strong>per-vial margin lift</strong> that flows into Margin now <em>and</em> the recommender. <strong>Leave it blank</strong> and it&rsquo;s one-time <strong>free stock</strong> &mdash; shown as its own 🎁 chip on the brand, never faking a per-unit number. The math reads honestly either way: per-unit levers (rebates + deal-samples) feed Margin now; lump freebies sit in their own lane. Live preview shows exactly which lens you&rsquo;re in as you type; fully editable. The flexible thing nobody ships because &ldquo;it&rsquo;s hard.&rdquo; <strong>Touched</strong>: <code>brand-economics.ts</code> + <code>refill-brand-economics.functions.ts</code> (paidUnits on the entry), Brand economics form (order field + effective-cost math + live two-lens preview) and table (per-unit vs free-stock split), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.127.0",
    date: "June 2026",
    items: [
      "<strong>v2.127.0 &mdash; Click a live incentive to edit it.</strong> In the Brand economics table, the existing values in the <em>Active incentive</em> and <em>Patient feels</em> columns (e.g. Botox&rsquo;s &ldquo;+$1,220 flat&rdquo;) are now clickable &mdash; click one and its incentive opens in the form ready to edit, same as the &ldquo;+ Add&rdquo; on empty cells. So every cell in those two columns is now actionable: add when empty, edit when filled. <strong>Touched</strong>: Brand economics panel (filled cells link to their ledger entry via <code>activeIncentives</code>), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.126.0",
    date: "June 2026",
    items: [
      "<strong>v2.126.0 &mdash; Sample incentives are editable.</strong> A Sample incentive now remembers the <em>free-unit count + unit</em> (not just the computed dollars), so hitting <strong>Edit</strong> reopens it pre-filled (&ldquo;2 free vials&rdquo;) and re-values live as you change the count &mdash; or as the brand&rsquo;s cost changes. <strong>Touched</strong>: <code>brand-economics.ts</code> (freeUnits/sampleUnit on the entry), <code>refill-brand-economics.functions.ts</code> (store + hydrate them), Brand economics form (repopulate on edit), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.125.0",
    date: "June 2026",
    items: [
      "<strong>v2.125.0 &mdash; Samples in vials/syringes, valued automatically.</strong> Add a <strong>Sample (free units)</strong> incentive the way you actually get them: pick <strong>Vial</strong> or <strong>Syringe</strong> (those two on top) and enter <em>how many are free</em> &mdash; SmartSpa computes the dollar value from that brand&rsquo;s cost (e.g. 2 free vials of a $790/vial brand = <strong>$1,580</strong> to you) and books it as a margin-side incentive. No manual math, and you see the computed value live as you type. (If the brand has no cost set yet, it nudges you to set it on the Products tab first.) <strong>Touched</strong>: Brand economics incentive form (Sample &rarr; unit + free-unit count, auto-$ from catalog cost), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.124.0",
    date: "June 2026",
    items: [
      "<strong>v2.124.0 &mdash; Brand economics follows your product order.</strong> The Brand economics table now lists brands within a category in the <em>same manual order</em> you set by dragging on the Products page (not auto-sorted by margin) &mdash; so the two screens read identically, top to bottom. You don&rsquo;t lose the margin view: the <strong>BEST MARGIN</strong> badge now marks the actual highest-margin brand <em>wherever it sits</em> in your order. (The recommender re-ranks internally, so this is purely the visual order &mdash; recommendations are unaffected.) <strong>Touched</strong>: <code>brand-economics.ts</code> (order by sort_order, carry it on the snapshot), <code>refill-brand-economics.functions.ts</code> (load sort_order), Brand economics panel (best-margin badge by value, not position), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.123.0",
    date: "June 2026",
    items: [
      "<strong>v2.123.0 &mdash; Airtight substitution + cost reads in real units.</strong> Three sharpenings. <strong>(1) The recommender now substitutes within SUB-CATEGORY</strong>, not the whole Filler category &mdash; Biostim competes only with Biostim, Mid-face HA with Mid-face HA, etc. So a patient profile shows a recommendation per filler area (&ldquo;Filler &middot; Mid-face &rarr; Volux,&rdquo; &ldquo;Filler &middot; Biostim &rarr; Radiesse&rdquo;), and it will never suggest swapping a collagen biostimulator for a hyaluronic filler. A product tagged for several areas competes in each. <strong>(2) Cost now reads in the real unit</strong> &mdash; $/vial, $/syringe (from the product&rsquo;s unit type) across Brand economics and the product form labels, instead of a generic &ldquo;/u.&rdquo; <strong>(3) Brand economics category order</strong> now matches Services / Products / the booking page (the one shared category order). <strong>Touched</strong>: <code>brand-economics.ts</code> (unitType + subcategories on the snapshot), <code>refill-brand-economics.functions.ts</code> (substitution-group recommender), patient profile card, Brand economics (unit display + shared category order), product form (unit-aware labels), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.122.0",
    date: "June 2026",
    items: [
      "<strong>v2.122.0 &mdash; Catalog fine-tuning: defined sub-categories, one-click incentives, biostim under Filler.</strong> Three sharpenings. <strong>(1) Sub-categories are now a defined checklist</strong> instead of free text &mdash; multi-select chips: Mid-face / NLF / Lips / Liplines / Fine-lines / Marionettes / Jawline / Chin / Biostim (a product can carry several). Lives on the product form and the canonical registry; pick the ones that apply and they inherit on Recategorize. <strong>(2) One-click incentives</strong>: the Brand economics table now has a <strong>&ldquo;+ Add&rdquo;</strong> right inside each row&rsquo;s <em>Active incentive</em> and <em>Patient feels</em> cells &mdash; click it and the form opens pre-filled with that brand + the right beneficiary, so you only type the amount and dates. <strong>(3) Biostim is now a Filler sub-category</strong>, not its own top-level category (clinically it&rsquo;s a filler-family distinction) &mdash; existing biostim products + canonical brands (Sculptra, Radiesse) re-filed to Filler with a &ldquo;Biostim&rdquo; sub-category tag. <strong>Touched</strong>: migration <code>v2_122_0_biostim_to_filler_subcat</code>, new <code>product-subcategories.ts</code>, product + canonical forms (multi-select), Brand economics (inline +Add), recommender mapping (biostim&rarr;Filler), <code>changelog.ts</code>. <strong>Run the migration.</strong>",
    ],
  },
  {
    version: "v2.121.0",
    date: "June 2026",
    items: [
      "<strong>v2.121.0 &mdash; The Recommender: &ldquo;for THIS patient, use THIS brand&rdquo; (Pillar 2.2).</strong> The piece everything was building toward. Each patient profile now carries a <strong>Brand recommendations</strong> card that makes the chairside call <em>right</em>, not just greedy &mdash; by reading their value tier against live brand economics. A <strong>Top-value, reliable</strong> patient &rarr; the <em>premium</em> brand they trust, even at thinner margin (lifetime value outweighs one visit; it even tells you the per-unit margin you&rsquo;re trading and why). A <strong>Core</strong> patient &rarr; the high-margin clinically-equivalent <em>substitute</em> + the value-feel they experience as a great price. A <strong>new</strong> patient &rarr; an acquisition play that earns the second visit. A <strong>Watch</strong> patient (chronic no-show / discount-only) &rarr; protect margin, don&rsquo;t burn premium incentive. It recommends per injectable category the patient actually gets (tox / filler / biostimulator), reading <code>margin_now</code> + patient value-feel straight from the catalog &times; the live incentive ledger. <strong>Internal only &mdash; never shown to the patient.</strong> This closes the 3-pillar Patient-Profitability OS: value tiering (1) feeds brand economics (2.1) feeds the recommender (2.2). <strong>Touched</strong>: <code>brand-recommender.ts</code> (new pure lib), <code>refill-brand-economics.functions.ts</code> (<code>getPatientBrandRecommendations</code> + shared economics loader), <code>brand-economics.ts</code> (price on the snapshot), patient profile card, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.120.0",
    date: "June 2026",
    items: [
      "<strong>v2.120.0 &mdash; Drag to reorder products within a category (full Services parity).</strong> Products now have the same <strong>grab-and-drag</strong> handle Services do &mdash; reorder brands within a category and the order sticks (persisted to a new <code>sort_order</code>, lower first; unset rows fall back to brand-alphabetical until you drag them). Combined with v2.119.0&rsquo;s shared category order, the Product catalog and Service catalog now order identically top to bottom. <strong>Touched</strong>: migration <code>v2_120_0_product_sort_order</code> (nullable <code>sort_order</code> on products), <code>refill-catalog.ts</code> (<code>reorderProductsFn</code> + list orders by sort_order, Product type), product list (drag handle + within-category reorder, mirrors the services list), generated DB types, <code>changelog.ts</code>. <strong>Run the migration.</strong>",
    ],
  },
  {
    version: "v2.119.0",
    date: "June 2026",
    items: [
      "<strong>v2.119.0 &mdash; Products page now orders categories like Services (and Bookable).</strong> The Product catalog groups by category in the <em>same sequence</em> as your Service catalog &mdash; both read from the one shared category order (the same one the public booking page uses), so the three surfaces finally line up instead of each sorting its own way. Also fixed the category filter chips to include the new <strong>Biostimulator</strong> and <strong>Facial</strong> groups (they were stuck on the old five). <strong>Touched</strong>: <code>app.refill.catalog.products.tsx</code> (loads <code>getCategoryOrderFn</code>, sorts groups by <code>orderedCategoryRank</code>, filter chips from the full category list), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.118.0",
    date: "June 2026",
    items: [
      "<strong>v2.118.0 &mdash; Clean up duplicates (and a reconcile fix).</strong> Two parts. <strong>Fix</strong>: v2.117.0&rsquo;s &ldquo;Link products&rdquo; created one product per <em>duplicate service</em> &mdash; so if you had &ldquo;CO2 Fractional&rdquo; three times, it made three CO2 products. It now de-dupes <em>within a run</em> (one product per brand, no matter how many services point at it). <strong>Cleanup</strong>: a new <strong>Clean up duplicates</strong> button on the Service catalog finds duplicate services <em>and</em> duplicate products (same name/brand), collapses each group to one row &mdash; the most complete (priced first) &mdash; and <strong>re-points any product links onto the survivor so no service loses its COGS</strong>. It clears services first (so the link state settles), then products. Preview-first: it shows every group it&rsquo;ll collapse and removes nothing until you confirm; the read is fully paginated. <strong>Touched</strong>: <code>refill-catalog.ts</code> (within-run reconcile de-dupe + <code>previewCatalogDedupeFn</code>/<code>commitCatalogDedupeFn</code> + shared <code>deleteServiceWithCleanup</code> + link re-point), <code>app.refill.catalog.services.tsx</code> (Clean up duplicates button + preview panel), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.117.0",
    date: "June 2026",
    items: [
      "<strong>v2.117.0 &mdash; Link products: stop re-creating what you already entered.</strong> You set up Services to feed bookables, and many of them are the same brands as your Products &mdash; so a new <strong>Link products</strong> button on the Service catalog matches services to products <em>by brand</em> (via the canonical registry) and wires them together, so each service <strong>derives its COGS automatically</strong> from product cost &times; quantity. Where a service names a real brand but has <em>no</em> product yet, it <strong>auto-creates the product stub</strong> (brand / category / manufacturer filled in &mdash; you just set cost &amp; price once) and links it. <strong>Preview-first</strong>, exactly like the CSV import: it shows every proposed link + create, and nothing is written until you confirm. Quantity defaults to 1; tox / unit-variable services are flagged <strong>&#9873; set units</strong> so you can drop in the real unit count after (it never clobbers a COGS you set manually). Services with no brand match (consults, facials) are skipped. <strong>Touched</strong>: <code>refill-catalog.ts</code> (<code>previewServiceProductReconcileFn</code> + <code>commitServiceProductReconcileFn</code>, idempotent, recomputes server-side), <code>app.refill.catalog.services.tsx</code> (Link products button + preview panel), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.116.0",
    date: "June 2026",
    items: [
      "<strong>v2.116.0 &mdash; Canonical brand registry: full CRUD on the sub-categories too.</strong> Follow-on to v2.115.0 so the &ldquo;source of truth&rdquo; is editable end-to-end. The admin canonical-brands editor (<code>/app/admin/canonical-brands</code>) now exposes the new <strong>area</strong> + <strong>family</strong> sub-category fields and the <strong>Biostimulator</strong> category &mdash; so you can author the shipping defaults <em>once</em> on the registry and every tenant&rsquo;s products inherit them on Recategorize (fills blanks only, never clobbers an explicit choice). Closes the last CRUD gap from the catalog-foundation work: products, services, the incentive ledger, AND the canonical registry are now fully create/edit/delete across every field. <strong>Touched</strong>: <code>refill-catalog.ts</code> (brand payload + create/update write area/family), <code>app.admin.canonical-brands.tsx</code> (Biostimulator option + Area/Family inputs), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.115.0",
    date: "June 2026",
    items: [
      "<strong>v2.115.0 &mdash; Catalog foundation: biostimulators get their own shelf + filler sub-categories.</strong> Now that the intelligence groups substitutable brands <em>by category</em> (Brand Economics &rarr; the coming recommender), the catalog taxonomy is load-bearing &mdash; and it had a gap. <strong>Biostimulator is now a first-class category</strong> (Sculptra, Radiesse) instead of being mis-filed as filler &mdash; you don&rsquo;t swap a collagen biostimulator for an HA filler, and the recommender won&rsquo;t either. Added <strong>Facial</strong> too, so brands stop dumping into &ldquo;Other.&rdquo; Products also gain two <strong>optional</strong> sub-category fields &mdash; <strong>area</strong> (the primary substitution group, e.g. cheek / lip / jawline) and <strong>family</strong> (a secondary refiner) &mdash; so fillers substitute <em>within the area you&rsquo;re actually treating</em>. They&rsquo;re free-text and optional; seed the defaults once on the canonical brand registry and every product inherits them on the <strong>Recategorize</strong> sweep. <strong>Touched</strong>: migration <code>v2_115_0_catalog_biostim_subcat</code> (category CHECK + <code>subcategory_area</code>/<code>subcategory_family</code> columns on products + canonical_brands, re-points Sculptra/Radiesse &rarr; biostimulator), <code>refill-catalog.ts</code> (enums, sweep inherits sub-cats), product form (Biostimulator/Facial + Area/Family fields), Brand Economics labels, generated DB types, <code>changelog.ts</code>. <strong>Run the migration, then hit Recategorize.</strong>",
    ],
  },
  {
    version: "v2.114.0",
    date: "June 2026",
    items: [
      "<strong>v2.114.0 &mdash; Brand Economics: the hidden lever, made visible (Pillar 2.1).</strong> The first piece of the Brand-Substitution Margin Optimizer. A new <strong>Brand economics</strong> tab under Catalog shows, per category, what each brand <em>actually nets you right now</em> &mdash; your catalog margin (price &minus; cost) plus any <strong>active manufacturer incentive</strong> &mdash; ranked so the high-margin substitute is obvious at a glance (e.g. &ldquo;Jeuveau $8.50/u vs Botox $6.00/u this month&rdquo;), with the patient&rsquo;s <em>value-feel</em> (vouchers / rewards they experience as a better price) in its own column. You maintain the new <strong>Manufacturer Incentive Ledger</strong> with a structured form &mdash; per brand: rebate / sample / voucher / patient-reward / instant, who it accrues to (you = lifts margin-now, or the patient = value-feel), amount, per-unit vs flat, and a date window so a promo that ends next month stops counting automatically. This is the data spine the upcoming tier-aware recommender reads (top-tier patient &rarr; the premium brand for LTV; price-sensitive &rarr; the high-margin substitute + a price they love). Reuses the existing products catalog margin + canonical brand map; the &ldquo;type it in plain English&rdquo; entry is a fast-follow. <strong>Internal only &mdash; margin-now is YOUR margin, never the patient&rsquo;s view, and unrelated to SmartSpa&rsquo;s $5.</strong> <strong>Also</strong>: hardened value tiering to skip non-patient catch-all buckets (e.g. a QuickBooks &ldquo;Unassigned&rdquo; row that was ranking as a fake top-tier whale) &mdash; excluded from the percentile and de-tiered. <strong>Touched</strong>: <code>brand-economics.ts</code> (new pure lib), <code>refill-brand-economics.functions.ts</code> (new ledger + economics fns), <code>app.refill.catalog.brand-economics.tsx</code> (new route) + Catalog sub-nav, <code>patient-value.ts</code> + <code>refill-patient-value.functions.ts</code> (non-patient guard), <code>changelog.ts</code>. No migration (ledger rides <code>knowledge_nodes</code> jsonb).",
    ],
  },
  {
    version: "v2.113.0",
    date: "June 2026",
    items: [
      "<strong>v2.113.0 &mdash; Patient Value Tiering: know who your best patients are.</strong> The first pillar of the Patient-Profitability OS. Every patient is now scored on <strong>two independent axes</strong>, ranked within <em>your own</em> book (never against other spas): <strong>Value</strong> &mdash; an RFM blend of recency, visit frequency &amp; spend, percentile-ranked into <em>Top&nbsp;20%</em> / <em>Core</em> / <em>New</em>; and <strong>Reliability</strong> &mdash; a behavior flag (<em>Watch</em> for chronic no-show/cancel or discount-only), kept separate from value so a brand-new high-intent patient reads as an opportunity, not &ldquo;low value.&rdquo; New patients land <em>New</em>, never low. It&rsquo;s <strong>internal only</strong> &mdash; &ldquo;Watch,&rdquo; not &ldquo;bad,&rdquo; and never shown to the patient. See it as a badge on the patient list &amp; profile, filter the book by tier, and hit <strong>Recompute tiers</strong> to refresh the whole book at once. Best part: it plugs straight into the offer composer&rsquo;s <strong>Refine targeting</strong> &mdash; aim an offer at your Top&nbsp;20%, and the live &ldquo;~N match&rdquo; count, the push audience and the $5 win-integrity gate all honor it identically. Reuses the same signals the recognition engine &amp; A-list rules already tier on (no third system), and rides <code>knowledge_nodes.attachments</code> jsonb &mdash; <strong>no migration</strong>, and the tier survives sales-CSV re-imports. <strong>Touched</strong>: <code>patient-value.ts</code> (new pure lib), <code>refill-patient-value.functions.ts</code> (new whole-book recompute), <code>patient-csv.ts</code> + <code>patient-ingest.functions.ts</code> (storage + re-roll preservation), <code>promo-calendar.ts</code> + <code>refill-promo-calendar.functions.ts</code> (valueTier refine filter), <code>OfferComposer.tsx</code>, patient list + profile routes, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.112.0",
    date: "June 2026",
    items: [
      "<strong>v2.112.0 &mdash; Your spa&rsquo;s name in the live preview.</strong> The composer&rsquo;s Email and AI-text previews now show your real spa name instead of the &ldquo;Your spa&rdquo; placeholder &mdash; so the patient-facing artifact you&rsquo;re previewing reads exactly as it&rsquo;ll send (&ldquo;Hi Jordan! It&rsquo;s <em>Rejuv Skin Spa</em> &mdash; $50 off your next Botox&hellip;&rdquo;). Pulled from your spa profile (<code>getSpaProfileBundleFn</code>), with a neutral fallback until it loads. Small polish toward full mockup parity. <strong>Touched</strong>: <code>OfferComposer.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.111.0",
    date: "June 2026",
    items: [
      "<strong>v2.111.0 &mdash; Refine targeting, completed: &ldquo;has had Tox but never Filler.&rdquo;</strong> The composer&rsquo;s Refine section gains the cross-sell axis &mdash; two chip rows, <strong>Has had</strong> and <strong>&hellip;but never had</strong>, over Tox / Filler / Device / Retail. So the classic upsell &mdash; <em>&ldquo;patients who&rsquo;ve had Botox but never tried filler&rdquo;</em> &mdash; is two taps, AND-combined with the recency / visits / spend / reachable filters from v2.110. Purchase history reads from <code>patient_transactions</code> (real purchases, amount &gt; 0), only loaded when a bought/not-bought filter is set (kept cheap by scanning just the relevant kinds, fully paginated). It flows through the <em>same</em> resolver as everything else, so the live &ldquo;~N match&rdquo; count, the push audience, and the <strong>$5 win-integrity gate</strong> all honor it identically &mdash; a refined offer still only earns when the booking patient actually matches. No migration (rides the existing <code>filter_spec</code> jsonb). <strong>Touched</strong>: <code>promo-calendar.ts</code> (boughtKinds/notBoughtKinds on the spec + predicate + <code>needsPurchaseHistory</code>), <code>refill-promo-calendar.functions.ts</code> (<code>loadPurchasedKinds</code> + resolver/gate wiring, paginated), <code>OfferComposer.tsx</code> (Has had / never had chip rows), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.110.0",
    date: "June 2026",
    items: [
      "<strong>v2.110.0 &mdash; Refine targeting: narrow any audience to exactly the patients you want.</strong> The composer&rsquo;s new <strong>Refine targeting</strong> section layers optional filters on top of whoever you picked in <em>Who gets it</em> &mdash; <strong>not seen in N+ days</strong>, <strong>at least N visits</strong>, <strong>spent at least $X</strong>, and <strong>reachable by text</strong> &mdash; all AND-combined. So &ldquo;Lapsed who spent $500+&rdquo; or &ldquo;Everyone not seen in 90+ days, reachable by text&rdquo; just work. The live <strong>&ldquo;~N match&rdquo;</strong> count re-counts as you type, reading the <em>same</em> resolver the push uses, so the number is exactly who you&rsquo;ll reach. Adding any filter makes the offer <strong>targeted</strong>: it stops badging publicly and drafts to your inbox to send, and &mdash; critically &mdash; the <strong>$5 only records when the booking patient actually matches the filter</strong>, so the number never lies. Filters read each patient&rsquo;s visit-history &amp; spend rollup; &ldquo;bought X but not Y&rdquo; is the next pass. <strong>Requires a one-line migration</strong> (adds the offer <code>filter_spec</code> jsonb) &mdash; applied separately. <strong>Touched</strong>: <code>promo-calendar.ts</code> (OfferTargetFilters type + pure <code>patientMatchesFilters</code> predicate + badge exclusion), <code>refill-promo-calendar.functions.ts</code> (refined resolver reused by count/push/$5-gate + createSpaOffer threading), <code>OfferComposer.tsx</code> (Refine section + live count), migration <code>v2_110_0_offer_target_filters.sql</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.109.0",
    date: "June 2026",
    items: [
      "<strong>v2.109.0 &mdash; The crown jewel: test a manufacturer&rsquo;s promo head-to-head against your own offer, judged impartially by real bookings.</strong> A new <strong>Test head-to-head</strong> tab in the composer opens a <em>neutral arena</em>: a manufacturer promo and your own offer(s) enter as <strong>equal A/B arms</strong> on the same trigger service, shown to the same audience, and SmartSpa keeps whichever your customers actually <strong>book</strong> more. SmartSpa takes <strong>no side</strong> &mdash; if the manufacturer&rsquo;s $75-off wins, great; if your $50-off out-books it, that&rsquo;s the impartial proof, and the pressure on them to do better. Pick the service the arms compete on, stack any mix of <em>Your offer</em> (type + terms) and <em>Manufacturer promo</em> (terms locked &#128274;, only promos matching the service show), set the shared who/when, and start the arena. The winner is chosen by the same bandit the spa-only A/B already uses. Architecturally clean: a manufacturer arm is written <code>source='spa'</code> (so a promo-calendar re-import&rsquo;s delete-and-replace can never orphan the experiment) but carries <code>manufacturer</code> forward, so the pooled result is attributable to the maker in <code>getArbiterVerdicts</code> &mdash; the sellable impartial-arbiter signal, for free. <strong>Touched</strong>: <code>smart-ab.functions.ts</code> (optional <code>manufacturer</code> on the variant schema, threaded into the arm row), <code>OfferComposer.tsx</code> (new <code>OfferArena</code> component + third mode tab), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.108.0",
    date: "June 2026",
    items: [
      "<strong>v2.108.0 &mdash; Save an offer as a draft.</strong> The composer&rsquo;s action bar now has <strong>Save as draft</strong> alongside Activate (for a single spa offer). It saves the offer <em>paused</em> &mdash; it won&rsquo;t badge, list, or push until you flip it on from <em>Your offers</em> &mdash; so you can compose now and launch later, or park a half-formed idea without losing it. <code>createSpaOffer</code> gained an <code>isActive</code> flag (defaults true; draft = false), reusing the pause/resume the offers list already has. <strong>Touched</strong>: <code>refill-promo-calendar.functions.ts</code> (isActive on createSpaOffer), <code>OfferComposer.tsx</code> (Save-as-draft button), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.107.0",
    date: "June 2026",
    items: [
      "<strong>v2.107.0 &mdash; Two more audiences in the composer: VIP / A-list and Waitlist.</strong> The <em>Who gets it</em> picker grows from four cohorts to six &mdash; you can now target an offer at your <strong>VIP / A-list</strong> (the patients you&rsquo;ve starred, plus anyone your A-list rules mark) or your opted-in <strong>Waitlist</strong>, alongside Everyone / Lapsed / New / Expiring-reward. Both resolve through the <strong>same cohort engine the push path uses</strong> (opt-out-filtered), so they get the live &ldquo;~N match&rdquo; count for free and reach patients the same trusted way (draft-to-inbox). VIP reads each patient&rsquo;s star flag; Waitlist reads opted-in (<code>status=active</code>) waitlist entries and hydrates their contact. Like any targeted cohort, these don&rsquo;t badge publicly &mdash; they&rsquo;re pushed, and earn the $5 only when an in-cohort patient books. <strong>Requires a one-line migration</strong> (widens the offer <code>target_cohort</code> CHECK constraint) &mdash; applied separately. <strong>Touched</strong>: <code>promo-calendar.ts</code> (OfferCohort type), <code>refill-promo-calendar.functions.ts</code> (vip + waitlist resolvers in listOfferCohortTargets, 3 cohort enums), <code>smart-ab.functions.ts</code> (A/B cohort enum), <code>OfferComposer.tsx</code> + <code>app.refill.recognition.rewards.tsx</code> (cohort pickers), migration <code>v2_107_0_vip_waitlist_cohorts.sql</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.106.0",
    date: "June 2026",
    items: [
      "<strong>v2.106.0 &mdash; The composer counts your patients live: &ldquo;~31 match.&rdquo;</strong> Pick an audience in the composer&rsquo;s <em>Who gets it</em> section and SmartSpa now tells you, in real time, how many patients that is &mdash; a pulsing <strong>&ldquo;~31 match&rdquo;</strong> chip in the section header plus a plain-English line (<em>&ldquo;~31 lapsed patients match · 27 reachable by text&rdquo;</em>). Switch to Lapsed, New, or Expiring-reward and the number re-counts; pick Everyone and it shows your whole book (<em>&ldquo;1,240 patients in your book &mdash; they&rsquo;ll see this at booking&rdquo;</em>). The count comes from the <strong>same cohort engine the push path uses</strong> (<code>listOfferCohortTargets</code>, opt-out-filtered), so &ldquo;match&rdquo; means exactly what it&rsquo;ll mean when you send &mdash; not an estimate. This is the &ldquo;system does the math&rdquo; half of the agentic-native vision: you state the intent, SmartSpa shows you the reach before you commit. <strong>Touched</strong>: <code>refill-promo-calendar.functions.ts</code> (new <code>getCohortReachFn</code>), <code>OfferComposer.tsx</code> (live match pill + line + Section badge slot), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.105.0",
    date: "June 2026",
    items: [
      "<strong>v2.105.0 &mdash; The composer gets its Delivery keystone: where it shows &amp; how it reaches patients.</strong> The unified Offer Composer gains the section the mockup calls the keystone, and it reflects exactly how delivery actually works &mdash; no overpromising. For an <strong>Everyone</strong> offer it&rsquo;s <em>Show it</em>: a booking-page badge (always on &mdash; that&rsquo;s what a public offer does) plus a real <strong>Public Deals page</strong> toggle (now wired through for spa offers too, not just manufacturer promos &mdash; honored by the public Deals list). For a <strong>targeted</strong> cohort it&rsquo;s <em>Push it</em>: the live &ldquo;You send &mdash; draft to my inbox&rdquo; mode (SmartSpa writes a message per patient, you tap send in your own blue bubble) with &ldquo;SmartSpa auto-sends&rdquo; shown as <em>coming soon</em>, plus honest guardrail chips (opt-outs &amp; no-phone excluded · you review before send · every booking attributed back). The plain-English hero and the section&rsquo;s resting summary update live with the toggle. Works in manufacturer-delivery mode too (the Deals toggle maps to the promo&rsquo;s <code>show_on_deals</code>). <strong>Touched</strong>: <code>OfferComposer.tsx</code> (Delivery section + ToggleLine/SpectrumItem), <code>refill-promo-calendar.functions.ts</code> (<code>createSpaOffer</code> now accepts <code>showOnDeals</code>), <code>changelog.ts</code>. No migration (column existed).",
    ],
  },
  {
    version: "v2.104.0",
    date: "June 2026",
    items: [
      "<strong>v2.104.0 &mdash; Offers get their own home.</strong> The unified Offer Composer (and its two result lists, &ldquo;Your offers&rdquo; and &ldquo;Your tests&rdquo;) were buried at the bottom of the Rewards tab, sharing the page with manufacturer reward-tracking, the attribution queue, and CSV imports. They now have a <strong>dedicated Promos → Offers tab</strong> (<code>/app/refill/recognition/offers</code>) where the composer is the hero of its own page &mdash; author your own offer or deliver a manufacturer promo, single or A/B, with the live patient-facing preview right there. The Rewards tab keeps what it&rsquo;s actually about: manufacturer reward signals, attribution review, and imports. This is the first structural step toward the agentic-native authoring vision &mdash; one clear surface for &ldquo;create an offer.&rdquo; <strong>Touched</strong>: new <code>app.refill.recognition.offers.tsx</code>, <code>RecognitionTabs.tsx</code> (new tab), <code>app.refill.recognition.rewards.tsx</code> (cards moved out), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.103.0",
    date: "June 2026",
    items: [
      "<strong>v2.103.0 &mdash; Manufacturer promos now flow through the one composer too.</strong> The unified Offer Composer gains a second mode &mdash; a tab pair appears at the top the moment you have any manufacturer promos loaded: <strong>Author an offer</strong> (your own) and <strong>Deliver a manufacturer promo</strong>. In manufacturer mode the <strong>terms are locked</strong> &mdash; the discount, product, and dates come straight from the manufacturer&rsquo;s calendar and render read-only with a &ldquo;Terms set by Allergan&rdquo; lock chip &mdash; and you author only the <strong>delivery</strong>: who it&rsquo;s for (cohort), when it runs (weekday recurrence + cap), all with the same plain-English hero and live patient-facing preview (badge / Deals / email / SMS) as a spa offer. Pick a promo and the controls prefill from its current delivery; Activate writes only the delivery columns (<code>updateManufacturerOfferDelivery</code>), so the manufacturer&rsquo;s terms are never touched and your settings survive the next calendar re-import. This is the <strong>hybrid model</strong> &mdash; manufacturer owns the terms, you own the delivery &mdash; finally living in the one agentic-native authoring surface instead of a separate inline editor. <em>(The crown-jewel &ldquo;run a manufacturer promo against your own offer as an A/B&rdquo; is the next ship.)</em> <strong>Touched</strong>: <code>OfferComposer.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.102.0",
    date: "June 2026",
    items: [
      "<strong>v2.102.0 &mdash; The composer gets a live preview: watch the offer become real as you build it.</strong> The unified Offer Composer now shows the patient-facing artifact updating in real time, with a four-way toggle: <strong>Badge</strong> (the at-booking row — service + offer chip + struck → discounted price, computed from the real catalog price), <strong>Deals</strong> (the public Deals card), <strong>Email</strong> (the win-back email mock), and <strong>AI text</strong> (the personalized SMS draft). It reflects every choice live &mdash; pick &ldquo;20% off Filler for lapsed&rdquo; and the badge shows the strike-through, the targeting note flips to &ldquo;not shown at public booking,&rdquo; and the SMS gains a &ldquo;just for you.&rdquo; When you&rsquo;re running an A/B it notes the preview shows the base version while the bandit tests all of them. This is the WYSIWYG half of the agentic-native vision &mdash; plain-English intent up top, the real artifact down here, the math hidden in between. <em>(Spa-name personalization in the preview copy is a trivial follow-up; uses a neutral placeholder for now.)</em> <strong>Touched</strong>: <code>OfferComposer.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.101.0",
    date: "June 2026",
    items: [
      "<strong>v2.101.0 &mdash; Catalog hygiene: provider bake-ins cleaned out of every service picker.</strong> Many spas (esp. solo Acuity setups) weld the provider into the catalog label &mdash; &ldquo;Tox w/ Karen&rdquo;, &ldquo;Filler with Karen&rdquo; &mdash; which made the offer + A/B + composer service dropdowns read messy. Now <code>listServicesFn</code> returns a provider-cleaned <code>displayName</code> alongside the raw <code>name</code>: it loads the tenant&rsquo;s provider names and strips a trailing <em>connector + known provider name</em> (&ldquo;Tox w/ Karen&rdquo; → &ldquo;Tox&rdquo;) for display. <strong>Precision over recall</strong> &mdash; only a KNOWN provider/owner name is stripped, so &ldquo;Facial with Dermaplane&rdquo; or &ldquo;Botox / Dysport&rdquo; pass through untouched. The three pickers (the unified composer, &ldquo;Your offers&rdquo;, &ldquo;Your tests&rdquo;) + the composer&rsquo;s plain-English summary now show the clean label; the option&rsquo;s <strong>value stays the raw name</strong> so matching, badging, and the $5 win path are byte-identical. <strong>Clean-on-read, never mutate the source</strong> &mdash; re-typing the catalog isn&rsquo;t the owner&rsquo;s job (same philosophy as the v1.26.15 rescue-label strip; that logic is now a shared <code>stripProviderSuffix</code> util). Best-effort: a tenant with no providers configured just sees names unchanged. <strong>Note</strong>: genuinely mis-entered rows (e.g. a stray &ldquo;Lyft&rdquo;) aren&rsquo;t provider bake-ins and stay as-is &mdash; those need an owner rename/hide in the catalog (a future cleanup tool). <strong>Touched</strong>: <code>service-categories.ts</code> (new <code>stripProviderSuffix</code>), <code>refill-catalog.ts</code> (<code>displayName</code> on Service + provider load in <code>listServicesFn</code>), <code>OfferComposer.tsx</code> / <code>SmartAbCard.tsx</code> / <code>SpaOffersCard.tsx</code> (display cleaned label), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.100.0",
    date: "June 2026",
    items: [
      "<strong>v2.100.0 &mdash; The unified Offer Composer: one agentic-native surface to author an offer (single OR A/B).</strong> The marquee UX consolidation. There were two separate create forms &mdash; &ldquo;new offer&rdquo; (cross/up-sell) and &ldquo;new test&rdquo; (Smart-A/B). They&rsquo;re now ONE composer that leads with plain-English intent: a live summary at the top reads back your offer as you build it (&ldquo;<em>$50 off Tox for lapsed — Tue, cap 20/wk — sent to your matching patients</em>&rdquo;), with the controls tucked behind one expander each (KISS additive-on-demand). <strong>A single offer and an A/B are the same intent here</strong>: flip &ldquo;Let SmartSpa find your best version&rdquo; and add a version &mdash; the base offer becomes version 1 and the alternates differ only in terms. On Activate, no extra versions → one offer (<code>createSpaOffer</code>); one+ versions → an A/B (<code>createAbExperiment</code>, the impartial bandit picks the winner by real bookings). Cohort, schedule (weekday recurrence + cap + dates), and the resulting delivery surfaces (badge + Deals for Everyone; cohort push otherwise) are shared across versions, shown honestly. The two old cards keep their <strong>result lists</strong> &mdash; &ldquo;Your offers&rdquo; (pause/resume/delete/push) and &ldquo;Your tests&rdquo; (simulate/push/verdict) &mdash; with their create forms gated off (<code>hideCreate</code>), and refresh the moment the composer creates something. Same DNA as the original agentic platform: plain-English intent, the system does the math &mdash; the differentiator vs form-based incumbents. <strong>New</strong>: <code>OfferComposer.tsx</code>. <strong>Touched</strong>: <code>SpaOffersCard.tsx</code> + <code>SmartAbCard.tsx</code> (hideCreate + refreshKey), <code>app.refill.recognition.rewards.tsx</code>, <code>changelog.ts</code>. No migration. <em>Next passes: a richer live preview (badge/Deals/email tabs) + folding manufacturer-promo delivery into the same composer.</em>",
    ],
  },
  {
    version: "v2.99.0",
    date: "June 2026",
    items: [
      "<strong>v2.99.0 &mdash; The Impartial Arbiter: anonymized cross-spa promo-performance verdict (admin, internal-first).</strong> The crown-jewel data product the whole consolidation was building toward. New admin surface <code>/app/admin/arbiter</code> (&ldquo;Arbiter&rdquo; chip) pools <em>every</em> Smart-A/B arm across <em>all</em> spas by product + offer structure, and lets the same impartial bandit say which structure books best &mdash; rendered manufacturer-facing: &ldquo;<em>Botox — which offer books best? Allergan · across N spas · X patients reached. $65 off wins — it books 1.7× more than the runner-up. 92% confidence.</em>&rdquo; with a bar per structure (booked-rate + P(best)) and the impartiality note (&ldquo;SmartSpa didn&rsquo;t choose, the owner didn&rsquo;t choose, patients chose by booking&rdquo;). <strong>Anonymized</strong> (Grasshopper Q3=yes): spa identity is masked to a <em>count</em> (&ldquo;across N spas&rdquo;), no patient data &mdash; per-spa noise becomes a manufacturer-grade signal. <strong>Internal-first</strong> (Q2): this is the pitch-deck / partnership-leverage asset; a manufacturer-facing dashboard would build on the same pooled read later. New <code>getArbiterVerdicts</code> server fn (admin-gated via <code>requireAdmin</code>, paginated cross-tenant read, groups by product→structure, dominant-manufacturer + distinct-spa count, runs <code>computeVerdict</code> per product). A product appears once ≥2 structures have run as variants. <strong>This completes the manufacturer-promo consolidation arc</strong> (v2.92 move → v2.93 hybrid fold → v2.94 rep loop → v2.95–v2.98 Smart-A/B engine+UI+live loop → v2.99 arbiter). <strong>New</strong>: <code>app.admin.arbiter.tsx</code>, <code>getArbiterVerdicts</code>. <strong>Touched</strong>: <code>smart-ab.functions.ts</code>, <code>AdminNav.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.98.0",
    date: "June 2026",
    items: [
      "<strong>v2.98.0 &mdash; Smart-A/B live loop: real bookings now drive the bandit.</strong> The simulator showed the loop; this wires it to reality. <strong>Assignment half</strong>: a new <strong>Push test</strong> button on a running, cohort-targeted experiment drafts it to the cohort &mdash; each patient is assigned ONE version by Thompson sampling (sticky, so they always see the same one), an impression is counted for that version, and the per-patient message carries THEIR version. Same draft-first / opt-out-safe / human-gated path as offer push (batch emailed to the spa&rsquo;s proxy inbox for review + iMessage send). <strong>Booking half</strong>: when a booking is attributed to a patient, SmartSpa credits a conversion (the &ldquo;vote&rdquo;) to the exact version that patient was assigned &mdash; sticky + counted once per patient (a second booking can&rsquo;t double-vote). Hooked into the existing cross-sell/recovery attribution, fully additive + best-effort: it never touches the $5 decision or the booking flow. As votes land, <code>getAbVerdict</code> shifts reach toward the front-runner and auto-decides the winner. <strong>An &lsquo;Everyone&rsquo; test can&rsquo;t be pushed</strong> (it runs on the public booking page, where versions can&rsquo;t be split per patient) &mdash; the Push button only shows for cohort tests, with a clear message otherwise. <strong>Robustness</strong>: the re-push read of existing assignments is paginated (a lapsed cohort runs to ~2,000 &mdash; a fixed read would silently re-assign the overflow); conversion crediting uses atomic <code>bump_ab_conversion</code> / <code>bump_ab_impression</code> RPCs so concurrent bookings can&rsquo;t lose increments. <strong>New</strong>: <code>offer_experiment_assignments</code> table + the two RPCs (migration), <code>smart-ab-conversion.ts</code>, <code>draftExperimentPush</code>. <strong>Touched</strong>: <code>smart-ab.functions.ts</code>, <code>refill-promo-calendar.functions.ts</code> (export push helpers + best-effort conversion hook), <code>SmartAbCard.tsx</code>, <code>changelog.ts</code>. Migration applied separately.",
    ],
  },
  {
    version: "v2.97.0",
    date: "June 2026",
    items: [
      "<strong>v2.97.0 &mdash; Offer service pickers group by category (categories · services · brands flow).</strong> The &ldquo;Service to test&rdquo; (Smart-A/B) and &ldquo;Service&rdquo; (Create offer) dropdowns were a flat alphabetical dump &mdash; CoolSculpting, Defyne, HIFU&nbsp;NECK, BBL&nbsp;-Chest all in one ungrouped run. They&rsquo;re now fed from the bookable catalog and grouped into <code>&lt;optgroup&gt;</code> buckets by category in canonical order (Tox ▸ … / Filler ▸ Defyne, Refyne, Restylane… / Laser ▸ HIFU, BBL… / Facial / Skincare / Other, then any custom categories), sorted by the catalog&rsquo;s own sort-order then name within each bucket &mdash; so categories, individual services, and brand-named services all read in a sensible hierarchy. Shared pure helper <code>groupServicesByCategory</code> (in <code>service-categories.ts</code>) so the two pickers can&rsquo;t drift. <strong>Touched</strong>: <code>service-categories.ts</code>, <code>SmartAbCard.tsx</code>, <code>SpaOffersCard.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.96.0",
    date: "June 2026",
    items: [
      "<strong>v2.96.0 &mdash; Smart-A/B is visible: the &ldquo;Let SmartSpa find your best version&rdquo; composer + the verdict view.</strong> v2.95.0 landed the bandit engine; this puts a face on it. New <strong>SmartAbCard</strong> on the Rewards offers surface: pick a service, drop in 2&ndash;4 versions that differ only in the offer (e.g. <em>$50 off</em> vs <em>20% off</em> vs <em>free brow wax</em>), pick who it&rsquo;s for, and start the test. Each running test renders the <strong>verdict</strong> live &mdash; a trophy line (&ldquo;<em>$50 off is ahead &mdash; it books 1.7&times; more, 71% likely the best</em>&rdquo;), then a bar per version showing its booking rate, <em>n&nbsp;booked&nbsp;/&nbsp;sent</em>, and the bandit&rsquo;s <em>P(this is best)</em>. When the bandit clears its confidence threshold it flips to <strong>decided</strong>, names the winner, and pauses the losing arms &mdash; SmartSpa never asks you to &ldquo;declare a winner.&rdquo; The framing is the crown jewel: <strong>real bookings decide, the AI measures, SmartSpa just reports</strong> &mdash; the impartiality that makes this data sellable to manufacturers. <strong>Real bookings populate it via the push/booking path (next slice)</strong>; until that&rsquo;s wired, a clearly-synthetic <strong>&ldquo;Simulate bookings&rdquo;</strong> button (mirrors the proven Simulate-a-cancellation tool, no live data touched) Thompson-allocates 40 synthetic rounds so you can watch reach shift toward the front-runner and see the verdict get called end-to-end. <strong>(API used)</strong>: <code>createAbExperiment</code> / <code>getAbVerdict</code> (auto-decides) / <code>listAbExperiments</code> + new <code>simulateAbRound</code> (synthetic, owner-safe). <strong>Next</strong>: assign arms on the push path + count a real booking as that arm&rsquo;s conversion (the live loop), then the manufacturer-facing impartial-arbiter view. <strong>New</strong>: <code>SmartAbCard.tsx</code>. <strong>Touched</strong>: <code>smart-ab.functions.ts</code> (simulator), <code>app.refill.recognition.rewards.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.95.0",
    date: "June 2026",
    items: [
      "<strong>v2.95.0 &mdash; Smart-A/B engine core: the impartial-arbiter bandit (crown-jewel mechanism).</strong> The heart of the thesis &mdash; when an offer runs as several variants, the winner is chosen by <strong>real customer bookings</strong> (&ldquo;customers vote&rdquo;) measured by an impartial AI bandit. Not SmartSpa choosing, not the owner, not the manufacturer: the market votes, the AI measures, the result is objective &mdash; and <em>that impartiality</em> is what makes the promo-performance verdict sellable to manufacturers. This ship lands the engine CORE (the math + data model + API); wiring it into delivery + the verdict UI is the next slice. <strong>(1) The bandit</strong> (<code>src/lib/smart-ab.ts</code>, pure + deterministic + validated): each variant is a Beta-Bernoulli posterior over its booking rate; <code>thompsonPickArm</code> assigns the next send by sampling the posteriors (shifts reach toward the front-runner while still exploring, so almost no reach is spent on a loser); <code>computeVerdict</code> Monte-Carlos the posteriors to get <em>P(this variant is best)</em>, the lift vs the runner-up, and an auto-stop once the winner clears a confidence threshold with enough sample. Sanity-checked against the mockup&rsquo;s own numbers (9/38 vs 4/31 vs 3/22 &rarr; &ldquo;$50 off, ~1.7&times; more, 71% confident&rdquo;; a 30/80-vs-8/80 split &rarr; 100%, auto-stops). Seedable RNG so a verdict is reproducible from the same counts (no UI jitter). <strong>(2) Data model</strong> (migration, applied): an experiment is a set of offer rows sharing an <code>experiment_id</code> &mdash; each row one arm; per-arm <code>ab_impressions</code> / <code>ab_conversions</code> / <code>ab_label</code> counters on the row, experiment-level status/winner in a new <code>offer_experiments</code> table; an atomic <code>bump_ab_outcome</code> RPC so concurrent sends/bookings can&rsquo;t lose increments. <strong>(3) API</strong> (<code>smart-ab.functions.ts</code>): <code>createAbExperiment</code> (one trigger service + shared targeting, the variants differ only in terms), <code>getAbVerdict</code> (computes + <strong>auto-decides</strong>: locks the winner + pauses losing arms once the bandit is confident), <code>recordAbOutcome</code> (the seam the push-assignment + booking attribution call next), <code>listAbExperiments</code>. <strong>Next</strong>: assign arms on the push path + count a booking as that arm&rsquo;s conversion, then the &ldquo;Let SmartSpa find your best version&rdquo; composer panel + the &ldquo;see the numbers&rdquo; verdict view. <strong>New</strong>: <code>src/lib/smart-ab.ts</code>, <code>src/server/smart-ab.functions.ts</code>, migration <code>v2_95_0_smart_ab.sql</code>. tsc 164, truncation 188.",
    ],
  },
  {
    version: "v2.94.0",
    date: "June 2026",
    items: [
      "<strong>v2.94.0 &mdash; Rep loop folds into the Rewards offers surface (built + gated dark).</strong> Step 3 of the manufacturer-surface consolidation. The standalone <strong>Brand Promos</strong> page (manufacturer offers you&rsquo;re eligible for &middot; the [VERIFIED] tier ladder &middot; &ldquo;I&rsquo;m interested &rarr; ping my rep&rdquo; + dismiss) is retired into a single <strong>Brand promos for you</strong> card on the unified Rewards surface, sitting alongside the manufacturer-promo calendar + your own offers. <strong>Why it ships dark</strong>: the eligible promos come from the rep-side CRM (the Liz / openagenticv4 stack), which isn&rsquo;t feeding SmartSpa standalone yet &mdash; so for now the matched list is empty. Rather than show an always-empty surface, the whole loop is gated behind a new <code>rep_loop_enabled</code> feature flag (catalog default <strong>OFF</strong>); flip it ON globally or per-tenant at <code>/app/admin/flags</code> &mdash; no deploy &mdash; once a rep pipeline exists. <strong>This is the first consumer of the feature-flag read path</strong> the catalog always anticipated: new <code>getRepLoopEnabled</code> resolves global-OR-tenant (mirrors the concierge-mode read), default OFF. <strong>(1)</strong> New <code>rep_loop_enabled</code> catalog entry. <strong>(2)</strong> <code>getRepLoopEnabled</code> server fn (loose-typed feature_flags read, viewAs-honored). <strong>(3)</strong> New <code>RepPromosCard.tsx</code> &mdash; the eligible-promo list + interest/dismiss/tier-ladder, repackaged as a self-contained Rewards card; mounted on Rewards only when the flag is ON. <strong>(4)</strong> The <strong>Brand Promos</strong> tab removed from the Recognition tab strip; the old <code>/recognition/brand-promos</code> route now <strong>302-redirects to Rewards</strong> so any bookmark / email link still resolves. The spa-side server fns (<code>listEligiblePromosForSpa</code> / <code>expressInterestInPromo</code> / <code>dismissPromoForSpa</code>) are unchanged and back the card. <strong>Next</strong>: the Smart-A/B engine (the impartial-arbiter mechanism), then the manufacturer-facing arbiter view. <strong>New</strong>: <code>RepPromosCard.tsx</code>. <strong>Touched</strong>: <code>feature-flag-catalog.ts</code>, <code>refill-promos.ts</code>, <code>RecognitionTabs.tsx</code>, <code>app.refill.recognition.brand-promos.tsx</code> (&rarr; redirect), <code>app.refill.recognition.rewards.tsx</code>, <code>changelog.ts</code>. No migration &mdash; reuses the existing feature_flags table.",
    ],
  },
  {
    version: "v2.93.0",
    date: "June 2026",
    items: [
      "<strong>v2.93.0 &mdash; Manufacturer promos fold into the unified offer engine (hybrid model: lock terms, author delivery).</strong> Step 2 of the manufacturer-surface consolidation. Manufacturer promos and your own spa offers already share one table (<code>manufacturer_promo_offers</code>, <code>source='manufacturer'</code> vs <code>'spa'</code>); this ship makes a manufacturer promo a first-class, <em>deliverable</em> offer instead of a read-only calendar line. The model is <strong>&ldquo;lock terms, author delivery&rdquo;</strong>: the promo&rsquo;s TERMS stay the manufacturer&rsquo;s (the discount, product, dates &mdash; read-only, straight from the calendar), but YOU now choose how it&rsquo;s delivered at your spa &mdash; <strong>who</strong> it&rsquo;s for (everyone / lapsed / new / expiring-reward cohort), <strong>when</strong> it runs (weekday recurrence + redemption cap), and <strong>where</strong> it shows (Deals page + pause). <strong>(1) The keystone fix</strong> &mdash; calendar import was a delete-and-replace that silently wiped any owner setting on re-import (even today&rsquo;s On-Deals toggle). The ingest now <em>preserves</em> your delivery overrides across re-import, re-attaching them to each promo by a stable natural key (manufacturer + product + title + window + amount); change the manufacturer&rsquo;s terms and it correctly reads as a new promo and resets. Runtime counters (redeemed count) reset per fresh snapshot, by design. <strong>(2) New write path</strong> <code>updateManufacturerOfferDelivery</code> &mdash; writes ONLY the delivery columns (cohort, weekdays, cap, Deals visibility, pause), scoped to <code>source='manufacturer'</code>; terms are never mutable here. <strong>(3) Delivery editor</strong> on the Manufacturer-promos card: each promo shows a plain-English &ldquo;Delivers to Everyone &middot; every day &middot; on Deals&rdquo; summary with a <strong>Delivery</strong> expander to set who/when; a targeted promo drops off public booking and earns the $5 only for in-cohort patients, exactly like a spa offer. <strong>(4)</strong> The cohort-push loader is now source-agnostic, so a manufacturer promo you&rsquo;ve targeted to a cohort can be pushed the same way a spa offer can. <strong>Next</strong>: migrate the <code>/brand-promos</code> rep-interest loop onto the unified card (behind a toggle), then the Smart-A/B engine + the impartial-arbiter manufacturer data view. <strong>Touched</strong>: <code>refill-promo-calendar.functions.ts</code> (re-ingest preservation + <code>mfrOfferKey</code> + <code>updateManufacturerOfferDelivery</code> + source-agnostic push loader), <code>app.refill.recognition.rewards.tsx</code> (delivery editor), <code>changelog.ts</code>. Also fixed a dead link left by v2.92.0&rsquo;s route move &mdash; <code>allocation.tsx</code> still pointed &ldquo;Manufacturer profiles&rdquo; at the old <code>/recognition/manufacturers</code> path (now <code>/settings/manufacturers</code>). No migration &mdash; the delivery columns already existed on the shared table.",
    ],
  },
  {
    version: "v2.92.0",
    date: "June 2026",
    items: [
      "<strong>v2.92.0 &mdash; Manufacturers moved out of Recognition, under Account/Settings (manufacturer-surface consolidation, step 1).</strong> There were three &ldquo;manufacturer&rdquo;-flavored surfaces under Recognition causing confusion. The <strong>Manufacturers</strong> tab isn&rsquo;t actually a promos surface &mdash; it&rsquo;s a manufacturer <em>loyalty-tier-structure editor</em> (paste an Allē/ASPIRE program doc &rarr; AI extracts the rebate tiers &rarr; review + save), built to feed the Recognition-Allocation rebate engine (which isn&rsquo;t wired yet). That&rsquo;s a different altitude from time-bound patient promos, so it doesn&rsquo;t belong in the promos area. Moved <strong>as-is</strong> (zero functional change) from <code>/app/refill/recognition/manufacturers</code> to <code>/app/refill/settings/manufacturers</code> &mdash; added to the Settings tab strip (with a Factory icon), removed from the Recognition tab strip. Preserved so we don&rsquo;t lose a good idea (a single dashboard of every manufacturer&rsquo;s tier structure); we&rsquo;ll wire it to feed something later, or leave it as a reference. The real promo consolidation (folding manufacturer promos into the unified offer engine + the impartial Smart-A/B arbiter) comes next. <strong>Touched</strong>: route renamed <code>app.refill.recognition.manufacturers.tsx</code> &rarr; <code>app.refill.settings.manufacturers.tsx</code> (path + nav-strip swap), <code>SettingsTabStrip.tsx</code>, <code>RecognitionTabs.tsx</code>, <code>changelog.ts</code>. No migration, no data change.",
    ],
  },
  {
    version: "v2.91.0",
    date: "June 2026",
    items: [
      "<strong>v2.91.0 &mdash; Cutover close-out: email senders + app-host move to smartspa.app.</strong> Two finishers on the front-door cutover. <strong>(1) Transactional from-addresses → @smartspa.app</strong> &mdash; <code>karen@</code> (drips, booking, reminders, outreach), <code>recall@</code>, <code>offers@</code>, <code>recognition@</code>, and the <code>hello@</code> sender now send from the smartspa.app domain (verified for sending, so SPF/DKIM/DMARC pass cleanly). Deliberate timing: the low-volume test phase is the <em>ideal</em> warming window &mdash; you build a sending domain's reputation by ramping gently from near-zero, so switching now (before there's volume to protect) starts the clock the safe way. Replies are unaffected &mdash; they already route via the <code>reply+&lt;token&gt;@smartspa.app</code> Reply-To. <strong>The support address <code>gethelp@getrefill.app</code> was deliberately left put</strong> &mdash; it's human-monitored, and smartspa.app's catch-all receiving would route a flipped contact address into the inbound webhook (lost mail). <strong>(2) App-host → smartspa.app apex</strong> &mdash; <code>REFILL_APP_DEFAULT</code> moved off <code>app.getrefill.app</code>, so onboarding + magic-link redirects land users on smartspa.app (the <code>.smartspa.app</code> cookie scope from v2.89.0 carries the session). <strong>(3) Branding sweep</strong> &mdash; <code>og:image</code> social-preview assets, the trial-drip email footer, and the story-page CTA all now read smartspa.app; the ICS booking-invite UID domain too. <strong>Left for deferred item #4</strong>: the <code>{slug}.getrefill.app</code> vanity-URL display strings (still gated on standing up a <code>*.smartspa.app</code> wildcard &mdash; flipping them early would show 404 URLs). getrefill.app keeps serving + 302-redirecting. <strong>Touched</strong>: ~14 server files (from-addresses), <code>refill-auth.ts</code>, <code>__root.tsx</code> + rescue/waitlist routes (og:image), <code>refill-drip-shell.ts</code>, <code>story.tsx</code>, <code>s.$slug.tsx</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.90.0",
    date: "June 2026",
    items: [
      "<strong>v2.90.0 &mdash; getrefill.app now redirects to smartspa.app (surgically).</strong> The front-door cutover&rsquo;s visible finish: a browser that loads any page on the <code>getrefill.app</code> (or <code>www.getrefill.app</code>) apex is redirected to the same path on <code>smartspa.app</code>. <strong>Deliberately surgical</strong> &mdash; a blanket redirect would have broken live non-browser traffic that still targets getrefill.app, so the worker only bounces a request when ALL of these hold: it&rsquo;s a <code>GET</code>/<code>HEAD</code>, on the bare apex/www (not <code>app.getrefill.app</code> or other subdomains &mdash; the cross-host app bridge stays put), a top-level <strong>navigation</strong> (<code>Sec-Fetch-Mode: navigate</code> / <code>Accept: text/html</code>, so asset + data + server-fn fetches pass through), and <strong>not</strong> under <code>/api/*</code>. That last exclusion is load-bearing: the local-agent <code>/api/agent/heartbeat</code> POSTs, Resend/Twilio webhooks, and any API client on getrefill.app keep working untouched (those clients don&rsquo;t follow redirects). Uses <strong>302 (temporary)</strong> with <code>Cache-Control: no-store</code> during the transition so it&rsquo;s fully reversible &mdash; upgrade to 301 once smartspa.app has settled. Old booking/email links (<code>getrefill.app/s/&lt;slug&gt;</code>) now land on <code>smartspa.app/s/&lt;slug&gt;</code> with the path preserved; newly generated links already point at smartspa.app directly (v2.89.0). <strong>Touched</strong>: <code>src/worker.ts</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.89.0",
    date: "June 2026",
    items: [
      "<strong>v2.89.0 &mdash; Front-door cutover to smartspa.app (phase 1): generated links + session cookies move over.</strong> smartspa.app has long served the same worker as a custom domain; this turns it into the real front door. <strong>(1) Public-link origin flipped</strong>: <code>REFILL_PUBLIC_ORIGIN</code> &rarr; <code>https://smartspa.app</code>, plus all 14 of its <code>?? \"https://getrefill.app\"</code> fallback defaults across 10 files (admin password-reset, reschedule, connection-health, waitlist, rep-platform, promo-calendar, rescue, recognition-allocation, recall-digest + patient-export crons) &mdash; so every booking (<code>/s/&lt;slug&gt;</code>), reset, offer-push and recall link the app generates now points at smartspa.app (which serves the same app). <strong>(2) smartspa.app is now a first-class cookie domain</strong>: both cookie helpers (<code>cookie-storage.ts</code> auth-storage + <code>product-context.ts</code> <code>cookieDomainFor</code>) were host-pinned to <code>.getrefill.app</code> &mdash; the latter returned it <em>unconditionally</em>, which the browser silently rejects on smartspa.app (and on workers.dev/preview), so init/onboarding cookies never set there. They now scope to <code>.smartspa.app</code> on smartspa.app, keep <code>.getrefill.app</code> on getrefill.app, and fall back to host-only off our apexes &mdash; fixing session persistence on the new front door. <strong>getrefill.app keeps serving in parallel</strong> &mdash; every existing booking/email link still resolves; nothing is cut off. <strong>Deliberately deferred</strong> (separate care): the <code>app.getrefill.app</code> app-host migration, the <code>{slug}.getrefill.app</code> vanity-URL display strings (gated on a <code>*.smartspa.app</code> wildcard), the <code>og:image</code>, and from-addresses (<code>karen@</code>/<code>recall@</code>/<code>offers@getrefill.app</code> &mdash; deferred sending-domain warming). <strong>One external finish step</strong> (gates clean auth redirects): Supabase &rarr; Auth &rarr; URL Configuration &mdash; set Site URL to smartspa.app + add it to Redirect URLs, else reset/magic-link redirects fall back to the current Site URL domain. <strong>Touched</strong>: <code>wrangler.jsonc</code>, the 10 fallback files, <code>cookie-storage.ts</code>, <code>product-context.ts</code>, <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.88.0",
    date: "June 2026",
    items: [
      "<strong>v2.88.0 &mdash; Inbound replies go live on smartspa.app: the reply loop is fully connected.</strong> v2.87.0 shipped the catcher (the <code>/api/resend/inbound</code> route); this ship plugs in the domain and turns it on. <strong>(1) Receiving enabled</strong> on the (already sending-verified) <code>smartspa.app</code> Resend domain &mdash; the inbound MX (<code>inbound-smtp.us-east-1.amazonaws.com</code>, priority 10) was written straight into Cloudflare via Resend's Cloudflare auto-configure (no API token, no manual DNS), and confirmed live on two independent resolvers. <strong>(2) Webhook</strong> wired: Resend's <code>email.received</code> event now POSTs to <code>https://smartspa.app/api/resend/inbound</code>. <strong>(3) Signature enforced</strong>: the webhook signing secret is set as the worker secret <code>RESEND_INBOUND_SIGNING_SECRET</code> &mdash; verified by an unsigned probe returning <code>401 bad_signature</code> while a properly-shaped event routes cleanly. <strong>(4) Reply domain flipped</strong>: <code>REFILL_DRIP_REPLY_DOMAIN</code> &rarr; <code>smartspa.app</code>, which a single env read swings across all three touchpoints at once &mdash; outbound drip Reply-To, outbound outreach Reply-To, and inbound <code>parseReplyToToken</code> &mdash; so a spa owner or prospect who hits reply lands as <code>reply+&lt;eventId&gt;@smartspa.app</code>, gets received by Resend, posted to our route, and stamped onto the originating engagement row. <strong>From-addresses stay on getrefill.app for now</strong> (deferred sending-domain warming &mdash; Reply-To can differ from From without deliverability cost). First brick of the broader getrefill.app&rarr;smartspa.app front-door cutover, which is already ~80% done (smartspa.app has long served the same worker as a custom domain). <strong>Touched</strong>: <code>wrangler.jsonc</code> (the env flip), <code>changelog.ts</code>. No code change &mdash; the pipeline was already built. No migration.",
    ],
  },
  {
    version: "v2.87.0",
    date: "June 2026",
    items: [
      "<strong>v2.87.0 &mdash; Inbound email replies are wired: the catcher's mitt for the reply pipeline.</strong> The SEND half has been live for ages &mdash; every Refill trial-drip and prospect-outreach email already goes out with a plus-addressed Reply-To (<code>reply+&lt;eventId&gt;@reply.getrefill.app</code>), and the engagement tables already reserve <code>response_text</code> / <code>response_received_at</code> columns. But the inbound handler that catches the reply never existed: ~325 lines of verification + parsing + routing logic (<code>verifyResendWebhook</code>, <code>parseReplyToToken</code>, <code>fetchReceivedEmail</code>, <code>normalizeInReplyTo</code>, <code>isInboundEvent</code>, and three reply routers) sat fully built but with <strong>zero callers</strong> &mdash; orphaned because the one route file, <code>/api/resend/inbound</code>, was missing. This ship creates it. When a spa owner or prospect hits reply, Resend posts the inbound email here; the route verifies the webhook signature, resolves the body, parses the plus-address token, and dispatches to the three routers in order &mdash; Refill drip &rarr; Refill outreach &rarr; Emma patient &mdash; first match wins, stamping the reply onto the originating engagement row (where the admin dashboard already reads it). <strong>Robustness:</strong> tolerates BOTH Resend inbound payload shapes (metadata-only <code>email.received</code> event &rarr; fetch the body; or full inline envelope like the light-mode receiver) so it works regardless of which Resend ships; signature verification is enforced when <code>RESEND_INBOUND_SIGNING_SECRET</code> is set and advisory (skipped + logged) until then; returns 200 on any processed/ignored event so Resend never retry-storms, 401 only on a real bad signature, 500 only on a transient body-fetch (the one case worth a retry). No schema, no new deps &mdash; pure glue that activates work already paid for. <strong>One external step before live replies land</strong> (code is ready regardless): Resend inbound MX + an Inbound Route pointing <code>reply.getrefill.app</code> at <code>/api/resend/inbound</code>, and optionally the signing secret. <strong>New</strong>: <code>src/routes/api.resend.inbound.ts</code>. <strong>Touched</strong>: <code>changelog.ts</code>. tsc 164, truncation 188.",
    ],
  },
  {
    version: "v2.86.0",
    date: "June 2026",
    items: [
      "<strong>v2.86.0 &mdash; The Resend send-seam is complete: all 17 email call sites now route through one helper.</strong> Follow-through on v2.85.0 (which did the first 3): the remaining 14 hand-rolled <code>fetch(\"https://api.resend.com/emails\")</code> calls &mdash; across recall (&times;2), rescue, blast, preshow, promo, waitlist-invite, reschedule, scan-followup (&times;2), data-export, offers, setup, light-mode-writeback, and recognition-allocation &mdash; are migrated onto <code>postResendEmail()</code>. <strong>Net &minus;85 lines</strong>, and every caller&rsquo;s exact behavior is preserved: its own throw-vs-return, its own error-string format + slice length (200 here, 300 there, un-sliced in one), its own message-id read or non-read, and its engagement-event inserts in their original order. <strong>The careful part:</strong> each site was migrated and individually verified, not swept &mdash; three callers (blast, preshow, data-export) carried fields the helper didn&rsquo;t support and were caught rather than silently broken: <code>tracking:{opens,clicks}</code> (Resend open/click webhooks that feed patient-engagement reporting) on the two campaign senders, and <code>attachments</code> (the CSV patient-export file) on data-export. The helper gained optional <code>tracking</code> + <code>attachments</code> params (purely additive &mdash; omitted when unset, so no existing caller&rsquo;s body changed), and then those three folded in cleanly. <strong>Why it matters:</strong> the upcoming smartspa.app from-address cutover, any Resend API-version bump, or adding retry/timeout policy is now a one-file change instead of a 17-site sweep. A scout had reported only 3 of these sites; a <code>grep</code> backstop found all 17 &mdash; the reason this is whole and not three-quarters done. tsc 164, truncation 188. No migration.",
    ],
  },
  {
    version: "v2.85.0",
    date: "June 2026",
    items: [
      "<strong>v2.85.0 &mdash; One Resend send-seam: the hand-rolled <code>fetch(\"https://api.resend.com/emails\")</code> is being consolidated into a single helper.</strong> Every transactional + outreach email in the app POSTs to the same Resend endpoint with the same auth header, the same JSON body shape, and the same <code>!resp.ok</code> &rarr; status+body-snippet handling &mdash; copy-pasted across <strong>17 call sites</strong> (a scout first reported only 3; a direct grep found the other 14 &mdash; a textbook bounded-search undercount, caught by the grep backstop). This ship introduces <code>src/server/resend-send.ts</code> &rarr; <code>postResendEmail()</code> and migrates the first three senders onto it: the trial-drip cron (<code>refill-drip.ts</code>), rep outreach (<code>refill-outreach-send.ts</code>), and scheduling confirmation/reminder email (<code>scheduling-email.ts</code>). <strong>Behavior-preserving by design:</strong> the helper returns a STRUCTURED result for any HTTP outcome (so each caller keeps its own throw-vs-return), <strong>re-throws</strong> network/transport errors (so existing try/catch or bare propagation is unchanged), parses the message <code>id</code> on success, and returns the raw <code>status</code>+<code>body</code> so each caller formats its own error string at its own slice length. The genuinely per-caller logic &mdash; engagement-event inserts and their ordering relative to the send &mdash; was deliberately left untouched. <strong>Honest scope:</strong> 3 of 17 done; the remaining 14 call sites (recall, rescue, blast, preshow, promo, waitlist-invite, reschedule, scan-followup, data-export, offers, setup, writeback, allocation) are a verified follow-up migration, folded in careful batches &mdash; not a blind sweep, since each has its own surrounding control flow. tsc 164, truncation 188. No migration.",
    ],
  },
  {
    version: "v2.84.0",
    date: "June 2026",
    items: [
      "<strong>v2.84.0 &mdash; Owner preview: test the native booking flow on your own <code>/s/&lt;slug&gt;</code> page even when you run on Acuity.</strong> Since v2.72.0, a spa with a live Acuity connection has its public self-booking page gated off &mdash; it shows an honest &ldquo;contact the practice directly&rdquo; card instead of open slots, because Acuity sync is one-way (read-only) and a native booking would be a &ldquo;confirmed ghost&rdquo; off the provider&rsquo;s real calendar. That guard is correct for real patients, but it also blocked the <em>owner</em> from ever walking the booking experience to test it &mdash; which is exactly what surfaced after the Rejuv calendar was re-pointed onto its real account (the Acuity connection moved, the gate flipped on, and <code>/s/rejuv</code> went to the &ldquo;unavailable&rdquo; card). <strong>Fix:</strong> append <code>?preview</code> to the booking URL (e.g. <code>/s/rejuv?preview</code>) and, <em>only</em> when you&rsquo;re signed in to SmartSpa as the spa owner or an admin, the full native flow unlocks &mdash; pick a service, see real open slots, hold, and confirm end-to-end. <strong>The guard is never weakened for patients:</strong> the bypass requires a valid owner/admin session token (verified server-side on every gated call &mdash; context, slot list, and hold); an absent, expired, or non-owner token falls straight back to the normal &ldquo;contact the practice&rdquo; card. A clear &ldquo;Owner preview&rdquo; ribbon shows on the page so it&rsquo;s never mistaken for the live patient view. Fully additive &mdash; no behavior change for any non-preview visit. <strong>Touched</strong>: <code>scheduling.functions.ts</code> (new <code>isBookingPreviewer</code>/<code>externalPmsGateBlocks</code> + <code>previewToken</code> threaded through <code>getPublicBookingContextFn</code>, <code>listAvailableSlots</code>, <code>holdSlot</code>; <code>confirmBooking</code> rides the existing hold), <code>s.$slug.tsx</code> (<code>?preview</code> search param, token resolution, preview ribbon), <code>changelog.ts</code>. tsc 164, truncation 188. No migration.",
    ],
  },
  {
    version: "v2.83.0",
    date: "June 2026",
    items: [
      "<strong>v2.83.0 &mdash; Connection-health: the circuit breaker now reacts to REAL backend traffic, not just a 30-second idle probe.</strong> The app already had a circuit breaker + an <code>OfflineBanner</code> that flips to a gray &ldquo;You&rsquo;re offline&rdquo; / amber &ldquo;Backend is unreachable&rdquo; banner &mdash; but its <code>recordSupabaseSuccess</code>/<code>recordSupabaseFailure</code> hooks were dead code, never wired. So a real outage wasn&rsquo;t noticed until the slow 30 s probe happened to catch it. <strong>Now every browser-side Supabase request feeds the breaker</strong> via a single <code>global.fetch</code> hook on the client, so a burst of real failures trips &ldquo;degraded&rdquo; immediately. <strong>Honest failure attribution (the load-bearing detail):</strong> only a network-level error, a 5xx, or a 429 counts as a failure. Any real HTTP response &mdash; including a 4xx like an expired token, an empty <code>single()</code> 406, or a 409 conflict &mdash; proves the backend is reachable and is recorded as a SUCCESS. A naive <code>response.ok</code> check would have flipped the banner to &ldquo;degraded&rdquo; on a perfectly healthy connection every time a token expired or a query returned no rows &mdash; the exact false-alarm the connection-health doctrine exists to prevent. <strong>Architecture:</strong> extracted the breaker into a dependency-free <code>src/lib/circuit-breaker.ts</code> leaf so the Supabase client can feed it without an import cycle in init-critical code; <code>networkHealth.ts</code> keeps the idle probe but now just issues synthetic traffic for the one fetch-hook classifier to observe (no more double-counting the probe&rsquo;s own request). tsc 164, truncation 188, full vite build clean. <em>To see it: open DevTools &rarr; Network &rarr; Offline, and the amber/gray banner appears.</em> (Server-side <code>admin()</code> backend health is a separate surface, not wired here.)",
    ],
  },
  {
    version: "v2.82.0",
    date: "June 2026",
    items: [
      "<strong>v2.82.0 &mdash; Tier-1 tidy: delete 8 genuinely-dead functions + consolidate the tenant-lookup helper.</strong> Two behavior-preserving cleanups in one pass. <strong>(1) Dead code:</strong> removed 8 functions confirmed to have zero callers anywhere &mdash; <code>formatBytes</code>, <code>initTheme</code>, <code>metricLabel</code>, <code>describeRate</code>, <code>describeCadence</code>, <code>flywheelHeadline</code>, <code>ambiguityReason</code>, and the entire 348-line <code>src/lib/seed.ts</code> (old-app demo seeding, nothing imported it). <strong>(2) <code>getTenantIdForUser</code>:</strong> five files each carried a byte-similar copy of the same tenant-membership lookup, differing only in the not-found error message. Collapsed them into one canonical <code>getTenantIdForUser(sb, userId, notFoundMsg?)</code> in <code>auth-helpers.ts</code> &mdash; the four owner-facing surfaces (catalog, billing, scheduling, wishlist) pass their tailored onboarding copy verbatim so the human-facing messages are unchanged; <code>light-mode</code>&rsquo;s genuinely-different nullable owner-only variant was left alone. <strong>The methodology lesson worth recording:</strong> the initial dead-list flagged a 9th function, <code>summarizeRewardEntries</code>, as dead &mdash; it is NOT. Its only caller lives in a file that <code>grep</code>/<code>rg</code> treat as <em>binary</em> (it contains a <code>&quot;\\0&quot;</code> null-character separator literal), so default text search silently skips it &mdash; a false-negative of the exact silent-truncation class we audit for. A tsc break caught the bad delete; we reverted it and re-verified every candidate in <code>grep -a</code> text mode. Net &minus;437 lines. <strong>tsc baseline re-frozen 251 &rarr; 164</strong> (the deleted code carried ~87 pre-existing baselined errors; tightening the floor so future regressions can&rsquo;t hide in the slack). Truncation tripwire 188.",
    ],
  },
  {
    version: "v2.81.0",
    date: "June 2026",
    items: [
      "<strong>v2.81.0 &mdash; Tier-1 refactor: one canonical admin authorization gate.</strong> Security-sensitive structural cleanup, behavior preserved. Eleven server files each carried their own copy of an admin-role gate under three different names &mdash; <code>assertAdmin</code> (4 files), <code>requireAdmin</code> (4), <code>requireAdminRole</code> (1) &mdash; plus a second &ldquo;assert an already-resolved userId is admin&rdquo; variant in 2 more. All of them ran the same <code>user_roles</code> role=&lsquo;admin&rsquo; check; the names and error strings had simply drifted. Collapsed them into two canonical exports in the existing <code>src/server/auth-helpers.ts</code>: <code>requireAdmin(accessToken) &rarr; userId</code> (verify token + assert admin) and <code>assertUserIsAdmin(sb, userId) &rarr; void</code> (assert a known userId). <strong>The discipline that matters here:</strong> this touches authorization, so the gate was independently re-counted before and after &mdash; <strong>34 gate calls in, 34 gate calls out</strong> (27 <code>requireAdmin</code> + 7 <code>assertUserIsAdmin</code>), proving not a single check was dropped, only renamed/relocated. The 4 call sites that needlessly passed an <code>sb</code> client now let the helper make its own. Retired names fully gone (zero <code>assertAdmin</code>/<code>requireAdminRole</code> definitions remain). Net &minus;133 lines. Done in an isolated worktree; tsc frozen at 251, truncation at 188. (<code>getTenantIdForUser</code> consolidation deliberately split into a separate pass &mdash; its onboarding error messages and a nullable owner-only variant genuinely differ; the <code>loose()</code>/<code>LooseClient</code> escape-hatch scatter is being left alone as low-value/high-noise.)",
    ],
  },
  {
    version: "v2.80.0",
    date: "June 2026",
    items: [
      "<strong>v2.80.0 &mdash; Tier-1 refactor: one service-role Supabase client (the <code>admin()</code> consolidation).</strong> Pure structural cleanup, zero behavior change. <strong>75 server files</strong> each declared their own near-identical <code>function admin()</code> factory (service-role <code>createClient</code> + a local <code>SbClient</code> type alias) &mdash; ~980 lines of copy-paste. Collapsed all of them into one shared <code>src/server/admin-client.ts</code> that exports <code>admin()</code> + the <code>SbClient</code> type; every caller now imports from <code>./admin-client</code>. <strong>The load-bearing detail:</strong> the bodies had drifted &mdash; ~44 files read only <code>SUPABASE_URL</code>/<code>SUPABASE_SERVICE_ROLE_KEY</code> while ~32 added <code>?? VITE_SUPABASE_URL</code> / <code>?? SUPABASE_SERVICE_KEY</code> fallbacks. The shared helper carries the <strong>union</strong> of both, so it&rsquo;s a strict superset &mdash; no env path any file relied on was dropped (a regression tsc could not have caught). Also shed the now-orphaned <code>createClient</code> import from all 75 and the dead <code>Database</code> import from 41 where it was referenced only inside the deleted block. <strong>Net &minus;903 lines.</strong> Done in an isolated worktree, independently verified before merge: tsc frozen at 251, truncation tripwire at 188, zero <code>function admin(</code> stragglers, zero broken refs. (Next Tier-1 pass: centralize <code>requireAdmin</code>/<code>assertAdmin</code>/<code>getTenantIdForUser</code>/<code>loose()</code> into auth-helpers.)",
    ],
  },
  {
    version: "v2.79.0",
    date: "June 2026",
    items: [
      "<strong>v2.79.0 &mdash; Tier-1 cleanup (low-risk wins from the code scout).</strong> Dependency + dead-code hygiene, no behavior change. <strong>Removed 6 unused npm dependencies</strong> (verified zero imports anywhere): <code>@dnd-kit/core</code> + <code>@dnd-kit/sortable</code> + <code>@dnd-kit/utilities</code> (drag uses the local <code>use-drag-autoscroll</code>), <code>@composio/core</code> (only ever a UI label string), <code>pdf-lib</code>, and <code>resend</code> (email goes through the REST API via <code>fetch</code>, the SDK was never imported). Kept <code>@tanstack/router-plugin</code> (possible build peer &mdash; not worth the risk for one package). <strong>Deleted 2 dead files</strong>: <code>src/lib/toast.ts</code> (callers import <code>toast</code> from <code>sonner</code> directly) and <code>src/lib/provider-service.ts</code> (its resolver logic was inlined elsewhere). tsc + truncation tripwires green. (~15 dead named functions + the centralize-helpers and <code>admin()</code>-consolidation refactors are the next, larger Tier-1 passes.)",
    ],
  },
  {
    version: "v2.78.0",
    date: "June 2026",
    items: [
      "<strong>v2.78.0 &mdash; Tier-0 correctness fixes from the code scout (billing accuracy + the migration ledger).</strong> Three correctness issues a fresh code scout surfaced. <strong>(1) Migration-ledger collision:</strong> the two emma_ migrations were mis-timestamped <code>20260618…</code>, colliding EXACTLY with two existing <code>v417</code> migrations &mdash; and since <code>version</code> is the ledger&rsquo;s primary key, the v2.77 backfill&rsquo;s <code>on conflict do nothing</code> silently dropped the two <code>v417</code> rows from <code>_schema_migrations</code>. Renamed the emma files to their correct forward sequence (<code>20260816020000/030000</code>) and added a ledger-correction migration so all four are recorded under unique versions. <strong>(2) Month-revenue under-bill:</strong> the current- &amp; prior-month <code>recovery_events</code> reads that feed the $5 pay-for-performance meter weren&rsquo;t paginated (only the lifetime sibling was), so a spa with &gt;1,000 verified events in a month would under-SUM revenue &rarr; under-bill. Now both page via <code>fetchAllRows</code>. <strong>(3) Reconcile truncation:</strong> the attribution reconcile loaded unverified events with no <code>.range()</code> (silently capped at 1,000), so a busy spa stopped matching/billing past row 1,000 &mdash; now paginated so every event reconciles. (The per-event txn lookup in that loop is a known N+1, batching queued for the perf pass.) Truncation tripwire re-frozen 189&rarr;188. <strong>Touched</strong>: <code>emma-attribution.functions.ts</code>, the ledger migration + correction migration, 2 emma migration renames. Apply the new <code>20260817010000_v2_78_0_ledger_collision_fix.sql</code> migration to reconcile the live ledger.",
    ],
  },
  {
    version: "v2.77.0",
    date: "June 2026",
    items: [
      "<strong>v2.77.0 &mdash; Migration ledger: always know what&rsquo;s applied, never fear a re-run.</strong> Because we apply migrations by Supabase dashboard-paste (no local DB creds &rarr; no <code>supabase db push</code>), Supabase&rsquo;s automatic <code>schema_migrations</code> tracking was never maintained &mdash; nothing recorded which migrations had run. New <code>public._schema_migrations</code> ledger (<code>version</code>, <code>name</code>, <code>applied_at</code>, <code>backfilled</code>) closes that gap: <code>select * from public._schema_migrations order by applied_at, version</code> shows exactly what&rsquo;s applied and when, right in the table editor. The ledger migration backfills all 154 existing migrations (marked <code>backfilled=true</code>; the app is live so they&rsquo;re applied) and self-records. <strong>Standing convention going forward</strong>: every migration (a) starts with the <code>create table if not exists public._schema_migrations</code> bootstrap and (b) ends with a one-line self-record <code>insert &hellip; on conflict (version) do nothing</code>. Paired with idempotent bodies, re-running any migration is a visible no-op &mdash; never an overwrite. Migration: <code>20260817000000_v2_77_0_migration_ledger.sql</code>. (Also noted: the two emma_ migrations carry early <code>20260618</code> timestamps that sort before the current sequence head &mdash; cosmetic only under dashboard-paste; the ledger uses the correct forward timestamp.)",
    ],
  },
  {
    version: "v2.76.0",
    date: "June 2026",
    items: [
      "<strong>v2.76.0 &mdash; <code>emma_</code> retirement, phase 2: rewrite DB function bodies + drop the compat views.</strong> v2.75.0 renamed the tables and left a <code>security_invoker</code> compatibility VIEW under each old <code>emma_</code> name so DB function/trigger bodies (which reference table names as TEXT and don&rsquo;t auto-update on a table rename) kept working. This phase finishes the job: a single atomic migration rewrites every <code>public</code> function whose body still references an <code>emma_</code> table &mdash; pulling each definition via <code>pg_get_functiondef</code>, whole-word-replacing the 24 table tokens (<code>\\m...\\M</code> so the deliberately-kept <code>emma_appointment_id</code> COLUMN and <code>emma_sweep_cron</code> job are never touched), and <code>CREATE OR REPLACE</code>-ing the corrected body &mdash; then drops all <code>emma_*</code> compatibility views. Atomic (rolls back on any malformed rewrite), idempotent, and order-safe (functions rewritten before views dropped). DB-only; app code already runs on bare names since v2.75.0. Migration: <code>20260618030000_v2_76_0_emma_retire_phase2_functions_drop_views.sql</code> (apply via Supabase dashboard). After this, the only <code>emma_</code> left anywhere is the intentional residue: the <code>emma_appointment_id</code> column + the <code>emma_sweep_cron</code> job.",
    ],
  },
  {
    version: "v2.75.0",
    date: "June 2026",
    items: [
      "<strong>v2.75.0 &mdash; Retire the legacy <code>emma_</code> table prefix (internal cleanup; zero user-facing change).</strong> &ldquo;Emma&rdquo; was the product&rsquo;s original codename; ~24 core tables still carried it (<code>emma_appointments</code>, <code>emma_scheduler_connections</code>, &hellip;). Renamed every one to its bare name (<code>appointments</code>, <code>scheduler_connections</code>, &hellip;) to match the rest of the schema (<code>scheduling_*</code>, <code>services</code>, <code>tenants</code>, <code>brand_settings</code> &mdash; none of which carry a brand prefix). <strong>Done safely</strong>: a single atomic, idempotent migration renames each table + all child objects (FK constraints so PostgREST relationship hints stay valid, indexes, sequences, triggers) and leaves a <code>security_invoker</code> compatibility VIEW under every old name &mdash; so DB trigger/cron/function bodies keep working untouched until a follow-up rewrites them and drops the views. ~570 code references + generated types updated in lockstep; both tripwires re-frozen at the same counts (tsc 251, truncation 189 &mdash; the rename touched names, not read behavior). A pre-flight dry run caught the traps (an <code>emma_appointment_id</code> COLUMN, an <code>emma_sweep_cron</code> job, zero-code-ref tables) so a blanket strip couldn&rsquo;t silently break anything &mdash; those are left as a tiny follow-up. Migration: <code>20260618020000_v2_75_0_retire_emma_table_prefix.sql</code>.",
    ],
  },
  {
    version: "v2.74.0",
    date: "June 2026",
    items: [
      "<strong>v2.74.0 &mdash; Stop sending patients to a dead-end booking link.</strong> v2.72.0 gated native self-booking for external-PMS (Acuity) spas &mdash; but several surfaces still <em>handed out</em> the now-gated <code>/s/&lt;slug&gt;</code> link, dead-ending patients on the &ldquo;unavailable&rdquo; page. This sweeps them all: the <strong>rebook nudge</strong> and <strong>promo push</strong> SMS drop the &ldquo;Book/Rebook here&rdquo; link (the &ldquo;Reply and I&rsquo;ll find you a time&rdquo; CTA still stands &mdash; the human rebooks via Acuity, the act-layer model); the public <strong>deals page</strong> drops its per-offer + bottom Book buttons (offers still show, statically); and the owner&rsquo;s <strong>booking-settings</strong> page replaces the &ldquo;share your booking link&rdquo; box with an honest note (&ldquo;your calendar is managed in Acuity&hellip; SmartSpa watches it and acts on cancellations to recover revenue&rdquo;). The <code>/book/&lt;token&gt;</code> intent flow is untouched (it&rsquo;s already owner-mediated, fine for Acuity spas). The gate helper <code>tenantBooksOnExternalPms</code> moved to <code>scheduling-settings.functions.ts</code> so all callers share one source of truth. All conditional + reversible; SmartSpa-primary spas keep every booking link. <strong>Touched</strong>: <code>scheduling-settings.functions.ts</code>, <code>scheduling.functions.ts</code>, <code>reschedule.functions.ts</code>, <code>refill-promo-calendar.functions.ts</code>, <code>deals.$slug.tsx</code>, <code>app.refill.calendar.booking.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.73.0",
    date: "June 2026",
    items: [
      "<strong>v2.73.0 &mdash; Retire the last user-visible &ldquo;Emma&rdquo; references &rarr; SmartSpa.</strong> &ldquo;Emma&rdquo; is the legacy assistant codename; it survived only in a handful of owner-facing notification strings. Rebranded all six rendered copy sites to SmartSpa: the reliability notifications (&ldquo;<em>SmartSpa flagged X as in-recovery</em>&rdquo;, &ldquo;<em>SmartSpa never asks VIPs for deposits</em>&rdquo;, &ldquo;<em>SmartSpa&rsquo;s pre-show reminders adopt a warmer tone</em>&rdquo;, &ldquo;<em>SmartSpa still treats them normally</em>&rdquo;), the same-day-rescue recommendation (&ldquo;<em>SmartSpa offers the freed slot&hellip;</em>&rdquo;), and the spa-profile save toast (&ldquo;<em>Saved. SmartSpa&rsquo;s brain is up to date.</em>&rdquo;). <strong>Deliberately untouched</strong>: internal <code>emma_*</code> table names and <code>Emma*</code> TypeScript types &mdash; those are plumbing no user ever sees, and renaming 23 live tables (~480 refs + RLS + migrations) would be pure risk for zero user-facing gain. <strong>Touched</strong>: <code>emma-reliability.functions.ts</code>, <code>emma-intelligence.functions.ts</code>, <code>app.refill.index.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.72.0",
    date: "June 2026",
    items: [
      "<strong>v2.72.0 &mdash; Safety gate: no native self-booking for spas that run on an external calendar.</strong> Found during a live walk of the booking page: a patient could book a slot on <code>/s/&lt;slug&gt;</code>, get a &ldquo;You&rsquo;re booked&rdquo; confirmation + email, and <strong>never land on the provider&rsquo;s actual calendar</strong>. Root cause: for a spa whose real schedule lives in an external PMS (Acuity), the sync is <strong>one-way</strong> &mdash; Acuity &rarr; SmartSpa, read-only &mdash; and SmartSpa never writes back. The native slot engine also only treats an appointment as &ldquo;busy&rdquo; if it shares the exact native <code>provider_id</code> with a real timestamp, so untimed/unlinked mirrored appointments are invisible to it &rarr; it offered slots the provider was actually booked for. That&rsquo;s a confirmed ghost &mdash; a trust failure. <strong>Fix:</strong> native self-booking is now gated off whenever the spa has a live external-scheduler connection (<code>emma_scheduler_connections</code>, Acuity). The public booking page shows an honest &ldquo;contact the practice to schedule&rdquo; card instead of open slots, and the hold/confirm write-path refuses too (defense-in-depth). Native booking stays fully available for SmartSpa-PRIMARY spas; the one-way feed and the cancellation-recovery loop are completely untouched; fully reversible (disconnect Acuity &rarr; native opens). Real two-way Acuity writeback is the future unlock, scoped separately. <strong>Touched</strong>: <code>scheduling.functions.ts</code> (new <code>tenantBooksOnExternalPms</code> gate in <code>loadBaseContext</code> + <code>getPublicBookingContextFn</code>). No migration.",
    ],
  },
  {
    version: "v2.71.0",
    date: "June 2026",
    items: [
      "<strong>v2.71.0 &mdash; &ldquo;Your Brand&rdquo; accent reaches the whole booking flow.</strong> The public booking page (<code>/s/&lt;slug&gt;</code>) already wore the spa&rsquo;s name + logo + powered-by line, but every <em>selected/active</em> state was still hardcoded SmartSpa green &mdash; so a white-label spa with, say, a navy or rose accent got a green-highlighted flow. Now the booking page resolves <code>brand.accent</code> and threads it through every accent moment: the &ldquo;Anyone&rdquo; smart card, the add-on checkboxes, the Soonest/Pick-a-day toggle, the held-time summary card, the &ldquo;You&rsquo;re booked&rdquo; check, and the numbered step circles. Tailwind can&rsquo;t take a runtime hex, so these use inline styles derived from the accent (solid for borders/fills, an 8% tint for backgrounds). <strong>Defaults to the SmartSpa house green</strong> (<code>#056048</code>), so any spa without a custom accent looks unchanged. A malformed hex degrades to green rather than throwing &mdash; a public page never breaks on bad brand data. <strong>Touched</strong>: <code>s.$slug.tsx</code> (20 accent sites + new <code>hexAlpha</code> helper). No migration.",
    ],
  },
  {
    version: "v2.70.0",
    date: "June 2026",
    items: [
      "<strong>v2.70.0 &mdash; White-label polish: logo never breaks, billing copy reads right.</strong> Two small round-up fixes closing loose ends from the &ldquo;Your Brand&rdquo; work. <strong>(A) Logo-404 safety net:</strong> the patient pages (rescue claim, waitlist opt-in, booking) rendered a spa&rsquo;s logo image with no failure handling &mdash; a typo&rsquo;d or expired logo URL left a broken-image gap mid-flow. They now share one <code>PublicBrandHeader</code> component that falls back to the spa&rsquo;s letter-mark + name the instant a logo fails to load, so the brand never vanishes. (Email keeps its alt-text fallback, the correct pattern there since mail clients don&rsquo;t run <code>onError</code>.) <strong>(B) Billing copy:</strong> the billing scoreboard&rsquo;s &ldquo;The math behind it&rdquo; line read &ldquo;<em>$29.00 charges this month</em>&rdquo; (treating a dollar amount as a count); now it reads &ldquo;<em>$29.00 this month</em>.&rdquo; <strong>Touched</strong>: new <code>PublicBrandHeader.tsx</code>, the 3 patient routes, <code>app.billing.index.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.69.0",
    date: "June 2026",
    items: [
      "<strong>v2.69.0 &mdash; &ldquo;Your Brand&rdquo; reaches the inbox: white-labeled booking emails.</strong> The patient transactional emails &mdash; <strong>booking confirmation</strong> + <strong>appointment reminder</strong> &mdash; now carry an entitled spa&rsquo;s brand instead of SmartSpa. The <strong>from-name</strong> shows the spa&rsquo;s brand (e.g. &ldquo;Rejuv Skin Spa&rdquo;), and the email chrome adopts the spa&rsquo;s <strong>logo/letter-mark, accent color, and name</strong> in the header, with the &ldquo;SmartSpa&rdquo; footer credit dropped when the spa has removed it. The shared email shell gained an optional brand parameter, so the <strong>owner-acquisition trial drips stay 100% SmartSpa</strong> (byte-identical) &mdash; only the patient-facing booking emails white-label. Brand is resolved per-tenant (mapped to the owner), cached once per reminder-cron batch so a busy night doesn&rsquo;t re-resolve per email. <strong>Deliberately unchanged</strong>: the sending <em>address</em> stays the verified <code>@getrefill.app</code> mailbox (only the display name brands) &mdash; a per-spa sending <em>domain</em> is the deferred custom-domain tier. <strong>Touched</strong>: <code>refill-drip-shell.ts</code> (optional brand in header/footer/CTA), <code>scheduling-email.ts</code> (brand on confirmation + reminder), <code>brand-resolver.ts</code> (<code>resolveBrandForTenant</code>), and the three callers (<code>scheduling.functions</code>, <code>scheduling-owner.functions</code>, the reminder cron). No migration.",
    ],
  },
  {
    version: "v2.68.0",
    date: "June 2026",
    items: [
      "<strong>v2.68.0 &mdash; &ldquo;Your Brand&rdquo; goes self-serve: turn white-label on yourself for $29/mo.</strong> Until now the white-label add-on was unlocked manually (a SQL flip); this makes it a one-tap upgrade. On <strong>Settings &rarr; Your Brand</strong>, a spa can now <strong>Start white-label &mdash; $29/mo</strong> (and cancel anytime) right from the page. Turning it on activates the brand immediately and adds a flat <strong>$29/mo</strong> line to the spa&rsquo;s existing monthly invoice &mdash; no separate subscription, it rides the same Stripe customer + card already used for the per-booking fees. The charge is wired through the <em>single</em> billing math (<code>loadEffectiveFeeConfig</code> &rarr; <code>computeInvoiceTotal</code>), so the <strong>billing scoreboard</strong>, the recovery-dashboard <strong>preview</strong>, and the actual <strong>monthly invoice</strong> can never disagree &mdash; the add-on shows as its own line (&ldquo;Your Brand &mdash; white-label&rdquo;) everywhere. The monthly cron also unions in subscribed spas that have no other billable activity, so the $29 is never silently skipped. Price lives in one config constant. <strong>Honest framing kept</strong>: the give still ships first &mdash; any spa can fully set up + live-preview its brand for free; you only pay to flip it on for patients. <strong>Touched</strong>: <code>brand.ts</code> (price = $29), <code>brand-settings.functions.ts</code> (<code>setWhiteLabelAddonFn</code>), <code>billing-fee-core.ts</code> (add-on in the shared fee config + total), <code>refill-billing.ts</code> (invoice line + cron union + preview), <code>refill-fee-rules.functions.ts</code> (ledger), <code>app.refill.settings.brand.tsx</code> (self-serve start/cancel), <code>app.billing.index.tsx</code> (scoreboard line). No migration.",
    ],
  },
  {
    version: "v2.67.0",
    date: "June 2026",
    items: [
      "<strong>v2.67.0 &mdash; &ldquo;Your Brand&rdquo; spreads to the waitlist + booking pages.</strong> v2.66.0 flipped the first white-label surface (the rescue-claim page); this extends it to the other two patient-facing surfaces a spa shares by link. The <strong>waitlist opt-in page</strong> (<code>/waitlist/optin/&hellip;</code>) now renders the spa&rsquo;s logo/letter-mark + brand name, accent-colored &ldquo;count me in&rdquo; button + highlights, an optional &ldquo;powered by SmartSpa&rdquo; footer (suppressed when the spa removes it), and a white-labeled <code>og:site_name</code> in the iMessage/Mail link preview. The <strong>public booking page</strong> (<code>/s/&lt;slug&gt;</code>) gains a brand header (logo + name on every step) + the suppressible powered-by footer + a real branded link preview (the page had no preview meta at all before). Brand is keyed by <code>user_id</code> while booking is keyed by <code>tenant_id</code>, so the booking resolver maps tenant&rarr;owner via the same lookup native bookings already use. All three surfaces flow through one shared <code>PublicBrand</code> shape + the single <code>resolveBrand</code>/<code>mergeBrand</code> gate, which keeps SmartSpa for any spa that isn&rsquo;t entitled + live. <strong>Deliberately deferred</strong>: deep accent-theming of the booking page&rsquo;s interactive states (selected chips, step pills) stays SmartSpa-green this pass &mdash; a focused tighten. <strong>Touched</strong>: <code>brand-resolver.ts</code> (shared <code>PublicBrand</code> + <code>toPublicBrand</code>), <code>emma-waitlist.functions.ts</code> + <code>waitlist.optin.$token.tsx</code>, <code>scheduling.functions.ts</code> + <code>s.$slug.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.66.0",
    date: "June 2026",
    items: [
      "<strong>v2.66.0 &mdash; &ldquo;Your Brand&rdquo;: make the patient experience yours.</strong> v2.65.0 gave every spa a tasteful, fully <strong>SmartSpa</strong>-branded experience for free; this is the paid flip on top of it. A new <strong>Settings &rarr; Your Brand</strong> tab with a live preview, split into two tiers. <strong>Free, on every plan:</strong> name your assistant. <strong>Paid white-label add-on:</strong> replace the SmartSpa name with your own, set your accent color, drop in your logo, and remove the &ldquo;powered by SmartSpa&rdquo; credit &mdash; applied across patient-facing surfaces. Give-before-you-get: a non-entitled spa can fully set up <em>and preview</em> its brand for free; it goes live the moment they upgrade. First surface flipped: the patient <strong>rescue-claim page</strong> &mdash; logo/letter mark, brand name, accent-colored button + highlights, optional powered-by removal, and a white-labeled <code>og:site_name</code> in the link preview (iMessage / Mail). The free/paid gate lives in exactly one pure function (<code>mergeBrand</code>) consumed via one server resolver (<code>resolveBrand</code>), so every future surface (waitlist opt-in, booking, emails) inherits it. Pricing is a single config constant for later. <strong>New:</strong> <code>brand_settings</code> table + RLS, <code>src/lib/brand.ts</code> merge, <code>src/server/brand-resolver.ts</code>, <code>src/server/brand-settings.functions.ts</code>, <code>app.refill.settings.brand.tsx</code>. <strong>Touched</strong>: rescue claim page + <code>getRescueOfferPayload</code> (brand on the payload). Migration: <code>brand_settings</code> (entitlement flipped per-spa via SQL until billing wires up).",
    ],
  },
  {
    version: "v2.65.0",
    date: "June 2026",
    items: [
      "<strong>v2.65.0 &mdash; Brand alignment: one name everywhere a person looks.</strong> The app header already said <strong>SmartSpa</strong>, but the old <em>Emma</em> assistant persona was still leaking into ~25 user-facing strings (empty-states on Rescue, Campaigns, Patients, Reports, Recovery, the services editor, calendar-install pages, breadcrumbs) and the patient-facing link previews still read <em>Refill</em>. This pass finishes the rebrand on every surface a human actually sees: the assistant is now <strong>SmartSpa</strong> (de-gendered to plain product voice &mdash; &ldquo;SmartSpa offers it to your waitlist,&rdquo; not &ldquo;Emma&hellip; she&rdquo;); the patient <strong>rescue-claim and waitlist link previews</strong> now say <strong>SmartSpa</strong>; Karen&rsquo;s trial-drip and offer emails now name <strong>SmartSpa</strong> as the product (Karen stays the human signature); the landing hero, README, and new-sender default display-name all align. Also fixed an inverted brand default on the Acuity-connections page. <strong>Deliberately unchanged</strong>: <em>Refill</em> stays the name of the recovery <em>Solution</em> (the Recovery nav chip, the <code>shell===&ldquo;refill&rdquo;</code> deployment); the <code>getrefill.app</code> domain + transactional from-addresses stay put until the daylight DNS/email cutover; and the internal <code>emma_*</code> table/cron/route identifiers are infra (a separate rename, not user-facing). <strong>Touched</strong>: user-facing strings across <code>src/routes</code>, <code>src/components</code>, and the email composers (<code>refill-drip.ts</code>, <code>refill-offers.ts</code>, <code>refill-setup.functions.ts</code>, <code>emma-sender.functions.ts</code>). No migration.",
    ],
  },
  {
    version: "v2.64.4",
    date: "June 2026",
    items: [
      "<strong>v2.64.4 &mdash; &ldquo;Simulate a cancellation&rdquo; now uses the service&rsquo;s real duration.</strong> The test slot was hardcoded to 30 minutes, so simulating a Tox cancellation (really 15 min) didn&rsquo;t faithfully test the duration gate &mdash; a 45-min filler patient could slip into a 30-min slot. Now the sim looks up the freed treatment&rsquo;s actual catalog duration, so a Tox sim is a true 15-min slot and a Filler patient is correctly excluded. The confirmation toast now shows the slot length (&ldquo;Rescue fired on a 15-min slot&rdquo;) so you can see exactly what was tested. <strong>Touched</strong>: <code>simulateRescueDispatchFn</code> (resolve freed duration), rescue page toast.",
    ],
  },
  {
    version: "v2.64.3",
    date: "June 2026",
    items: [
      "<strong>v2.64.3 &mdash; Set service durations for a whole category at once.</strong> Accurate durations are what make Smart Slot-Fill safe (a 45-min filler patient should never be offered a 15-min slot) &mdash; but CSV-imported services all land at the 30-min default, and fixing them one by one is tedious. Each category header in <strong>Booking Settings &rarr; Bookable services</strong> now has a <strong>&ldquo;Set all times&rdquo;</strong> control: type one number and every service in that category (e.g. all Tox &rarr; 15, all Filler &rarr; 45) is set in a couple taps, then Save. Per-service and per-provider duration overrides still work as before for the exceptions. <strong>Touched</strong>: <code>BookableServicesSection</code> (category quick-fill).",
    ],
  },
  {
    version: "v2.64.2",
    date: "June 2026",
    items: [
      "<strong>v2.64.2 &mdash; Smart Slot-Fill copy fix: no more double provider names.</strong> Some spas bake the provider into the service name itself (&ldquo;Tox w/ Karen&rdquo;). When Smart Slot-Fill named that service in an offer, it collided with the &ldquo;&hellip;with Karen&rdquo; clause the message already adds &mdash; reading &ldquo;opening with Karen for Tox w/ Karen.&rdquo; Now the provider baked into the service name is stripped everywhere it&rsquo;s shown (the rescue SMS, the proxy email drafts, and the claim page), so it reads cleanly: &ldquo;opening with Karen for Tox.&rdquo; Also: the &ldquo;Simulate a cancellation&rdquo; harness now picks the provider whose name matches your practice owner, so the test reads the way a real solo-practitioner cancellation would. <strong>Touched</strong>: <code>composeRescueSms</code> / <code>composeProxyEmail</code> / <code>getRescueOfferPayload</code> (strip baked-in provider from the per-offer service label), <code>simulateRescueDispatchFn</code> (owner-matching provider pick).",
    ],
  },
  {
    version: "v2.64.1",
    date: "June 2026",
    items: [
      "<strong>v2.64.1 &mdash; &ldquo;Simulate a cancellation&rdquo; now tests Smart Slot-Fill for real.</strong> The go-live test button used to create an unlinked test slot, which quietly fell back to the old same-service matcher &mdash; so it could never show off the new cross-service fills. Now the simulated slot is automatically linked to your primary provider (and accepts an optional duration), so firing it runs the full provider-qualification &times; duration match and surfaces cross-service candidates just like a real cancellation would. <strong>Touched</strong>: <code>simulateRescueDispatchFn</code> (auto-link provider + optional <code>durationMin</code>/<code>providerId</code>).",
    ],
  },
  {
    version: "v2.64.0",
    date: "June 2026",
    items: [
      "<strong>v2.64.0 &mdash; Smart Slot-Fill: a freed slot now finds the right patient, not just the same-service one.</strong> When an appointment cancels, the rescue matcher used to look only for waitlist patients who wanted the <em>exact same treatment</em>. Now it understands your business: a freed <strong>30-min opening with Karen</strong> can also reach a patient on the waitlist for a <strong>different</strong> service &mdash; as long as Karen is qualified for it (per your Booking Settings provider grid) and it fits the freed time. Same-service matches still rank first, then these cross-service fills, then everyone else, all by patient value. <strong>Honest by design</strong>: a cross-service offer is framed around the opening and names <em>that patient&rsquo;s</em> service (&ldquo;last-minute opening with Karen &mdash; grab it for your Filler&rdquo;), the claim page shows the right service, and when they grab it the rescue log tells you to set the matching appointment type in your calendar. Nothing changes for slots without a qualified cross-service match &mdash; they behave exactly as before. <strong>Touched</strong>: <code>selectFitPatients</code> (layered provider-qualification + duration matcher, reads <code>scheduling_provider_services</code> + the catalog-linked <code>desired_service_ids</code> spine), <code>dispatchRescueAttempt</code> (qualification context), the SMS/email/claim copy, held-offer queue + send, and a new <code>offer_service_id</code> column on <code>emma_rescue_offers</code>. Migration: <code>offer_service_id uuid</code> (additive, nullable).",
    ],
  },
  {
    version: "v2.63.1",
    date: "June 2026",
    items: [
      "<strong>v2.63.1 &mdash; &ldquo;Link existing waitlist to catalog&rdquo; now actually links the people who never stated a preference.</strong> Most patients joined the waitlist via the footer link without picking a service, so the first version of the backfill had nothing to resolve and (worse) reported a cheery &ldquo;already linked&rdquo; when really nothing was linked. Fixed both: when a waitlist patient has no stated desire, the backfill now <strong>infers their usual service from visit history</strong> (the same &ldquo;infer, don&rsquo;t ask&rdquo; the Smart Invite uses) and links that — and the toast tells the honest truth (&ldquo;Linked N (M inferred from visit history)&rdquo;, or &ldquo;no history to infer from yet&rdquo;, never a false success). <strong>Touched</strong>: <code>backfillWaitlistServiceLinksFn</code> (history inference + honest counts), invite-page toast copy.",
    ],
  },
  {
    version: "v2.63.0",
    date: "June 2026",
    items: [
      "<strong>v2.63.0 &mdash; Waitlist desires now link to your real service catalog.</strong> Until now, what a waitlist patient &ldquo;wants&rdquo; lived as free text (&ldquo;Tox w/ Karen&rdquo;, &ldquo;RF Micro Face+Neck&rdquo;) &mdash; fine for the loose rescue match, but the upcoming <strong>Smart Slot-Fill</strong> needs to know the exact service so it can check which providers are qualified for it and whether it fits a freed slot&rsquo;s duration. This lays that spine: every waitlist row now also carries the <strong>catalog service(s)</strong> its desire maps to. New Smart-Invite sends and patient opt-ins resolve it automatically; a <strong>&ldquo;Link existing waitlist to catalog&rdquo;</strong> button (on the Invite page) backfills everyone already on the list, and the waitlist tab shows each patient&rsquo;s linked service as a green chip. The matcher is whole-word (so &ldquo;Detox Facial&rdquo; never mis-links to &ldquo;Tox&rdquo;), and anything that doesn&rsquo;t match a catalog service keeps working on the old free-text path &mdash; nothing breaks. <strong>Touched</strong>: new <code>desired_service_ids</code> column on <code>emma_waitlist</code> (additive), new <code>treatment-catalog-resolver</code> + <code>loadServiceCatalogForUser</code>, <code>sendOptInInviteBatchFn</code> / <code>optInToWaitlist</code> / <code>listWaitlist</code> / <code>backfillWaitlistServiceLinksFn</code>, rescue + invite UI. Migration: <code>desired_service_ids uuid[]</code>.",
    ],
  },
  {
    version: "v2.62.0",
    date: "June 2026",
    items: [
      "<strong>v2.62.0 &mdash; Smart Invite: the waitlist invite composer now prioritizes who to ask first.</strong> The “Invite to waitlist” page used to show a flat list of A-listers by lifetime spend. It now ranks them by the moment that matters: <strong>patients on the schedule right now</strong> (a live upcoming booking &mdash; already engaged, the quick win) float to the top with a <em>“Booked Jun 20”</em> chip, then <strong>patients due for a touch-up</strong> (Recall’s overdue engine) with a <em>“Due 45d”</em> chip, then the rest by spend. Patients already on the active waitlist are filtered out automatically &mdash; no more inviting people who are already in. One-tap selects: <strong>On the schedule (N)</strong> and <strong>Due soon (N)</strong>, and the on-schedule cohort is pre-selected for you. Each patient’s usual service is <strong>inferred from their history and pre-filled</strong> onto their waitlist row when the invite goes out &mdash; so when a slot frees up, the matcher already knows what they’d want, without anyone having to ask. <strong>Touched</strong>: <code>getSmartInviteCohortFn</code> (new &mdash; future-booking + Recall-overdue + waitlist-status + inferred-service signals, all paginated), <code>sendOptInInviteBatchFn</code> (seeds inferred desired-service onto the paused row, never clobbering a patient’s own pick), <code>app.refill.waitlist.invite</code>. No migration.",
    ],
  },
  {
    version: "v2.61.0",
    date: "June 2026",
    items: [
      "<strong>v2.61.0 &mdash; Fixed waitlist opt-in links pointing at the wrong domain.</strong> The “join the waitlist” link patients tap was still built against an old domain (<code>emma.agentiport.com</code>) instead of <strong>getrefill.app</strong> &mdash; the same dead-host bug the rescue claim link had fixed long ago, which the opt-in builder never got. Opt-in links now resolve on getrefill.app. <strong>Touched</strong>: <code>buildWaitlistOptInUrl</code>. No migration.",
    ],
  },
  {
    version: "v2.60.2",
    date: "June 2026",
    items: [
      "<strong>v2.60.2 &mdash; Renamed the Calendar tab “Online booking” to “Booking Settings.”</strong> The tab is where you configure providers, hours, and which services each provider offers &mdash; “Booking Settings” describes that better than “Online booking” (which sounded like just the on/off switch). <strong>Touched</strong>: Calendar tab + page title. No migration.",
    ],
  },
  {
    version: "v2.60.1",
    date: "June 2026",
    items: [
      "<strong>v2.60.1 &mdash; Fixed misleading Rescue copy.</strong> The Eligible Treatments card said an empty list meant Rescue would “skip every cancellation” &mdash; the opposite of what actually happens. With no treatments listed, Rescue fires for <strong>every</strong> cancellation; you add treatments only to <em>narrow</em> it. The copy now says so. <strong>Touched</strong>: Rescue page. No migration, no behavior change.",
    ],
  },
  {
    version: "v2.60.0",
    date: "June 2026",
    items: [
      "<strong>v2.60.0 &mdash; A one-click way to test the rescue flow (operator tool).</strong> When the SmartSpa team is setting up or checking a spa, the Rescue page now has a <strong>Simulate a cancellation</strong> button that fires the real rescue end-to-end &mdash; it creates a throwaway cancelled slot, picks waitlist patients, and sends the offer to the proxy inbox &mdash; <em>without</em> touching a live calendar. Visible only when viewing a spa as the team (not to spa owners). <strong>Touched</strong>: <code>emma-rescue.functions</code> + the Rescue page. No migration. <em>(Cleanup of test slots: delete where notes='rescue-sim'.)</em>",
    ],
  },
  {
    version: "v2.59.0",
    date: "June 2026",
    items: [
      "<strong>v2.59.0 &mdash; Win back brand-new patients who cancel, too.</strong> Reschedule Reminders used to only nudge patients who already had a profile in SmartSpa &mdash; so a first-time patient who booked, cancelled, and had no record yet was silently skipped, even though we had their phone from the booking. Now those <strong>unmatched cancellers show up as targets</strong> (tagged <em>New patient</em>) and get the same warm rebook nudge, reachable by the contact on their booking. They&rsquo;re deduped by phone, held through your grace window, and skipped if they&rsquo;ve already rebooked &mdash; same rules as everyone else. <strong>Touched</strong>: <code>reschedule.functions</code> + the Reschedule page. No migration.",
    ],
  },
  {
    version: "v2.58.2",
    date: "June 2026",
    items: [
      "<strong>v2.58.2 &mdash; Rotate your local agent's secret in one click.</strong> The Local delivery agent has a private secret the Mac uses to check in; if it ever leaks (caught in a screenshot, say), you can now hit <strong>Rotate</strong> on the card to generate a fresh one &mdash; the old value stops working instantly, and you re-run the install command on the Mac. The new secret is shown only in the app, so it never has to travel through email or chat. <strong>Touched</strong>: Connection health page + a rotate server function. No migration.",
    ],
  },
  {
    version: "v2.58.1",
    date: "June 2026",
    items: [
      "<strong>v2.58.1 &mdash; Connection health now updates itself.</strong> The page used to only refresh when you clicked <em>Recheck</em>, so a connection that had just come back (like the local delivery agent checking in) could sit looking stale until you noticed. It now <strong>quietly re-checks every 45 seconds on its own</strong> &mdash; the status cards flip to Live / Quiet / Silent in near-real-time with no click. <strong>Touched</strong>: the Connection health page only. No migration.",
    ],
  },
  {
    version: "v2.58.0",
    date: "June 2026",
    items: [
      "<strong>v2.58.0 &mdash; Connection health now watches the Mac that relays your rescue texts.</strong> The no-show rescue loop runs itself right up to the email&nbsp;&mdash;&nbsp;the last step, relaying that to the patient by text, happens on your Mac through iMessage, and SmartSpa couldn&rsquo;t see whether it was actually online. Now it can: a tiny check-in runs on that Mac every few minutes, and <strong>Settings → Connection health</strong> shows a new <strong>Local delivery agent</strong> card that reads <em>Live</em>, <em>Quiet</em>, or <em>Silent</em>&nbsp;&mdash;&nbsp;so if the Mac sleeps or Messages closes, you find out here instead of wondering why texts stopped. Set it up in one paste from that page. <strong>Touched</strong>: new <code>local_agents</code> table + <code>/api/agent/heartbeat</code>, a <code>presence</code> tier in connection-health, the health page, and <code>scripts/local-agent/</code>. <em>Migration: local_agents.</em>",
    ],
  },
  {
    version: "v2.57.1",
    date: "June 2026",
    items: [
      "<strong>v2.57.1 &mdash; Branding cleanup: the old &ldquo;Emma(OS)&rdquo; name is gone from the UI.</strong> A handful of pages (Campaigns, Sharing, and a few legacy headers) still showed the deprecated <em>Emma(OS)</em> name; every user-facing instance is now <strong>SmartSpa</strong>. (The recovery solution is still called <em>Refill</em> &mdash; that&rsquo;s intentional and unchanged.) <strong>Touched</strong>: user-facing strings across <code>src/routes</code> + <code>src/components</code>. No migration.",
    ],
  },
  {
    version: "v2.57.0",
    date: "June 2026",
    items: [
      "<strong>v2.57.0 &mdash; Reschedule nudges can now reach patients whose profile has no phone on file.</strong> The win-back nudge for a cancelled/no-showed patient looked up their phone/email only on their patient record &mdash; so if that record had no contact (common), they were silently unreachable even though the appointment itself carried their number. It now <strong>falls back to the contact captured on the appointment</strong> (<code>booking_phone</code>/<code>booking_email</code>), so more cancellers can actually be nudged. <strong>Touched</strong>: <code>reschedule.functions</code>. No migration. <em>(Follow-up: brand-new patients with no profile at all are still out of scope — a separate change.)</em>",
    ],
  },
  {
    version: "v2.56.10",
    date: "June 2026",
    items: [
      "<strong>v2.56.10 &mdash; Clearer hour lines on the day &amp; week calendar.</strong> The hourly gridlines were very faint; they&rsquo;re now a clean, slightly stronger line so you can actually see where each hour breaks at a glance. <strong>Touched</strong>: <code>schedule/grids.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.56.9",
    date: "June 2026",
    items: [
      "<strong>v2.56.9 &mdash; Full-screen calendar.</strong> A new expand button (next to the view toggle) blows the schedule up edge-to-edge so you can run the whole day/week/month full-screen on a big monitor; press it again or hit Escape to come back. Toolbar and all controls come along. <strong>Touched</strong>: <code>calendar.schedule</code> (native Fullscreen API). No migration.",
    ],
  },
  {
    version: "v2.56.8",
    date: "June 2026",
    items: [
      "<strong>v2.56.8 &mdash; Month view gets the provider filter too.</strong> The <em>All / per-provider</em> filter chips that week view has are now on month as well, so you can focus a month on one provider&rsquo;s appointments. The choice is shared with week, so switching views keeps your filter. <strong>Touched</strong>: <code>schedule/grids.tsx</code> (MonthGrid), <code>calendar.schedule</code>. No migration.",
    ],
  },
  {
    version: "v2.56.7",
    date: "June 2026",
    items: [
      "<strong>v2.56.7 &mdash; Month view now color-codes by treatment too.</strong> Each appointment chip in a month cell now carries the same treatment tint as the day &amp; week views (Tox = violet, Filler = pink, Sculptra = green, Laser = blue, Peel = amber&hellip;), so a month reads at a glance and matches the rest of the calendar instead of being a wall of plain green. Hover a chip to see the patient + full treatment. <strong>Touched</strong>: <code>schedule/grids.tsx</code> (MonthGrid chips). No migration.",
    ],
  },
  {
    version: "v2.56.6",
    date: "June 2026",
    items: [
      "<strong>v2.56.6 &mdash; Week view now has the same roomy spacing as the day, and cards are color-coded by treatment.</strong> <strong>(1) Week spacing</strong> &mdash; the week grid was running at a compressed vertical scale, so busy days crammed together; it now uses the <em>same</em> scale as the day view, so stacking and spacing read identically across both. <strong>(2) Color by treatment</strong> &mdash; appointment cards are now tinted by treatment type (Tox, Filler, Sculptra, Laser, Peel&hellip;) so a packed day is scannable at a glance, just like Acuity. Common treatments get a sensible fixed color; anything else gets a stable color derived from its name (same treatment → same color every time). Applies to day &amp; week. <strong>Touched</strong>: <code>calendar.schedule</code> (week scale), <code>schedule/shared.ts</code> (<code>treatmentColor</code>), <code>schedule/grids.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.56.5",
    date: "June 2026",
    items: [
      "<strong>v2.56.5 &mdash; Schedule cards: compact ones now center their text instead of cramming it at the top.</strong> On tightly-packed appointments, the single-line card (name + treatment) is now vertically centered so it reads cleanly instead of looking clipped against the top edge; taller cards still top-align their two lines. The shrink floor also gives a compact card a touch more room for one clean line. <strong>Touched</strong>: <code>schedule/grids.tsx</code>, <code>schedule/shared.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.56.4",
    date: "June 2026",
    items: [
      "<strong>v2.56.4 &mdash; Schedule cards now line up with the clock and show the treatment.</strong> Two upgrades to day &amp; week, matching how Acuity reads. <strong>(1) Times match the rail</strong> &mdash; every card now sits exactly on its start-time line, so a 1:00 PM appointment is always at 1:00 and the provider columns line up with each other. When appointments are packed tighter than a full card, each one trims to the gap before the next (staying snug and non-overlapping) rather than drifting below its real time. <strong>(2) Treatment shown</strong> &mdash; each card now shows its treatment (e.g. <em>Tox</em>, <em>Filler</em>, <em>Sculptra</em>) next to the patient name &mdash; inline on compact cards, on its own line with the time on taller ones. <strong>Touched</strong>: <code>scheduling-owner.functions</code> (carry <code>treatment_type</code> through to the calendar), <code>schedule/shared.ts</code> (anchor-to-time + shrink-to-fit layout), <code>schedule/grids.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.56.3",
    date: "June 2026",
    items: [
      "<strong>v2.56.3 &mdash; Day &amp; week now stack appointments cleanly instead of fanning them out sideways.</strong> Replaced the side-by-side overlap layout (which fanned close appointments into an awkward staircase) with a <strong>stacked, full-width list</strong> &mdash; the same readable style as the Month view. Each appointment shows at its real time when the day is open, and when bookings are too close together to fit, the later one is pushed down to sit just below the previous one (full width, in time order, never overlapping). An open morning keeps real time-of-day spacing; a packed afternoon stacks into a clean column that grows as needed. <strong>Touched</strong>: <code>schedule/shared.ts</code> (new <code>stackApptCards</code> push-down layout, replaced the lane packer), <code>schedule/grids.tsx</code> (day single + multi + week). No migration.",
    ],
  },
  {
    version: "v2.56.2",
    date: "June 2026",
    items: [
      "<strong>v2.56.2 &mdash; Schedule cards: no more mashing or clipped text.</strong> Fixes two real bugs in the day &amp; week grids. <strong>(1) Mashed cards</strong> &mdash; a card's height and the side-by-side overlap check were computed from two different sources, so a card could render taller than the gap to its neighbour and sit <em>on top</em> of it. Both now use the same rendered span, so any two cards that would visually collide are placed <strong>side-by-side</strong> instead of overlapping. <strong>(2) Clipped info</strong> &mdash; every card is now guaranteed enough height to show its full info (patient name + time) without the text being cut off at the edge. Longer appointments still grow with their duration. <strong>Touched</strong>: <code>schedule/shared.ts</code> (lane packer uses the content-floored span), <code>schedule/grids.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.56.1",
    date: "June 2026",
    items: [
      "<strong>v2.56.1 &mdash; Day &amp; week schedule: clearer, less cramped.</strong> Four refinements to the positioned calendar. <strong>(1) Honest card heights</strong> &mdash; appointments now size to their real duration instead of being inflated to a tall minimum, so a short visit no longer overflows and merges into the one below it; a hairline gap keeps back-to-back bookings visually distinct. <strong>(2) More breathing room</strong> &mdash; the default zoom is taller, so a typical day reads comfortably (zoom out anytime). <strong>(3) Week &ldquo;+N more&rdquo;</strong> &mdash; when a day has more simultaneous appointments than fit side-by-side, the extras collapse into a <em>+N more</em> pill (tap it to open that day) instead of slicing into unreadable slivers. <strong>(4) Wider columns</strong> &mdash; the week grid and multi-provider day columns are wider so names stop truncating to &ldquo;Barb&hellip;&rdquo;. Month is unchanged. <strong>Touched</strong>: <code>schedule/shared.ts</code> (lane cap + min/zoom constants), <code>schedule/grids.tsx</code>. No migration.",
    ],
  },
  {
    version: "v2.56.0",
    date: "June 2026",
    items: [
      "<strong>v2.56.0 &mdash; Overlapping appointments now sit side-by-side instead of hiding each other.</strong> On the schedule, two or more bookings at the same time used to stack directly on top of one another &mdash; so a busy 11 AM looked like a single block and the appointments underneath were invisible. They now lay out <strong>side-by-side in columns</strong> (the way Google Calendar does it): a slot with two simultaneous appointments splits into two, three splits into three, and so on, each still sized to its real duration. Back-to-back appointments keep the full width, and a run of staggered overlaps packs in tightly and reuses space as earlier appointments end &mdash; so nothing is hidden and the day reads at a glance. Applies to the day grid (single and multi-provider) and the week grid. <strong>Touched</strong>: <code>schedule/shared.ts</code> (new <code>assignApptLanes</code> overlap-packer), <code>schedule/grids.tsx</code> (<code>ApptCard</code> lane layout). No migration.",
    ],
  },
  {
    version: "v2.55.1",
    date: "June 2026",
    items: [
      "<strong>v2.55.1 &mdash; Provider rename now shows on the calendar grid too (fix).</strong> v2.55.0 let you rename a calendar column and it stuck in the Providers dialog &mdash; but the schedule grid&rsquo;s column header still showed the original calendar name, because the grid loads its columns through a different path. The day, week, and month grids now use your renamed label, matching the dialog and the booking page. The patient booking-confirmation email uses it as well. <strong>Touched</strong>: <code>scheduling-owner.functions</code> (the owner-calendar provider loader + confirmation email now resolve display_name ?? name). No migration.",
    ],
  },
  {
    version: "v2.55.0",
    date: "June 2026",
    items: [
      "<strong>v2.55.0 &mdash; Rename a calendar column &mdash; and it sticks.</strong> When SmartSpa mirrors your Acuity calendars, each one becomes a column on your schedule named exactly as it is in Acuity. If a calendar is named after your business but is really one provider&rsquo;s column (e.g. &ldquo;Rejuv Skin Spa&rdquo; that&rsquo;s actually Karen&rsquo;s), you can now <strong>rename it</strong> right from the <em>Providers</em> button on the schedule &mdash; tap the pencil, type the real name, save. The rename shows everywhere the provider appears (your grid and the patient booking page) and, crucially, <strong>survives re-syncing</strong>: the background sync keeps the underlying Acuity link intact but never overwrites your chosen label. Clear the name to go back to the calendar&rsquo;s own. The Providers dialog also shows the original calendar name underneath a rename so the link is never a mystery. <strong>New</strong>: <code>scheduling_providers.display_name</code> (a distinct override, like the hide toggle &mdash; the mirror leaves it alone), <code>setProviderDisplayNameFn</code>, inline rename in <code>ManageProvidersDialog</code>. Migration: <code>v2_55_0_provider_display_name</code>.",
    ],
  },
  {
    version: "v2.54.0",
    date: "June 2026",
    items: [
      "<strong>v2.54.0 &mdash; SmartSpa now suggests Reschedule Reminders when you&rsquo;re leaving rebookings on the table.</strong> The &ldquo;Suggested for you&rdquo; strip on your Skills page already nudges you toward Pre-Visit Reminders and Waitlist Auto-Fill when it spots an unfilled gap. This adds the third rule: if you haven&rsquo;t turned on <em>Reschedule Reminders</em> and you&rsquo;ve had several cancellations or no-shows in the last 30 days, SmartSpa now surfaces it &mdash; &ldquo;N patients cancelled or didn&rsquo;t show in the last 30 days &mdash; Reschedule Reminders would nudge them to rebook&rdquo; &mdash; with one-tap Add or Dismiss. It&rsquo;s honest by construction: it only appears for a real gap, so a spa that already has Reschedule Reminders on (or has dismissed it) sees nothing. Read-only over data you already have; nothing is sent. <strong>Touched</strong>: <code>skills.functions</code> (mining rule + signal). No migration.",
    ],
  },
  {
    version: "v2.53.0",
    date: "June 2026",
    items: [
      "<strong>v2.53.0 &mdash; Large-spa data-integrity hardening: bulk reads now page through everything, so nothing is silently dropped.</strong> The database returns at most 1,000 rows per read unless you explicitly page &mdash; and a read that doesn&rsquo;t page gets <em>silently</em> capped, with no error, so the code treats a partial answer as the whole. That same class of bug surfaced three times recently (the reliability recompute fixed in v2.52.0 was one). This release closes the rest of it and makes it impossible to reintroduce. <strong>Calendar:</strong> the Acuity history backfill, the self-healing reconcile sweep, and the wide provider-relink now page through your <em>entire</em> date window instead of stopping at the first 1,000 appointments &mdash; so a busy spa&rsquo;s calendar can no longer be quietly half-synced (and if a single day ever exceeds the cap, that&rsquo;s now reported rather than hidden). <strong>Rep program:</strong> a rep&rsquo;s recruit-page earnings, downstream-recruit count, and full commission ledger now page completely, so a long-tenured rep&rsquo;s numbers can&rsquo;t under-report. <strong>And permanently:</strong> a new deploy guard (a &ldquo;truncation tripwire&rdquo;) scans every ship and blocks it if anyone adds an unpaginated bulk read &mdash; the same way the type-error tripwire already works &mdash; so this class of silent data loss can&rsquo;t come back. <strong>New</strong>: <code>selectAllRows</code> (shared pagination helper), <code>listAllAcuityAppointmentsInWindow</code> (date-window paging for Acuity), <code>scripts/truncation-tripwire.mjs</code> (baselined, wired into <code>npm run deploy</code>). No migration.",
    ],
  },
  {
    version: "v2.52.0",
    date: "June 2026",
    items: [
      "<strong>v2.52.0 &mdash; Reliability recompute now covers every patient at larger spas (bug fix).</strong> The rule-aware nightly recompute added in v2.50.0 gathered its patient list in a single query, which the database silently caps at 1,000 rows &mdash; so at a spa with thousands of appointments, only the patients appearing in that first page were recomputed and the rest kept stale scores. Caught on real data (a spa with 695 patients was only recomputing 174). Now it pages through the full appointment history so <em>every</em> patient is recomputed. Affects only spas that opted the no-show rule into reliability scoring.",
    ],
  },
  {
    version: "v2.51.0",
    date: "June 2026",
    items: [
      "<strong>v2.51.0 &mdash; Reliability recompute can now target a single spa.</strong> The nightly reliability recompute can be pointed at one account (<code>?userId=</code>) instead of always sweeping every tenant &mdash; matching how the calendar reconcile and provider-relink jobs already work. Internal operability only (no behavior change to the scheduled nightly run): it makes a targeted recompute safe to run for one spa without recomputing everyone or emitting unrelated alerts.",
    ],
  },
  {
    version: "v2.50.0",
    date: "June 2026",
    items: [
      "<strong>v2.50.0 &mdash; Reliability scoring: your no-show rule now sticks, everywhere, consistently.</strong> If you&rsquo;ve turned on <em>&ldquo;apply the no-show rule to reliability scoring&rdquo;</em> (so a late cancel counts as a no-show when scoring a patient), two rough edges are now smoothed. <strong>(1) The nightly recompute no longer reverts it.</strong> Live status changes already scored late cancels correctly, but the once-a-night full recompute used a faster shortcut that couldn&rsquo;t see your notice-window rule &mdash; so it quietly overwrote those scores with raw ones each night. Now, for spas that opted in, the nightly pass uses the exact same rule-aware calculation as the live path, so a patient&rsquo;s score means the same thing at 2pm and at 2am. <strong>(2) No double penalty on grace credits.</strong> When a late cancel is treated as a no-show, it no longer <em>also</em> spends one of the patient&rsquo;s grace credits &mdash; grace forgives fair-notice cancellations only, and a late cancel takes the no-show penalty instead of forgiveness. Spas that haven&rsquo;t opted in see no change at all. <strong>New</strong>: rule-aware bulk recompute path (delegates to the canonical per-patient calculation so there&rsquo;s one source of truth), grace credits computed from honored cancels only. No migration.",
    ],
  },
  {
    version: "v2.49.0",
    date: "June 2026",
    items: [
      "<strong>v2.49.0 &mdash; Calendar security: verified &amp; documented how your Acuity webhook feed is protected.</strong> Following up on v2.47.0: we watched real, live webhook deliveries and confirmed that Acuity signs them with an <em>account key</em> that OAuth integrations like ours can&rsquo;t access (it lives in your Acuity settings, not in the connection we&rsquo;re granted). So the real protection on your booking feed is &mdash; and always has been &mdash; the unique 48-character secret address in its webhook URL: a strong, server-to-server key that never appears in a browser. We&rsquo;ve settled on that as the gate and kept the signature-checking machinery <strong>dormant</strong>; it arms itself automatically if that account key ever becomes available (for example, via an optional &ldquo;paste your Acuity key&rdquo; step down the road). No behavior change &mdash; your feed kept flowing throughout (verified live) &mdash; this is the settled posture, written down so it&rsquo;s never re-investigated from scratch.",
    ],
  },
  {
    version: "v2.48.0",
    date: "June 2026",
    items: [
      "<strong>v2.48.0 &mdash; Calendar: older imported appointments now sit in the right provider&rsquo;s column.</strong> When your Acuity history first came in, appointments could only be matched to a provider by <em>name</em> &mdash; which often missed, so older bookings landed unattributed or in the wrong column. The ongoing sync already fixes this for recent appointments by using Acuity&rsquo;s own calendar id (the reliable link), but anything older than the sync window kept its weaker name-match. This adds a <strong>one-time &ldquo;wide relink&rdquo;</strong> that re-reads your full Acuity history just long enough to learn each appointment&rsquo;s real calendar and corrects its provider column &mdash; and <em>only</em> the provider column: it never touches an appointment&rsquo;s status (so a visit you marked <em>showed</em> stays <em>showed</em>) or its patient. Safe to run more than once. <strong>New</strong>: <code>wideRelinkAcuityProviders</code> (surgical provider_id-only backlink over a deep window, idempotent, flags if Acuity&rsquo;s page cap is hit so a partial pass is never mistaken for complete) and a secret-gated <code>/api/cron/acuity-provider-relink</code> fire-once endpoint. No migration.",
    ],
  },
  {
    version: "v2.47.0",
    date: "June 2026",
    items: [
      "<strong>v2.47.0 &mdash; Calendar security: your Acuity webhook feed now verifies who&rsquo;s really sending it.</strong> When your calendar (Acuity) tells SmartSpa that an appointment was booked, changed, or cancelled, it signs that message so we can prove it genuinely came from Acuity and not an impostor. We checked our live feed and found the signatures weren&rsquo;t matching the key we used to verify them &mdash; so the check was effectively decorative (every real update still arrived safely, because the unguessable 48-character address in the webhook URL is the real lock). Rather than guess, SmartSpa now <strong>learns the correct signing key from your own live feed</strong>: on each incoming update it tries every credential we hold and records which one matches. The moment a real signature checks out, that connection is marked <strong>verified</strong> &mdash; and from then on any update with a bad signature is rejected as a forgery. Until a connection is proven, we keep accepting updates exactly as before, so a working calendar can never go silently dark. <strong>New</strong>: <code>webhook_signature_verified_at</code> latch on <code>emma_scheduler_connections</code>, a multi-candidate signature probe, and per-connection hard-reject of forged webhooks once the key is proven. One small migration (a new nullable column).",
    ],
  },
  {
    version: "v2.46.0",
    date: "June 2026",
    items: [
      "<strong>v2.46.0 &mdash; Calendar import: imported appointments now show the patient&rsquo;s name, and you control which calendars appear as columns.</strong> Two fixes to how your Acuity calendar comes in. <strong>(1) Names.</strong> Imported appointments were landing without a patient name unless we&rsquo;d already met that patient &mdash; the import was throwing the name away. Now every imported appointment carries the patient&rsquo;s name (and email/phone) straight from Acuity, so the calendar and the edit dialog show <em>who</em> it is. <strong>(2) Provider columns.</strong> The importer used to make a column for <em>every</em> Acuity calendar &mdash; including your business/shared calendar, which isn&rsquo;t a real provider &mdash; so the grid had spurious columns. There&rsquo;s no way for Acuity to tell us which calendar is a person, so instead of guessing silently we put you in control: a new <strong>&ldquo;Providers&rdquo;</strong> button on the Schedule lets you show or hide any column, and the list shows each one&rsquo;s appointment count so you know what hiding it affects (hiding a column hides its appointments too). Smart default: an imported business-named calendar that has <em>no</em> appointments is hidden for you automatically &mdash; but never one that carries bookings, so nothing real ever disappears. And it&rsquo;s never silent: a <strong>&ldquo;N calendars hidden &mdash; review&rdquo;</strong> note sits right on the calendar. Your choices survive future syncs. <strong>New</strong>: <code>booking_name/email/phone</code> populated in both Acuity converters (webhook + reconcile), <code>hidden_at</code> on <code>scheduling_providers</code> (a visibility override distinct from active/bookable), the empty-business-calendar smart default in the mirror, and a manage-providers dialog. One small migration (a new nullable column).",
    ],
  },
  {
    version: "v2.45.0",
    date: "June 2026",
    items: [
      "<strong>v2.45.0 &mdash; Billing: cap any feature so it never bills more than you choose per year.</strong> The pricing twin of v2.44.0&rsquo;s grace window. Metered &ldquo;$5 per win&rdquo; is fair &mdash; you only pay on value created &mdash; but some owners want a known ceiling. So in <em>Settings → Billing → How you&rsquo;re billed</em>, every metric now has an optional <strong>&ldquo;Cap this feature at $___ / year.&rdquo;</strong> Set one and that feature keeps working at full strength, but once its charges reach your cap for the calendar year, it bills <strong>$0</strong> for the rest of the year &mdash; it never flips to a flat subscription and never over-charges you for a feature you barely use (the manufacturer-style misalignment we&rsquo;re built against). It&rsquo;s pure progressive disclosure: <strong>default is no cap</strong>, so nothing changes unless you set one. And it&rsquo;s legible &mdash; each metric shows, from your real data, how much it&rsquo;s billed so far this year (vs the cap) and what it billed over your last 90 days, so you can size a cap before you commit. The cap is enforced everywhere the same way &mdash; the monthly invoice, the live scoreboard, and the dashboard preview &mdash; and individual wins reconcile to the capped total so the audit CSV always adds up. <strong>New</strong>: <code>cap_usd</code> + <code>cap_period</code> on <code>billing_fee_rules</code>, an <code>applyCap</code> primitive threaded through <code>computeInvoiceTotal</code>, and annual headroom derived from prior invoices (no new ledger table). <strong>Touched</strong>: <code>billing-fee-core</code>, <code>refill-billing</code>, <code>refill-fee-rules.functions</code>, the billing settings page. One small migration (two new nullable columns).",
    ],
  },
  {
    version: "v2.44.0",
    date: "June 2026",
    items: [
      "<strong>v2.44.0 &mdash; Reschedule Reminders: set a grace window before a nudge goes out.</strong> The strongest objection to a rebook nudge is &ldquo;they would&rsquo;ve rebooked anyway.&rdquo; This kills it. In <em>Reminders → No-show definition</em>, there&rsquo;s a new setting: <strong>&ldquo;Give patients N days to rebook on their own first.&rdquo;</strong> A patient who cancels or no-shows now sits in a grace window for however long you choose &mdash; and SmartSpa only surfaces them for a nudge once that window has passed <em>and</em> they <em>still</em> haven&rsquo;t rebooked. So you never chase someone who was always going to come back on their own; you only spend a nudge on the ones who truly slipped away. The reschedule page shows it honestly &mdash; the people still inside their grace window appear as &ldquo;N still in their grace window&rdquo; rather than the list silently leaving them out. It&rsquo;s a fairness dial: a longer wait means fewer nudges (and fewer recovered visits) &mdash; your call, your patients. Default is <strong>0 days</strong> (off), so nothing changes unless you set it. <strong>New</strong>: <code>reschedule_delay_days</code> policy field + the grace filter in the reschedule sweep (anchored on the cancel time, or the appointment time when the cancel wasn&rsquo;t timed). <strong>Touched</strong>: <code>reschedule.functions</code>, <code>emma-appointments</code> policy, the no-show settings card + reschedule page. One small migration (a new column, default 0).",
    ],
  },
  {
    version: "v2.43.0",
    date: "June 2026",
    items: [
      "<strong>v2.43.0 &mdash; Connection Health now watches your <em>appointments</em>, not just your connection.</strong> Your synced calendar (Acuity) has a self-healing sweep that re-checks it every couple of hours &mdash; which is great, except it means the connection always reads &ldquo;Healthy,&rdquo; even in the rare case where the connection is fine but new bookings have quietly stopped reaching SmartSpa. A healthy <em>pipe</em> with nothing flowing through it is exactly the silent failure this dashboard exists to catch. So Connection Health now shows a second, honest row per calendar &mdash; <strong>&ldquo;Acuity appointments&rdquo;</strong> &mdash; that tracks when your <em>newest booking</em> actually landed, separately from the connection&rsquo;s heartbeat. If bookings keep flowing it stays green (&ldquo;most recent landed 2 hours ago&rdquo;); if nothing new has arrived in a while it gently flags &mdash; &ldquo;often just a quiet calendar, but if you expected bookings, the live feed may have stopped even though the connection still reads healthy&rdquo; &mdash; with a next step. It never cries wolf: a quiet calendar is legitimate, so it only soft-flags after about a week and never hard-fails on silence alone. <strong>Touched</strong>: <code>connection-health.functions</code> (a new appointment-arrival row per connected scheduler, scored on the freshest <code>emma_appointments</code> timestamp through the same freshness engine). No schema change &mdash; it reads a timestamp that already exists; the page renders the new row automatically.",
    ],
  },
  {
    version: "v2.42.0",
    date: "June 2026",
    items: [
      "<strong>v2.42.0 &mdash; Synced appointments now link to the right provider automatically.</strong> When an appointment flows in from your booking calendar (Acuity), SmartSpa now connects it to the matching provider in your SmartSpa calendar on the spot &mdash; instead of leaving it unlinked until you re-ran the one-time setup mirror by hand. It resolves the provider from the appointment&rsquo;s own calendar, every time it ingests &mdash; live webhooks, the on-connect backfill, and the new reconcile sweep all do it now. So your provider columns, per-provider filtering, and the calendar grid stay correct continuously, with no manual step. Best-effort and non-breaking: an appointment on a calendar you haven&rsquo;t mapped simply keeps its provider name and waits, exactly as before. <strong>New</strong>: <code>buildAcuityProviderIndex</code> (calendar-id → provider lookup). <strong>Touched</strong>: <code>emma-scheduler</code> (backfill + reconcile resolve provider_id), the Acuity webhook handler (resolves it per event). No schema change &mdash; it uses the provider link that already existed.",
    ],
  },
  {
    version: "v2.41.0",
    date: "June 2026",
    items: [
      "<strong>v2.41.0 &mdash; Your synced calendar now heals itself: a reconcile sweep so a dropped update can&rsquo;t leave it silently stale.</strong> SmartSpa mirrors your booking calendar (Acuity) in real time over a webhook &mdash; every booking, cancel, and no-show flows in the instant it happens. But a webhook can be missed (your calendar&rsquo;s servers hiccup, ours restarts), and a <em>silently</em> stale calendar is the worst kind of broken &mdash; everything looks fine while you quietly miss a cancellation. This release adds the safety net: a background sweep that, every few hours, re-pulls a recent window of your real calendar and reconciles it against what SmartSpa has &mdash; catching anything the live feed dropped. It&rsquo;s idempotent (re-checking the same appointment changes nothing) and it only acts on a <em>genuine</em> change versus what&rsquo;s stored, so it never double-handles something the live feed already caught &mdash; but if it finds a cancellation the webhook missed, it still kicks off the same rescue and reliability updates, so a missed cancel still becomes a filled slot. Your calendar goes from &ldquo;hope the webhook fired&rdquo; to &ldquo;guaranteed to catch up.&rdquo; <strong>New</strong>: <code>reconcileAcuityForConnection</code> (reuses the existing Acuity pull + upsert + patient match) and the secret-gated <code>/api/cron/acuity-reconcile</code> sweep (a new fleet job). <strong>Touched</strong>: <code>emma-scheduler.functions</code>.",
    ],
  },
  {
    version: "v2.40.0",
    date: "June 2026",
    items: [
      "<strong>v2.40.0 &mdash; See exactly <em>who</em> is flagged for cancellation history, by name.</strong> On the A-list rules page, the &ldquo;Exclude cancellation history&rdquo; summary told you <em>how many</em> patients were flagged (e.g. &ldquo;103 flagged · 151 cancels / 1 no-show&rdquo;) but not who. Now there&rsquo;s a <strong>&ldquo;View the N flagged patients&rdquo;</strong> link that opens the actual list &mdash; each patient by name with their last-6-months cancels and no-shows, worst first. You&rsquo;ll recognize your problem clients instantly, which is the fastest way to confirm the rule is doing the right thing (most owners know their flaky regulars by heart). It&rsquo;s a read-only drill-in &mdash; opening it never changes the rule. <strong>Touched</strong>: the A-list rules page (joins the flagged reliability rows to patient names + a collapsible list).",
    ],
  },
  {
    version: "v2.39.0",
    date: "June 2026",
    items: [
      "<strong>v2.39.0 &mdash; Your no-show rule can now apply to reliability scoring, not just reschedule nudges.</strong> Closing the Reschedule arc. On the No-show definition card there&rsquo;s a new opt-in: <strong>&ldquo;Apply to reliability scoring.&rdquo;</strong> Turn it on and a <em>late cancel</em> (a cancellation given inside your required-notice window) counts as a no-show in a patient&rsquo;s reliability too &mdash; so a chronic <em>late-canceller</em>, not just a chronic no-shower, can reach the <em>in-recovery</em> tier (which can trigger your deposit policy). It&rsquo;s off by default and opt-in by design, because it changes how a patient is <em>judged</em> &mdash; we never flip that silently. Off = exactly today&rsquo;s behavior (only an outright no-show counts). The reclassification is additive and uses the same one no-show rule everywhere, so the definition you set is the definition that&rsquo;s applied &mdash; no second place to configure, no drift. (Your raw cancellation counts and grace display are left as-is; this affects the reliability tier that gates deposits.) Takes effect as reliability recomputes &mdash; on each patient&rsquo;s next status change and the daily sweep. <strong>New</strong>: migration <code>v2_39_0_noshow_rule_reliability</code> (<code>noshow_rule_applies_reliability</code> opt-in flag). <strong>Touched</strong>: <code>emma-reliability</code> (reclassifies late cancels via the shared <code>classifyAppointmentOutcome</code> when opted in), <code>emma-appointments</code> (carries the flag), the No-show definition card (the toggle).",
    ],
  },
  {
    version: "v2.38.0",
    date: "June 2026",
    items: [
      "<strong>v2.38.0 &mdash; Reschedule: nudged patients stay visible instead of vanishing.</strong> A UX fix. Before, drafting rebook nudges made those patients disappear from the page (the dedup, so you never text someone twice) &mdash; correct, but it could leave the list looking empty with no sense of what happened. Now they move into a greyed <strong>&ldquo;Recently nudged&rdquo;</strong> section, each showing when it was drafted, so you keep continuity and always see who&rsquo;s been reached. The actionable <strong>&ldquo;to win back&rdquo;</strong> count and the Draft button now reflect only the not-yet-nudged patients, and the empty state distinguishes &ldquo;nobody to win back&rdquo; from &ldquo;all caught up &mdash; everyone&rsquo;s been nudged.&rdquo; Already-nudged patients are still never re-texted. <strong>Touched</strong>: <code>reschedule.functions</code> (surfaces a <code>nudgedAt</code> per patient instead of hiding them; the drafter skips them), the Reschedule page (split list + greyed section).",
    ],
  },
  {
    version: "v2.37.0",
    date: "June 2026",
    items: [
      "<strong>v2.37.0 &mdash; Reschedule Reminders now earns its keep: a $5 win when a nudged patient rebooks.</strong> When a patient you nudged through Reschedule Reminders comes back and books &mdash; whether you book them or they self-book from the link &mdash; SmartSpa records a <strong>reschedule booking</strong> win, the same flat $5 as every other meter. It&rsquo;s credited only when the rebooking lands within 30 days of the nudge, and it&rsquo;s <em>provisional</em> until your books confirm the patient actually paid &mdash; so, exactly like recall and cross-sell, it only ever bills on a <em>verified</em> win, never on a hopeful one. No double-billing: a rebooking that&rsquo;s also a cross-sell stays a single $5 win (one event per appointment). Nothing about your pricing changes &mdash; this just lights up the meter that was always part of &ldquo;free + $5 per win&rdquo; for this Solution. <strong>New</strong>: migration <code>v2_37_0_reschedule_agent</code> (adds <code>reschedule</code> to the recovery-agent set), <code>reschedule_booking</code> joins the billable-metric taxonomy at $5, and <code>recordRescheduleWinIfNudged</code> in both booking paths. <strong>Touched</strong>: <code>billing-metrics</code>, <code>emma-attribution</code> (the agent type), <code>scheduling-owner</code> + <code>scheduling</code> (the booking hooks).",
    ],
  },
  {
    version: "v2.36.0",
    date: "June 2026",
    items: [
      "<strong>v2.36.0 &mdash; Reschedule Reminders now hides anyone who already came back.</strong> A correctness fix that makes the win-back list real. Before, the list only skipped patients with a <em>future</em> booking &mdash; so someone who cancelled on the 26th but already <em>attended</em> a visit on the 1st could still show up as &ldquo;to win back,&rdquo; even though they&rsquo;d clearly rescheduled. Now a patient drops off the list the moment they have <em>any</em> real appointment dated after the cancel or no-show we&rsquo;d nudge them about &mdash; a future booking they made <em>or</em> a past visit they already showed up for. Only the patients who genuinely haven&rsquo;t come back remain. <strong>Touched</strong>: <code>reschedule.functions</code> (the &ldquo;already rescheduled&rdquo; suppression now compares each patient&rsquo;s most recent good appointment against the outcome date, instead of only checking for a future booking).",
    ],
  },
  {
    version: "v2.35.0",
    date: "June 2026",
    items: [
      "<strong>v2.35.0 &mdash; Reschedule Reminders goes live: draft a warm rebook nudge to everyone who cancelled or no-showed.</strong> Part 2 builds on last release&rsquo;s no-show definition. There&rsquo;s a new <strong>Reschedule</strong> tab in Refill with its own page: it lists the recent cancels and no-shows you chose to follow up &mdash; each classified by <em>your</em> notice rule, one row per patient &mdash; and a <strong>Draft rebook nudges</strong> button composes a warm &ldquo;we&rsquo;d love to get you back on the calendar&rdquo; message for each and sends the batch to your iMessage proxy inbox, where you review and tap Send. Nothing goes to a patient on its own. Honest and careful by construction: it never nudges someone who has <em>already rebooked</em> (a future appointment), never double-nudges (anyone messaged in your look-back window is held back), respects opt-outs and the global sending pause, and only counts patients it can actually reach. The <strong>Reschedule Reminders</strong> Skill is now live on your Skills page &mdash; adopt it to turn the routine on and jump straight to the page; On/Pause is the real engine switch. (This renames and replaces the old &ldquo;No-Show Follow-Up&rdquo; coming-soon tile &mdash; the last of the four.) <strong>New</strong>: <code>listRescheduleTargets</code> + <code>draftRescheduleNudges</code> (reusing the same human-gated draft-to-proxy pipeline as Recall and offer-push), the <code>/app/refill/recovery/reschedule</code> page, and a Reschedule sub-tab. <strong>Touched</strong>: <code>skill-catalog</code> (Reschedule Reminders → live), <code>skills.functions</code> (the On/Pause gate), the no-show settings card (now links to the page).",
    ],
  },
  {
    version: "v2.34.0",
    date: "June 2026",
    items: [
      "<strong>v2.34.0 &mdash; Reschedule Reminders, part 1: define a no-show on <em>your</em> terms, and see what it means.</strong> Groundwork for winning back the patients who cancel or no-show. The heart of it is a distinction you control: a <strong>cancellation</strong> is notice given before the appointment <em>and</em> at or beyond your required window; a <strong>no-show</strong> is no notice &mdash; or a &ldquo;cancellation&rdquo; that lands inside your window (a late cancel, which now counts as a no-show). On your Reminders page there&rsquo;s a new <strong>&ldquo;No-show definition &amp; Reschedule Reminders&rdquo;</strong> card: set your required-notice window (e.g. 24h), choose whether to follow up cancels, no-shows, or both, and flip the whole thing on or off. The card shows a <strong>live read on your real data</strong> &mdash; &ldquo;in the last 30 days, <em>N</em> of your <em>M</em> cancellations count as no-shows by your 24h rule&rdquo; &mdash; so the rule is concrete, not abstract. Honest by construction: a cancellation we can&rsquo;t time (no cancellation timestamp on record, as with some imported history) is left as a cancel and <em>never</em> branded a no-show. Nothing is sent yet &mdash; this release is the definition and the mirror; the rebook-nudge drafting lands next. Under the hood this no-show rule is built as one canonical primitive, so a later release can apply it spa-wide (to reliability scoring and grace credits) without a rewrite. <strong>New</strong>: migration <code>v2_34_0_reschedule_reminders</code> (<code>cancelled_at</code> on appointments, backfilled from the status-event audit trail; the notice rule + targeting columns on your no-show policy), a pure <code>classifyAppointmentOutcome</code> classifier, <code>getRescheduleClassificationPreview</code>, and the settings card. <strong>Touched</strong>: <code>emma-appointments</code> (writes <code>cancelled_at</code> on cancel; carries the new policy fields), the Reminders page (mounts the card).",
    ],
  },
  {
    version: "v2.33.0",
    date: "June 2026",
    items: [
      "<strong>v2.33.0 &mdash; Weekly Offer (&ldquo;Tox Tuesday&rdquo;) goes live as a Skill &mdash; a standing weekly special you set up once.</strong> Building on last release&rsquo;s weekly-cap engine, the <strong>Weekly Offer</strong> Skill is now adoptable from your Skills page. Adopt it and it opens your offers page, where authoring a recurring special is now a one-step thing: pick the day it runs (e.g. Tuesdays) and an optional per-week limit, and it badges that service at booking <em>every</em> week on its own &mdash; with the limit resetting each week, so &ldquo;20 per Tox Tuesday&rdquo; gives a fresh 20 every Tuesday. No re-creating it Monday morning. The On/Pause is the honest master switch for your weekly offers: <em>Pause</em> takes every weekly offer off booking at once, <em>On</em> brings them back &mdash; it really flips them, it&rsquo;s not a cosmetic toggle. The offer form now says what it&rsquo;s doing: choose a weekday and the limit field relabels to &ldquo;Limit / week&rdquo; and tells you it resets weekly, so there&rsquo;s no guessing whether the cap is lifetime or per-week. This is the third <em>routine</em>-style Skill in the Promos solution, and it closes the third of the four Skill-funnel &ldquo;coming soon&rdquo; tiles. <strong>New</strong>: <code>setSpaWeeklyOffersActive</code> (the master-switch helper). <strong>Touched</strong>: <code>skill-catalog</code> (Weekly Offer &rarr; live routine, deep-links your offers page), <code>skills.functions</code> (adopt + On/Pause flip the weekly offers), <code>SpaOffersCard</code> (a chosen weekday now authors a weekly-resetting offer, with honest per-week cap labelling).",
    ],
  },
  {
    version: "v2.32.0",
    date: "June 2026",
    items: [
      "<strong>v2.32.0 &mdash; Offer caps can now reset every week &mdash; the engine groundwork for a recurring &ldquo;Tox Tuesday&rdquo; offer.</strong> Your spa-authored offers already support a weekday window (run only on Tuesdays) and a redemption limit (&ldquo;first 20 patients&rdquo;). Until now that limit was <em>lifetime</em>: once it filled, the offer was done forever. This release teaches the cap to recur. An offer&rsquo;s cap can now be <em>weekly</em> &mdash; it counts only <em>this week&rsquo;s</em> redemptions and resets on its own at the start of each week, so &ldquo;20 per Tox Tuesday&rdquo; gives a fresh 20 every Tuesday with no re-creating it. The reset is lazy and lock-safe: the same atomic increment that already prevents two simultaneous bookings from miscounting now also notices a new week and resets the count inside the row lock &mdash; no nightly job, no race. The week boundary (Monday) is computed in <em>your</em> timezone so it flips at your local week-start, not UTC. Existing offers and every manufacturer promo are untouched &mdash; they default to the old <em>lifetime</em> behavior exactly as before. This is the engine half; the Skill and the authoring toggle that let you turn it on land next. <strong>New</strong>: migration <code>v2_32_0_weekly_offer_cap</code> (<code>cap_period</code> + <code>cap_period_start</code> on the offers table, and a week-aware <code>increment_offer_redemption</code> RPC), plus pure <code>mondayOf</code> / <code>effectiveRedeemedCount</code> helpers so the at-booking matcher and the database agree on the reset. <strong>Touched</strong>: <code>promo-calendar</code> (week-aware <code>isActive</code>), <code>refill-promo-calendar.functions</code> (reads the new columns, passes the week start to the increment).",
    ],
  },
  {
    version: "v2.31.0",
    date: "June 2026",
    items: [
      "<strong>v2.31.0 &mdash; Monthly Patient-Book Export goes live as a Skill: your patient book, emailed to you every month for bookkeeping.</strong> Adopt the <strong>Monthly Patient-Book Export</strong> Skill and once a month SmartSpa emails you a CSV of your full patient book &mdash; no manual download each month. It goes to <em>you</em> only; nothing is ever sent to a patient (it&rsquo;s your own data, in the honest-exit spirit &mdash; so it&rsquo;s deliberately <em>not</em> affected by the patient-sending pause). On/Pause genuinely starts and stops the monthly send, and Manage opens your account page where you can pull a full export (with appointments and waitlist) any time. Honest by construction: a spa with an empty book gets no email, an owner with no email on file is skipped, and a 25-day dedup guard makes the job safe to re-run. The monthly export reuses the <em>exact</em> same roster as the on-demand account-page export &mdash; one definition, no drift. This is the second <em>routine</em>-style Skill in the Account solution to ship. <strong>New</strong>: migration <code>v2_31_0_patient_export</code> (<code>patient_export_enabled</code> + <code>patient_export_last_sent_at</code>), <code>sendPatientBookExportForUser</code> + the <code>/api/cron/patient-export</code> monthly job, and a factored-out <code>gatherSpaDataExport</code> shared by the page and the cron. <strong>Touched</strong>: <code>data-export</code> (the gather split + the emailer), <code>skill-catalog</code> (export Skill &rarr; live routine), <code>skills.functions</code> (the On/Pause gate).",
    ],
  },
  {
    version: "v2.30.0",
    date: "June 2026",
    items: [
      "<strong>v2.30.0 &mdash; Auto-Verify Recoveries goes live as a Skill &mdash; the first <em>flip</em>-style Skill on your Skills page.</strong> SmartSpa already reconciles your recovered-revenue wins against your books automatically (matching each patient&rsquo;s transaction inside your verification window and confirming it), so you don&rsquo;t confirm every win by hand. Until now that lived only deep in Attribution settings. <strong>Auto-Verify Recoveries</strong> brings it up onto your Skills page as a Skill you own: <em>On</em> keeps auto-reconciliation running, <em>Pause</em> stops it and sends wins back to your manual confirm queue, and <em>Manage</em> opens Attribution to tune the window and the auto-confirm threshold. The On/Pause is the real switch &mdash; it flips the same master toggle the nightly reconciliation honors, not a cosmetic record. (It&rsquo;s on by default, so adopting it just puts the knob in your hands; nothing changes until you pause it.) This is the fourth kind of Skill to ship: a <em>flip</em> &mdash; turning an existing engine knob into something you own and see, alongside the routine, surface, and autonomous kinds already live. <strong>New</strong>: <code>setAttributionEnabled</code> (server-side master-toggle flip). <strong>Touched</strong>: <code>skill-catalog</code> (Auto-Verify → live, deep-links Attribution), <code>skills.functions</code> (adopt + On/Pause flip the attribution toggle).",
    ],
  },
  {
    version: "v2.29.0",
    date: "June 2026",
    items: [
      "<strong>v2.29.0 &mdash; Recovery events that never matched now retire on their own &mdash; so you&rsquo;re never billed for a stale one months later.</strong> Closing a fairness gap in the money trail. When an agent saves an appointment, SmartSpa logs a <em>provisional</em> recovery and the daily reconciliation tries to match it to a real transaction inside your verification window (72h by default). If a match never lands, that provisional used to sit unverified <em>forever</em> &mdash; quietly re-checked on every pass, and worse, a transaction imported months later could still match it and <strong>bill you for a months-old recovery</strong>. Now, once a provisional has gone unmatched past its window (plus a safety buffer, with a 30-day floor), reconciliation retires it: it stops being re-checked and can never late-match into a surprise charge. <strong>What&rsquo;s protected:</strong> a recovery that&rsquo;s already matched and waiting for your one-tap review is <em>never</em> retired &mdash; only the truly dead ones are. And nothing about billing changes: a recovery only ever bills once <em>you</em> (or a confirmed match) verify it, so a retired one was never going to bill anyway. If you ever want to claim a retired recovery, confirming it manually still works and clears the retirement. The reconciliation summary now reports how many it retired each run. <strong>New</strong>: migration <code>v2_29_0_recovery_event_expiry</code> (<code>expired_at</code> + <code>expiry_reason</code> on <code>emma_recovery_events</code>, plus a partial index that keeps the daily scan fast as dead rows accumulate). <strong>Touched</strong>: <code>emma-attribution</code> (skip-expired scan + the retire sweep + manual-confirm clears the flag), <code>api.cron.emma-reconcile</code> (skips spas whose only unverified rows are already retired; reports an <code>expired</code> tally).",
    ],
  },
  {
    version: "v2.28.0",
    date: "June 2026",
    items: [
      "<strong>v2.28.0 &mdash; One opt-out rule, everywhere: a patient who says stop is never messaged by any agent.</strong> Hardening the compliance layer. &ldquo;Banned&rdquo; and &ldquo;opted-out&rdquo; are now read through a single shared rule (<code>patient-contactability</code>) that every outbound path consults &mdash; closing two real gaps a fresh-walk audit surfaced: <strong>Recall</strong> had <em>no</em> opt-out check at all (an opted-out patient could appear in a recall list, the weekly digest dollars, and the drafts), and <strong>campaign blasts</strong> caught a STOP-reply opt-out but missed a spa-flagged one (someone you marked opted-out without a STOP text could still get a blast). Recall now drops banned/opted-out patients <em>at the source</em>, so they vanish from the list, the digest totals, and the drafts at once. Pre-show reminders now run through the same shared rule too. (Rescue already filtered opt-outs correctly &mdash; verified, not changed.) <strong>New</strong>: <code>patient-contactability.ts</code> (<code>patientOptOutStatus</code> + batch <code>loadBlockedNodeIds</code>). <strong>Touched</strong>: <code>refill-recall</code> (source-level filter), <code>emma-blast</code> (the missed flag), <code>emma-preshow</code> (now shares the rule).",
    ],
  },
  {
    version: "v2.27.0",
    date: "June 2026",
    items: [
      "<strong>v2.27.0 &mdash; Autonomous Rescue: the agent earns the right to act on its own (Tier-2 Autonomous &middot; Slice 4 &mdash; the payoff).</strong> The fourth and final kind of Skill goes live: <em>autonomous</em> &mdash; one the agent <em>runs</em>, not just one you switch on. <strong>Autonomous Rescue</strong> lets SmartSpa text a confident waitlist match the instant a slot frees, with no tap from you. The point is <em>how you get there</em>: it&rsquo;s earned, not default. By design, rescue now <strong>holds every confident match for your one-tap OK</strong> in the review queue &mdash; that queue is the training ground. Each offer you approve is a vote of confidence in the agent&rsquo;s judgment, recorded in the action ledger; once you&rsquo;ve approved a handful, the Autonomous Rescue Skill <em>unlocks</em> on your Skills page (you&rsquo;ll see your progress, e.g. &ldquo;2/3 so far&rdquo;). Adopt it and confident offers start sending on their own &mdash; while uncertain (&ldquo;blind&rdquo;) matches keep waiting for you, always. The scariest version &mdash; an agent blasting people you never vetted &mdash; is impossible by construction. And everything from the last three releases is what makes this safe: the agent acts with a <strong>memory</strong> (the ledger), a <strong>governor</strong> (the frequency cap), a <strong>brake</strong> (pause-all), and now an <strong>earned licence</strong>. <em>Nothing changes for spas already running rescue</em> &mdash; you&rsquo;re grandfathered in as autonomous. <strong>New</strong>: migration <code>v2_27_0_rescue_autonomous</code> (<code>rescue_autonomous_enabled</code> + grandfather backfill), the <code>autonomous_rescue</code> catalog Skill, <code>getAutonomyRung</code> + the earn gate. <strong>Touched</strong>: <code>emma-rescue</code> (the autonomy gate + logs each held-offer approval), <code>skills.functions</code> + the Skills page (the earned card).",
    ],
  },
  {
    version: "v2.26.0",
    date: "June 2026",
    items: [
      "<strong>v2.26.0 &mdash; One switch to pause everything: the honest-pause kill switch (Tier-2 Autonomous &middot; Slice 3).</strong> The third and final safety rail under autonomy &mdash; the brake you can always reach. A new <strong>&ldquo;Pause all sending&rdquo;</strong> control at the top of your Skills page stops <em>every</em> automated message at once: rescue texts, appointment reminders, and the weekly recall digest. It sits above every individual on/off &mdash; flip it and a paused spa sends nothing to anyone until you resume, no matter what each engine&rsquo;s own setting says. One tap turns a calm &ldquo;sending is active&rdquo; row into a clear amber &ldquo;all sending is paused&rdquo; banner; one tap brings it back. Every flip is written to the immutable agent-action ledger (who paused, when), so the brake is as auditable as the sends it governs. It&rsquo;s deliberately <em>not</em> locked behind earning Skills &mdash; a safety control has to be reachable the moment you want it. With this, the autonomous tier now has all three rails it needs before an agent ever sends on its own: a <strong>memory</strong> (the action ledger, v2.24.0), a <strong>governor</strong> (the cross-agent frequency cap, v2.25.0), and now a <strong>brake</strong> (this). <strong>New</strong>: migration <code>v2_26_0_sending_pause</code> (<code>sending_paused</code> on the policy row), <code>sending-pause.ts</code>, <code>getSendingPaused</code>/<code>setSendingPaused</code>. <strong>Touched</strong>: <code>emma-rescue</code> + <code>emma-preshow</code> + the recall-digest cron (all poll the switch at dispatch) &middot; the Skills page (the control). Next: the explicit, <em>earned</em> Autonomous Rescue Skill &mdash; the payoff these rails make safe.",
    ],
  },
  {
    version: "v2.25.0",
    date: "June 2026",
    items: [
      "<strong>v2.25.0 &mdash; The cross-agent frequency cap: no patient gets texted twice in a day, even by two different agents (Tier-2 Autonomous &middot; Slice 2).</strong> The second safety rail under autonomy. Your rescue agent and your pre-show reminder agent are separate engines that don&rsquo;t know about each other &mdash; so without a shared brake, a patient could get a &ldquo;your appointment is tomorrow&rdquo; reminder in the morning and a &ldquo;a slot just opened&rdquo; rescue blast in the afternoon. Now every patient text writes a per-patient row to the <code>agent_actions</code> ledger (the shared memory we built in v2.24.0), and before the <strong>rescue agent</strong> auto-texts anyone, it checks that ledger: if <em>any</em> agent already reached that patient by SMS in the last ~20 hours, it <em>yields</em> &mdash; the redundant blast is held and recorded as a suppression (an honest audit of a thing the agent decided <em>not</em> to do), and a later sweep re-evaluates once the window passes. The asymmetry is deliberate: a <strong>pre-show reminder</strong> always counts toward the tally but is never itself suppressed &mdash; we never silence a reminder for an appointment a patient actually has (that&rsquo;s the part that reduces no-shows). The cap exists to stop redundant outreach from stacking, not to drop the messages that matter. <strong>New</strong>: <code>frequency-cap.ts</code> (<code>checkRecentContact()</code>, fails open so a telemetry hiccup never blocks a real send). <strong>Touched</strong>: <code>emma-rescue.functions.ts</code> (reads the cap + yields, now logs one ledger row per patient texted) &middot; <code>emma-preshow.functions.ts</code> (contributes to the tally). Next: a one-tap &ldquo;pause all sending&rdquo; kill switch, then the explicit, <em>earned</em> Autonomous Rescue Skill.",
    ],
  },
  {
    version: "v2.24.0",
    date: "June 2026",
    items: [
      "<strong>v2.24.0 &mdash; The agent action ledger: every autonomous send is now an immutable, dispute-proof record (Tier-2 groundwork).</strong> The first piece of the autonomous-Skills tier &mdash; the trust spine that has to exist <em>before</em> an agent ever sends on its own. A new append-only <code>agent_actions</code> table records one immutable row per action an agent takes on your behalf: what it did, for whom, on which channel, at what confidence, and crucially whether the <em>agent</em> initiated it autonomously or <em>you</em> did. (Distinct from the existing delivery heartbeat, which is a mutable per-channel freshness signal &mdash; this is a permanent, per-action history that&rsquo;s never overwritten.) Wired into the two places the system already acts on its own: the <strong>Weekly Recall Digest</strong> send and the <strong>rescue direct-mode text</strong> (the one truly-autonomous patient send today). It changes nothing you can see yet &mdash; it&rsquo;s the foundation the next slices ride on (a per-patient/cross-agent frequency cap so no one gets texted twice in a day, a one-tap &ldquo;pause all sending&rdquo; kill switch, then the explicit, <em>earned</em> Autonomous Rescue Skill). <strong>New</strong>: migration <code>v2_24_0_agent_actions</code>, <code>recordAgentAction()</code> in <code>messaging-activity.ts</code>. <strong>Touched</strong>: <code>refill-recall.functions.ts</code> + <code>emma-rescue.functions.ts</code> (both now log to the ledger, best-effort). Full design: <code>~/Desktop/SmartSpa-Tier2-Autonomous-Scope-v1.html</code>.",
    ],
  },
  {
    version: "v2.23.0",
    date: "June 2026",
    items: [
      "<strong>v2.23.0 &mdash; Reward-Expiry Sweep goes live as an ambient badge &mdash; the funnel&rsquo;s new <em>surface</em> Skill.</strong> A fourth Skill is now adoptable, and it&rsquo;s a new <em>kind</em>: instead of flipping a flag or running a routine, it turns on an in-app <strong>surface</strong>. Adopt <strong>Reward-Expiry Sweep</strong> and a live count of manufacturer rewards expiring soon gets pinned right on your <strong>Recall</strong> tab &mdash; the money about to evaporate is always one glance away, all week, with <em>no extra email</em>. <strong>Honest by construction</strong>: we deliberately did <em>not</em> ship &ldquo;another expiring-rewards email&rdquo; (the Weekly Recall Digest already covers that &mdash; a second one would be redundant noise). This is a genuinely different <em>channel</em> &mdash; ambient and glanceable vs. the weekly digest &mdash; and it&rsquo;s gated: adopting shows the badge, pausing hides it. <strong>New</strong>: <code>surface</code> materializer (migration <code>v2_23_0_skill_surface_materializer</code> widens the constraint; 4th kind on the funnel&rsquo;s materializer axis), <code>getRewardExpiryBadge</code> + <code>RecallExpiryBadge</code> (shares one &ldquo;expiring&rdquo; definition with Recall). <strong>Touched</strong>: <code>skill-catalog.ts</code> (Reward-Expiry Sweep &rarr; live), <code>refill-recall.functions.ts</code>, <code>RecognitionTabs.tsx</code>. <em>Next</em>: the autonomous (Tier-2) tier, still gated on the agent-bundle sign/notarize + MFA externals.",
    ],
  },
  {
    version: "v2.22.2",
    date: "June 2026",
    items: [
      "<strong>v2.22.2 &mdash; Recall emails now use your real spa name, not &ldquo;Your spa.&rdquo;</strong> The Weekly Recall Digest (and the recall iMessage-draft email) were falling back to a generic &ldquo;Your spa&rdquo; because they looked the spa name up the wrong way (<code>node_type='spa_profile'</code> instead of the canonical <code>context='spa-profile'</code>/<code>lookup_key='spa-name'</code> node). Both now call the shared <code>getSpaName()</code> resolver, so the subject and body read your actual practice name. Small thing, big difference in how the email <em>feels</em> &mdash; craft is the moat. <strong>Touched</strong>: <code>refill-recall.functions.ts</code> (digest + dispatch both use <code>getSpaName</code>). No migration.",
    ],
  },
  {
    version: "v2.22.1",
    date: "June 2026",
    items: [
      "<strong>v2.22.1 &mdash; Recall page now shows when the Weekly Digest is on.</strong> Small honesty touch: once you&rsquo;ve adopted the <strong>Weekly Recall Digest</strong> Skill, the Recall page shows a quiet line confirming it &mdash; <em>&ldquo;Weekly Recall Digest is on. Your first weekly heads-up emails you Monday,&rdquo;</em> or the date the last one went out. The routine&rsquo;s state is visible right where you act on it, so there&rsquo;s never a question of whether it&rsquo;s running. <strong>Touched</strong>: <code>refill-recall.functions.ts</code> (<code>computeRecallView</code> now returns <code>digestEnabled</code> + <code>digestLastSentAt</code>), <code>app.refill.recognition.recall.tsx</code> (the status line). No migration.",
    ],
  },
  {
    version: "v2.22.0",
    date: "June 2026",
    items: [
      "<strong>v2.22.0 &mdash; Weekly Recall Digest: the first Skill that runs on a schedule and reports back.</strong> &ldquo;Auto-Recall&rdquo; is now a real, adoptable Skill &mdash; and an honest one. Once a week, SmartSpa tallies who&rsquo;s due back (expiring manufacturer rewards + lapsed regulars &mdash; the exact same math the Recall page shows) and emails you a heads-up: <em>the dollars on the table and how many patients</em>, broken down by trigger, with a one-tap link into Recall. <strong>It never messages a patient on its own</strong> &mdash; the digest is a nudge to come review; the sending stays one-click and consented on the Recall page. That boundary is deliberate: a routine can surface and summarize for you, but a blue-bubble to a patient still wants your tap (the autonomous tier is a separate, gated step). <strong>Honest by construction</strong>: a spa with nobody to recall gets <em>no</em> email (no noise), and adopting the Skill / its On&middot;Pause genuinely flips the engine &mdash; <code>recall_digest_enabled</code> on your policy is what the weekly job reads. <strong>New</strong>: <code>api.cron.recall-digest.ts</code> (secret-gated weekly cron), <code>computeRecallView</code> + <code>composeRecallDigestEmail</code> + <code>sendRecallDigestForUser</code> in <code>refill-recall.functions.ts</code>, migrations <code>v2_22_0_recall_digest</code> (gate columns) + <code>_cron</code> (pg_cron registration). <strong>Touched</strong>: <code>skill-catalog.ts</code> (Auto-Recall &rarr; live, now &ldquo;Weekly Recall Digest&rdquo;), <code>skills.functions.ts</code> (<code>recall_digest_enabled</code> in the engine gate). <em>Next</em>: the autonomous tier (agent sends with confidence-gating) &mdash; gated on the agent-bundle sign/notarize + MFA track.",
    ],
  },
  {
    version: "v2.21.0",
    date: "June 2026",
    items: [
      "<strong>v2.21.0 &mdash; Skills: concierge can hand-author a suggestion (the third source).</strong> When our team is co-driving a spa's onboarding, we can now turn what we heard in the conversation straight into product. Viewing a spa as operator (persona switcher), the Skills page shows an <strong>&ldquo;Operator · suggest a Skill for this spa&rdquo;</strong> panel: pick a live Skill, add a short tailored note (&ldquo;You mentioned a lot of last-minute cancellations&hellip;&rdquo;), and send &mdash; it lands in <em>that</em> spa's &ldquo;Suggested for you&rdquo; queue on the <em>exact same rails</em> she'd see a mined suggestion, marked &ldquo;From your SmartSpa team.&rdquo; She still adopts or dismisses it herself &mdash; concierge is a setting, never a separate track. That completes the funnel's <strong>source axis: catalog &rarr; mined &rarr; concierge</strong>. Honest by construction: only <em>live</em> Skills are suggestable, never one the spa already has, and only a real operator (admin viewing-as) can author. <strong>New</strong>: <code>createConciergeProposal</code> in <code>skills.functions.ts</code> (+ <code>listSuggestedSkills</code> now merges persisted pending proposals with mined ones; adopting clears any pending proposal), <code>OperatorAuthorPanel</code> + source marker in <code>app.refill.skills.tsx</code>. No migration. <em>Next</em>: net-new custom templates + the autonomous tier both need an engine behind them (Tier-2, gated on the agent-bundle sign/notarize + MFA track).",
    ],
  },
  {
    version: "v2.20.0",
    date: "June 2026",
    items: [
      "<strong>v2.20.0 &mdash; Skills now suggests itself: &ldquo;Suggested for you,&rdquo; mined from your own activity.</strong> The Skills page can now <em>notice</em> when a routine would help and offer it &mdash; the Ownership Flywheel's mining source. It reads signals you already generate (upcoming appointments with no reminder cadence set up; a waitlist building while rescue is off) and surfaces the matching Skill at the top of the page with the reason in plain language &mdash; <em>&ldquo;You have 12 upcoming appointments but no reminder cadence set up yet,&rdquo;</em> &mdash; one tap to add, or dismiss it and we won't bring it up again. <strong>Honest by construction</strong>: it only ever suggests a Skill that's actually live and that you haven't already added, and it only fires on a real gap &mdash; a fully-set-up spa (reminders on, rescue on) correctly sees <em>nothing</em>. Same earned-gate as the rest of Skills (nothing before your first verified win). <strong>New</strong>: <code>listSuggestedSkills</code> + <code>dismissSuggestion</code> in <code>skills.functions.ts</code> (read-only telemetry mirror of the recommendation engine; a dismissal is remembered as a dismissed <code>skill_proposals</code> row), &ldquo;Suggested for you&rdquo; section + <code>SuggestionCard</code> in <code>app.refill.skills.tsx</code>; <code>adoptSkill</code> now records the proposal <code>source</code> ('catalog' vs 'mined'). No migration. <em>Next</em>: concierge/custom authoring (source 3) and the autonomous tier.",
    ],
  },
  {
    version: "v2.19.1",
    date: "June 2026",
    items: [
      "<strong>v2.19.1 &mdash; Skills: Waitlist Auto-Fill goes live, and On/Pause now really starts &amp; stops the routine.</strong> A second Skill is now adoptable for real: <strong>Waitlist Auto-Fill</strong> &mdash; adopting it turns on your Rescue routine (fill a canceled slot from the best-fit waitlist patient; low-confidence fits held for your one-tap OK) and deep-links you into Rescue to set the eligible treatments + limits. And the <strong>On / Pause toggle on a wired Skill is now genuinely wired to the engine</strong>: pausing Pre-Visit Reminders or Waitlist Auto-Fill actually stops that agent from dispatching (it flips <code>preshow_enabled</code> / <code>rescue_enabled</code> on your no-show policy &mdash; the same switch the Reminders/Rescue pages use), not just a cosmetic record flag. <strong>Honesty note</strong>: Auto-Verify Recoveries stays <em>Coming soon</em> on purpose &mdash; the reconciliation already runs against whatever transactions flow in, so there's no real on/off for you to &ldquo;turn on,&rdquo; and we won't ship a toggle that does nothing. <strong>Touched</strong>: <code>skill-catalog.ts</code> (Waitlist Auto-Fill &rarr; live), <code>skills.functions.ts</code> (engine-gate wiring on adopt + toggle). No migration.",
    ],
  },
  {
    version: "v2.19.0",
    date: "June 2026",
    items: [
      "<strong>v2.19.0 &mdash; Your Skills: the routines SmartSpa runs for you, that you own.</strong> A new <strong>Skills</strong> tab (in the nav, after Refill) is the start of the Ownership Flywheel &mdash; a curated catalog of automations you can adopt with one tap, make yours, and turn off any time. Phase 1 wires the first one end-to-end: adopt <strong>Pre-Visit Reminders</strong> and it sets up your default reminder cadence, then deep-links you into Reminders to fine-tune the timing, tone, and wording. The rest of the catalog (Auto-Recall, Waitlist Auto-Fill, Weekly Offer, Reward-Expiry Sweep, No-Show Follow-Up, Auto-Verify Recoveries, Monthly Export) is shown so you can see what's coming, but clearly marked <em>Coming soon</em> &mdash; we won't let you &ldquo;add&rdquo; a Skill that doesn't do anything yet. Like the wishlist, Skills is <strong>earned</strong>: it stays out of the way until your first verified win, then unlocks. Removing a Skill never deletes the routine it set up &mdash; leaving is clean by design. <strong>New</strong>: <code>skills.functions.ts</code> (<code>listMySkills</code> / <code>adoptSkill</code> / <code>setSkillEnabled</code> / <code>removeSkill</code>), <code>skill-catalog.ts</code>, <code>app.refill.skills.tsx</code>, migration <code>v2_19_0_skill_funnel</code> (<code>skill_proposals</code> &rarr; <code>skills</code> lineage). <strong>Touched</strong>: <code>RefillNav.tsx</code>, <code>RefillShellChrome.tsx</code>. One gate, two pluggable axes (source: catalog&rarr;mined&rarr;concierge &middot; materializer: flip&rarr;routine&rarr;autonomous) &mdash; mining + concierge sources and the autonomous tier plug in next, no rebuild.",
    ],
  },
  {
    version: "v2.18.4",
    date: "June 2026",
    items: [
      "<strong>v2.18.4 &mdash; Concierge onboarding is now a setting, not a separate track.</strong> When our team is white-glove onboarding a spa, the owner now sees an honest banner &mdash; &ldquo;Your SmartSpa team is setting this up with you&rdquo; &mdash; so they always know a person is co-driving. The point (per the trusted-onboarding strategy): concierge and self-serve ride the <em>exact same rails</em>. The flag forks no behavior &mdash; same gates, same code paths &mdash; it only adds transparency, which is the whole moat: the owner should always know who&rsquo;s driving, and can take the wheel any time. Admin flips <code>concierge_mode_enabled</code> per spa from <strong>/app/admin/agents</strong> (ON during onboarding, OFF once they self-drive). <strong>New</strong>: <code>concierge.functions.ts</code> (<code>getConciergeState</code>), <code>ConciergeBanner.tsx</code>, catalog entry in <code>feature-flag-catalog.ts</code>. <strong>Touched</strong>: <code>RefillShellChrome.tsx</code>. No migration (uses the existing feature_flags table).",
    ],
  },
  {
    version: "v2.18.3",
    date: "June 2026",
    items: [
      "<strong>v2.18.3 &mdash; The Wishlist is now an <em>earned</em> affordance.</strong> The floating Wishlist button used to appear the moment a spa logged in &mdash; but a feature wishlist before you've felt SmartSpa earn its keep is just noise. It now stays out of sight until the spa hits its first <strong>verified recovery win</strong> (real recovered revenue &mdash; the same &ldquo;first verifiable dollar&rdquo; the billing scoreboard counts). Once earned, it stays. This is the trusted-onboarding ladder's <em>wishlist-as-earned-gate</em> rung: the request channel shows up exactly when a wish is informed by real value, not before. (It also always shows for us when we're viewing a spa as an operator, so the support line is never hidden.) <strong>New</strong>: <code>hasReachedValueMoment</code> in <code>wishlist.functions.ts</code>. <strong>Touched</strong>: <code>WishlistWidget.tsx</code>. No migration. <em>Follow-on</em>: the Skill funnel (agent proposes a wish &rarr; built Skill from observed work) is an agent-side build, scoped separately.",
    ],
  },
  {
    version: "v2.18.2",
    date: "June 2026",
    items: [
      "<strong>v2.18.2 &mdash; Public Deals page no longer shows duplicate-looking offers.</strong> When a manufacturer promo calendar lists the same product across several rows (or a spa-authored offer mirrors a manufacturer promo), the public <code>/deals/&lt;slug&gt;</code> page could render two or more identical-looking lines for the same thing. It now collapses any offers that are indistinguishable in every patient-visible field (headline, type, end date, learn-more link, service), keeping the first. Purely a display tidy-up &mdash; at-booking badging and the cross-sell <code>$5</code> win path are untouched. <strong>Touched</strong>: <code>refill-promo-calendar.functions.ts</code> (<code>getPublicDealsFn</code>). No migration.",
    ],
  },
  {
    version: "v2.18.1",
    date: "June 2026",
    items: [
      "<strong>v2.18.1 &mdash; Export now reads the spa you&rsquo;re viewing (fixes &ldquo;exported 0&rdquo;).</strong> The one-click data export was resolving to the signed-in operator&rsquo;s own account instead of the spa being viewed, so an admin/owner viewing a tenant exported an empty book (header row, zero patients). The patient book, appointments, and waitlist export now forward the same <code>viewAsUserId</code> every other spa-owner data fetcher uses, so the download contains the full roster of the spa on screen. Client-only fix on <code>app.refill.settings.account.tsx</code>; the server fn already verified the viewer&rsquo;s admin role before honoring it (security boundary unchanged). No migration.",
    ],
  },
  {
    version: "v2.18.0",
    date: "June 2026",
    items: [
      "<strong>v2.18.0 &mdash; Your data is yours: one-click export + an honest way out (the leaveable rung).</strong> On <strong>Settings &rarr; Login</strong> there&rsquo;s now a <strong>&ldquo;Your data &amp; leaving&rdquo;</strong> section: download your full <strong>patient book as a CSV</strong> (names, contact, lifetime value &mdash; Excel-ready) or <strong>everything as a JSON archive</strong> (patients + appointments + waitlist, the complete operational record, paged so nothing is silently truncated). Beside it, a plain three-step path out: export your data, remove your card on Billing (and billing stops &mdash; we never charge after that), done. No phone call, no &ldquo;are you sure,&rdquo; no retention maze. Easy-to-leave de-risks the decision to start <em>and</em> is the best reason to stay &mdash; it proves SmartSpa earns its keep by being useful, not by holding your data hostage. (The billing ledger keeps its own CSV on the Billing page.) <strong>New</strong>: <code>data-export.functions.ts</code> (<code>exportSpaData</code>), &ldquo;Your data &amp; leaving&rdquo; section on <code>app.refill.settings.account.tsx</code>. Read-only; no migration.",
    ],
  },
  {
    version: "v2.17.0",
    date: "June 2026",
    items: [
      "<strong>v2.17.0 &mdash; Billing is a scoreboard, not a tax (the billing-trust rung).</strong> The top of the Billing page now leads with the <strong>value SmartSpa created for you</strong> &mdash; the wins it booked this month, and the visit revenue recovered when we know it &mdash; and shows our fee as the small cut beside it, never the hero. When the recovered revenue is known you see exactly how good the trade is: <em>&ldquo;you keep 98% of what we recovered (72&times; your fee).&rdquo;</em> Three honesties are stated plainly, because we can afford to: <strong>charged only after the patient actually pays</strong> (never on a maybe &mdash; and a new line shows how many wins are recorded-but-not-charged, waiting on the visit), <strong>you keep every dollar of the visit</strong> (the fee is a small flat amount per win, not a cut of your revenue), and <strong>turn any meter off or remove your card anytime</strong> (no lock-in). The full per-meter math + every win + CSV export are still one click away under &ldquo;the math behind it.&rdquo; <strong>No billing math changed</strong> &mdash; radical honesty means telling the truth more loudly, not charging differently. <strong>Touched</strong>: <code>refill-fee-rules.functions.ts</code> (<code>pendingCount</code> on the ledger &mdash; recorded-not-charged), <code>app.billing.index.tsx</code> (scoreboard reframe of the hero), <code>changelog.ts</code>. No migration.",
    ],
  },
  {
    version: "v2.16.1",
    date: "June 2026",
    items: [
      "<strong>v2.16.1 &mdash; Rescue gate, calibrated against real data: a flexible patient still gets the text.</strong> v2.16.0 held every fit with no explicit treatment match — but real waitlists mostly carry <em>no</em> per-patient treatment preference (a patient who just said &ldquo;text me about any opening&rdquo;). Holding those would have quietly switched rescue automation off. Corrected: a patient with no preference on a <strong>typed</strong> slot now <strong>auto-texts</strong> as before — they opted into any opening and we can honestly tell them what it&rsquo;s for. The gate now holds only the genuinely-blind case: an opening with <strong>no treatment set at all</strong>, where SmartSpa can&rsquo;t tell the patient what they&rsquo;d be coming in for. Narrow, honest, and it won&rsquo;t suppress legitimate sends. <strong>Touched</strong>: <code>emma-rescue.functions.ts</code> (tier split explicit/open/blind; hold only &ldquo;blind&rdquo;), <code>RescueReviewQueue.tsx</code> (copy matches the real reason). No migration.",
    ],
  },
  {
    version: "v2.16.0",
    date: "June 2026",
    items: [
      "<strong>v2.16.0 &mdash; Rescue won&rsquo;t auto-text on a weak match — it holds it for your OK (the confidence gate, on the autonomous send).</strong> When a slot frees, Rescue in direct mode texts your waitlist on its own — the one truly hands-off message SmartSpa sends. Now that send is gated by confidence, the same way reward attribution is. A patient is only <strong>auto-texted</strong> when their own waitlist treatment genuinely matches the freed slot. A <strong>low-confidence</strong> fit — someone on the waitlist with <em>no</em> treatment preference, or an opening with no treatment set — is <strong>held</strong> instead of texted blind, and surfaces in a new <strong>&ldquo;a few rescue texts need your OK&rdquo;</strong> queue at the top of the Rescue page. One tap sends the exact text the auto-path would have; or skip it. Nothing is lost — a held offer is one tap from going out, never a silent drop and never an auto-blast to someone who didn&rsquo;t really fit. Held offers don&rsquo;t count as &ldquo;sent&rdquo; until you send them. <strong>Touched</strong>: <code>emma-rescue.functions.ts</code> (confidence tier on fit-patients + hold in direct dispatch + <code>listHeldRescueOffers</code>/<code>sendHeldRescueOffer</code>/<code>dismissHeldRescueOffer</code>), <code>RescueReviewQueue.tsx</code>, <code>recovery.rescue.tsx</code>, migration <code>emma_rescue_offers.held_at/held_reason</code>.",
    ],
  },
  {
    version: "v2.15.0",
    date: "June 2026",
    items: [
      "<strong>v2.15.0 &mdash; Cross-sell offers only badge a real match (the confidence gate, applied).</strong> The same &ldquo;don&rsquo;t guess&rdquo; discipline now behind reward attribution now guards the at-booking offer badge. Matching a manufacturer/spa offer to a service used a loose <em>substring</em> test, so a partial keyword buried inside an unrelated service name would wrongly badge it: a <strong>&ldquo;Detox Treatment&rdquo;</strong> picked up a <strong>Botox</strong> offer (because &ldquo;de<strong>tox treatment</strong>&rdquo; contains the &ldquo;tox treatment&rdquo; synonym), and <strong>&ldquo;Eyelash Extensions&rdquo;</strong> picked up a <strong>Latisse</strong> offer (because &ldquo;eye<strong>lash</strong> extensions&rdquo; contains &ldquo;lash&rdquo;). The matcher now requires a <strong>whole-word</strong> hit, so a weak partial match simply doesn&rsquo;t badge &mdash; the honest move, exactly like a low-confidence reward match is held rather than guessed. Every real match is unchanged (Dermal&nbsp;Filler &rarr; Juv&eacute;derm, Neurotoxin &rarr; Botox, Lash&nbsp;Lift &rarr; Latisse, &hellip;). Because this is the one canonical <code>productMatchesName</code>, the fix lands in all three places at once: the booking badge, the $5 cross-sell win path, and the Promo Intelligence &ldquo;Applies&nbsp;to&rdquo; card. <strong>Touched</strong>: <code>promo-calendar.ts</code> (<code>productMatchesName</code> now word-boundary).",
    ],
  },
  {
    version: "v2.14.3",
    date: "June 2026",
    items: [
      "<strong>v2.14.3 &mdash; Attribution review: honest &ldquo;why&rdquo; copy + exact counts.</strong> Two fixes to the &ldquo;needs your eyes&rdquo; queue, both honesty. (1) A card said &ldquo;2 patients <em>share an email address</em>&rdquo; even when the two candidates clearly had <em>different</em> emails on screen &mdash; the ambiguity there is actually the reward&rsquo;s email pointing to one patient and its name to another. The reason is now derived from the candidates actually shown: it only says &ldquo;share an email/phone&rdquo; when they genuinely do, otherwise it says the patients &ldquo;match {name}&rdquo; &mdash; never a claim the screen contradicts. (2) The import receipt&rsquo;s &ldquo;held&rdquo; count now equals the number of cards in the queue exactly (deduped by reward, which also hardens the import against a duplicate export row).",
    ],
  },
  {
    version: "v2.14.2",
    date: "June 2026",
    items: [
      "<strong>v2.14.2 &mdash; Rewards page polish: consistent chevrons + clearer offer label.</strong> The <strong>Manufacturer promos</strong> collapse chevron now sits on the far right of its header, matching the offers card exactly (consistent UI). And the spa-authored offers card is retitled <strong>&ldquo;Create a Cross/Up-sell Offer&rdquo;</strong> &mdash; it says what it does.",
    ],
  },
  {
    version: "v2.14.1",
    date: "June 2026",
    items: [
      "<strong>v2.14.1 &mdash; Rewards page: jump to your patient book, and tidy the stack.</strong> Small UX on the Reward signals page. The import receipt now ends with an <strong>&ldquo;Open your patient book &rarr;&rdquo;</strong> link &mdash; once an import has matched patients (and flagged held/unmatched ones), you can go straight to the client list to act on them, instead of hunting for the nav. And the two management cards &mdash; <strong>Manufacturer promos</strong> and <strong>Your own offers</strong> &mdash; now have <strong>expand/collapse chevrons</strong>, so the page collapses to a clean header stack when you&rsquo;re focused on imports and the review queue, and opens up when you want to manage offers.",
    ],
  },
  {
    version: "v2.14.0",
    date: "June 2026",
    items: [
      "<strong>v2.14.0 &mdash; Confidence as a gate: a reward never lands on the wrong patient.</strong> The first trust-repair rung of the onboarding ladder. When you import a manufacturer&rsquo;s rewards, SmartSpa matches each one to a patient in your book &mdash; but until now, if a reward matched <em>more than one</em> patient (a couple who share an email, two patients with the same name), it silently credited whoever happened to sort first. A wrong dollar on the very number you check against your own portal. Now SmartSpa <strong>refuses to guess</strong>: a reward it can&rsquo;t pin to exactly one person is <strong>held in a &ldquo;needs your eyes&rdquo; queue</strong> on the Rewards page instead of being attributed. You pick the right patient in one tap &mdash; and <strong>it remembers</strong>, so the next import of that same person resolves automatically. Confident matches (a single patient, or one corroborated by name + email) still attribute instantly, untouched. The gate only opens on real ambiguity.",
      "Under the hood this is a reusable <em>confidence-as-a-gate</em> primitive (sibling of the Connection Health expect-gate): a pure match-verdict (learned / high / ambiguous / none, an honest match-tier &mdash; not a black-box score), a held-queue table, and a learned-alias table keyed on the full contact fingerprint (so a spouse on a shared email is never silently inherited). Cross-sell and rescue can adopt the same gate as registry additions.",
    ],
  },
  {
    version: "v2.13.2",
    date: "June 2026",
    items: [
      "<strong>v2.13.2 &mdash; Deals page: &ldquo;Book this&rdquo; is an outlined chip, and &ldquo;Learn more&rdquo; stays independently clickable.</strong> Two touch-ups on the tap-to-book card: the &ldquo;Book this &rarr;&rdquo; affordance is now an <strong>outlined box</strong> (emerald border + text) rather than a solid button &mdash; lighter, since the whole card is already the tap target. And &ldquo;Learn more&rdquo; (the manufacturer brand link) is confirmed as a <strong>separate, independently clickable</strong> element: it&rsquo;s a sibling <em>outside</em> the booking link, and a negative margin that could have dragged it under the card (killing its click) was replaced with clean padding. Tap the card &rarr; booker; tap &ldquo;Learn more&rdquo; &rarr; brand page. Pure layout.",
    ],
  },
  {
    version: "v2.13.1",
    date: "June 2026",
    items: [
      "<strong>v2.13.1 &mdash; Deals page polish: the &ldquo;Book this&rdquo; affordance is now a button on the right.</strong> Follow-up to <code>v2.13.0</code>: the tap-to-book cue moved from a small text link <em>below</em> the offer to a solid <strong>&ldquo;Book this &rarr;&rdquo; button on the right</strong>, vertically centered with the offer name &mdash; reads as the clear call-to-action it is. The whole card stays tappable (the button is styled, not a nested control). Pure layout.",
    ],
  },
  {
    version: "v2.13.0",
    date: "June 2026",
    items: [
      "<strong>v2.13.0 &mdash; Tap a Special, land in the booker with that service already chosen.</strong> On your public Deals page (<code>/deals/&lt;spa&gt;</code>), each offer that maps to a bookable service is now a <strong>deep-link</strong>: a patient who sees &ldquo;30% off Xeomin&rdquo; taps it and lands straight in the booking flow with <strong>Xeomin preselected</strong> &mdash; no more tapping a generic &ldquo;Book an appointment&rdquo; and then hunting for the service. Closes the gap between seeing the deal and acting on it (the human-first, guide-to-the-next-step principle). The offer card grows a &ldquo;<strong>Book this &rarr;</strong>&rdquo; affordance; the booker reads a <code>?service=</code> hint and matches it the same way the at-booking badge links offers to services (by name). Tolerant by design &mdash; an offer with no mappable service stays a static info card, and an unrecognized hint falls through to the normal service menu (never errors). Manufacturer offers keep their &ldquo;Learn more&rdquo; brand link alongside the new tap-to-book. No migration; pure routing + a new <code>serviceName</code> on the public deal payload.",
    ],
  },
  {
    version: "v2.12.0",
    date: "June 2026",
    items: [
      "<strong>v2.12.0 &mdash; Recall booking meter now reads honestly: live, not &ldquo;Coming soon.&rdquo;</strong> A verification walk caught a billing-honesty mismatch: the <strong>Recall booking</strong> $5 metric still wore a <em>&ldquo;Coming soon&rdquo;</em> badge in Billing &mdash; but it was already wired end-to-end to bill. Every recall booking writes a <code>recall_booking</code> recovery event, and the invoice math charges on the <em>enabled toggle &times; verified wins</em> &mdash; it never consulted the <code>live</code> flag. So the badge was purely cosmetic, and it misled in the <em>unsafe</em> direction: it read &ldquo;not live / free&rdquo; on a meter that was actually armed to charge. Exactly the kind of confident-wrong-number our billing-trust principle forbids. Fix mirrors the v2.4.8 cross-sell flip: <code>recall_booking.live = true</code> &mdash; the badge drops and it reads as a real $5/win meter alongside filled-slot and cross-sell. <strong>Billing behavior is unchanged</strong> (the math already included it); this only aligns the display with the truth. No money moved retroactively (the account showed $0 due). One-line flag flip; no migration. The genuinely not-yet-built meters (Lead conversion / Acquire) keep their honest &ldquo;Coming soon.&rdquo;",
    ],
  },
  {
    version: "v2.11.0",
    date: "June 2026",
    items: [
      "<strong>v2.11.0 &mdash; Connection Health now sees your <em>blue-bubble</em> messages &mdash; recall &amp; rescue sent from your own iMessage finally count toward the delivery card.</strong> Closes the gap <code>v2.10.0</code>&rsquo;s walk surfaced: the delivery card read your &ldquo;last sent&rdquo; from the campaign message log &mdash; but SmartSpa&rsquo;s primary channel, the <strong>iMessage drafts</strong> you send from your own Apple ID (recall/rescue drafts emailed to your Mac, then sent from Messages), leave <em>no</em> row in that log. So a spa actively texting patients every day could still see <strong>&ldquo;no text ever sent&rdquo;</strong> &mdash; the card was blind to the channel that matters most. Now every recall and rescue dispatch drops a lightweight <strong>outbound heartbeat</strong>, and the delivery card reads the <em>fresher</em> of your direct sends and your iMessage dispatches &mdash; so &ldquo;Text messages&rdquo; lights up the moment you dispatch a batch, and goes quiet (honestly) only when sending actually stops. <strong>Under the hood:</strong> a small, purpose-built <code>messaging_activity</code> table (one beat per channel, stamped at dispatch), written best-effort so a telemetry miss can never interfere with the messages themselves. The heartbeat fires at <em>dispatch</em> (reliable, server-side) rather than at your manual Send tap (which happens on your Mac with no callback) &mdash; the right scope for a card that guards the <em>system</em> going silent, not whether a human tapped Send. New table ships in its own migration; the delivery read degrades gracefully until it lands.",
    ],
  },
  {
    version: "v2.10.0",
    date: "June 2026",
    items: [
      "<strong>v2.10.0 &mdash; The &ldquo;watch&rdquo; gate now covers the messages going OUT, not just the data coming in &mdash; recall &amp; rescue sending can&rsquo;t go silently dark anymore.</strong> Extends the expect-gate to a <strong>third feed</strong>, and the first <em>outbound</em> one. Until now Connection Health watched what flows <em>in</em> &mdash; your reward portals (<code>v2.8.0</code>) and your calendar (<code>v2.9.0</code>). The blind spot was the other direction: the recall and rescue <strong>messages SmartSpa sends your patients</strong>. If that quietly stops &mdash; the sending agent isn&rsquo;t running, texts stop being delivered, an opt-out cascade &mdash; nothing errors and the recovery engine just goes dark, unnoticed. Now your <strong>Text messages</strong> and <strong>Emails</strong> appear as health cards (last send + an honest freshness read), and a new <strong>&ldquo;Watch your messaging&rdquo;</strong> toggle on the Connection Health page lets you declare the channels you rely on &mdash; so a long quiet stretch surfaces as a flagged <strong>&ldquo;nothing has gone out&rdquo;</strong> nudge with a <em>Check sending</em> button, instead of silence. It <strong>never cries wolf</strong>: a quiet week is legitimate, so outbound silence soft-flags after a few days and is <em>never</em> marked a hard failure. <strong>Under the hood &mdash; the gate proving itself a third time:</strong> the third kind was a single widened constraint plus a read section &mdash; one new <code>delivery</code> tier, a registry entry, and the same <code>expected_sources</code> table, the same server-fn pair, the same shared toggle UI. No new system. Freshness reads from your existing outbound message log; verdict logic covered by the runnable spec (now 19 checks). Migrate-then-deploy; existing watches untouched.",
    ],
  },
  {
    version: "v2.9.1",
    date: "June 2026",
    items: [
      "<strong>v2.9.1 &mdash; Toggling &ldquo;Watch a calendar&rdquo; now updates the page instantly &mdash; no manual Recheck.</strong> Tiny UX finish on <code>v2.9.0</code>: turning a calendar watch on/off wrote the change correctly (the chip flipped, the toast fired) but the Connection Health cards above didn&rsquo;t redraw until you hit Recheck, so the new &ldquo;expected but not connected&rdquo; card didn&rsquo;t appear right away. The watch toggle now re-fetches the report on a successful change, so the flagged card shows (or clears) the instant you flip it. Same for the portal toggles. One-line callback; no migration.",
    ],
  },
  {
    version: "v2.9.0",
    date: "June 2026",
    items: [
      "<strong>v2.9.0 &mdash; The &ldquo;watch&rdquo; gate now covers your calendar too &mdash; a disconnected scheduler can&rsquo;t vanish silently anymore.</strong> Extends the <code>v2.8.0</code> expect-gate from reward portals to <strong>PMS / calendar connections</strong>, the same primitive on a second feed. The blind spot it closes: a calendar whose token gets revoked (or that you disconnect, or never connected) has <em>no active connection row</em>, so it simply disappears from Connection Health &mdash; looks fine, but appointments have stopped flowing. Now you can declare the calendar you use (a <strong>&ldquo;Watch a calendar&rdquo;</strong> toggle on the Connection Health page, Acuity / Vagaro), and if it&rsquo;s ever expected-but-not-connected it shows as a flagged <strong>&ldquo;Needs attention &mdash; SmartSpa isn&rsquo;t receiving appointments&rdquo;</strong> card with a <em>Connect</em> button, instead of nothing. <strong>Under the hood &mdash; one gate, not two:</strong> the <code>expected_portal_sources</code> table is generalized to <code>expected_sources</code> (a <code>kind</code> column for portal vs. scheduler), with one shared server-fn pair, one shared verdict rule (declared-but-not-connected &rarr; flagged now, no grace), and one shared toggle UI behind both surfaces. The trusted-onboarding thesis made real: build the gate primitive once, and the second surface falls out of it. Verdict logic covered by the runnable spec (11 checks). Generalize migration ships separately; reads degrade gracefully until it lands.",
    ],
  },
  {
    version: "v2.8.1",
    date: "June 2026",
    items: [
      "<strong>v2.8.1 &mdash; Connection Health now shows one clean card per manufacturer, and the &ldquo;watch&rdquo; gate merges with real imports.</strong> Follow-up to <code>v2.8.0</code>, caught the moment it went live: reward-portal cards were keyed by the <em>report variant</em> the import wrote (<code>allergan-patient360</code>, <code>galderma-savings</code>, <code>galderma-all-patients</code>) rather than the brand &mdash; so a single manufacturer showed up as multiple cards with raw titleCased names, and a <strong>&ldquo;Watch these portals&rdquo;</strong> toggle (canonical <code>allergan</code>) wouldn&rsquo;t merge with its own import (<code>allergan-patient360</code>), double-carding the same brand (one &ldquo;Allergan-patient360 &middot; Needs attention,&rdquo; one &ldquo;Allē &middot; Setting up&rdquo;). The read model now collapses every report variant to its canonical manufacturer (via the mapper&rsquo;s own <code>manufacturer</code> field), so each brand is exactly one card showing its freshest pull &mdash; and a watched portal merges with its real import instead of sitting next to it. Also fixed a grammar bug the same screens exposed: &ldquo;hasn&rsquo;t imported in 4 days ago&rdquo; &rarr; &ldquo;Allē&rsquo;s last import was 4 days ago.&rdquo; No migration; pure read-model + copy.",
    ],
  },
  {
    version: "v2.8.0",
    date: "June 2026",
    items: [
      "<strong>v2.8.0 &mdash; Connection Health can now catch a portal that was set up but never imported &mdash; the silent failure it previously couldn&rsquo;t see.</strong> The health page built its reward-portal rows from imports that had <em>already arrived</em>. So a portal you set up whose <em>first</em> pull never landed (a wrong portal login on day one, say) wrote no data, and therefore showed <strong>nothing at all</strong> &mdash; the scariest version of a connection failure, invisible on the very page meant to catch it. &ldquo;Absence is the signal&rdquo; only works if there was ever a presence. Now you can tell SmartSpa which portals you <em>expect</em> to flow (a new <strong>&ldquo;Watch these portals&rdquo;</strong> toggle on Recognition &rarr; Rewards, in the auto-import card). A watched portal that hasn&rsquo;t imported shows up honestly as <strong>&ldquo;Setting up &mdash; waiting for the first import,&rdquo;</strong> and if that first pull stays overdue it escalates on its own to <strong>&ldquo;Needs attention &mdash; the first import never landed. Check the portal login&rdquo;</strong> &mdash; with the boundary clause that SmartSpa is fine, the <em>connection</em> is the issue. New <code>expected_portal_sources</code> table; verdict logic proven by a runnable spec (<code>scripts/verify-connection-health.ts</code>); the expected read degrades gracefully (it never breaks the trust page if it can&rsquo;t load). The first concrete piece of the trusted-onboarding &ldquo;gate&rdquo;: you declare it, it gets watched, a broken first pull becomes visible and fixable.",
    ],
  },
  {
    version: "v2.7.3",
    date: "June 2026",
    items: [
      "<strong>v2.7.3 &mdash; Connection Health hardening: a connection that never synced no longer borrows its login time to look &ldquo;healthy.&rdquo;</strong> Freshness should measure a real <em>data</em> event (a sync / import / webhook), never the OAuth <em>handshake</em> time. The scheduler read had a fallback &mdash; if a connection had no last-sync timestamp, it used <code>connected_at</code> instead &mdash; which would let a connected-but-never-synced calendar read &ldquo;Healthy &middot; syncing live&rdquo; off its login time alone, then drift to &ldquo;stale&rdquo; days later. The honest verdict for &ldquo;connected, nothing has arrived yet&rdquo; is <strong>&ldquo;Setting up &mdash; waiting for the first sync.&rdquo;</strong> Fallback removed; freshness now reads <code>last_sync_at</code> only. <strong>Not reachable on the live Acuity path</strong> (it stamps <code>last_sync_at</code> at connect, so behavior there is unchanged) &mdash; this hardens any future platform that could set <em>connected</em> without a sync stamp. The #2 finding from the same fresh walk as <code>v2.7.2</code>; honestly downgraded from the agent&rsquo;s &ldquo;critical&rdquo; to hardening after verifying the live connect path. Zero migration.",
    ],
  },
  {
    version: "v2.7.2",
    date: "June 2026",
    items: [
      "<strong>v2.7.2 &mdash; Connection Health fresh-walk fix: a silent query failure could fake &ldquo;all healthy.&rdquo;</strong> The Connection Health read (<code>connection-health.functions.ts</code>) loaded your PMS/scheduler connections but <em>ignored</em> the database error on that query &mdash; so if the read ever failed, your calendar connections would silently <em>vanish</em> from the page and it would render as if everything were fine. That is the exact failure this surface exists to prevent: the doctrine&rsquo;s cardinal sin is a <em>silent absence</em>, not an error. The read now surfaces a failure <strong>loudly</strong> (throws &rarr; on-screen error) instead of swallowing it &mdash; consistent with the reward-portal read right beside it, which already failed loud. A non-technical owner now sees &ldquo;couldn&rsquo;t load &mdash; retry&rdquo; rather than a falsely-green dashboard. Caught by a skeptical 3-agent fresh walk of the rung-1 trust surface; verified against source (3 louder agent findings downgraded as false positives in the same pass). One-line behavior change, zero migration.",
    ],
  },
  {
    version: "v2.7.1",
    date: "June 2026",
    items: [
      "<strong>v2.7.1 &mdash; Branding-migration prep: the smartspa.app cutover is now a one-switch flip.</strong> Groundwork (zero behavior change today) for consolidating the app&rsquo;s front door onto <code>smartspa.app</code> for consistent SmartSpa branding. Almost every <code>getrefill.app</code> reference already routes through the <code>REFILL_PUBLIC_ORIGIN</code> env var &mdash; so the flip is mostly one env change plus a DNS/auth cutover, not a 156-line edit. This ship fixes the two public-origin URLs that <em>bypassed</em> the env and would have broken on the flip: the offer-push booking link patients receive (<code>refill-promo-calendar.functions.ts</code> &mdash; was a hardcoded <code>getrefill.app/s/&lt;slug&gt;</code>) and the password-reset redirect (<code>admin-users.ts</code>). Both now read <code>REFILL_PUBLIC_ORIGIN</code> &mdash; identical output today, correct after the cutover. <strong>Not in this ship</strong> (sequenced for daylight + email warming): the DNS/custom-domain + Supabase auth-origin cutover, the outbound from-addresses (<code>offers@</code> / <code>recall@</code> / <code>recognition@</code> &mdash; gated on smartspa.app email warming), and the <code>app.getrefill.app</code> auth-host. getrefill.app keeps serving regardless (live patient booking links). Runbook on the Desktop.",
    ],
  },
  {
    version: "v2.7.0",
    date: "June 2026",
    items: [
      "<strong>v2.7.0 &mdash; Program Intelligence: your manufacturer&rsquo;s rewards program, read for you.</strong> Phase 2 of Purchase &amp; Rebate Intelligence goes visible. New <strong>Recognition &rarr; Program</strong> tab renders the dashboard nobody logs in to read: your <strong>membership tier</strong> + progress to the next level, each <strong>rebate</strong> with its real status (Brand Adoption &ldquo;3% secured,&rdquo; Rise &ldquo;not eligible &mdash; $0 baseline quarter&rdquo;), the per-SKU progress rings, and &mdash; the headline &mdash; your <strong>moves</strong>: the exact &ldquo;buy N more X &rarr; unlock the Y% rebate.&rdquo; Built on the pure-logic brain (<code>program-intel.ts</code>): it derives moves only for rebates actually in reach (an already-secured rebate shows <em>no</em> false &ldquo;you&rsquo;re short&rdquo; &mdash; we don&rsquo;t nudge you to buy product you don&rsquo;t need), and a <strong>&ldquo;what changed&rdquo;</strong> feed that will flag the moment the manufacturer moves a threshold, price, or tier (the time-series no one else holds). Wired to a <strong>dated manual capture of the real Rejuv ASPIRE dashboard</strong> &mdash; labelled honestly as a capture, never implied live; the auto-pull swaps in behind it with zero UI change. <strong>Added</strong>: <code>app.refill.recognition.program.tsx</code>, <code>program-intel.functions.ts</code>; <strong>touched</strong>: <code>RecognitionTabs.tsx</code> (+Program tab). No migration yet (snapshot persistence rides with the pull).",
    ],
  },
  {
    version: "v2.6.1",
    date: "June 2026",
    items: [
      "<strong>v2.6.1 &mdash; Promo Intelligence match is now consistent with what actually badges.</strong> Same-night fix to v2.6.0: the new card&rsquo;s &ldquo;Applies to&rdquo; line rolled its own naive substring match (<code>serviceName.includes(product)</code>), so a <em>&ldquo;$75 off Juvéderm&rdquo;</em> offer read &ldquo;No match in your menu yet&rdquo; against a service named <strong>Filler</strong> &mdash; even though the at-booking badge and the $5 cross-sell path matched it correctly via the synonym map. Only the display was wrong (the money path was always fine). The card now reuses the canonical <code>productMatchesName</code> (Juvéderm/Restylane/Dysport &rarr; <em>Filler</em>, Botox/Dysport &rarr; <em>neurotoxin</em>, etc.), so what the card says matches what actually badges. True retail-only promos (SkinMedica serum, Latisse) still honestly show no match when the spa has no matching <em>service</em> &mdash; correct, not a miss. <strong>Touched</strong>: <code>promo-calendar.ts</code> (export <code>productMatchesName</code>), <code>app.refill.recognition.rewards.tsx</code> (card reuses it). Caught in a fresh-walk of v2.6.0 on real Rejuv data.",
    ],
  },
  {
    version: "v2.6.0",
    date: "June 2026",
    items: [
      "<strong>v2.6.0 &mdash; Promo Intelligence: the manufacturer&rsquo;s promo dump, made legible.</strong> The manufacturer-promo card used to be a black box &mdash; upload a calendar CSV, see a bare count (&ldquo;12 offers loaded&rdquo;). The owner couldn&rsquo;t see <em>which</em> promos were loaded, what was active, which of their own services each matched, or control which ones reached patients. They just badged at booking, invisibly. Now it&rsquo;s a real surface: the actual manufacturer offers, routed into <strong>Active now / Upcoming / Expired</strong>, each showing its product, dollar value, date window, and <strong>which of your own services it matches</strong> (reusing the booking matcher) &mdash; plus a one-tap <strong>on/off control for your public Deals page</strong> (<code>show_on_deals</code>). This is Phase&nbsp;1 of the Purchase &amp; Rebate Intelligence solution: turn the data nobody reads into routed, controllable intelligence. The portal <em>pull</em> that auto-feeds this (no more CSV upload) is the next slice. <strong>Touched</strong>: <code>refill-promo-calendar.functions.ts</code> (new <code>setPromoOfferOnDeals</code>, manufacturer-scoped), <code>app.refill.recognition.rewards.tsx</code> (<code>PromoCalendarCard</code> &rarr; <code>PromoIntelligenceCard</code>). No migration &mdash; every column already existed.",
    ],
  },
  {
    version: "v2.5.3",
    date: "June 2026",
    items: [
      "<strong>v2.5.3 &mdash; Offer redemption count is now race-proof.</strong> A limited offer (&ldquo;first 20&rdquo;) counted each redemption with a read-modify-write, so two bookings landing at once could both read the same count and both write back the same +1 &mdash; losing one, drifting the cap. The increment is now a single atomic <code>UPDATE</code> behind a DB function (<code>increment_offer_redemption</code>), so concurrent bookings each count correctly. The cap stays a soft visibility limit (the offer just stops badging at the cap); the booking and the $5 win are unaffected &mdash; this only makes the count accurate. <strong>Migration</strong>: <code>20260722000000_v2_5_3_offer_redemption_increment.sql</code>. <strong>Touched</strong>: <code>refill-promo-calendar.functions.ts</code> (rpc instead of read-modify-write). The last open item from the fresh-walk, closed.",
    ],
  },
  {
    version: "v2.5.2",
    date: "June 2026",
    items: [
      "<strong>v2.5.2 &mdash; Fresh-walk fix: offer push is safe + complete at scale.</strong> Two scale bugs in cohort push targeting: (1) the <em>expiring</em> cohort read wasn't paginated, so on a spa with 1,000+ reward entries it silently dropped patients past the cap; now it pages through all of them. (2) More important &mdash; the opt-out filter passed the whole cohort's patient IDs to a single <code>.in()</code> query, which on a large cohort could exceed the URL limit and <strong>fail silently, disabling opt-out filtering entirely</strong> (drafts could include opted-out patients). The id list is now chunked into ≤200-id batches, which also stays under the read cap. <strong>Touched</strong>: <code>refill-promo-calendar.functions.ts</code> (<code>listOfferCohortTargets</code>: <code>fetchAllRows</code> + chunked opt-out). Caught in the fresh-walk; the opt-out one is the priority fix &mdash; a patient who opts out must never be drafted.",
    ],
  },
  {
    version: "v2.5.1",
    date: "June 2026",
    items: [
      "<strong>v2.5.1 &mdash; Fresh-walk fix: offer-push draft email is HTML-safe.</strong> The offer-push draft (the batch emailed to the spa's own inbox for iMessage review) interpolated patient names and the offer title straight into HTML. A patient named &ldquo;Tom &amp; Jerry&rdquo; or an offer title with a <code>&lt;</code> would corrupt the draft table (and, in the owner's inbox, render stray markup). Those values are now escaped. <strong>Touched</strong>: <code>refill-promo-calendar.functions.ts</code> (<code>escHtml</code> on name / phone / message / offer title). Caught in the same fresh-walk.",
    ],
  },
  {
    version: "v2.5.0",
    date: "June 2026",
    items: [
      "<strong>v2.5.0 &mdash; Fresh-walk fix: Connection Health reads every manufacturer's latest pull.</strong> The reward-portal freshness check loaded only the newest 400 import rows, then took the first one per manufacturer. On a spa with a long history where one manufacturer pulls daily, that manufacturer could fill the whole window and bury another's most-recent pull past the cutoff &mdash; so a perfectly healthy portal could falsely read as &ldquo;no data / setting up.&rdquo; Now it pages through all import rows (newest-first) so each manufacturer's true latest pull is always seen. <strong>Touched</strong>: <code>connection-health.functions.ts</code> (<code>fetchAllRows</code> instead of <code>.limit(400)</code>). Caught in a solo fresh-walk of this session's new code.",
    ],
  },
  {
    version: "v2.4.9",
    date: "June 2026",
    items: [
      "<strong>v2.4.9 &mdash; Acuity mirror is now rename-safe (external-id mapping).</strong> The staging mirror used to match providers and services by name, so renaming one (in SmartSpa or Acuity) made a re-mirror create a duplicate. Each mirrored provider/service is now anchored to its Acuity id (<code>external_source</code> + <code>external_id</code>): a re-mirror finds the row by id and just updates its name &mdash; never duplicating &mdash; and pre-existing name-mirrored rows are adopted on the next run. Native (non-mirrored) providers/services are untouched. <strong>Migration</strong>: <code>20260721000000_v2_4_9_mirror_external_id.sql</code> (additive nullable columns + a partial unique index per tenant/source/external-id). <strong>Touched</strong>: <code>scheduler-mirror.functions.ts</code> (match/adopt/insert by external id; rename detection).",
    ],
  },
  {
    version: "v2.4.8",
    date: "June 2026",
    items: [
      "<strong>v2.4.8 &mdash; Cross-sell meter is live.</strong> With the offer engine fully shipped (types · cohort targeting · Deals page · push · recurrence), the <strong>$5 cross-sell add-on</strong> metric is flipped on: it drops the &ldquo;Coming soon&rdquo; badge in Billing and is now a real earning meter alongside filled-slot. The billing math already included it (per-tenant rule, flat $5, enabled, charged on verified wins) &mdash; this flag was the display gate; cross-sell wins now read as billable, not pending. Same conservative model: free to run, $5 per booking we create, the spa keeps every dollar of the visit. <strong>Touched</strong>: <code>billing-metrics.ts</code> (<code>cross_sell_addon.live = true</code>).",
    ],
  },
  {
    version: "v2.4.7",
    date: "June 2026",
    items: [
      "<strong>v2.4.7 &mdash; Acuity mirror hardening: staged calendar comes up with real hours.</strong> When the Acuity staging mirror creates a provider, it now seeds sensible default weekly hours (Mon&ndash;Fri 9&ndash;5, weekend closed) so the staged SmartSpa calendar renders proper availability bands instead of blank provider columns &mdash; a far better &ldquo;side-by-side&rdquo; before cutover. (Acuity's API exposes only computed availability, not weekly business hours, so these are tune-able defaults the owner adjusts in Online booking, not a literal hours mirror.) Idempotent + non-destructive: re-running the mirror never overwrites hours you've since adjusted (<code>ignoreDuplicates</code> on the provider/day key). <strong>No migration.</strong> <strong>Touched</strong>: <code>scheduler-mirror.functions.ts</code> (<code>seedDefaultHours</code> for newly-mirrored providers). Deferred (needs a migration, next session): an external-id mapping so re-mirroring after a provider/service rename matches by ID instead of name.",
    ],
  },
  {
    version: "v2.4.6",
    date: "June 2026",
    items: [
      "<strong>v2.4.6 &mdash; Spa offer engine, slice 2: recurring + limited offers.</strong> An offer can now run on specific <strong>days of the week</strong> (&ldquo;Tox Tuesdays&rdquo; = pick Tue) and/or carry a <strong>redemption limit</strong> (&ldquo;first 20&rdquo;). The matcher honors both everywhere it already runs &mdash; the at-booking badge, the Deals page, and the cross-sell win all respect the weekday window and stop the moment the cap is hit; each win increments the offer's redemption count. Blank weekdays = every day (unchanged); no limit = unlimited (unchanged), so existing offers behave exactly as before. <strong>No migration</strong> (<code>active_weekdays</code>/<code>quantity_cap</code>/<code>redeemed_count</code> shipped with v2.4.2). <strong>Touched</strong>: <code>promo-calendar.ts</code> (weekday + cap gating in <code>isActive</code>), <code>refill-promo-calendar.functions.ts</code> (authoring + redemption increment), <code>SpaOffersCard.tsx</code> (day picker + limit field). All four offer-engine dimensions &mdash; types, recurrence, targeting, delivery &mdash; are now complete.",
    ],
  },
  {
    version: "v2.4.5",
    date: "June 2026",
    items: [
      "<strong>v2.4.5 &mdash; Spa offer engine, slice 4b: push a targeted offer to its patients (draft-first, human-gated).</strong> A cohort-targeted offer (lapsed / new / expiring) now has a <strong>Push</strong> button on the authoring card. It composes one message per matching patient &mdash; reusing the exact same cadence engine the Deals page and Recall use to define the cohort &mdash; filters out anyone who's opted out, and emails the batch to your own iMessage proxy inbox for review. From there it's the proven branded-iMessage flow: paste into Claude Desktop, the iMessage MCP drafts each into Messages.app, you review and tap Send (blue bubble from your own number). <strong>Nothing is sent to a patient by the server</strong> &mdash; push is staged, not blasted, and opt-outs are honored before a draft is ever written. Patients with no phone are counted and skipped. <strong>No migration</strong> (<code>push_enabled</code>/cohort columns shipped with v2.4.2). <strong>New</strong>: <code>draftOfferPushFn</code> + <code>getOfferReachFn</code> + cohort target resolver (<code>listOfferCohortTargets</code>, opt-out-filtered) in <code>refill-promo-calendar.functions.ts</code>. <strong>Touched</strong>: <code>SpaOffersCard.tsx</code> (Push button). With this, the offer engine's four dimensions &mdash; types, recurrence, targeting, and now delivery (Deals page + push) &mdash; are all live.",
    ],
  },
  {
    version: "v2.4.4",
    date: "June 2026",
    items: [
      "<strong>v2.4.4 &mdash; Spa offer engine, slice 4a: a public Deals page.</strong> Every spa now has a shareable, patient-facing offers page at <code>/deals/&lt;slug&gt;</code> &mdash; the same slug as their booking link, no login &mdash; listing all currently-active public offers with one tap to <strong>Book an appointment</strong>. It's the &ldquo;pull&rdquo; half of offer delivery: post it in a bio link, an email footer, a window sign. Cohort-targeted offers (lapsed / new / expiring) are intentionally <em>excluded</em> from the public list &mdash; those reach their patients via push (slice 4b), not a page anyone can read. Manufacturer promos and the spa's own offers both show, with a tidy headline and any end date. <strong>No migration</strong> (<code>show_on_deals</code> shipped with v2.4.2). <strong>New</strong>: <code>deals.$slug.tsx</code>, <code>getPublicDealsFn</code>. <strong>Touched</strong>: <code>promo-calendar.ts</code> (<code>publicDeals</code> + <code>dealHeadline</code>), <code>refill-promo-calendar.functions.ts</code>. Next (slice 4b): push targeted offers to matching patients via the outreach rails &mdash; draft-first + opt-out-filtered.",
    ],
  },
  {
    version: "v2.4.3",
    date: "June 2026",
    items: [
      "<strong>v2.4.3 &mdash; Spa offer engine, slice 3: cohort targeting (the offer gets smart).</strong> An offer can now target a patient segment &mdash; <strong>lapsed</strong>, <strong>new</strong>, or <strong>expiring-reward</strong> &mdash; not just &ldquo;all patients,&rdquo; reusing the exact same cadence engine that powers Recall (last-visit vs product-kind lapse window, first-visit recency, reward-expiration window). Two principles make targeting real rather than cosmetic: <strong>no-leak</strong> &mdash; a targeted offer does <em>not</em> badge to anonymous booking visitors (a &ldquo;lapsed-patient 20% off&rdquo; shown to everyone would defeat the point and mis-imply eligibility), so only <em>all</em>-patient offers badge publicly; and <strong>win integrity</strong> &mdash; when a booking resolves to a real patient, a targeted offer earns its $5 only if that patient is actually <em>in</em> the cohort. The authoring card gains a &ldquo;Who's this for?&rdquo; picker; targeted offers show a cohort chip. Delivery of targeted offers to the right patients (push) is the next slice &mdash; this slice builds the targeting model, the cohort resolver, and the integrity guards it rides on. <strong>No migration</strong> (the <code>target_cohort</code> column shipped with v2.4.2). <strong>Touched</strong>: <code>promo-calendar.ts</code> (cohort type + <code>matchOfferForName</code>/<code>badgeableOffers</code>), <code>refill-promo-calendar.functions.ts</code> (<code>resolvePatientCohorts</code> + win-gate + authoring), <code>scheduling.functions.ts</code> &amp; <code>scheduling-owner.functions.ts</code> (no-leak badge filter), <code>SpaOffersCard.tsx</code> (cohort picker).",
    ],
  },
  {
    version: "v2.4.2",
    date: "June 2026",
    items: [
      "<strong>v2.4.2 &mdash; Spa offer engine, slice 1: your own offers grow real types.</strong> Spa-authored offers are SmartSpa's anti-lock-in differentiator &mdash; a manufacturer's offer routes your patient to &ldquo;a provider near you,&rdquo; but your <em>own</em> offer is spa-loyal by construction and impossible for a manufacturer to copy. This slice promotes spa offers from &ldquo;a fixed dollar amount off a service&rdquo; into a real engine: author <strong>$ off</strong>, <strong>% off</strong>, a <strong>free add-on</strong> (&ldquo;book Botox, get a free brow wax&rdquo;), or <strong>$ off an add-on</strong> &mdash; each badges the trigger service at booking with a type-aware label and earns the same $5 cross-sell win. Offers can be <strong>paused</strong> without deleting. The unified offers table now carries columns for all four planned dimensions (types &middot; recurrence &middot; cohort targeting &middot; deals-page/push), so the next slices ship with <em>no</em> further migrations. <strong>Touched</strong>: <code>promo-calendar.ts</code> (typed offers + per-type badge labels), <code>refill-promo-calendar.functions.ts</code> (<code>createSpaOffer</code> types + <code>setSpaOfferActive</code>), <code>SpaOffersCard.tsx</code> (type picker + conditional fields + pause). <strong>Migration</strong>: <code>20260720000000_v2_4_2_spa_offer_engine.sql</code> (additive columns on <code>manufacturer_promo_offers</code>).",
    ],
  },
  {
    version: "v2.4.1",
    date: "June 2026",
    items: [
      "<strong>v2.4.1 &mdash; Acuity staging mirror: stand up your SmartSpa calendar from Acuity, and compare them side-by-side before you cut over.</strong> A new <strong>Calendar &rarr; Staging</strong> tab (<code>/app/refill/calendar/staging</code>) that mirrors your Acuity <em>structure</em> &mdash; not just appointments &mdash; into SmartSpa so you can see your native calendar fully stood up next to your current one before committing. Until now the live Acuity link streamed appointments in, but pulled no calendars or services &mdash; so those appointments carried only a text label and never lined up under a provider, and there was nothing to make SmartSpa <em>look</em> like your Acuity. <strong>Mirror now</strong> pulls your Acuity calendars &rarr; providers, appointment-types &rarr; services (name, duration, price, bookable), and back-links every imported appointment to its mirrored provider. It's idempotent (safe to re-run &mdash; it matches what's already there, never duplicates), non-destructive (mirrored providers are visualization rows, not yet bookable until cutover, and <em>nothing</em> changes on Acuity's side), and honest: anything it can't map (a blank calendar, a private type) is <strong>flagged in the report</strong>, never silently dropped. The page shows a mirror report (what came across, what needs a look) beside your now-populated SmartSpa calendar &mdash; the trustworthy &ldquo;after&rdquo; to compare against your Acuity tab. <strong>New</strong>: <code>scheduler-mirror.functions.ts</code> (<code>stageAcuityMirrorFn</code> + <code>getMirrorStatusFn</code>), <code>app.refill.calendar.staging.tsx</code>. <strong>Touched</strong>: <code>schedulers/acuity.ts</code> (added <code>listAcuityCalendars</code> + <code>listAcuityAppointmentTypes</code>), <code>CalendarTabs.tsx</code>, <code>changelog.ts</code>. No migration &mdash; mirrors by name into existing columns.",
    ],
  },
  {
    version: "v2.4.0",
    date: "June 2026",
    items: [
      "<strong>v2.4.0 &mdash; Connection Health: the trust layer that tells &ldquo;SmartSpa is down&rdquo; apart from &ldquo;your connection is down.&rdquo;</strong> A new <strong>Account &rarr; Health</strong> tab (<code>/app/refill/settings/health</code>) watches every external feed SmartSpa depends on &mdash; your live calendar sync <em>and</em> the daily reward-portal pulls (Allē / ASPIRE / Evolus) &mdash; in one place. The biggest risk in any integration isn&rsquo;t our code; it&rsquo;s that a connection we don&rsquo;t control fails (a token gets revoked, a webhook silently stops, a portal login expires) and it <em>looks</em> like SmartSpa stopped working. The worst version is <strong>silence</strong> &mdash; a feed that just stops reporting throws no error. Connection Health catches the silence with a <strong>freshness check</strong> tuned to each connection&rsquo;s access tier, and is honest about what each tier guarantees: <em>live</em> (webhooks, e.g. Acuity), <em>scheduled</em> (daily pulls), and <em>snapshot</em> (CSV imports that age) &mdash; a snapshot never masquerades as live. Every red or amber state says plainly that the connection is the issue, not SmartSpa, and ships the one button that fixes it (Reconnect / Check auto-import). One spine now guards both PMS connectors and the portal pulls. <strong>New</strong>: <code>connection-health.ts</code> (pure verdict engine), <code>connection-health.functions.ts</code> (unified read model), <code>app.refill.settings.health.tsx</code>. <strong>Touched</strong>: <code>SettingsTabStrip.tsx</code>, <code>changelog.ts</code>. No migration &mdash; every freshness timestamp it needs already existed.",
    ],
  },
  {
    version: "v2.3.53",
    date: "June 2026",
    items: [
      "<strong>v2.3.53 &mdash; One-click SmartSpa Agent download, live from the Rewards card.</strong> Option B of the <em>Hands-free auto-import</em> card now has a real <strong>Download SmartSpa Agent</strong> button (macOS · Apple Silicon · 564&nbsp;MB) next to the token &mdash; so a spa can grab the desktop installer and paste their token in two clicks, no concierge hand-off of the file. The bundle (the signed <code>.mcpb</code> Desktop Extension that logs into reward portals on the spa&rsquo;s own Mac and pulls each report into SmartSpa) is hosted on Cloudflare R2 (public bucket <code>smartspa-agent</code>, zero-egress) and served with <code>Content-Disposition: attachment</code>. <strong>Touched</strong>: <code>app.refill.recognition.rewards.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.52",
    date: "June 2026",
    items: [
      "<strong>v2.3.52 &mdash; Hands-free auto-import card now hands you the SmartSpa Agent token, with self-serve rotation.</strong> The <em>Hands-free auto-import</em> card on Recognition &rarr; Rewards used to surface only the email-drop address. It now presents <strong>two delivery options side by side</strong>: <em>Option A</em> &mdash; forward a report to your private <code>&lt;token&gt;@rewards.smartspa.app</code> address (unchanged), and <em>Option B</em> &mdash; install <strong>SmartSpa Agent</strong> (the one-click desktop app) and paste the same token, shown with its own <strong>Copy</strong> button. Same underlying per-tenant token, now copy-pasteable into the installer so it never has to live in an onboarding email. Added a self-serve <strong>Rotate token</strong> action (revokes the active token &mdash; old address + any installed Agent stop working &mdash; and mints a fresh one) behind a confirm, for &ldquo;it leaked / start clean.&rdquo; New server fn <code>rotateRewardIngestToken</code> (revoke-active-then-insert, respects the partial-unique index; no migration &mdash; <code>revoked_at</code> already existed). <strong>Touched</strong>: <code>app.refill.recognition.rewards.tsx</code>, <code>manufacturer-reward-ingest.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.51",
    date: "June 2026",
    items: [
      "<strong>v2.3.51 &mdash; Portal-MCP M1: the second faucet&rsquo;s server side.</strong> New <code>POST /api/ingest/rewards</code> &mdash; the direct CSV drop the local <code>refill-portal</code> MCP (coming next) will POST into after Playwright pulls a report from Allē/ASPIRE/Evolus on the spa&rsquo;s own Mac. Body <code>{token, filename, csv}</code>, the reward-ingest token <em>is</em> the credential (portal logins never touch our servers). Extracted the transaction-vs-reward <strong>lane routing</strong> into one shared helper (<code>ingestCsvByTokenRouted</code>) so the email-drop receiver and this direct endpoint share a single path into <code>detect()</code> &rarr; ingest &rarr; Recall &mdash; and refactored <code>/api/resend/inbound-rewards</code> to use it (no more duplicated lane logic). Critical correctness: Allē&rsquo;s Patient360 export routes to the <em>transaction</em> lane, so the endpoint had to be both-lane from day one, not reward-only. <strong>New</strong>: <code>manufacturer-ingest-router.ts</code>, <code>api.ingest.rewards.ts</code>. <strong>Touched</strong>: <code>api.resend.inbound-rewards.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.50",
    date: "June 2026",
    items: [
      "<strong>v2.3.50 &mdash; Inbound reward email-drop is LIVE &mdash; plus observability so we can see what landed.</strong> First real manufacturer forward to <code>&lt;token&gt;@rewards.smartspa.app</code> ingested end-to-end (Resend MX &rarr; webhook &rarr; <code>data</code>-envelope read &rarr; Attachments-API fetch &rarr; reward lane &rarr; <code>processed:1</code>). Added structured server logs to <code>/api/resend/inbound-rewards</code> at every decision point (<code>no_reward_token</code>, <code>no_csv_attachment</code> with meta/email-id/api-key presence, and a per-file <code>processed</code> summary with lane/ok/detected/reason + whether content came <code>inline</code> or via <code>api</code>) &mdash; so future ingests are visible in <code>wrangler tail</code> without round-tripping through Resend&rsquo;s dashboard. No PII logged (token prefix + filenames only). <strong>Touched</strong>: <code>api.resend.inbound-rewards.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.49",
    date: "June 2026",
    items: [
      "<strong>v2.3.49 &mdash; Hands-free reward auto-import is now wired for Resend&rsquo;s <em>real</em> inbound webhook.</strong> The email-drop receiver (<code>/api/resend/inbound-rewards</code>) was built expecting the forwarded CSV to arrive baked into the webhook as base64. Resend&rsquo;s actual <code>email.received</code> webhook ships attachment <strong>metadata only</strong> (filename + type, no bytes) and nests the whole envelope under <code>data</code> &mdash; so a real forward would have failed twice over: the per-spa token wouldn&rsquo;t resolve, and the attachment would read as empty. Fixed both: the receiver now reads the <code>data</code> envelope, and when no inline content is present it fetches the file through the Resend Attachments API (lists the received email&rsquo;s attachments &rarr; pulls the signed <code>download_url</code> &rarr; downloads the CSV) before routing it into the same reward / transaction ingest cores as a manual upload. Inline-base64 fixtures still work, so the smoke tests are untouched. <strong>Touched</strong>: <code>api.resend.inbound-rewards.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.48",
    date: "June 2026",
    items: [
      "<strong>v2.3.48 &mdash; The &ldquo;run your scan&rdquo; email now drops you right at the uploader.</strong> Someone clicking the link-back email <em>already</em> saw the demo &mdash; that&rsquo;s why they asked for the link &mdash; so landing them at the top to re-scroll Rejuv&rsquo;s numbers is friction. The email link now deep-links below the fold (<code>/scan#drop</code>) and the page smooth-scrolls straight to the &ldquo;Now see YOUR real numbers&rdquo; drop zone on arrival. Small thing; it&rsquo;s the difference between &ldquo;oh, here it is&rdquo; and &ldquo;where do I&hellip;&rdquo; <strong>Touched</strong>: <code>scan.tsx</code>, <code>scan-followup.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.47",
    date: "June 2026",
    items: [
      "<strong>v2.3.47 &mdash; Scan demo: &ldquo;email me a link to run my own scan&rdquo; (Slice B).</strong> A visitor who loves the Rejuv example but doesn&rsquo;t have their export handy can now drop their email and get a link back to <code>/scan</code> &mdash; so we capture the lead instead of losing them to &ldquo;I&rsquo;ll do it later.&rdquo; The capture sits right under the uploader in the pre-loaded view; submitting records the lead (<code>csv_scanner_leads</code>, source <code>sample-return-link</code>) and sends a short, friendly &ldquo;run yours&rdquo; email via the existing Resend path (warm sender, link to the SmartSpa scan). Also tightened the hero subhead to three clean lines. <strong>SMS is the fast-follow</strong> (needs an outbound gateway). <strong>New</strong>: <code>sendScanReturnLink</code> fn + <code>sendScanReturnLinkEmail</code> composer. <strong>Touched</strong>: <code>scan.tsx</code>, <code>scan.functions.ts</code>, <code>scan-followup.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.46",
    date: "June 2026",
    items: [
      "<strong>v2.3.46 &mdash; Scan page leads with the wow: Rejuv&rsquo;s real numbers first, &ldquo;drop yours&rdquo; below.</strong> The pre-loaded example now renders <strong>above</strong> the uploader, not below it &mdash; so the instant a visitor lands they see real results (5,754 appointments, $6,592/mo saved, money on the table), <em>then</em> a &ldquo;Now see YOUR real numbers&rdquo; prompt with the drag-and-drop zone directly beneath. The immediate proof does the selling; the ask comes second. (The same dropzone still appears on its own after a reset.) <strong>Touched</strong>: <code>scan.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.45",
    date: "June 2026",
    items: [
      "<strong>v2.3.45 &mdash; Scan CTAs reframed: &ldquo;see <em>our</em> real numbers&rdquo; &rarr; &ldquo;drop yours to see <em>yours</em>.&rdquo;</strong> Now that the page opens on Rejuv&rsquo;s real scan, the copy leans into it: the subhead reads &ldquo;these are our own spa&rsquo;s real numbers &mdash; Rejuv; drag &amp; drop your CSV to see yours,&rdquo; the uploader says &ldquo;Drag &amp; drop your CSV to see YOUR numbers,&rdquo; the example banner says &ldquo;these are Rejuv&rsquo;s real numbers &mdash; drag &amp; drop above to see yours,&rdquo; and the closing prompt reads &ldquo;Now see YOUR real numbers.&rdquo; The OURS&rarr;YOURS pairing makes the value obvious before any effort. <strong>Touched</strong>: <code>scan.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.44",
    date: "June 2026",
    items: [
      "<strong>v2.3.44 &mdash; Sample scan: genericized staff names (kept Karen, who&rsquo;s verifiable).</strong> The pre-loaded example&rsquo;s provider breakdown showed real staff first names. <strong>Karen</strong> stays &mdash; she&rsquo;s the only provider listed on the public site, so the numbers stay verifiable &mdash; while the other two providers now show as <strong>Provider 2 / Provider 3</strong>. Same numbers, less exposure. <strong>Touched</strong>: <code>scan-sample.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.43",
    date: "June 2026",
    items: [
      "<strong>v2.3.43 &mdash; The scan page now shows real numbers the instant you land &mdash; no upload required.</strong> Asking a cold visitor to find and drop their CSV <em>before</em> they&rsquo;ve seen any value is where funnels lose people. So <code>/scan</code> now opens with a <strong>live example already filled in</strong> &mdash; and we own it: it&rsquo;s <strong>Rejuv Skin Spa, our own med-spa</strong> (&ldquo;we run SmartSpa on ourselves&rdquo;). The visitor immediately sees the full receipt &mdash; 5,754 appointments, $6,592/mo their team already saves, $944&ndash;$1,154/mo more on the table &mdash; with the uploader right above it (&ldquo;drop your export to see your own numbers&rdquo;) and a bottom prompt to run theirs. The example is <strong>pipeline-true</strong>: Rejuv&rsquo;s real Acuity export run through the exact same code path a customer&rsquo;s file hits, baked as aggregate numbers only (zero patient data ships). Drop your own file and it instantly replaces the sample with your real scan + the email/connect actions. <strong>New</strong>: <code>scan-sample.ts</code> (the baked receipt). <strong>Touched</strong>: <code>scan.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.42",
    date: "June 2026",
    items: [
      "<strong>v2.3.42 &mdash; The scan funnel now quotes the real price: free + $5 per booking.</strong> The public <strong>scan</strong> page&rsquo;s call-to-action still read &ldquo;only pay <em>12% of what we recover</em>&rdquo; in two spots &mdash; the <strong>retired</strong> revenue-share model that the pricing sweep missed on this page. Every prospect who ran a scan saw the wrong price. Both now read &ldquo;<strong>only $5 per booking we recover</strong>,&rdquo; matching the one canonical plan everywhere else. Removed the now-unused take-rate import. (The rep-commission economics page legitimately still uses the take-rate &mdash; that&rsquo;s the separate rep-pay system, not customer pricing.) <strong>Touched</strong>: <code>scan.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.41",
    date: "June 2026",
    items: [
      "<strong>v2.3.41 &mdash; Fixed a dead &ldquo;View all activity&rdquo; link in the notification center.</strong> The footer link in the notifications dropdown pointed at <code>/app/operations</code> &mdash; a route that doesn&rsquo;t exist (it was never built), so clicking it went nowhere. It now points at your <strong>Refill home</strong>, which surfaces the live activity feed. Caught by a type-check while shipping copy changes. <strong>Touched</strong>: <code>NotificationCenter.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.40",
    date: "June 2026",
    items: [
      "<strong>v2.3.40 &mdash; Copy that sounds like a spa owner, not a strategy deck.</strong> Swept the word <em>&ldquo;leak&rdquo;</em> out of everything a customer reads &mdash; nobody at a front desk says &ldquo;leak.&rdquo; On the <strong>Our Story</strong> page: &ldquo;Pure leak&rdquo; &rarr; &ldquo;Just gone,&rdquo; &ldquo;watching the leak&rdquo; &rarr; &ldquo;watching that money walk out the door,&rdquo; &ldquo;it&rsquo;s not a leak&rdquo; &rarr; &ldquo;it&rsquo;s not lost,&rdquo; &ldquo;empty-slot leak&rdquo; &rarr; &ldquo;empty slots,&rdquo; and &ldquo;every Monday morning&rdquo; &rarr; &ldquo;every morning&rdquo; (it&rsquo;s not just Mondays). In <strong>Recall</strong> and <strong>Rewards</strong>, the manufacturer no longer &ldquo;leaks&rdquo; your patient to &ldquo;a provider near you&rdquo; &mdash; it <em>sends</em> them (keeping the damning real quote, dropping the jargon). And the public <strong>scan</strong> footer now reads &ldquo;% of lost revenue&rdquo; instead of &ldquo;% of leak.&rdquo; <strong>Touched</strong>: <code>story.tsx</code>, <code>app.refill.recognition.recall.tsx</code>, <code>app.refill.recognition.rewards.tsx</code>, <code>scan.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.39",
    date: "June 2026",
    items: [
      "<strong>v2.3.39 &mdash; Hands-free auto-import moves to the SmartSpa domain.</strong> The email-drop address for auto-ingesting manufacturer reports is now <code>&lt;token&gt;@rewards.smartspa.app</code> (was <code>rewards.getrefill.app</code>) &mdash; so the one verified <strong>smartspa.app</strong> Resend domain now does both directions: it <em>sends</em> patient mail and <em>receives</em> the spa&rsquo;s forwarded reward/treatment exports. Consolidating onto SmartSpa (and off the deprecating getrefill domain) keeps everything under one brand. The displayed address on <strong>Recognition &rarr; Rewards &rarr; Hands-free auto-import</strong> updates automatically (it&rsquo;s built from the ingest-domain constant), and the unguessable per-spa token is unchanged. This was never live before (no inbound MX existed), so there&rsquo;s nothing to migrate. <strong>Go-live needs</strong> a one-time MX on <code>rewards.smartspa.app</code> + a Resend Inbound route to the receiver endpoint. <strong>Touched</strong>: <code>manufacturer-reward-ingest.functions.ts</code> (ingest-domain constant), <code>api.resend.inbound-rewards.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.38",
    date: "June 2026",
    items: [
      "<strong>v2.3.38 &mdash; Promo offers start and end on the right day in your timezone, not UTC&rsquo;s.</strong> Whether a cross-sell promo is &ldquo;active&rdquo; was decided against <strong>today&rsquo;s date in UTC</strong> &mdash; which flips to tomorrow in the evening for any US timezone (after 6pm Mountain, ~5pm Pacific). So on a promo&rsquo;s <strong>first or last day</strong>, the badge would appear (or disappear) several hours early, and &mdash; more consequentially &mdash; a booking made during those evening hours on the final day could be judged past the offer and <strong>skip its $5 cross-sell win</strong> (or record one a few hours before the offer truly opened). The active-offer check now resolves &ldquo;today&rdquo; in <strong>the spa&rsquo;s own timezone</strong> (the same timezone the scheduler already uses) everywhere it runs: public booking badges, the owner calendar, and the win-recording path. Added a shared <code>todayIsoInTz</code> helper alongside the slot engine&rsquo;s existing timezone math. <strong>Touched</strong>: <code>scheduling-slots.ts</code> (new helper), <code>scheduling.functions.ts</code>, <code>scheduling-owner.functions.ts</code>, <code>refill-promo-calendar.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.37",
    date: "June 2026",
    items: [
      "<strong>v2.3.37 &mdash; Allocation queue stats stay accurate past 500 suggestions.</strong> The Recognition allocation screen loads your suggestion history to show the <strong>pending</strong> and <strong>confirmed</strong> counts and the queue list &mdash; but that read was capped at the most recent 500 rows, so a practice that accrued more than 500 suggestions over time would see those counts computed from a partial set (and the &ldquo;showing latest 100 of N&rdquo; label would understate N). The read now pages through the full set, so the headline counts reconcile at any volume. Low-frequency in practice (allocation runs are manual and inventory-bounded), but it&rsquo;s the same load-bearing-list class as the rest of this sweep. <strong>Touched</strong>: <code>refill-recognition-allocation.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.36",
    date: "June 2026",
    items: [
      "<strong>v2.3.36 &mdash; The allocation engine&rsquo;s patient-load fix is now actually in the code (v2.3.33 shipped the note, not the change).</strong> v2.3.33 described paginating the rebate-allocation patient read past Postgres&rsquo;s 1,000-row cap &mdash; but that release committed only the changelog entry; the code edit never landed, so the <code>limit(5000)</code> (which doesn&rsquo;t lift the server&rsquo;s 1,000-row ceiling) was still live. For any practice over 1,000 patients that meant <strong>patients beyond row 1,000 were excluded from rebate allocation</strong>, and the spend-decile cutoff (the &ldquo;top 10% by spend&rdquo; cohort boundary) was computed from a truncated population &mdash; skewing who landed in each cohort. The read now genuinely pages through the full patient set (stable ordering so paging can&rsquo;t skip or double-count a patient), so allocation scores the whole practice. Caught on a fresh-walk re-audit by checking the commit, not just the changelog. <strong>Touched</strong>: <code>refill-recognition-allocation.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.35",
    date: "June 2026",
    items: [
      "<strong>v2.3.35 &mdash; Confirming a rebate allocation now updates the deployed-units counter atomically &mdash; no lost updates, no double-counts.</strong> When you confirm an allocation suggestion, Refill marks those rebate units as <strong>deployed</strong> against the manufacturer pool. That counter was updated with a read-then-write (read the current deployed count, add this suggestion&rsquo;s units, write it back) &mdash; which has two failure modes: <strong>(1)</strong> if two suggestions drawing from the <em>same</em> rebate pool were confirmed at nearly the same moment, both read the same starting number and the second write clobbered the first, so a unit deployment was <strong>silently lost</strong> and the pool looked more available than it really was (risking over-allocation); and <strong>(2)</strong> re-confirming an already-confirmed suggestion (a double-click or retry) <strong>added the units a second time</strong>. The increment is now a single atomic database operation (clamped so it can never exceed the pool total), and the deploy only fires on a genuine pending&rarr;confirmed transition, so re-confirms are no-ops. The mutation is also now tenant-scoped at the database layer, not just the preceding read. <strong>Touched</strong>: new <code>refill_increment_units_deployed</code> RPC (migration), <code>refill-recognition-allocation.functions.ts</code>, <code>types.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.34",
    date: "June 2026",
    items: [
      "<strong>v2.3.34 &mdash; Your lifetime recovered-revenue total now counts every win, not just the latest 1,000.</strong> The live recovery feed on your home screen reads all of your recovery events to sum the headline <strong>&ldquo;recovered revenue&rdquo;</strong> figure (lifetime + today / 7-day / 30-day) &mdash; but that read was unpaginated, so past the database&rsquo;s 1,000-row ceiling it silently returned only your <strong>most recent</strong> 1,000 events. Because the events come back newest-first, the recent windows stayed correct, but the <strong>lifetime total and event count would quietly stop growing</strong> &mdash; understating the very number that proves Refill&rsquo;s worth, and dropping your oldest wins first. The read now pages through the full set, so the lifetime total reflects every recovered booking at any scale. <strong>Touched</strong>: <code>refill-tenants.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.33",
    date: "June 2026",
    items: [
      "<strong>v2.3.33 &mdash; Rebate allocation now considers every patient, not just the first 1,000.</strong> The Recognition allocation engine loaded your patient list with a <code>limit(5000)</code> &mdash; but the database enforces its own 1,000-row ceiling that a client-side limit can&rsquo;t lift, so any spa past 1,000 patients had everyone beyond row 1,000 <strong>silently excluded from rebate allocation</strong>, and the spend-decile cutoff (which decides the &ldquo;top 10% by spend&rdquo; cohort) was computed from that truncated list &mdash; skewing who landed in each cohort. The read now pages through the full patient set, so allocation scores the whole practice. <strong>Touched</strong>: <code>refill-recognition-allocation.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.32",
    date: "June 2026",
    items: [
      "<strong>v2.3.32 &mdash; The rescue matcher can&rsquo;t silently skip waitlist patients at scale.</strong> When an appointment cancels, the recovery engine picks who to offer the open slot to &mdash; and it did so with three unpaginated reads: the active-waitlist list, the patient-details lookup, and (the riskiest) each candidate&rsquo;s all-time appointment history used to match them to the right provider. Past the database&rsquo;s 1,000-row ceiling, any of these silently returned a partial set &mdash; so waitlist members beyond row 1,000 were <strong>excluded from every rescue</strong>, and a truncated appointment history could mis-read a patient&rsquo;s usual provider and drop them from the match. All three now page through the full set (the appointment read also chunks the candidate list so a big waitlist can&rsquo;t overflow the query). On the core revenue path, no eligible patient gets silently passed over. <strong>Touched</strong>: <code>emma-rescue.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.31",
    date: "June 2026",
    items: [
      "<strong>v2.3.31 &mdash; Blocked time can&rsquo;t silently &ldquo;disappear&rdquo; from a busy calendar.</strong> The owner calendar (day &amp; week/month views) and the public booking slot engine each read your time-blocks (lunches, vacations, meetings) with an unpaginated query &mdash; so once a spa accrued more than 1,000 blocks (a single daily lunch block is ~365/year), the database silently returned only the first 1,000 and dropped the rest. The visible symptom: older blocked time would render as <strong>free</strong> on the calendar, and worse, the booking page could offer patients a <strong>bookable slot over blocked time</strong>. All three reads now page through the full set, so blocks stay complete at any age. <strong>Touched</strong>: <code>scheduling-owner.functions.ts</code>, <code>scheduling.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.30",
    date: "June 2026",
    items: [
      "<strong>v2.3.30 &mdash; Deleting a service cleans up after itself, and new services are bookable by default.</strong> Two Catalog fixes: (1) deleting a service left its provider assignments, add-on links, and product/COGS links behind as dangling rows (no database cascade) &mdash; it now removes them so nothing orphans or resurfaces. (2) A service created in Catalog now defaults to <strong>online-bookable</strong> (matching services created in Booking, and the natural expectation that a service you add can be booked) &mdash; except add-ons, which attach to a service rather than being booked on their own. <strong>Touched</strong>: <code>refill-catalog.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.29",
    date: "June 2026",
    items: [
      "<strong>v2.3.29 &mdash; Catalog import + lists no longer cap at 1,000 rows.</strong> The services/products CSV import matched existing rows with an unpaginated read &mdash; so on a catalog with more than 1,000 services or products, a re-import didn&rsquo;t recognize the overflow and <strong>re-created them as duplicates</strong> (the same bug just fixed for patient import). The service and product list reads had the same silent cap. All four now page through the full set, so re-imports stay idempotent and the lists stay complete at any size &mdash; important as the Service Library balloons the catalog. <strong>Touched</strong>: <code>refill-catalog.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.28",
    date: "June 2026",
    items: [
      "<strong>v2.3.28 &mdash; A failed service reorder now surfaces instead of silently not saving.</strong> Drag-reordering services fired all the position updates in parallel but never checked whether any failed &mdash; so a network hiccup or row lock could leave the saved order out of sync with what the screen showed, with no error. The reorder now verifies every update and reports a failure, so the on-screen order and the stored order can&rsquo;t silently diverge. <strong>Touched</strong>: <code>scheduling-settings.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.27",
    date: "June 2026",
    items: [
      "<strong>v2.3.27 &mdash; Bulk-tagging all patients no longer stops at 1,000, + a date-window edge fixed.</strong> Applying a tag to a large selection read the selected patients in one query that silently capped at 1,000 &mdash; so a select-all on a 1,000+ patient list (Rejuv is ~1.1k) tagged only the first 1,000 and ignored the rest (the write side already batched correctly; the read was the bottleneck). It now reads in chunks so every selected patient is tagged. Also fixed the &ldquo;last 12 months&rdquo; filter on the patient list + detail, which computed the cutoff in local time but compared against UTC dates &mdash; a one-day edge near midnight for non-UTC users. <strong>Touched</strong>: <code>patient-ingest.functions.ts</code>, <code>app.refill.patients.index.tsx</code>, <code>app.refill.patients.$patientId.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.26",
    date: "June 2026",
    items: [
      "<strong>v2.3.26 &mdash; Confirming/dismissing a contact suggestion now works while viewing-as a spa.</strong> On the Contacts page, the &ldquo;confirm match&rdquo; and &ldquo;dismiss&rdquo; actions authed as the raw signed-in user instead of the spa being viewed &mdash; so when an operator was viewing-as a tenant, the candidate and patient were looked up under the wrong account and the action failed or misfiled. Both ends now plumb the view-as identity (matching every other action on the page), so the write lands on the right spa&rsquo;s records. <strong>Touched</strong>: <code>patient-ingest.functions.ts</code>, <code>app.refill.patients.contacts.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.25",
    date: "June 2026",
    items: [
      "<strong>v2.3.25 &mdash; Patient import is now safe past 1,000 patients.</strong> The QuickBooks/sales re-upload matched existing patients with one unpaginated query, and read the upsert result the same way &mdash; both silently cap at 1,000 rows. On a tenant with more than 1,000 patients (Rejuv is already ~1.1k), the overflow wasn&rsquo;t recognized as existing, so a re-upload <strong>re-created those patients as duplicates</strong> (wiping the contact fields they&rsquo;d accumulated) and dropped some of their transactions. The import now pages through every existing patient before matching, and chunks the write so the full id map comes back intact &mdash; no duplicates, no dropped history, at any tenant size. <strong>Touched</strong>: <code>patient-ingest.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.24",
    date: "June 2026",
    items: [
      "<strong>v2.3.24 &mdash; Old install-wizard URLs no longer drop the connect token on redirect.</strong> The four back-compat redirects (Vagaro, Zenoti, Booker, Boulevard) from the v2.3.18 reorg sent stale bookmarks to the new Calendar paths but stripped the OAuth <code>state</code> (and Boulevard&rsquo;s <code>application_id</code>) query params &mdash; so a bookmark opened mid-connect would land param-less and break the handshake. The redirects now forward those params through. Latent edge (every live caller already points at the new paths), closed for completeness. <strong>Touched</strong>: <code>app.refill.settings.{vagaro,zenoti,booker,boulevard}-install.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.23",
    date: "June 2026",
    items: [
      "<strong>v2.3.23 &mdash; The billing ledger now shows every win, and exports.</strong> The expandable win list was capped at the 100 most recent &mdash; so a busy month&rsquo;s summary could say &ldquo;138 wins&rdquo; while the detail listed only 100, with no way to see the rest. The list now shows <strong>all</strong> of the period&rsquo;s wins (paged past Postgres&rsquo;s 1,000-row read cap so nothing is silently dropped), inside a scrollable area, so the count always reconciles with the totals above. Added a <strong>Download CSV</strong> button that exports the full month&rsquo;s billable events (date &middot; type &middot; patient &middot; visit revenue &middot; charge) as an audit-grade record. Reinforces the visible-counterfactual-ledger promise. <strong>Touched</strong>: <code>refill-fee-rules.functions.ts</code>, <code>app.billing.index.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.22",
    date: "June 2026",
    items: [
      "<strong>v2.3.22 &mdash; A confirmation-email hiccup can no longer fail a booking that actually worked.</strong> Both booking paths (public self-book and owner-created) awaited the confirmation email <em>after</em> the appointment was already saved, but without a guard &mdash; so if the send threw (e.g. a mis-set timezone hitting <code>Intl</code>), the handler crashed and the patient/owner saw an error for a booking that had in fact succeeded. The send is now best-effort on both paths: it&rsquo;s wrapped so a failure is logged but never blocks the confirmed booking. Companion to v2.3.21&rsquo;s reminder-cron fix. <strong>Touched</strong>: <code>scheduling.functions.ts</code>, <code>scheduling-owner.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.21",
    date: "June 2026",
    items: [
      "<strong>v2.3.21 &mdash; Reminder emails no longer silently dropped on a transient failure.</strong> The reminder cron marked an appointment as &ldquo;reminded&rdquo; <em>before</em> the email actually sent &mdash; so if the send hiccuped (a Resend blip, a timeout, a bad-data throw), the appointment was flagged done and <strong>never retried</strong>, and the patient just never got their reminder. Now the send runs inside a guard and, on any failure, the &ldquo;reminded&rdquo; claim is <strong>released</strong> so the next run retries it. A throw on one appointment also no longer aborts the rest of the batch. Double-send protection is unchanged. New <code>retryQueued</code> count in the cron response surfaces how many were re-queued. <strong>Touched</strong>: <code>api.cron.scheduling-reminders.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.20",
    date: "June 2026",
    items: [
      "<strong>v2.3.20 &mdash; Closed a double-billing race on recovery events.</strong> Every billable win (slot-fill, recall, cross-sell) was de-duplicated by an app-level check-then-insert, so two near-simultaneous calls for the same appointment &mdash; a retried claim, a double-tap, a timed-out-then-retried request &mdash; could both slip through and record the win twice, billing $5 twice for one booking. There&rsquo;s now a database-level guarantee of <strong>one win per appointment</strong>, and the recorder treats a lost race as the idempotent success it is (returns the existing win) instead of erroring. Conservative-billing integrity restored. <strong>Touched</strong>: <code>emma-attribution.functions.ts</code>, migration <code>v2_3_20_recovery_event_unique_appointment</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.19",
    date: "June 2026",
    items: [
      "<strong>v2.3.19 &mdash; Nav now reads as Solutions-first.</strong> The 6-chip nav was a flat row that interleaved workspaces with data/back-office. It&rsquo;s now grouped with a divider: the action <strong>Solutions</strong> lead &mdash; <strong>Calendar &middot; Promos &middot; Refill</strong> &mdash; then the <strong>Spine</strong> &mdash; <strong>Patients &middot; Catalog &middot; Account</strong>. Same chips, same routes; the order + divider just make the daily-driver workspaces come first and the structure read at a glance. Part of the ongoing IA reorg. <strong>Touched</strong>: <code>RefillNav.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.18",
    date: "June 2026",
    items: [
      "<strong>v2.3.18 &mdash; PMS install wizards moved into the Calendar Solution + a connect-flow bug fixed.</strong> The four per-platform install wizards (Vagaro, Zenoti, Booker, Boulevard) now live under <code>/app/refill/calendar/*-install</code> instead of the old Settings namespace &mdash; finishing the &ldquo;each Solution owns its own setup&rdquo; reorg (old URLs still redirect). <strong>Bug fixed:</strong> the PMS connect flow had been throwing &ldquo;not in the OAuth allowlist&rdquo; for every platform since the v2.1.0 reorg &mdash; the Connections page moved to <code>/calendar/connections</code> but the returnTo allowlist was never updated. Also fixed a latent Boulevard route-id mismatch and restored the &ldquo;X connected &mdash; syncing live&rdquo; success toast (it was being dropped through a param-stripping redirect). <strong>Touched</strong>: <code>app.refill.calendar.{vagaro,zenoti,booker,boulevard}-install.tsx</code> (new), settings stubs, <code>emma-scheduler.functions.ts</code>, <code>schedulers/{vagaro,boulevard}.ts</code>, the 4 install-callbacks, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.17",
    date: "June 2026",
    items: [
      "<strong>v2.3.17 &mdash; Booking emails now itemize the visit.</strong> Confirmation and reminder emails were generic &mdash; just &ldquo;your appointment is confirmed for [date] at [time].&rdquo; They now show the <strong>service</strong>, the <strong>provider</strong>, the <strong>total visit length</strong>, and any <strong>add-ons</strong> the patient chose &mdash; matching the in-app booking card and the add-to-calendar event. Covers all three send paths: public self-book, owner-created bookings, and the reminder cron. <strong>Touched</strong>: <code>scheduling-email.ts</code>, <code>scheduling.functions.ts</code>, <code>scheduling-owner.functions.ts</code>, <code>api.cron.scheduling-reminders.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.16",
    date: "June 2026",
    items: [
      "<strong>v2.3.16 &mdash; Hours label is context-aware again.</strong> Refined v2.3.15: with a <strong>single provider</strong>, their hours are the business hours &mdash; so the section reads <strong>&ldquo;Business hours&rdquo;</strong> and a day is <strong>&ldquo;Closed/off&rdquo;</strong> (a business is closed). With <strong>multiple providers</strong>, each has their own week &mdash; <strong>&ldquo;Provider hours&rdquo;</strong> and <strong>&ldquo;Off/Closed&rdquo;</strong> (a person takes time off). The v2.3.15 toggle symmetry stays (select a day → it reopens; mark off/closed → it deselects). <strong>Touched</strong>: <code>BusinessHoursSection.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.15",
    date: "June 2026",
    items: [
      "<strong>v2.3.15 &mdash; Provider hours: selecting a day reopens it, and it&rsquo;s always &ldquo;Provider hours&rdquo; now.</strong> Completes the toggle symmetry: checking <strong>Off/Closed</strong> deselects the day (v2.3.14), and now <em>selecting</em> a day (its left checkbox) clears any Off/Closed on it &mdash; selecting a day means it&rsquo;s a working day. Also retired the &ldquo;Business hours&rdquo; label entirely: this section is always <strong>Provider hours</strong> (even a solo provider&rsquo;s hours are, in practice, the business hours), and the label is always <strong>Off/Closed</strong> (a person takes time off). <strong>Touched</strong>: <code>BusinessHoursSection.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.14",
    date: "June 2026",
    items: [
      "<strong>v2.3.14 &mdash; Provider hours: marking a day off now deselects it, and the label fits the context.</strong> Two fixes on the weekly grid: (1) checking a day&rsquo;s <strong>Off/Closed</strong> box now also clears that day from the bulk day-selection (it no longer stays highlighted/selected for the next &ldquo;Apply&rdquo;), and the bulk &ldquo;Set off/closed&rdquo; clears the selection too. (2) The label is now context-aware &mdash; in multi-provider <em>Provider hours</em> it reads <strong>&ldquo;Off/Closed&rdquo;</strong> (a person takes a day off), while single-provider <em>Business hours</em> keeps <strong>&ldquo;Closed/off&rdquo;</strong> (a business is closed). <strong>Touched</strong>: <code>BusinessHoursSection.tsx</code>, <code>useBookingSettings.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.13",
    date: "June 2026",
    items: [
      "<strong>v2.3.13 &mdash; Expand/Collapse pill spacing, done right (and bleed-proof).</strong> Root cause of the earlier overlap: the pill was the first item in the section list, so its &ldquo;bottom gap&rdquo; was really the next card&rsquo;s top margin &mdash; and any negative margin to shrink that gap dragged the full-width card up <em>under</em> the right-aligned pill. Fix: the pill now sits on its own with a positive 12px margin, and the section list moved into its own spacing wrapper. Result is ~12px above and below the pill (the 50% you wanted), with zero chance of the card bleeding into it. <strong>Touched</strong>: <code>app.refill.calendar.booking.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.12",
    date: "June 2026",
    items: [
      "<strong>v2.3.12 &mdash; Tightened the spacing around the Expand/Collapse pill by ~50%.</strong> Both the gap above (tabs → pill) and below (pill → first card) went from ~24px to ~12px. The top half is done with reduced container padding; the bottom half uses the same clean negative margin from v2.3.9 (card stays a clear 12px below the pill, no bleed). <strong>Touched</strong>: <code>app.refill.calendar.booking.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.11",
    date: "June 2026",
    items: [
      "<strong>v2.3.11 &mdash; Fix: Expand/Collapse pill no longer bleeds into the first card.</strong> The previous spacing used a negative margin, which pulled the full-width &ldquo;Your booking link&rdquo; card upward behind the right-aligned pill, overlapping it. Removed the negative margins entirely &mdash; the pill now sits with the page&rsquo;s even section gap on both sides (equal above and below), cleanly separated from the card. <strong>Touched</strong>: <code>app.refill.calendar.booking.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.10",
    date: "June 2026",
    items: [
      "<strong>v2.3.10 &mdash; Spacing polish: the Expand/Collapse pill now sits higher with equal padding above and below.</strong> It was hanging low against the first card (more room above than below); swapped the asymmetric bottom margin for a balanced one so it reads as a centered control between the tabs and the cards. <strong>Touched</strong>: <code>app.refill.calendar.booking.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.9",
    date: "June 2026",
    items: [
      "<strong>v2.3.9 &mdash; Expand all / Collapse all on Online booking is now a real control.</strong> The two faint text links became a single <strong>segmented pill</strong> with chevron icons (⌄ Expand all · ⌃ Collapse all) &mdash; clearly tappable, both actions always visible, matching the segmented toggles used elsewhere on the page. <strong>Touched</strong>: <code>app.refill.calendar.booking.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.8",
    date: "June 2026",
    items: [
      "<strong>v2.3.8 &mdash; Small polish: no more &ldquo;Online booking&rdquo; said three times on one screen.</strong> The page title, the active tab, and the on/off section header all read &ldquo;Online booking.&rdquo; The first two are expected (the tab names the page you&rsquo;re on); the third was just redundant. Since that section actually holds the master on/off toggle <em>and</em> the timezone, it&rsquo;s now titled <strong>&ldquo;Status &amp; timezone&rdquo;</strong> &mdash; which says what it does. <strong>Touched</strong>: <code>app.refill.calendar.booking.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.7",
    date: "June 2026",
    items: [
      "<strong>v2.3.7 &mdash; Author your own cross-sell offers now also lives in Calendar, as a tab.</strong> The owner-authored cross-sell card (pick a service, set a discount, it badges that service at booking and earns $5 when a matched patient books) used to live only in <strong>Promos → Reward signals</strong>. Since those offers surface <em>at booking</em>, it now also has a home right in the <strong>Calendar</strong> Solution as a new <strong>Cross-sell</strong> tab (Schedule · Appointments · Online booking · <strong>Cross-sell</strong> · Connections). Both surfaces read and write the same offers &mdash; one room, two doors &mdash; via a shared <code>SpaOffersCard</code> component (extracted from the Promos page, no behavior change). <strong>Touched</strong>: new <code>SpaOffersCard.tsx</code> + <code>app.refill.calendar.offers.tsx</code>, <code>CalendarTabs.tsx</code>, <code>app.refill.recognition.rewards.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.6",
    date: "June 2026",
    items: [
      "<strong>v2.3.6 &mdash; Date-specific hours can now cover a date range, not just one day.</strong> The override editor still defaults to a single date, but there&rsquo;s now a <strong>Range</strong> checkbox &mdash; tick it and a <strong>From / To</strong> pair appears, so you can close (or re-time) a whole stretch in one go (a vacation week, a holiday span) instead of adding each day by hand. Under the hood each day in the range becomes its own single-date override, so the booking page, the saved list, and per-day removal all keep working exactly as before &mdash; no new data model. Saving a span confirms with &ldquo;Saved override for N dates.&rdquo; <strong>Touched</strong>: <code>useBookingSettings.ts</code>, <code>DateOverridesSection.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.5",
    date: "June 2026",
    items: [
      "<strong>v2.3.5 &mdash; Online-booking settings now make it obvious when a change needs saving.</strong> Some edits on this page save instantly (adding a provider, a room, a date override, adding/deleting a service &mdash; each confirms with a toast); others are <em>staged</em> and only persist when you hit Save (your weekly hours, booking rules, the on/off toggle + timezone, and a service&rsquo;s bookable/duration/buffer). Previously the only &ldquo;unsaved&rdquo; cue was tiny text by a Save button at the very bottom of the page &mdash; so staging hours mid-page felt like it &ldquo;didn&rsquo;t stick.&rdquo; Now: <strong>a &ldquo;Save changes&rdquo; chip appears + lights up right in the header of whichever section you edited</strong> (Hours, Booking rules, Bookable services, the on/off card), and a <strong>sticky Save bar</strong> (with Discard) floats up the moment anything is pending, so Save is always one click away no matter where you&rsquo;ve scrolled. The hours &ldquo;Apply&rdquo; toast no longer says &ldquo;Applied&rdquo; (which read as saved) &mdash; it now says &ldquo;&hellip; &mdash; Save changes to confirm.&rdquo; <strong>Touched</strong>: <code>useBookingSettings.ts</code>, <code>booking/fields.tsx</code>, <code>BusinessHoursSection.tsx</code>, <code>BookingRulesSection.tsx</code>, <code>BookableServicesSection.tsx</code>, <code>app.refill.calendar.booking.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.4",
    date: "June 2026",
    items: [
      "<strong>v2.3.4 &mdash; Fix: adding a service from &ldquo;Bookable services&rdquo; no longer duplicates it in your Catalog.</strong> The &ldquo;Add service&rdquo; box inside the Online-booking settings used to <em>always insert a brand-new row</em> &mdash; so typing the name of a service that already lived in your Catalog (it&rsquo;s hidden there until it&rsquo;s bookable) created a silent second copy. Now it&rsquo;s <strong>dedup-aware</strong>: if a service with that name already exists, we just flip <em>that</em> one bookable and tell you so (&ldquo;already in your catalog &mdash; made it bookable&rdquo;) instead of cloning it. Also added a re-entrancy guard so a fast Enter+click (or double-Enter) can&rsquo;t fire two creates. The Catalog page&rsquo;s own &ldquo;new service&rdquo; button is unchanged (there the full list is in view and a fresh entry is the intent). <strong>Touched</strong>: <code>scheduling-settings.functions.ts</code>, <code>useBookingSettings.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.3",
    date: "June 2026",
    items: [
      "<strong>v2.3.3 &mdash; Checkout is now one simple plan: free + $5 per booking.</strong> Retired the tiered <code>$99/mo Hybrid</code> (which still carried an 8% revenue share) and <code>$299/mo Predictable</code> subscription plans from the checkout flow &mdash; there&rsquo;s now a single performance plan: <strong>free to run, $5 for each booking we create</strong>, billed monthly. This aligns checkout with how the billing engine has actually charged since v1.93.0 (it retired tiers as the rate source). The checkout endpoint drops the recurring-subscription path entirely and always opens the simple setup-mode flow (just puts a card on file); the owner billing page already used this single plan. <strong>Touched</strong>: <code>api.refill-checkout.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.2",
    date: "June 2026",
    items: [
      "<strong>v2.3.2 &mdash; Pricing copy now everywhere says the real model: free + $5 per booking.</strong> Swept every customer-facing surface that still pitched the retired &ldquo;12% of recovered revenue&rdquo; model &mdash; it now reads <strong>free + $5 for each booking we create</strong>, matching how the billing engine has actually charged since v1.93.0. Updated: the Our Story pricing paragraph (with corrected example math), the Start page fine print, the Claim-your-business cards + footer, the Scan follow-up emails (plain-text + both HTML variants), Karen&rsquo;s Day-28 trial-drip message, the owner dashboard hero line, and the checkout &ldquo;Performance&rdquo; plan description. The admin <strong>revenue-share-discount offer type is retired</strong> &mdash; no longer creatable (kept defined only so any legacy offer still renders). <strong>Left untouched on purpose:</strong> rep/recruiter commission (a separate economics system), the billing engine&rsquo;s internal percent-mode infrastructure, and the paid <code>$99/mo Hybrid</code> / <code>$299/mo Predictable</code> checkout tiers (those are a product-structure decision, flagged separately). <strong>Touched</strong>: <code>story.tsx</code>, <code>start.tsx</code>, <code>claim-your-business.tsx</code>, <code>scan-followup.ts</code>, <code>refill-drip.ts</code>, <code>RefillHome.tsx</code>, <code>api.refill-checkout.ts</code>, <code>refill-offers.ts</code>, <code>app.admin.refill-trials.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.1",
    date: "June 2026",
    items: [
      "<strong>v2.3.1 &mdash; Hands-free auto-import now handles treatment history too.</strong> The email-drop pipe (<code>&lt;your-token&gt;@rewards.getrefill.app</code>) previously only understood reward snapshots. It now <strong>routes each attachment by what it actually is</strong>: a manufacturer treatment report (last-transaction or full TransactionsReport) goes to the cadence ingest, a reward export goes to the reward ingest &mdash; auto-detected from the file&rsquo;s own columns, no dropdown, no sender trust. So once you point a scheduled Allergan export (of either kind) at your private address, it just lands. Still gated on the one-time DNS/MX setup to go live. <strong>Touched</strong>: <code>api.resend.inbound-rewards.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.3.0",
    date: "June 2026",
    items: [
      "<strong>v2.3.0 &mdash; The full Allergan treatment history now feeds cadence too (Lane 2, history half).</strong> The <strong>Treatment history</strong> card on Promos &rarr; Reward signals now also accepts Allergan&rsquo;s full <strong>TransactionsReport</strong> export (the year-by-year history files) &mdash; Refill auto-detects which kind of report you dropped. Where the pre-aggregated report gave only each patient&rsquo;s <em>last</em> Botox / Juv&eacute;derm date, the history gives their <strong>complete dated treatment timeline</strong> across every product (all the Juv&eacute;derm variants &mdash; Voluma, Volbella, Volux, Vollure, Ultra, Skinvive &mdash; plus Botox, Latisse, CoolSculpting and more), and reaches patients beyond the 365 in the short report. Validated against Rejuv&rsquo;s real exports: <strong>8,134 treatment dates</strong> (5,682 toxin &middot; 2,119 filler &middot; 333 device/retail) across 2020&ndash;2026. Each line dedupes on its real Allergan transaction ID, so the overlapping date-range files collapse cleanly and re-uploads are no-ops. <strong>Loyalty rows</strong> (Points Used / Giftcard Applied / Promotion Applied) are counted and skipped &mdash; they&rsquo;re not treatments. <strong>Key finding:</strong> Allergan&rsquo;s reports carry <em>no treatment price</em> (only loyalty points + redemption credits), so this lane is cadence/recency only &mdash; treatment revenue stays sourced from QuickBooks, which is the only place it exists. Same $0 / source-tagged safety model as v2.2.0; no migration. <strong>Touched</strong>: <code>manufacturer-transaction-csv.ts</code> (long-format parser + shape dispatcher, reuses the canonical <code>resolveProduct</code> classifier), <code>manufacturer-transaction-ingest.functions.ts</code> (real-transaction-id dedupe), <code>recognition.rewards.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.2.0",
    date: "June 2026",
    items: [
      "<strong>v2.2.0 &mdash; Manufacturer treatment history now sharpens &ldquo;overdue for a touch-up&rdquo; (Lane 2).</strong> On <strong>Promos &rarr; Reward signals</strong>, a new <strong>Treatment history</strong> card accepts Allergan&rsquo;s last-transaction reports &mdash; &ldquo;Last BOTOX&reg; / JUV&Eacute;DERM&reg; Transaction&rdquo; (and the device/retail one). Refill matches each patient to your book (email &rarr; phone &rarr; name) and records their most-recent treatment date. A patient who&rsquo;s gone quiet in your books but was recently treated is now read correctly, and one who&rsquo;s genuinely lapsed surfaces sooner &mdash; on the Today card and in the rep-shared overdue counts alike. <strong>These are dates only:</strong> they carry no dollar amount, so they&rsquo;re never counted as revenue or a visit (your patient list&rsquo;s visit counts, lifetime spend and primary brand stay untouched) &mdash; they exist purely to make recency accurate. Re-uploading the same report is a clean no-op. <strong>Touched</strong>: new <code>manufacturer-transaction-csv.ts</code> (parser) + <code>manufacturer-transaction-ingest.functions.ts</code> (matcher/ingest), <code>patient-csv.ts</code> (summary stays QuickBooks-truth), <code>patient-ingest.functions.ts</code> + <code>spa-rep-data-share.functions.ts</code> (lapsed queries opt the dates in), <code>recognition.rewards.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.1.5",
    date: "June 2026",
    items: [
      "<strong>v2.1.5 &mdash; Page headers no longer squeeze the title when there are lots of buttons.</strong> On pages with several header actions (e.g. Service catalog: Re-categorize / Import CSV / Add from library / Add category / Add service), the buttons claimed the row and crushed the title + description into a narrow left sliver. The shared page header now gives the title a sensible minimum width and lets the action buttons <strong>wrap onto their own line</strong> when they don&rsquo;t fit beside it &mdash; so the description reads full-width instead of stacking one word per line. Affects every page header; simple ones are unchanged. Also swapped the Catalog sub-tabs so <strong>Services leads</strong> (before Products) and the Catalog chip lands on Services &mdash; it&rsquo;s the headliner for a spa. <strong>Touched</strong>: <code>PageHeader.tsx</code>, <code>catalog.services.tsx</code> / <code>catalog.products.tsx</code> (tab order), <code>RefillNav</code> / <code>RefillHome</code> (Catalog → Services), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.1.4",
    date: "June 2026",
    items: [
      "<strong>v2.1.4 &mdash; &ldquo;Light Mode&rdquo; is now &ldquo;Lite Mode.&rdquo;</strong> Renamed the email-forwarding connector tier from <em>Light Mode</em> to <em>Lite Mode</em> across every user-facing surface (the setup page, the Account tab, scheduler-card links, copy). Purely a naming change &mdash; same feature. The light/dark <em>theme</em> toggle is untouched. <strong>Touched</strong>: <code>settings.light-mode.tsx</code>, <code>SettingsTabStrip</code>, <code>calendar.connections.tsx</code>, and related copy; <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.1.3",
    date: "June 2026",
    items: [
      "<strong>v2.1.3 &mdash; Lite Mode auth fix + two polish passes.</strong> <strong>Fix:</strong> the <strong>Lite Mode</strong> settings page threw <em>&ldquo;Invalid session &mdash; please sign out and back in (no claims)&rdquo;</em> even on a healthy session. Its server functions called the auth resolver with the wrong argument shape (an extra <code>data:</code> wrapper), so the access token arrived empty and token verification found no claims &mdash; a long-standing bug the type-checker had flagged but that was buried in baselined noise. Corrected all six calls. <strong>Polish:</strong> the <strong>Account</strong> and <strong>Spa profile</strong> pages were left-pinned with empty space on the right &mdash; now centered like the rest of the section. And switching to <strong>Reminders</strong> briefly flashed an old &ldquo;Preshow agent&rdquo; title with no tabs before loading &mdash; the loading state now matches the real page (title + tabs), so the transition is seamless. <strong>Touched</strong>: <code>light-mode.functions.ts</code>, <code>settings.account.tsx</code> / <code>settings.spa-profile.tsx</code>, <code>recovery.preshow.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.1.2",
    date: "June 2026",
    items: [
      "<strong>v2.1.2 &mdash; Back-office consolidated under one &ldquo;Account&rdquo; chip.</strong> Reports and Settings were two separate top-level chips for what are both owner/back-office surfaces. Merged them: the <strong>Settings</strong> chip is now <strong>Account</strong>, and <strong>Reports</strong> joined it as the lead tab &mdash; so the Account section&rsquo;s tabs are <strong>Reports · Login · Sender · Spa profile · Lite mode · Billing · Attribution</strong> (the old Account tab renamed <em>Login</em> to avoid Account-inside-Account). One flat tab strip, no nesting; every old URL still resolves. That takes the nav to <strong>6 chips</strong> &mdash; Patients · Calendar · Catalog · Promos · Refill · Account &mdash; cleanly split into operational Solutions and back-office. <strong>Touched</strong>: <code>SettingsTabStrip</code> (Reports tab + Login relabel), <code>RefillNav</code> / <code>RefillShellChrome</code>, <code>reports.tsx</code> (tab strip), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.1.1",
    date: "June 2026",
    items: [
      "<strong>v2.1.1 &mdash; Solution grouping, round 2 (+ a Connections fix).</strong> Tightened the nav from 10 chips to <strong>7</strong> by folding standalone surfaces into the Solutions they belong to: <strong>Recognition is now &ldquo;Promos&rdquo;</strong> (the umbrella for rebate inventory, rewards, recall, allocation) and the old manufacturer-offers page moved in as its <strong>&ldquo;Brand Promos&rdquo;</strong> tab; <strong>Inbox</strong> became a tab inside the <strong>Refill</strong> Solution (where outreach replies belong); and <strong>Billing + Attribution</strong> folded into <strong>Settings</strong> (account-level config). Every retired chip&rsquo;s URL still resolves. Also relabeled the booking-hours <em>&ldquo;Closed&rdquo;</em> control to <em>&ldquo;Closed/off&rdquo;</em>. <strong>Fix:</strong> the Calendar &rarr; Connections page crashed with &ldquo;Invariant failed&rdquo; &mdash; it still read OAuth-callback search params (and routed connector return-trips) through the <em>old</em> scheduler route id after the v2.1.0 move; repointed to the new route so connecting a PMS works again. <strong>Touched</strong>: <code>RecognitionTabs</code> / <code>RefillSolutionTabs</code> / <code>SettingsTabStrip</code> / <code>RefillNav</code> / <code>RefillShellChrome</code>, the moved promos/inbox/billing routes + redirect stub, <code>BusinessHoursSection</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.1.0",
    date: "June 2026",
    items: [
      "<strong>v2.1.0 &mdash; The app is now SmartSpa, organized around Solutions.</strong> Two structural moves. <strong>(1) Brand:</strong> the product is now <strong>SmartSpa</strong> &mdash; the umbrella for the whole patient-profitability suite &mdash; everywhere it shows (top-left chrome, sign-in, landing, onboarding, SEO, email chrome). &ldquo;Refill&rdquo; lives on as the <strong>no-show-recovery Solution</strong> under that umbrella, so language describing how it catches and fills cancellations is unchanged. (getrefill.app keeps serving; the smartspa.app domain is a later step.) <strong>(2) IA:</strong> the flat 12-chip strip is regrouping around Solutions that each own their workspace <em>and</em> their settings. This pass: a new <strong>Calendar</strong> Solution merges the old <em>Appointments</em> + <em>Schedule</em> tabs and pulls their settings in as tabs (Schedule &middot; Appointments &middot; Online booking &middot; Connections), and the old standalone <strong>Agents</strong> section <strong>dissolved</strong> into the Solutions it configured &mdash; Reminders + Rescue now live inside <strong>Refill</strong> (with the recovery dashboard as Overview), Allocation moved under <strong>Recognition</strong>, and Attribution moved under <strong>Billing</strong> (Ledger &middot; Attribution). Every old URL redirects, so nothing breaks. <strong>New</strong>: <code>CalendarTabs</code> / <code>RefillSolutionTabs</code> / <code>BillingTabs</code>, <code>onlineBookable</code> on the Service type. <strong>Touched</strong>: <code>brand.ts</code> + chrome/landing/SEO/email wordmarks, the Calendar/Refill/Billing route trees (parent Outlets + index + children + redirect stubs), <code>RefillNav</code> / <code>RefillShellChrome</code> / <code>SettingsTabStrip</code>, <code>changelog.ts</code>. Deferred: smartspa.app domain, trial-email copy refresh.",
    ],
  },
  {
    version: "v2.0.5",
    date: "June 2026",
    items: [
      "<strong>v2.0.5 &mdash; Offer-authoring picker now lists only what patients can actually book.</strong> When authoring your own cross-sell offer (Recognition → Rewards → &ldquo;Your own offers&rdquo;), the service dropdown could show entries a patient can&rsquo;t book &mdash; so an offer authored against one would never surface or earn. The picker now mirrors the public booking page exactly: it lists precisely the services flagged <strong>online-bookable</strong> (the same flag <code>/s/&lt;slug&gt;</code> uses), at whatever granularity you&rsquo;ve chosen to expose. If nothing is bookable yet, it says so instead of offering dead options. <strong>New</strong>: <code>onlineBookable</code> on the <code>Service</code> type. <strong>Touched</strong>: <code>refill-catalog.ts</code> (Service type + mapper), <code>app.refill.recognition.rewards.tsx</code> (filter the picker), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.0.4",
    date: "June 2026",
    items: [
      "<strong>v2.0.4 &mdash; Your own offers now show your own words on the badge.</strong> A spa-authored cross-sell offer with a custom label (e.g. <em>&ldquo;Save $100 w/ Emma&rdquo;</em>) was showing the generic computed badge (&ldquo;$100 off through Jul 31&rdquo;) at booking instead of your label &mdash; even though the recorded win already used your label correctly. Now the at-booking badge shows the <strong>spa offer&rsquo;s own label</strong>, while manufacturer promos keep their clean computed &ldquo;$X off through &lt;date&gt;&rdquo; (their raw titles are verbose). <strong>Touched</strong>: <code>promo-calendar.ts</code> (bestActiveOfferForName), <code>changelog.ts</code>. <em>(Known follow-up: the offer-authoring service picker should list only bookable services &mdash; today it can show a category.)</em>",
    ],
  },
  {
    version: "v2.0.3",
    date: "June 2026",
    items: [
      "<strong>v2.0.3 &mdash; Fix: &ldquo;Add to calendar&rdquo; landed bookings at the wrong time.</strong> A self-book confirmation&rsquo;s Google/Apple calendar link put the appointment <strong>6 hours late</strong> (a 2:00 PM MDT booking showed as 8:00 PM). The confirmation email was always correct; only the calendar link drifted. Cause: the timestamp came back from the database as <code>2026-06-18T20:00:00+00:00</code> (offset form, no <code>Z</code>), and the calendar formatter string-stripped it into a timezone-less stamp that calendars then read as <em>floating local time</em>. Now both ends are canonicalized to true UTC (<code>…Z</code>) from the epoch, so the calendar event matches the confirmed time exactly. <strong>Touched</strong>: <code>s.$slug.tsx</code> (buildCalendarLinks), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.0.2",
    date: "June 2026",
    items: [
      "<strong>v2.0.2 &mdash; Fix: patients can actually book on the self-book page.</strong> Picking a time on <code>/s/&lt;slug&gt;</code> silently did nothing &mdash; the slot list would just refresh. Root cause: holding a slot needs an owner auth-user to stamp on the appointment, and it only looked at the <em>provider&rsquo;s</em> linked user; when a practice&rsquo;s providers were created without a linked login (common &mdash; a provider is a person, not necessarily an account), there was no user to resolve, so the hold bailed out. It now <strong>falls back to the tenant&rsquo;s owner</strong> (the founding membership), exactly like an owner-side booking. Also fixed the <em>silence</em>: a failed hold set an error banner that the immediate slot-refresh wiped before you could see it &mdash; now the reason stays on screen, and the list only refreshes when a time was genuinely just taken. <strong>New</strong>: <code>getTenantOwnerUserId</code> (<code>scheduling-settings.functions.ts</code>). <strong>Touched</strong>: <code>scheduling.functions.ts</code> (holdSlot owner fallback), <code>s.$slug.tsx</code> (keep failure reason visible), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.0.1",
    date: "June 2026",
    items: [
      "<strong>v2.0.1 &mdash; Self-book &ldquo;Pick a day&rdquo; uses the whole screen, and you can step day-to-day.</strong> On the public booking page (<code>/s/&lt;slug&gt;</code>), choosing a specific date now <strong>fans every open time across the full width of the card</strong> &mdash; no more squinting at a tall single column that you had to scroll. On a desktop the whole day&rsquo;s availability is visible at a glance. Added <strong>&larr; / &rarr; arrows flanking the date</strong> so a patient can nudge one day earlier or later without reopening the calendar picker (bounded to the bookable window). The &ldquo;Soonest available&rdquo; view keeps its one-column-per-day layout. <strong>Touched</strong>: <code>s.$slug.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v2.0.0",
    date: "June 2026",
    items: [
      "<strong>v2.0.0 &mdash; Cross-Sell is whole: your own offers, and the win on every booking path (Patient-Profitability OS).</strong> The third revenue meter is now complete on both axes. <strong>(1) Author your own offers.</strong> Cross-sell is no longer manufacturer-only &mdash; on <strong>Recognition &rarr; Rewards</strong> there&rsquo;s now <em>&ldquo;Your own offers&rdquo;</em>: pick one of your services, set a dollar discount and an optional date window, and it badges that service at booking exactly like an Allergan promo &mdash; and earns the same <code>$5 cross_sell_addon</code> win when a matched patient books it. Spa-authored offers and manufacturer promos are <strong>peers in one pipeline</strong>: same table, same at-booking badge, same ledger. (A manufacturer-calendar re-upload now only replaces <em>its</em> offers and leaves yours untouched.) <strong>(2) The win fires on patient self-book too.</strong> Until now the <code>$5</code> cross-sell win recorded only when the <em>owner</em> booked; now it also fires when a <strong>patient books themselves</strong> on <code>/s/&lt;slug&gt;</code> &mdash; where most bookings actually happen. We snapshot the chosen service onto the held appointment so the two-phase hold&rarr;confirm flow can attribute it (which also backfills what every native booking was treated for). Still matched-only: <code>$5</code> only when the patient resolves in your book, and the booking never blocks on attribution. <strong>New</strong>: <code>createSpaOffer</code> / <code>deleteSpaOffer</code> (<code>refill-promo-calendar.functions.ts</code>), migration <code>v2_00_0_spa_authored_offers</code> (<code>source</code> column + nullable manufacturer). <strong>Touched</strong>: <code>scheduling.functions.ts</code> (self-book win + service snapshot), <code>promo-calendar.ts</code> (offer id/source), <code>app.refill.recognition.rewards.tsx</code> (authoring card), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.99.0",
    date: "June 2026",
    items: [
      "<strong>v1.99.0 &mdash; Cross-Sell now earns: a $5 win when a promo-offered treatment is booked (Patient-Profitability OS, Phase 2, Slice B). </strong>The badge stops being just a display and becomes tracked revenue. When an owner books a patient into a <strong>service (or add-on) that carried an active manufacturer promo</strong>, Refill records a <code>cross_sell_addon</code> win &mdash; $5 on the same fee-rules ledger as slot-fills and recalls, verified later when the patient actually pays. It attributes only when the patient resolves in your book (matched by email, then phone), so nothing bills against a stranger; the booking never blocks on it. Same <code>recordRecoveryEvent</code> spine as Recall &mdash; the only schema change is widening <code>recovery_agent</code> to accept <code>'cross_sell'</code> (the <code>cross_sell_addon</code> fee-rule was already priced at $5). This first cut fires on the <strong>owner booking path</strong>; the patient self-book win is a fast-follow (same helper, needs the service name threaded through the two-phase hold&rarr;confirm). <strong>New</strong>: <code>recordCrossSellWin</code> + contact&rarr;patient-node resolver (<code>refill-promo-calendar.functions.ts</code>), migration <code>v1_99_0_cross_sell_agent</code>. <strong>Touched</strong>: <code>emma-attribution.functions.ts</code> (RecoveryAgent union), <code>scheduling-owner.functions.ts</code> (fire on booking), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.98.1",
    date: "June 2026",
    items: [
      "<strong>v1.98.1 &mdash; Cross-Sell offer badges now show on services too, not just add-ons. </strong>The first cut badged only add-ons &mdash; but the manufacturer offers ($75 off Juv&eacute;derm, $50 off Botox) are for <em>primary treatments</em>, which spas list as services, and a spa with no add-ons configured saw nothing. Now the offer badge surfaces on the matching <strong>service</strong> in the picker (patient self-book + owner book dialog) as well as on any matching add-on &mdash; so the discount shows on the exact treatment the patient is choosing. Also made matching <strong>synonym-aware</strong>: a Juv&eacute;derm offer badges a service named &ldquo;Dermal Filler,&rdquo; a Botox offer badges &ldquo;Neurotoxin,&rdquo; etc., so it fires whether the spa names services by brand or generically. <strong>Touched</strong>: <code>promo-calendar.ts</code> (synonym map), <code>scheduling.functions.ts</code> + <code>scheduling-owner.functions.ts</code> (badge services), <code>s.$slug.tsx</code> + <code>schedule/dialogs.tsx</code> + <code>schedule/shared.ts</code> (render), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.98.0",
    date: "June 2026",
    items: [
      "<strong>v1.98.0 &mdash; Cross-Sell: manufacturer offers now surface as add-on nudges at booking (Patient-Profitability OS, Phase 2, Slice A). </strong>The proactive revenue meter. Upload your manufacturer&rsquo;s promotions calendar (Recognition &rarr; Rewards &rarr; <strong>Promo calendar</strong>) &mdash; Refill parses the patient-facing &ldquo;Dollars Off&rdquo; offers, maps each to a product, and <strong>badges the matching add-on at the moment of booking</strong> on both the patient&rsquo;s self-book page and the owner&rsquo;s book dialog: <em>&ldquo;add filler &mdash; $75 off through Jun 30.&rdquo;</em> It shows the best currently-active offer (highest dollar amount, inside its date window), matched by product keyword in the add-on&rsquo;s name &mdash; so a $50/$65/$20 stack of Botox offers correctly surfaces the $65 while it&rsquo;s live. This is the easiest incremental dollar in the system: it fires when the patient is <em>already booking</em>, intent at its peak &mdash; not a reactivation chase. Validated against the real Allergan calendar (8 Dollars-Off offers across Juv&eacute;derm / Botox / SkinMedica / Latisse / DiamondGlow). Slice B (the $5 <code>cross_sell_addon</code> win when an offered add-on is taken) is next. <strong>New</strong>: <code>promo-calendar.ts</code> (parse + match), <code>refill-promo-calendar.functions.ts</code> (ingest/list), table <code>manufacturer_promo_offers</code> (migration). <strong>Touched</strong>: <code>scheduling.functions.ts</code> + <code>scheduling-owner.functions.ts</code> (hydrate <code>activeOffer</code>), <code>s.$slug.tsx</code> + <code>schedule/dialogs.tsx</code> (badge), <code>app.refill.recognition.rewards.tsx</code> (upload), <code>changelog.ts</code>. Two honest v1 punts: &ldquo;new to Juv&eacute;derm&rdquo; eligibility shows ungated; the embedded pause window uses start/end + amount.",
    ],
  },
  {
    version: "v1.97.0",
    date: "June 2026",
    items: [
      "<strong>v1.97.0 &mdash; Reward data can now import itself (email-drop auto-ingest). </strong>Manual CSV upload doesn&rsquo;t scale, so we built the first hands-free channel. Each spa gets a private inbox address &mdash; <code>&lt;token&gt;@rewards.getrefill.app</code> &mdash; on the <strong>Recognition &rarr; Rewards</strong> tab (&ldquo;Hands-free auto-import&rdquo;). Point any manufacturer report at it (a scheduled portal export, or a one-time Gmail auto-forward rule) and Refill <strong>auto-detects which manufacturer it is from the CSV headers</strong> and imports it &mdash; no dropdown, no upload, nobody touches anything. It rides the same inbound-email pipe as Lite Mode (Resend Inbound) and the same import core as the manual path, so results land in the import history exactly the same way. Built on the <code>detect()</code> auto-router shipped with the Allergan/Galderma mappers &mdash; the same engine that makes the upcoming local portal-MCP a thin client on this pipe. We trust the unguessable token, not the sender, so a report forwarded from any staff inbox just works. <strong>New</strong>: table <code>reward_ingest_tokens</code> (migration), <code>detectMapper()</code> / <code>detectMapperFromCsv()</code>, <code>getOrCreateRewardIngestToken</code> + <code>ingestRewardCsvByToken</code>, the <code>/api/resend/inbound-rewards</code> endpoint, and the auto-import settings card. <strong>Go-live needs</strong> a one-time MX + Resend Inbound route for <code>rewards.getrefill.app</code>. <strong>Touched</strong>: <code>manufacturer-reward-csv.ts</code>, <code>manufacturer-reward-ingest.functions.ts</code>, <code>app.refill.recognition.rewards.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.96.1",
    date: "June 2026",
    items: [
      "<strong>v1.96.1 &mdash; Recall&rsquo;s &ldquo;Expiring&rdquo; now reads the real date, not a frozen label. </strong>With all three manufacturers loaded, most signals showed as &ldquo;Eligible&rdquo; and few as &ldquo;Expiring&rdquo; &mdash; because the bucket keyed off a status stamped at import time. Recall now decides &ldquo;expiring&rdquo; from each reward&rsquo;s <strong>actual expiration date vs. today</strong> (within 60 days), so a patient whose reward genuinely closes in surfaces in <strong>Expiring</strong> no matter which manufacturer it came from or what the import label said &mdash; and the bucket can&rsquo;t drift stale between exports. Already-expired rewards drop out of the actionable triggers. Also refreshed the Rewards tab copy now that Allergan and Galderma are live (was still &ldquo;coming next&rdquo;). <strong>Touched</strong>: <code>refill-recall.functions.ts</code>, <code>app.refill.recognition.rewards.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.96.0",
    date: "June 2026",
    items: [
      "<strong>v1.96.0 &mdash; Recall now speaks Allergan and Galderma, not just Evolus (Patient-Profitability OS, ingestion Sprint 1). </strong>The reward engine had only one mapper wired (Evolus) &mdash; so Recall was running on a third of the real manufacturer data. Added three new validated importers, each checked against the spa&rsquo;s real export: <strong>Allergan (Allē) &mdash; Patient360</strong> (points value + expiration, gift cards, assigned promotions), <strong>Galderma (ASPIRE) &mdash; All patients</strong> (point balances + expiring + last-treatment), and <strong>Galderma (ASPIRE) &mdash; Savings certificates</strong> (the dollar-valued 60-day coupons). Pick the report on the <strong>Recognition &rarr; Rewards</strong> tab and upload &mdash; everything flows into the same patient-match + Recall surface you already have. On Rejuv&rsquo;s real files that&rsquo;s <strong>~390 more reward signals / ~$13.5K on the table</strong> lighting up Recall, with zero new screens. Honest calls baked in: Galderma points ship as <strong>counts</strong> (their export carries no points&rarr;dollar rate) while Galderma certificates and all Allergan values are real dollars; expiration is read from each file&rsquo;s real date column, never hardcoded; the matcher stays fuzzy for messy real-world names. <strong>New</strong>: <code>ALLERGAN_PATIENT360_MAPPER</code>, <code>GALDERMA_PATIENTS_MAPPER</code>, <code>GALDERMA_SAVINGS_MAPPER</code> in <code>manufacturer-reward-csv.ts</code>; the report picker now keys on the registry source. No schema change. <strong>Touched</strong>: <code>manufacturer-reward-csv.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.95.0",
    date: "June 2026",
    items: [
      "<strong>v1.95.0 &mdash; Recall can now text and book (Patient-Profitability OS, Phase 1, Slice B). </strong>The Recall list went from read-only to a two-click revenue engine. <strong>(1) Draft outreach:</strong> tick the patients you want to reach (any of the four triggers), hit <strong>Draft outreach</strong>, and Refill emails a batch of <strong>ready-to-send iMessage drafts</strong> to your drafts inbox &mdash; paste into Claude Desktop and each one opens in Messages as a blue bubble from <em>your own number</em>, you tap Send. The copy writes itself per trigger (&ldquo;your reward expires Jun 14 &mdash; want me to get you on the calendar?&rdquo; vs. &ldquo;it&rsquo;s been a while and we miss you&rdquo;). <strong>(2) Book:</strong> when a patient says yes, hit <strong>Book</strong> right on their row &mdash; the scheduler opens prefilled with their name and number, you pick a time, and Refill records a <strong>$5 <code>recall_booking</code></strong> win on the ledger (billed only after we confirm they actually paid). Only patients in your book can be drafted/booked. <strong>Under the hood:</strong> reused the rescue iMessage-MCP rail, the owner BookDialog, and the recovery-event engine; the one schema change is widening <code>recovery_agent</code> to accept <code>'recall'</code>, and teaching <code>recordRecoveryEvent</code> to stamp <code>metric_key</code> explicitly (it was silently defaulting every win to <code>slot_fill</code>). Billing needed <strong>zero</strong> changes &mdash; the fee-rules already price <code>recall_booking</code> at $5. <strong>New</strong>: <code>recordRecallBookingFn</code> + <code>draftRecallOutreachFn</code> (<code>refill-recall.functions.ts</code>), migration <code>v1_95_0_recall_agent</code>. <strong>Touched</strong>: <code>emma-attribution.functions.ts</code>, <code>schedule/dialogs.tsx</code> (BookDialog prefill + recall write), <code>app.refill.recognition.recall.tsx</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.94.0",
    date: "June 2026",
    items: [
      "<strong>v1.94.0 &mdash; Recall: the money-on-the-table list (Patient-Profitability OS, Phase 1, Slice A). </strong>New <strong>Recall</strong> tab under Recognition (Inventory / Manufacturers / Rewards / <strong>Recall</strong>). It unions everything Refill already knows into the four patients-worth-calling-back lists: <strong>Expiring</strong> (a reward evaporates within 60 days), <strong>Eligible &amp; idle</strong> (can redeem now but hasn&rsquo;t been in for 90+ days), <strong>Becoming-eligible</strong> (reward unlocks soon &mdash; pre-position the outreach), and <strong>Lapsed</strong> (past their treatment-cadence window, computed from your own visit history). Up top: a twin headline &mdash; <strong>&ldquo;$X on the table &middot; N patients&rdquo;</strong> &mdash; the dollars the manufacturer&rsquo;s 0-click email never moves. This is read-only (the surface); one-click &rarr; iMessage &rarr; book &rarr; <code>$5 recall_booking</code> is the next slice. Also extracted the Recognition tab strip into <strong>one shared component</strong> (it had drifted into three different inline copies). <strong>New</strong>: <code>refill-recall.functions.ts</code> (<code>listRecallTargets</code>, reusing the reward signals + <code>doListOverdue</code> + cadence math &mdash; no new tables), <code>app.refill.recognition.recall.tsx</code>, <code>RecognitionTabs.tsx</code>. <strong>Touched</strong>: the three Recognition routes, <code>patient-ingest.functions.ts</code> (exported <code>doListOverdue</code>), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.93.0",
    date: "June 2026",
    items: [
      "<strong>v1.93.0 &mdash; Billing is now &ldquo;Free + $5 per win,&rdquo; configurable (Patient-Profitability OS, P0 ship #2). </strong>Retired the old tiered plans (Starter/Pro/Predictable revenue-share). Billing is now <strong>one formula</strong>: a monthly base (<strong>$0 by default</strong>) plus a per-metric fee for each win Refill creates &mdash; a flat dollar amount <em>or</em> a percent of that win&rsquo;s revenue, each toggleable. The <strong>Billing</strong> page got a live admin panel (tune every metric + the base) and a <strong>visible ledger</strong>: &ldquo;if this month closed today&rdquo; with every win priced line-by-line. The Recovery dashboard&rsquo;s month-to-date preview now reads from the <em>same</em> math, so it can never drift from the real invoice. Default config = $0 base + <strong>$5 per filled slot</strong> (Recall / Cross-Sell / Acquire metrics are pre-wired and light up as those Solutions ship). Percent mode is kept for a future hybrid. You only ever pay on value demonstrably created. <strong>New tables</strong>: <code>billing_config</code>, <code>billing_fee_rules</code>; <code>metric_key</code> added to recovery events; <code>fee_breakdown</code> on invoices. <strong>Touched</strong>: new <code>billing-metrics.ts</code> + <code>billing-fee-core.ts</code> + <code>refill-fee-rules.functions.ts</code>, <code>refill-billing.ts</code> (cron + preview swapped to fee-rules), <code>app.billing.tsx</code> (full rebuild), <code>app.refill.recovery.tsx</code>, <code>types.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.92.1",
    date: "June 2026",
    items: [
      "<strong>v1.92.1 &mdash; Truncation sweep, round 2 (overdue + reward matching). </strong>Chasing the &ldquo;Overdue&rdquo; count: confirmed it&rsquo;s computed by a <em>separate, already-paginated</em> path (so it was correct all along &mdash; that&rsquo;s why it didn&rsquo;t move when the patient book grew), but found three more reads to harden. <strong>(1)</strong> The reward-signal importer was matching patients against only the first <strong>1,000</strong> of a 1,140-patient book &mdash; so the match rate was understated and some patients were wrongly flagged &ldquo;not in your book.&rdquo; It now matches against the <strong>whole</strong> book (<strong>re-upload your export to pick up the newly-matched patients</strong>). <strong>(2)</strong> The reward name-lookup is now chunked (a single lookup capped at 1,000 and risked a too-long-URL error as matches grew). <strong>(3)</strong> The overdue patient-hydration and transaction pagers now use a stable unique <code>ORDER BY</code> so offset paging can never skip a row. <strong>Touched</strong>: <code>patient-ingest.functions.ts</code>, <code>manufacturer-reward-ingest.functions.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.92.0",
    date: "June 2026",
    items: [
      "<strong>v1.92.0 &mdash; Truncation sweep: every count now reads your whole book. </strong>Found and killed a systemic data-integrity bug: Supabase silently caps <em>every</em> read at <strong>1,000 rows</strong> (a <code>.limit(2000)</code> doesn&rsquo;t override it), so at scale a bunch of headline numbers were computed over a 1,000-row slice and shown as complete. Most visibly, the <strong>Patients</strong> page said &ldquo;1,000 in your patient book&rdquo; when the real count is higher &mdash; and every filter on it (Overdue, A-list, per-manufacturer, tags) was counting only that slice. Fixed with one shared paginator applied to the load-bearing reads: <strong>patient list</strong> (+ all its filter counts), <strong>recovery lifetime revenue</strong> (a pay-for-performance under-bill risk), <strong>Reports funnel</strong> (targeted&rarr;sent&rarr;booked&rarr;revenue), <strong>campaign cohorts</strong> (patients past row 1,000 were silently never messaged), the <strong>invoice cron</strong>, and the new <strong>reward signals</strong> summary. <strong>Touched</strong>: new <code>server/paginate.ts</code> (<code>fetchAllRows</code>), <code>patient-ingest</code>, <code>emma-attribution</code>, <code>emma-reports</code>, <code>emma-blast</code>, <code>refill-billing</code>, <code>manufacturer-reward-ingest</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.91.1",
    date: "June 2026",
    items: [
      "<strong>v1.91.1 &mdash; Reward-signal headline math fix. </strong>The <strong>Rewards</strong> stat tiles (eligible-now / expiring / patients / dollars) were computed over a <strong>1,000-row slice</strong> of the import &mdash; PostgREST caps a read at 1,000 rows, and a single 879-patient Evolus export is 1,758 signals, so the headline undercounted. The summary now <strong>pages through the full set</strong> before counting. Also fixed: <strong>dollars-on-the-table</strong> no longer double-counts a patient whose reward shows on both their Jeuveau and Evolysse rows (summed once per patient). The entries table still streams the soonest-expiring first. <strong>Touched</strong>: <code>manufacturer-reward-ingest.functions.ts</code> (paginated summary read), <code>manufacturer-reward-csv.ts</code> (per-patient dollar dedupe), <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.91.0",
    date: "June 2026",
    items: [
      "<strong>v1.91.0 &mdash; Manufacturer reward signals (Patient-Profitability OS, Phase 0). </strong>New <strong>Recognition &rarr; Rewards</strong> tab: upload a manufacturer&rsquo;s patient-insights export and Refill matches every patient to <em>your</em> book, then shows you the money their own emails never move &mdash; who&rsquo;s <strong>eligible now</strong>, whose reward is <strong>expiring &le;60 days</strong>, who&rsquo;s lapsed, and the total dollars on the table. <strong>Evolus first</strong> (Jeuveau + Evolysse), validated against a real 879-patient export &mdash; each row becomes one signal per brand, matched by email&rarr;phone&rarr;name, with blank patient contact gaps filled in automatically. Re-upload anytime; signals collapse onto one row per patient-brand (latest snapshot wins). Allergan &amp; Galderma slot into the same engine next. This is the raw material the upcoming <strong>Recall</strong> solution turns into booked appointments. <strong>New tables</strong>: <code>reward_signal_imports</code>, <code>patient_reward_entries</code>. <strong>Touched</strong>: new <code>manufacturer-reward-csv.ts</code> (pure parser + Evolus mapper), <code>manufacturer-reward-ingest.functions.ts</code> (match + upsert + gap-fill), <code>app.refill.recognition.rewards.tsx</code>, Recognition sub-nav, <code>types.ts</code>, <code>changelog.ts</code>.",
    ],
  },
  {
    version: "v1.90.0",
    date: "June 2026",
    items: [
      "<strong>v1.90.0 &mdash; Appointments crash fix + Booking-settings polish + Recognition rename. </strong><strong>Fix:</strong> the <strong>Appointments</strong> page could crash to a &ldquo;Something went wrong&rdquo; screen when the list contained an appointment whose status wasn&rsquo;t one of the six import statuses &mdash; the native scheduler writes <code>held</code> into the same table, and the status pill had no entry for it (<code>cfg[status]</code> was undefined). The pill now renders <strong>any</strong> status safely (and labels <em>Held</em> properly) so the section can never crash on an unexpected value. <strong>Polish:</strong> Booking-settings sections now <strong>default collapsed</strong> for a clean entry view, the master <strong>Online booking</strong> card got its own chevron, and there&rsquo;s an <strong>Expand all / Collapse all</strong> control. <strong>Rename:</strong> Recognition inventory&rsquo;s &ldquo;on-hand&rdquo; bucket is now <strong>Promo/samples</strong> (clearer; same underlying data). <strong>Touched</strong>: <code>app.refill.appointments.tsx</code>, <code>app.refill.settings.booking.tsx</code>, <code>booking/useSectionCollapse.ts</code>, <code>app.refill.recognition.inventory.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.89.0",
    date: "June 2026",
    items: [
      "<strong>v1.89.0 &mdash; Collapsible sections on Booking settings. </strong>The booking-settings page got long &mdash; so every card is now a <strong>turn-down</strong>: click a section header (chevron) to collapse <strong>Your booking link</strong>, <strong>Providers</strong>, <strong>Provider hours</strong>, <strong>Date-specific hours</strong>, <strong>Rooms &amp; resources</strong>, <strong>Booking rules</strong>, or <strong>Bookable services</strong>. Open/closed is <strong>remembered per section</strong> across reloads, so if you mostly live in one section the rest stay tucked away. (The master <em>Online booking</em> on/off + timezone card stays open by design &mdash; no hiding the main switch.) <strong>Touched</strong>: new <code>useSectionCollapse.ts</code>, each <code>booking/*Section.tsx</code>, <code>app.refill.settings.booking.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.88.0",
    date: "June 2026",
    items: [
      "<strong>v1.88.0 &mdash; Add-ons in the owner calendar too. </strong>Completes the add-ons arc: when you book a patient from <strong>Schedule &rarr; Add booking</strong>, any service with add-ons now shows an <strong>Add-ons</strong> picker (with a live &ldquo;total visit&rdquo; duration + price). Picks <strong>extend the visit</strong> &mdash; the time grid finds openings for the combined duration, and the appointment saves the chosen add-ons (snapshotted, price/min frozen). Open any appointment&rsquo;s <strong>Edit</strong> dialog and you&rsquo;ll see its add-ons listed. Eligibility &amp; pricing are re-validated server-side. <strong>Touched</strong>: <code>scheduling-owner.functions.ts</code> (service add-ons in day/week/month payloads, <code>ownerCreateAppointmentFn</code> combined duration + <code>appointment_addons</code> snapshot, add-ons hydrated onto appointments), <code>schedule/shared.ts</code> (<code>ServiceLite.addOns</code>), <code>schedule/dialogs.tsx</code> (Book add-ons picker + Edit detail), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.87.0",
    date: "June 2026",
    items: [
      "<strong>v1.87.0 &mdash; Patients can choose add-ons at booking. </strong>The payoff for v1.86.0: on the patient self-book page (<code>/s/&lt;slug&gt;</code>), any service that offers add-ons now shows an <strong>Add-ons</strong> step (after Provider, before When). Each pick <strong>extends the visit</strong> &mdash; the slot engine finds times for the <em>combined</em> duration (base + add-ons), and the confirm screen + calendar event reflect the combined time and price. Chosen add-ons are <strong>snapshotted</strong> onto the appointment (price/duration frozen at booking time) so history stays accurate if the add-on later changes. Eligibility &amp; pricing are re-validated server-side &mdash; the client&rsquo;s prices are never trusted. <strong>Touched</strong>: <code>scheduling.functions.ts</code> (<code>getPublicBookingContext</code> add-ons, <code>listAvailableSlots</code>/<code>holdSlot</code> combined duration + <code>appointment_addons</code> snapshot), <code>s.$slug.tsx</code> (Add-ons accordion step), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.86.0",
    date: "June 2026",
    items: [
      "<strong>v1.86.0 &mdash; Set up service add-ons (owner setup). </strong>On a service in <strong>Catalog &rarr; Services</strong> you can now mark it <strong>&ldquo;Offer as an add-on to other services,&rdquo;</strong> and on any base service&rsquo;s editor pick which add-ons it offers (e.g. <em>Signature Facial &rarr; Dermaplane, LED</em>). Each add-on keeps its own price &amp; duration (and can be Free). This is the setup layer &mdash; patients will be able to choose add-ons at booking in the next release. <strong>Touched</strong>: <code>services.is_addon</code> + <code>service_addons</code>, <code>refill-catalog.ts</code> (<code>getServiceAddonIdsFn</code>/<code>setServiceAddonsFn</code>), <code>app.refill.catalog.services.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.85.0",
    date: "June 2026",
    items: [
      "<strong>v1.85.0 &mdash; Free services + &ldquo;fee applied toward treatment.&rdquo; </strong>On a service in <strong>Catalog &rarr; Services</strong> you can now mark it <strong>Free / no charge</strong> (shows &ldquo;Free&rdquo; on the booking page and stays bookable &mdash; the set-price guardrail no longer holds it back) or, for a paid service like a consult, <strong>Fee applied toward treatment</strong> (renders a clean &ldquo;applied toward your treatment&rdquo; line for patients). This closes the &ldquo;$0 is ambiguous&rdquo; gap: <em>Free</em> is intentional; <em>$0 and not free</em> still means unpriced. <strong>Touched</strong>: <code>services.is_free</code> + <code>services.fee_credit</code>, <code>refill-catalog.ts</code>, <code>app.refill.catalog.services.tsx</code>, <code>scheduling.functions.ts</code>, <code>scheduling-settings.functions.ts</code>, <code>BookableServicesSection.tsx</code>, <code>s.$slug.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.84.0",
    date: "June 2026",
    items: [
      "<strong>v1.84.0 &mdash; Show-prices toggle + a set-price guardrail. </strong>New in <strong>Booking settings &rarr; Booking rules</strong>: <strong>Show prices on your booking page</strong>. Off &rarr; patients never see prices and the price-based &ldquo;Best value&rdquo; option disappears. On &rarr; a bookable service still at <strong>$0</strong> is treated as <em>unpriced</em>: it&rsquo;s held back from your public page (so nothing goes live mispriced) and shows an amber <strong>Set price</strong> flag in Bookable services until you price it. Library-added services (which come in at $0) won&rsquo;t surprise-publish. <strong>Touched</strong>: <code>scheduling_settings.show_prices</code>, <code>scheduling-settings.functions.ts</code>, <code>scheduling.functions.ts</code>, <code>s.$slug.tsx</code>, <code>BookableServicesSection.tsx</code>, <code>BookingRulesSection.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.83.1",
    date: "June 2026",
    items: [
      "<strong>v1.83.1 &mdash; Guided first-run for new spas. </strong>A brand-new catalog now opens with a welcoming <strong>&ldquo;Let&rsquo;s build your booking menu&rdquo;</strong> screen that leads straight into the service library (with &ldquo;add one manually&rdquo; as the alternate). Once you&rsquo;ve added services, a dismissible <strong>Getting started</strong> checklist guides the rest &mdash; set prices (flags how many are still $0), set provider hours, and turn on online booking, each linking right to where you do it. <strong>Touched</strong>: <code>src/routes/app.refill.catalog.services.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.83.0",
    date: "June 2026",
    items: [
      "<strong>v1.83.0 &mdash; Add from the service library. </strong>New on <strong>Catalog &rarr; Services</strong>: an <strong>Add from library</strong> button opens a pre-loaded catalog of common med-spa services (Tox, Filler, IPL/BBL, Laser, RF Microneedling, Facials, Peels, Body, Wellness, Consults&hellip;). Browse by category, search, multi-select (or &ldquo;Select all&rdquo; a category), and add them to your catalog in one click &mdash; already-added services show as <em>Added</em> and can&rsquo;t duplicate. Added services come in bookable at $0 with a sensible default duration, ready to <strong>price and drag into your order</strong>. Onboarding becomes curation, not data entry. <strong>Touched</strong>: new <code>service_library</code> table + seed, <code>listServiceLibraryFn</code> / <code>addServicesFromLibraryFn</code>, <code>ServiceLibraryPicker.tsx</code>, <code>app.refill.catalog.services.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.82.1",
    date: "June 2026",
    items: [
      "<strong>v1.82.1 &mdash; Catalog now matches: category order + category drag. </strong>The <strong>Catalog &rarr; Services</strong> page now shows categories in your chosen order and lets you <strong>drag category headers to reorder</strong> them right there &mdash; same as Booking settings. Service order and category order are now consistent across Booking settings, Catalog, and the patient page. (The internal Add-booking picker still lists categories in the default order by design.) <strong>Touched</strong>: <code>getCategoryOrderFn</code>, <code>src/routes/app.refill.catalog.services.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.82.0",
    date: "June 2026",
    items: [
      "<strong>v1.82.0 &mdash; Reorder your categories too. </strong>In <strong>Booking settings &rarr; Bookable services</strong>, each category header now has a <strong>drag handle</strong> &mdash; drag a category up or down to set the order your categories appear in (e.g. lead with <em>Tox</em> and <em>Filler</em>, push <em>Other</em> to the bottom). The order shows on your <strong>patient booking page</strong>. Combined with service reorder, you now control the <em>entire</em> menu layout &mdash; something Acuity simply can&rsquo;t do. <strong>Touched</strong>: <code>reorderCategoriesFn</code> + <code>scheduling_settings.category_order</code>, <code>orderedCategoryRank</code> helper, <code>scheduling-settings.functions.ts</code>, <code>scheduling.functions.ts</code>, <code>useBookingSettings.ts</code>, <code>BookableServicesSection.tsx</code>, <code>s.$slug.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.81.1",
    date: "June 2026",
    items: [
      "<strong>v1.81.1 &mdash; Drag-to-reorder services on the Catalog page too. </strong>You can now reorder services within a category by dragging right on <strong>Catalog &rarr; Services</strong> (it already honored the order; now you can set it there as well as in Booking settings). Same instant save, same order everywhere. <strong>Touched</strong>: <code>src/routes/app.refill.catalog.services.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.81.0",
    date: "June 2026",
    items: [
      "<strong>v1.81.0 &mdash; Drag to reorder services (the order is finally yours). </strong>In <strong>Booking settings &rarr; Bookable services</strong>, grab a service by its handle and <strong>drag it up or down within its category</strong> to set the exact order &mdash; no more forced alphabetical. Your order is honored everywhere it matters: the patient booking page, the owner Add-booking picker, and the Catalog. (Drag onto a category header still moves a service to that category.) Category reordering + drag-reorder on the Catalog page are next. <strong>Touched</strong>: new <code>services.sort_order</code> + <code>scheduling_settings.category_order</code> columns, <code>reorderServicesFn</code>, and order-aware sorting across <code>scheduling-settings.functions.ts</code>, <code>scheduling.functions.ts</code>, <code>scheduling-owner.functions.ts</code>, <code>refill-catalog.ts</code>, <code>useBookingSettings.ts</code>, <code>BookableServicesSection.tsx</code>, <code>s.$slug.tsx</code>, <code>dialogs.tsx</code>, <code>app.refill.catalog.services.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.80.1",
    date: "June 2026",
    items: [
      "<strong>v1.80.1 &mdash; Add to calendar on the booking confirmation. </strong>After a patient books on your public page, the &ldquo;You&rsquo;re booked!&rdquo; screen now offers <strong>Add to calendar</strong> &mdash; a <strong>Google</strong> link and an <strong>Apple / Outlook</strong> (.ics) download, pre-filled with the service, time, duration, provider, and your spa name. Fewer forgotten appointments. <strong>Touched</strong>: <code>src/routes/s.$slug.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.80.0",
    date: "June 2026",
    items: [
      "<strong>v1.80.0 &mdash; Patient booking flow polish. </strong>(1) The <strong>Anyone</strong> provider option now reads &ldquo;Any available provider&rdquo; and shows the right price (no more &ldquo;from&rdquo; when every provider charges the same). (2) The collapsed <strong>When</strong> step now shows the chosen date (e.g. <em>Pick a day · Tue, Jun 24</em>), not just the mode. (3) The contact step validates the <strong>email format</strong> with an inline hint and only enables <strong>Confirm</strong> for a valid address. <strong>Touched</strong>: <code>src/routes/s.$slug.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.79.2",
    date: "June 2026",
    items: [
      "<strong>v1.79.2 &mdash; Warning when a bookable service has no provider. </strong>A service can be marked <strong>Bookable</strong> but still have <em>no provider assigned to perform it</em> &mdash; in which case it silently won&rsquo;t appear on your patient booking page (you can&rsquo;t book what no one does). Now <strong>Booking settings &rarr; Bookable services</strong> shows an amber <strong>No provider</strong> flag on any such service, so you can spot it and assign someone (expand the row, or check it for a provider under Providers). <strong>Touched</strong>: <code>src/components/refill/booking/BookableServicesSection.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.79.1",
    date: "June 2026",
    items: [
      "<strong>v1.79.1 &mdash; Booking service rows: one line, less scrolling. </strong>On the patient booking page, each service now shows its <strong>name on the left and duration · price on the right of the same line</strong> instead of stacking three lines &mdash; using the desktop width and cutting the scrolling way down. Notes that simply repeat the service name are hidden; genuinely useful notes still show beneath. <strong>Touched</strong>: <code>src/routes/s.$slug.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.79.0",
    date: "June 2026",
    items: [
      "<strong>v1.79.0 &mdash; Patient booking page uses desktop width. </strong>Your public self-book page (<code>/s/&lt;your-link&gt;</code>) was a phone-width card even on a big screen, so the <strong>Soonest available</strong> week got chopped at ~2 days. It now widens on desktop to show a fuller week of openings, while staying single-column on phones. The contact and confirmation steps stay a tidy centered width on purpose. <strong>Touched</strong>: <code>src/routes/s.$slug.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.78.3",
    date: "June 2026",
    items: [
      "<strong>v1.78.3 &mdash; Patients: last-visit date no longer wraps. </strong>The <strong>Last visit</strong> date in the Patients table now stays on a single line (e.g. <em>May 14, 2026</em>) instead of breaking across three lines when the column is tight. <strong>Touched</strong>: <code>src/routes/app.refill.patients.index.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.78.2",
    date: "June 2026",
    items: [
      "<strong>v1.78.2 &mdash; Two-tier desktop widths. </strong>Card and form pages keep the comfortable centered <code>960px</code> column, while the genuinely data-dense screens &mdash; <strong>Schedule</strong>, <strong>Patients</strong> (list + contacts), <strong>Appointments</strong>, <strong>Reports</strong>, and <strong>Waitlist bulk</strong> &mdash; widen to <code>1280px</code> so calendars and tables breathe. Page headers on those screens match the wider column. <strong>Touched</strong>: <code>src/components/PageHeader.tsx</code> (new <code>wide</code> prop), 6 data-page containers + their headers, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.78.1",
    date: "June 2026",
    items: [
      "<strong>v1.78.1 &mdash; Dialed the desktop width to a comfortable 960px, centered. </strong>v1.78.0&rsquo;s 1600px felt too wide for card/form content; pages now use a centered <code>960px</code> column &mdash; roomier than the old layout but easy on the eye. <strong>Touched</strong>: <code>src/components/PageHeader.tsx</code> + ~30 <code>src/routes/app.refill.*</code> containers, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.78.0",
    date: "June 2026",
    items: [
      "<strong>v1.78.0 &mdash; Full-width on desktop. </strong>The app now uses far more of your screen on a desktop monitor. Every data-rich screen &mdash; <strong>Schedule</strong>, <strong>Patients</strong>, <strong>Appointments</strong>, <strong>Catalog</strong>, <strong>Reports</strong>, <strong>Recovery/Rescue</strong>, <strong>Campaigns</strong>, <strong>Inbox</strong>, <strong>Recognition</strong>, <strong>Booking settings</strong>, and the page headers &mdash; widened from a narrow centered column to a roomy <code>1600px</code> layout, so tables, calendars, and lists breathe. Single-column forms and setup wizards stay a comfortable reading width on purpose. <strong>Touched</strong>: <code>src/components/PageHeader.tsx</code> + ~30 <code>src/routes/app.refill.*</code> page containers, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.77.2",
    date: "June 2026",
    items: [
      "<strong>v1.77.2 &mdash; Wider Add-booking dialog on desktop. </strong>The <strong>Add booking</strong> dialog now uses much more screen width on desktop, so the Smart Schedule day-columns lay out inside the dialog and scroll horizontally there &mdash; instead of spilling past the edge of the box. (On mobile it stays a single column as before.) <strong>Touched</strong>: <code>src/components/refill/schedule/dialogs.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.77.1",
    date: "June 2026",
    items: [
      "<strong>v1.77.1 &mdash; Fix: Add booking crashed when picking a service with openings. </strong>In <strong>Schedule &rarr; Add booking</strong>, selecting a service that had available times threw an &ldquo;Invalid time value&rdquo; error and blanked the page. The day-heading formatter was being handed a full timestamp instead of a date — fixed. <strong>Touched</strong>: <code>src/components/refill/schedule/dialogs.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.77.0",
    date: "June 2026",
    items: [
      "<strong>v1.77.0 &mdash; Date-specific provider availability. </strong>Under <strong>Provider hours</strong> in Booking settings there&rsquo;s now <strong>Date-specific hours</strong>: override a provider&rsquo;s weekly schedule for a single date &mdash; <strong>closed all day</strong> (a holiday) or <strong>different open/close</strong> just that day (e.g. &ldquo;Michelle 10–2 on Jul 3&rdquo;). The booking engine honors these everywhere openings are computed: the patient self-book page, owner Smart Schedule, and the calendar. Recurring weekly hours stay the default; a partial-day time-off still uses <strong>Block</strong> on the Schedule. <strong>Touched</strong>: <code>supabase/migrations/20260707000000_v1_77_0_provider_date_overrides.sql</code> (new <code>scheduling_date_overrides</code> table), <code>src/lib/scheduling-slots.ts</code> (engine honors overrides), <code>src/server/scheduling.functions.ts</code>, <code>src/server/scheduling-settings.functions.ts</code> (bundle + upsert/delete fns), <code>src/components/refill/booking/{useBookingSettings,DateOverridesSection}.tsx</code>, <code>src/routes/app.refill.settings.booking.tsx</code>, <code>src/integrations/supabase/types.ts</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.76.1",
    date: "June 2026",
    items: [
      "<strong>v1.76.1 &mdash; Find each provider&rsquo;s schedule from the provider. </strong>Setting a provider&rsquo;s weekly availability was always there, but buried under &ldquo;Business hours&rdquo; below other sections. Now in <strong>Booking settings</strong>: the hours editor sits <strong>right under Providers</strong> and is titled <strong>Provider hours</strong> when you have a team, and each active provider has a <strong>Hours</strong> button that jumps straight to their week. (Each provider keeps their own Mon–Sun open/close; one-off days off still go through <strong>Block</strong> on the Schedule.) <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code> (reorder), <code>src/components/refill/booking/BusinessHoursSection.tsx</code> (title + anchor), <code>src/components/refill/booking/ProvidersSection.tsx</code> (per-provider Hours jump), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.76.0",
    date: "June 2026",
    items: [
      "<strong>v1.76.0 &mdash; Smart Schedule in the owner Add-booking dialog. </strong>When you add a booking from the <strong>Schedule</strong> tab, the time step now offers the same Smart Schedule as patients: <strong>Soonest</strong> (the provider&rsquo;s real openings), <strong>Pick a day</strong> (a date, then that day&rsquo;s openings), and <strong>Custom time</strong> (manual day + time for walk-ins / off-hours overrides). Tapping a real opening fills the time for you, so you can&rsquo;t accidentally pick a slot the provider isn&rsquo;t free. <strong>Touched</strong>: <code>src/server/scheduling-owner.functions.ts</code> (return tenantId), <code>src/components/refill/schedule/dialogs.tsx</code> (Smart Schedule picker + Custom escape), <code>src/routes/app.refill.schedule.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.75.0",
    date: "June 2026",
    items: [
      "<strong>v1.75.0 &mdash; Patient booking is now a clean 3-step accordion with Smart Schedule. </strong>Replaced the multi-page flow on <code>/s/&lt;your-link&gt;</code> with one tidy page: <strong>1) Choose a service</strong>, <strong>2) Choose a provider</strong> (Anyone / Best value / a specific person &mdash; only those who perform it), then <strong>3) When</strong> &mdash; our <strong>Smart Schedule</strong> step with two ways to find a time: <strong>Soonest available</strong> (earliest openings across all days) or <strong>Pick a day</strong> (choose a date). Each step collapses to a summary as you go, so patients always see where they are. Real availability under the hood as before. <strong>Touched</strong>: <code>src/routes/s.$slug.tsx</code> (accordion + Smart Schedule), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.74.1",
    date: "June 2026",
    items: [
      "<strong>v1.74.1 &mdash; Patient time picker: Acuity-style day columns on desktop. </strong>The &ldquo;pick a time&rdquo; step now lays the available days out as <strong>side-by-side columns</strong> on desktop (each day a column of open times, only days with openings shown) while staying a clean vertical stack on mobile. Same live availability under the hood &mdash; real openings from the chosen provider&rsquo;s hours. <strong>Touched</strong>: <code>src/routes/s.$slug.tsx</code> (responsive day-column layout), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.74.0",
    date: "June 2026",
    items: [
      "<strong>v1.74.0 &mdash; Patient booking page: service notes + category groups. </strong>Your public self-book page (<code>/s/&lt;your-link&gt;</code>) now shows each service&rsquo;s <strong>notes</strong> right under its name (e.g. &ldquo;Botox, Jeuveau, Xeomin, or Dysport&rdquo; or &ldquo;$100 fee waived if treating same day&rdquo;) and organizes the menu into <strong>collapsible category groups</strong> &mdash; a patient taps <em>Tox</em>, <em>Filler</em>, <em>Laser</em>&hellip; to see those options. A single-category menu stays flat. This surfaces the rich context that sets us apart. <em>Note: the per-service Notes field is now patient-facing.</em> <strong>Touched</strong>: <code>src/server/scheduling.functions.ts</code> (return <code>category</code> + <code>notes</code> on each public service), <code>src/routes/s.$slug.tsx</code> (grouped, notes-rich service picker), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.73.0",
    date: "June 2026",
    items: [
      "<strong>v1.73.0 &mdash; Add booking is now service-first. </strong>In <strong>Schedule &rarr; Add booking</strong>, you now pick the <strong>service first</strong>, then the <strong>provider</strong> &mdash; and the provider list shows only the people who actually perform that service. If just one provider does it, they&rsquo;re chosen automatically (&ldquo;with Michelle&rdquo;); if none do, you&rsquo;re told to assign one in Booking settings. Matches how you think about a booking: the treatment, then who does it. <strong>Touched</strong>: <code>src/components/refill/schedule/dialogs.tsx</code> (reordered fields + provider eligibility), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.72.1",
    date: "June 2026",
    items: [
      "<strong>v1.72.1 &mdash; Booking picker now shows only what the chosen provider performs. </strong>In <strong>Schedule &rarr; Add booking</strong>, the Service list is filtered to the selected provider&rsquo;s services &mdash; so picking <em>Michelle</em> (who does Laser &amp; Facial) no longer shows Tox or Filler. Switch the provider and the list updates; if a service you&rsquo;d picked isn&rsquo;t offered by the new provider, it clears so you can&rsquo;t book a mismatch. Eligibility uses the same per-provider assignment from <strong>Booking settings</strong> (opt-out: a provider performs everything unless unchecked). <strong>Touched</strong>: <code>src/server/scheduling-owner.functions.ts</code> (return per-provider unoffered set), <code>src/components/refill/schedule/dialogs.tsx</code> (filter picker + clear stale pick), <code>src/routes/app.refill.schedule.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.72.0",
    date: "June 2026",
    items: [
      "<strong>v1.72.0 &mdash; Booking service picker is now grouped by category (collapsed by default). </strong>In <strong>Schedule &rarr; Add booking</strong>, the Service field used to be one long alphabetical dropdown of every service. It&rsquo;s now organized into <strong>collapsible category groups</strong> &mdash; all collapsed by default &mdash; so you tap a category (e.g. <em>BBL</em>, <em>RF Microneedling</em>) to reveal its services, then pick one. The chosen service shows at the top, and reopening jumps you back to its category. Far easier to scan a big catalog. <strong>Touched</strong>: <code>src/components/refill/schedule/dialogs.tsx</code> (new collapsible ServicePicker), <code>src/components/refill/schedule/shared.ts</code> + <code>src/server/scheduling-owner.functions.ts</code> (carry <code>category</code> on each bookable service), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.71.0",
    date: "June 2026",
    items: [
      "<strong>v1.71.0 &mdash; Make a whole category bookable at once. </strong>On <strong>Booking &rarr; Bookable services</strong>, every category header now has an <strong>All bookable</strong> checkbox that flips every service in that family on or off in one click &mdash; handy when a spa offers, say, all of its <em>RF Microneedling</em> areas (Face, Neck, Chest&hellip;) or none of them. The box shows a dash when a category is partly on. Individual service toggles still work as before; click <strong>Save changes</strong> to keep it. <em>Tip: to switch a category back on after turning it all off, use <strong>Show inactive</strong> to bring those services back into view.</em> <strong>Touched</strong>: <code>src/components/refill/booking/useBookingSettings.ts</code> (<code>setCategoryBookable</code> + per-category visible map), <code>src/components/refill/booking/BookableServicesSection.tsx</code> (category-header checkbox), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.70.0",
    date: "June 2026",
    items: [
      "<strong>v1.70.0 &mdash; Category management comes to the Catalog. </strong>The <strong>Catalog &rarr; Services</strong> page now matches Booking: a <strong>+ Add category</strong> button creates a category on its own (it appears as a highlighted drop zone), every service row has a <strong>drag handle</strong> so you can <strong>drag it into any category</strong> (saved instantly &mdash; no extra step), each category header <strong>collapses</strong> with a chevron, and a <strong>Collapse all / Expand all</strong> control folds the whole list at once. Great for re-organizing a big catalog. <strong>Touched</strong>: <code>src/routes/app.refill.catalog.services.tsx</code> (add-category, drag-to-recategorize, collapse), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.69.1",
    date: "June 2026",
    items: [
      "<strong>v1.69.1 &mdash; Expand / Collapse all categories. </strong>On <strong>Booking &rarr; Bookable services</strong>, a new <strong>Collapse all</strong> / <strong>Expand all</strong> button (next to the search box) folds or unfolds every category group at once &mdash; handy for scanning a long catalog or jumping straight to the category you want. Individual category headers still toggle on their own. <strong>Touched</strong>: <code>src/components/refill/booking/useBookingSettings.ts</code> (<code>toggleAllCats</code> + all-collapsed state), <code>src/components/refill/booking/BookableServicesSection.tsx</code> (toggle button), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.69.0",
    date: "June 2026",
    items: [
      "<strong>v1.69.0 &mdash; Create categories on their own, then drag services in. </strong>You no longer have to invent a service just to make a category. On <strong>Booking &rarr; Bookable services</strong> there&rsquo;s now a <strong>+ Add category</strong> button next to <strong>+ Add service</strong>: name it (e.g. <em>Body Contouring</em>) and it appears as an empty, highlighted <strong>drop zone</strong> at the top of the list. <strong>Drag any existing service onto it</strong> to file it there, then <strong>Save changes</strong> to keep it &mdash; perfect for re-organizing services you already have. The new category also shows up in every service&rsquo;s category picker, so you can assign it from the dropdown too. (Empty categories are a working draft until a service lands in one &mdash; an unused one simply goes away.) <strong>Touched</strong>: <code>src/components/refill/booking/useBookingSettings.ts</code> (pending-category state + <code>onAddCategory</code>), <code>src/components/refill/booking/BookableServicesSection.tsx</code> (Add-category button, form, and droppable empty headers), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.68.3",
    date: "June 2026",
    items: [
      "<strong>v1.68.3 &mdash; Add-a-service moved to the top + clearer &ldquo;create category.&rdquo; </strong>On <strong>Booking &rarr; Bookable services</strong>, the <strong>Add service</strong> form now sits at the <strong>top</strong> of the card instead of the bottom &mdash; so you don&rsquo;t scroll past the whole list to add one, and its category dropdown opens in view. The category picker also now shows a persistent <strong>Type a name to create a new category</strong> hint, so it&rsquo;s obvious you can make your own (e.g. <em>Body Contouring</em>) and not just pick a built-in. <strong>Touched</strong>: <code>src/components/refill/booking/BookableServicesSection.tsx</code> (add-form moved to top), <code>src/components/refill/CategoryCombobox.tsx</code> (create-category hint), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.68.2",
    date: "June 2026",
    items: [
      "<strong>v1.68.2 &mdash; Category picker fixed + new services now stick. </strong>Two fixes to last week&rsquo;s custom-categories release: (1) The <strong>category picker</strong> now shows <strong>all</strong> your categories &mdash; the built-ins plus any you&rsquo;ve made &mdash; the moment you click it, with a <strong>Create &ldquo;&hellip;&rdquo;</strong> row when you type something new. It was previously collapsing to just one option (a quirk of the old browser-native dropdown). Works the same in <strong>Catalog &rarr; Services</strong> and <strong>Booking &rarr; Bookable services</strong>. (2) <strong>Add service</strong> on the Bookable-services card now makes the new service bookable right away, so it appears in the list instead of silently landing in the hidden <em>inactive</em> bucket. <strong>Touched</strong>: <code>src/components/refill/CategoryCombobox.tsx</code> (rewritten as a real combobox popover), <code>src/server/scheduling-settings.functions.ts</code> (new services created bookable), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.68.1",
    date: "June 2026",
    items: [
      "<strong>v1.68.1 &mdash; Internal cleanup, part 2 (no visible changes). </strong>Finished the <strong>Online booking</strong> settings refactor: all of its state and save-logic now live in a single <code>useBookingSettings</code> hook, and the big Providers, Rooms, and Bookable-services sections each became their own component file. The screen file dropped from ~2,100 lines to ~250. Purely under-the-hood &mdash; behavior is identical. <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code>, new <code>src/components/refill/booking/{useBookingSettings,ProvidersSection,RoomsSection,BookableServicesSection}.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.68.0",
    date: "June 2026",
    items: [
      "<strong>v1.68.0 &mdash; Internal cleanup (no visible changes). </strong>Consolidation pass that splits the two largest screens into focused, reusable pieces so they&rsquo;re faster and safer to build on: the <strong>Online booking</strong> settings page (business-hours and booking-rules sections + shared field controls pulled into their own files; the repeated save-on-the-spot logic collapsed into one helper) and the <strong>Schedule</strong> calendar (Day/Week/Month grids, the appointment dialogs, and the date helpers split out of a single 1,300-line file into a lean orchestrator). Behavior is identical &mdash; this is purely under-the-hood. <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code>, <code>src/routes/app.refill.schedule.tsx</code>, new <code>src/components/refill/booking/*</code> + <code>src/components/refill/schedule/*</code> + <code>src/lib/use-drag-autoscroll.ts</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.67.0",
    date: "June 2026",
    items: [
      "<strong>v1.67.0 &mdash; Make your own service categories. </strong>Categories are no longer locked to the six built-ins (Tox, Filler, Laser, Facial, Skincare, Other). In both <strong>Catalog &rarr; Services</strong> and <strong>Booking &rarr; Bookable services</strong>, the category field is now a <strong>type-to-create combobox</strong>: pick a suggestion or just type a new one (e.g. <em>Body Contouring</em>, <em>Wellness / IV</em>, <em>Memberships</em>) and it&rsquo;s created on the spot. <strong>Rename a category</strong> from the little pencil on any category header in Booking &mdash; it updates every service in that category at once, and the change shows up in the Catalog too (both read the same source). Typing one of the built-in names folds onto the canonical one, so you won&rsquo;t get a stray &ldquo;tox&rdquo; next to &ldquo;Tox.&rdquo; <strong>Touched</strong>: <code>supabase/migrations/20260708000000_v1_67_0_custom_service_categories.sql</code> (relax the <code>services.category</code> CHECK to any non-empty text), <code>src/lib/service-categories.ts</code> (new shared source), <code>src/components/refill/CategoryCombobox.tsx</code> (new), <code>src/server/refill-catalog.ts</code>, <code>src/server/scheduling-settings.functions.ts</code> (<code>renameServiceCategoryFn</code> + free-text validators), <code>src/routes/app.refill.catalog.services.tsx</code>, <code>src/routes/app.refill.settings.booking.tsx</code>, <code>src/routes/app.refill.catalog.import.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.66.1",
    date: "June 2026",
    items: [
      "<strong>v1.66.1 &mdash; Dragging a service now auto-scrolls the page. </strong>When you drag a service toward the top or bottom edge, the page scrolls so you can reach a category that&rsquo;s far up or down the list (e.g. moving <em>smooth</em> or <em>Skinvive</em> up to <em>filler</em>). <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.66.0",
    date: "June 2026",
    items: [
      "<strong>v1.66.0 &mdash; Bookable services, grouped by category + drag to move. </strong>The services list is now organized into <strong>collapsible category groups</strong> (with a count on each header), so a long catalog is easy to scan. Each row has a <strong>drag handle</strong> &mdash; grab a service and drop it on another category&rsquo;s header to <strong>move it there</strong> (then <strong>Save changes</strong> to keep it). Search and Show-inactive still work across all groups. <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code> (category grouping, collapse, drag-to-recategorize), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.65.1",
    date: "June 2026",
    items: [
      "<strong>v1.65.1 &mdash; Buffer is a simple 5-minute picker. </strong>Cleanup <strong>Buffer</strong> on each service is now a single minutes dropdown (0–60 in 5-minute steps) instead of an hours+minutes pair — buffers are never hours. The duration H:M / minutes toggle is unchanged. <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.65.0",
    date: "June 2026",
    items: [
      "<strong>v1.65.0 &mdash; Add, edit, and delete services right here. </strong>No more bouncing to the catalog: the <strong>Bookable services</strong> list now does full CRUD. <strong>+ Add service</strong> creates one (name, category, price) on the spot; expand any service to <strong>edit its name, category, and price</strong> (alongside duration, buffer, room, and per-provider settings); and a <strong>delete</strong> button removes one cleanly (handy for retail products that slipped into your services). Name/category/price edits save with the <strong>Save changes</strong> button; add and delete apply immediately. <strong>Touched</strong>: <code>src/server/scheduling-settings.functions.ts</code> (<code>createBookableServiceFn</code> / <code>deleteBookableServiceFn</code> + name/category/price in the save), <code>src/routes/app.refill.settings.booking.tsx</code> (add form, inline details editor, delete), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.64.1",
    date: "June 2026",
    items: [
      "<strong>v1.64.1 &mdash; Fix: provider service picker no longer lists non-bookable items. </strong>The per-provider picker was showing your <em>whole</em> catalog by category (including retail-ish entries that aren&rsquo;t scheduled). It now defaults to your <strong>bookable</strong> services; <strong>search</strong> still reaches the full catalog to pull in (and make bookable) anything else. <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.64.0",
    date: "June 2026",
    items: [
      "<strong>v1.64.0 &mdash; Provider services, grouped by category. </strong>The per-provider service picker now organizes your catalog into <strong>category turn-downs</strong>, each with a <strong>select-all checkbox</strong> (tri-state &mdash; full / partial / none) plus the individual service checkboxes. Check a whole category to assign it to that provider in one move, or fine-tune service by service; the count (e.g. <code>6/9</code>) shows at a glance. Search still filters across everything and auto-opens matching categories. <strong>Touched</strong>: <code>src/server/scheduling-settings.functions.ts</code> (<code>category</code> in the bundle + <code>assignProviderServicesBulkFn</code>), <code>src/routes/app.refill.settings.booking.tsx</code> (category accordions + tri-state select-all), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.63.0",
    date: "June 2026",
    items: [
      "<strong>v1.63.0 &mdash; Drag an appointment to move it. </strong>On the calendar, <strong>grab a booking and drop it</strong> on a new time to reschedule &mdash; and in the multi-provider <strong>Day</strong> view, drop it in another provider&rsquo;s column to reassign at the same time. Works in Week too (drop on any day). Drops snap to 5 minutes; if the new spot overlaps another appointment it&rsquo;s refused with a clear message. Pair it with double-click-to-edit for the full toolkit. <strong>Touched</strong>: <code>src/routes/app.refill.schedule.tsx</code> (draggable cards + drop targets on day/week columns → <code>ownerUpdateAppointmentFn</code>), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.62.0",
    date: "June 2026",
    items: [
      "<strong>v1.62.0 &mdash; Double-click an appointment to view &amp; edit it. </strong>On the calendar, <strong>double-click any booking</strong> to open it &mdash; change the <strong>day, time, provider, duration, or patient name</strong>, or cancel it, all in one place. Moving it to a time that overlaps another appointment is blocked with a clear message (the database keeps it honest). Until now you could only add or cancel; now you can reschedule and reassign too. (Drag-to-move on the grid is coming next.) <strong>Touched</strong>: <code>src/server/scheduling-owner.functions.ts</code> (<code>ownerUpdateAppointmentFn</code>, EXCLUDE-guarded), <code>src/routes/app.refill.schedule.tsx</code> (double-click → Edit dialog), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.61.0",
    date: "June 2026",
    items: [
      "<strong>v1.61.0 &mdash; A tidy Bookable services list. </strong>The services list no longer dumps your whole catalog on you. It now shows just your <strong>active</strong> (bookable) services, with a <strong>search box</strong> to find any service and a <strong>Show inactive (N)</strong> expander for the rest. Turning a service&rsquo;s <strong>Bookable</strong> toggle off tucks it into inactive; on brings it back &mdash; so the list stays focused on what you actually book. <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code> (search + active/inactive grouping), <code>src/lib/changelog.ts</code>. No catalog changes &mdash; the Bookable toggle is the active line.",
    ],
  },
  {
    version: "v1.60.0",
    date: "June 2026",
    items: [
      "<strong>v1.60.0 &mdash; Set up services from the provider side. </strong>Each provider in the <strong>Providers</strong> list now has a turn-down with a <strong>search box</strong> &mdash; find a service and check it to say this provider performs it. If the service wasn&rsquo;t bookable yet, checking it <strong>makes it bookable and assigns it to that provider</strong> (the others start opted-out, so you build each provider&rsquo;s menu cleanly). It&rsquo;s the same data as the per-service view you already have &mdash; edit from whichever side you like; they stay in sync. Empty search shows that provider&rsquo;s current services; typing searches your whole catalog. <strong>Touched</strong>: <code>src/server/scheduling-settings.functions.ts</code> (<code>assignProviderServiceFn</code>), <code>src/routes/app.refill.settings.booking.tsx</code> (provider service-assignment panel), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.59.0",
    date: "June 2026",
    items: [
      "<strong>v1.59.0 &mdash; Hours &amp; minutes for service times, in 5-minute steps. </strong>On the <strong>Online booking</strong> page, service <strong>Duration</strong> and <strong>Buffer</strong> now edit as <strong>hours + minutes</strong> (minutes in clean 5-minute increments) instead of one raw minutes box. Prefer plain minutes? A toggle at the top of <strong>Bookable services</strong> flips the whole list between <strong>1h 30m</strong> and <strong>90 min</strong> &mdash; your choice is remembered. <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code> (new <code>DurationField</code> + format toggle), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.58.0",
    date: "June 2026",
    items: [
      "<strong>v1.58.0 &mdash; Services can require a room &mdash; and we stop double-booking it. </strong>In <strong>Online booking</strong>, expand a service and set <strong>Requires: Any room / chair / device</strong>. Now a time is only offered to patients when both the provider <em>and</em> a resource of that type are free, and every booking (patient or owner) automatically grabs an open one &mdash; so two laser appointments can&rsquo;t land in your one laser bay, even across different providers. Rooms of the same type pool together (3 rooms = 3 concurrent), and the database enforces no double-booking at the lowest level. Services that don&rsquo;t need a room are unchanged. <strong>Touched</strong>: new column <code>services.required_resource_type</code>; new <code>src/server/scheduling-resources.ts</code> (capacity check + free-resource assignment); <code>scheduling.functions.ts</code> (slot filter + hold assigns a resource), <code>scheduling-owner.functions.ts</code> (manual booking assigns a resource), <code>scheduling-settings.functions.ts</code> + <code>app.refill.settings.booking.tsx</code> (the Requires control), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.57.0",
    date: "June 2026",
    items: [
      "<strong>v1.57.0 &mdash; Rooms &amp; resources. </strong>New section on the <strong>Online booking</strong> page to manage the <strong>rooms, chairs, or devices</strong> your appointments occupy &mdash; add them, rename inline, set each one&rsquo;s type, and activate/deactivate (never deleted, so history stays intact). It&rsquo;s optional: a spa can have none. This is the setup half &mdash; next we&rsquo;ll let a service <strong>require</strong> a room so two appointments can&rsquo;t need the same one at once (the database already enforces no double-booking of a resource). <strong>Touched</strong>: <code>src/server/scheduling-settings.functions.ts</code> (resources in the bundle + <code>createResourceFn</code> / <code>updateResourceFn</code>), <code>src/routes/app.refill.settings.booking.tsx</code> (Rooms &amp; resources section), <code>src/lib/changelog.ts</code>. No schema change &mdash; the <code>scheduling_resources</code> table shipped with the v1.48.0 spine.",
    ],
  },
  {
    version: "v1.56.0",
    date: "June 2026",
    items: [
      "<strong>v1.56.0 &mdash; Choose which services each provider performs. </strong>In the <strong>Online booking</strong> page, expand any service and you&rsquo;ll see an <strong>&ldquo;offers this&rdquo; checkbox</strong> next to every provider &mdash; uncheck it and that provider is hidden from this service everywhere patients book (the provider list, First available, and Best deal). Perfect for an aesthetician who doesn&rsquo;t do injectables, or a tech who only runs the laser. By default everyone offers everything (nothing changes until you opt someone out), and a service no one performs simply won&rsquo;t show on the booking page. <strong>Touched</strong>: new column <code>scheduling_provider_services.offered</code> (default true), <code>src/server/scheduling-settings.functions.ts</code> (offered in the override row + save), <code>src/server/scheduling.functions.ts</code> (filters non-offering providers out of context, slots, hold), <code>src/routes/app.refill.settings.booking.tsx</code> (the checkbox), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.55.0",
    date: "June 2026",
    items: [
      "<strong>v1.55.0 &mdash; Choose which booking option leads. </strong>New setting on the <strong>Online booking</strong> page: when a service is priced differently across providers and patients see both smart options, you choose which one shows <strong>first</strong> &mdash; <strong>Best deal</strong> or <strong>First available</strong>. The default is <strong>Best deal leads</strong>. (When every provider charges the same for a service, only First available shows, so this has no effect there.) <strong>Touched</strong>: new column <code>scheduling_settings.booking_lead_option</code> (default <code>best_deal</code>), <code>src/server/scheduling-settings.functions.ts</code> + <code>src/server/scheduling.functions.ts</code> (read/write + expose to the public page), <code>src/routes/app.refill.settings.booking.tsx</code> (the setting control), <code>src/routes/s.$slug.tsx</code> (orders the two smart cards by the setting), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.54.0",
    date: "June 2026",
    items: [
      "<strong>v1.54.0 &mdash; &ldquo;Best deal&rdquo; booking. </strong>A new smart option on the patient booking page, right under <strong>First available</strong>: <strong>Best deal</strong> finds the soonest opening with your <strong>lowest-priced</strong> provider for that service (ties are pooled together). It only appears when your providers actually price the service differently &mdash; if everyone charges the same, it stays hidden (no pointless choice). So <em>First available</em> optimizes for time, <em>Best deal</em> optimizes for price, and patients pick what matters to them. <strong>Touched</strong>: <code>src/server/scheduling.functions.ts</code> (<code>listAvailableSlots</code> gains a server-resolved <code>cheapestOnly</code> mode that unions across the lowest-price provider(s)), <code>src/routes/s.$slug.tsx</code> (Best deal option, shown only when prices vary), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.53.1",
    date: "June 2026",
    items: [
      "<strong>v1.53.1 &mdash; Fix: added providers now show on the public booking page. </strong>Providers you add (who don&rsquo;t have their own login) were being skipped on the patient-facing page, so it jumped straight past the provider step. Now <strong>every active provider is bookable</strong> &mdash; their appointments are filed under the practice owner&rsquo;s account while still tracking the real provider, so the patient sees the full team and the <strong>First available</strong> option as intended. <strong>Touched</strong>: <code>src/server/scheduling.functions.ts</code> (public booking no longer requires a provider login; holds stamp the tenant owner&rsquo;s <code>user_id</code> with the provider tracked via <code>provider_id</code>), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.53.0",
    date: "June 2026",
    items: [
      "<strong>v1.53.0 &mdash; Patients can pick their provider &mdash; or let us find the soonest. </strong>Your public booking page (<code>/s/&lt;slug&gt;</code>) is now multi-provider. After choosing a service, a patient picks <strong>who</strong> they want to see &mdash; each provider shown with <strong>their</strong> price &mdash; or taps <strong>First available</strong> to grab the soonest opening across your whole team (we quietly assign whoever&rsquo;s free, and reveal &ldquo;with [name]&rdquo; before they confirm). Per-provider pricing now shows up for patients (services that vary read &ldquo;from $X&rdquo;), and the slot times honor each provider&rsquo;s own duration. Single-provider spas see the same simple flow as before. <strong>Touched</strong>: <code>src/server/scheduling.functions.ts</code> (multi-provider context with per-provider prices; <code>listAvailableSlots</code> unions across the team and tags each slot with its provider; <code>holdSlot</code> holds the chosen provider with their effective duration), <code>src/routes/s.$slug.tsx</code> (stepped Service → Provider → Time → Contact flow with a First-available option), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.52.0",
    date: "June 2026",
    items: [
      "<strong>v1.52.0 &mdash; Per-provider time &amp; price (a thing Acuity can&rsquo;t do). </strong>Med spas really do price by provider &mdash; a senior injector commands more than an RN, and a faster provider runs a shorter appointment. Now each bookable service can carry <strong>per-provider overrides</strong>: on the <strong>Online booking</strong> page, expand a service to set a different <strong>duration</strong> or <strong>price</strong> for any provider. Leave a field blank and it simply <strong>inherits</strong> your catalog value (shown as a ghost placeholder), so a single-provider spa never sees any of this &mdash; the complexity only appears when you use it. Owner bookings already honor each provider&rsquo;s effective duration. <strong>Touched</strong>: new table <code>scheduling_provider_services</code> (provider×service map + nullable duration/buffer/price overrides), new <code>src/lib/provider-service.ts</code> (the inherit-by-default resolver), <code>src/server/scheduling-settings.functions.ts</code> (load overrides + <code>setProviderServiceOverrideFn</code>), <code>src/server/scheduling-owner.functions.ts</code> (booking uses effective duration), <code>src/routes/app.refill.settings.booking.tsx</code> (expandable per-provider override panel), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.51.0",
    date: "June 2026",
    items: [
      "<strong>v1.51.0 &mdash; A column for every provider on your calendar. </strong>When you run more than one provider, the <strong>Day</strong> view now shows a <strong>column per provider</strong> side by side &mdash; click any open time in a provider&rsquo;s column to book straight into <em>that</em> provider&rsquo;s day. <strong>Add booking</strong> gained a <strong>Provider</strong> picker, and <strong>Week</strong> view has an <strong>All / per-provider</strong> filter so you can zero in on one person&rsquo;s week (their own hours shade in). Single-provider spas see exactly the same calendar as before &mdash; the columns only appear once you&rsquo;ve added a second provider. Month view is unchanged. <strong>Touched</strong>: <code>src/server/scheduling-owner.functions.ts</code> (day/range loaders return active providers + per-provider hours + provider-tagged appointments; book accepts a provider; cancel ownership widened to all providers; whole-practice block query now tenant-scoped), <code>src/routes/app.refill.schedule.tsx</code> (multi-column day grid, week provider filter, Add-booking provider picker), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.50.0",
    date: "June 2026",
    items: [
      "<strong>v1.50.0 &mdash; Multiple providers, each with their own hours. </strong>Your spa can now run more than one provider &mdash; perfect for part-time injectors, an aesthetician a couple days a week, or a rotating laser tech. On the <strong>Online booking</strong> page there&rsquo;s a new <strong>Providers</strong> section: add a provider, rename anyone inline, and flip them active/inactive (we never delete &mdash; past appointments stay intact, and you always keep at least one active provider). Each new provider starts with a sensible <strong>Mon&ndash;Fri 9&ndash;5</strong> week, and <strong>Business hours</strong> now has a provider picker so every provider keeps their own schedule. The calendar foundation was already provider-aware under the hood &mdash; this is the surface that lets you actually run a team. <strong>Touched</strong>: <code>src/server/scheduling-settings.functions.ts</code> (provider list/create/update fns, per-provider hours save, <code>hoursByProvider</code> bundle), <code>src/routes/app.refill.settings.booking.tsx</code> (Providers section + per-provider hours grid), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.49.0",
    date: "June 2026",
    items: [
      "<strong>v1.49.0 &mdash; Click any open time to book + pick the day right in the dialog. </strong>Two ways to add a booking now: <strong>click an open slot</strong> on the Day or Week calendar (it opens Add booking pre-filled with that day + time, snapped to 5 min), or hit <strong>Add booking</strong> and choose everything in the dialog. The dialog now has its own <strong>Day</strong> (calendar) + <strong>Time</strong> fields, both editable &mdash; so whether you start from the calendar or the button, you can confirm or change the day before booking. Clicking an existing appointment or block won&rsquo;t start a new booking. <strong>Touched</strong>: <code>src/routes/app.refill.schedule.tsx</code> (slot-click → seed dialog; editable Day/Time fields; click-guards on cards/blocks), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.12",
    date: "June 2026",
    items: [
      "<strong>v1.48.12 &mdash; True 5-minute time pickers everywhere. </strong>Chrome&rsquo;s native time dropdown ignores the 5-minute step and always lists every minute &mdash; so every time picker in the scheduler is now a real 5-minute selector (12-hour labels). Applies to Add booking, Block time, business hours, and bulk-apply &mdash; and it&rsquo;s a shared component, so any future time picker is 5-minute by default too. <strong>Touched</strong>: new <code>src/components/refill/TimeSelect.tsx</code>, <code>src/routes/app.refill.schedule.tsx</code> + <code>src/routes/app.refill.settings.booking.tsx</code> (all time inputs swapped), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.11",
    date: "June 2026",
    items: [
      "<strong>v1.48.11 &mdash; Week view: no more clipped appointment cards. </strong>Same readability fix as Day, now applied to Week &mdash; cards have enough height for name + time, and in the narrow week columns the card shows just the <strong>start time</strong> (the full range was getting truncated). <strong>Touched</strong>: <code>src/routes/app.refill.schedule.tsx</code> (week min card height + compact start-time label), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.10",
    date: "June 2026",
    items: [
      "<strong>v1.48.10 &mdash; Add-booking service list shows only bookable services. </strong>The <strong>Add booking</strong> dialog on the calendar was listing your entire catalog; it now shows only services you&rsquo;ve marked <strong>Bookable</strong> on the Online booking page &mdash; matching what patients can book. <strong>Touched</strong>: <code>src/server/scheduling-owner.functions.ts</code> (day + range loaders filter services to <code>online_bookable</code>), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.9",
    date: "June 2026",
    items: [
      "<strong>v1.48.9 &mdash; Bulk-set business hours + 5-minute time pickers. </strong>Two quality-of-life wins on the Online booking page. (1) <strong>Set hours for multiple days at once</strong>: tick the days (or use <em>Weekdays</em> / <em>All</em>), set an open + close time, and hit <strong>Apply</strong> &mdash; no more editing each day one by one (you can also bulk <em>Set closed</em>). (2) <strong>Time pickers now step in 5-minute increments</strong> everywhere in the scheduler (business hours, bulk apply, Add booking, Block time) instead of 1-minute. <strong>Touched</strong>: <code>src/routes/app.refill.settings.booking.tsx</code> (day multi-select + bulk-apply), <code>src/routes/app.refill.schedule.tsx</code>, both via <code>step={300}</code> on every time input, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.8",
    date: "June 2026",
    items: [
      "<strong>v1.48.8 &mdash; Short appointments no longer clip. </strong>Fix: a short appointment (e.g. 15 min) was sized purely by its duration, so its card was too short to fit the patient name AND the time &mdash; the time line got cut off. Cards now have a minimum height that always fits name + time, while longer appointments still size by their real duration (the standard calendar behavior). Applies to Day and Week. <strong>Touched</strong>: <code>src/routes/app.refill.schedule.tsx</code> (min card heights), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.7",
    date: "June 2026",
    items: [
      "<strong>v1.48.7 &mdash; Calendar zoom + readable appointment cards. </strong>Day/Week grids are now denser by default so a 30-minute appointment is tall enough to show the patient name AND the time without clipping, and a new <strong>zoom stepper</strong> (the &minus;/+ control next to the view toggle) lets you size the grid to taste &mdash; your choice is remembered in your browser. Cards always show the name; the time line appears whenever the card is tall enough. <strong>Touched</strong>: <code>src/routes/app.refill.schedule.tsx</code> (pixels-per-minute zoom on Day/Week, height-aware card content), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.6",
    date: "June 2026",
    items: [
      "<strong>v1.48.6 &mdash; Schedule: Week &amp; Month views. </strong>The Schedule tab now has <strong>Day / Week / Month</strong> toggles. Week is a 7-column time grid (open-hours band + bookings + holds + blocks per day, click a day header to jump to it); Month is a clickable calendar (each day shows its first few bookings + a &lsquo;+N more&rsquo;, click any day to open its Day view). Navigation adapts to the view (&plusmn;1 day / &plusmn;1 week / &plusmn;1 month), and Add booking / Block / cancel work from every view. All times in your practice timezone. <strong>Touched</strong>: <code>src/routes/app.refill.schedule.tsx</code> (Day/Week/Month + shared appt/block components), <code>src/server/scheduling-owner.functions.ts</code> (new getRangeScheduleFn + shared name-hydrator), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.5",
    date: "June 2026",
    items: [
      "<strong>v1.48.5 &mdash; Appointment reminder cron. </strong>A scheduled job now sends each patient a reminder email before their appointment (at your configured lead time, default 24h) and clears expired holds as a backstop. Reminders are dedupe-safe &mdash; a patient is never reminded twice &mdash; via a UNIQUE claim-then-send. Runs every 5 minutes on the same pg_cron&rarr;Worker mechanism as the existing sweep. <strong>Touched</strong>: new <code>src/routes/api.cron.scheduling-reminders.ts</code> (release holds + send reminders), new migrations <code>scheduling_reminder_sends</code> (dedupe table) + cron registration, <code>src/integrations/supabase/types.ts</code>, <code>src/lib/changelog.ts</code>. <strong>Activation needs two dashboard pastes</strong> (the dedupe table + the cron registration). Next: the $5/$5 billing meter + the recovery&rarr;native-slot loop.",
    ],
  },
  {
    version: "v1.48.4",
    date: "June 2026",
    items: [
      "<strong>v1.48.4 &mdash; Booking confirmation emails. </strong>When a patient confirms online (or you book them in manually with an email), they now get a clean confirmation email &mdash; date + time in your timezone, from your spa&rsquo;s name &mdash; on the existing Refill email rails. Closes the &lsquo;confirmation on its way&rsquo; gap from the public booking flow. <strong>Touched</strong>: new <code>src/server/scheduling-email.ts</code> (reusable confirmation + reminder senders on the shared email template), <code>src/server/scheduling.functions.ts</code> (confirmBooking sends), <code>src/server/scheduling-owner.functions.ts</code> (manual book sends when an email is given), <code>src/lib/changelog.ts</code>. Next: the reminder cron (24h before) and the $5/$5 billing meter.",
    ],
  },
  {
    version: "v1.48.3",
    date: "June 2026",
    items: [
      "<strong>v1.48.3 &mdash; Owner calendar: the Schedule tab. </strong>A new <strong>Schedule</strong> tab (between Appointments and Catalog) shows your day on a real time grid &mdash; bookings, in-flight holds, your open hours band, and blocked time, all in your timezone. Page through days, <strong>Add booking</strong> to drop a patient in manually (double-book-proof &mdash; the database rejects any overlap), <strong>Block time</strong> for lunch/vacation/meetings, and cancel an appointment to free the slot (a freed native slot is exactly what the recovery engine can later fill). <strong>Touched</strong>: new <code>src/routes/app.refill.schedule.tsx</code> (day-view grid + book/block/cancel dialogs), new <code>src/server/scheduling-owner.functions.ts</code> (getDaySchedule + ownerCreate/Block/Cancel), <code>RefillNav.tsx</code> + <code>RefillShellChrome.tsx</code> (Schedule chip), <code>scheduling-settings.functions.ts</code> (shared helpers), <code>src/lib/changelog.ts</code>. Still to come: confirmation emails + reminders, and the $5/$5 billing meter.",
    ],
  },
  {
    version: "v1.48.2",
    date: "June 2026",
    items: [
      "<strong>v1.48.2 &mdash; Your booking link, front and center. </strong>The Online booking settings page now shows your shareable public link (<code>/s/&lt;your-slug&gt;</code>) right at the top, with one-tap <strong>Copy</strong> and <strong>Open</strong> &mdash; no more guessing the URL. It notes that the link goes live once Online booking is on and a service is marked bookable. <strong>Touched</strong>: <code>src/server/scheduling-settings.functions.ts</code> (returns tenant slug), <code>src/routes/app.refill.settings.booking.tsx</code> (booking-link card), <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.48.1",
    date: "June 2026",
    items: [
      "<strong>v1.48.1 &mdash; Public self-booking page is live. </strong>Patients can now book themselves at <code>/s/&lt;your-slug&gt;</code> &mdash; pick a service, see real open times computed live from your hours + existing appointments, and confirm with their name + email. The slot they pick is <strong>held</strong> during checkout and the booking is double-book-proof at the database layer (if two people grab the same time, one gets a friendly &lsquo;just taken&rsquo; and fresh times). Times render in your practice timezone. Also: the Online booking settings page now <strong>guides you to the persona switcher</strong> when you land on it outside a spa view (instead of a dead-end &lsquo;select a spa&rsquo;). <strong>Touched</strong>: new <code>src/routes/s.$slug.tsx</code> (public booking flow), <code>src/server/scheduling.functions.ts</code> (getPublicBookingContextFn), <code>src/routes/app.refill.settings.booking.tsx</code> (empty-state guide), <code>src/lib/changelog.ts</code>. Still to come: owner calendar grid, confirmation emails + reminders, and the $5/$5 billing meter.",
    ],
  },
  {
    version: "v1.48.0",
    date: "June 2026",
    items: [
      "<strong>v1.48.0 &mdash; Smart Scheduling: the spine + Online booking setup. </strong>The first drop of Refill&rsquo;s native scheduler &mdash; free scheduling, performance-only pricing. New <strong>Online booking</strong> settings tab (Settings &rarr; Online booking) where you flip on patient self-booking, set your timezone, define business hours per day, tune booking rules (minimum notice, booking window, slot interval, hold window, reminders), and choose which services are bookable online with their duration + cleanup buffer. First open auto-creates your provider + a sensible default week, so there&rsquo;s nothing to wire up. <strong>Under the hood</strong>: a database-enforced no-double-booking guarantee (Postgres exclusion constraints &mdash; overlaps are physically impossible, not just app-checked), a timezone/DST-correct slot engine, and the HOLD&rarr;CONFIRM booking flow. The public self-book page, owner calendar, confirmation emails, and the $5/$5 billing meter land next. <strong>Touched</strong>: new migration <code>20260702000000_v1_48_0_smart_scheduling_spine.sql</code> (6 tables + emma_appointments/services extensions + btree_gist EXCLUDE constraints), <code>src/lib/scheduling-slots.ts</code> (slot engine), <code>src/server/scheduling.functions.ts</code> (booking fns), <code>src/server/scheduling-settings.functions.ts</code> + <code>src/routes/app.refill.settings.booking.tsx</code> (settings), <code>SettingsTabStrip.tsx</code> (new tab), <code>src/integrations/supabase/types.ts</code>, <code>src/lib/changelog.ts</code>.",
    ],
  },
  {
    version: "v1.47.11",
    date: "June 2026",
    items: [
      "<strong>v1.47.11 &mdash; Save-as-template dialog: one consistent prompt, Grasshopper&rsquo;s wording. </strong>The save-as-template dialog showed different headlines depending on how it was opened (the inline &lsquo;Save as template&rsquo; link vs. the Send-all prompt). Now it&rsquo;s one line everywhere: <strong>&lsquo;You&rsquo;ve made changes to this template &mdash; save it as a new template?&rsquo;</strong> with a consistent sub-line (&lsquo;Name it to keep it under &ldquo;My templates&rdquo; and reuse it anytime&rsquo;). The footer buttons still adapt to context &mdash; the Send-all path keeps <code>Send without saving</code> / <code>Save &amp; send</code>, the standalone path keeps <code>Cancel</code> / <code>Save template</code> &mdash; but the question the rep reads is identical regardless of where it&rsquo;s invoked. <strong>Touched</strong>: <code>src/routes/app.rep.outreach.tsx</code> (SaveTemplateDialog title + description unified), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.47.10",
    date: "June 2026",
    items: [
      "<strong>v1.47.10 &mdash; Fix: &lsquo;Done&rsquo; in the Add-recipient dialog now saves the current entry. </strong>Grasshopper filled in a recipient (email + name + spa) and clicked <strong>Done</strong> &mdash; and it was discarded. The dialog had two buttons: &lsquo;Save &amp; add another&rsquo; (save + keep stacking) and &lsquo;Done&rsquo; (which just <em>closed</em>, dropping whatever was in the form). <strong>Fix</strong>: Done now commits the current entry before closing &mdash; if there&rsquo;s a valid email typed, it saves it then closes; an invalid email blocks the close and shows the error so nothing is lost silently; an empty form just closes. The X / Escape still cancel (deliberate discard). So both buttons save the in-progress recipient now; they only differ in whether the dialog stays open. <strong>Touched</strong>: <code>src/routes/app.rep.outreach.tsx</code> (AddRecipientDialog: <code>save</code> returns success, new <code>done</code> handler, Done button rewired), <code>src/lib/changelog.ts</code> (this entry). First ship through the new tsc tripwire gate. No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.47.9",
    date: "June 2026",
    items: [
      "<strong>v1.47.9 &mdash; Search-and-destroy round 1: killed the one real dangling-ref bug hiding in the type errors. </strong>After the v1.47.8 <code>setResult</code> lesson, ran <code>tsc --noEmit</code> across the whole codebase to hunt for the same class of bug (an identifier referenced but never declared — the kind esbuild ships and that throws at runtime). Of 268 pre-existing type errors, exactly <strong>ONE</strong> was a genuine dangling reference: <code>emma-inbox.functions.ts</code> used <code>userId</code> (undefined) in the outbound-message lookup instead of the in-scope <code>effectiveUserId</code> that the rest of the function uses &mdash; so the inbox couldn&rsquo;t thread a reply back to the parent message it answered. Fixed (one-word typo). The remaining ~267 errors are non-throwing noise: mostly a stale generated <code>types.ts</code> (SelectQueryError on queries the typed client can&rsquo;t infer) concentrated in <code>seed.ts</code> / <code>zoho.functions.ts</code> / nav-features / NotificationCenter, plus <code>Record</code>-vs-<code>Json</code> and <code>string|null</code> strictness &mdash; cosmetic until <code>types.ts</code> is regenerated. <strong>Touched</strong>: <code>src/server/emma-inbox.functions.ts</code> (userId &rarr; effectiveUserId), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.47.8",
    date: "June 2026",
    items: [
      "<strong>v1.47.8 &mdash; Save an edited message as your own reusable template (+ a fold-regression fix). </strong>Grasshopper&rsquo;s GAP: an edited template IS a new template &mdash; so when you edit and go to send, name it and keep it. <strong>What ships</strong>: (1) <strong>Server</strong> &mdash; <code>outreach_templates</code> gains <code>owner_rep_user_id</code> (NULL = global/admin library, set = rep-private) + <code>name</code>; new <code>saveAsRepTemplate</code> fn inserts a rep-OWNED template with a generated-unique channel (no collision with the shared library); <code>listOutreachTemplates</code> now returns the global library PLUS the rep&rsquo;s own. (2) <strong>UI</strong> &mdash; a <strong>My templates</strong> group in the picker (shown by name) so saved ones are one click away; a <strong>Save as template</strong> link in the composer (appears once you&rsquo;ve edited the body) to save without sending; and on <strong>Send all</strong>, if the body&rsquo;s been edited, a dialog prompts <em>&lsquo;Save your edited message?&rsquo;</em> &rarr; name it then <strong>Save &amp; send</strong>, or <strong>Send without saving</strong> &mdash; exactly the flow Grasshopper described. Rep-private scope (chosen over a shared library) keeps each rep&rsquo;s shelf their own and the admin-curated library uncluttered. The v1.47.7 autosave stays underneath as a mid-edit safety net. (3) <strong>Bug fix</strong>: the v1.47.5 fold removed the <code>result</code>/<code>setResult</code> state but left two <code>onPick</code> handlers calling <code>setResult(null)</code> &mdash; a dangling reference that threw on every template pick (React swallowed it after the select already ran, so it looked fine but errored in console). Removed both. Surfaced because <code>vite build</code> uses esbuild (no type-check); a real <code>tsc --noEmit</code> on the touched files now passes clean. <strong>Touched</strong>: <code>supabase/migrations/20260701000000_v1_47_8_rep_private_templates.sql</code> (Phase A, applied via dashboard), <code>types.ts</code>, <code>src/server/refill-outreach.ts</code> (<code>saveAsRepTemplate</code> + list widening), <code>src/routes/app.rep.outreach.tsx</code> (My-templates group, Save-as-template button, save-on-send dialog, setResult fix), <code>src/lib/changelog.ts</code> (this entry). No dep change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.47.7",
    date: "June 2026",
    items: [
      "<strong>v1.47.7 &mdash; Fix: the shared composer body now persists (autosaves as you type). </strong>Grasshopper composed a message (&lsquo;Hi this is Kelly…&rsquo;) and it vanished — nowhere to find it. <strong>Root cause</strong>: the v1.47.5 fold removed the &lsquo;Save draft&rsquo; button but never replaced it with persistence, so the shared subject/body lived only in React state — edit it, refresh or switch templates, and it was gone. My gap, not a missed button. <strong>Fix</strong>: the shared composer now <strong>autosaves as you type</strong> to <code>localStorage</code>, keyed per <code>(audience, template)</code> — no Save button to hunt for, it just sticks. On returning to a template (same browser) the composed body restores; clearing back to the template default removes the saved slot. Per-recipient tweaks were already persisted server-side (unchanged); this closes the gap for the shared body. <strong>Note</strong>: this is per-browser persistence for now — a follow-up can promote it to a server table so a composed body follows the rep across devices. <strong>Touched</strong>: <code>src/routes/app.rep.outreach.tsx</code> (sharedBodyKey + load/autosave effects replacing the ephemeral reset), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.47.6",
    date: "June 2026",
    items: [
      "<strong>v1.47.6 &mdash; Hotfix: re-adding a recipient who was already sent now re-activates them in the roster. </strong>Grasshopper added two recipients (<code>davidfand303@gmail.com</code> + <code>karen.aslak@gmail.com</code>) and the roster showed only one. <strong>Root cause</strong>: <code>davidfand303</code> had been <em>sent</em> earlier in testing (its draft row carried a <code>sent_at</code> timestamp). <code>saveOutreachDraft</code> upserts on <code>(rep, email, template)</code>, and the upsert <em>preserved</em> the existing <code>sent_at</code> — so re-adding him updated the stamped row, and the roster (which intentionally hides <code>sent_at</code>-set rows so a re-send skips them) silently dropped him. Karen was a fresh row, so she showed; the dialog&rsquo;s &lsquo;2 added&rsquo; counter was correct, one was just hidden. <strong>Fix</strong>: <code>saveOutreachDraft</code> now sets <code>sent_at = null</code> + <code>sent_event_id = null</code> on every save — adding or editing a recipient means they&rsquo;re an ACTIVE draft to send, period. <code>sendOutreachBatch</code> re-stamps at send, so single-batch idempotency (re-click skips sent) is unaffected. The bulk upload path keeps its skip-and-report behavior for already-sent emails (re-uploading a whole list shouldn&rsquo;t silently re-fire). Confirmed against the live rows. <strong>Touched</strong>: <code>src/server/refill-outreach-drafts.ts</code> (<code>saveOutreachDraft</code> re-activation), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.47.5",
    date: "June 2026",
    items: [
      "<strong>v1.47.5 &mdash; Outreach composer: the fold. One clean flow — shared message → roster → send. </strong>Now that the roster (+ Recipient dialog · upload · per-account tweak · Send all) owns adding and sending, the old inline single-recipient path is removed. <strong>What changed</strong>: SendPanel is now purely the <strong>shared composer</strong> — subject + body (the one message the whole batch sends with) + a live preview + the shared Rejuv figures (Prospects). Gone: the inline Recipient-email / First-name / Spa-name fields, the per-recipient &lsquo;Save draft&rsquo; button, the single &lsquo;Send (dry-run)&rsquo; button, and the single-send result panel — all superseded by the roster. Per-recipient identity lives only in the roster now (one way to add: + Recipient or Upload), and <strong>[first name] / [spa name] stay literal in the preview</strong> with a note that they fill per recipient at send. The preview substitutes only the shared figures. Removed the now-dead single-send/save handlers, state, and the <code>SendResultPanel</code> + <code>SendResult</code> type; trimmed the unused imports. The composer reads top-to-bottom exactly as the model works: shape the shared message, build the recipient list, send all. <strong>Touched</strong>: <code>src/routes/app.rep.outreach.tsx</code> (strip single-send/save plumbing + slim SendPanel to the shared composer), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change. <strong>v1.47 outreach composer is now feature-complete on the locked spec</strong> (P0→P4, P5 absorbed, fold done). Remaining work is the parked Zoho-contacts phase (history-dedup, reusable cross-template lists, true upload-before-template).",
    ],
  },
  {
    version: "v1.47.4",
    date: "June 2026",
    items: [
      "<strong>v1.47.4 &mdash; Outreach composer, P4: upload-to-start (paste / CSV, smart column mapping, validate + dedup) + nav cleanup. </strong>Grasshopper&rsquo;s &lsquo;let&rsquo;s do YOUR lean&rsquo; &mdash; bulk-load a recipient list so a rep can start from their spreadsheet instead of typing one by one. <strong>What ships</strong>: (1) NEW <code>src/components/refill/RecipientBulkImport.tsx</code> &mdash; an <strong>Upload list</strong> button in the roster opens a paste-or-CSV dialog. <strong>Paste</strong> handles spreadsheet copy (tab-delimited) natively; <strong>Upload</strong> reads a <code>.csv</code>. <strong>Smart column auto-detect</strong> maps email / first name / spa in any order via a forgiving alias map (and detects headerless data by spotting the <code>@</code> column); the rep can re-map any column with dropdowns + a &lsquo;first row is a header&rsquo; toggle. <strong>Validate + dedup, never silent-drop</strong>: a live &lsquo;47 valid · 2 need a fix · 1 duplicate merged&rsquo; readout, bad-email rows surfaced in a skipped list, dupes merged by lowercased email. (2) NEW server fn <code>saveOutreachDraftsBulk</code> &mdash; dedups the batch, bulk-upserts with <code>on conflict do nothing</code> so a re-pasted list never clobbers a hand-tweaked override, reports inserted-vs-skipped. Uploaded recipients are just drafts, so resume is free (upload 50, send 10, come back for the rest). (3) The empty roster now reads &lsquo;Upload a list to start.&rsquo; <strong>Nav cleanup</strong>: the standalone <strong>Recruit</strong> chip is retired from <code>RefillRepNav</code> &mdash; recruiting is the Recruits tab now, so one Outreach chip covers both books (<code>/app/rep/recruit</code> still redirects in). <strong>Parked for the Zoho-contacts phase</strong>: history-dedup (&lsquo;you&rsquo;ve emailed 3 of these&rsquo;) and reusable cross-template lists. <strong>Touched</strong>: NEW <code>RecipientBulkImport.tsx</code>, <code>src/server/refill-outreach-drafts.ts</code> (<code>saveOutreachDraftsBulk</code>), <code>src/routes/app.rep.outreach.tsx</code> (Upload button + bulk handler + modal wiring), <code>src/components/refill/RefillRepNav.tsx</code> (drop Recruit chip), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change. <strong>Next</strong>: P3b-cleanup (fold the inline single-recipient fields into the dialog now the roster is proven).",
    ],
  },
  {
    version: "v1.47.3",
    date: "June 2026",
    items: [
      "<strong>v1.47.3 &mdash; Outreach composer, P3b: the real recipient roster (+ Recipient, per-account tweak, Send all). </strong>The temp &lsquo;batch send&rsquo; scaffold is replaced by the actual multi-recipient list (edit model A). <strong>What ships</strong>: (1) A <strong>Recipients</strong> roster under the composer in both tabs &mdash; each row is a saved draft (<code>name · email · spa</code>), rows with a per-account override carry an <strong>&lsquo;edited&rsquo;</strong> badge, and a ✕ removes one. (2) <strong>+ Recipient</strong> opens a dialog (email, first name, spa name for Prospects) with <strong>Save &amp; add another</strong> &mdash; stack recipients infinitely without leaving the dialog, a running &lsquo;N added&rsquo; counter confirms each. Every add persists immediately as a draft on the shared body (NULL override = floats with the composer). (3) <strong>Tweak</strong> on a row inline-expands a TipTap editor seeded with that recipient&rsquo;s effective body (override ?? shared ?? template) → <strong>Save override</strong> badges the row, <strong>Reset to shared</strong> clears it. The model-A payoff: edit the shared body up top and every untweaked recipient updates at send time; only the tweaked ones hold their own copy. (4) <strong>Send all (N)</strong> fires the whole roster through the v1.47.1 <code>sendOutreachBatch</code> engine (server-batch, concurrency-3, idempotent), with the live/dry-run telegraph. Uses the house <code>ui/dialog</code> primitive for consistency. <strong>Note</strong>: the inline single-recipient fields + Save-draft stay this ship as a quick-add path (they feed the same roster); the fold-into-dialog cleanup is the next pass once the roster&rsquo;s confirmed. <strong>Touched</strong>: <code>src/routes/app.rep.outreach.tsx</code> (RecipientRoster + AddRecipientDialog + RecipientRow + add/tweak/reset/remove handlers, replacing the temp BatchScaffold), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change. <strong>Next</strong>: P4 = upload-to-start (CSV + paste, smart column mapping, validate/dedup) as a front-door + in the roster.",
    ],
  },
  {
    version: "v1.47.2",
    date: "June 2026",
    items: [
      "<strong>v1.47.2 &mdash; Outreach composer, P3a: Prospects + Recruits unified as tabs (one page, two books). </strong>Grasshopper&rsquo;s call: rep&rsquo;s two outreach books &mdash; spas they&rsquo;re pitching (Prospects) and reps they&rsquo;re recruiting (Recruits) &mdash; now live side-by-side as tabs on <code>/app/rep/outreach</code> instead of two separate pages. The whole data layer is already audience-keyed (<code>outreach_drafts.audience</code>, <code>outreach_templates.audience</code>, <code>outreach_engagement_events.purpose</code>), so the tab just scopes everything: template library, saved drafts, batch send, and past-sends all filter by the active book&rsquo;s audience. <strong>What ships</strong>: (1) A Prospects | Recruits tab bar; switching re-scopes the page (re-fetches templates/sends/drafts/stats for that audience, clears the selected template so a spa template never lingers on Recruits). (2) The Recruits tab carries everything the old standalone page had &mdash; live-stat chips (<code>[my commission rate]</code> / <code>[my month earnings]</code> / <code>[my downstream count]</code> from <code>getMyRecruitStats</code>), the &lsquo;or just share your link&rsquo; affordance, recruit-tone heading + ICP labels (warm peer / cold peer), no spa-name field, and the <code>kelly@getrefill.app</code> rep-voice From line (server-side, unchanged). Prospects keeps the spa-name + Rejuv-figure fields. (3) <code>/app/rep/recruit</code> is retired &mdash; it now redirects to <code>/app/rep/outreach?tab=recruits</code>, so existing links / nav chips / bookmarks keep working. This also absorbs the plan&rsquo;s P5 (recruit parity): the Recruits tab IS the recruit composer, sharing 100% of the P3/P4 roster + bulk machinery via the audience param &mdash; one build, both books. <strong>Touched</strong>: <code>src/routes/app.rep.outreach.tsx</code> (audience tab state, scoped loads, tab bar + chips + share-link + audience-aware SendPanel/IcpSection), <code>src/routes/app.rep.recruit.tsx</code> (now a redirect), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change. <strong>Next</strong>: P3b = the recipient roster (+ Recipient dialog, infinite add, per-account tweak) inside each tab.",
    ],
  },
  {
    version: "v1.47.1",
    date: "June 2026",
    items: [
      "<strong>v1.47.1 &mdash; Outreach composer, P2: shared send core + multi-send batch engine. </strong>Server-batch orchestration (Grasshopper&rsquo;s call over client-pool: one auth check, one mode resolution, idempotency adjacent to the send). <strong>What ships</strong>: (1) The single-send pipeline is refactored into a shared core in <code>refill-outreach-send.ts</code> &mdash; <code>buildOutreachSendContext</code> (resolves the once-per-batch bits: auth-derived sender persona, mode, template, From: line, rep stats) + <code>dispatchOneOutreachEmail</code> (the per-recipient work: placeholder context &rarr; render &rarr; engagement-row insert &rarr; reply-to token alloc &rarr; Resend). The engagement-row insert and the reply-to plus-address token stay welded together (the row id IS the token) so inbound reply routing is untouched &mdash; the partial-port footgun the plan flagged. <code>sendOutreachEmail</code> keeps its exact input/output contract and now just delegates through the same core, so there is one send path, not two. (2) NEW <code>sendOutreachBatch</code> &mdash; loads this rep&rsquo;s unsent drafts for a template, resolves context once, dispatches each recipient through a 3-wide worker pool (awaits the whole pool, no fire-and-forget per <code>feedback-workers-no-fire-and-forget</code>), resolves per-account-override-vs-shared-body per recipient, and stamps <code>sent_event_id</code>/<code>sent_at</code> on each draft so a re-click skips already-sent rows (idempotent). (3) A temporary &lsquo;Send all (dry-run)&rsquo; scaffold under the SendPanel exercises the batch &mdash; P3 replaces it with the real multi-recipient list. Forced <code>dryRun:true</code> so it never fires real email regardless of the OUTREACH_LIVE flag. <strong>Touched</strong>: <code>src/server/refill-outreach-send.ts</code> (extract shared core, slim single-send), <code>src/server/refill-outreach-drafts.ts</code> (<code>sendOutreachBatch</code> + worker pool), <code>src/routes/app.rep.outreach.tsx</code> (batch handler + temp scaffold), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change. <strong>Next</strong>: P3 = real multi-recipient list + +Recipient + per-account tweak panel.",
    ],
  },
  {
    version: "v1.47.0",
    date: "June 2026",
    items: [
      "<strong>v1.47.0 &mdash; Outreach composer, P1: inline edits now persist as drafts (save without sending). </strong>Surfaced in the 2026-06-05 smoke walk: a rep&rsquo;s inline TipTap edit on the Outreach SendPanel was a one-shot in-memory <code>bodyOverride</code> &mdash; it evaporated the moment you navigated away. P1 lands the persistence substrate + the first &lsquo;Save draft&rsquo; control of the per-account-draft, multi-recipient composer (edit model A: one shared body + per-account overrides). <strong>What ships</strong>: (1) NEW <code>outreach_drafts</code> table &mdash; one flat row per <code>(rep, recipient_email, template)</code>, nullable <code>subject_override</code> / <code>body_override</code> (NULL = use the shared batch body), Zoho-forward <code>contact_id</code>, <code>sent_at</code>/<code>sent_event_id</code> stamps for the P2 batch-send idempotency, service-role RLS. <code>recipient_email</code> stored lowercased so the unique index doubles as the dedup guard. (2) NEW <code>src/server/refill-outreach-drafts.ts</code> &mdash; <code>saveOutreachDraft</code> (upsert), <code>listOutreachDrafts</code>, <code>deleteOutreachDraft</code>; <code>requireRepOrAdmin</code> gate + ownership in every WHERE, mirrors the <code>refill-outreach.ts</code> module shape. (3) &lsquo;Save draft&rsquo; button on the SendPanel + a &lsquo;Saved &mdash; reopens with this edit&rsquo; chip; a matching saved draft re-hydrates its overrides once per <code>(template, recipient)</code> on the next visit, proving persistence without sending. The existing single-send path is completely untouched &mdash; this is purely additive. <strong>Touched</strong>: NEW <code>supabase/migrations/20260630000000_v1_47_0_outreach_drafts.sql</code> (applied via dashboard), NEW <code>src/server/refill-outreach-drafts.ts</code>, <code>src/integrations/supabase/types.ts</code> (outreach_drafts types, hand-added scoped), <code>src/routes/app.rep.outreach.tsx</code> (drafts state + Save button + hydrate-on-match), <code>src/lib/changelog.ts</code> (this entry). No dep change, no wrangler.jsonc change. <strong>Next</strong>: P2 = shared <code>dispatchOneOutreachEmail</code> helper + <code>sendOutreachBatch</code> (server-batch, concurrency-3).",
    ],
  },
  {
    version: "v1.46.8",
    date: "June 2026",
    items: [
      "<strong>v1.46.8 &mdash; &lsquo;Our Story&rsquo; promoted to a prominent hero CTA. </strong>The v1.46.7 header-nav link wasn&rsquo;t prominent enough for Grasshopper&rsquo;s taste. Added a secondary <code>Our Story &rarr;</code> button in the landing hero, positioned BETWEEN the primary &lsquo;See what we&rsquo;d recover&rsquo; CTA and the &lsquo;30-day free trial&rsquo; microcopy. Styled as an OUTLINE button (emerald border + emerald text, transparent fill) so it&rsquo;s button-prominent but reads clearly secondary to the filled-green conversion CTA above it &mdash; doesn&rsquo;t steal the primary click. The header-nav <code>Our Story</code> link stays too (per <code>feedback-multiple-routes-landing</code>: multiple routes to the same destination are fine/preferred on landing pages). <strong>Touched</strong>: <code>src/routes/index.tsx</code> (hero secondary CTA), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.46.7",
    date: "June 2026",
    items: [
      "<strong>v1.46.7 &mdash; Demo arrow now mirrors the conversion funnel + &lsquo;Our Story&rsquo; gets a home. </strong>Grasshopper spotted it walking the v1.46.6 demo arrow: on the landing, the big green CTA (&lsquo;See what we&rsquo;d recover&rsquo;) goes <code>/ &rarr; /scan</code>, but the Next arrow was sending you <code>/ &rarr; /story</code> &mdash; two different next-steps on one screen, confusing in a live demo. <strong>Fix</strong>: (1) The <code>WALKTHROUGH</code> sequence in <code>DemoNav.tsx</code> drops <code>/story</code> and now mirrors the real funnel &mdash; <code>/ &rarr; /scan &rarr; /start &rarr; /claim-your-business</code> &mdash; so the arrow always agrees with the page&rsquo;s own primary CTA. <code>/story</code> isn&rsquo;t a funnel step; it&rsquo;s the why-narrative branch. (2) Added an <code>Our Story &rarr;</code> link to the landing header nav (<code>RefillLanding</code>, alongside Sign in / For reps) so the narrative is one tap away without hijacking the conversion path. <code>/story</code> already carries a &lsquo;See your number &rarr;&rsquo; link in its own header, so a detour into the story flows right back into <code>/scan</code> &mdash; the funnel self-heals, no dead end. <strong>Touched</strong>: <code>src/components/DemoNav.tsx</code> (sequence), <code>src/routes/index.tsx</code> (landing header link), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.46.6",
    date: "June 2026",
    items: [
      "<strong>v1.46.6 &mdash; Guided demo walkthrough arrow on the public marketing pages. </strong>Grasshopper asked for a bottom-right &lsquo;Next&rsquo; arrow so the public surfaces can be presented as a linear demo instead of standalone screens you navigate by hand. <strong>What ships</strong>: NEW <code>src/components/DemoNav.tsx</code> &mdash; a single position-fixed controller mounted ONCE in <code>__root.tsx</code> (the public pages share no layout route, so per-page mounting would be six copies). It self-gates: a <code>WALKTHROUGH</code> array (<code>/</code> &rarr; <code>/story</code> &rarr; <code>/scan</code> &rarr; <code>/start</code> &rarr; <code>/claim-your-business</code>) is the single source of truth for the sequence; on any path NOT in that array <code>findIndex</code> returns -1 and the component renders null &mdash; so it never appears on <code>/app/*</code>, <code>/login</code>, <code>/unsubscribe</code>, etc. Beyond the bare Next arrow: a subtle Back chevron (hidden on the first stop) + an <code>N / total</code> step counter so a live demo is bidirectional and the audience sees their position; the last stop swaps Next for an &lsquo;End of tour&rsquo; pill. Brand-matched (<code>bg-emerald text-paper</code>, paper Back button, <code>border-border-soft</code>), uses TanStack <code>Link</code> for client-side nav (no full reload between stops) and <code>useLocation</code> to derive the current stop. <code>z-40</code> sits below the sonner Toaster&rsquo;s <code>z-[100]</code> so a toast never hides behind it. <strong>Reorder/extend</strong>: edit the one <code>WALKTHROUGH</code> array. <strong>Touched</strong>: NEW <code>src/components/DemoNav.tsx</code>, <code>src/routes/__root.tsx</code> (import + mount next to Toaster), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.46.5",
    date: "June 2026",
    items: [
      "<strong>v1.46.5 &mdash; Nav-coherency pass: two chip-less orphans closed + one dead link ripped. </strong>Surfaced during the v1.46.4 smoke walk &mdash; Grasshopper hit <code>/app/refill/appointments</code> from its RefillHome card and noticed there was no way back to it once off the home grid (no nav chip). A full nav-orphan audit across all three persona strips (Spa / Rep / Admin) confirmed the complete set: Rep and Admin navs are perfectly coherent; the Spa-owner strip had exactly two ungated landing-cards with no chip &mdash; <code>Appointments</code> and <code>Reports</code> &mdash; reachable only from the home grid. <strong>What ships</strong>: (1) <code>Appointments</code> chip added to <code>RefillNav</code> right after Patients (WHO &rarr; WHEN pairing), short-label <code>Appts</code> so the now-11-chip strip doesn&rsquo;t overflow on mobile. (2) <code>Reports</code> chip added after Recovery (ungated; subtitle already &lsquo;Promotions funnel + per-campaign rollups&rsquo;). (3) Ripped the dead <code>/personas</code> link on <code>login.tsx</code> (the &lsquo;Testing the system? Visit /personas&rsquo; line) &mdash; that route never existed, the data layer in <code>src/lib/personas.ts</code> is consumed only by the authenticated admin PersonaSwitcher, and a public demo-identity switcher would cut against stealth-first + the queued frontdoor password-protect. <code>Link</code> import retained (still used by the logo + brand links). <strong>Audit also corrected a stale finding</strong>: the &lsquo;Admin Flags chip missing&rsquo; note from the prior session is resolved &mdash; Flags was renamed to Agents and <code>app.admin.flags.tsx</code> is now a redirect stub to <code>/app/admin/agents</code>. Intentionally chip-less surfaces (Campaigns/Sharing/Waitlist suite via featureKey gates; rescue/health diagnostic deep-links) left untouched by design. <strong>Touched</strong>: <code>src/components/refill/RefillNav.tsx</code> (2 chips + 2 union keys), <code>src/routes/login.tsx</code> (removed dead-link paragraph), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.46.4",
    date: "June 2026",
    items: [
      "<strong>v1.46.4 &mdash; Hotfix: app-wide toast system was silently dead. </strong>~50 callsites across the codebase fire <code>toast.success()</code> / <code>toast.error()</code> from sonner (Zoho connect/disconnect, outreach send, template import, light-mode enable, search-param-bounce success/error banners, etc.) but the <code>&lt;Toaster /&gt;</code> render component was never mounted anywhere in the tree. So every toast call has been a no-op since the Refill cleave. Grasshopper caught it during Zoho disconnect testing &mdash; the &ldquo;Zoho disconnected · grant revoked&rdquo; toast that v1.46.3 was supposed to surface was firing but with nowhere to render. <strong>Fix</strong>: mount <code>&lt;Toaster richColors position=&ldquo;bottom-right&rdquo; theme=&ldquo;light&rdquo; closeButton /&gt;</code> at <code>src/routes/__root.tsx</code> so every page in the app inherits it. <strong>Touched</strong>: <code>src/routes/__root.tsx</code> (Toaster import + mount), <code>src/lib/changelog.ts</code> (this entry). Per <code>feedback-own-it-no-excuses</code>: I should have walked the Zoho connect flow end-to-end before claiming v1.46.0 shipped &mdash; the OAuth round-trip worked but every status toast was invisible, masking the actual UX for the rep.",
    ],
  },
  {
    version: "v1.46.3",
    date: "June 2026",
    items: [
      "<strong>v1.46.3 &mdash; Disconnect now revokes the grant on Zoho&rsquo;s side too. </strong>Before: Disconnect cleared Refill&rsquo;s local row but left the grant alive in Zoho. Next Connect click skipped the first-time consent screen because Zoho remembered Refill was already authorized &mdash; impossible to demo the clean-slate flow, and left stale grants in the rep&rsquo;s Zoho permissions panel. After: <code>disconnectZoho</code> first POSTs to Zoho&rsquo;s <code>/oauth/v2/token/revoke</code> with the refresh_token, then clears the local row. Best-effort &mdash; if Zoho is down the local clear still proceeds, and the toast tells the rep which path landed. <strong>Touched</strong>: <code>src/server/zoho.functions.ts</code> (revoke call + return flag), <code>src/routes/app.rep.integrations.tsx</code> (toast text reflects revoke outcome + confirm dialog explains the side effect), <code>src/lib/changelog.ts</code> (this entry). No dep change, no schema change.",
    ],
  },
  {
    version: "v1.46.2",
    date: "June 2026",
    items: [
      "<strong>v1.46.2 &mdash; Hotfix: Zoho OAuth callback bounce crashed the integrations page on render.</strong> The OAuth round-trip itself worked end-to-end &mdash; token exchange succeeded, connection row upserted, browser redirected back to <code>/app/rep/integrations?zoho_connected=1</code>. But the page&rsquo;s Zod <code>validateSearch</code> schema demanded a string for <code>zoho_connected</code>, and TanStack Router&rsquo;s search-param parser auto-coerced the literal <code>1</code> to a number. Zod rejected, error boundary swallowed. <strong>Fix</strong>: relax both <code>zoho_connected</code> and <code>zoho_error</code> to accept string/number/boolean and normalize to string in the transform &mdash; same downstream <code>=== &ldquo;1&rdquo;</code> comparison still works. Per <code>feedback-own-it-no-excuses</code>: my schema was too strict; I should have tested the bounce against the actual redirect URL before shipping v1.46.0. <strong>Touched</strong>: <code>src/routes/app.rep.integrations.tsx</code> (schema only), <code>src/lib/changelog.ts</code> (this entry). No dep change, no migration, no behavior change for any other surface.",
    ],
  },
  {
    version: "v1.46.1",
    date: "June 2026",
    items: [
      "<strong>v1.46.1 &mdash; Hotfix: light-mode server fns broken by transitive TanStack Start bump in v1.45.0. Scheduler settings page (and any page that transitively imports light-mode-functions) was throwing <code>TypeError: e(...).validator is not a function</code> in the browser error boundary. Root cause: <code>src/server/light-mode.functions.ts</code> had 6 callsites using the deprecated <code>.validator(SCHEMA)</code> signature; the rest of the codebase (~263 callsites) had already been migrated to <code>.inputValidator((raw: unknown) => SCHEMA.parse(raw))</code>. The TipTap install in v1.45.0 transitively upgraded TanStack Start and that release dropped the deprecated alias.</strong> <strong>Owning it</strong>: I bumped TanStack transitively in v1.45.0 and didn&rsquo;t audit the rest of the codebase for deprecated APIs before shipping. Per <code>feedback-own-it-no-excuses</code>: name + act + done. <strong>What ships</strong>: convert all 6 <code>.validator(SCHEMA)</code> callsites in <code>light-mode.functions.ts</code> to the current <code>.inputValidator((raw: unknown) =&gt; SCHEMA.parse(raw))</code> pattern. Schemas extracted to local const so the lambda stays one line. Affected fns: <code>listLightModeConnections</code>, <code>enableLightMode</code>, <code>disableLightMode</code>, <code>listLightModeQuarantine</code>, <code>getPendingGmailVerification</code>, <code>getSchedulerExtras</code>. Behavior unchanged &mdash; same input shapes, same handlers. <strong>Touched</strong>: <code>src/server/light-mode.functions.ts</code> (6 fn signatures), <code>src/lib/changelog.ts</code> (this entry). No schema change; no dependency change; no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.46.0",
    date: "June 2026",
    items: [
      "<strong>v1.46.0 &mdash; Zoho CRM direct OAuth. Self-service Connect on <code>/app/rep/integrations</code>. Cleanest gated vendor we&rsquo;ve scoped: no partner approval, no Mindbody-style Go-Live wait, no Jane AI-exclusion problem &mdash; pure OAuth2 like Stripe/Square. Per <code>project-customer-as-developer-access</code>: Refill registers one Server-based App at api-console.zoho.com; every rep ever (including tomorrow&rsquo;s pilot user Kelly) connects with one click via the standard consent screen.</strong> <strong>What ships</strong>: (1) NEW migration <code>20260629000000_v1_46_0_zoho_connections.sql</code> &mdash; <code>zoho_connections</code> table (one row per rep) with per-rep encrypted access_token + refresh_token, region-aware accounts_server + api_domain (US/EU/IN/AU/JP/CA), scopes, status enum (connected/expired/revoked/error), identity hints (zoho_user_email + zoho_org_name + zoho_org_id), bookkeeping (connected_at, last_sync_at, last_synced_contact_count, last_error). RLS: rep reads own row only; writes via service role only. Indexes on user_id, tenant_id, status. (2) NEW <code>src/lib/integrations/zoho.ts</code> &mdash; pure helpers: <code>ZOHO_SCOPES</code> (contacts.READ + leads.READ + notes.CREATE + tasks.CREATE + users.READ + aaaserver.profile.READ), <code>buildZohoAuthorizeUrl</code> (access_type=offline + prompt=consent so we always get a refresh token), <code>exchangeCodeForTokens</code>, <code>refreshAccessToken</code>, <code>fetchZohoCurrentUser</code>, <code>fetchZohoOrgName</code>, <code>fetchZohoContacts</code>, <code>deriveApiDomain</code> + <code>normalizeAccountsServer</code> for region routing. (3) NEW <code>src/server/zoho.functions.ts</code> &mdash; server fns: <code>initiateZohoOAuth</code> (builds redirect with CSRF-signed state), <code>getMyZohoConnection</code> (UI read; fail-soft if table doesn&rsquo;t exist yet, so the page renders before migration applies), <code>syncMyZohoContacts</code> (auto-refreshes access token if expiring within 60s; stamps last_sync_at; flips status to expired on 401), <code>disconnectZoho</code> (sets status=revoked + clears tokens). State encoding (<code>encodeZohoState</code>/<code>decodeZohoState</code>) is independent of the SchedulerPlatform type &mdash; Zoho isn&rsquo;t a scheduler. (4) NEW <code>src/routes/api.integrations.zoho.oauth-callback.ts</code> &mdash; GET handler: decodes state, exchanges code, captures the <code>accounts-server</code> query param for region routing, fetches /users?type=CurrentUser + /org for display hints, upserts the row, redirects to state.returnTo with ?zoho_connected=1. Failure modes redirect with ?zoho_error=&lt;code&gt; so the UI surfaces a sonner toast. (5) Rewrote Zoho card on <code>app.rep.integrations.tsx</code>: validateSearch schema for the callback bounce params; useEffect surfaces success/error toasts; new <code>ConnectZohoCard</code> component handles 3 states: not-connected (green Connect button), connected (status pill + email + org + last-sync-N-ago + Sync now button + contact preview table + Disconnect link), expired/error (amber Reconnect needed pill + last_error banner + amber Reconnect button). <code>humanizeZohoError</code> maps callback error codes to plain-English toast text. HubSpot + Salesforce ComingSoonCards untouched. <strong>Deleted</strong>: <code>src/server/composio-zoho.ts</code> (~295 LOC) &mdash; the never-shipped v398 Composio-broker orphan. Its docblock literally said &lsquo;re-port that stack OR rewire to a direct OAuth flow&rsquo;; we rewired. <strong>What this DOESN&rsquo;T ship</strong>: local contact persistence (no <code>zoho_contacts</code> table &mdash; preview only; Kelly&rsquo;s meeting tomorrow is &lsquo;can we connect&rsquo;, not &lsquo;where does her data live&rsquo;); &lsquo;Add to outreach audience&rsquo; CTA (post-Kelly-call); Notes/Tasks writeback (scopes declared but no UI calls them yet); HubSpot + Salesforce (stay Coming Soon &mdash; scope discipline); Zoho Notifications API webhooks (polling-on-demand is fine for v1); onboarding wizard hook. <strong>Operator handoff after deploy lands</strong>: (a) paste <code>20260629000000_v1_46_0_zoho_connections.sql</code> via Supabase dashboard (per <code>feedback-migrations-via-dashboard</code>); (b) register Refill at api-console.zoho.com (Server-based Application, redirect URI = https://getrefill.app/api/integrations/zoho/oauth-callback) and grab Client ID + Secret; (c) wrangler secret put ZOHO_CLIENT_ID + ZOHO_CLIENT_SECRET (per <code>feedback-wrangler-secret-routine</code>). Then Connect button works end-to-end. <strong>Touched</strong>: 3 NEW files (<code>zoho.ts</code> + <code>zoho.functions.ts</code> + <code>api.integrations.zoho.oauth-callback.ts</code>), 1 NEW migration, <code>src/routes/app.rep.integrations.tsx</code> (rewrote main page + new ConnectZohoCard component), DELETED <code>src/server/composio-zoho.ts</code>, <code>src/lib/changelog.ts</code> (this entry). No new dependencies; no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.45.0",
    date: "June 2026",
    items: [
      "<strong>v1.45.0 &mdash; WYSIWYG inline edit + paste/upload template seeding. The v1.44 inline-edit shipped as a raw-HTML textarea &mdash; reps saw <code>&lt;p&gt;Hi [first name],&lt;/p&gt;</code> markup, not compose-like output. Per [[feedback-human-first-design]]: computers must think like humans, not the reverse. v1.45.0 swaps that textarea for a TipTap WYSIWYG editor across all 3 surfaces (rep Outreach + rep Recruit + admin EditModal) and adds a paste-or-upload template seeder for admin.</strong> <strong>What ships</strong>: (1) NEW <code>src/components/refill/TemplateEditor.tsx</code> &mdash; TipTap-based React editor with StarterKit (bold/italic/lists/headings/blockquote), Link extension, and a custom ProseMirror Decoration plugin that renders placeholder tokens (<code>[first name]</code>, <code>$[exact figure]</code>, <code>[my commission rate]</code>, etc.) as inline chips. Underlying text stays as the bracketed literal &mdash; same shape the server <code>substitutePlaceholders</code> regex in <code>refill-outreach-send.ts</code> already consumes; no round-trip serializer needed. Toolbar: Bold, Italic, Link, Bulleted list, Numbered list, Undo. Light-bg styling, self-contained CSS, ~350 LOC. (2) Wired into <code>app.rep.outreach.tsx</code> SendPanel (replaces the per-send <code>bodyOverride</code> textarea), <code>app.rep.recruit.tsx</code> SendPanel (same), and <code>app.admin.outreach.tsx</code> EditModal (non-Loom channels &mdash; Loom scripts keep the plain Textarea since they're read aloud, not rendered). (3) NEW <code>src/components/refill/TemplateBulkImport.tsx</code> &mdash; paste-or-upload modal for seeding the template library from any source. Accepts raw paste, <code>.txt</code>, <code>.md</code>, and <code>.docx</code> (via <code>mammoth.js</code>). Auto-detects multi-message docs by splitting on <code>---</code> separators (markdown HR for text/md; <code>&lt;hr&gt;</code> or <code>&lt;p&gt;---&lt;/p&gt;</code> for HTML from mammoth). Auto-extracts subject from leading <code>Subject: …</code> line per message. Each parsed message lands in a review card with editable subject + TipTap body + ICP + channel + audience (spa/rep) selectors. Save-all calls the existing <code>bulkUpsertOutreachTemplates</code> path. Mounted alongside the existing HTML-pack import on <code>/app/admin/outreach</code> &mdash; two buttons, two mental models (HTML pack = structured Refill-Outreach-Pack.html doc; Paste/upload = any source). (4) Server fn <code>bulkUpsertOutreachTemplates</code> in <code>refill-outreach.ts</code> extended: <code>templateInputSchema</code> gains optional <code>audience</code> field; existing-active lookup scopes by audience so a new rep-audience template doesn't accidentally deactivate the matching spa-audience template with the same (icp, channel) tuple. Back-compat preserved &mdash; the HTML-pack importer doesn't pass audience and defaults to 'spa' which matches its prior behavior. <strong>Why this matters</strong>: reps are not engineers. Markdown asterisks visible in an editor = the computer leaking through. Per [[feedback-ux-standard]]: perfection is the baseline. Per [[feedback-setup-wizards-auto-advance]]: every friction point is a customer-loss risk &mdash; same applies to authoring friction on the rep tools. The paste/upload path lets admin seed templates from Notes / Word / Gmail draft / any source in seconds instead of HTML-marker-hand-coding. <strong>What this DOESN&rsquo;T ship</strong>: rep-side template upload (would require per-rep template scoping; current <code>outreach_templates</code> is a shared library &mdash; deferred to a follow-up ship if Kelly/Karen request it); atomic-delete placeholder semantics (chips render via Decoration; rep can still backspace through a token char-by-char &mdash; v2 concern); frontdoor password-protect (queued for post-demo tomorrow per stealth-first doctrine, NOT touching creds before a demo). <strong>Dependencies added</strong>: <code>@tiptap/react</code>, <code>@tiptap/pm</code>, <code>@tiptap/starter-kit</code>, <code>@tiptap/extension-link</code>, <code>mammoth</code>. ~72 new package descendants. Bundle: admin.outreach chunk grew to 1.11MB (TipTap + mammoth) &mdash; admin-only surface, not on the critical path. <strong>Touched</strong>: 2 NEW files (<code>TemplateEditor.tsx</code>, <code>TemplateBulkImport.tsx</code>), <code>src/routes/app.rep.outreach.tsx</code> (+ editor import + replaced textarea), <code>src/routes/app.rep.recruit.tsx</code> (+ editor import + replaced textarea), <code>src/routes/app.admin.outreach.tsx</code> (+ TemplateBulkImport mount + Paste/upload button + FileText icon + audience-passthrough wired via existing bulk handler), <code>src/server/refill-outreach.ts</code> (+ audience in templateInputSchema and bulkUpsert insert), <code>src/integrations/supabase/types.ts</code> (stripped accidental Supabase CLI update-notice stomp at EOF; pre-existing, unrelated to feature), <code>src/lib/changelog.ts</code> (this entry), <code>package.json</code> + <code>package-lock.json</code> (deps). No new migrations; no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.44.3",
    date: "June 2026",
    items: [
      "<strong>v1.44.3 &mdash; Orphan punch list closed. Registers the 4 remaining audit-surfaced orphans (Sharing + Waitlist invite/seed/bulk) as nav-features in the v1.44.2 registry. All default_visible=false &mdash; invisible to every tenant until admin flips per-tenant via <code>/app/admin/nav-features</code>. The v1.45.0 strategic-triage from the post-v1.44.0 audit collapses to seed rows.</strong> <strong>What ships</strong>: (1) Migration <code>20260628000000_v1_44_3_orphan_registration.sql</code> &mdash; 4 INSERTs into <code>nav_features</code> (idempotent via ON CONFLICT DO NOTHING). (2) <code>RefillHome.tsx</code> ACTIONS array gains 4 cards: Sharing (Share2 icon), Waitlist invite + Waitlist seed (per patient) + Waitlist seed (bulk) (UserPlus icon for the trio). All gated by featureKey + filter through <code>visibleKeys</code> from the v1.44.2 visibility fetch. <strong>Status of each registered orphan</strong>: (a) <code>nav.sharing</code> &mdash; spa-grant page exists at <code>/app/refill/sharing</code>; rep-side reader <code>getSpaAggregatesForRep[s]</code> never wired to UI. Registered but should STAY off until rep-side ships OR a strategic call to rip the half-feature. Adding to registry is forward-looking; flipping ON would surface a card that links to a half-build. (b) Waitlist trio &mdash; fully functional 691 + 425 + 686 LOC composers. Safe to flip ON for any tenant that needs them. Likely first user: Rejuv during cohort migration, then any spa with a sizeable pre-existing waitlist. <strong>End-to-end punch-list state post-v1.44.3</strong>: Account / Sender / Spa profile (always-on via SettingsTabStrip from v1.44.1); Appointments / Reports (always-on cards from v1.44.1); Canonical brands (admin nav from v1.44.1); Economics (rep nav from v1.44.0); Campaigns (opt-in via nav.campaigns, ON for Rejuv); Sharing + Waitlist trio (opt-in registered, default OFF). ~5,000 LOC of pre-existing UI either surfaced or queued behind an admin toggle. No orphans remaining from the 2026-06-03 audit. <strong>Doctrine note</strong>: future opt-in features land as another INSERT into <code>nav_features</code> + a featureKey-gated card in the relevant Home component. The decision-cost of \"where do we put this?\" is now ~3 lines of code. <strong>What this DOESN&rsquo;T ship</strong>: the rep-side P4 sharing reader (still needs strategic call &mdash; ship the reader OR rip the spa-grant); per-rep nav features (RefillRepNav doesn&rsquo;t filter through visibility yet; can extend when a rep-side opt-in lands); frontdoor password-protect for getrefill.app (queued separately per stealth-first doctrine). <strong>Migration</strong>: paste via Supabase dashboard before deploy lands; idempotent ON CONFLICT keeps re-runs safe. <strong>Touched</strong>: 1 NEW migration, <code>src/components/refill/RefillHome.tsx</code> (+ 4 cards, + Share2 + UserPlus icons), <code>src/lib/changelog.ts</code> (this entry). No new files, no new dependencies, no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.44.2",
    date: "June 2026",
    items: [
      "<strong>v1.44.2 &mdash; Nav-features registry + per-tenant overrides. Admin-controlled visibility for optional nav surfaces. Same architectural pattern as v1.34.3 agent_defaults applied to nav. Ship features stealth + light them up for Rejuv first (per [[project-customer-as-developer-access]] + [[feedback-stealth-first-doctrine]]), then expand per-tenant as the rollout matures. Doctrine extension banked: the orphan audit + sprint-vs-marathon framing surfaced that we needed an admin-controlled visibility layer, not just nav-additions.</strong> <strong>New tables (migration v1_44_2_nav_features)</strong>: <code>public.nav_features</code> (feature_key text PK, label, description, persona [spa/rep/admin], default_visible bool, timestamps) + <code>public.tenant_nav_overrides</code> (tenant_id + feature_key composite PK, visible bool, updated_by, updated_at). RLS: registry world-readable for authenticated (so RefillNav can resolve at render); overrides readable by tenant members or admins. Writes via service role only. Seed: <code>nav.campaigns</code> default_visible=false &mdash; admin flips it on for Rejuv as the first opt-in via the new admin UI. <strong>New server fn module</strong> <code>src/server/refill-nav-features.functions.ts</code> (~250 LOC): <code>listVisibleNavFeaturesForTenant</code> (read path used by RefillHome filter), <code>listNavFeatures</code> + <code>listTenantNavOverrides</code> + <code>listAllTenantsForNavOverride</code> (admin reads), <code>setNavFeatureDefault</code> + <code>setTenantNavOverride</code> + <code>clearTenantNavOverride</code> (admin writes). All admin fns go through <code>requireAdminRole</code>. <strong>New admin UI</strong> <code>/app/admin/nav-features</code> (~360 LOC): one card per nav feature with global-default toggle on the right + per-tenant overrides list below. Add override flow: pick tenant from dropdown + pick ON/OFF + Save. Revert-to-default link on each override. Demo tenants flagged with amber badge. Mirrors <code>/app/admin/agents</code> shape so the operator pattern is consistent. <strong>RefillHome rewire</strong>: ACTIONS now uses an optional <code>featureKey</code> field. Cards without featureKey are always-on (the daily/weekly core flow); cards with featureKey only render when <code>listVisibleNavFeaturesForTenant</code> returns the key for the current tenant. Hides opt-ins during the visibility-flag-fetch (null state) to avoid flash-then-disappear. Fail-soft on fetch error: empty set, only always-on cards show. <strong>Campaigns card</strong>: added to ACTIONS with <code>featureKey: 'nav.campaigns'</code>. Pre-ship the 1,500-LOC Promotions Engine Phase 1 lived as orphan since v355-v360. Post-ship: invisible to every tenant by default + admin enables Rejuv first to validate the surface; once Karen exercises it, default can flip ON globally or it stays opt-in per the strategic call. <strong>AdminNav + admin index</strong>: chip + card added for /app/admin/nav-features (LayoutGrid icon). <strong>Why this matters</strong>: per [[user-dog-years-thesis]] + stealth-first, every shipped surface is an identification risk if it's visible to every tenant. Per-tenant nav visibility lets us iterate on features behind a per-tenant ON switch &mdash; same posture as feature_flags but at the nav layer, not the behavior layer. Also unblocks the v1.45.0 strategic triage on Sharing / Waitlist trio: instead of binary keep-or-rip, those can ship behind opt-in flags + we validate with one or two tenants before deciding default. <strong>What this DOESN&rsquo;T ship</strong>: per-rep nav features for RefillRepHome (persona='rep' rows can be added later when a rep-side opt-in lands), per-user-not-tenant overrides (deferred until a real need), self-serve opt-in by spa owner (admin-only writes for now &mdash; matches founder-velocity-phase doctrine). <strong>Migration</strong>: <code>supabase/migrations/20260627000000_v1_44_2_nav_features.sql</code>. Paste via Supabase dashboard per [[feedback-migrations-via-dashboard]] BEFORE the deploy goes live &mdash; the listVisibleNavFeaturesForTenant call will return empty (fail-soft) until the tables exist, but cleaner to apply migration first. <strong>Touched</strong>: 2 NEW files (<code>src/server/refill-nav-features.functions.ts</code>, <code>src/routes/app.admin.nav-features.tsx</code>), 1 NEW migration, <code>src/components/refill/RefillHome.tsx</code> (visibility filter + Campaigns card + Megaphone icon + useEffect lookup), <code>src/components/admin/AdminNav.tsx</code> (+ Nav features chip), <code>src/routes/app.admin.index.tsx</code> (+ surface card + LayoutGrid icon), <code>src/lib/changelog.ts</code> (this entry). No new dependencies; no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.44.1",
    date: "June 2026",
    items: [
      "<strong>v1.44.1 &mdash; Discoverability Sweep #1: 6 orphan surfaces wired into nav + 1 dead component deleted. Pre-ship, the v1.44.0 fix exposed a systemic gap (Economics page lived as a polished orphan reachable only by direct URL since v405). A full-substrate orphan audit ran in parallel with the v1.44.0 deploy and surfaced ~5,000 LOC of built-but-unreachable features clustered around v355-v360 Promotions, v1.18/v1.22 account settings, and v1.30/v1.34 admin CRUD. This ship closes the 6 quick wins; items needing strategic calls (Campaigns tree, P4 Sharing, Waitlist trio) defer to v1.45.0 triage.</strong> <strong>What ships</strong>: (1) <code>app.admin.canonical-brands.tsx</code> (578 LOC v1.30.0 admin CRUD for system-wide canonical brand registry) wired into <code>AdminNav.tsx</code> as new &lsquo;Brands&rsquo; chip + added to <code>app.admin.index.tsx</code> ADMIN_SURFACES grid with Package icon. Also added Wishlist chip to AdminNav (was in surface grid but chipless). (2) <code>app.refill.settings.account.tsx</code> (397 LOC change-email + change-password flow from v1.18/v1.22) reachable via NEW UserCog icon link in <code>AppShell.tsx</code>'s always-on chrome — every signed-in user (spa-owner, rep, admin) gets it. (3) NEW <code>SettingsTabStrip.tsx</code> component (~55 LOC, AgentsTabStrip pattern) mounted at top of all 5 settings pages (Scheduler / Account / Sender / Spa profile / Lite mode) — pre-ship, the only way to reach Sender / Spa profile / Lite mode was direct URL (light-mode had one outbound from scheduler, the other 3 had zero). (4) <code>app.refill.appointments.tsx</code> (679 LOC source-of-truth appointment table + CSV import) surfaced as Appointments card on <code>RefillHome.tsx</code> with Calendar icon. (5) <code>app.refill.reports.tsx</code> (389 LOC Promotions Phase 9 marketing rollups) surfaced as Reports card with BarChart3 icon. (6) <code>src/components/onboarding/OnboardingWizard.tsx</code> deleted — 496 LOC of code that imported <code>./SpaWizard</code> and <code>./RepWizard</code> from siblings that don&rsquo;t exist; never mounted; pure dead-on-arrival from the Refill cleave. Directory removed. Stale RepShell.tsx docblock reference cleaned up. <strong>What this DOESN&rsquo;T ship</strong>: (a) Campaigns tree (5 files / ~1,500 LOC entire Promotions Engine Phase 1) &mdash; needs strategic call: surface as nav chip OR deprecate (Agents/Preshow/Rescue may have superseded). (b) P4 Sharing (255 LOC <code>app.refill.sharing.tsx</code> spa-grant page + 2 server fns <code>getSpaAggregatesForRep[s]</code> with zero rep-side reader UI) &mdash; needs decision: finish the rep-side OR rip the half-feature. (c) Waitlist trio (1,802 LOC invite + seed + bulk) &mdash; needs entry-point placement decision (Patients toolbar vs Agents/Rescue page vs Recognition). (d) 10 unreferenced server fns from the audit (3 Zoho integration, others trivially deletable) &mdash; defer to a separate cleanup ship to avoid touching billing/auth surfaces in a discoverability ship. <strong>Why this matters</strong>: per <code>user-dog-years-thesis</code>, every shipped surface that goes un-navved is value that decays daily at 1:20 compression. Per Grasshopper&rsquo;s 2026-06-03 sprint-vs-marathon framing: sprint cadence got us here; marathon discipline is what surfaces orphans like these BEFORE they go 6-ships stale. Pre-ship the orphan-audit was reactive (Grasshopper noticed Economics himself); post-ship the audit pattern is captured in the workflow + the discoverability sweep becomes a recurring cadence. <strong>Touched</strong>: <code>src/components/admin/AdminNav.tsx</code> (+ Wishlist + Brands chips), <code>src/routes/app.admin.index.tsx</code> (+ Canonical brands surface card + Package icon import), <code>src/components/AppShell.tsx</code> (+ Account link with UserCog icon), <code>src/components/refill/SettingsTabStrip.tsx</code> (NEW), <code>src/routes/app.refill.settings.scheduler.tsx</code> + <code>app.refill.settings.account.tsx</code> + <code>app.refill.settings.sender.tsx</code> + <code>app.refill.settings.spa-profile.tsx</code> + <code>app.refill.settings.light-mode.tsx</code> (each: + SettingsTabStrip import + mount), <code>src/components/refill/RefillHome.tsx</code> (+ Appointments + Reports cards with Calendar/BarChart3 icons), <code>src/components/shells/RepShell.tsx</code> (cleaned stale OnboardingWizard reference), DELETED <code>src/components/onboarding/OnboardingWizard.tsx</code> + directory, <code>src/lib/changelog.ts</code> (this entry). No schema change; no server fn change; no new dependencies; no wrangler.jsonc change. ~2,900 LOC of pre-existing UI made discoverable.",
    ],
  },
  {
    version: "v1.44.0",
    date: "June 2026",
    items: [
      "<strong>v1.44.0 &mdash; Rep workspace: two features that fell through the cracks finally surfaced. (a) Commission Economics page wired into the rep nav + home (was a fully-built orphan route at <code>/app/rep/economics</code> reachable only by direct URL since v405). (b) Per-send inline body edit on Outreach + Recruit (templates were read-only previews; reps now tweak subject + body in-place before send, with live post-substitution preview + Reset to default).</strong> <strong>(a) Economics nav-wire</strong>: <code>RefillRepNav</code> chip array gains a 7th &lsquo;Economics&rsquo; chip between Recruit and Network (sequence: Outreach &rarr; Recruit &rarr; Economics &rarr; Network &rarr; Commissions &rarr; Referral links &rarr; Integrations &mdash; learn-the-model sits between &lsquo;invite peers&rsquo; and &lsquo;see your downstream&rsquo;). <code>RefillRepHome</code> ACTIONS array gains a matching Economics card (TrendingUp icon, &lsquo;How the 8/3/1 split pays you&rsquo;). <code>RefillRepShellChrome.deriveActiveKey</code> recognizes the &lsquo;/economics&rsquo; path prefix. <code>RefillRepNavKey</code> union extended. No new component &mdash; the page itself has lived at <code>src/routes/app.rep.economics.tsx</code> since v405 with the SplitDiagram + worked-example table (computed from <code>lib/rep-economics.ts</code> constants per the v409 Pinch #12 hand-edit-drift safeguard). <strong>(b) Per-send inline edit</strong>: <code>sendOutreachEmail</code> server fn input schema extended with optional <code>subjectOverride: string | null</code> + <code>bodyOverride: string</code>. When provided the override text replaces the template body (still runs <code>substitutePlaceholders</code> against the same ctx so any [first name] / [my month earnings] tokens the rep types still fill at send time). subjectOverride is tri-state: undefined = template default, null = explicit no-subject, string = override. <code>template_id</code> still references the original row so analytics (open rate per template, A/B groups) keep working &mdash; the override is a per-send variation, not a fork. <strong>SendPanel UX</strong> on both <code>/app/rep/outreach</code> and <code>/app/rep/recruit</code>: subject input + 10-row monospace body textarea seeded from the template default, &lsquo;edited&rsquo; pill on the header when dirty, Reset-to-default link that wipes both overrides back to null. Below the editor: a &lsquo;Preview as recipient&rsquo; collapsible (default-open) that renders the post-substitution result so the rep sees exactly what the recipient will read, with live edits reflected in the preview as they type. Placeholders surfaced inline beneath the textarea as <code>&lt;code&gt;</code> chips so the rep knows what they can reference. Override state resets on template-selection change (per-template defaults, not cross-template carry-over). <strong>Why this matters</strong>: per [[feedback-real-world-first-then-match]] + the Kelly persona &mdash; reps need to voice the send for the specific recipient (warm intro &lsquo;hey Sarah, randi mentioned you guys&rsquo; vs cold &lsquo;quick intro&rsquo;); read-only templates structurally force the rep to either send something generic or skip the platform and use Gmail directly. The admin-only editor at <code>/app/admin/outreach</code> already had the primitive (textarea + Save) &mdash; this ship ports the per-send variant to the rep surface. <strong>Touched</strong>: <code>src/components/refill/RefillRepNav.tsx</code> (chip array + key union), <code>src/components/refill/RefillRepHome.tsx</code> (ACTIONS array + TrendingUp import), <code>src/components/refill/RefillRepShellChrome.tsx</code> (deriveActiveKey), <code>src/server/refill-outreach-send.ts</code> (sendInput schema + subjectOverride/bodyOverride handling + renderedSubject/renderedBody source resolution), <code>src/routes/app.rep.outreach.tsx</code> (override state + reset effect + SendPanel signature + inline editor block + live preview helper applyOutreachSubstitutions), <code>src/routes/app.rep.recruit.tsx</code> (override state + reset effect + SendPanel signature + inline editor block + substitute helper folded into preview useMemos), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.43.3",
    date: "June 2026",
    items: [
      "<strong>v1.43.3 &mdash; Vagaro install wizard: Rejuv-tested onboarding playbook. Per <code>project-customer-as-developer-access</code> doctrine banked this ship: Refill can&rsquo;t proxy Rejuv&rsquo;s Vagaro API key for other spas (per-merchant model), BUT we can replay Rejuv&rsquo;s proven onboarding path for every other spa &mdash; collapsing the install flow from &lsquo;figure it out + risk a denial&rsquo; to &lsquo;follow this 60-second guided playbook + 7-day wait.&rsquo;</strong> <strong>What ships</strong>: <code>src/routes/app.refill.settings.vagaro-install.tsx</code> rewritten with a 4-step guided wizard replacing the prior 2-step paste-and-go flow. <strong>Step 1 (Prerequisites)</strong>: Vagaro Pro plan (not free trial), computer/tablet/Pay Desk/PayPro version, Vagaro CC Processing active &mdash; with deep-link to <code>vagaro.com/pro/pricing</code> if signup needed. <strong>Step 2 (Submit API request)</strong>: deep-link to Vagaro&rsquo;s actual API/Webhooks Contact form &mdash; <code>form.asana.com/?k=SX3qQGg2syKV53wH-rl34w&amp;d=35883393372079</code> &mdash; the Asana-hosted form Grasshopper discovered during Rejuv onboarding 2026-06-03 (which scout extraction missed: scout described the gate structure but didn&rsquo;t extract the form URL itself). Plus a <strong>pre-written use-case template</strong> with one-tap clipboard copier: the template describes the spa&rsquo;s operational need (cancellation rebooking + waitlist outreach, ~8-12% revenue recovery) WITHOUT mentioning the Refill brand &mdash; minimizes aggregate Refill-brand signal at Vagaro&rsquo;s review queue per the stealth-posture refinement banked in <code>project-vagaro-priority-reframe</code>. Form-event checkboxes called out explicitly (Customers + Appointments + Transactions + Form Submissions). <strong>Step 3 (Wait)</strong>: sets expectations (~7 business days; Rejuv cleared in 4); explicitly states the &lsquo;minor add-on fee&rsquo; using Vagaro&rsquo;s exact wording (per Gemini transcript Grasshopper sourced) &mdash; addresses the unverified $10/mo claim by routing pricing through Vagaro Enterprise Sales rather than asserting a specific number. <strong>Step 4 (Generate key + paste)</strong>: correct path surfaced &mdash; <code>Settings &rarr; Developers &rarr; APIs &amp; Webhooks</code> (NOT the v1.38.0&rsquo;s incorrect <code>Settings &rarr; API</code>). <strong>NEW component</strong>: <code>UseCaseTemplateCopier</code> &mdash; clipboard-API + ephemeral &lsquo;Copied&rsquo; check-state for 3s + sonner toast. Per <code>feedback_copy_paste_actionables</code>: reduces friction at the Asana form to tap-once-paste-once. <strong>Why this matters strategically</strong>: every Vagaro merchant who lands on Refill&rsquo;s wizard hits a proven path Rejuv already validated. No trial-and-error. No misframed use-case field. No skipped prerequisites. Rejuv&rsquo;s real-customer status compresses the SMB onboarding flow for every subsequent Vagaro spa &mdash; the structural advantage no pure-software competitor has. <strong>What this DOESN&rsquo;T ship</strong>: Refill-side proxy of Rejuv&rsquo;s API key (architecturally impossible &mdash; Vagaro&rsquo;s per-merchant model issues keys tied to each account); per-spa platform-subdomain capture (Vagaro is single-domain so N/A); auto-population of form fields (Asana&rsquo;s form is third-party HTML; we deep-link + clipboard-template only). <strong>Touched</strong>: <code>src/routes/app.refill.settings.vagaro-install.tsx</code> (extensive: 2-step &rarr; 4-step + 3 new constants + UseCaseTemplateCopier component), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.43.2",
    date: "June 2026",
    items: [
      "<strong>v1.43.2 &mdash; Vagaro connector ground-truth from public docs scout. 3 v1.38.0 architecture-pre-build guesses corrected against verbatim Vagaro Public Developer Docs (docs.vagaro.com scout 2026-06-03). Stealth-first doctrine applied recursively: Vagaro&rsquo;s docs are FULLY PUBLIC with zero identification cost &mdash; we extracted the entire webhook envelope shape + 11-value bookingStatus enum + appointment payload field names without paying for a merchant account.</strong> <strong>Bug 1 &mdash; Webhook envelope shape</strong>: v1.38.0 expected <code>{ event, eventId/id, merchantId/businessId, occurredAt/createdAt, data }</code> based on architecture-pre-build best-guess. Reality (verbatim from docs.vagaro.com): <code>{ id, createdDate, type, action, payload }</code> where <code>type</code> is the bucket (appointment / customer / formResponse / transaction / business_location / employee) and <code>action</code> is the verb (created / updated / deleted). parseVagaroWebhookBody now combines <code>type + action</code> into the v1.38.0-style &lsquo;type.action&rsquo; event string with backward-compat fallback to legacy <code>event</code> field if present. <code>createdDate</code> replaces <code>occurredAt</code>/<code>createdAt</code>. <code>payload</code> replaces <code>data</code>. Connector accepts both new and legacy field names for forward + backward compatibility. <strong>Bug 2 &mdash; bookingStatus enum mismatch</strong>: v1.38.0 had 6 camelCase guesses (Booked / Confirmed / CheckedIn / Completed / Cancelled / NoShow). Reality is 11 verbatim values with spaces and human-readable form: Accepted / Awaiting Confirmation / Cancel / Confirmed / Deleted / Denied / Need Acceptance / No Show / Ready to Start / Service Completed / Service In Progress / Show. Status enum union expanded to include all 17 (11 verbatim + 6 legacy aliases). vagaroStatusToRefillStatus mapper rewritten to handle all 17 with intent-aware grouping: cancellation family (Cancel / Deleted / Denied + legacy Cancelled), no-show family (No Show + legacy NoShow), terminal-happy (Service Completed / Show + legacy Completed / CheckedIn), active forward-looking (Accepted / Awaiting Confirmation / Confirmed / Need Acceptance / Ready to Start / Service In Progress + legacy Booked). <strong>Bug 3 &mdash; appointment payload field names</strong>: docs surface <code>appointmentId / bookingStatus / startTime / endTime / customerId / serviceProviderId / businessId / amount / eventType / onlineVsInhouse / appointmentTypeCode / appointmentTypeName / bookingSource / calendarEventId / formResponseIds[]</code>. v1.38.0 used REST-style guesses (<code>id / status / startDateTime / staffId / serviceId / notes</code>) which may have been right for REST API responses but wrong for webhook payloads. VagaroAppointmentRaw type expanded to cover BOTH variants; rawToAppointment uses tolerant fallback chain (appointmentId &rArr; id, bookingStatus &rArr; status, startTime &rArr; startDateTime, serviceProviderId &rArr; staffId, etc.). Falls back gracefully if either form is missing. <strong>Bug 0 (signature scheme) &mdash; ALREADY CORRECT</strong>: v1.38.0 verifyVagaroWebhookSignature already used shared-token constant-time equality (NOT HMAC). Scout confirmed this is the right scheme. No change needed; v1.35.x doctrine timing-attack-defense pattern stands. <strong>Strategic implications surfaced by this scout</strong>: (1) Vagaro is the LARGEST TAM of the 6 platforms scouted today (220K+ professionals, 95%+ at 1-10 employees) and has the WORST native recovery tooling (Waitlist is notify-only, not recovery-revenue-engine) &mdash; arguably Refill&rsquo;s #1 outreach target, not the &lsquo;defer to candidate pool&rsquo; framing v1.38.0 banked. (2) The $10/mo API fee in our memory was likely conflated with Vagaro&rsquo;s $10/mo Large-Merchant CC-processing surcharge; actual API fee structure routes through Enterprise Sales and isn&rsquo;t public &mdash; treat as unverified pending first merchant. (3) docs.vagaro.com is fully public; Refill identification risk is ZERO. Scout report banked at Refill-Gated-Vendor-Stealth-Map-v1.html. <strong>What this DOESN&rsquo;T ship</strong>: REST API field-name validation (we only ground-truthed the webhook payload; the REST appointment shape may differ &mdash; defer to first real merchant onboarding). The 6 outbound IP whitelist (Vagaro publishes these only post-activation in merchant portal). Actual rate-limit ceilings (zero public docs; first real merchant will surface). project_multi_scheduler_thesis memory update (Vagaro priority elevation from &lsquo;defer&rsquo; to &lsquo;#1 outreach&rsquo;) &mdash; separate housekeeping. <strong>Touched</strong>: <code>src/lib/schedulers/vagaro.ts</code> (VagaroAppointmentStatus type expanded; vagaroStatusToRefillStatus rewritten; VagaroAppointmentRaw type expanded; rawToAppointment field-name fallback chain; parseVagaroWebhookBody envelope shape corrected), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new files; no new dependencies.",
    ],
  },
  {
    version: "v1.43.1",
    date: "June 2026",
    items: [
      "<strong>v1.43.1 &mdash; Mindbody comprehensive sandbox intel sweep route. Strategic pivot: Grasshopper&rsquo;s CTO-grade call 2026-06-03 &mdash; stay in stealth/sandbox mode with Mindbody until we&rsquo;ve fully exhausted the platform surface, so even a Go-Live DENIAL leaves us with complete competitive intel. Boulevard&rsquo;s denial (Jira DS-4790) taught the asymmetric-risk lesson: requesting Go Live identifies Refill to the vendor + lets them study us + gives them a chance to say no. The sandbox is anonymous-ish + extractive. Banked as feedback_stealth_first_doctrine memory + applies across all gated vendors (Mindbody, Booker, Jane, Zenoti &mdash; Mangomint excepted, no public surface).</strong> <strong>What lands</strong>: NEW <code>src/routes/api.integrations.mindbody.sandbox-intel-sweep.ts</code> (~245 LOC) &mdash; read-only comprehensive probe across 13 Mindbody Public API v6 endpoints + Webhooks API. Auth chain reuses the confirmed sandbox login (mindbodysandboxsite@gmail.com / Apitest1234 per v1.42.2 4-candidate probe). For each endpoint: hits with Api-Key + SiteId=-99 + Bearer header, captures status / ok / top keys / pagination shape / per-array { count, firstItemKeys, firstItemSample }. Surfaces failed endpoints + error bodies for diagnostic. <strong>Probed surface</strong>: <code>/site/sites</code> (already covered) + <code>/site/locations</code> (multi-location support) + <code>/site/sessiontypes</code> (services taxonomy &mdash; CRITICAL for treatment_type mapping) + <code>/site/programs</code> + <code>/class/classdescriptions</code> + <code>/staff/staff</code> (sampled) + <code>/client/clients</code> (sampled) + <code>/appointment/bookableitems</code> (booking-write shape probe) + <code>/sale/sales</code> + <code>/sale/services</code> + <code>/sale/packages</code> + <code>/subscriptions/eventIds</code> (LIST of all webhook event types available) + <code>/subscriptions/subscriptions</code> (LIST current webhook subscriptions on the partner key). <strong>Read-only by design</strong>: no POSTs / PUTs / DELETEs in this sweep. Sandbox writes wipe nightly but we stay invisible to whoever monitors -99 traffic by sticking to GETs. <strong>Roll-up summary</strong>: counts of working endpoints + list of failed labels for triage. <strong>Strategic anchor</strong>: per <code>feedback_stealth_first_doctrine</code> banked this ship + <code>user_dog_years_thesis</code> + <code>project_platforms_pre_built_architecture</code>: every day of stealth extraction = a day of velocity compounded into the substrate while Refill remains invisible to Mindbody. If Mindbody ever denies our Go-Live (whenever we finally submit), we still have: full endpoint catalog, response shapes, webhook event types, payload shapes, integration substrate &mdash; AKA: Lite Mode parser fully tuned + per-platform sales sheet of what we DO support + comprehensive sandbox demo &mdash; and the only thing we lose is production webhook real-time signal which Lite Mode already covers ~70% of for free. <strong>What this DOESN&rsquo;T ship</strong>: actual write tests (POST appointment / addclient / createSubscription) &mdash; deferred until we&rsquo;ve absorbed everything from reads + decided which writes we need to verify before Go-Live ever happens. Webhook subscription CREATE in sandbox (could expose Refill&rsquo;s URL pattern to Mindbody&rsquo;s aggregated logs &mdash; defer). <strong>Touched</strong>: 1 NEW route, <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; reuses existing sandbox secrets in CF (MINDBODY_SANDBOX_API_KEY).",
    ],
  },
  {
    version: "v1.43.0",
    date: "June 2026",
    items: [
      "<strong>v1.43.0 &mdash; Lite Mode Recovery loop closed end-to-end. Deep-link writeback flow ships per Refill-LightMode-Spec-v1.html &sect;4 + Refill-Followups-Plan-v1.html P2-1. When a rescue claim lands on a Lite Mode connection (emma_appointments.source matches /^lite-/), Refill composes + sends the spa owner an email with a deeplink to the relevant vendor portal + a copy-paste payload of patient + slot details. Owner taps deeplink &rarr; portal opens &rarr; books rescued-in patient manually in &lt;30 seconds. The vendor&rsquo;s own confirmation email triggered by that manual book parses through the existing Lite Mode pipeline + closes the loop automatically.</strong> <strong>This is the moat moment</strong>: Lite Mode without writeback was a half-product (one-way signal feed). With writeback, Refill&rsquo;s full Recovery loop works on platforms we have ZERO API access to. The sales demo collapses from a staged hand-wave to a single coherent narrative: 60-second Gmail-filter setup &rarr; real cancellation arrives &rarr; Refill matches + texts patient &rarr; patient taps yes &rarr; owner gets one-tap-confirm email &rarr; book lands in portal &rarr; next email parse closes the loop. <strong>What lands in this ship</strong>: (1) NEW <code>src/lib/light-mode/writeback.ts</code> (~135 LOC) &mdash; pure builders (no Supabase coupling): <code>buildClaimDeeplink(args)</code> returns per-platform { url, supportsPrefill, cta, instructions } with verified deeplink patterns where publicly known (Mindbody clients.mindbodyonline.com/classic/ws?studioid=&lt;siteId&gt;, Boulevard dashboard.joinblvd.com, Booker app.booker.com, Zenoti login.zenoti.com, Jane &lt;clinic&gt;.jane.app/admin OR jane.app/login fallback). <code>buildCopyPayload(args)</code> composes the plain-text quick-copy block with patient name / phone / email / service / time / notes, one field per line per <code>feedback_copy_paste_actionables</code>. Per <code>feedback_verify_third_party_ui</code>: only confirmed paths; no guessed prefill URLs; <code>supportsPrefill: false</code> on all platforms in v1 (future polish per-platform scout). (2) NEW <code>src/server/light-mode-writeback.functions.ts</code> (~265 LOC) &mdash; <code>dispatchLightModeWriteback({ userId, appointmentId, patientNodeId, source, originalExternalId, scheduledAt })</code>. Validates source is Lite Mode (looksLikeLightModeSource discriminator), loads owner email (tenant primary_contact_email &rarr; auth.users.email fallback), loads patient attrs (first_name / last_name / phone / email) from knowledge_nodes, loads appointment treatment_type + provider_name, resolves spa platform slug from emma_scheduler_connections if a parallel API connection exists, builds deeplink + copy payload, composes plain-text + HTML email (KAREN_FROM brand consistent with refill-drip), sends via Resend /emails with tags=[type=light-mode-writeback, platform=&lt;p&gt;]. Email subject &lsquo;\u{1F389} Rescue claimed &mdash; book &lt;Patient&gt; into &lt;slot time&gt;&rsquo; (party-popper emoji per <code>feedback_keep_it_fun</code>). HTML body uses light-bg inline-CSS card layout with emerald CTA button + monospace copy-payload block. Per <code>feedback_workers_no_fire_and_forget</code>: full await chain; Resend fetch is bounded by AbortSignal.timeout(20s). (3) MODIFIED <code>src/server/emma-rescue.functions.ts</code> <code>claimRescueSlot</code> &mdash; new Lite Mode branch lands BEFORE the existing source-name discriminator. When <code>source.startsWith(&lsquo;lite-&rsquo;)</code>: dynamic import of <code>dispatchLightModeWriteback</code> + invoke with offer/claimedApt context; stamp result.notes on emma_rescue_attempts (same fail-open pattern as the existing API writeback branches). Existing 8 API-source branches (acuity / square / boulevard / mindbody / vagaro / booker / zenoti / jane) UNCHANGED. (4) <code>src/lib/changelog.ts</code> &mdash; this entry. <strong>Strategic anchors</strong>: per <code>project_imessage_mcp_killer_arch</code> &mdash; iMessage MCP is the patient-side primitive; owner-self notification uses plain email (lower friction, no MCP setup-step on owner&rsquo;s side). Per <code>project_platforms_pre_built_architecture</code> &mdash; same doctrine applied recursively: the deeplink substrate ships universal across all 5 platforms now; per-platform prefill-URL refinements happen as we surface them via pilot tenants. Per <code>user_dog_years_thesis</code> &mdash; one engineering session (~30 min) unlocks Recovery loop on 5 platforms simultaneously, no vendor approval needed. <strong>What this DOESN&rsquo;T ship</strong>: per-spa Zenoti / Jane subdomain capture at Lite Mode wizard time (deeplink falls back to generic landing pages on those two; future polish ship); query-param-prefill URLs (need per-platform scout of actual portal URL formats); SMS-to-owner option (email is the v1 channel); owner-side iMessage MCP variant (deferred until a spa specifically asks). <strong>Touched</strong>: 2 NEW files (<code>src/lib/light-mode/writeback.ts</code>, <code>src/server/light-mode-writeback.functions.ts</code>), 1 EXTENDED file (<code>src/server/emma-rescue.functions.ts</code> &mdash; light-mode branch in claimRescueSlot), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.42.4",
    date: "June 2026",
    items: [
      "<strong>v1.42.4 &mdash; Mindbody sandbox-verify smoke walks all staff in batches to surface real Appointment object shape. Closes the &lsquo;what doesn&rsquo;t ship&rsquo; gap from v1.42.3: connector Raw field-name verification (Id vs AppointmentId, Status enum strings, NoShow bool, ClientId, SessionTypeId, StaffId, StartDateTime fields). The v1.35.x-debug-arc loop continues &mdash; sandbox iteration finds gaps, fix lands, re-verify.</strong> <strong>What changed</strong>: <code>src/routes/api.integrations.mindbody.sandbox-verify.ts</code> Step 4 replaced with a staff-walk pattern. Pulls all StaffMember IDs from Step 3&rsquo;s <code>/staff/staff</code> response (100 of 141 staff returned on first page), batches them in groups of 20, calls <code>/appointment/staffappointments</code> per batch with widened <strong>1-year-back-to-1-year-forward window</strong> (vs prior 30d-back/90d-forward) to catch any test bookings in sandbox -99 regardless of when they were created. Stops on first batch returning non-empty Appointments[]; dumps the first 2 appointment objects verbatim + lists their full key set + reports the PaginationResponse for that batch. <strong>What this surfaces</strong>: when smoke finds appointments, we know the actual Raw field names Mindbody uses; if no batch returns appointments after walking all 141 staff with the 2-year window, we know sandbox -99 truly has no appointments seeded (write a sandbox booking to seed, then re-run; or wait for production Karen-Rejuv data). Either outcome is useful debug-arc signal. <strong>Per-batch reporting</strong>: each batch summary contains batchIndex, staffIdsRange (first..last), staffIdsCount, status, ok, apptCount, totalResults &mdash; even empty batches yield diagnostic data so we can see exactly where in the staff list (if anywhere) appointments lived. <strong>Connector code unchanged</strong>: v1.42.3 fixes already shipped (listMindbodyAppointments + getMindbodyAppointment + listMindbodyStaff). This smoke ship is pure diagnostic &mdash; we&rsquo;re hitting the endpoint directly via fetch + walking staff to find data, then comparing shape against rawToAppointment&rsquo;s field-name assumptions. <strong>Touched</strong>: <code>src/routes/api.integrations.mindbody.sandbox-verify.ts</code> (Step 4 replaced &mdash; removed 4-endpoint candidate probe since staffappointments was definitively right; added staff-walk loop with per-batch metrics + raw-shape dump on first hit), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new files; no new dependencies. <strong>Strategic anchor</strong>: per <code>feedback-validate-against-real-exports</code> + v1.35.x-debug-arc-doctrine, &lsquo;synthetic guesses are NOT validation; only real provider responses are.&rsquo; This walk either confirms the connector&rsquo;s rawToAppointment assumptions OR surfaces the corrections needed before Karen-Rejuv production data lands. Cheap to run (140 staff / 20 batch = 7 calls max; each call < 500ms); high information density.",
    ],
  },
  {
    version: "v1.42.3",
    date: "June 2026",
    items: [
      "<strong>v1.42.3 &mdash; Mindbody connector bug fixes from v1.42.2 sandbox-verify smoke. Three concrete v1.37.0 guesses ground-truthed against real Mindbody Public API v6 responses and corrected. The v1.35.x-debug-arc playbook applied cleanly to Mindbody &mdash; sandbox surfaces gap, ship fixes the gap, next iteration validates.</strong> <strong>Bug 1 (silent zero-results regression)</strong>: <code>listMindbodyAppointments</code> assumed response top-level key was <code>StaffAppointments</code>; the actual Mindbody v6 response from <code>/appointment/staffappointments</code> returns <code>Appointments</code> at the top level. Result: every prior call would have returned an empty array even when real appointments existed. Fix: rename Raw type field + access path to <code>Appointments</code>. <strong>Bug 2 (required parameter missing)</strong>: <code>/appointment/staffappointments</code> requires <code>StaffIds=&lt;id&gt;</code> query parameter scoping &mdash; v1.37.0 didn&rsquo;t pass it. Without StaffIds the endpoint still returns 200 but with zero results (silent fail). Fix: <code>staffIds</code> is now a REQUIRED arg on <code>listMindbodyAppointments</code>, serialized as repeated <code>StaffIds=1&amp;StaffIds=2</code> query params via <code>URLSearchParams.append</code> (NOT comma-joined which URLSearchParams percent-encodes). <strong>Bug 3 (wrong endpoint URL for single-fetch)</strong>: <code>getMindbodyAppointment</code> hit <code>/appointment/appointments?AppointmentIds=X</code> which Mindbody returns 404 HTML for &mdash; that endpoint doesn&rsquo;t exist. Fix: route through <code>/appointment/staffappointments?AppointmentIds=X&amp;StaffIds=...</code> + filter the returned Appointments array client-side for the matching ID. <strong>Bug 4 (case-sensitive pagination params)</strong>: query string used lowercase <code>limit</code>+<code>offset</code>; Mindbody&rsquo;s server treats lowercase as unknown and defaults to <code>Limit=100</code>. Fix: capital <code>Limit</code>+<code>Offset</code>. <strong>NEW: <code>listMindbodyStaff</code> connector function</strong> (~30 LOC). Mirrors <code>listMindbodyClients</code> shape; calls <code>/staff/staff</code> which is the canonical Mindbody v6 staff list endpoint (confirmed via sandbox smoke 2026-06-03 returning 141 staff in test site -99). Used by the refactored backfill to enumerate staff before iterating appointment list per-staff. <strong>Refactored <code>backfillMindbodyAppointments</code></strong> (server/emma-scheduler.functions.ts): two-level pagination &mdash; outer loop walks staff in batches of 20 (Mindbody accepts repeated StaffIds params, so we can query 20 staff per appointment call), inner loop paginates appointments per batch with the existing 50-page hard cap. Staff pagination has its own 20-page hard cap (~4,000 staff ceiling, well above any realistic spa). <strong>Caller-surface compatibility</strong>: <code>getMindbodyAppointment</code> made <code>staffIds</code> OPTIONAL with internal auto-resolve fallback. When omitted (typical for rescue-flow callers which only know the appointment ID), the function fetches the staff roster via <code>listMindbodyStaff</code> internally and passes all staff IDs as the StaffIds query. This keeps the emma-rescue.functions.ts call site at <code>claimRescueSlot</code> unchanged &mdash; no need to plumb staff context through the rescue path just to fix this Mindbody endpoint quirk. Slightly heavier per single-fetch call (one extra paginated staff list) but acceptable for a low-frequency operation. <strong>Sandbox-verify validation chain confirmed</strong>: Auth (Api-Key + Authorization Bearer + SiteId triple-header) &mdash; works as v1.37.0 wired. Pagination shape (<code>PaginationResponse: { RequestedLimit, RequestedOffset, PageSize, TotalResults }</code>) &mdash; matches v1.37.0 assumption. Site / Staff / Client endpoint URLs and shapes &mdash; match v1.37.0 assumptions (1 site, 141 staff, 4514 clients in sandbox -99). <strong>What this DOESN&rsquo;T ship</strong>: the actual MindbodyAppointment Raw field-name verification (NoShow / Status / StartDateTime / ClientId / SessionTypeId / StaffId etc.) &mdash; first sandbox staff queried had 0 appointments in window; getting real shape verification deferred to (a) iterating across more staff to find one with appointments OR (b) production OAuth + first Karen-Rejuv MB data. The connector code currently maps each raw field tolerantly to both Id and AppointmentId so missing/extra fields fail soft. <strong>Touched</strong>: <code>src/lib/schedulers/mindbody.ts</code> (3 fn updates: listMindbodyAppointments + getMindbodyAppointment + NEW listMindbodyStaff), <code>src/server/emma-scheduler.functions.ts</code> (import listMindbodyStaff + refactor backfillMindbodyAppointments to enumerate staff + iterate per batch), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no wrangler.jsonc change. <code>src/server/emma-rescue.functions.ts</code> <em>not</em> touched &mdash; getMindbodyAppointment&rsquo;s caller-surface change is backwards-compatible (staffIds optional). <strong>Strategic anchor</strong>: per <code>feedback-validate-against-real-exports</code> + v1.35.x-debug-arc-doctrine, this is the canonical &lsquo;sandbox surfaces gap &rarr; fix &rarr; re-verify&rsquo; loop. Two more loops likely needed before Mindbody connector is production-ready: (1) real Appointment shape verification when we find/book sandbox test appointments OR get production OAuth; (2) full webhook payload shape verification when first Mindbody webhook fires. Both gated on credentials we still need (sandbox test data + production OAuth client from Go-Live ticket). Doctrine compounding: every Mindbody bug fixed now is one less Karen-Rejuv go-live bug later.",
    ],
  },
  {
    version: "v1.42.2",
    date: "June 2026",
    items: [
      "<strong>v1.42.2 &mdash; Mindbody sandbox-verify smoke route. The v1.35.x-debug-arc unlock for Mindbody: Grasshopper just discovered the free Mindbody sandbox at developers.mindbodyonline.com (Studio ID -99, nightly refresh, no Support-ticket gate). Sandbox API Key + Source Password landed in CF (MINDBODY_SANDBOX_API_KEY + MINDBODY_SANDBOX_SOURCE_PASSWORD via the wrangler-secret-routine). NEW route <code>/api/integrations/mindbody/sandbox-verify</code> issues a UserToken via TokenIssue (staff creds Siteowner/apitest1234) + runs 4 follow-on reads (sites / staff / appointments / clients) and returns the RAW response shape from every step.</strong> <strong>What this unblocks</strong>: every guess in v1.37.0 connector code &mdash; mindbodyFetch triple-header (Api-Key + Authorization + SiteId), MindbodyAppointment field names, listMindbodyAppointments pagination shape (PaginationResponse.TotalResults?), client search filters, status mapper coverage (NoShow priority over Status field?), error response shape on auth failure &mdash; gets ground-truthed against actual Mindbody response payloads. Whatever the smoke surfaces, that&rsquo;s the queue for v1.42.3+ connector tightenings. Per <code>feedback-validate-against-real-exports</code>: synthetic guesses are NOT validation; only real provider responses are. <strong>Scout-confirmed auth chain</strong> (per a4a953fcb scout report 2026-06-03): Step 1 POST <code>/public/v6/usertoken/issue</code> with Api-Key + SiteId=-99 + body <code>{Username: &lsquo;Siteowner&rsquo;, Password: &lsquo;apitest1234&rsquo;}</code> returns Bearer AccessToken. Step 2+ GET any v6 endpoint with Api-Key + SiteId + Authorization: Bearer triple-header. Sandbox refreshes nightly at midnight PST &mdash; reads are stable, writes wipe. <strong>v1.35.x doctrine applied from minute one</strong>: defensive whitespace strip on both env vars at read site (v1.35.4), credential length logging in response payload so type-confusion surfaces (v1.35.4), per-step error capture with full Mindbody error text bubbled into the response (v1.35.4 surface-everything pattern), fail-soft early-return when AccessToken absent (v1.35.6 fail-degrade), defensive try/catch around each fetch so step 4 failing doesn&rsquo;t block step 5&rsquo;s diagnostic. <strong>Strategic implication</strong>: Mindbody timeline collapses from &lsquo;weeks waiting on Support ticket for OAuth client provisioning&rsquo; to &lsquo;hours to sandbox-verified connector code&rsquo; for the API surface (response shapes, pagination, status mapping, error handling). Production-ready OAuth flow still needs the Request-to-Go-Live ticket, but that&rsquo;s now a parallel track NOT a serial dependency. <strong>What this DOESN&rsquo;T ship</strong>: production flag flip (REFILL_MINDBODY_ENABLED stays 0); end-to-end OAuth flow exercise (sandbox doesn&rsquo;t use the OAuth client path our v1.37.0 connector code goes through &mdash; it uses staff-credential TokenIssue, which is a different auth surface than the 3-legged user-OAuth our production connector targets &mdash; both authenticate against the SAME /public/v6 endpoints, so the API-surface validation transfers cleanly even though the auth-surface validation needs the Go-Live OAuth credentials). <strong>How to invoke</strong>: visit <code>https://refill-app.davidfand303.workers.dev/api/integrations/mindbody/sandbox-verify</code> (GET, no auth) and inspect the returned JSON. Will be pulled before any production ship. <strong>Touched</strong>: 1 NEW file (<code>src/routes/api.integrations.mindbody.sandbox-verify.ts</code>, ~210 LOC), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no wrangler.jsonc change (uses existing secrets).",
    ],
  },
  {
    version: "v1.42.1",
    date: "June 2026",
    items: [
      "<strong>v1.42.1 &mdash; Lite Mode polish + observability bundle: D-2 (types regen) + D-7 (Gmail forwarding auto-confirm) + P2-3 (silence-detection cron). Three non-blocking follow-ups from Refill-Followups-Plan-v1.html closed while Grasshopper is OOO.</strong> <strong>D-2 &mdash; supabase types regen</strong>: ran <code>npx supabase gen types typescript --project-id pxgzbibcmbxxqycuplga</code> against the live Supabase project (now that v1.42.0 migration has been applied), regenerated <code>src/integrations/supabase/types.ts</code> with full typings for <code>emma_light_mode_connections</code> + <code>emma_email_quarantine</code>. <code>as never</code> casts in <code>light-mode.functions.ts</code> and <code>api.resend.inbound-lite.ts</code> stay in place (consistent with the wishlist + canonical_brands pattern; works against properly-typed Database now) &mdash; future ships can opt into stricter typing per-fn without one big refactor. <strong>D-7 &mdash; Gmail forwarding-address verification auto-confirm</strong>: when the spa owner sets up the Gmail filter and enters our per-spa inbound address as a forwarding destination, Gmail first sends a verification email to that address. Without intervention the owner has to dig that mail out of their forwarded queue + manually click the confirm link &mdash; awkward UX for a 60-second onboarding promise. v1.42.1 intercepts the verification email at the Resend inbound webhook + extracts the confirm URL + surfaces it as a one-tap Confirm button in the Lite Mode wizard. <strong>How it works</strong>: (1) <code>src/routes/api.resend.inbound-lite.ts</code> &mdash; new <code>looksLikeGmailVerification(env)</code> guard (matches from-address <code>forwarding-noreply@google.com</code>/<code>noreply@google.com</code>/<code>mail-noreply@google.com</code> OR subject regex for &lsquo;Gmail Forwarding Confirmation&rsquo; / &lsquo;Confirm forwarding&rsquo; / &lsquo;Confirmation code&rsquo;) runs BEFORE platform parser dispatch. When match: <code>extractGmailConfirmUrl(env)</code> regex-pulls the <code>https://mail.google.com/mail/...ConfirmForward...</code> URL from raw_html OR raw_text. Insert quarantine row with <code>reason=&lsquo;gmail_verification&rsquo;</code> + <code>reviewed_notes=&lt;URL&gt;</code>; bump connection.last_event_at so wizard shows progress. (2) NEW <code>getPendingGmailVerification</code> server fn in <code>src/server/light-mode.functions.ts</code> &mdash; returns most recent unreviewed gmail_verification row for current spa+platform. (3) <code>src/routes/app.refill.settings.light-mode.tsx</code> wizard &mdash; polls every 15s while connection is not yet active. When a pending verification exists, renders an amber alert card above the Gmail-filter step with a prominent &ldquo;Confirm Gmail forwarding&rdquo; emerald CTA button linking to the extracted URL (target=_blank). If URL extraction failed for some reason, falls back to a plain &ldquo;open your forwarded mail and click the link&rdquo; instruction. Per <code>feedback_setup_wizards_auto_advance</code>: deep-link to the exact action, no instructions to read. <strong>Per <code>feedback_validate_against_real_exports</code>:</strong> the from-address + subject regex is best-guess against Gmail&rsquo;s typical forwarding-confirmation shape; first real pilot will confirm exact strings. If pattern doesn&rsquo;t match, the email falls through to normal quarantine + admin review &mdash; safe fail-soft. <strong>Touched</strong>: <code>src/integrations/supabase/types.ts</code> (regenerated, 3898 lines), <code>src/routes/api.resend.inbound-lite.ts</code> (D-7 detection branch + 3 helpers), <code>src/server/light-mode.functions.ts</code> (NEW <code>getPendingGmailVerification</code> server fn), <code>src/routes/app.refill.settings.light-mode.tsx</code> (D-7 verification state + 15s polling + amber alert card with Confirm CTA), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new files; no new dependencies. Reuses existing emma_email_quarantine table (the <code>reviewed_notes</code> TEXT column is hijacked for the extracted URL until a dedicated <code>extracted_data</code> JSONB column lands in a future ship). <strong>P2-3 &mdash; silence-detection cron</strong>: NEW <code>src/routes/api.cron.light-mode-silence.ts</code> (~115 LOC). Hourly sweep over <code>emma_light_mode_connections</code> WHERE status=&lsquo;active&rsquo; AND last_event_at &lt; NOW &mdash; 24h; flips matching rows to status=&lsquo;silent&rsquo; with last_error explaining what likely broke (Gmail filter / label / forwarding disabled / vendor template change collapsing all events into quarantine). Settings wizard&rsquo;s status chip updated with explicit handling for &lsquo;silent&rsquo; (destructive-red &ldquo;Silent &mdash; no events in 24h. Re-check Gmail forwarding.&rdquo;) and &lsquo;paused&rsquo; (muted grey) alongside the existing &lsquo;active&rsquo; emerald + &lsquo;pending&rsquo; amber states. Cron callable via both GET (manual testing / first sweep) and POST (Cloudflare cron trigger). <strong>Architecture-stub</strong>: cron trigger config in wrangler.jsonc is held back until Lite Mode has its first pilot tenant &mdash; firing a cron with zero connections to sweep is pure waste. Add <code>triggers.crons</code> entry once Zenoti pilot lands. The route exists + works when called manually today. <strong>Touched</strong> across all three: <code>src/integrations/supabase/types.ts</code> (D-2: regenerated, 3898 lines), <code>src/routes/api.resend.inbound-lite.ts</code> (D-7 detection branch + 3 helpers), <code>src/server/light-mode.functions.ts</code> (D-7 NEW <code>getPendingGmailVerification</code>), <code>src/routes/app.refill.settings.light-mode.tsx</code> (D-7 + P2-3 UX: verification state + 15s polling + amber alert card with Confirm CTA + status chip handling for &lsquo;silent&rsquo;/&lsquo;paused&rsquo;), <code>src/routes/api.cron.light-mode-silence.ts</code> (P2-3 NEW), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new migrations; no new dependencies. Reuses existing emma_email_quarantine table for D-7 (<code>reviewed_notes</code> TEXT column hijacked for extracted URL until a dedicated <code>extracted_data</code> JSONB column lands in a future ship). <strong>Strategic anchor</strong>: closes friction points that would have shown up at the first pilot enrollment. Lite Mode promises 60-second setup; manually hunting the Gmail confirmation email would have broken that promise. Silence detection turns a black-hole failure mode into an actionable owner-facing alert. Per <code>user-dog-years-thesis</code> &mdash; remove friction BEFORE the customer feels it. <strong>What still waits on Grasshopper</strong> (per Refill-Followups-Plan-v1.html): MX record on <code>inbound.getrefill.app</code> + Resend Inbound Route (P0-2 + P0-3); Mangomint partnership email send (E-1 draft at Refill-Mangomint-Outreach-Email-v1.html on Desktop, ready to review and send).",
    ],
  },
  {
    version: "v1.42.0",
    date: "June 2026",
    items: [
      "<strong>v1.42.0 &mdash; Lite Mode + Plaid Mode launch. Two new connector tiers ship alongside the existing API Mode, giving every spa on a gated platform (Boulevard / Mindbody / Booker / Jane / Zenoti) an immediate path to Refill without waiting on vendor approval.</strong> <strong>The thesis</strong>: every gated scheduler ALREADY emails the spa owner on booking / cancellation / no-show events. If the owner sets a Gmail filter that forwards those notifications to a per-spa Refill inbound address, Refill receives a continuous event feed without ever touching the vendor&rsquo;s API or portal. 100% ToS-clean (owner is forwarding their OWN mail). Coverage estimate vs full API access: ~70% of Recovery + Recognition value for $0 vendor friction and 60s setup. <strong>Briefs</strong>: Refill-LightMode-Spec-v1.html + Refill-PlaidMode-BackPocket-v1.html + Refill-PolyAPI-Workaround-Research-v1.html (all on Desktop). <strong>Three tiers of the same engine</strong>: API Mode (live: Acuity/Square/Vagaro; pre-built: Boulevard/Mindbody/Booker/Jane/Zenoti) &middot; Lite Mode (NEW &mdash; universal across all 5 gated platforms, no approval gate) &middot; Plaid Mode (NEW &mdash; browser-automation-under-owner-credentials, gray-zone, opt-in for design partners under admin gate). <strong>What lands in this ship</strong>: (1) NEW migration <code>supabase/migrations/20260626000000_v1_42_0_light_mode.sql</code> &mdash; two tables: <code>emma_light_mode_connections</code> (per-spa per-platform enrollment row with auto-generated inbound_slug hex via gen_random_bytes(8), status enum pending/active/paused/silent, last_event_at + events_parsed_total + events_quarantined_total counters, RLS owner+service-role) and <code>emma_email_quarantine</code> (append-only queue of inbound emails the parser couldn&rsquo;t classify; holds full raw envelope so retro-parsing remains possible after parser fixes; review workflow with reviewed_outcome enum ignored/reparsed/parser_fix_filed/spam). Both tables NOTIFY pgrst at the end per migration-dashboard SOP. (2) NEW <code>src/lib/email-parsers/index.ts</code> (~165 LOC) &mdash; ParseResult / ParseInput / ParsedAppointment types, platform dispatch with try/catch error funnel, stripHtml + emailBodyText helpers tolerant of HTML entities (&rsquo; &nbsp; &mdash; etc.) and inline styles/scripts. (3) NEW <code>src/lib/email-parsers/zenoti.ts</code> (~165 LOC) &mdash; LIVE parser for v1.42.0 launch. Keys on <code>noreply@zenoti.com</code> default sender (confirmed via Zenoti docs) with Enterprise custom-From fallback via body-marker detection. Subject matchers for booking.created / booking.cancelled / booking.rescheduled / booking.no_show / client.created. Body-field extractors tolerant of Zenoti macro substitution patterns ([GuestFirstName], [ServiceName], etc.) producing label:value pairs &mdash; pulls patient name / phone / email, service, staff, datetime (Date.parse + Zenoti template variant &lsquo;Jun 5, 2026 at 3:00 PM&rsquo; normalization), location, external ID. Confidence scoring base + per-field bonus, capped at 0.95. (4) NEW <code>src/lib/email-parsers/jane.ts</code> + <code>boulevard.ts</code> + <code>mindbody.ts</code> + <code>booker.ts</code> (~40-50 LOC each) &mdash; architecture-stub parsers per the platforms-pre-built doctrine. Each recognizes likely-shaped emails (body markers like joinblvd.com / mindbodyonline.com / booker.com / jane.app) and quarantines with a distinct reason tag so the v1.42.x follow-up parser activations target them precisely after pilot tenants forward real exemplars. Per <code>feedback-validate-against-real-exports</code>: parsers are NOT shipped until ≥1 real-world exemplar per (platform x event) validates them. Estimate ~25 exemplars to lock the full matrix; Jane batches up to N appointments per email on a ~3-min window so its parser specifically must split multi-event payloads (architecture noted in stub). (5) NEW <code>src/routes/api.resend.inbound-lite.ts</code> (~270 LOC) &mdash; Resend Inbound Parse webhook handler. Separate endpoint from the existing reply-inbound at /api/resend/inbound so platform parsers don&rsquo;t compete with reply parsing. Extracts inbound slug from the to-address local-part (supports both <code>@inbound.getrefill.app</code> and <code>@inbound.refill.app</code>), looks up the matching connection row, dispatches through per-platform parser, upserts emma_appointments on success (firing the existing rescue + recognition trigger graph via status=&lsquo;cancelled&rsquo;/&lsquo;no_show&rsquo;/&lsquo;scheduled&rsquo;), quarantines on unknown / no-match with full raw envelope retained. Bumps connection counters + flips status pending&rarr;active on first parse. Always returns 200 to prevent Resend retry storms; failures stamp the quarantine row. Synthesized external_id pattern <code>lite:&lt;platform&gt;:&lt;message_id&gt;:&lt;index&gt;</code> when parser can&rsquo;t extract a vendor-stable ID. (6) NEW <code>src/server/light-mode.functions.ts</code> (~340 LOC) &mdash; five server fns following the wishlist + canonical_brands service-role-with-manual-scoping pattern: <code>listLightModeConnections</code>, <code>enableLightMode</code> (per-platform, generates inbound_slug via migration default OR reactivates existing row), <code>disableLightMode</code> (pause, keeps slug so re-enable hits same address &mdash; owner&rsquo;s Gmail filter stays valid), <code>listLightModeQuarantine</code>, and <strong><code>getSchedulerExtras</code></strong> (the per-tenant flag check: lightMode always true; plaidMode reads <code>feature_flags WHERE flag_key=&lsquo;plaid_mode_enabled&rsquo; AND scope=&lsquo;tenant&rsquo; AND scope_id=&lt;tenant_id&gt;</code>). All with resolveEffectiveUserId + service-role client + manual user_id scoping. Types.ts not yet regenerated for the new tables so all <code>.from()</code> calls use <code>as never</code> casts at the supabase boundary per the wishlist pattern. (7) NEW <code>src/routes/app.refill.settings.light-mode.tsx</code> (~300 LOC) &mdash; onboarding wizard at sibling path (NOT nested under /scheduler) to avoid <code>feedback_tanstack_outlet_trap</code>. 3-step UX: platform picker (driven by ?platform=zenoti query param when entering from a scheduler-page card; otherwise interactive picker with 5 platform options + filter-from-hint chip), generate-inbound-address with Copy button + clipboard-API + success toast, Gmail filter walkthrough with deep-link CTA to <code>mail.google.com/mail/u/0/#search/from%3A</code> + 6-step ordered list with platform-specific From-address hint, verification step showing &ldquo;Waiting for first event&rdquo; amber chip vs &ldquo;Lite Mode active &middot; last event &lt;timestamp&gt;&rdquo; emerald chip + manual refresh + counter telemetry (events parsed / quarantined / last_error as warning). Per <code>feedback_setup_wizards_auto_advance</code>: deep-links not instructions, zero-friction setup. (8) <code>src/lib/feature-flag-catalog.ts</code> &mdash; TWO new catalog entries: <code>light_mode_enabled</code> (defaultEnabled=false; admin opt-in per spa even though feature is universally safe &mdash; lets admin stagger rollouts) and <strong><code>plaid_mode_enabled</code></strong> (defaultEnabled=false; the per-tenant admin gate Grasshopper requested for browser-automation availability). Catalog-driven admin UI at /app/admin/agents picks both up automatically; admin toggles per tenant via the existing override-add flow with scope=tenant scope picker. (9) <code>src/routes/app.refill.settings.scheduler.tsx</code> &mdash; SchedulerSettingsPage now loads <code>schedulerExtras</code> via getSchedulerExtras on mount (soft-fail to defaults if the call errors; never blocks the page). Passes <code>schedulerExtras</code> + <code>onConnectLightMode</code> + <code>onConnectPlaid</code> callbacks down to AvailablePlatforms. New <code>&lt;PlatformExtrasRow&gt;</code> helper component renders a thin link row INSIDE each of the 5 gated-platform cards (Boulevard / Mindbody / Booker / Jane / Zenoti) beneath the description: &ldquo;&#x26A1; Connect Lite Mode (no approval needed) &rarr;&rdquo; in emerald (always visible when extras.lightMode=true) + &ldquo;&#x1F510; Plaid Connect (Pro) &rarr;&rdquo; in fuchsia (only when extras.plaidMode=true per admin flag). LightMode link routes to /app/refill/settings/light-mode?platform=&lt;platform&gt; pre-selecting the picker; Plaid link surfaces a concierge-setup toast (v1.42.0 ships the surface; the actual browser-automation backend is a follow-up ship gated on first design-partner opt-in per recursive doctrine). platformLabel switch extended to handle &lsquo;zenoti&rsquo;/&lsquo;booker&rsquo;/&lsquo;vagaro&rsquo; for cleaner display strings. (10) <code>src/lib/changelog.ts</code> &mdash; this entry. <strong>Architecture-stub items pending pilot exemplars</strong>: Jane / Boulevard / Mindbody / Booker parser activation each &mdash; stubs return unknown with platform-tagged reason so pilot tenant exemplars route directly to the right parser&rsquo;s tuning queue; Resend Inbound Route DNS+dashboard config (inbound.getrefill.app MX + Resend Route POSTing to /api/resend/inbound-lite); per-spa Resend Inbound auto-confirmation of Gmail&rsquo;s forwarding-address verification email (currently this fires the verification email at the inbound address but we don&rsquo;t parse + ack &mdash; in v1.42.x we add the confirmation flow); deep-link writeback flow (the owner-confirmed claim-the-slot via portal deep-link from Refill-LightMode-Spec-v1.html section 4 &mdash; v1.43.0 ship); backfill-from-past-emails bulk-forward flow (v1.43.1 ship); admin quarantine review surface (rows already write to emma_email_quarantine; admin UI lives in /app/admin/wishlist-shaped page in next ship). <strong>What this DOESN&rsquo;T ship</strong>: actual Plaid Mode backend (browser-automation, credential vault, per-platform Puppeteer/Playwright scripts &mdash; the brief estimates 8-12 days of engineering for the v1; v1.42.0 ships only the admin/UI surface to enable the per-spa demo posture Grasshopper called out: &lsquo;we have a Plaid-style connector ready to deploy for partners who can&rsquo;t wait on Enterprise approval&rsquo;); cron-driven Lite Mode silence detection (status=&lsquo;silent&rsquo; flip after &gt;24h since last_event_at when one was expected); per-spa-events admin dashboard (rows are queryable but no dedicated surface yet). <strong>Required to flip Lite Mode fully live in production</strong>: MX record on <code>inbound.getrefill.app</code> pointing at Resend&rsquo;s inbound MX + Inbound Route in Resend dashboard catching <code>*@inbound.getrefill.app</code> and POSTing to https://app.getrefill.app/api/resend/inbound-lite. Once those land, Zenoti Lite Mode is ready for first pilot tenant. <strong>Strategic anchor</strong>: per <code>project-platforms-pre-built-architecture</code> + <code>user-dog-years-thesis</code>, Lite Mode collapses 5 vendor-approval timelines into 1 universal owner-side setup. Refill can sell + onboard Boulevard / Mindbody / Booker / Jane / Zenoti spas this week, not when vendor gates clear. Sales credibility lift: &ldquo;we don&rsquo;t need approval from your scheduler &mdash; we run on your existing email notifications, opt-in in 60 seconds.&rdquo; Plaid Mode admin surface + per-spa toggle lets Grasshopper demo the Pro tier to design-partner prospects today, with the actual backend gated on real demand per recursive doctrine. <strong>Touched</strong>: 1 NEW migration (20260626000000_v1_42_0_light_mode.sql), 5 NEW email parsers (email-parsers/{index,zenoti,jane,boulevard,mindbody,booker}.ts), 1 NEW route (api.resend.inbound-lite.ts), 1 NEW server fn module (light-mode.functions.ts), 1 NEW wizard route (app.refill.settings.light-mode.tsx), 2 EXTENDED files (feature-flag-catalog.ts adds 2 entries, app.refill.settings.scheduler.tsx adds schedulerExtras loading + PlatformExtrasRow helper + 5 card insertions + platformLabel extension), 1 changelog entry. No new dependencies. No wrangler.jsonc change &mdash; relies on existing RESEND_API_KEY secret + Supabase service-role; Resend Inbound Route is configured in Resend dashboard, not code.",
    ],
  },
  {
    version: "v1.41.0.1",
    date: "June 2026",
    items: [
      "<strong>v1.41.0.1 &mdash; UX polish: Vagaro card moved up under Square on the scheduler settings page.</strong> Live-connectable platforms (Acuity / Square / Vagaro) now grouped at the top; architecture-ready-pending platforms (Boulevard / Mindbody / Zenoti / Jane / Booker) group below with their amber pending badges. Mental model: green-button = live now, amber-badge = waiting on credentials. <strong>Touched</strong>: <code>src/routes/app.refill.settings.scheduler.tsx</code> (Vagaro card block relocated from end of AvailablePlatforms list to immediately after Square card), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new files; no behavior change &mdash; pure ordering tweak.",
    ],
  },
  {
    version: "v1.41.0",
    date: "June 2026",
    items: [
      "<strong>v1.41.0 &mdash; Zenoti architecture pre-build. Fifth execution of the platforms-pre-built doctrine, closing out the multi-scheduler integration sprint with the enterprise-tier ceiling. Zenoti runs the high end of the wellness/spa market (large multi-location chains); shipping architecture-ready here = direct credibility lift in enterprise sales conversations alongside Boulevard / MB.</strong> Divergences absorbed: API key + JWT auth (NOT user-consent OAuth — Zenoti is app-to-app), multi-center via center_id path parameter scoping, dual-gate API Groups + user roles authorization model, offset (page/size) pagination, CSM-led API package subscription gate. Files: src/lib/schedulers/zenoti.ts (NEW ~520 LOC connector), src/server/emma-scheduler.functions.ts (initiateZenotiConnect server fn + disconnect/resync branches + backfillZenotiAppointments helper + zenotiAppointmentToRow), src/routes/api.integrations.zenoti.install-callback.ts (NEW ~190 LOC, paste-center_id wizard validator), src/routes/api.webhooks.scheduler.zenoti.$secret.ts (NEW ~280 LOC, per-spa path secret + HMAC scaffold), src/routes/app.refill.settings.zenoti-install.tsx (NEW ~150 LOC, paste-center_id wizard), src/server/emma-rescue.functions.ts (Zenoti branch in claimRescueSlot), src/routes/app.refill.settings.scheduler.tsx (Zenoti card with CSM-onboarding-pending badge). Credentials needed when CSM activates: ZENOTI_API_KEY (or ZENOTI_SANDBOX_API_KEY). Switch: REFILL_ZENOTI_ENABLED=1 in wrangler.jsonc vars + redeploy.",
    ],
  },
  {
    version: "v1.40.0",
    date: "June 2026",
    items: [
      "<strong>v1.40.0 &mdash; Jane architecture pre-build. Fourth execution of the doctrine with the biggest new architectural axis yet: polling-only sync (Jane is the first scheduler with NO webhook support).</strong> Divergences absorbed: OAuth 2.0 + PKCE mandatory (code_verifier embedded in state token for round-trip), 5-minute access token TTL (aggressive refresh ~30s before expiry), per-clinic base URLs (each Jane clinic has its own subdomain — tenant routing via clinic_url instead of UUID/site_id), polling-based sync via a new cron route pattern, partnership-approval gate, date-versioned API (/api/2025-02-28-beta/), patient terminology (not client). Files: src/lib/schedulers/jane.ts (NEW ~530 LOC connector including generateJanePkcePair helper + 5-min refresh window), src/server/emma-scheduler.functions.ts (initiateJaneOAuth with PKCE-in-state + JaneOAuthState type + backfillJaneAppointments + janeAppointmentToRow + withFreshJaneToken with 30s refresh window), src/routes/api.integrations.jane.oauth-callback.ts (NEW ~210 LOC, decodes Jane state with embedded codeVerifier), <strong>src/routes/api.cron.jane-poll.ts</strong> (NEW ~180 LOC, new architectural pattern — cron-driven polling per active Jane connection, 24h-back + 14d-forward window, dispatches trigger graph on status transitions), src/server/emma-rescue.functions.ts (Jane claim writeback branch wrapped in withFreshJaneToken), src/routes/app.refill.settings.scheduler.tsx (Jane card with partnership-approval-pending badge). The polling cron pattern joins the connector substrate as a new shape — schedulers without webhooks now have a clean architectural slot. Credentials needed when Jane partnership clears: JANE_CLIENT_ID + JANE_CLIENT_SECRET + JANE_IAM_BASE_URL (per-partner hostname provisioned at approval). Switch: REFILL_JANE_ENABLED=1 in wrangler.jsonc vars + redeploy + add cron trigger to wrangler.jsonc.",
    ],
  },
  {
    version: "v1.39.0",
    date: "June 2026",
    items: [
      "<strong>v1.39.0 &mdash; Booker architecture pre-build. Third execution of the doctrine. Critical scout finding: despite Mindbody acquiring Booker in 2018 (~$150M) and unifying both under the Playlist parent brand in June 2025, Booker operates COMPLETELY SEPARATE API infrastructure from Mindbody — different base URL (api.booker.com), different developer portal (developers.booker.com), different auth (subscription_key + client credentials), different webhook scheme. Booker needs its own connector despite the unified corporate ownership.</strong> Divergences absorbed: server-to-server OAuth client_credentials (NOT user-consent), Azure API Management subscription_key (Ocp-Apim-Subscription-Key header) layered ON TOP of OAuth Bearer, paste-LocationId tenant identification (similar pattern to Boulevard's app-ID paste), HMAC-SHA256 base64 signature in X-Booker-Signature header, page-number pagination. Files: src/lib/schedulers/booker.ts (NEW ~470 LOC connector), src/server/emma-scheduler.functions.ts (initiateBookerConnect server fn + disconnect/resync branches + backfillBookerAppointments helper), src/routes/api.integrations.booker.install-callback.ts (NEW ~170 LOC, location-ID validator), src/routes/api.webhooks.scheduler.booker.$secret.ts (NEW ~270 LOC, per-spa path secret + mint-token-per-event pattern), src/routes/app.refill.settings.booker-install.tsx (NEW ~120 LOC, paste-Location-ID wizard), src/server/emma-rescue.functions.ts (Booker claim writeback), src/routes/app.refill.settings.scheduler.tsx (Booker card with App-approval-pending badge). Credentials needed when Booker dev portal approval clears: BOOKER_CLIENT_ID + BOOKER_CLIENT_SECRET + BOOKER_SUBSCRIPTION_KEY (sandbox triplet too). Switch: REFILL_BOOKER_ENABLED=1 in wrangler.jsonc vars + redeploy.",
    ],
  },
  {
    version: "v1.38.0",
    date: "June 2026",
    items: [
      "<strong>v1.38.0 &mdash; Vagaro architecture LIVE. First platform shipped with no Refill-side approval gate — Vagaro is paste-API-key, where each spa independently signs up for Vagaro's API access form ($10/mo, 7-business-day activation) + pastes their generated key into our wizard.</strong> Divergences absorbed: per-spa API key auth (NOT OAuth, NOT app-level credentials), no Refill-side env secrets needed (each spa is its own mini-tenant with Vagaro), X-Vagaro-Signature shared-token webhook auth (NOT HMAC — strict-equal comparison), 6 outbound IPs documented but irrelevant for our Cloudflare Workers receiver. Files: src/lib/schedulers/vagaro.ts (NEW ~380 LOC connector with shared-token verifyVagaroWebhookSignature), src/server/emma-scheduler.functions.ts (initiateVagaroConnect server fn + disconnect/resync branches + backfillVagaroAppointments helper), src/routes/api.integrations.vagaro.install-callback.ts (NEW ~190 LOC, paste-API-key wizard validator using getVagaroBusiness to ping the key before storing), src/routes/api.webhooks.scheduler.vagaro.$secret.ts (NEW ~260 LOC, per-spa path secret + shared-token verify), src/routes/app.refill.settings.vagaro-install.tsx (NEW ~150 LOC, paste-API-key wizard with deep link to Vagaro's API settings + cost disclosure), src/server/emma-rescue.functions.ts (Vagaro claim writeback branch with createVagaroAppointment + upsertVagaroCustomer roster-miss fallback), src/routes/app.refill.settings.scheduler.tsx (Vagaro card ENABLED in production — no Refill-side gate). <strong>Strategic note</strong>: 4-platform sprint (Vagaro/Booker/Jane/Zenoti) shipping in one cascade after BLVD-DENIED-doctrine-validation moment. Architecture-pre-build pattern now operationalized across 8 platforms total (Acuity live, Square live, Boulevard pre-built, MB pre-built, Vagaro live, Booker pre-built, Jane pre-built, Zenoti pre-built). Mangomint deferred — no public developer portal, sales-led $375/mo Unlimited tier required, no contract surface to build against.",
    ],
  },
  {
    version: "v1.37.0",
    date: "June 2026",
    items: [
      "<strong>v1.37.0 &mdash; Mindbody architecture pre-build. Second ship of the platforms-pre-built doctrine, landing the SAME DAY as the Boulevard DENIED-and-pivoted moment. The doctrine paid for itself inside 12 hours: Boulevard&rsquo;s dev portal came back with &lsquo;API access is exclusive to Enterprise tier&rsquo; (Justin Warren, Jira DS-4790) &mdash; the credentials gate moved from a 48hr clock to a Customer-Success-led Enterprise-upgrade negotiation that could take weeks or never close. Zero engineering hours lost. v1.36.0 architecture sits disabled-and-ready; pivoted instantly to Mindbody pre-build while CS conversation runs in parallel.</strong> <strong>What lands in this ship</strong>: (1) <code>src/lib/schedulers/mindbody.ts</code> (NEW, ~620 LOC) &mdash; full REST connector mirroring src/lib/schedulers/{acuity,square}.ts shape: types (MindbodyOAuthCredentials with apiKey field for partner-level Api-Key header, MindbodyTokenResponse carrying siteId + effectiveUserId from id_token claims, MindbodyAppointment with full status union Confirmed/Requested/Arrived/Started/Completed/Cancelled/LateCancelled/NoShow with NoShow priority over Status field per spec, MindbodyClient, MindbodyWebhookSubscription, MindbodyEnv), <code>resolveMindbodyEnv()</code> helper with defensive trim+lowercase per v1.35.2 doctrine, MINDBODY_OAUTH_SCOPES export (email profile openid offline_access Mindbody.Api.Public.v6 &mdash; offline_access is MANDATORY for refresh_token issuance), MINDBODY_WEBHOOK_EVENTS list (appointmentBooking.created/updated/cancelled/noShow + client.created), MINDBODY_WEBHOOK_SIGNATURE_HEADER constant (X-Mindbody-Signature), <code>buildMindbodyAuthorizeUrl()</code> with response_type=code+id_token + response_mode=form_post + nonce, <code>exchangeMindbodyCodeForToken</code> + <code>refreshMindbodyAccessToken</code> with form-encoded body per v1.35.7 doctrine + JWT claims decoder for siteId/effectiveUserId extraction, mindbodyFetch wrapper sending Authorization + Api-Key + SiteId triple-header on every API call (MB&rsquo;s double-auth model: OAuth proves user, Api-Key proves partner app), all CRUD functions (listMindbodyAppointments paginated via PaginationResponse.TotalResults / getMindbodyAppointment / bookMindbodyAppointment with literal Execute:Force admin-override flag analog to Acuity&rsquo;s ?admin=true / addMindbodyClient for roster-miss fallback / listMindbodyClients paginated), webhook subscription lifecycle (createMindbodyWebhookSubscription posting to MINDBODY_WEBHOOKS_API_BASE / deleteMindbodyWebhookSubscription), HMAC-SHA256 verification (verifyMindbodyWebhookSignature over raw body keyed by messageSignatureKey &mdash; NO URL prefix unlike Square&rsquo;s notification_url+body concatenation), parseMindbodyWebhookBody (generous field-name fallbacks for messageId/eventInstanceId, eventInstanceOriginationDateTime/occurredAt, eventData/data, with NoShow boolean priority collapsed into status=&lsquo;NoShow&rsquo; before downstream code dispatches), mindbodyStatusToRefillStatus mapper, plus stub for createMindbodyActivationLink (partner-activation-link generation for the link-first UX path per feedback_setup_wizards_auto_advance). (2) <code>initiateMindbodyOAuth</code> in <code>src/server/emma-scheduler.functions.ts</code> &mdash; server fn launching standard 3-legged OAuth. Reads MINDBODY_OAUTH_CLIENT_ID + MINDBODY_API_KEY (or sandbox triplet per MINDBODY_ENV), applies stripWs whitespace strip per v1.35.4, length-logs both credential lengths to console.log, throws &lsquo;not configured&rsquo; with explicit missing-var list until credentials land. (3) Mindbody branch in <code>disconnectScheduler</code> &mdash; deletes per-spa subscription via partner-level Api-Key (still works after OAuth grant revoked). (4) Mindbody branch in <code>resyncSchedulerConnection</code> &mdash; gate accepts &lsquo;mindbody&rsquo; alongside acuity/square/boulevard, dispatches to backfillMindbodyAppointments with connectionId + accessToken + siteId. (5) <code>backfillMindbodyAppointments</code> helper &mdash; mirrors backfillSquareBookings shape on REST substrate: 30d-back + 90d-forward window via YYYY-MM-DD StartDate/EndDate, offset-based pagination walking until consumed&gt;=total with 50-page hard cap, idempotent upsert on (user_id, external_id, source), pre-migrates csv-mindbody-sourced rows for upsert dedupe, patientIndex matching pattern. <code>mindbodyAppointmentToRow</code> shape helper. <code>withFreshMindbodyToken</code> just-in-time refresh helper (~5min before expiry &mdash; MB tokens last ~1hr, much shorter than Square&rsquo;s 30-day window). (6) <code>src/routes/api.integrations.mindbody.oauth-callback.ts</code> (NEW, ~340 LOC) &mdash; OAuth callback handling Mindbody&rsquo;s form_post POST shape (body application/x-www-form-urlencoded carrying code+id_token+state) AND GET fallback for sandbox debug convenience. Body-parsing tolerant of JSON OR form-encoded OR querystring. v1.35.x error-bubbling on token_exchange catch (sanitized + URL-safe-encoded). siteId extracted from id_token JWT claims becomes platform_account_id (tenant routing key). Generates per-spa webhook path secret upfront so URL we register matches URL receiver dispatches on. Webhook subscribe FAIL-DEGRADED per v1.35.6 pattern with HEAD-request-specific error detection (Mindbody validates webhook URL via HEAD before subscription create &mdash; receiver MUST 2xx HEAD). Backfill FAIL-DEGRADED per v1.35.8. Warning priority: backfill warning &gt; webhook warning (no spa-side tier-gate on MB unlike Square Free / Boulevard Premier). (7) <code>src/routes/api.webhooks.scheduler.mindbody.$secret.ts</code> (NEW, ~430 LOC) &mdash; webhook receiver. Per-spa path secret pattern (back to Acuity, NOT Square&rsquo;s single-global); tenant routing via WHERE platform=&lsquo;mindbody&rsquo; AND webhook_secret=:secret lookup. <strong>HEAD handler returns 2xx</strong> &mdash; required for Mindbody&rsquo;s subscription-create URL validation; without it, subscriptions never get created. Defense-in-depth: cross-check siteId in payload vs connection.platform_account_id (mismatch = ignore). HMAC verification uses messageSignatureKey stored at oauth-callback time. Degraded-mode fallback if signing secret missing (accepts events with warning stamped, flipping to hard-reject when secret reliably lands). CLIENT_CREATED branch ack+defers roster enrichment to follow-up. Appointment events resolve embedded-or-GET, upsert, fire status-transition trigger graph into rescue dispatch + reliability recompute. Always returns 200; failures stamp audit row error column. (8) Mindbody branch in <code>claimRescueSlot</code> at src/server/emma-rescue.functions.ts &mdash; writeableSource tagged-union now (acuity | square | boulevard | mindbody | null). Branch loads active mindbody connection, reads partner Api-Key from env, runs withFreshMindbodyToken to ensure token current, fetches original appointment via getMindbodyAppointment for sessionTypeId+staffId+locationId, resolves ClientId via roster-hit path (claimer&rsquo;s attachments.scheduler_client_id) OR roster-miss path (addMindbodyClient first, stamp returned ID back to patient node attachments for fast-path next time per spec Q6 recommendation), POSTs replacement booking with Execute:&quot;Force&quot; admin override at same start time. Stamps new external_id on emma_appointments row. Fail-open on any step failure (claim stays committed, notes stamped). (9) <code>src/routes/app.refill.settings.scheduler.tsx</code> &mdash; new Mindbody card. Disabled state with &lsquo;OAuth setup pending&rsquo; amber badge + button labeled &lsquo;Connect Mindbody &middot; Pending&rsquo;. Title attribute explains the Support ticket dependency. Card enables when REFILL_MINDBODY_ENABLED=1. Coming Soon section now JaneApp-only (Mindbody moved into Available alongside Acuity / Square / Boulevard). (10) <code>wrangler.jsonc</code> &mdash; MINDBODY_ENV added to vars block per v1.35.3 doctrine (anything that must survive deploys lives in config). (11) <code>src/lib/changelog.ts</code> &mdash; this entry. <strong>Architecture-stub items pending sandbox-verify</strong>: exact id_token claim keys for siteId + effectiveUserId (assumed siteId/SiteId/site_id + effectiveUserId/EffectiveUserId/sub with fallback chain); exact webhook payload field names (messageId/eventInstanceId, eventInstanceOriginationDateTime/occurredAt, eventData/data &mdash; parser tolerant of all); MB Webhooks API base URL (pinned to mb-api.mindbodyonline.com/platform/webhooks/api/v1 &mdash; verify at sandbox); messageSignatureKey storage path (currently overloads platform_account_email as architecture-stub; clean schema migration adding webhook_message_signature_key column queued for sandbox-verify); partner-activation-link endpoint exact path (placeholder URL); CSV-mindbody source migration pre-step verified empty (no historical rows to migrate). <strong>Substrate now genuinely platform-agnostic across 4 axes</strong>: webhook URL routing (per-spa path secret like Acuity/MB vs single-global like Square/Boulevard), token lifecycle (long-lived Acuity vs 1hr+refresh Mindbody vs 30-day+refresh Square vs app-level no-token Boulevard), auth model (3-legged OAuth Acuity/Square/MB vs paste-Application-ID Boulevard), API substrate (REST Acuity/Square/MB vs GraphQL Boulevard). The connector pattern absorbs all four shapes cleanly. <strong>Touched</strong>: 3 NEW files (schedulers/mindbody.ts, api.integrations.mindbody.oauth-callback.ts, api.webhooks.scheduler.mindbody.$secret.ts), 3 EXTENDED files (emma-scheduler.functions.ts, emma-rescue.functions.ts, app.refill.settings.scheduler.tsx, wrangler.jsonc), 1 changelog entry. No schema change (webhook_message_signature_key migration queued as architecture-stub; current overload uses platform_account_email until sandbox-verify justifies the migration). <strong>Credentials needed when MB Support ticket clears</strong>: MINDBODY_OAUTH_CLIENT_ID + MINDBODY_OAUTH_CLIENT_SECRET + MINDBODY_API_KEY (production), MINDBODY_SANDBOX_OAUTH_CLIENT_ID + MINDBODY_SANDBOX_OAUTH_CLIENT_SECRET + MINDBODY_SANDBOX_API_KEY (sandbox). All via <code>wrangler secret put</code>. Then flip REFILL_MINDBODY_ENABLED to 1 in wrangler.jsonc vars + redeploy. <strong>Strategic anchor</strong>: per [[project-platforms-pre-built-architecture]] this ship validates the doctrine in real-time conditions. Two platform integrations now sit architecture-complete-disabled, both gated on external timelines we don&rsquo;t control. Engineering velocity stays compounded. Either platform&rsquo;s credentials clearing = minutes-to-live, not days-to-build. The doctrine isn&rsquo;t a thought experiment anymore; it&rsquo;s the operating system.",
    ],
  },
  {
    version: "v1.36.0",
    date: "June 2026",
    items: [
      "<strong>v1.36.0 &mdash; Boulevard architecture pre-build. First ship of the new project-platforms-pre-built doctrine banked at v1.35.11 close. Instead of waiting on Boulevard&rsquo;s 48hr app-approval clock before writing code, the entire connector substrate ships NOW in architecture-complete state &mdash; UI lives in a disabled card labeled <code>Connect Boulevard &middot; Pending</code>, all 1,260+ LOC compiles clean, every file applies the 11 v1.35.x architectural patterns from minute one. When approval clears: paste BLVD_APPLICATION_ID + BLVD_API_KEY + BLVD_API_SECRET to CF secrets, set <code>REFILL_BOULEVARD_ENABLED=1</code> in wrangler.jsonc vars, redeploy. Card enables, button works, test, ship live. Engineering velocity is now decoupled from approval timelines.</strong> <strong>What lands in this ship</strong>: (1) <code>src/lib/graphql/boulevard-admin-client.ts</code> (NEW, ~190 LOC) &mdash; generic GraphQL POST wrapper, URN normalization helpers (parseBoulevardUrn / boulevardUrnTail / isBoulevardUrn), BoulevardGraphQLException with errors[] surfacing, Basic-auth header builder, env-aware API base resolution, pinned BOULEVARD_API_VERSION constant. (2) <code>src/lib/schedulers/boulevard.ts</code> (NEW, ~660 LOC) &mdash; full connector mirroring src/lib/schedulers/square.ts shape: types (BoulevardOAuthCredentials, BoulevardTokenResponse, BoulevardBusiness, BoulevardLocation, BoulevardAppointment with full status union BOOKED/CONFIRMED/ARRIVED/STARTED/COMPLETED/CANCELLED/NO_SHOW/RESCHEDULED, BoulevardClient, BoulevardWebhookSubscription, BoulevardEnv), <code>resolveBoulevardEnv()</code> helper with defensive trim+lowercase per v1.35.2 doctrine, BOULEVARD_OAUTH_SCOPES export, BOULEVARD_WEBHOOK_EVENTS list (CLIENT_CREATED + 5 APPOINTMENT_* events), <code>buildBoulevardInstallUrl()</code> for the Refill-internal wizard launch, all CRUD functions (getBoulevardBusiness / listBoulevardLocations / listBoulevardAppointments / getBoulevardAppointment / createBoulevardAppointment / upsertBoulevardClient), webhook subscription lifecycle (createBoulevardWebhookSubscription / deleteBoulevardWebhookSubscription), HMAC-SHA256 signature verification scaffold (verifyBoulevardWebhookSignature with BOULEVARD_WEBHOOK_SIGNATURE_HEADER constant for one-line edit on sandbox-verify), parseBoulevardWebhookBody (generous on field naming &mdash; accepts businessId/business_id, id/eventId/event_id, etc), boulevardStatusToRefillStatus mapper, and all GraphQL operation strings pinned as constants at the bottom so any Boulevard-side mutation rename is a one-line edit. (3) <code>initiateBoulevardOAuth</code> in <code>src/server/emma-scheduler.functions.ts</code> &mdash; server fn launching the wizard. Reads BLVD_APPLICATION_ID + BLVD_API_KEY + BLVD_API_SECRET from env (or sandbox equivalents per BLVD_ENV), applies stripWs defensive whitespace strip per v1.35.4 doctrine, length-logs all three credential lengths to console.log per v1.35.4 diagnostic doctrine, throws &lsquo;not configured&rsquo; with explicit missing-var list until credentials land (that&rsquo;s the point &mdash; UI button stays disabled, code is ready). (4) Boulevard branch in <code>disconnectScheduler</code> &mdash; deletes the per-spa webhook subscription via app-level creds + businessId URN scoping, fail-open if creds aren&rsquo;t configured. (5) Boulevard branch in <code>resyncSchedulerConnection</code> &mdash; gate now accepts &lsquo;boulevard&rsquo; alongside &lsquo;acuity&rsquo; + &lsquo;square&rsquo;, dispatches to backfillBoulevardAppointments; access_token gate skipped for Boulevard (app-level auth model has no per-spa token), replaced with platform_account_id gate (businessId URN required). (6) <code>backfillBoulevardAppointments</code> helper &mdash; mirrors backfillSquareBookings shape but on GraphQL substrate: 30d-back + 90d-forward window, GraphQL pageInfo cursor pagination with 50-page hard cap, idempotent upsert on (user_id, external_id, source), pre-migrates csv-boulevard-sourced rows to boulevard source for upsert dedupe, patientIndex matching identical to Square path. <code>boulevardAppointmentToRow</code> shape helper. (7) <code>src/routes/api.integrations.boulevard.install-callback.ts</code> (NEW, ~330 LOC) &mdash; install callback receiver. Architecture supports BOTH the callback model (Boulevard POSTs on install) AND polling fallback (cron OR GET-driven invocation routing through the same handleInstallCallback) so whichever Boulevard actually uses at sandbox surfaces without code change. Parses JSON OR form-encoded OR querystring payloads with generous field-name fallbacks. Tier-gate detection (Premier OR Enterprise required &mdash; matches verified-research scout 2026-06-02 that Boulevard&rsquo;s API access is broader than marketing&rsquo;s &lsquo;Enterprise only&rsquo; framing). Webhook subscribe fail-degrades non-fatal per v1.35.6 pattern; backfill fail-degrades non-fatal per v1.35.8 pattern. Warning priority order: tier_gate &gt; backfill warning &gt; webhook warning. Connection lands status=&lsquo;connected&rsquo; with warning stamped on last_error; settings card&rsquo;s warning-vs-error UI split from v1.35.9 renders amber NOTE vs red LAST ERROR correctly. (8) <code>src/routes/api.webhooks.scheduler.boulevard.ts</code> (NEW, ~330 LOC) &mdash; webhook receiver. Single global URL pattern matching Square (no per-spa secret in path); tenant routing via businessId URN in payload against (platform=&lsquo;boulevard&rsquo;, platform_account_id) lookup. HMAC-SHA256 signature verification with degraded-mode fallback (if connection.webhook_secret empty &mdash; e.g. install-callback&rsquo;s webhook subscribe fail-degraded &mdash; accepts events with warning stamped to audit row, flipping to hard-reject when signing secret reliably lands). CLIENT_CREATED branch acknowledges + defers full roster enrichment to a follow-up ship. Appointment events resolve embedded-or-GET (same pattern as Square receiver), upsert, fire status-transition trigger graph into rescue dispatch + reliability recompute. Always returns 200 to prevent Boulevard retry storms; failures stamp audit row error column for replay. (9) Boulevard branch in <code>claimRescueSlot</code> at src/server/emma-rescue.functions.ts &mdash; writeableSource tagged-union extended to (acuity | square | boulevard | null). Branch loads the active boulevard connection by businessId, reads app creds from env, calls getBoulevardAppointment for original slot + upsertBoulevardClient for the rescuee + createBoulevardAppointment with the new client at the same start time. Tier-block detection on common GraphQL error substrings (premier / enterprise / subscription / forbidden); fail-degrades with <code>writeback_failed: tier_blocked (Boulevard Premier/Enterprise required)</code> distinct from generic failures so the Rescue dashboard renders an upgrade prompt. Fail-open if app creds aren&rsquo;t configured. (10) <code>src/routes/app.refill.settings.scheduler.tsx</code> &mdash; new Boulevard card in AvailablePlatforms. Disabled state with &lsquo;App approval pending&rsquo; amber badge + button labeled &lsquo;Connect Boulevard &middot; Pending&rsquo;. Title attribute explains the 48hr clock. Card enables when REFILL_BOULEVARD_ENABLED=1. Coming Soon section updated (Mindbody &middot; JaneApp only; Boulevard now lives in Available). (11) <code>src/routes/app.refill.settings.boulevard-install.tsx</code> (NEW, ~190 LOC) &mdash; install wizard page. 3-step ordered list: copy Application ID with prominent Copy button + clipboard API + success toast, deep-link CTA to Boulevard&rsquo;s Manage Business &rarr; Apps &amp; Integrations dashboard (https://dashboard.joinblvd.com/manage-business/apps), explainer of what happens next. Sibling path (NOT scheduler-nested) to avoid feedback_tanstack_outlet_trap (bit 3x at v331/341.3/349). Empty-state fallback if accessed without Application ID. Bottom-of-page tier-eligibility hint (Premier/Enterprise required for Custom Apps install). (12) <code>wrangler.jsonc</code> &mdash; BLVD_ENV added to vars block per v1.35.3 doctrine (anything that must survive deploys lives in config, NOT dashboard plaintext). (13) <code>src/lib/changelog.ts</code> &mdash; this entry. <strong>What waits on credentials (the architecture-stub items)</strong>: exact GraphQL mutation names (probably <code>appointmentCreate</code> / <code>clientCreate</code> / <code>webhookSubscriptionCreate</code> per Extole convention &mdash; pinned as constants at the bottom of boulevard.ts so one-line edits at sandbox-verify); webhook signature header name + HMAC algorithm specifics (best guess <code>X-Boulevard-Signature</code> + HMAC-SHA256 over raw body); whether install-callback POST or polling cron is the right path (handler covers both); exact API endpoint host (assumed <code>https://dapi.joinblvd.com</code>); app-level permissions config in Boulevard dev console; test business URN for sandbox auth. <strong>What this DOESN&rsquo;T ship</strong>: actual go-live (button stays disabled until REFILL_BOULEVARD_ENABLED flips); GraphQL queries are pinned-string assumptions that may need one-line edits at sandbox-verify; CLIENT_CREATED roster enrichment defers to a follow-up ship; live test against Boulevard&rsquo;s actual API. <strong>Strategic anchor</strong>: per [[project-platforms-pre-built-architecture]] this is the first execution of the doctrine end-to-end. Mindbody architecture pre-build queues next; whichever platform&rsquo;s credentials clear first ships live first. By the time the Mindbody Support ticket clears + Boulevard 48hr approval clears, BOTH substrates will be hardened against divergent webhook routing (per-spa secret path vs single global URL), divergent token lifecycles (30-day refresh vs app-level vs long-lived OAuth), divergent auth models (3-legged OAuth vs paste-Application-ID vs activation-link), and divergent API substrates (REST vs GraphQL). The substrate is now genuinely platform-agnostic, not acuity-or-square-shaped pretending. <strong>Touched</strong>: 5 NEW files (boulevard-admin-client.ts, schedulers/boulevard.ts, api.integrations.boulevard.install-callback.ts, api.webhooks.scheduler.boulevard.ts, app.refill.settings.boulevard-install.tsx), 4 EXTENDED files (emma-scheduler.functions.ts, emma-rescue.functions.ts, app.refill.settings.scheduler.tsx, wrangler.jsonc), 1 changelog entry (this one). No schema change &mdash; Boulevard connections use the existing emma_scheduler_connections substrate (businessId URN fits in platform_account_id varchar). No new dependencies. <strong>Credentials needed when 48hr approval clears</strong>: BLVD_APPLICATION_ID + BLVD_API_KEY + BLVD_API_SECRET (production tier), BLVD_SANDBOX_APPLICATION_ID + BLVD_SANDBOX_API_KEY + BLVD_SANDBOX_API_SECRET (sandbox tier). All via <code>wrangler secret put</code>. Then flip REFILL_BOULEVARD_ENABLED to 1 in wrangler.jsonc vars + redeploy.",
    ],
  },
  {
    version: "v1.35.11",
    date: "June 2026",
    items: [
      "<strong>v1.35.11 &mdash; Re-sync clears stale last_error on success. Final polish from the v1.35.x debug arc. Grasshopper&rsquo;s post-v1.35.10 Re-sync succeeded under the chunked-window fix (backfill went through cleanly), but the connection card kept showing the pre-deploy backfill error message because <code>resyncSchedulerConnection</code> updated only <code>last_sync_at</code> &mdash; never touched <code>last_error</code>. Stale warnings stuck in place until a full disconnect+reconnect cycle refreshed everything. Now: Re-sync clears <code>last_error</code> to NULL on success path alongside the <code>last_sync_at</code> bump. Stale warning artifacts from prior shipping iterations get wiped on next manual sync, matching what the user expects (they hit Re-sync to clear something; it should actually clear).</strong> <strong>Touched</strong>: <code>src/server/emma-scheduler.functions.ts</code> (resyncSchedulerConnection update block), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes. <strong>The v1.35.x arc closes here.</strong> 11 ships across one debug session traced the Square OAuth integration from initial sandbox spec all the way through end-to-end working state on a real (sandbox) test account. Every layer proven: OAuth start + sandbox routing (v1.35.3 wrangler.jsonc); token exchange (v1.35.7 form-encoded + Grasshopper&rsquo;s correct Sandbox Application Secret re-paste from the OAuth tab); merchant fetch + connection upsert; webhook subscribe graceful degradation (v1.35.6); tier probe; backfill with location_id (v1.35.9) chunked into 30-day windows (v1.35.10); status flip + UI rendering with warning vs error split (v1.35.9 UI fix); platform-aware disconnect dialog (v1.35.9); Re-sync clears stale state (this ship). <strong>Production-seller reality</strong>: every degradation we ran into is sandbox-specific. Real Square sellers (production tier, Appointments enabled, no partner-tier webhook gate) walk through the green path our code already executes &mdash; they Just Connect.",
    ],
  },
  {
    version: "v1.35.10",
    date: "June 2026",
    items: [
      "<strong>v1.35.10 &mdash; Square backfill 30-day window chunking. v1.35.9 location_id fix unblocked /v2/bookings auth but Square&rsquo;s spec has another constraint: the duration between <code>start_at_min</code> and <code>start_at_max</code> must be <strong>less than 31 days</strong>. The 120-day backfill window (30 back + 90 forward) violates this and returns 400 INVALID_REQUEST_ERROR.</strong> <strong>Fix</strong>: backfill now chunks the 120-day window into ≤30-day slices and iterates per (location, slice). Per location, the loop walks 4&ndash;5 slices each making a paginated /v2/bookings call. All slice results combined into the single backfill upsert. 50-page hard cap retained per slice (so per location). The chunking adds 3-4x API calls per backfill but each call returns ≤200 bookings; total network cost stays under the daily Square sandbox quota by orders of magnitude. <strong>What this doesn&rsquo;t change</strong>: window total stays at 30 days back + 90 days forward (mirrors Acuity); slice size set to 30 days (just under Square&rsquo;s 31-day cap). If Square ever expands the date-range constraint, just bump <code>SLICE_DAYS</code>. <strong>Touched</strong>: <code>src/server/emma-scheduler.functions.ts</code> (slice builder + nested loop over locations × slices), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes.",
    ],
  },
  {
    version: "v1.35.9",
    date: "June 2026",
    items: [
      "<strong>v1.35.9 &mdash; Square backfill location_id fix + UI polish from v1.35.x debug arc. Grasshopper&rsquo;s re-sync after enabling Appointments on the Default Test Account sandbox seller surfaced the next layer of bug: Square&rsquo;s /v2/bookings returned 400 INVALID_REQUEST_ERROR BAD_REQUEST with empty detail. Square&rsquo;s ListBookings spec requires AT LEAST ONE of location_id, team_member_id, or customer_id in the query string — none specified = 400. My <code>backfillSquareBookings</code> sent only start_at_min + start_at_max + limit, so Square rejected.</strong> <strong>Fix</strong>: <code>backfillSquareBookings</code> now fetches the seller&rsquo;s /v2/locations first, then iterates /v2/bookings per location_id. Zero-location response treated as legitimate empty state (no error, just returns 0). 50-page hard cap retained per location. <strong>UI polish from arc retrospective</strong>: (1) <strong>Disconnect dialog</strong> &mdash; was hardcoded <code>&quot;Disconnect Acuity?&quot;</code> even when disconnecting Square. Now uses <code>platformLabel(connection.platform)</code> for both the confirm text and the success toast. Will work for Mindbody / Boulevard / Jane on future ships. (2) <strong>Last error vs Note UI split</strong> &mdash; the connection card previously rendered ALL lastError values in red &lsquo;LAST ERROR&rsquo; styling regardless of severity. v1.35.6 (webhook degrade) + v1.35.8 (backfill degrade) populate lastError with WARNINGS on status=connected rows, not real errors. Now: if <code>status === &quot;connected&quot;</code> the lastError renders as &lsquo;NOTE&rsquo; in amber (informational); if <code>status === &quot;error&quot;</code> it renders as &lsquo;LAST ERROR&rsquo; in destructive-red (action required). The visual cue now matches semantic severity. <strong>Touched</strong>: <code>src/server/emma-scheduler.functions.ts</code> (backfillSquareBookings fetches /v2/locations + iterates per location), <code>src/routes/app.refill.settings.scheduler.tsx</code> (platform-aware disconnect confirm + warning vs error UI split), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes.",
    ],
  },
  {
    version: "v1.35.8",
    date: "June 2026",
    items: [
      "<strong>v1.35.8 &mdash; Square backfill failure degraded to non-fatal (seller-onboarding state, not a credential failure). Real progress milestone: v1.35.7&rsquo;s form-encoded body fix + Grasshopper&rsquo;s sandbox-Application-Secret re-paste (the value buried on Square&rsquo;s OAuth tab, not the personal access token on the Credentials tab) finally cleared the token exchange + merchant fetch + connection upsert + webhook-degrade chain end-to-end. The connection row materialized. NEW error surfaced from a deeper step: <code>backfillSquareBookings</code> hit <code>/v2/bookings</code> and Square returned <code>401 AUTHENTICATION_ERROR UNAUTHORIZED &quot;not onboarded to Appointments&quot;</code>. That&rsquo;s a seller-config state &mdash; the Default Test Account in Grasshopper&rsquo;s sandbox doesn&rsquo;t have Square Appointments enabled &mdash; not a credentials problem. The connection itself works (token exchange is fine); there&rsquo;s just nothing for backfill to read until the seller turns on the Appointments product on their sandbox dashboard.</strong> <strong>Fix</strong>: same fail-degraded pattern v1.35.6 applied to webhook subscribe now applies to backfill. <code>backfillSquareBookings</code> failure no longer returns <code>errReturn(&quot;backfill&quot;)</code> &mdash; instead the warning gets stamped to <code>last_error</code> with a clear message (&ldquo;Square Appointments not enabled on this seller. Enable it on your Square dashboard, then hit Re-sync.&rdquo; for the specific onboarding case; generic &ldquo;Initial backfill failed (...). Hit Re-sync to retry.&rdquo; otherwise) and the connection still lands <code>status=connected</code>. <strong>Warning priority order</strong>: <code>tier_gate</code> &gt; <code>backfillWarning</code> &gt; <code>webhookWarning</code> &gt; <code>null</code>. Tier wins because it blocks writeback (the actual value-prop call); backfill + webhook warnings are read-side degradations the user can fix on their Square dashboard then re-sync. <strong>Debug arc retrospective &mdash; full chain</strong>: v1.35.0 (webhook scope gap) → v1.35.1 (added scopes) → v1.35.2 (defensive trim SQUARE_ENV value) → v1.35.3 (SQUARE_ENV into wrangler.jsonc, deploys were wiping the dashboard-side var) → v1.35.4 (whitespace strip credentials + bubble Square error to URL + length logging) → v1.35.5 (redirect_uri in token body) → v1.35.6 (drop partner-tier webhook scopes; degrade webhook subscribe to non-fatal) → v1.35.7 (form-encoded body per OAuth 2.0 RFC) → Grasshopper re-pasted the Sandbox Application Secret from the OAuth tab (NOT the Access token from the Credentials tab &mdash; Square&rsquo;s UI has two confusingly-similar pages and the OAuth-required secret only lives on one) → v1.35.8 (degrade backfill non-fatal because some sandbox sellers aren&rsquo;t onboarded to Appointments). The v1.35.4 logging ship was the unlock for everything that followed &mdash; without surfacing Square&rsquo;s actual error text + credential lengths, we&rsquo;d still be guessing. <strong>Architectural learning banked across the arc</strong>: (1) any OAuth callback that catches errors MUST bubble actual provider error text to the user-visible redirect URL or settings dashboard (v1.35.4 pattern), (2) credential value length logging surfaces type-confusion (access-token vs application-secret) before code-fix iterations begin, (3) every callback step that touches an external provider needs explicit fail-degrade-vs-fail-fast semantics declared at design time (steps that block the connection&rsquo;s value-prop fail fast; steps that block fringe read-sync degrade), (4) Cloudflare dashboard plaintext vars + observability config are EPHEMERAL relative to wrangler deploys; anything that must survive deploys lives in wrangler.jsonc, (5) Cloudflare secret input fields can leak whitespace into stored values; defensive strip + length-log pattern is the workaround. Same patterns now need to land on Mindbody, Boulevard, JaneApp OAuth handlers when they ship. <strong>Touched</strong>: <code>src/routes/api.integrations.square.oauth-callback.ts</code> (backfill try/catch now stamps warning instead of errReturn; warning priority added to status=&quot;connected&quot; lastError chain), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes.",
    ],
  },
  {
    version: "v1.35.7",
    date: "June 2026",
    items: [
      "<strong>v1.35.7 &mdash; Square token exchange switched to application/x-www-form-urlencoded body per OAuth 2.0 RFC 6749. Square&rsquo;s docs claim both JSON and form-encoded work but sandbox /oauth2/token returned persistent 401 service.not_authorized with JSON body across every variation tried in v1.35.4-6. Form-encoded is the OAuth 2.0 spec-compliant default and the format every major OAuth server accepts unambiguously, so eliminating the JSON-vs-form variable rules out content-type as the cause regardless of whether Square&rsquo;s docs are technically correct.</strong> Same change applied to <code>exchangeSquareCodeForToken</code> and <code>refreshSquareAccessToken</code> for consistency. Body construction uses <code>URLSearchParams</code> with all required params (<code>client_id</code>, <code>client_secret</code>, <code>code</code>/<code>refresh_token</code>, <code>grant_type</code>, <code>redirect_uri</code> for authorization_code grant); Content-Type header flipped from <code>application/json</code> to <code>application/x-www-form-urlencoded</code>; Square-Version header retained. If v1.35.7 token exchange still 401s, the issue is definitively not request-format-related and we need to look at app-level config in Square (sandbox app may need explicit OAuth enablement, or credentials genuinely mismatch despite repeated re-paste cycles &mdash; Worker Logs in CF dashboard would reveal the latter via the v1.35.4 client_id_len/client_secret_len console output). <strong>Touched</strong>: <code>src/lib/schedulers/square.ts</code> (both OAuth-flow fns updated), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes.",
    ],
  },
  {
    version: "v1.35.6",
    date: "June 2026",
    items: [
      "<strong>v1.35.6 &mdash; Square OAuth scope set narrowed + webhook subscription degraded to non-fatal. v1.35.5&rsquo;s redirect_uri fix didn&rsquo;t resolve the <code>401 service.not_authorized</code> from Square&rsquo;s token exchange. Same identical rejection on every retry across multiple OAuth flows. The diagnostic confirmed via the consent screen rendering only 7 scopes instead of the 9 we request &mdash; Square&rsquo;s auth UI is silently dropping the two <code>DEVELOPER_APPLICATION_WEBHOOKS_*</code> scopes from user-facing consent, but the token-exchange POST still includes them in the requested scope set, triggering Square to reject the whole exchange with <code>service.not_authorized</code> because the app isn&rsquo;t app-level approved to use those partner-tier scopes.</strong> <strong>v1.35.6 isolates the issue</strong>: (1) removed <code>DEVELOPER_APPLICATION_WEBHOOKS_WRITE</code> + <code>DEVELOPER_APPLICATION_WEBHOOKS_READ</code> from <code>SQUARE_OAUTH_SCOPES</code> &mdash; back to the 7-scope set the consent UI actually surfaces. Token exchange should now succeed. (2) Webhook subscription step in the OAuth callback degraded to non-fatal &mdash; if <code>createSquareWebhookSubscription</code> throws (it will, since we&rsquo;re not requesting the scope anymore), the connect flow continues with <code>webhook_subscription_id</code> left null. The connection lands in <code>status=&quot;connected&quot;</code> with the warning stamped on <code>last_error</code> for settings-page visibility: &lsquo;Webhook subscription unavailable on this Square app. Connection works for read sync; real-time push deferred until app approval or polling fallback.&rsquo; <strong>Strategic implications</strong>: read sync via polling becomes the v1 substrate for Square instead of push webhooks. Polling fallback is a follow-up ship &mdash; for v1.35.6 the connection just doesn&rsquo;t auto-sync after the initial 90-day backfill; user can hit Re-sync manually to refresh. <strong>What this doesn&rsquo;t fix yet</strong>: real-time push notifications when a Square seller cancels/no-shows. Without webhooks Refill won&rsquo;t know about a cancellation until the next poll or manual re-sync. The proper fix is either (a) get Square to approve the app for partner-tier webhook scopes (manual support ticket process), or (b) ship a polling-based sync (~30-min cron sweeping each connected Square seller&rsquo;s next-7-day window). Banking the choice for after this debug arc closes. <strong>Touched</strong>: <code>src/lib/schedulers/square.ts</code> (removed 2 webhook scopes from SQUARE_OAUTH_SCOPES + extended comment), <code>src/routes/api.integrations.square.oauth-callback.ts</code> (try/catch around createSquareWebhookSubscription now stamps webhook_warning to last_error instead of returning errReturn; subscription persistence conditional on success; final status=&quot;connected&quot; preserves either tier_gate OR webhook_warning), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes. <strong>Debug arc retrospective</strong>: v1.35.0 (webhook scope gap) → v1.35.1 (added scopes) → v1.35.2 (defensive trim SQUARE_ENV) → v1.35.3 (SQUARE_ENV into wrangler.jsonc) → v1.35.4 (whitespace strip credentials + bubble Square error to URL) → v1.35.5 (redirect_uri in token body) → v1.35.6 (drop the over-scoped request). Each step necessary, each surfaced by the prior step&rsquo;s diagnostic improvement. The error-surfacing pattern (v1.35.4) was the unlock for every subsequent step.",
    ],
  },
  {
    version: "v1.35.5",
    date: "June 2026",
    items: [
      "<strong>v1.35.5 &mdash; Square OAuth token-exchange redirect_uri fix. v1.35.4&rsquo;s error-surfacing revealed the actual Square rejection: <code>401 { message: &quot;Not Authorized&quot;, type: &quot;service.not_authorized&quot; }</code> &mdash; not a credentials problem (those were verified clean across multiple re-paste cycles) but a missing required parameter in my token exchange body.</strong> Square&rsquo;s <code>/oauth2/token</code> spec requires <code>redirect_uri</code> to be echoed back in the POST body for <code>authorization_code</code> grants, even when the corresponding <code>/oauth2/authorize</code> URL didn&rsquo;t include redirect_uri explicitly (Square uses the registered URL from app config silently in authorize, but expects the value back during token exchange for verification). My <code>exchangeSquareCodeForToken</code> body had <code>client_id</code> + <code>client_secret</code> + <code>code</code> + <code>grant_type</code> but omitted <code>redirect_uri</code>. Square returned <code>service.not_authorized</code> for the missing field rather than a clear &lsquo;missing parameter&rsquo; error &mdash; an error-classification quirk that masked the real cause through three deploys and six debug iterations. <strong>Fix</strong>: added <code>redirect_uri: args.credentials.redirectUri</code> to the POST body in <code>exchangeSquareCodeForToken</code>. The <code>redirectUri</code> arg was already being passed in by the OAuth callback (constructed as <code>${url.origin}/api/integrations/square/oauth-callback</code>) but unused. Now sent and verified against Square&rsquo;s registered Sandbox Redirect URL. <strong>Debug arc retrospective</strong>: v1.35.0 webhook scope gap → v1.35.1 added scopes → v1.35.2 defensive trim on SQUARE_ENV value → v1.35.3 SQUARE_ENV into wrangler.jsonc (deploys were wiping the dashboard-side var) → v1.35.4 whitespace strip on credentials + bubble Square error detail to URL → v1.35.5 redirect_uri in token body. Each fix was necessary; the underlying causes were genuinely independent (scope config, defensive trim, dashboard-vs-config var persistence, error message visibility, missing required param). The token_exchange ERROR-SURFACING ship (v1.35.4) was the unlock &mdash; without it, the redirect_uri root cause would have stayed buried under the generic <code>scheduler_error=token_exchange</code> redirect URL. Architectural learning banked: <strong>any OAuth callback handler that catches errors MUST bubble the actual provider error text into the user-visible redirect URL or settings dashboard</strong>, not just emit a generic error code. The 10-minute fix took an hour because the generic code obscured the actual rejection reason. Same pattern needs to land on Mindbody / Boulevard OAuth handlers when they ship. <strong>Touched</strong>: <code>src/lib/schedulers/square.ts</code> (add redirect_uri to body in <code>exchangeSquareCodeForToken</code> + explanatory comment), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes.",
    ],
  },
  {
    version: "v1.35.4",
    date: "June 2026",
    items: [
      "<strong>v1.35.4 &mdash; Square credential whitespace defense + token-exchange error surfacing. Grasshopper&rsquo;s catch under v1.35.3 testing: CF&rsquo;s Variables and Secrets input field wraps long values visually, and that wrap can introduce embedded whitespace (newlines/tabs/spaces) into the stored secret value &mdash; not just leading/trailing, but mid-string. He&rsquo;d already checked leading whitespace and re-pasted both SQUARE_SANDBOX_APP_ID and SQUARE_SANDBOX_APP_SECRET, but token exchange still failed with the generic <code>?scheduler_error=token_exchange</code> redirect that hid Square&rsquo;s actual rejection reason.</strong> <strong>Two fixes in this ship</strong>: (1) <strong>Defensive whitespace strip on credential env vars at runtime</strong> &mdash; <code>process.env.SQUARE_*_APP_ID</code> + <code>process.env.SQUARE_*_APP_SECRET</code> now get <code>.replace(/\\s+/g, &quot;&quot;)</code> applied before use. Square credentials are alphanumeric + hyphens with no legitimate whitespace, so stripping all whitespace is safe and defends against the multi-line copy-paste artifacts CF&rsquo;s input field is leaking. Applied at every call site: <code>initiateSquareOAuth</code> (oauth-start), <code>oauth-callback</code> token exchange. (2) <strong>Bubble Square&rsquo;s actual error detail into the redirect URL</strong> &mdash; the prior <code>errReturn(&quot;token_exchange&quot;)</code> threw away Square&rsquo;s response message and emitted a one-word generic code. Now sanitizes Square&rsquo;s actual error string (limit 180 chars, URL-safe filter) and appends it as <code>scheduler_error=token_exchange__&lt;sanitized_message&gt;</code>. Visible in the browser Network panel response Location header on next attempt without needing wrangler tail. <strong>Diagnostic logging strengthened</strong>: both oauth-start and oauth-callback now <code>console.log</code> the RAW env-var contents alongside the resolved value AND the resolved credential string LENGTHS &mdash; so if the issue is still whitespace, the lengths will mismatch between attempts and reveal the contamination directly. <strong>What this doesn&rsquo;t change</strong>: the underlying CF input field bug is unfixable on our side; the strip + length-log is the workaround. The wrangler.jsonc <code>vars</code> block doesn&rsquo;t help here because secrets (not plaintext) can&rsquo;t live in wrangler.jsonc &mdash; they MUST be in CF&rsquo;s secret store. The strip-on-read pattern is the only durable defense until/unless CF fixes their input field. <strong>Touched</strong>: <code>src/routes/api.integrations.square.oauth-callback.ts</code> (strip + length-log + error-surfacing on token_exchange catch), <code>src/server/emma-scheduler.functions.ts</code> (strip + length-log in initiateSquareOAuth), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes; no wrangler.jsonc change.",
    ],
  },
  {
    version: "v1.35.3",
    date: "June 2026",
    items: [
      "<strong>v1.35.3 &mdash; SQUARE_ENV migrated to wrangler.jsonc. The actual root cause of the production-routing bug surfaced under v1.35.2 diagnosis: SQUARE_ENV was added manually as a Cloudflare-dashboard plaintext var when v1.35.0 first shipped, but every subsequent <code>npm run deploy</code> wiped it because wrangler enforces the <code>vars</code> block from <code>wrangler.jsonc</code> as the authoritative plaintext-var list &mdash; anything in the dashboard but not the config file gets stripped.</strong> v1.35.1 deploy and v1.35.2 deploy each wiped SQUARE_ENV. At runtime <code>process.env.SQUARE_ENV</code> returned <code>undefined</code>, the strict-equality check defaulted to the production branch, and the OAuth URL routed against <code>app.squareup.com</code> using SQUARE_APP_ID (production credentials) against Grasshopper&rsquo;s real Rejuv Skin Spa production Square account &mdash; not the sandbox test account. v1.35.2&rsquo;s whitespace-contamination hypothesis was wrong; the var was simply missing entirely each time. <strong>Real fix</strong>: added <code>&quot;SQUARE_ENV&quot;: &quot;sandbox&quot;</code> to the <code>vars</code> block in <code>wrangler.jsonc</code>. Now wrangler deploys push SQUARE_ENV from config every time + it never disappears. <strong>v1.35.2&rsquo;s <code>resolveSquareEnv()</code> hardening still stands</strong> &mdash; defensive trim + lowercase remains the right pattern for ANY runtime config string-equality check, and now-tested catches both whitespace contamination AND case variation should either ever surface in the future. <strong>Architectural learning banked</strong>: Cloudflare Worker plaintext vars added via dashboard UI are EPHEMERAL relative to wrangler deploys. Only secrets (set via <code>wrangler secret put</code>) persist across deploys. Any plaintext config that needs to survive deploys MUST live in <code>wrangler.jsonc</code>. This pattern needs to land on every future scheduler env-var (MINDBODY_ENV, BOULEVARD_ENV, etc.) and every cron-trigger config var. The deploy-bindings printout (visible in <code>npm run deploy</code> tail) lists only vars from wrangler.jsonc &mdash; a fast sanity-check that a new plaintext var has been added to config-side, not dashboard-side. <strong>Touched</strong>: <code>wrangler.jsonc</code> (1 line added to vars), <code>src/lib/changelog.ts</code> (this entry). No code change; no migration; no Worker secret change. <strong>Companion cleanup owed (manual)</strong>: the v1.35.0 first OAuth attempt established a production-side grant on Grasshopper&rsquo;s real Rejuv Square account (visible at <code>app.squareup.com/dashboard/apps/my-applications</code> as &lsquo;Refill &middot; Connected 18 minutes ago&rsquo;). That grant persists on Square&rsquo;s server even after Refill&rsquo;s local Disconnect &mdash; Grasshopper needs to click Disconnect App on Square&rsquo;s production dashboard to fully revoke it.",
    ],
  },
  {
    version: "v1.35.2",
    date: "June 2026",
    items: [
      "<strong>v1.35.2 &mdash; Square env routing hardening + diagnostic logging. Second live sandbox test attempt under v1.35.1 surfaced a NEW catch BEFORE Grasshopper clicked Allow: the Square OAuth consent screen rendered against PRODUCTION host (<code>app.squareup.com</code>) instead of sandbox (<code>app.squareupsandbox.com</code>), with the PRODUCTION client_id and the OLD 7-scope set, against his REAL Rejuv Skin Spa production Square account.</strong> Had he clicked Allow, Refill would have been authorized against the real production calendar, granting real appointment-write access for a sandbox debug. <strong>Root cause</strong>: <code>process.env.SQUARE_ENV</code> was being read at runtime as something OTHER than the literal string <code>&quot;sandbox&quot;</code> — almost certainly the same whitespace-contamination pattern Cloudflare&rsquo;s secret input field demonstrated on the NAME side of the secrets earlier in the v1.35.0 deploy chain (delete + retype was the workaround). This time the contamination was on the VALUE side of the SQUARE_ENV plaintext var. My code did strict equality (<code>process.env.SQUARE_ENV === &quot;sandbox&quot;</code>) at 7 separate call sites; any whitespace silently routed to production via the default branch. <strong>Fix</strong>: extracted a single source of truth <code>resolveSquareEnv()</code> helper in <code>src/lib/schedulers/square.ts</code> that does <code>(process.env.SQUARE_ENV ?? &quot;&quot;).trim().toLowerCase() === &quot;sandbox&quot;</code> &mdash; defensive against leading/trailing whitespace AND case variation. All 7 prior call sites (initiateSquareOAuth, disconnect dispatch, resync dispatch, OAuth callback, webhook receiver, rescue claim writeback, withFreshSquareToken) now route through the single helper. <strong>Diagnostic logging added</strong>: oauth-start + oauth-callback both <code>console.log</code> the raw env-var contents alongside the resolved value, so Worker logs surface whether sandbox routing actually engages on next live test. <strong>Architectural learning banked</strong>: Cloudflare&rsquo;s secret/plaintext input field can silently contaminate stored values with whitespace; defensive trim is required on ANY runtime config string-equality check. Same pattern needs to land on every future scheduler env-var (Mindbody, Boulevard, etc.) and probably every cron-trigger env-var that does <code>process.env.X === &quot;something&quot;</code> across the codebase. <strong>Touched</strong>: <code>src/lib/schedulers/square.ts</code> (new <code>resolveSquareEnv()</code> export), <code>src/server/emma-scheduler.functions.ts</code> (4 call sites + import + diagnostic log), <code>src/routes/api.integrations.square.oauth-callback.ts</code> (1 call site + diagnostic log), <code>src/routes/api.webhooks.scheduler.square.ts</code> (1 call site), <code>src/server/emma-rescue.functions.ts</code> (1 call site in dynamic import block), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Worker secret changes. <strong>What this does NOT fix yet</strong>: the Rejuv Square production-side grant already exists on Square&rsquo;s server even though Grasshopper clicked Deny on the consent screen tonight. Should the v1.35.1 first-attempt callback have hit production Square too? Possibly &mdash; need to check whether Refill silently authorized against PROD on the first attempt where the user clicked Allow. The connection_row from that flow has merchant_id <code>{TBD}</code> &mdash; if it begins with the production format vs sandbox, the answer is yes and a PROD-side revoke is also owed.",
    ],
  },
  {
    version: "v1.35.1",
    date: "June 2026",
    items: [
      "<strong>v1.35.1 &mdash; Square OAuth scopes fix. First live sandbox test of v1.35.0 surfaced the gap immediately: OAuth round-trip itself completed (Rejuv Skin Spa LLC authorized, token issued, connection row upserted with merchant_id) but the subsequent webhook subscription POST failed 403 INSUFFICIENT_SCOPES with the structured error <code>The merchant must authorize your application for the following scopes: DEVELOPER_APPLICATION_WEBHOOKS_WRITE</code>.</strong> Spec gap: my <code>SQUARE_OAUTH_SCOPES</code> list in <code>src/lib/schedulers/square.ts</code> had APPOINTMENTS_* + CUSTOMERS_* + MERCHANT_PROFILE_READ but treated webhook subscription as a default-included capability. It isn&rsquo;t &mdash; Square treats developer-app webhook lifecycle (create/list/delete subscription) as a separately-scoped operation requiring explicit grant. <strong>Fix</strong>: added <code>DEVELOPER_APPLICATION_WEBHOOKS_WRITE</code> + <code>DEVELOPER_APPLICATION_WEBHOOKS_READ</code> to the requested scope set. WRITE is the strict requirement (passes the subscription POST); READ added defensively in case a future ship needs to inspect existing subscriptions (e.g. orphan teardown by URL match like Acuity&rsquo;s pattern, or pre-create idempotency check). <strong>Architectural note</strong>: the error-surfacing path in v1.35.0 worked exactly as designed &mdash; Square&rsquo;s 403 with structured error bubbled cleanly into <code>emma_scheduler_connections.last_error</code> with the exact INSUFFICIENT_SCOPES message + the named missing scope; settings page rendered <em>Square Appointments &middot; Connection error &middot; reconnect required</em> + the full error string verbatim; no silent failure anywhere. The v0.1 spec&rsquo;s &ldquo;fail-degraded with explicit user-facing diagnostics&rdquo; pattern held under first contact with reality. <strong>Reconnect required after this deploy</strong> &mdash; the existing Rejuv connection has the OLD scope set on its already-issued token. To pick up the new scopes the spa must Disconnect (clears the bad-scoped token + connection row + best-effort tears down whatever subscription exists, fail-open on the latter) + Connect Square again. New OAuth URL will request the expanded scope set; Square re-prompts the merchant to authorize including the new scope; callback completes the webhook subscription POST this time. <strong>Touched</strong>: <code>src/lib/schedulers/square.ts</code> (2 new entries in SQUARE_OAUTH_SCOPES + updated comment), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies; no Cloudflare Worker secret changes. <strong>Strategic anchor</strong>: per [[feedback-validate-against-real-exports]] this is exactly the value real-test-vs-spec catches surface &mdash; synthetic spec inspection of Square&rsquo;s docs wouldn&rsquo;t have caught that webhook subscription is scope-gated separately from APPOINTMENTS_*. First-touch live test caught it at v1.35.0 + 1 hour, not at v1.35.0 + a week of post-deploy silence. The error-surfacing architecture made the diagnosis trivial &mdash; one screenshot of the settings card included the exact scope name to add.",
    ],
  },
  {
    version: "v1.35.0",
    date: "June 2026",
    items: [
      "<strong>v1.35.0 &mdash; Square Appointments as Refill&rsquo;s second live scheduler integration. Closes the 4-platform multi-scheduler thesis Phase 1 by hardening the connector substrate against its first divergence axis (single-global webhook URL + tenant-routing-via-payload pattern). Pre-ship: Acuity-only since v381; sales conversations stalled on &lsquo;do you support [other scheduler]?&rsquo; Post-ship: Square is the second platform live, on the same <code>emma_scheduler_connections</code> substrate, with platform-agnostic dispatch through claim writeback.</strong> <strong>Substrate divergences Square absorbs (vs Acuity-shaped today)</strong>: (1) <strong>Single global webhook URL</strong> &mdash; <code>/api/webhooks/scheduler/square</code> with no per-spa secret in the path; tenant routing happens via <code>merchant_id</code> in the payload, looked up against <code>(platform=&quot;square&quot;, platform_account_id)</code>. Mindbody / Boulevard will reuse this pattern when they ship. (2) <strong>HMAC verification scheme</strong> &mdash; SHA-256 over <code>(notification_url + raw_body)</code> concatenation, base64-encoded in <code>x-square-hmacsha256-signature</code>. Unlike Acuity&rsquo;s advisory-mode signature, Square&rsquo;s scheme is well-documented + consistent so verification is HARD-reject on mismatch. (3) <strong>30-day token + refresh flow</strong> &mdash; new <code>withFreshSquareToken</code> helper in emma-scheduler.functions.ts auto-refreshes any token within 3 days of expiry on every webhook + writeback call. (4) <strong>Subscription resource ID lifecycle</strong> &mdash; Square issues a subscription_id at create-time + requires it for delete; new <code>webhook_subscription_id</code> column on emma_scheduler_connections stores it; <code>webhook_secret</code> column overloaded to hold Square&rsquo;s per-subscription signature_key (kept the column name; Acuity rows continue using it as URL-path secret). (5) <strong>Tier-gate detection</strong> &mdash; first-sync probes <code>GET /v2/bookings</code> and surfaces &lsquo;Square Free tier detected&rsquo; on the settings page if the seller can&rsquo;t write. <code>CreateBooking</code> requires Square Appointments Plus or Premium. Free-tier sellers sync + Rescue still fires; only the claim writeback degrades with a banner explaining the upgrade need (NOT a hard disconnect). <strong>Files added</strong>: <code>src/lib/schedulers/square.ts</code> (~500 LOC connector mirroring acuity.ts shape: OAuth helpers, GET/POST API client, getSquareMerchant + probeSquareBookingsTier + listSquareBookings + getSquareBooking + createSquareBooking + upsertSquareCustomer + createSquareWebhookSubscription + deleteSquareWebhookSubscription + verifySquareWebhookSignature + parseSquareWebhookBody + squareStatusToRefillStatus); <code>src/routes/api.integrations.square.oauth-callback.ts</code> (~280 LOC OAuth callback: 5-min code exchange, connection upsert, webhook subscription registration, tier probe, backfill, status finalization &mdash; structural mirror of api.integrations.acuity.oauth-callback.ts); <code>src/routes/api.webhooks.scheduler.square.ts</code> (~330 LOC receiver: HMAC verification, merchant_id lookup, booking resolution via embedded-or-GET, upsert, trigger graph &mdash; same trigger-graph fanout as Acuity receiver into rescue dispatch + reliability recompute); <code>supabase/migrations/20260625000000_v1_35_0_square_webhook_subscription.sql</code> (adds <code>webhook_subscription_id</code> column nullable). <strong>Files extended</strong>: <code>src/server/emma-scheduler.functions.ts</code> &mdash; new <code>initiateSquareOAuth</code> server fn (mirror of initiateAcuityOAuth with sandbox/prod env switch); Square dispatch in <code>disconnectScheduler</code> (calls deleteSquareWebhookSubscription with stored subscription_id; fail-open if token revoked); Square dispatch in <code>resyncSchedulerConnection</code> (calls backfillSquareBookings); new <code>backfillSquareBookings</code> helper paginating /v2/bookings with cursor + idempotent upsert on (user_id, external_id, source); new <code>squareBookingToRow</code> + <code>withFreshSquareToken</code> exports. <code>src/server/emma-rescue.functions.ts</code> &mdash; claimRescueSlot writeback dispatch refactored from <code>acuitySource</code> boolean to <code>writeableSource</code> tagged-union ({acuity, square, null}); new Square branch (~120 LOC) that loads the rescue claimer&rsquo;s contact info, runs <code>withFreshSquareToken</code> to ensure token is current, fetches the original booking for locationId + appointmentSegments, find-or-creates the customer via upsertSquareCustomer, posts createSquareBooking with the original slot info + new customerId + idempotency key tied to the offer ID, stamps new external_id, and SPECIFICALLY detects Square Free-tier 403s to emit <code>writeback_failed: tier_blocked (Square Appointments Plus/Premium required)</code> for the Rescue dashboard to render as an upgrade prompt distinct from generic failures. Same fail-open semantics as Acuity v382 &mdash; claim stays committed even on writeback failure; the patient never gets punished for an upstream API issue. <code>src/routes/app.refill.settings.scheduler.tsx</code> &mdash; new Connect Square card in AvailablePlatforms; new <code>connectSquare</code> handler that calls initiateSquareOAuth + window.location.href to the redirect URL; &ldquo;Coming soon&rdquo; section updated (Mindbody / JaneApp / Boulevard remain queued). <strong>What this DOES NOT change</strong>: the Acuity hot path is untouched &mdash; same routes, same connector, same trigger graph. Mindbody / Boulevard / JaneApp specs were banked Jun 2 as v0.3 / v0.1 / TBD respectively but no code lands for them yet. The patient-roster import path is Acuity-specific (importAcuityClients); Square&rsquo;s customer roster is exposed via /v2/customers but the rich field-mapping into knowledge_nodes is queued for a follow-up ship &mdash; the initial backfill seeds emma_appointments without resolving patient_node_id for net-new Square sellers (matches the v381 Acuity baseline). <strong>Prerequisites for live test</strong> (not blocking ship): (1) Square Developer Console signup at developer.squareup.com/apps &mdash; create Refill app + sandbox app; (2) Cloudflare Worker secrets set: SQUARE_APP_ID, SQUARE_APP_SECRET, SQUARE_SANDBOX_APP_ID, SQUARE_SANDBOX_APP_SECRET, SQUARE_ENV (=&quot;sandbox&quot; or &quot;production&quot;); (3) migration applied via Supabase dashboard before first Square OAuth flow fires; (4) redirect URL <code>https://getrefill.app/api/integrations/square/oauth-callback</code> registered on the Square app&rsquo;s OAuth tab. <strong>Strategic anchor</strong>: per [[project-multi-scheduler-thesis]] the substrate is now genuinely platform-agnostic, not Acuity-shaped-pretending-to-be. By the time Mindbody&rsquo;s OAuth client provisioning ticket clears, the abstraction handles divergent webhook routing, divergent token lifecycles, divergent admin-override semantics, and divergent tier-gating &mdash; one connector pattern, four absorbed shapes. Boulevard&rsquo;s GraphQL substrate divergence is the only NEW axis remaining; same connector shape will hold.",
    ],
  },
  {
    version: "v1.34.9.7",
    date: "June 2026",
    items: [
      "<strong>v1.34.9.7 &mdash; Gap close #5: Karen-Mac → Refill iMessage-sent callback. Closes the Recognition loop end-to-end on the v1.34.8 substrate.</strong> Pre-ship: v1.34.8 shipped Dispatch via iMessage but status flipped to <code>dispatched</code> on email-send to Karen&rsquo;s proxy, not on actual draft-creation in Messages.app &mdash; Karen had no way to see which of N allocations actually got drafted vs sat in the proxy inbox unopened. Post-ship: each draft email includes a per-suggestion callback URL the MCP POSTs once after creating the Messages.app draft. Refill flips status from <code>dispatched</code> → <code>sent</code> on hit. <strong>Architecture decision: URL is the auth.</strong> Each suggestion has an unguessable UUID; the callback URL is <code>https://getrefill.app/api/recognition/sent/&lt;suggestionId&gt;</code>. No bearer token in the email body, no rotating tenant key, no HMAC. Worst case if a URL leaks: a third party can flip ONE suggestion&rsquo;s status (annoying but recoverable &mdash; Karen can re-dispatch from the queue). The contents of the suggestion (patient name, message body, etc.) stay private &mdash; the endpoint returns <code>{ok: true}</code> on success, nothing more. <strong>New route</strong>: <code>POST /api/recognition/sent/:suggestionId</code> at <code>src/routes/api.recognition.sent.$suggestionId.ts</code>. Validates UUID shape, looks up the suggestion as a knowledge_nodes row with node_type=&lsquo;allocation_suggestion&rsquo;, refuses to advance from any status other than &lsquo;dispatched&rsquo; (409 if pending/dismissed/already-sent &mdash; except &lsquo;sent&rsquo; → 200 with alreadySent=true for retry-idempotency), flips status to &lsquo;sent&rsquo;, returns minimal JSON. Also handles GET for human-debug convenience (Grasshopper testing in browser; returns text/plain). Service-role supabase client, no tenant gating in the URL itself &mdash; the UUID + node_type compound is unguessable enough for v1. <strong>Status union extended</strong>: <code>pending | confirmed | dismissed | dispatched | sent</code>. AllocationSuggestion type updated in both the engine module + the SuggestionAttachments type used by the persistence path. <strong>Dispatch email rewire</strong>: <code>dispatchAllocationBatch</code> in <code>refill-recognition-allocation.functions.ts</code> now composes the callback URL via <code>REFILL_PUBLIC_ORIGIN env (default https://getrefill.app)</code> + per-suggestion id, embeds it in BOTH the HTML draft block (as a styled link with explainer prefix &lsquo;After drafting, POST:&rsquo;) AND the text/plain fallback (matching shape). Updated the top auto-send banner to explain the new callback expectation. <strong>UI</strong> (<code>/app/refill/agents/recognition</code>): suggestion row status pill gains a new &lsquo;Sent&rsquo; variant (filled emerald + CheckCircle2 icon, distinct from Dispatched&rsquo;s outlined-emerald variant). Footer disclaimer rewritten from &lsquo;Three-step flow&rsquo; with the full Pending → Confirmed → Dispatched → Sent progression documented; explicitly notes that the actual user-tap-Send in Messages.app stays out of Refill&rsquo;s observability by design (would require OS-level instrumentation we don&rsquo;t want). <strong>What this DOES NOT ship</strong>: bulk-sent (one suggestion per callback by design &mdash; matches the per-draft MCP call shape); &lsquo;sent&rsquo; → &lsquo;delivered&rsquo; or &lsquo;read&rsquo; downstream states (would need iOS-level instrumentation in Messages.app); retry-detection (if MCP fails midway through a batch and re-runs, idempotency-via-already-sent handles it); HMAC signing on the callback URL (could be added later if abuse appears); tenant rotation of the URL secret (the unguessable UUID is the secret today). <strong>Touched</strong>: <code>src/server/refill-recognition-allocation.functions.ts</code> (status union extended in both AllocationSuggestion type + SuggestionAttachments type via replace_all; callback URL injection into dispatch email HTML + text), <code>src/routes/api.recognition.sent.$suggestionId.ts</code> (new file, ~140 lines), <code>src/routes/app.refill.agents.recognition.tsx</code> (Sent status pill variant + footer disclaimer rewrite to 4-step flow), <code>src/lib/changelog.ts</code> (this entry). No schema change &mdash; allocation_suggestion rows just gain a new valid status value on attachments JSONB. No new dependencies. <strong>Strategic anchor</strong>: per [[project-imessage-mcp-killer-arch]] the loop is now closed end-to-end on real infrastructure. Karen confirms a Recognition allocation, taps Dispatch, the batch lands in her proxy email, MCP drafts each Messages.app conversation + reports back, Refill&rsquo;s queue shows &lsquo;Sent&rsquo;, Karen taps Send. Allergan-spend → rebate units → cohort match → Karen-voice iMessage → blue bubble lands as &lsquo;from Karen&rsquo; not a platform shortcode. Compounding retention flywheel, fully wired.",
    ],
  },
  {
    version: "v1.34.9.6",
    date: "June 2026",
    items: [
      "<strong>v1.34.9.6 &mdash; Gap close #4: Recognition + Rescue engines now respect the soft-hidden flag on patients. Pre-ship: v1.34.9.3 added per-patient Hide (excludes from patient list by default) but the engines still saw hidden patients in their scoring + targeting queries &mdash; meaning a patient Karen explicitly hid could still surface as a Recognition allocation suggestion or get texted a Rescue offer. Post-ship: both engines explicitly skip soft-hidden patients in the same place they already skip banned patients.</strong> <strong>Recognition Allocation engine</strong> (<code>refill-recognition-allocation.functions.ts</code>, <code>runAllocation</code> handler): the cohort classification loop at step 4 was already iterating ptRows and skipping rows with no summary. Added two filters there: <code>if (summary.hidden) continue;</code> + <code>if (summary.banned) continue;</code>. Banned wasn&rsquo;t being filtered before either — banned patients could surface as Recognition candidates which is a worse compliance footgun than the hidden case, so this gap close fixes both at once. Hidden + banned patients are excluded from the spend-decile cutoff calculation too because the cutoff iteration runs after this filter would conceptually apply — left it pre-filter for now since the cutoff is computed over ALL patients with netSpendUsd &gt; 0 (population stat), and excluding hidden from the population denominator would skew the cutoff in spas with many hidden high-spenders. Tradeoff: a hidden top-spender still contributes to the &lsquo;Top 10%&rsquo; cutoff but doesn&rsquo;t get a suggestion. <strong>Rescue dispatch engine</strong> (<code>emma-rescue.functions.ts</code>, the waitlist-candidate selection helper): the existing patientById map at line 180-202 was already tracking <code>banned</code> + <code>optedOut</code> on each waitlist candidate. Added <code>hidden</code> alongside; the fit-filter loop at line 210-220 already had <code>if (p.banned || p.optedOut) continue;</code> &mdash; added <code>if (p.hidden) continue;</code> matching it. The attachments cast was extended to include the hidden field so the read is type-safe. <strong>What this DOES NOT change</strong>: Preshow dispatch (<code>emma-preshow.functions.ts</code>) still sends reminders to hidden patients who have already-booked appointments. Rationale: hide is forward-looking outreach exclusion (&lsquo;don&rsquo;t include them in NEW outreach&rsquo;), not retroactive engagement cancellation. If a patient is already on the books, the spa has time + product reserved &mdash; killing the reminder mid-flight risks a no-show on a slot Karen needed; the patient detail page&rsquo;s explicit banned + opted-out gates already handle the harder compliance cases. <strong>What this also DOES NOT change</strong>: queryside filtering. Both engines load all patient rows from knowledge_nodes and filter in-memory rather than adding a JSONB attachments-&gt;hidden WHERE clause to the SELECT. The in-memory pass is cleaner (matches the already-existing banned filter pattern), keeps the SELECTs simple, and a tenant with 50,000 patients still iterates in &lt; 30ms. If volume ever justifies pushing the filter down to SQL, partial index + WHERE clause would be the move. <strong>Touched</strong>: <code>src/server/refill-recognition-allocation.functions.ts</code> (hidden + banned skip in classification loop), <code>src/server/emma-rescue.functions.ts</code> (hidden in patientById map + skip in fit-filter loop), <code>src/lib/changelog.ts</code> (this entry). No schema change; no new dependencies. <strong>Strategic anchor</strong>: per [[feedback-debug-routine]] the engine + UI now agree on what &lsquo;hidden&rsquo; means (excluded from active flows), closing the inconsistency where Karen would hide a patient on the list page but still see them in allocation suggestions or get rescue offers texted to them.",
    ],
  },
  {
    version: "v1.34.9.5",
    date: "June 2026",
    items: [
      "<strong>v1.34.9.5 &mdash; Gap close #3: Attribution settings now actually reach dispatch. Pre-ship: <code>/app/refill/agents/attribution</code> let Karen set window-hours / auto-confirm-threshold / master-toggle, but emma-attribution.functions.ts dispatch ignored them and used a hardcoded 14-day window + always-auto-confirm. Karen could move sliders all day; reconcile didn&rsquo;t care.</strong> The v1.34.7 changelog explicitly said &lsquo;SURFACE first, enforcement next so we don&rsquo;t silently change behavior on existing spas mid-ship&rsquo; — this is the enforcement ship. <strong>What changed</strong>: <code>reconcileRecoveryEventsForUser</code> (the cron-driven reconcile fn that scans unverified recovery events + tries to match each to a patient_transaction in a time window) now loads AttributionSettings from the tenant&rsquo;s knowledge_nodes row at the start of every pass and enforces three of them: (1) <code>enabled</code> &mdash; if false, the whole reconcile bails with zeroes (no QBO matching happens until Karen flips it back on); (2) <code>windowHours</code> &mdash; replaces the hardcoded 14 days. Default is 72h (3 days) per the v1.34.7 schema; spas can extend up to 720h (30 days); (3) <code>autoConfirmThresholdUsd</code> &mdash; threshold gate. Below threshold: auto-confirm (current behavior, stamps verified_at + amount + source=&lsquo;qbo&rsquo;); AT or above threshold: write the proposed amount + matched_transaction_id but leave verified_at NULL so Karen reviews on the Recovery surface. Threshold of 0 = always auto-confirm (the pre-v1.34.9.5 behavior). <strong>What this DOESN&rsquo;T enforce</strong>: <code>agentWeights</code> tie-break (Rescue 1.0 / Preshow 0.5 / Post-recovery 0.8) &mdash; deferred. Tie-break only matters when MULTIPLE agents could claim the same appointment, but recordRecoveryEvent is idempotent on appointment_id (first-writer-wins, second writer no-ops). Wiring weights properly requires multi-agent eligibility logic that doesn&rsquo;t exist yet (would need a recovery_event_candidates table or similar). Karen can tune the weight values in the UI but they&rsquo;re cosmetic until that substrate ships. <strong>Return shape extended</strong>: reconcileRecoveryEventsForUser now returns <code>{ matched, scanned, queuedForReview }</code> &mdash; the new third field counts at/above-threshold matches that got proposed data but stayed unverified. Additive change; existing callers&rsquo; destructures of matched + scanned keep working. <strong>Cron sweep</strong> (<code>api.cron.emma-reconcile.ts</code>) propagates the new counter into its summary JSON as <code>queued_for_review</code> so admin observability includes it. <strong>New export</strong>: <code>loadAttributionSettings</code> from refill-attribution-agent.functions.ts (was the private <code>loadSettings</code>) so dispatch can call the same default-fallback loader the UI uses &mdash; single-sourced. <strong>What this means for Karen</strong>: she can now actually tune the engine. Spas with low-volume / high-trust environments leave threshold at 0 and the engine auto-confirms everything (existing behavior). Spas with high-volume / cautious operators set threshold to e.g. $500 and only big-ticket matches get auto-confirmed; everything else queues for manual review on /app/refill/recovery. Pausing master toggle = no reconcile until she turns it back on (useful during EOM accounting close-outs). <strong>Touched</strong>: <code>src/server/refill-attribution-agent.functions.ts</code> (loadSettings → exported as loadAttributionSettings wrapper), <code>src/server/emma-attribution.functions.ts</code> (import loadAttributionSettings + load at reconcile start + bail-if-disabled + windowMs from settings.windowHours + threshold-gated auto-confirm vs queue-for-review + queuedForReview counter), <code>src/routes/api.cron.emma-reconcile.ts</code> (propagate queuedForReview into summary JSON), <code>src/lib/changelog.ts</code> (this entry). No new dependencies; no schema change.",
    ],
  },
  {
    version: "v1.34.9.4",
    date: "June 2026",
    items: [
      "<strong>v1.34.9.4 &mdash; Gap close #1+#2: stale on-screen copy that lied. Two cosmetic ships that say wrong things to Karen got corrected.</strong> Grasshopper called this out during the post-marathon plan review &mdash; the v1.34 arc shipped feature surfaces faster than the explanatory copy could catch up, so the Preshow + Recognition agent pages still had &lsquo;coming soon&rsquo; disclaimers for features that already shipped. <strong>Gap #1: Preshow page footer</strong> at <code>/app/refill/agents/preshow</code> said &lsquo;Patient routing comes in v1.34.3.1. Until then, every patient gets the Default profile&rsquo;s cadence.&rsquo; That copy was true for ~30 minutes during the marathon; v1.34.3.1 shipped patient routing UI on both the patient list (bulk-tag picker &rarr; Preshow profile section) AND the patient detail page (SoftTagsCard inline picker), and dispatch was rewired to read patient.softTags.preshowProfileId. New copy: explains both routing surfaces with a Link to <code>/app/refill/patients</code>, names the bulk flow (&ldquo;select N patients &rarr; Apply soft tag &rarr; Preshow profile&rdquo;), and documents the deleted-profile fallback. <strong>Gap #2: Recognition page disclaimer</strong> at <code>/app/refill/agents/recognition</code> said &lsquo;v1.34.5 surface only. Confirm = &ldquo;I&rsquo;ll reach out personally.&rdquo;&rsquo; That was true at v1.34.5 but v1.34.8 shipped the iMessage MCP dispatch integration; Confirm is no longer the end of the flow. New copy: documents the two-step Confirm &rarr; Dispatch flow with status progression Pending &rarr; Confirmed &rarr; Dispatched, flags the still-deferred Karen-Mac callback for actual-send tracking. Also corrected the file header docstring (was &lsquo;a deferred Confirm &rarr; noted placeholder&rsquo;). <strong>Also fixed</strong>: stale code comment in <code>api.cron.emma-preshow-sweep.ts</code> claiming &lsquo;dispatchPreShowReminder currently always uses default profile until v1.34.3.1 patient-routing UI ships&rsquo; &mdash; updated to reflect the v1.34.3.1 + v1.34.4 actual behavior (reads patient.softTags.preshowProfileId, falls back to is_default; unions per-treatment-type override hours). <strong>What this DOESN&rsquo;T fix</strong>: gap #3 (Attribution settings &rarr; dispatch enforcement, queued v1.34.7.1); gap #4 (Recognition + Rescue engines respecting hidden patients, queued v1.34.9.3.1); gap #5 (Karen-Mac MCP &rarr; Refill callback for actually-sent tracking, architectural). <strong>Touched</strong>: <code>src/routes/app.refill.agents.preshow.tsx</code> (Link import + Patient assignment hint rewrite with inline link), <code>src/routes/app.refill.agents.recognition.tsx</code> (file header docstring rewrite + bottom disclaimer rewrite, swapped AlertTriangle for Sparkles since it&rsquo;s no longer a warning), <code>src/routes/api.cron.emma-preshow-sweep.ts</code> (comment rewrite), <code>src/lib/changelog.ts</code> (this entry). No new dependencies; pure copy + comment fix. <strong>Strategic anchor</strong>: per [[feedback-no-blame-no-glide]] &mdash; the marathon shipped at high velocity AND a coherency tax that lies to the operator is the cost of that velocity. Gap-close ships pay the tax.",
    ],
  },
  {
    version: "v1.34.9.3",
    date: "June 2026",
    items: [
      "<strong>v1.34.9.3 &mdash; Hide affordance on Patient list rows. Mirrors the Catalog Hide UX (v1.34.9.1) but for patients.</strong> Pre-ship: Karen had Banned + Opted-out signals but no way to soft-remove a patient from her active list without nuking the record. Post-ship: per-row Hide button + Show hidden toggle pill in the filter area; hidden patients are excluded from list, rescue, and recognition flows by default. <strong>No migration</strong>: patients live as <code>knowledge_nodes.attachments</code> JSONB, so the new <code>hidden: boolean</code> field rides alongside <code>banned</code> + <code>vip</code> in <code>PatientContactSummary</code> &mdash; same shape, same re-roll preservation path. A sales-CSV re-upload won&rsquo;t unhide patients (the rollup preserves contact-summary fields like vip + banned + softTags by design). <strong>Server side</strong>: <code>PatientSummary</code> + <code>PatientListRow</code> + <code>PatientContactSummary</code> all gain <code>hidden: boolean</code>; <code>hydratePatientListRow</code> + <code>extractContactSummary</code> propagate it; <code>listInput</code> zod gains <code>includeHidden?: boolean</code> (default false &mdash; list fn filters in-memory after hydrate, post-load so the underlying query stays simple); new <code>setPatientHidden</code> server fn mirrors the setPatientSoftTag JSONB-merge pattern (honors admin viewAs via resolveEffectiveUserId, writes back the full PatientSummary with new <code>hidden</code>). <strong>UI</strong>: per-row Hide / Unhide button as a dedicated table column at the right edge of each PatientRow (matches the existing A-list + Waitlist column shape). Hidden rows render at 60% opacity with hover-to-100% reveal. Show hidden toggle pill renders below the soft-tag filter strip (always visible, regardless of whether any tags exist in the tenant); tap to re-fetch with includeHidden=true. <strong>Optimistic UI</strong>: hide flow drops the row from the visible list immediately (or flips opacity if Show hidden is active); on server failure, full re-fetch restores consistent state. <strong>What this DOESN&rsquo;T ship</strong>: bulk hide / unhide via the v1.34.0 bulk-tag selection flow (one-at-a-time only); confirmation dialog before hide (just toasts); rescue + recognition allocation enforcement that respects hidden (engines still see hidden patients in their scoring queries; suggestions skipping hidden patients lands in a follow-on); a per-tenant default for showing-hidden. <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (PatientContactSummary gains hidden field), <code>src/server/patient-ingest.functions.ts</code> (PatientListRow + hydrate + extract + listInput + filter + setPatientHidden fn), <code>src/routes/app.refill.patients.index.tsx</code> (Eye/EyeOff imports + showHidden state + load dep + onToggleHidden handler + Show hidden pill in filter area + Hide column header + per-row Hide button + opacity treatment + onToggleHidden prop threaded through PatientRow), <code>src/lib/changelog.ts</code> (this entry). No migration; no new dependencies.",
    ],
  },
  {
    version: "v1.34.9.2",
    date: "June 2026",
    items: [
      "<strong>v1.34.9.2 &mdash; Recategorize button on Products page. Grasshopper&rsquo;s ask: when CSV-imported products land in the wrong category or with no manufacturer, sweep the catalog through the canonical brand registry the same way Services already does.</strong> Pre-ship: Services had this button (top-right, Wand2 icon) since v1.30.x but Products didn&rsquo;t. Post-ship: matching button on the Products page sweeps every product&rsquo;s <code>brand</code> field through the canonical brand registry, updates both <code>category</code> AND <code>manufacturer</code> where matched, and surfaces a receipt banner with per-product changes. <strong>New server fn</strong> <code>recategorizeProductsFromBrandsFn</code> in refill-catalog.ts (mirrors recategorizeServicesFromBrandsFn but extended): loads canonical brands → iterates all tenant products → looks up each product&rsquo;s brand (fallback to notes) → if matched, computes new category via existing canonicalToProductCategory mapper (services taxonomy → products taxonomy: <code>laser</code>→<code>laser_consumable</code>, <code>facial</code>→<code>other</code>, rest pass-through) AND new manufacturer (only overrides if the canonical brand has one AND it differs from current — explicit manufacturer choices aren&rsquo;t nuked when the registry has none). Returns RecategorizeProductsReceipt with scanned / recategorized / unchanged / unmatched counts + per-change details (old vs new category, old vs new manufacturer, matched brand displayName). <strong>UI</strong>: Recategorize button (Wand2 icon, Loader2 spinner while busy) added to PageHeader actions left of Import CSV + Add product. Disabled when no products exist. Receipt banner renders above the category filter strip after a sweep finishes — green for success with collapsible &lsquo;Show N changes&rsquo; details (per-row old→new category + manufacturer), red for errors. Dismissable. Auto-fires a list re-fetch after a successful sweep so the changes show up immediately. <strong>What this DOESN&rsquo;T ship</strong>: bulk-edit affordance to mass-fix mis-categorized products that don&rsquo;t match the canonical registry (manual edit per row for now); a way to ADD a new brand to the canonical registry from the Products page (admin route at <code>/app/admin/canonical-brands</code>); confirmation dialog before sweeping (just toasts after). <strong>Touched</strong>: <code>src/server/refill-catalog.ts</code> (RecategorizeProductsReceipt type + recategorizeProductsFromBrandsFn — reuses existing canonicalToProductCategory helper from v1.30.1 CSV importer code), <code>src/routes/app.refill.catalog.products.tsx</code> (Wand2 / CheckCircle2 imports + recategorize state + onRecategorize handler + button in PageHeader actions + receipt banner), <code>src/lib/changelog.ts</code> (this entry). No migration; reuses canonical_brands table from v1.30.0.",
    ],
  },
  {
    version: "v1.34.9.1",
    date: "June 2026",
    items: [
      "<strong>v1.34.9.1 &mdash; Hide affordance on Catalog Products + Services rows. Grasshopper&rsquo;s ask: a soft-delete that lets him remove products/services from the active list without nuking history.</strong> Built on the <code>hidden_at timestamptz</code> column added in the v1.34.9 migration (pasted via Supabase dashboard per [[feedback-migrations-via-dashboard]]). <strong>Server side</strong>: <code>Product.hiddenAt</code> + <code>Service.hiddenAt</code> on the public types and row mappers; <code>readInput</code> gains <code>includeHidden</code> bool (default false &mdash; list fns now filter <code>.is(\"hidden_at\", null)</code> unless caller asks for hidden too); new <code>setProductHiddenFn</code> + <code>setServiceHiddenFn</code> server fns that flip the column (toggling = pass <code>hidden: !current</code>). <strong>UI</strong>: each Product / Service row gets a vertical button stack on the right with Edit + Hide / Unhide (Eye / EyeOff icons). Hidden rows render at 60% opacity with a small &lsquo;hidden&rsquo; pill chip next to the brand name &mdash; visible only when &lsquo;Show hidden&rsquo; is toggled on. <strong>Show hidden toggle</strong>: a pill chip on the right edge of the category-filter strip (ml-auto), pairs with the v1.34.9 category chips visually. Tap to reveal hidden rows; tap again to hide. Re-fires the list query with includeHidden=true so the data refreshes. <strong>Row click</strong>: the per-row main click area (brand/name + notes) still triggers Edit. Button stack on the right is the dedicated action group. <strong>What this DOESN&rsquo;T ship</strong>: bulk hide / unhide (one at a time); confirmation dialog before hide (just toasts); separate &lsquo;Archived&rsquo; URL; per-tenant default for showing-hidden; preservation of hidden state across CSV re-import. <strong>Touched</strong>: <code>src/server/refill-catalog.ts</code> (Product + Service types + ProductRow + ServiceRow shapes + rowToProduct / rowToService + includeHidden in readInput + setHiddenInput zod + setProductHiddenFn + setServiceHiddenFn + filter in list fns), <code>src/routes/app.refill.catalog.products.tsx</code> (Eye/EyeOff imports + showHidden state + onToggleHidden + Show hidden chip + ProductRow refactor with action stack + hidden visual treatment), <code>src/routes/app.refill.catalog.services.tsx</code> (same pattern), <code>src/lib/changelog.ts</code> (this entry). No new dependencies. <strong>Migration applied</strong>: 20260624000000_v1_34_9_catalog_hidden.sql.",
    ],
  },
  {
    version: "v1.34.9",
    date: "June 2026",
    items: [
      "<strong>v1.34.9 &mdash; Category filter chips on Catalog Products + Services pages.</strong> Grasshopper asked for filter for product &amp; service categories while walking. Pure UI on top of the existing <code>category</code> column on both <code>products</code> and <code>services</code> tables &mdash; no migration. <strong>UI</strong>: filter chip strip rendered above the list on each catalog page. One chip per category that has at least one row (so empty categories don&rsquo;t take up space). Multi-select with union semantics &mdash; click multiple chips to show all matching categories. Count badge per chip shows row count in that category. Active chips render emerald-soft; inactive chips muted with hover state. &lsquo;Clear&rsquo; affordance appears when any filter is active. Products supports tox / filler / laser_consumable / skincare / other; Services supports tox / filler / laser / facial / skincare / other (per the existing CHECK constraints). <strong>Filter logic</strong>: extracted into a <code>filteredProducts</code> / <code>filteredServices</code> useMemo before the existing <code>byCategory</code> grouping useMemo, so the per-category group renderer naturally only shows selected categories. Empty filter set = show all. <strong>What this DOESN&rsquo;T ship (queued v1.34.9.1)</strong>: Hide affordance next to Edit on each row (Grasshopper&rsquo;s other ask). Needs a <code>hidden_at timestamptz</code> column added to both tables — migration file written at <code>supabase/migrations/20260624000000_v1_34_9_catalog_hidden.sql</code>, awaits Grasshopper paste via Supabase dashboard per [[feedback-migrations-via-dashboard]]. Once pasted, v1.34.9.1 ships the Hide / Unhide buttons + &lsquo;Show hidden&rsquo; toggle. <strong>Touched</strong>: <code>src/routes/app.refill.catalog.products.tsx</code> (categoryFilter state + toggleCategory + categoryCounts useMemo + filteredProducts useMemo + chip strip render), <code>src/routes/app.refill.catalog.services.tsx</code> (same pattern with ServiceCategory enum), <code>src/lib/changelog.ts</code> (this entry), <code>supabase/migrations/20260624000000_v1_34_9_catalog_hidden.sql</code> (new, queued for paste). No new dependencies.",
    ],
  },
  {
    version: "v1.34.8.3",
    date: "June 2026",
    items: [
      "<strong>v1.34.8.3 &mdash; UX gap: Catalog had a Services route but no UI to navigate to it. Grasshopper caught it walking <code>/app/refill/catalog/products</code>: the nav chip drops you on Products, Services is one route over but the only way to reach it was URL-typing.</strong> Same gap I&rsquo;d already fixed for Recognition (Inventory ↔ Manufacturers tab strip, v1.34.2.2) and Agents (AgentsTabStrip, v1.34.6), but Catalog didn&rsquo;t get the treatment because both routes pre-existed v1.34. <strong>The fix</strong>: matching Products ↔ Services tab strip rendered below the PageHeader on both pages. Same chrome as the Recognition tab strip — emerald underline on active, muted on inactive, hover-state. <strong>Touched</strong>: <code>src/routes/app.refill.catalog.products.tsx</code> (insert tab strip below PageHeader), <code>src/routes/app.refill.catalog.services.tsx</code> (same, with Services active), <code>src/lib/changelog.ts</code> (this entry). Three-line UI fix per route.",
    ],
  },
  {
    version: "v1.34.8.2",
    date: "June 2026",
    items: [
      "<strong>v1.34.8.2 &mdash; Hotfix: &lsquo;No Refill tenant&rsquo; flash error on Recovery + Billing pages during initial load. Grasshopper hit it walking <code>/app/refill/recovery</code> and then <code>/app/billing</code> as admin viewing-as Karen.</strong> Pre-fix: both pages&rsquo; load useEffects fired immediately on first render, BEFORE <code>useTenantMembership()</code> resolved. During the ~1s loading window, <code>viewAsUserId</code> was undefined, so <code>getInvoicePreview</code> ran with no viewAsUserId. Server-side, when admin@refill.platform hits getInvoicePreview without viewAsUserId, refill-billing.ts throws &lsquo;No Refill tenant — finish onboarding before viewing billing.&rsquo; The error rendered briefly in the page&rsquo;s loadError banner, then the membership hook resolved, the real data loaded, and the error went away &mdash; ugly flash. Same exact bug on both pages because they both hit getInvoicePreview. <strong>The fix</strong>: gate each load useEffect on <code>membership.status !== &quot;loading&quot;</code>. This is the same canonical pattern <code>app.refill.catalog.products.tsx</code> + <code>app.refill.catalog.services.tsx</code> already use (with the stricter <code>!== &quot;tenant&quot;</code> variant); Recovery + Billing were the two pages in the Refill shell that missed it. <strong>Sweep verified</strong>: appointments + health pages don&rsquo;t call any no-tenant-throwing fn (refill-billing / refill-catalog / wishlist.functions) so they don&rsquo;t have the same surface bug. The new v1.34.6/v1.34.7/v1.34.5 agent pages I built also already gate via the canonical pattern. <strong>Touched</strong>: <code>src/routes/app.refill.recovery.tsx</code> (useEffect adds membership.status loading guard), <code>src/routes/app.billing.tsx</code> (same), <code>src/lib/changelog.ts</code> (this entry). Two-line code fix.",
    ],
  },
  {
    version: "v1.34.8.1",
    date: "June 2026",
    items: [
      "<strong>v1.34.8.1 &mdash; Hotfix: Rescue Agent surface crashed on load because v1.34.6&rsquo;s getRescueAgentMetrics fn referenced <code>emma_rescue_offers.expires_at</code> &mdash; the actual column is <code>expired_at</code> (past tense; the moment the offer WAS expired). My verb-tense typo on the v1.34.6 ship. Grasshopper hit it on first preview-walk of <code>/app/refill/agents/rescue</code>: &lsquo;Couldn&rsquo;t load rescue metrics: column emma_rescue_offers.expires_at does not exist.&rsquo;</strong> <strong>The fix</strong>: rename <code>expires_at</code> → <code>expired_at</code> in the SELECT + the filter predicate. The filter logic also simplifies because <code>expired_at</code> being non-null IS the expired signal (the DB already stamped it when expiry happened) &mdash; no client-side time comparison needed. <strong>Why this didn&rsquo;t catch in CI</strong>: the generated supabase/types.ts has the correct column name, but my pre-existing &lsquo;column does not exist&rsquo; TS errors (from the v1.34.3 tables that haven&rsquo;t been regenerated into types.ts) had me skim past the type errors during the marathon. The runtime SQL is what hits. Lesson: real-walk a new agent surface on testspaowner BEFORE bundling 3 more ships on top. <strong>Touched</strong>: <code>src/server/refill-rescue-agent.functions.ts</code> (expires_at → expired_at, drop client-side time check), <code>src/lib/changelog.ts</code> (this entry). No migration; no schema change; one-line code fix.",
    ],
  },
  {
    version: "v1.34.8",
    date: "June 2026",
    items: [
      "<strong>v1.34.8 &mdash; iMessage MCP delivery integration. The closing ship that turns Recognition Allocation suggestions into actual blue-bubble sends through Karen&rsquo;s Apple ID. The v1.34 arc is now complete.</strong> Pre-ship: v1.34.5 generated allocation suggestions Karen could Confirm or Dismiss, but Confirm was a local note — no actual outreach. Post-ship: Karen confirms N suggestions, taps &lsquo;Dispatch N via iMessage&rsquo;, Refill composes per-cohort messages with variable substitution, sends a single batch email to the spa&rsquo;s rescue_proxy_email, Karen pastes the email body into Claude Desktop with the iMessage MCP installed, MCP calls <code>draft_imessage(recipient_phone, body)</code> per row, one Messages.app conversation per draft, Karen reviews + taps Send. The blue bubble lands as <em>from Karen</em>, not from a platform shortcode &mdash; the architectural primitive incumbents structurally can&rsquo;t match. <strong>Per-cohort message templates</strong>: <code>AllocationSettings.templates</code> extended with three Karen-voice defaults &mdash; Loyal Vintage (relationship-celebrating, references tenure), Top-Decile Current (impact-celebrating, references the spa name), At-Risk (warm check-in, no pressure). Editable from <code>/app/refill/agents/recognition</code> &lsquo;iMessage templates per cohort&rsquo; section. Variables supported: <code>{{firstName}}</code>, <code>{{brand}}</code>, <code>{{units}}</code>, <code>{{spaName}}</code>, <code>{{manufacturer}}</code>. <strong>New server fn</strong> <code>dispatchAllocationBatch(suggestionIds: uuid[])</code>: loads each confirmed suggestion + the patient&rsquo;s phone from knowledge_nodes, skips patients without phones (returned in result.skipped), loads spa proxy email + spa name, composes per-row drafts using the cohort&rsquo;s template, sends a single batch email via Resend (same infrastructure as v379 waitlist invites &mdash; reuses fromEmail + html template style + auto-send header), flips each dispatched suggestion&rsquo;s status from &lsquo;confirmed&rsquo; → &lsquo;dispatched&rsquo;. Suggestion status union extended with &lsquo;dispatched&rsquo;. Skip reason &lsquo;no rescue_proxy_email set&rsquo; surfaces if the spa hasn&rsquo;t configured one. <strong>UI</strong>: Recognition Agent header gains a conditional &lsquo;Dispatch N via iMessage&rsquo; button (shows count of confirmed-not-yet-dispatched). Templates editor section above the allocation queue with one textarea per cohort + variable-hint footer + Save button. Suggestion row gains a Dispatched status pill (filled emerald, distinct from the Confirmed outline). The full lifecycle is now visible in one place: Pending → Confirmed → Dispatched. <strong>Architecture anchor</strong> per <code>project_imessage_mcp_killer_arch</code>: Karen-Mac MCP setup happened 2026-05-31 &mdash; no client work needed on her side. The Refill → Resend email → Claude Desktop iMessage MCP pipeline matches the v379 waitlist invite pattern proven on real Acuity data 2026-06-01. v1.34.8 extends that pipeline to Recognition. <strong>What this DOESN&rsquo;T ship</strong>: webhook callback from Karen-Mac to mark which drafts were actually sent vs deleted (status flips to &lsquo;dispatched&rsquo; on email-send, not on iMessage-send &mdash; the latter would need a Karen-Mac → Refill callback that&rsquo;s deferred); per-patient template override (uses cohort template uniformly); A/B template variants per cohort with performance tracking; auto-resend if a draft was deleted unsent; opt-out tracking back to the patient knowledge_node (currently the Refill opt-out flow stays separate); per-brand template variants. <strong>Touched</strong>: <code>src/server/refill-recognition-allocation.functions.ts</code> (AllocationSettings.templates field + dispatchAllocationBatch fn + suggestion status &lsquo;dispatched&rsquo;), <code>src/routes/app.refill.agents.recognition.tsx</code> (Send/MessageSquareText icon imports + draftTpls state + saveTemplates + dispatchConfirmed handlers + templates editor section + Dispatch CTA in PageHeader actions + Dispatched status pill), <code>src/lib/changelog.ts</code> (this entry). No migration; reuses existing knowledge_nodes pattern for settings + reuses Resend infrastructure. <strong>Strategic anchor</strong>: per [[project-recognition-allocation-engine-spec]] and [[project-imessage-mcp-killer-arch]] this closes the Recognition loop. Allergan-spend → rebate units → Recognition Agent allocates to right cohort → Karen-voice iMessage drafts → blue-bubble send → patient feels recognized → continues buying Allergan. The compounding flywheel is now end-to-end on real infrastructure.",
    ],
  },
  {
    version: "v1.34.5",
    date: "June 2026",
    items: [
      "<strong>v1.34.5 &mdash; Recognition Allocation Engine. The 4th Refill agent, the moat ship of the v1.34 arc.</strong> Per <code>project_recognition_allocation_engine_spec</code>: Recognition mechanics select silently → patients discover they were chosen → spas reinforce existing good behavior. Architecturally incumbent-uncatchable because the delivery primitive (Karen-Mac iMessage) requires owner-identity messaging that Vagaro/Mindbody/Boulevard structurally can&rsquo;t do. <strong>What ships</strong>: the engine that scores patients into three cohorts (Loyal Vintage / Top-Decile Current / At-Risk Overlay), splits available rebate inventory across them per Karen&rsquo;s 40/40/20 default (Karen-adjustable per period), and queues allocation suggestions Karen confirms one-by-one. <strong>New server fn module</strong> <code>src/server/refill-recognition-allocation.functions.ts</code> (~450 lines): <code>getAllocationSettings</code> / <code>updateAllocationSettings</code> (cohort splits + cooldown + manual-confirm gate, persisted via knowledge_nodes pattern); <code>listAllocationSuggestions</code> (with status pending / confirmed / dismissed); <code>confirmAllocationSuggestion</code> (decrements rebate_inventory_units.units_deployed on confirm); <code>dismissAllocationSuggestion</code>; <code>runAllocation</code> (the engine itself). <strong>Engine flow</strong> (runAllocation): (1) load available rebate_inventory_units (units_total − units_deployed > 0) + join brand/manufacturer metadata from knowledge_nodes. (2) Load all patient knowledge_nodes (5000-row cap; tenant-scoped). (3) Compute spend-decile cutoff (90th percentile of netSpendUsd > 0). (4) Score every patient → classify into ONE cohort: At-Risk if lastVisit 60-180d ago AND spend > 0; Top-Decile Current if netSpend ≥ cutoff AND lastVisit < 180d; Loyal Vintage if first-visit > 365d ago AND lastVisit < 180d AND totalVisits ≥ 4; else excluded. (5) Drop patients with a confirmed suggestion within cooldown window (12mo default). (6) Split total available units 40/40/20 (or per Karen&rsquo;s split) into per-cohort targets. (7) Per cohort, take top-N by score, greedy-allocate one unit per patient from the brand with most remaining inventory. (8) Persist as knowledge_nodes rows (node_type=&lsquo;allocation_suggestion&rsquo;, attachments hold the full suggestion shape including patient + cohort + brand + manufacturer + units + score + rationale + status). <strong>Operator UI</strong> at <code>/app/refill/agents/recognition</code>: PageHeader with &lsquo;Run allocation&rsquo; CTA + AgentsTabStrip (now 4 tabs: Preshow / Rescue / Attribution / Recognition). &lsquo;Recognition, not discounts&rsquo; explainer banner (frames the engine&rsquo;s philosophy; links to Inventory + Manufacturers). Pending-allocations overview card with 3-cohort grid (count + cohort description). Cohort split + cooldown editor (4 number inputs with Save button + sum-to-100 client-side validation). Allocation queue list (latest 100, with per-row patient link + cohort chip + brand+manufacturer pill + score + rationale + Confirm/Dismiss buttons; row-state changes inline after action). v1.34.5 disclaimer card flagging delivery integration lands in v1.34.x. <strong>AgentsTabStrip</strong> extended to 4 tabs with Award icon for Recognition; existing Preshow/Rescue/Attribution pages render the strip in the same place so cross-nav remains clean. <strong>Recognition Inventory + Manufacturers</strong>: still accessible via the dedicated Recognition nav chip (substrate); the Agent surface (engine) is reached via the Agents nav chip + Recognition tab. Two different mental models, two different starting points &mdash; Karen lands wherever matches her intent. <strong>What this DOESN&rsquo;T ship (queued)</strong>: ACTUAL iMessage delivery via Karen-Mac MCP bridge (v1.34.x next &mdash; until then Confirm = &ldquo;I&rsquo;ll reach out personally&rdquo;); nightly cron that auto-runs allocation (Karen taps Run allocation manually for now); message templating per cohort (each suggestion has a rationale string Karen reads, no auto-composed SMS yet); per-brand affinity inference (manual brand list from Manufacturer profile flows through); outcome tracking + cross-spa benchmarking (spec §5/§6, future ship); sparkline + before/after performance chart (needs 30d+ of post-Allocation-era data); milestone-aware cooldown overrides (10yr anniversary, post-recovery, birthday — currently 12mo flat); per-treatment cooldown granularity. <strong>Touched</strong>: <code>src/server/refill-recognition-allocation.functions.ts</code> (new, ~450 lines), <code>src/routes/app.refill.agents.recognition.tsx</code> (new, ~470 lines), <code>src/components/refill/AgentsTabStrip.tsx</code> (Award icon import + Recognition tab), <code>src/lib/changelog.ts</code> (this entry). No migration; no schema; no new dependencies. Builds entirely on substrate primitives from v1.34.2 (rebate_inventory_units), v1.34.2.2 (manufacturer_profile), and the knowledge_nodes patient summary primitive that&rsquo;s existed since v341 patient architecture. <strong>Strategic anchor</strong>: per [[project-recognition-allocation-engine-spec]] this is the engine. Allergan-spend → higher Alle for Business rebate tier → larger inventory next quarter → more patients recognized → recognized patients continue buying Allergan → spa Allergan-spend ↑. Same compounding incumbents have, pointed at retention instead of acquisition. v1.34.x closes the delivery loop.",
    ],
  },
  {
    version: "v1.34.2.2",
    date: "June 2026",
    items: [
      "<strong>v1.34.2.2 &mdash; Recognition Manufacturer Profile + AI extraction. The substrate ship that unblocks Recognition Allocation (v1.34.5). Renamed from v1.34.2.1 mid-arc after collision check &mdash; v1.34.2.1 was already taken by this morning&rsquo;s PersonaSwitcher revert hotfix.</strong> Per <code>project_recognition_allocation_engine_spec</code> each manufacturer (Allergan, Galderma, Merz, Revance, …) has a structured loyalty program: tier names, annual spend thresholds, units rebated per tier, eligible brands, cooldown windows. v1.34.5&rsquo;s Allocation engine reads these profiles to know what reward inventory the spa can earn at which spend level. Pre-ship: profiles didn&rsquo;t exist as a data primitive. Karen would have to enter every manufacturer&rsquo;s program structure by hand. Post-ship: Karen pastes a program doc (email, PDF text, web page) into <code>/app/refill/recognition/manufacturers</code> and Refill extracts the full tier structure via Claude in ~10 seconds. <strong>Storage</strong>: <code>knowledge_nodes</code> row with <code>node_type='manufacturer_profile'</code>, <code>context='recognition'</code>, <code>lookup_key=lowercased manufacturer name</code>, <code>attachments=ManufacturerProfile JSONB</code>. Zero migration cost; tenant scoping via existing user_id filter. <strong>New server fn module</strong> <code>src/server/refill-manufacturer-profile.functions.ts</code> (~360 lines): <code>listManufacturerProfiles</code>, <code>getManufacturerProfile</code>, <code>upsertManufacturerProfile</code>, <code>deleteManufacturerProfile</code>, <code>aiExtractManufacturerProfile</code>. All five honor admin viewAs via resolveEffectiveUserId. <strong>AI extraction</strong>: calls <code>claude-sonnet-4-6</code> via the existing @anthropic-ai/sdk infrastructure (process.env.ANTHROPIC_API_KEY, same secret v1.34.0&rsquo;s Wishlist AI uses). System prompt is a strict JSON extractor with explicit schema rules &mdash; manufacturer (lowercase) / displayName / programs[{name, period, tiers[{name, thresholdUsd, unitsRebated, notes}], brands[], cooldownMonths}] / notes. Tolerates markdown fences. zod-validates the returned shape before passing to UI; throws clear error if AI returns malformed JSON or empty programs. <strong>UI</strong> at <code>/app/refill/recognition/manufacturers</code>: PageHeader with Recognition eyebrow + Add via AI CTA. Inline tab-strip between Inventory and Manufacturers (mirrors Recognition Inventory page). Empty state shows a friendly explainer + Paste a program doc CTA. Grid of ProfileCard tiles (one per saved manufacturer): display name + updated date, program count + tier count stats, top 6 brand chips with overflow, collapsible details showing each program&rsquo;s name/period + per-tier spend-threshold and units-rebated. Per-card Edit + Delete affordances. <strong>AI Extract modal</strong>: manufacturer-hint input (optional, helps AI when doc doesn&rsquo;t name manufacturer) + textarea for pasted text + Extract button. Post-extract: preview pane shows extracted programs as editable JSON (Karen can fix any extraction errors before saving) + manufacturer/displayName fields + notes. Save button writes via upsertManufacturerProfile; Re-extract button goes back to text input. <strong>Edit modal</strong>: raw JSON edit + display name + notes; full structured form deferred to v1.34.2.x. <strong>Recognition Inventory page</strong> gets the matching tab-strip so Karen can cross-nav. <strong>What this DOESN&rsquo;T ship</strong>: structured per-program / per-tier inline editor (raw-JSON is the fallback for v1.34.2.1); PDF binary upload (text-paste only &mdash; Karen extracts text via Preview.app or copy/paste); admin-curated default profile seeds (e.g., a base Allergan profile shared across all spas &mdash; Karen creates her own copy per spa for now); auto-link of an existing rebate_inventory entry to its corresponding manufacturer profile (Recognition Allocation engine reads both separately in v1.34.5); brand-affinity inference from service taxonomy (manual brand list in v1.34.2.1); diff/version history of profile edits. <strong>Touched</strong>: <code>src/server/refill-manufacturer-profile.functions.ts</code> (new), <code>src/routes/app.refill.recognition.manufacturers.tsx</code> (new), <code>src/routes/app.refill.recognition.inventory.tsx</code> (Link import + tab strip), <code>src/server/refill-attribution-agent.functions.ts</code> (also patches a missing content:'' field on the knowledge_nodes insert from v1.34.7 that would have errored at first-save time), <code>src/lib/changelog.ts</code> (this entry). No migration; no new dependencies beyond the already-in-use @anthropic-ai/sdk. <strong>Strategic anchor</strong>: per [[project-recognition-allocation-engine-spec]] this is the substrate primitive for the Recognition Allocation 4th agent. v1.34.5 reads ManufacturerProfile.programs[].tiers[] to compute &ldquo;you spent $X with Allergan this quarter → you earned Y Botox units at the Diamond tier&rdquo; and then routes those units to the right cohort (Loyal Vintage 40% / Top-Decile Current 40% / At-Risk 20%) per Karen&rsquo;s default split.",
    ],
  },
  {
    version: "v1.34.7",
    date: "June 2026",
    items: [
      "<strong>v1.34.7 &mdash; Attribution Agent operator surface. The 3rd of three Refill agents gets the transparency reframe; Attribution trio is now complete.</strong> Pre-ship: Attribution ran as a black-box decider behind the Recovery surface &mdash; recoveries showed up but Karen had no control over the rules. Post-ship: Karen owns Attribution end-to-end at <code>/app/refill/agents/attribution</code> &mdash; master toggle, attribution window (1-720 hours from outreach to booking), auto-confirm threshold (recoveries under $X auto-confirm; at/above goes to manual review), per-agent tie-break weights (Rescue/Preshow/Post-recovery), and a 7-day performance card (recoveries / revenue / manual confirms / unconfirmed). <strong>Architecture decision: settings live as a knowledge_nodes row</strong> with <code>node_type='attribution_settings'</code> + <code>context='agent_config'</code>, attachments JSONB holding the full settings shape (same pattern v1.31.5 used for custom_tag_definitions). Zero migration cost &mdash; tenant scoping is automatic via the existing user_id filter pattern. Defaults baked into the read fn so spas without a row yet get sane behavior. <strong>New server fn module</strong> <code>src/server/refill-attribution-agent.functions.ts</code>: <code>getAttributionSettings</code> (read with defaults fallback), <code>updateAttributionSettings</code> (zod-validated patch with upsert), <code>getAttributionAgentMetrics</code> (queries emma_recovery_events for recoveries / revenue / manual / unconfirmed over last N days). All three honor admin viewAs. <strong>New route</strong> <code>src/routes/app.refill.agents.attribution.tsx</code>: master-state strip → 7-day performance grid (4 metrics with link to Recovery surface) → attribution window editor → auto-confirm threshold editor → agent tie-break weights (3 fields: Rescue 1.0, Preshow 0.5, Post-recovery 0.8 defaults) → &lsquo;How Attribution works&rsquo; help footer. Toggle writes through immediately; number fields use explicit Save buttons. <strong>Agents trio complete</strong>: Preshow (v1.34.3, full profile substrate) + Rescue (v1.34.6, single-policy + eligible-treatments) + Attribution (v1.34.7, settings as knowledge_node) all live at <code>/app/refill/agents/*</code> with the shared AgentsTabStrip cross-nav. Recognition Allocation lands as the 4th agent in v1.34.5 next. <strong>What this DOESN&rsquo;T ship</strong>: actual enforcement of the new attribution window + threshold + weights in <code>emma-attribution.functions.ts</code> (the dispatch logic still uses its prior hardcoded defaults; v1.34.7.1 will read the settings and apply them &mdash; this ship is the SURFACE first, enforcement next, so we don&rsquo;t silently change behavior on existing spas mid-ship); per-treatment-type attribution rules; per-agent-per-treatment weights; sparkline + before-vs-after performance comparison; auto-suggest threshold based on historical revenue distribution; export attribution audit log. <strong>Touched</strong>: <code>src/server/refill-attribution-agent.functions.ts</code> (new, ~220 lines), <code>src/routes/app.refill.agents.attribution.tsx</code> (new, ~430 lines), <code>src/lib/changelog.ts</code> (this entry). No migration; no schema changes; no new dependencies. <strong>Strategic anchor</strong>: per [[project-wishlist-thesis]] the agent-transparency trio is now closed. Karen controls all three reactive agents; Recognition Allocation (proactive 4th agent) builds on this pattern in v1.34.5.",
    ],
  },
  {
    version: "v1.34.6",
    date: "June 2026",
    items: [
      "<strong>v1.34.6 &mdash; Rescue Agent operator surface. The 2nd of three Refill agents gets the same transparency reframe Preshow got in v1.34.3.</strong> Pre-ship: Refill ran Rescue as a black-box agent &mdash; Karen had no idea when offers fired, how many were claimed, or which treatments were eligible. The dispatch logic was solid (emma-rescue.functions.ts) but the spa-owner control surface didn&rsquo;t exist. Post-ship: Karen owns Rescue end-to-end at <code>/app/refill/agents/rescue</code> &mdash; master toggle, eligible-treatments multi-tag editor, max-concurrent + outreach-window limits, proxy phone + email, and a 7-day rolling performance card (offers sent / claimed / expired / conversion rate). <strong>Architecture decision: Rescue stays SINGLE-POLICY (no profiles)</strong>. Preshow has profiles because cadence routing per cohort matters (Chronic vs Default). Rescue is event-driven (cancellation → offer to waitlist) and per-cohort cadence isn&rsquo;t a meaningful axis &mdash; one policy per spa is correct. The settings live on the existing <code>emma_noshow_policies</code> row (no new table). <strong>New server fn module</strong> <code>src/server/refill-rescue-agent.functions.ts</code>: <code>getRescuePolicy</code> (loads + hydrates the policy row), <code>updateRescuePolicy</code> (zod-validated patch with upsert-style fallback for spas without a policy row yet), <code>getRescueAgentMetrics</code> (queries emma_rescue_offers for offers_sent / claimed / expired / conversion in last N days). All three honor admin viewAs via <code>resolveEffectiveUserId</code>. <strong>New route</strong> <code>src/routes/app.refill.agents.rescue.tsx</code>: mirrors the visual rhythm of the Preshow page but flattened &mdash; no profiles section, so the cards are linear: master-state strip → 7-day performance grid (4 metrics) → eligible-treatments tag editor → limits (max concurrent + outreach window with explicit Save) → proxy contacts (phone + email with explicit Save) → &lsquo;How Rescue works&rsquo; help footer. Toggle + chip operations write through immediately; number + text inputs use explicit Save buttons (typing UX). <strong>New shared component</strong> <code>src/components/refill/AgentsTabStrip.tsx</code>: horizontal sub-nav rendered at the top of every <code>/app/refill/agents/*</code> page so Karen can cross-navigate Preshow ↔ Rescue ↔ Attribution without going back through the nav chip + breadcrumb chain. Active tab gets the emerald-ink underline; tabs lazy-render with the correct active state per page. <strong>Preshow page also gets the tab strip</strong> (parity) &mdash; minimal touch: import + insert below PageHeader. <strong>What this DOESN&rsquo;T ship</strong>: per-cohort Rescue profiles (intentional &mdash; see architecture decision above; can revisit if a real spa has a strong use case); waitlist-segment routing (Rescue offers fire to ALL eligible waitlist members for the cancelled-appointment&rsquo;s treatment-type, no further filtering); per-treatment-type proxy phone or email override (uniform spa-wide); custom Rescue message templates (uses the code-composed default in emma-rescue.functions.ts); auto-detect of which treatments to mark eligible based on historical no-show rates (Karen sets manually); conversion attribution to which message variant (single template). <strong>Touched</strong>: <code>src/server/refill-rescue-agent.functions.ts</code> (new, ~210 lines), <code>src/routes/app.refill.agents.rescue.tsx</code> (new, ~430 lines), <code>src/components/refill/AgentsTabStrip.tsx</code> (new shared component), <code>src/routes/app.refill.agents.preshow.tsx</code> (import + insert AgentsTabStrip below PageHeader), <code>src/lib/changelog.ts</code> (this entry). No migration; no schema changes; uses existing emma_noshow_policies columns + emma_rescue_offers. <strong>Strategic anchor</strong>: per [[project-wishlist-thesis]] this is the 2nd of three agent-transparency ships. Karen controls all three Refill agents; competitors ship black boxes. v1.34.7 closes the trio with Attribution.",
    ],
  },
  {
    version: "v1.34.4",
    date: "June 2026",
    items: [
      "<strong>v1.34.4 &mdash; Per-treatment-type cadence overrides on the Preshow Agent. The last leg of Preshow Agent surface completeness.</strong> Pre-ship: every patient in a profile got the same cadence regardless of treatment type &mdash; Botox (3-month repeat cycle, low show-rate cost) and laser (1-year repeat cycle, high anesthesia prep cost) both got 48/24/3h reminders. Post-ship: Karen can override cadence per treatment-type per profile &mdash; e.g., set <code>botox → 72, 24, 12</code> while everything else uses the profile&rsquo;s spa-wide cadence. The JSONB column <code>cadence_by_treatment_type</code> was reserved on <code>emma_preshow_profiles</code> in the v1.34.3 migration; v1.34.4 wires the UI + dispatch logic on top of it without any schema change. <strong>UI</strong> (<code>/app/refill/agents/preshow</code>): ProfileCard gains a new collapsible expander &lsquo;Per-treatment-type cadence overrides&rsquo; between Tone+Channel and Per-touchpoint messages, with Layers icon. Inline list shows existing overrides as treatment-type pill + array of hour chips + Remove button per row. &lsquo;Add override&rsquo; form: treatment-type text input (case-insensitive, lowercased on save) + comma/space-delimited hours csv input + Add button. Validation: hours 0-8760, dedupe, sort descending. Empty state copy: &lsquo;No overrides yet. Treatments not listed here use the spa-wide cadence above.&rsquo; Help copy: &lsquo;Treatment types match against appointment.treatment_type case-insensitive. Example: botox → 72, 24, 12 overrides the spa-wide cadence for any Botox appointment.&rsquo; <strong>Server fn</strong>: <code>updatePreshowProfile</code> input schema gains optional <code>cadenceByTreatmentType: Record&lt;string, number[]&gt;</code> field. Handler filters out empty arrays before writing (lets the UI clear a single override by sending an empty array). <strong>Dispatch rewire</strong> (<code>src/server/emma-preshow.functions.ts</code>): the resolved profile&rsquo;s SELECT now includes <code>cadence_hours</code> + <code>cadence_by_treatment_type</code> (was just <code>id, tone, channel</code>). After profile resolution but before tone/channel resolution: if <code>profile.cadence_by_treatment_type[apt.treatment_type.toLowerCase().trim()]</code> exists and is non-empty, the dispatch checks if <code>offsetHours</code> is in that array. If not, returns a new skip reason <code>outside_treatment_cadence</code> (added to <code>PreShowSkipReason</code> union) with message &lsquo;Treatment-type override for &lsquo;X&rsquo; doesn&rsquo;t include T-Nh.&rsquo; <strong>Cron rewire</strong> (<code>src/routes/api.cron.emma-preshow-sweep.ts</code>): the per-spa offset-union now includes BOTH each profile&rsquo;s spa-wide <code>cadence_hours</code> AND every value array in each profile&rsquo;s <code>cadence_by_treatment_type</code>. This is the right safety: the sweep catches every appointment that might need a reminder at this offset; <code>dispatchPreShowReminder</code> enforces per-treatment scoping. <strong>What this DOESN&rsquo;T ship</strong>: autocomplete of existing treatment-types from <code>emma_appointments.treatment_type</code> distinct values (Karen types free-text for now); per-treatment-type tone or channel override (uniform within profile); per-treatment-type custom message template (uses the profile&rsquo;s message templates uniformly &mdash; future ship could let overrides have their own templates); treatment-type alias mapping (e.g., &lsquo;tox&rsquo; &harr; &lsquo;botox&rsquo;); per-treatment-type performance counters (the 7-day metrics attribute to profile-level only). <strong>Touched</strong>: <code>src/server/refill-preshow-agent.functions.ts</code> (updateProfileInput zod schema + handler), <code>src/server/emma-preshow.functions.ts</code> (PreShowSkipReason union + profileRow shape + per-treatment override check), <code>src/routes/api.cron.emma-preshow-sweep.ts</code> (union over per-treatment override hours too), <code>src/routes/app.refill.agents.preshow.tsx</code> (Layers icon import + state + handlers + collapsible expander + override list + add form), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change; no new deps. <strong>Strategic anchor</strong>: closes Preshow Agent feature surface completeness &mdash; profile + cadence + per-touchpoint messages + tone + channel + patient routing (v1.34.3.1) + per-treatment override (v1.34.4). v1.34.5 unlocks the Recognition Allocation 4th agent on top of this proven pattern.",
    ],
  },
  {
    version: "v1.34.3.1",
    date: "June 2026",
    items: [
      "<strong>v1.34.3.1 &mdash; Patient routing for the Preshow Agent. The bridge ship that turns v1.34.3&rsquo;s named-profiles substrate into a real Chronic-vs-Default outcome on the spa floor.</strong> Pre-ship: Karen could create a Chronic profile with a 5d/3d/48h/24h/12h cadence at <code>/app/refill/agents/preshow</code>, but every patient still routed to the spa-wide Default profile because no UI existed to assign a patient to a non-default profile. Profiles were a substrate without an outcome. Post-ship: Karen routes patients to ANY profile from two surfaces &mdash; one chip click on the patient detail page (per-patient), one bulk-tag pass on the patient list (per-cohort, up to 2000 at a time). Dispatch reads the per-patient routing on every reminder. <strong>Per-patient detail page picker</strong> (<code>/app/refill/patients/$patientId</code>): SoftTagsCard gains a new &lsquo;Preshow profile&rsquo; row at the top of the seeded six. Same EnumTagRow UX as Income tier / Negotiator / etc. &mdash; tap to expand, chip per profile (Default chip labeled &lsquo;Default &middot; default&rsquo;), Reason field (optional), Save / Clear. Renders only when at least one profile exists (defense-in-depth; v1.34.3 backfill guarantees every spa has one). Value pill shows the live profile name resolved from the loaded profiles list. Edge case: if the routed profile was deleted, dispatch silently falls back to the default profile so a stale routing never blocks a reminder. <strong>Per-cohort bulk picker</strong> (<code>/app/refill/patients</code>): BulkTagPickerModal gains a third section &lsquo;Preshow profile (Agents &middot; routes reminders)&rsquo; between Preset tags and Custom tags. One chip per tenant profile, mutually exclusive, with sub-text &lsquo;Override the spa-wide default for these patients only.&rsquo; Click apply &rarr; bulkApplyPatientSoftTag writes <code>softTags.preshowProfileId = { value: profileId, setByUserId, setAt, reason }</code> across all selected patient rows in one batched in-memory merge (50/batch up to 2000 patients). Toast resolves the profile UUID back to the profile name for the success message (&lsquo;Preshow profile: Chronic applied to 47 patients.&rsquo;). <strong>Type extension</strong>: <code>PatientSoftTags.preshowProfileId?: PatientSoftTagEntry&lt;string&gt; | null</code> &mdash; UUID of the routed profile, full provenance entry shape (setByUserId, setAt, reason) matching the seeded six tags. Stored on knowledge_nodes.attachments.softTags so it follows the existing tenant-scoped read pattern (no new table). <strong>Server fn updates</strong>: <code>setPatientSoftTag</code> discriminated union gains <code>{ key: &lsquo;preshowProfileId&rsquo;, value: z.string().uuid() }</code> branch; <code>clearPatientSoftTag</code> key enum gains &lsquo;preshowProfileId&rsquo;; <code>bulkApplyPatientSoftTag</code> preset.key enum gains &lsquo;preshowProfileId&rsquo; (the in-memory merge code was already generic so no handler change). <strong>Dispatch rewire</strong> (<code>src/server/emma-preshow.functions.ts</code>): the v1.34.3 default-profile lookup at section 3.5 now reads <code>patient.softTags.preshowProfileId?.value</code> first; if set, resolves THAT profile by id; if missing OR the routed profile was deleted, falls back to the is_default=true profile. patientAttachments cast extended to include softTags.preshowProfileId. Same downstream behavior for tone + channel + template lookup &mdash; just sourced from the routed profile instead of always the default. <strong>What this DOESN&rsquo;T ship (queued)</strong>: per-treatment-type cadence overrides (v1.34.4; column reserved on profiles in v1.34.3 migration); auto-detect chronic-rescheduler patients from emma_appointment_status_events history (v1.34.3.x; Karen manually tags in v1.34.3.1); profile-drift visibility on /app/admin/agents (v1.34.3.x); Rescue + Attribution surfaces (v1.34.6 / v1.34.7 mirror Preshow); Recognition Allocation as 4th agent (v1.34.5 per the locked Recognition hybrid framing); email-channel template rendering (SMS-only stays in v1.34.3 / v1.34.3.1). <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (PatientSoftTags type gains preshowProfileId entry), <code>src/server/patient-ingest.functions.ts</code> (softTagWriteInput + softTagClearInput + bulkApplyInput zod schemas all gain preshowProfileId), <code>src/server/emma-preshow.functions.ts</code> (patientAttachments cast extended; dispatch section 3.5 resolves routed-profile-first with default-profile fallback), <code>src/routes/app.refill.patients.$patientId.tsx</code> (listPreshowProfiles loaded alongside getPatientById; SoftTagsCard takes preshowProfiles prop and renders new EnumTagRow at the top of the seeded six), <code>src/routes/app.refill.patients.index.tsx</code> (listPreshowProfiles loaded alongside list/overdue/waitlist/defs; BulkTagPickerModal takes preshowProfiles prop and renders new third section with one chip per profile; bulkApply tag type union widened to accept preshowProfileId key; apply formatter resolves profile name for toast), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change; no new dependencies. <strong>Strategic anchor</strong>: per [[project-wishlist-thesis]] this is the &lsquo;7 lines from substrate to floor&rsquo; ship &mdash; v1.34.3 laid 3 new tables + 9 server fns + 1 full agent surface; v1.34.3.1 adds 7 small edits across 6 files and the substrate becomes a Chronic-vs-Default outcome on Karen&rsquo;s real 1,140-patient roster. Per [[feedback-verify-deploy-after-push]] this ship was deployed via explicit <code>npm run deploy</code> + bundle-hash verification.",
    ],
  },
  {
    version: "v1.34.3",
    date: "June 2026",
    items: [
      "<strong>v1.34.3 &mdash; Agents &amp; overrides: the strategic reframe from engineer-shaped &lsquo;feature flags&rsquo; to operator-shaped &lsquo;agents the spa owner controls.&rsquo;</strong> The biggest UX bet of the v1.34 arc. Pre-ship: Refill ran Preshow / Rescue / Attribution as black-box agents Karen had zero visibility into &mdash; she trusted Refill was doing its job. Post-ship: Karen owns the Preshow agent end-to-end (unlimited cadence touchpoints, multiple named cadence PROFILES for different patient cohorts, per-touchpoint custom messages with variable substitution, tone + channel per profile, performance counters) at <code>/app/refill/agents/preshow</code>. The reframe captures what makes Refill incumbent-uncatchable: Vagaro / Mindbody / Boulevard ship a generic single-reminder toggle with one canned template, structurally incapable of per-spa configuration without breaking multi-tenant economics &mdash; Refill does it natively because per-tenant config has been the substrate since v1.24. <strong>Three new tables</strong>: <code>emma_preshow_profiles</code> (per-spa cadence profiles &mdash; each spa has at least one is_default=true; partial unique index enforces exactly-one-default-per-spa; cadence_hours[] is integer[] so unlimited touchpoints natively; cadence_by_treatment_type JSONB reserved for v1.34.4 per-type cadence overrides; tone + channel per profile), <code>emma_preshow_message_templates</code> (per profile × per offset_hours SMS body with variable substitution &mdash; missing template falls back to code-composed default in emma-preshow.functions.ts), <code>agent_defaults</code> (admin-only global defaults per agent_kind &mdash; preshow / rescue / attribution / recognition; seeded with the prior code-baked preshow defaults). <strong>Backfill in migration</strong>: every existing spa with an emma_noshow_policies row gets a &lsquo;Default&rsquo; profile auto-created mirroring their current preshow_cadence_hours / tone / channel. Zero behavior change for existing spas; cron + dispatch now read from profiles instead of policy columns. <strong>New server fn module</strong> <code>refill-preshow-agent.functions.ts</code> with 9 fns: listPreshowProfiles + listPreshowTemplates (load surface), createPreshowProfile (with optional cloneFromProfileId to copy cadence + templates), updatePreshowProfile (rename, edit cadence, tone, channel), setDefaultPreshowProfile (atomic-ish two-step flip with partial unique index as backstop), deletePreshowProfile (refuses if is_default, cascades templates), upsertPreshowMessageTemplate (per profile×offset), deletePreshowMessageTemplate (revert to code-composed), getAgentDefaults + updateAgentDefaults (admin-only, gated by requireAdmin role check), getPreshowPerformanceMetrics (7-day rolling sent/confirmed/cancelled counters; sparkline deferred to v1.34.3.x). <strong>Dispatch + cron rewired</strong>: emma-preshow.functions.ts dispatchPreShowReminder now loads the default profile, uses profile.tone + profile.channel (fallback to policy if profile missing for defense-in-depth), and looks up custom template for THIS profile × offset_hours &mdash; if found, renders with variable substitution (<code>{{patient.firstName}}</code> / <code>{{appt.dateTime}}</code> / <code>{{appt.time}}</code> / <code>{{appt.treatmentType}}</code> / <code>{{appt.providerName}}</code> / <code>{{spa.name}}</code>) and appends the standard opt-in + STOP footers; if no template, falls back to existing composePreShowSmsBody. Email channel stays code-composed in v1.34.3 (template support there lands in a follow-on). Cron sweep at api.cron.emma-preshow-sweep updated to load UNION of cadence_hours across all profiles per spa (Default + Chronic + custom) and sweep with that set; per-patient profile resolution happens inside dispatch (currently always uses default profile until v1.34.3.1 patient-routing UI ships). <strong>UI at <code>/app/refill/agents/preshow</code></strong>: PageHeader with &lsquo;Agents&rsquo; eyebrow. State strip with bell icon + master on/off toggle (writes preshow_enabled on emma_noshow_policies). What-it-does explainer in Karen&rsquo;s voice with profile counts. Performance card: 7-day rolling sent/confirmed/cancelled counts; sparkline deferred. Profiles section with one card per profile, each with: inline-editable name (click pencil) + (default) badge + Set as default link + Delete button (disabled for default). Cadence editor: chip per offset_hours with X to remove + Add input for new touchpoint (any 0-8760 hours, auto-sorted descending). Tone selector (warm / professional / casual) and Channel selector (auto / SMS / email) as segmented controls. Per-touchpoint messages collapsible expander &mdash; one TemplateEditor per offset showing default-or-customized state, variable chips (insert-on-click), textarea with character counter, Reset to default + Save buttons. Patient assignment hint at bottom flagging that routing lands in v1.34.3.1. <strong>New nav chip</strong>: &lsquo;Agents&rsquo; (Bell icon) between Recognition and Recovery in RefillNav. 9 chips total now; wraps to 2 lines on narrow screens per Grasshopper&rsquo;s 2a call (&lsquo;9 chips is fine on wrap&rsquo;). Dashboard quick-actions grid gains a matching Agents card. <strong>Redirects</strong>: old <code>/app/refill/settings/noshow</code> route file body replaced with <code>&lt;Navigate to=&quot;/app/refill/agents/preshow&quot; replace /&gt;</code>; stale bookmarks land on the new home. Old <code>/app/admin/flags</code> path replaced with redirect to <code>/app/admin/agents</code>; the page logic moved as-is to the new file with Page title relabeled &lsquo;Agents &amp; overrides.&rsquo; AdminNav chip renamed Flags &rarr; Agents; admin index card relabeled to match. <strong>Recognition Engine hybrid framing locked</strong> (per the 2026-06-02 morning call): Inventory + Profile = substrate primitives (already shipped v1.34.2, queued v1.34.2.1 for Profile); Allocation = 4th agent (cohort scoring + match + suggestion &mdash; the part that fires automatically per spec, queued v1.34.3.x). agent_defaults table covers all four agent_kinds so the substrate is forward-compatible. <strong>What this DOESN&rsquo;T ship (queued for v1.34.3.x)</strong>: patient routing UI (bulk-tag picker on patient list + per-patient profile picker on detail page &mdash; v1.34.3.1, very next); per-treatment-type cadence overrides (v1.34.4 per the cadence_by_treatment_type JSONB column reserved in this migration); message variable substitution for EMAIL channel (SMS only in v1.34.3); auto-detect chronic patients from emma_appointment_status_events history (manual soft-tag in v1.34.3.1, auto-detect later); sparkline + before-vs-after performance chart (needs 30d+ of post-Agents-era data); per-tenant config drift visibility table on /app/admin/agents (still showing FLAG_CATALOG view from v1.24.1, augmented with profile-drift view in v1.34.3.x); Rescue + Attribution + Recognition Allocation agent surfaces (mirror Preshow when ready); profile cloning + per-profile performance partition (counts attribute to default profile until routing lands). <strong>Migration</strong>: <code>supabase/migrations/20260623000000_v1_34_3_preshow_agents.sql</code> &mdash; 3 new tables, 6 RLS policies, 4 indexes, defensive constraints, agent_defaults seed, backfill for every existing emma_noshow_policies row. Pasted via Supabase dashboard per [[feedback-migrations-via-dashboard]]. <strong>Strategic anchor</strong>: per [[project-wishlist-thesis]] customer-customizable cadence in minutes &mdash; not weeks of incumbent product-meeting roadmap battles &mdash; is the structural moat. Karen creates a Chronic profile with a 5d/3d/48h/24h/12h cadence today; her wishlist hasn&rsquo;t been opened. <strong>Touched</strong>: <code>supabase/migrations/20260623000000_v1_34_3_preshow_agents.sql</code> (new file), <code>src/server/refill-preshow-agent.functions.ts</code> (new file ~480 lines), <code>src/routes/app.refill.agents.preshow.tsx</code> (new file ~770 lines), <code>src/routes/app.admin.agents.tsx</code> (new file, renamed from app.admin.flags.tsx with PageHeader retitled), <code>src/server/emma-preshow.functions.ts</code> (added renderPreshowTemplate + appendPreshowFooters helpers; dispatchPreShowReminder loads default profile + template, uses profile.tone + profile.channel, renders template if exists), <code>src/routes/api.cron.emma-preshow-sweep.ts</code> (load profiles + union cadence_hours across all profiles per spa), <code>src/components/refill/RefillNav.tsx</code> (RefillNavKey union extended with &lsquo;agents&rsquo;; Agents chip between Recognition and Recovery), <code>src/components/refill/RefillShellChrome.tsx</code> (deriveActiveKey adds /agents prefix), <code>src/components/refill/RefillHome.tsx</code> (Bell icon + Agents card in quick-actions grid), <code>src/components/admin/AdminNav.tsx</code> (Flags chip → Agents; matches both old + new paths during redirect grace period), <code>src/routes/app.admin.index.tsx</code> (admin index card relabeled), <code>src/routes/app.admin.flags.tsx</code> (replaced with redirect), <code>src/routes/app.refill.settings.noshow.tsx</code> (replaced with redirect), <code>src/lib/changelog.ts</code> (this entry). No new dependencies.",
    ],
  },
  {
    version: "v1.34.2.1",
    date: "June 2026",
    items: [
      "<strong>v1.34.2.1 &mdash; Hotfix: PersonaSwitcher revert-to-admin didn&rsquo;t smart-route. Admin switching from Kelly (Rep) or Karen (Spa Owner) back to Admin while on <code>/app/rep</code> or <code>/app/refill</code> stayed on that URL instead of landing in the admin shell &mdash; producing either the &ldquo;Your account isn&rsquo;t set up as a Refill rep yet&rdquo; empty state (RepShell with no rep_accounts row) or the &ldquo;Defaulted to Rejuv Skin Spa&rdquo; banner (RefillShell admin fallback to first-tenant). Grasshopper hit this 2026-06-02 morning walking the persona-switcher flow.</strong> <strong>Root cause</strong>: v1.24.3 generalized smart-routing (<code>pickRedirectForPersonaSwitch</code> in <code>src/lib/admin-view-as.ts</code>) covered the non-admin selection branch but explicitly NOT the revert-to-admin branch. The docstring even called it out: &ldquo;Reverting to admin (caller&rsquo;s own shell) → undefined → stay.&rdquo; The PersonaSwitcher&rsquo;s onChange used <code>setAdminViewAsUserId(null)</code> without computing a redirect target. The intent was &ldquo;stay where you were so admin can keep poking at the surface they were just viewing.&rdquo; In practice, admin has no rep_account and no spa-owner tenant_membership of their own &mdash; the rep + spa-owner shells fall through to empty states, not a coherent admin view. <strong>The fix</strong>: PersonaSwitcher.onChange revert branch now calls <code>pickRedirectForPersonaSwitch(window.location.pathname, &quot;admin&quot;)</code> exactly the same way the non-admin selection branch already does. The function logic ALREADY returned &ldquo;/app&rdquo; when URL shell-kind &ne; admin (the predicate was <code>urlKind !== target</code> with target=admin) &mdash; only the caller needed to invoke it. Three behaviors result, all correct: (a) admin reverts from <code>/app/admin/*</code> → urlKind=admin, target=admin, function returns undefined → setAdminViewAsUserId(null) reloads in place → admin lands back on the same admin tool they were viewing. (b) admin reverts from <code>/app/refill</code> or <code>/app/billing</code> → urlKind=spa-owner ≠ admin → function returns &ldquo;/app&rdquo; → setAdminViewAsUserId(null, &quot;/app&quot;) → /app auto-dispatches to admin shell home (/app/admin). (c) admin reverts from <code>/app/rep</code> → urlKind=rep ≠ admin → same &ldquo;/app&rdquo; redirect → admin shell home. <strong>What this DOESN&rsquo;T fix</strong>: the v1.24.4-era three-personas allowlist gate is unchanged (PersonaSwitcher still filters to caller + DEMO_PERSONA_EMAILS + ADMIN_VIEWABLE_REAL_EMAILS). The &lsquo;Defaulted to&hellip;&rsquo; v1.26.20 amber banner still shows correctly when admin lands on /app/refill via auto-dispatch without an explicit pick &mdash; that&rsquo;s a separate but adjacent path. The fix here is strictly the explicit-pick-revert flow. <strong>Architectural note</strong>: docstring on <code>pickRedirectForPersonaSwitch</code> updated &mdash; the old &ldquo;Reverting to admin → undefined → stay&rdquo; bullet was misleading (true only when admin is already on an admin URL). Now reads correctly. <strong>Touched</strong>: <code>src/components/admin/PersonaSwitcher.tsx</code> (revert branch computes redirectTo via pickRedirectForPersonaSwitch with target=&lsquo;admin&rsquo;), <code>src/lib/admin-view-as.ts</code> (docstring corrected to match actual behavior), <code>src/lib/changelog.ts</code> (this entry). No migration; no schema; no new deps. Tiny diff, big quality-of-life fix on the persona-switching flow Grasshopper uses every walk-through.",
    ],
  },
  {
    version: "v1.34.2",
    date: "June 2026",
    items: [
      "<strong>v1.34.2 &mdash; Recognition Allocation Engine: Pool Entry substrate. First surface of the strategic moat ship the engine no incumbent can structurally match.</strong> Per the 2026-06-02 scout of the manufacturer-rebate substrate (Galderma / Evolus / Allergan portal exports vs off-the-record sample-bridge dynamics) and Grasshopper&rsquo;s &ldquo;THIS IS THE NITTY GRITTY OF THE BUSINESS THAT THE MONO PLATFORMS DON&rsquo;T/CAN&rsquo;T COMPREHEND&rdquo; framing, this ship lays down the inventory substrate that v1.34.3 will score + allocate against and v1.34.4 will deliver via iMessage MCP. <strong>Documentation wall held at schema level</strong>: two distinct knowledge_node types ensure that the off-the-record portion of rebate sourcing can NEVER be reverse-engineered from the database, even by accident. (1) <code>rebate_inventory_documented</code> holds portal-issued credits (Alle for Business / Allergan APP / Galderma Aspire / Evolus official promos) with provenance fields in attachments &mdash; manufacturer, programName, programPeriod, sourceRef, earnedDate, expiresDate. (2) <code>rebate_inventory_anonymous</code> holds on-hand product with NO provenance fields at all (not nullable &mdash; absent). Two separate node_types instead of one with nullable source columns so a missing-field inference can&rsquo;t recover the off-the-record signal. No <code>reps</code> table, no <code>deals</code> table, no per-row link to a specific verbal negotiation &mdash; the deal context lives entirely in Karen&rsquo;s head per the scout-locked wall. <strong>Unit lifecycle in dedicated table</strong>: <code>rebate_inventory_units</code> (one row per inventory node, 1:1 unique on node_id) tracks units_total / units_deployed with atomic UPDATE semantics. Allocation increments (in v1.34.3) get correct concurrency-safe semantics; the lifecycle stays queryable without parsing JSONB. Constraint enforced at the DB layer: <code>units_deployed &lt;= units_total</code> &mdash; the application + the schema both refuse over-allocation. Soft delete via <code>soft_deleted_at</code> preserves audit trail. Two partial indexes: hot-path (active inventory by user_id) and allocation-engine (rows with deployable units remaining). <strong>Server fns at <code>src/server/refill-recognition.functions.ts</code></strong>: 5 createServerFn handlers &mdash; <code>listRebateInventory</code> (joins knowledge_nodes + rebate_inventory_units, returns unified RebateInventoryEntry shape with kind discriminator), <code>createDocumentedInventory</code> + <code>createAnonymousInventory</code> (mint node + lifecycle row atomically, with rollback on units-insert failure since the SDK has no tx), <code>updateRebateInventory</code> (refuses units_total below already-deployed), <code>deleteRebateInventory</code> (soft delete only). Auth pattern matches refill-catalog.ts verbatim: verifyAuth &rarr; resolveEffectiveUserId (admin viewAs honored). Zod validation including regex for ISO date fields and integer bounds on unit counts. <strong>UI surface at <code>/app/refill/recognition/inventory</code></strong>: PageHeader + two Add buttons (Portal-issued / On-hand) + groups of inline-editable cards. Click any row to expand the edit form; Delete inside the edit form. Documented entries render with program-name pill + earned/expires dates + manufacturer-relevant detail; anonymous entries render minimal (brand + SKU + units + notes). Available-units count at the top of the page aggregates across all entries. Empty state with guided CTA. Mobile-responsive (Karen on phone is the design target). <strong>Nav chip + dashboard card</strong>: Recognition added to RefillNav between Promos and Recovery (8 chips total now in the &lsquo;data primitives + actions&rsquo; cluster) and to the RefillHome dashboard quick-actions grid (8 cards in the lg:grid-cols-4 layout, balanced 4+4). Award icon for the chip + card. <strong>What this DOESN&rsquo;T ship (queued for follow-ons)</strong>: Manufacturer Profile snapshot surface at <code>/app/refill/recognition/profile</code> with tier-status from portal pastes (v1.34.2.1). AI extraction via Claude Sonnet 4.6 to parse portal screenshots into structured inventory entries (v1.34.2.1). Cohort scoring engine for the three-cohort split (Loyal Vintage 40% / Top-Decile Current 40% / At-Risk 20%) &mdash; reuses resolveCohort from emma-campaigns.functions.ts (v1.34.3). Allocation engine that intersects available inventory with patient primaryManufacturer + scoring (v1.34.3). Review + override UI + iMessage MCP delivery via Karen-Mac (v1.34.4). <strong>Migration</strong>: <code>supabase/migrations/20260622000000_v1_34_2_rebate_inventory_units.sql</code> &mdash; one new table, two RLS policies (user-owned + service-role), two partial indexes, defensive constraints. Pasted into Supabase dashboard per [[feedback-migrations-via-dashboard]]. <strong>Strategic anchor</strong>: per <code>project_recognition_allocation_engine_spec</code> v0.1 the Recognition Engine is incumbent-uncatchable because (1) the iMessage MCP delivery path (Karen-Mac) routes through Karen&rsquo;s Apple ID not a platform shortcode, and (2) the off-the-record sample-bridge dynamic is structurally unmodelable by Vagaro / Mindbody / Square / Zenoti / Boulevard because they&rsquo;d expose themselves to manufacturer ad-partner compliance. Refill builds for the operator&rsquo;s reality, behind the spa-tenant data wall. v1.34.2 is the substrate every downstream Recognition ship reads from. <strong>Touched</strong>: <code>supabase/migrations/20260622000000_v1_34_2_rebate_inventory_units.sql</code> (new file, 1 new table, RLS, indexes), <code>src/server/refill-recognition.functions.ts</code> (new file, ~470 lines, 5 server fns), <code>src/routes/app.refill.recognition.inventory.tsx</code> (new file, ~640 lines, Pool Entry surface), <code>src/components/refill/RefillNav.tsx</code> (Recognition chip + RefillNavKey union extended), <code>src/components/refill/RefillShellChrome.tsx</code> (deriveActiveKey adds /app/refill/recognition &rarr; &lsquo;recognition&rsquo;), <code>src/components/refill/RefillHome.tsx</code> (Recognition action card + Award icon import), <code>src/lib/changelog.ts</code> (this entry). One new dep: none. New tables: 1. New nav chip: 1. New routes: 1.",
    ],
  },
  {
    version: "v1.34.1.1",
    date: "June 2026",
    items: [
      "<strong>v1.34.1.1 &mdash; Hotfix: PersonaSwitcher allowlist out of sync with the v1.28.0 Karen auth-email rename. Karen&rsquo;s real production tenant was invisible in the admin dropdown for the past 2 days because the canonical allowlist was checking against a now-defunct email.</strong> Surfaced during the v1.34.1 coherency-pass walk-through when Grasshopper switched PersonaSwitcher to &lsquo;Rejuv Skin Spa&rsquo; expecting to see 1,140 patients + 217 catalog rows, got 1 patient + 0 catalog instead. Two-step diagnosis: (1) Supabase audit confirmed Karen&rsquo;s data IS intact under user_id <code>ecf8bcee-24a6-4a4d-a5f4-381166ca57fc</code> / email <code>karen.rejuv@gmail.com</code> in tenant <code>34834bcc-dbed-4e7c-99b1-8f4198fc739b</code> (slug=rejuv, is_demo=false) &mdash; 1140 patient rows present. The &lsquo;Rejuv Skin Spa&rsquo; in the PersonaSwitcher dropdown was a demo seed tenant (<code>decaffaa-0000-0000-0000-00000000000a</code>, slug=rejuv-demo, is_demo=true) owned by the synthetic <code>decaffaa-0000-0000-0000-000000000001</code> seed user with email <code>testspaowner@test.com</code> &mdash; only 1 patient. Two tenants with identical display names, only one visible. (2) A tenant-rename earlier in the session (demo tenant &rarr; &lsquo;Rejuv Skin Spa (Demo)&rsquo;) failed to surface Karen&rsquo;s real tenant. That eliminated the &lsquo;display-name dedup&rsquo; hypothesis and pointed to a filter, not a dedup. <strong>The root cause</strong>: <code>src/lib/personas.ts</code> defines a hardcoded allowlist <code>DEMO_PERSONA_EMAILS</code> that PersonaSwitcher uses to narrow the listImpersonableUsers result. The allowlist is derived from <code>PERSONA_EMAILS.karen</code> which still points at <code>testspaowner@test.com</code> &mdash; the demo seed email. v1.28.0&rsquo;s rename of dormantspaowner@test.com &rarr; karen.rejuv@gmail.com (the keystone Karen-Mac iMessage MCP ship that promoted Karen&rsquo;s dormant tenant to her real prod login) never propagated to this allowlist. The exact footgun v1.26.17 was supposed to close (single source of truth for persona emails) &mdash; but only for renames that affect both public mint AND admin dropdown. v1.28.0 was a tenant-identity promotion that touched neither file. <strong>The fix</strong>: NOT a one-line value swap on <code>PERSONA_EMAILS.karen</code>, because that const is ALSO consumed by <code>src/server/v417-persona-mint.ts</code> for the public <code>/personas</code> magic-link mint flow &mdash; pointing public mint at <code>karen.rejuv@gmail.com</code> would let anonymous visitors mint magic-links into Karen&rsquo;s real production account. Security regression. Instead: new constant <code>ADMIN_VIEWABLE_REAL_EMAILS</code> (a separate Set) holds real-but-admin-viewable emails. <code>DEMO_PERSONA_EMAILS</code> derives from <code>PERSONA_EMAILS + DEMO_PERSONA_KEYS</code> PLUS <code>ADMIN_VIEWABLE_REAL_EMAILS</code>, so PersonaSwitcher sees Karen&rsquo;s real account but the public mint flow does NOT. Architectural separation between &lsquo;who is admin-viewable&rsquo; (PersonaSwitcher) and &lsquo;who is publicly mint-able&rsquo; (PERSONA_EMAILS). Adding the email is one entry in the new Set. <strong>Net effect</strong>: PersonaSwitcher dropdown will now show TWO Spa Owner entries &mdash; &lsquo;Rejuv Skin Spa&rsquo; (Karen&rsquo;s real tenant under karen.rejuv@gmail.com) and &lsquo;Rejuv Skin Spa (Demo)&rsquo; (the testspaowner@test.com demo seed). Picking the real one lands Grasshopper on her actual 1,140-patient roster + 217-row catalog + the soft-tag work from earlier session. <strong>What this DOESN&rsquo;T fix</strong>: the broader pattern of v1.x.0 ships that touch auth.users.email without checking the personas.ts allowlist &mdash; needs a feedback memory bank (queued: <code>feedback_persona_allowlist_after_auth_rename</code>). The catalog &lsquo;0 products&rsquo; observation from the audit was a false alarm: catalog data lives in dedicated <code>catalog_products</code> + <code>catalog_services</code> tables, not <code>knowledge_nodes</code> (my Q4 diagnostic was wrong table). Karen&rsquo;s real 217 catalog rows are intact and will appear once she lands on the right tenant. <strong>Touched</strong>: <code>src/lib/personas.ts</code> (new ADMIN_VIEWABLE_REAL_EMAILS Set with karen.rejuv@gmail.com, DEMO_PERSONA_EMAILS spreads both DEMO_PERSONA_KEYS-derived + the new admin-viewable set), <code>src/lib/changelog.ts</code> (this entry). No migration, no schema change, no security regression.",
    ],
  },
  {
    version: "v1.34.1",
    date: "June 2026",
    items: [
      "<strong>v1.34.1 &mdash; Coherency pass. Before riffing forward into Recognition Engine v1.34.2, a deliberate stop-and-scan ship to make sure the v1.31-v1.34 feature wave has navigation, visual consistency, and discoverability.</strong> &ldquo;Amazing features + no map&rdquo; was the riff-coding trap from prior sessions; the Wishlist + Soft-Tags + Life Events + Purchase Patterns + Bulk-Apply all landed fast across one evening and risked the same. This ship is the receipt &mdash; four coherency gaps closed in one pass before the next strategic moat ship lands on top. <strong>Slice 1 &mdash; Promos chip promoted into RefillNav</strong>: <code>/app/refill/promos</code> has existed as a route since the v341 port from openagenticv4 but never had a nav chip &mdash; an entire feature (manufacturer promos matched to the spa&rsquo;s catalog via service-manufacturer eligibility) was undiscoverable from the spa-side nav. Now appears between Catalog and Recovery in the &lsquo;data primitives&rsquo; cluster (Patients = WHO, Catalog = WHAT we sell, Promos = WHAT manufacturers offer). <code>RefillNavKey</code> union extended, <code>deriveActiveKey</code> in <code>RefillShellChrome</code> updated so the chip highlights when the route is active. <strong>Slice 2 &mdash; Soft-tag filter chip strip on patient list</strong>: v1.34.0 shipped bulk soft-tag apply (Karen can tag 50 patients at once) but the list page had zero way to filter back to who&rsquo;s tagged what &mdash; structural asymmetry. Now: a new chip strip renders below the manufacturer filter whenever at least one tag has been applied in the tenant. One chip per (tag-key, value) tuple that appears on a real patient &mdash; seeded tags (Loyal, Specials seeker, High income, etc.) AND tenant-wide custom tag-def values (Galderma Loyalist, A-list Snowbird, etc.). Multi-select union semantics: clicking multiple chips shows patients matching ANY of them. Count badges per chip show how many patients have that specific value. Clear link appears when filters are active. Most-used chips surface first (sorted by count desc). Helper functions added: <code>patientTagFilterKeys</code> (enumerate a patient&rsquo;s filter-keys including seeded + custom selections, excluding free-text culturalNotes), <code>tagFilterKeyLabel</code> (resolve a filter-key to a display label including custom-def name lookup), <code>SEEDED_TAG_VALUE_LABELS</code> map (compact list-friendly labels distinct from the patient-detail page&rsquo;s richer renderings). New state: <code>softTagFilter: Set&lt;string&gt;</code>. New useMemo: <code>softTagCounts</code> (window-aware enumeration of active filter-key tuples). Filter logic threaded into the existing <code>filtered</code> useMemo with union check. <strong>Slice 3 &mdash; Tag pill cluster on each patient row</strong>: previously the list rows had ZERO indication of which patients were tagged; Karen had to drill into each detail page to find out. Now: a small inline <code>SoftTagPillCluster</code> renders below the displayName + overdue/banned chips in the Patient cell. Shows up to 2 tag pills (emerald-soft chip style matching the detail page) + a &lsquo;+N more&rsquo; overflow indicator when the patient has more than 2 tags. Title attribute on the cluster shows all tag labels space-separated for hover discoverability without expanding the row. Order matches the patient detail page card sequence (loyalty &rarr; specials &rarr; income &rarr; negotiator &rarr; personality &rarr; custom). Cultural notes intentionally skipped (free-text doesn&rsquo;t fit chip semantics). <code>customDefs</code> threaded through <code>PatientRow</code> props so per-row lookups for custom-def names are O(1) over the small defs array without re-fetching. <strong>Slice 4 &mdash; RefillHome dashboard parity with nav</strong>: dashboard quick-actions grid (5 cards: Patients/Recovery/Inbox/Settings/Billing) didn&rsquo;t match the nav chip strip (which had Catalog as a 6th chip since v1.29.2 + now Promos as the 7th from Slice 1). Visual contract broken &mdash; new Karen would see one mental model in nav, a different one on the dashboard. Now: dashboard cards = 7 cards = 7 nav chips, same order, same labels. Catalog (BookOpen icon, &lsquo;Services and products you offer&rsquo;) and Promos (Sparkles icon, &lsquo;Manufacturer offers matched to your catalog&rsquo;) added. Grid changed from <code>lg:grid-cols-5</code> to <code>lg:grid-cols-4</code> for a balanced 4+3 split on desktop (7 cards in a 5-col grid wraps awkwardly). <strong>What this DOESN&rsquo;T fix</strong> (deferred to dedicated future ships per Grasshopper&rsquo;s triage): the 3,451-line patient detail page + SoftTagsCard mega-container with 3 nested CRUD layers (needs its own surgical refactor); legacy per-patient custom tags from v1.31.1 deprecation banner + migration UI (isolated, lands in a v1.34.2.x sweep); the orphan-route inventory (Campaigns / Health / Reports / Rescue / Sharing / Waitlist / Appointments &mdash; need to confirm which are intentional deep-link tools vs nav gaps before sweeping); life-event decay history audit trail (low priority). <strong>Strategic anchor</strong>: per Grasshopper&rsquo;s prior-session feedback, &ldquo;riff coding&rdquo; produces amazing-feature-no-coherency outcomes. This ship operationalizes scout-first / Boris best-practice plan-first at the multi-feature level: stop, audit, fix, then move forward. Recognition Engine becomes v1.34.2 with a clean substrate underneath. <strong>Touched</strong>: <code>src/components/refill/RefillNav.tsx</code> (RefillNavKey union extended with &lsquo;promos&rsquo;, ITEMS array gains Promos entry between Catalog and Recovery), <code>src/components/refill/RefillShellChrome.tsx</code> (deriveActiveKey adds /app/refill/promos &rarr; &lsquo;promos&rsquo; mapping), <code>src/components/refill/RefillHome.tsx</code> (BookOpen + Sparkles icon imports, ACTIONS array gains Catalog + Promos entries in nav order, grid changed to lg:grid-cols-4 for 4+3 desktop layout), <code>src/routes/app.refill.patients.index.tsx</code> (PatientSoftTags type import + SEEDED_TAG_VALUE_LABELS map + patientTagFilterKeys helper + tagFilterKeyLabel helper + softTagFilter state + softTagCounts useMemo + filter logic in filtered useMemo + soft-tag chip strip rendered after manufacturer strip with multi-select chips and Clear affordance + SoftTagPillCluster component for per-row tag pill rendering with 2-visible-plus-overflow + customDefs prop threaded through PatientRow), <code>src/lib/changelog.ts</code> (this entry). No migration; no new dependencies; no schema change.",
    ],
  },
  {
    version: "v1.34.0.1",
    date: "June 2026",
    items: [
      "<strong>v1.34.0.1 &mdash; Five UX fixes caught during Grasshopper&rsquo;s v1.34.0 dry-run walk-through. All small, all annoying, all surfaced inside ~30 minutes of real use.</strong> Per the meta-loop the Wishlist proves: every friction Karen hits in production becomes a banked hotfix in the next ship. This is the receipt. <strong>Fix 1 &mdash; Markdown rendering on Wishlist description</strong>: the v1.33.1 AI polish returns the buildable spec as markdown (<code>**What**: ...</code>, <code>- bullet</code>), but the admin inbox and Karen&rsquo;s My-requests panel both rendered literal asterisks. New <code>MarkdownLight</code> component handles <code>**bold**</code> + <code>- bullets</code> + paragraph breaks &mdash; minimal renderer, no react-markdown dependency. Wired into both the admin row description and Karen&rsquo;s My-requests expanded view. <strong>Fix 2 &mdash; Filter chips vs Move-status chips visually distinct</strong>: prior version had two sets of identical-looking pill chips with the same status labels (Submitted / Reviewing / Building / Shipped / Declined) in two different places on the admin inbox &mdash; one as &lsquo;narrow this view&rsquo; (filter), one as &lsquo;mutate this wish&rsquo; (move status). Grasshopper got fluky clicks because they looked the same. Now: filter chips keep the pill shape + add a Filter funnel icon next to the &lsquo;Filter&rsquo; label. Move-status chips become a single segmented control (rounded rectangle, dividers between options) + ArrowRight icon next to the &lsquo;Move status:&rsquo; label. Clear semantic + visual distinction. <strong>Fix 3 &mdash; Filter chip counts show TRUE DB totals</strong>: prior version computed counts from the loaded <code>requests</code> array, which only contained the currently-filtered subset &mdash; so &lsquo;Shipped 0&rsquo; was misleading when there were 2 shipped wishes hidden by the filter. Now always loads ALL wishes server-side and filters client-side (volume justifies it &mdash; ~100s per tenant max). Counts now reflect the real database totals regardless of which chips are active. <strong>Fix 4 &mdash; Default filter = &lsquo;Show all&rsquo; on landing</strong>: prior default was Submitted+Reviewing+Building active (hiding Shipped+Declined). Grasshopper landed on the admin inbox, didn&rsquo;t see his wishes, had to UN-click each active chip OR add the missing ones &mdash; non-obvious either way. Now: default state is empty filter = show everything. Tapping a chip ADDS a narrow-by filter. New &lsquo;Show all&rsquo; link appears when 1+ chips are active, instantly clears. Also adds a new &lsquo;N of M shown&rsquo; counter on the right of the chip strip so you always know how much is visible vs hidden. The empty state copy now splits: &lsquo;No requests yet&rsquo; (truly empty DB) vs &lsquo;No requests match the current filter &middot; Show all N&rsquo; (filtered-out). <strong>Fix 5 &mdash; Reply send double-click idempotency</strong>: prior version&rsquo;s Send button relied on the <code>busy</code> state to disable, but a fast second click could fire before React rendered the disabled state (Grasshopper hit it during the Galderma close-out &mdash; sent the same reply twice). Both <code>WishlistWidget.sendReply</code> and <code>AdminRequestRow.sendReply</code> now: (a) bail if <code>busy</code> is already true, (b) OPTIMISTICALLY clear the draft on click so the button instantly disables via the empty-body check, (c) restore the draft if the server call fails. Same-text duplicate replies prevented; user retry path preserved on real errors. <strong>What this DOESN&rsquo;T fix</strong>: server-side dedup (a network retry could theoretically still dupe if the request actually succeeded but the response was lost &mdash; rare; would need an idempotency-key column on wishlist_replies, deferred unless it bites). Markdown features beyond bold + bullets + paragraphs (no italics, no inline code, no links &mdash; the polish format doesn&rsquo;t use them). Bulk reply-to-all / template replies on admin side. SLA-timeline cleanup of fluky workflow noise from earlier sessions (timestamps are correct, just visually noisy). <strong>Touched</strong>: <code>src/components/MarkdownLight.tsx</code> (NEW &mdash; ~80 lines, parses bold/bullets/paragraphs, no deps), <code>src/routes/app.admin.wishlist.tsx</code> (Filter+ArrowRight icon imports + MarkdownLight import + always-load-all in load fn + visibleRequests client-side filter useMemo + default empty filter + clearFilters helper + Filter label + Show all link + N of M counter + segmented Move status control + MarkdownLight on description + sendReply double-click guard), <code>src/components/WishlistWidget.tsx</code> (MarkdownLight import + render description via MarkdownLight in My-requests panel + sendReply double-click guard with optimistic clear + restore-on-error), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change; no new dependencies.",
    ],
  },
  {
    version: "v1.34.0",
    date: "June 2026",
    items: [
      "<strong>v1.34.0 &mdash; Bulk soft-tag apply. Karen&rsquo;s first AI-polished Wishlist becomes a shipped feature, completing the customer-wish &rarr; production-ship loop in one session arc.</strong> The wish that landed via v1.33.1&rsquo;s AI-assist flow (&lsquo;add bulk-set so I can apply a soft tag to many patients at once&rsquo;) is now a real platform feature: checkbox each patient row, &lsquo;Select all visible&rsquo; in the table header, a floating bottom-bar action toolbar with count + Apply soft tag + Clear, and a picker modal that surfaces ALL tag taxonomies in one place &mdash; seeded six + tenant-wide custom defs from v1.31.5 &mdash; with chip selection, optional reason note, and one-click apply across the selection. <strong>What ships</strong>: (1) <code>bulkApplyPatientSoftTag</code> server fn in <code>patient-ingest.functions.ts</code>: accepts patientNodeIds[] + a discriminated-union tag spec (preset key+value OR custom definitionId+selected[]) + optional reason. Single read of all selected patient attachments &rarr; in-memory merge of softTags / customSelections &rarr; parallelized per-row updates in batches of 50 (handles Karen-Rejuv&rsquo;s 1140-patient max cleanly in ~23 batched round-trips). Idempotent: patients already carrying the same tag value are silent no-ops, counted as &lsquo;skipped&rsquo; in the result. Returns <code>{touched, skipped}</code> for accurate toast feedback. Threaded through <code>resolveEffectiveUserId</code> for admin viewAs honored. setByUserId/setAt provenance captured per merged tag like every other soft-tag write. (2) Patient list state extended in <code>app.refill.patients.index.tsx</code>: <code>selectedIds: Set&lt;string&gt;</code> selection state, <code>bulkPickerOpen</code> modal state, <code>customDefs</code> loaded once on mount alongside patient list. Selection helpers: <code>toggleSelected</code>, <code>clearSelection</code>, <code>toggleSelectAllVisible</code> (intersect-with-filtered semantics: select-all-visible adds the filtered subset to the selection without clobbering out-of-filter selections; same for clear-visible). <code>visibleIds</code> / <code>selectedVisibleCount</code> / <code>allVisibleSelected</code> / <code>someVisibleSelected</code> derived from the existing <code>filtered</code> useMemo so filter changes don&rsquo;t lose selection. (3) <code>PatientRow</code> extended with <code>selected</code> + <code>onToggleSelected</code> props. New checkbox column at position 0 (8px wide); clicks <code>stopPropagation</code> so they don&rsquo;t navigate via the row Link. Selected rows render with emerald-soft background tint. Table header gains a matching checkbox cell with indeterminate state via React ref. (4) Floating bottom-center action toolbar (fixed bottom-6 left-1/2, z-40) appears the moment 1+ patients are selected: shows count + emerald &lsquo;Apply soft tag&rsquo; button + Clear. Mirrors the Sonner toast position pattern. (5) <code>BulkTagPickerModal</code> component &mdash; fixed inset-0 z-50 backdrop, centered card. Body lists all preset tag groups (Income tier / Negotiator / Specials seeker / Personality / Shopper loyalty &mdash; Cultural notes skipped because free-text doesn&rsquo;t fit bulk-set semantics) with chip selectors that single-select per group, then tenant-wide custom tag defs below with multi-select chip toggle. Reason note input. Apply button shows total count and chains into <code>bulkApply</code> handler. (6) <code>bulkApply</code> handler: calls server fn, surfaces success toast with proper grammar (&lsquo;Snowbird applied to 43 patients&rsquo; vs &lsquo;Snowbird applied to 43 patients (5 already had it)&rsquo;), clears selection, closes modal. Error toast surfaces server-side messages without losing the user&rsquo;s selection. <strong>Strategic significance</strong>: this is the first end-to-end demonstration of the project_wishlist_thesis loop: customer (Grasshopper-as-Karen) submitted a feature wish via the in-product Wishlist (v1.33.0), AI-assisted it to a buildable spec (v1.33.1), Po built it in one continuous session push, customer can now mark it &lsquo;shipped&rsquo; in the admin inbox via the same Wishlist. The 48h SLA promise is now structurally provable: timestamp on the wish submission + timestamp on the changelog entry below = same evening. <strong>What this DOESN&rsquo;T ship</strong>: bulk-REMOVE (only apply for v1.34.0 &mdash; remove/clear deferred to v1.34.0.x if requested). Server-side RPC for sub-second 1000-patient apply (current batched-Promise.all is fast enough at this volume; promote to RPC if a 5000+ patient tenant arrives). Bulk apply for cultural notes (free-text isn&rsquo;t a bulk-set semantic; per-patient still works). Audit log of who-bulk-applied-what-to-whom (per-tag provenance lives on each touched patient via setByUserId/setAt, but a centralized bulk-action log is a future tenant-ops ship). <strong>Touched</strong>: <code>src/server/patient-ingest.functions.ts</code> (new bulkApplyPatientSoftTag server fn with discriminated-union zod validator + idempotent merge logic + batched parallel writes with error aggregation), <code>src/routes/app.refill.patients.index.tsx</code> (selection state + visibleIds derivations + toggleSelected/clearSelection/toggleSelectAllVisible helpers + bulkApply handler + customDefs load alongside patient list + Check/Tag/X icon imports + sonner toast import + CustomTagDefinition type import + checkbox column header + checkbox column cell on PatientRow with stopPropagation + emerald-tinted selected row state + floating bottom action toolbar + BulkTagPickerModal sub-component with PRESET_TAGS catalog + BulkPickerSelection discriminated union + custom-def chip multi-select), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.33.1",
    date: "June 2026",
    items: [
      "<strong>v1.33.1 &mdash; AI-assist in the Wishlist: title &rarr; draft &rarr; polish &rarr; dev-cost preview. The meta-loop&rsquo;s first fulfillment.</strong> Grasshopper&rsquo;s second submitted wish (after the Galderma-rebate ask that was actually the Recognition Allocation Engine) was for AI-assist on the Wishlist itself: type a title, get a draft description in your voice; edit, get a polished buildable spec; see a one-line dev-cost preview before submitting. v1.33.1 ships exactly that, completing the meta-loop the v1.33.0 ship started: customer wishes for a feature in the Wishlist, customer gets the feature, all inside the 48h SLA window the platform promises. <strong>What ships</strong>: (1) Three new server fns in <code>src/server/wishlist.functions.ts</code>: <code>aiDraftWishlistDescription</code> (Claude Sonnet 4.6, no thinking, low effort &mdash; takes a title, returns 3-5 sentences in Karen&rsquo;s voice with concrete front-desk examples and no developer jargon), <code>aiPolishWishlistDescription</code> (Sonnet 4.6, adaptive thinking, medium effort, structured-output JSON schema enforced via Anthropic&rsquo;s <code>output_config.format</code> &mdash; returns WHAT / WHY / WHERE / acceptance criteria; Zod-validated server-side after parse), and <code>aiEstimateWishlistBuild</code> (Sonnet 4.6, no thinking, low effort, max_tokens 128 &mdash; returns a one-line cost preview like &lsquo;~45 min build &middot; scoped for v1.34.0&rsquo;). (2) All three fns gated through <code>resolveEffectiveUserId</code> + <code>getTenantIdForUser</code> so AI cost only fires for authenticated spa-owners with a tenant. (3) Typed Anthropic error translation via <code>translateAnthropicError</code> helper: <code>RateLimitError</code> &rarr; &lsquo;try again in 30s&rsquo;, <code>AuthenticationError</code> &rarr; &lsquo;ANTHROPIC_API_KEY missing&rsquo;, generic <code>APIError</code> &rarr; surfaced message. (4) <strong>Widget UX</strong> additions in <code>src/components/WishlistWidget.tsx</code>: <em>Title &rarr; draft</em> &mdash; after title hits 5+ chars AND description is empty, two affordances appear under the title input: emerald-tinted &lsquo;&#10024; AI-assist description&rsquo; button + plain &lsquo;Skip &mdash; I&rsquo;ll write my own&rsquo; link (focuses textarea on click). <em>Description &rarr; polish</em> &mdash; after description hits 20+ chars AND no polish is pending, &lsquo;&#10024; Polish into buildable spec&rsquo; button appears under the textarea. <em>Polish compare</em> &mdash; the new <code>PolishComparePanel</code> renders side-by-side: &lsquo;Your words&rsquo; (her original, preserved) vs &lsquo;Polished spec&rsquo; (WHAT/WHY/WHERE/AC) with &lsquo;Use polish&rsquo; / &lsquo;Keep mine&rsquo; buttons. Accepting writes a markdown-formatted version of the spec into the description field; rejecting clears polish + estimate state. <em>Po&rsquo;s reading</em> &mdash; while estimate is computing OR after it lands, an emerald summary card appears above Submit: &lsquo;Po&rsquo;s reading &middot; ~30 min build &middot; scoped for v1.34.0&rsquo;. Karen builds dev-cost intuition before submitting. (5) Estimate generation chains off polish acceptance &mdash; non-blocking; if estimate fails, polish is still usable (toast notes the failure but doesn&rsquo;t roll back the polish). (6) Submit clears polish + estimate state alongside the draft. <strong>Why Sonnet 4.6 not Opus 4.7</strong>: interactive UX (Karen waiting in modal), Sonnet&rsquo;s adaptive thinking + medium effort is the sweet spot for spec-writing quality + sub-3s response. Skill default would be Opus 4.7; explicitly downshifted to Sonnet 4.6 for this interactive surface. <strong>Why not Haiku 4.5</strong>: spec polish needs reasoning; Haiku would flatten Karen&rsquo;s voice in the draft pass too. <strong>Why no prompt caching in v1.33.1</strong>: system prompts at ~600-900 tokens are below the 2048-token minimum cacheable prefix on Sonnet 4.6 (4096 on Haiku 4.5). At spa-owner volume (~1-10 wishes/day), the cost difference is negligible. Will revisit if usage justifies. <strong>Zod version note</strong>: project is on Zod v3; <code>@anthropic-ai/sdk/helpers/zod</code>&rsquo;s <code>zodOutputFormat</code> requires Zod v4. Worked around by passing the JSON schema directly to <code>output_config.format</code> as <code>{type: &quot;json_schema&quot;, schema: POLISHED_SPEC_JSON_SCHEMA}</code> (hand-written, mirrors <code>PolishedSpecSchema</code>), then validating the response at runtime via <code>PolishedSpecSchema.parse(JSON.parse(text))</code>. Same safety either way; no SDK version coupling. <strong>What this DOESN&rsquo;T fix</strong>: streaming the draft / polish responses (short outputs, streaming complexity not worth it). Per-tenant cost caps (every authenticated spa-owner can call AI fns freely &mdash; rate limits will surface naturally). Prompt caching (deferred). Multi-language drafts (English-only system prompt). Visible token / cost reporting per submission. Auto-trigger on debounce (Karen must tap the button &mdash; no surprise AI fires). <strong>Setup required</strong>: <code>npm install @anthropic-ai/sdk</code> + <code>npx wrangler secret put ANTHROPIC_API_KEY</code> for the worker. <strong>Touched</strong>: <code>package.json</code> + <code>package-lock.json</code> (anthropic SDK + deps), <code>src/server/wishlist.functions.ts</code> (3 new server fns + JSON schema constant + Anthropic client helper + error translator + PolishedSpecSchema + 3 zod input validators), <code>src/components/WishlistWidget.tsx</code> (5 new state hooks for AI flow + 3 handler fns + acceptPolish/rejectPolish + draft input AI-assist buttons + polish compare panel + Po&rsquo;s reading preview card + Sparkles icon import), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.33.0",
    date: "June 2026",
    items: [
      "<strong>v1.33.0 &mdash; Wishlist: the 48h-customization-SLA in-product surface lands. The MOAT ships.</strong> Per project_wishlist_thesis (banked 2026-06-01): customer-requested feature build IS the competitive moat. Po-compression means Refill can ship per-tenant customizations in 48h or less &mdash; structurally impossible for every med-spa-software incumbent (Vagaro, Mindbody, Square, Zenoti, Boulevard) because their build pipelines are weeks-to-quarters, not hours. v1.33.0 is the in-product affordance that turns the thesis from a sales line into an operational loop: spa-owner submits request, Grasshopper triages, Po ships, status updates land in the spa-owner&rsquo;s feed. The whole thing happens inside the app. <strong>The meta-moment that drove this ship</strong>: Grasshopper wished for a Wishlist feature mid-session, watched Po build it inline, and saw it deploy live within minutes. That&rsquo;s the demo Karen will see when she submits her first request. <strong>What ships</strong>: (1) New <code>wishlist_requests</code> table with full schema: user_id (spa-owner) + tenant_id (spa account) FKs, title + description + auto-captured area context + priority enum (low / normal / high / urgent) + status enum (submitted / reviewing / in_progress / shipped / declined), JSONB messages array for inline reply thread, admin_notes + shipped_in_version fields, per-status timestamps for SLA metrics (reviewing_at, in_progress_at, shipped_at, declined_at) populated by trigger on status change. 3 indexes for hot paths. (2) RLS: spa-owners read+insert own; admins (per user_roles) read+update all; service-role bypass for daemons. (3) Five new server fns in <code>src/server/wishlist.functions.ts</code>: <code>submitWishlistRequest</code> (tenant), <code>listMyWishlistRequests</code> (tenant), <code>addReplyToWishlistRequest</code> (either side appends to thread), <code>listAllWishlistRequests</code> (admin cross-tenant with denormalized tenant name + submitter email), <code>updateWishlistRequestStatus</code> (admin, with shipped_in_version + admin_notes). All threaded through <code>resolveEffectiveUserId</code> + <code>requireAdmin</code> per existing patterns. (4) New <code>WishlistWidget</code> floating component &mdash; bottom-right lightbulb-icon launcher on every <code>/app/refill/*</code> page (hidden on <code>/app/admin/*</code>). Modal with two tabs: <strong>New request</strong> (title + description + priority chip selector + auto-captured route as &ldquo;from&rdquo; chip + Submit, with success toast &ldquo;submitted &mdash; we&rsquo;ll respond in 48 hours or less&rdquo;) and <strong>My requests</strong> (her status feed with expandable rows showing description + admin notes + conversation thread + reply textarea). Mounted in RefillShellChrome so it appears on every authenticated spa-owner surface. (5) New <code>/app/admin/wishlist</code> admin inbox &mdash; cross-tenant request list with status filter chips (count per status), expandable rows showing full description, status workflow chip-buttons (submitted &rarr; reviewing &rarr; in_progress &rarr; shipped / declined), shipped_in_version + admin_notes inline editor, SLA timeline summary (submitted Xd ago &middot; reviewing Yh ago &middot; shipped Zm ago), conversation thread with admin replies styled distinctly, reply textarea + Send. Tenant name + submitter email denormalized into each row for triage clarity. (6) <code>ADMIN_SURFACES</code> array appended with the wishlist surface so it&rsquo;s discoverable from <code>/app/admin</code>. <strong>Migration storage decision</strong>: dedicated <code>wishlist_requests</code> table (not JSONB inside knowledge_nodes) because the admin inbox needs cross-tenant queries + status indexing + per-tenant filtering &mdash; all things a proper table indexes cleanly and JSONB doesn&rsquo;t. <strong>Reply thread storage</strong>: JSONB <code>messages[]</code> column for v1.33.0 simplicity; upgrade to separate table only if reply volume justifies. <strong>What this DOESN&rsquo;T ship</strong>: Resend email notifications on status change (Resend is wired in <code>src/server/resend-gateway.ts</code> but the digest cadence + template needs its own v1.33.1 ship). Per-tenant config of which staff can submit (every authenticated user with a tenant membership can submit today). Public roadmap surface (intentionally not shipping &mdash; risks promising features Refill can&rsquo;t keep, breaks the 48h-or-decline promise). Auto-link &ldquo;shipped in vX.Y.Z&rdquo; to the actual git commit (manual field for now). Tenant-side notification when admin replies (status feed shows it on next open; bell-icon pulse is a v1.33.1 candidate). <strong>What this UNBLOCKS strategically</strong>: every customer conversation now has a built-in &ldquo;here&rsquo;s where you submit feature ideas&rdquo; close. Every Wishlist that ships becomes a marketing asset (the original request + the v-pill it shipped under is a verifiable receipt for the next prospect). The 48h SLA is now structurally measurable (per-status timestamps surface as cohort metrics later). <strong>Touched</strong>: <code>supabase/migrations/20260602002954_v1_33_0_wishlist.sql</code> (NEW migration &mdash; spa-owner pastes into Supabase dashboard), <code>src/server/wishlist.functions.ts</code> (NEW &mdash; 5 server fns + types + hydrate helper + requireAdmin/getTenantIdForUser local copies matching the canonical_brands pattern), <code>src/components/WishlistWidget.tsx</code> (NEW &mdash; floating widget + modal with Submit + My requests tabs + reply thread), <code>src/routes/app.admin.wishlist.tsx</code> (NEW &mdash; admin inbox with status workflow + reply thread + SLA timeline + version stamp), <code>src/components/refill/RefillShellChrome.tsx</code> (mounts WishlistWidget), <code>src/routes/app.admin.index.tsx</code> (ADMIN_SURFACES append + Lightbulb icon import), <code>src/lib/changelog.ts</code> (this entry). Note: types.ts has not yet been regenerated for the new <code>wishlist_requests</code> table, so the server fns use <code>as never</code> casts at the supabase boundary &mdash; runtime is correct against the live schema once the migration paste lands.",
    ],
  },
  {
    version: "v1.32.0",
    date: "June 2026",
    items: [
      "<strong>v1.32.0 &mdash; Life-Event Log: substrate primitive #3 of the Profitability Engine (&sect;3.3) lands.</strong> Per spec &sect;3.3: timestamped events Karen captures (job loss, engagement, vacation, divorce, milestone-birthday, etc.) that modulate the static soft-tag profile for a TTL window. Sarah lost her job 2026-04-15 &rarr; ATP -30% for 180 days &rarr; the engine knows to discount-calibrate rather than upsell during that window, then auto-decays back to baseline. Sister primitive to Soft-Tags (v1.31): same Karen-set / never-inferred posture, same JSONB preservation through QB re-rolls. <strong>What ships</strong>: (1) <code>PatientLifeEvent</code> type with id + eventType + customLabel + eventDate + wtpModifier (-100..+100) + atpModifier (-100..+100) + ttlDays (or null = never decays) + reason + setByUserId + setAt provenance. 13 seeded event types: job_loss / promotion / engagement / marriage / divorce / new_baby / milestone_birthday / retirement / vacation / health_event / relocation / graduation / bereavement + custom. (2) <code>LIFE_EVENT_DEFAULTS</code> map per seeded type with industry-norm WTP / ATP / TTL: e.g., job_loss = WTP 0 / ATP -30% / 180d, engagement = WTP +25% / ATP 0 / 365d, milestone_birthday = WTP +30% / ATP 0 / 90d. Karen can override any default per event. (3) <code>isLifeEventActive</code> + <code>computeActiveModifiers</code> helpers: auto-decay is computed at READ time (not stored stale); aggregate WTP / ATP from all active events stack-sum-clamped to [-100, +100]. (4) Three new server fns: <code>addPatientLifeEvent</code> / <code>updatePatientLifeEvent</code> / <code>deletePatientLifeEvent</code>, all threaded through <code>resolveEffectiveUserId</code> for admin viewAs. (5) Life events stored on <code>knowledge_nodes.attachments.lifeEvents</code> with same preservation pattern as softTags &mdash; <code>extractContactSummary</code> + <code>rollupPatientSummary</code> merge block both extended. (6) <strong>New Life events card</strong> on patient detail, slotted between Soft tags and Purchase patterns. Active-modifier summary pill at top (&ldquo;3 active events &middot; WTP +15% &middot; ATP -30% &middot; next decay in 47d&rdquo;) when any event is active. Timeline list with active events first (most-recent), archived (auto-decayed) collapsible below. Each row shows: event label + date + WTP/ATP modifiers + TTL window + reason note. Inline edit (chip selector for event type, date picker, TTL input, dual range-slider for WTP/ATP modifiers, reason input). &ldquo;+ Add a life event&rdquo; affordance at the bottom of the active section. (7) Selecting a seeded event type auto-fills WTP / ATP / TTL with the industry-norm defaults; Karen can fine-tune via the sliders. Custom event types prompt for a free-form label. <strong>Why this matters strategically</strong>: this is the LIVE-DATA layer of the Profitability Engine. Soft-Tags + Catalog + Purchase Patterns are the static foundation; Life-Events tell the engine when a patient&rsquo;s real-world situation shifts so the static profile gets modulated. Without this, the engine treats every patient as if their economic / emotional state is frozen in time. With this, &ldquo;Sarah went through a divorce 4 months ago&rdquo; means engines know NOT to push high-margin Botox upsells this quarter; &ldquo;Karen mentioned her own engagement&rdquo; means the milestone-birthday engine knows to ramp recognition during the wedding-prep window. <strong>Profitability Engine scoring factor #17</strong> (Active life-event modifiers) now has a substrate. <strong>What this DOESN&rsquo;T fix</strong>: ML-inferred life events (we will NEVER do this &mdash; only Karen captures these from her direct conversations with patients). Cross-patient event templates (e.g., &lsquo;everyone in the area got hit by the layoffs&rsquo;). Reminder workflows (&lsquo;flag patients with active job_loss for re-engagement in 90d&rsquo; &mdash; v1.32.x candidate). Event-driven push notifications to Karen-Mac. <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (PatientLifeEvent + PatientLifeEventType + ActiveLifeEventModifier types + isLifeEventActive + computeActiveModifiers + LIFE_EVENT_DEFAULTS + lifeEvents field on PatientContactSummary + rollupPatientSummary merge), <code>src/server/patient-ingest.functions.ts</code> (PatientLifeEvent imports + PatientListRow extended with lifeEvents + hydratePatientListRow maps it through + extractContactSummary preserves lifeEvents + 3 new server fns sharing the mutateLifeEvents helper), <code>src/routes/app.refill.patients.$patientId.tsx</code> (LifeEventsCard + LifeEventRow + LifeEventForm + LIFE_EVENT_ORDER + eventLabel + LifeEventDraft + emptyLifeEventDraft + eventToDraft + applySeededDefaults flow + Heart and Clock icon imports + wired into detail page JSX with optimistic state mirror), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.31.5",
    date: "June 2026",
    items: [
      "<strong>v1.31.5 &mdash; Custom tags become TENANT-WIDE definitions, not per-patient. Set tag affordance per patient matches seeded six exactly.</strong> Per Grasshopper&rsquo;s callout while creating &lsquo;Rescheduler&rsquo; with Chronic / Occasional / Never chips on one patient: that tag should appear on EVERY patient in the practice, just like the seeded six (Income tier, Negotiator, etc.). v1.31.4 stored custom tags per-patient &mdash; wrong shape. v1.31.5 fixes the architecture: Karen defines a tag once for her practice; it appears on every patient with a Set tag affordance; per-patient she picks which chips apply (or leaves Not set). <strong>What ships</strong>: (1) New <code>CustomTagDefinition</code> type stored as <code>knowledge_nodes</code> rows with <code>node_type=&lsquo;custom_tag_definition&rsquo;</code>, <code>context=&lsquo;soft_tag_definitions&rsquo;</code>, attachments holding the chip options array. Tenant-scoped via user_id like every other knowledge_node row. Lookup_key = lowercase(name) for dedupe-by-name. (2) New <code>PatientCustomTagValue</code> shape on <code>softTags.customSelections</code> &mdash; a <code>Record&lt;definitionId, {selected[], setByUserId, setAt, reason}&gt;</code>. Chip options come from the tenant definition; the patient&rsquo;s selected subset + provenance live here. (3) Six new server fns: <code>listCustomTagDefinitions</code>, <code>createCustomTagDefinition</code> (dedupes by name &mdash; if a def with this name exists, merges new options into existing one), <code>updateCustomTagDefinition</code>, <code>deleteCustomTagDefinition</code>, <code>setPatientCustomSelection</code>, <code>clearPatientCustomSelection</code>. All threaded through <code>resolveEffectiveUserId</code> for admin viewAs. (4) <code>PatientDetail</code> shape extended with <code>customTagDefinitions: CustomTagDefinition[]</code>; <code>getPatientById</code> fetches both patient and tenant definitions in one round trip. (5) <strong>UI overhaul</strong> of SoftTagsCard: new <code>TenantCustomTagsSection</code> renders one row per definition exactly like the seeded six (uppercase label + Set tag pill OR active chips with Edit + clear). New <code>CustomSelectionForm</code> chip-picker matches preset tag UX. New <code>DefinitionEditForm</code> handles both Create-new and Edit-existing (rename + change chip options + Delete-for-all-patients with confirm). Pencil icon next to each definition opens the def editor. &lsquo;+ Create a new custom tag (shows on all patients)&rsquo; button at the bottom. After creating, the Set tag form auto-opens so Karen can immediately apply it to this patient. (6) <strong>Legacy v1.31.4 custom tags</strong> still render in a small amber-tinted &lsquo;Legacy (per-patient) custom tags &middot; pre-v1.31.5&rsquo; subsection below the tenant-wide section, with a note explaining they were created per-patient and inviting Karen to re-create them as tenant-wide tags. Existing Family/Friends Pricing + Rescheduler will appear there, editable / deletable in their old form; Karen can re-create them via the new flow in &lt;30s each, then delete the legacy entries. <strong>What this unblocks</strong>: every spa-owner&rsquo;s editorial framework (their per-practice taxonomy of patient signals) lives in one tenant-scoped definition store. Karen&rsquo;s Rescheduler / Allergies / Family-Pricing / Preferred-Day / VIP-Tier / etc. all become first-class soft tags that compose with the seeded six. The Profitability Engine scoring layer can read any tenant-defined tag the same way it reads the seeded ones. <strong>What this DOESN&rsquo;T fix</strong>: cross-tenant tag definition sharing (e.g., &lsquo;most spas track Rescheduler&rsquo; &mdash; future ship could surface community-seeded tag suggestions). Tag-definition history (rename or option-change overwrites in place; older option labels disappear from new patient renders if a chip is removed even if some patients had it selected). Bulk-set affordance (set the same tag value on many patients at once &mdash; future ship). Tag-definition icon / color customization. Per-role visibility (still owner-only by default). <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (PatientCustomTagValue + CustomTagDefinition types + customSelections field on PatientSoftTags), <code>src/server/patient-ingest.functions.ts</code> (imports + 6 new server fns + hydrateCustomTagDefinition helper + getPatientById fetches definitions + PatientDetail extended), <code>src/routes/app.refill.patients.$patientId.tsx</code> (SoftTagsCard accepts definitions + onDefinitionsChange props + new TenantCustomTagsSection + DefinitionRow + CustomSelectionForm + DefinitionEditForm + Plus icon for create button + legacy section gated on customs.length and re-labeled as Legacy with amber chrome), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change. <code>knowledge_nodes.node_type</code> is free-form text so the new <code>custom_tag_definition</code> type slots in without a schema change.",
    ],
  },
  {
    version: "v1.31.4.1",
    date: "June 2026",
    items: [
      "<strong>v1.31.4.1 &mdash; Hotfix: StatusPill crash on legacy CadenceMetrics that pre-date v1.31.3.</strong> Caught immediately on the v1.31.4 dry-run: opening Debbie Donaldson&rsquo;s patient detail page crashed the entire section with the error boundary showing &ldquo;Cannot read properties of undefined (reading &lsquo;bg&rsquo;).&rdquo; <strong>Root cause</strong>: Debbie&rsquo;s <code>purchasePatterns</code> were computed and stored in her JSONB attachments during the v1.31.2 ship (when she first tapped &ldquo;Compute patterns from transactions&rdquo;) &mdash; BEFORE v1.31.3 added the <code>typicalExpectedDays</code> and <code>typicalStatus</code> fields to <code>CadenceMetrics</code>. v1.31.3 added the fields to the type and to fresh computations, but did NOT migrate existing stored metrics. When v1.31.4 loaded with the new <code>StatusPill</code> trying to render <code>m.typicalStatus</code> (undefined for her), the lookup <code>configMap[undefined]</code> returned undefined, and reading <code>.bg</code> on undefined threw. Error boundary caught it but the whole detail page was unrenderable. <strong>Patch</strong>: <code>StatusPill</code> now coerces unknown / undefined status values to the &lsquo;unknown&rsquo; config fallback (gray &ldquo;—&rdquo; pill) instead of crashing. <code>PatternRowView</code> conditional logic also tightened to check &lsquo;status &amp;&amp; status !== &ldquo;unknown&rdquo;&rsquo; (catches both undefined and the unknown sentinel) before rendering each pill. <code>typicalExpectedDays</code> existence check loosened from <code>!== null</code> to <code>!= null</code> (catches undefined too). <strong>Result</strong>: legacy patterns now render with personal-status pills only (typical fields show as nothing); tapping <strong>Refresh</strong> on the Purchase patterns card recomputes with v1.31.3 logic and populates typical baselines. <strong>What this DOESN&rsquo;T do</strong>: backfill the typical fields server-side across every existing tenant&rsquo;s stored purchasePatterns &mdash; that would require a tenant-wide rollup sweep, deferred. Each patient&rsquo;s metrics update naturally on the next Refresh / QB CSV re-upload. <strong>Touched</strong>: <code>src/routes/app.refill.patients.$patientId.tsx</code> (StatusPill defensive default to configMap.unknown + PatternRowView conditionals hardened against undefined typical/personal status values), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.31.4",
    date: "June 2026",
    items: [
      "<strong>v1.31.4 &mdash; Custom soft-tags become chip-style: full symmetry with the preset tags.</strong> Per Grasshopper&rsquo;s dry-run callout on Debbie Donaldson: when he typed &lsquo;Mother/Daughter, Siblings, Wife/Husband, Partner&rsquo; into a Family / Friends Pricing custom tag, the value rendered as one long string &mdash; not the slick chip-style UX of the preset tags (income tier&rsquo;s High / Mid / Low / Unknown). v1.31.4 closes that asymmetry: Karen now defines a custom tag&rsquo;s chip OPTIONS (comma-separated) once, then taps which ones apply for THIS patient &mdash; identical UX to picking &lsquo;High&rsquo; for income tier. <strong>What ships</strong>: (1) <code>PatientCustomTag</code> data shape evolved from <code>{ value: string }</code> to <code>{ options: string[], selected: string[] }</code>. Options = all the defined chip labels; selected = the subset active for this specific patient. (2) <code>normalizeCustomTag</code> helper handles backward-compat read of v1.31.1 string-shape tags: splits the value by comma, all chips selected by default, so existing tags continue to render correctly (and on first edit, persist forward in the new shape). (3) Three server fns updated: <code>addPatientCustomTag</code> / <code>updatePatientCustomTag</code> validators now take <code>options: string[]</code> + <code>selected: string[]</code> (zod-validated max 50 options, max 200 chars per option). <code>mutateCustomTags</code> normalizes priorCustom through the v1.31.1-shape adapter before applying any mutation, so legacy tags edit cleanly without data loss. (4) <strong>UI overhaul</strong> of the Custom tags section: <em>Row display</em> &mdash; each tag now shows chips with emerald-active / gray-inactive states (active = chip is in <code>selected[]</code>; inactive = chip is in <code>options[]</code> but not selected for this patient). <em>Form edit</em> &mdash; tag name input + comma-separated options textarea + live chip preview below that parses options on every keystroke + tap-to-toggle selection state + &lsquo;all&rsquo; / &lsquo;none&rsquo; bulk-select shortcuts + selected-count indicator + reason / context input + Save / Cancel / Delete tag. Inline parse handles commas AND newlines as separators, trims whitespace, dedupes. (5) Save logic filters draft.selected to only those still present in options (so editing the options text and removing a previously-selected entry cleans up the selection automatically). (6) Empty-options tag prevented at both client and server (zod min 1 option). <strong>Why this matters</strong>: the seeded six tags have a constrained-choice UX (Karen picks from a defined list); custom tags now have THE SAME UX, just with Karen-defined lists. Karen&rsquo;s mental model is one consistent &lsquo;tap a chip&rsquo; pattern across every tag, not &lsquo;sometimes chips, sometimes free text.&rsquo; <strong>What this DOESN&rsquo;T fix</strong>: per-tenant chip-option catalogs (Karen still defines options per-patient; defining &lsquo;Family / Friends Pricing&rsquo; options once for all her patients is a future ship). Single-vs-multi-select toggle (currently always multi-select &mdash; she can tap 0+ chips; a radio-style &lsquo;exactly one&rsquo; mode is deferred). Drag-to-reorder options. Chip color customization. <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (PatientCustomTag shape changed to options/selected + normalizeCustomTag backward-compat helper), <code>src/server/patient-ingest.functions.ts</code> (normalizeCustomTag import + zod validators rewritten for options/selected + mutateCustomTags normalizes prior tags before mutation + add/update handlers map options/selected through), <code>src/routes/app.refill.patients.$patientId.tsx</code> (normalizeCustomTag import + CustomTagDraft shape changed to optionsText/selected + parseOptions helper handling comma + newline separators with dedup + CustomTagRow renders chips with active/inactive states + CustomTagForm completely rebuilt: name input + options textarea + live chip preview with tap-toggle + all/none bulk shortcuts + selected-count indicator + reason input + Save/Cancel/Delete), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.31.3",
    date: "June 2026",
    items: [
      "<strong>v1.31.3 &mdash; TYPICAL cadence baseline added alongside PERSONAL: rewards programs + FDA windows now have a substrate.</strong> Per Grasshopper&rsquo;s mid-session callout: her personal cadence is great for &ldquo;is she slowing down?&rdquo;, but reward programs (Alle / Aspire / Merz Rewards) and FDA-label retreatment windows key off the TYPICAL / industry-standard cadence, not her personal pattern. Karen needs to see BOTH at a glance: she might be on-personal-cadence at 110 days for Botox (her own norm), but she&rsquo;s now past the 90-day TYPICAL window which means she&rsquo;s eligible for an Alle-rewards-mapped touchup nudge. v1.31.2 only tracked personal; v1.31.3 adds typical as a parallel signal across every dimension. <strong>What ships</strong>: (1) <code>CadenceWindow</code> in <code>patient-cadence.ts</code> gains <code>expectedDays</code> field; <code>KIND_CADENCE</code> defaults updated to Grasshopper&rsquo;s industry norms: toxin 90d expected (was implicitly 4mo soft), filler 270d expected (median of 6mo-2yr range), biostim 540d expected. Soft / lapsed windows unchanged. (2) <code>CadenceMetrics</code> in <code>patient-csv.ts</code> gains two new fields: <code>typicalExpectedDays</code> (typical retreatment interval for this dimension, null when not derivable) and <code>typicalStatus</code> (&lsquo;on-cadence&rsquo; / &lsquo;overdue&rsquo; / &lsquo;lapsed&rsquo; / &lsquo;unknown&rsquo; relative to the typical baseline). Existing <code>status</code> field continues to track personal-norm status (her own cadence). Both computed on every rollup. (3) <code>computeCadenceFromDates</code> now takes <code>typicalExpectedDays</code> + <code>typicalLapsedDays</code> as optional inputs; for daysSinceLastVisit ≤ expectedDays → on-cadence (eligible for rewards); past expected but ≤ lapsed → overdue; past lapsed → lapsed. (4) <code>computePurchasePatterns</code> resolves typical per dimension: byKind reads <code>KIND_CADENCE[kind]</code> directly; byManufacturer + byProduct derive typical from the DOMINANT kind of transactions for that group (e.g., a manufacturer whose transactions are mostly toxin gets the 90d typical; a product whose transactions are mostly filler gets the 270d typical). This means even brand-level / product-level cadence has a reward-program-relevant typical baseline. (5) <strong>UI flip</strong>: status pill on each Purchase patterns row now shows TYPICAL status (the action-relevant signal &mdash; &ldquo;On typical&rdquo; / &ldquo;Overdue (typical)&rdquo; / &ldquo;Lapsed (typical)&rdquo;). Personal status surfaces as a small secondary tag below the pill (&ldquo;personal: on/overdue/lapsed&rdquo;). Trend chip continues to track personal trajectory. New row-body line: <em>&ldquo;typical retreat 90d · 23d until eligible&rdquo;</em> (when on-cadence) or <em>&ldquo;typical retreat 90d · 12d past typical&rdquo;</em> (when overdue / lapsed). Karen sees in one scan: typical-vs-personal-vs-trend. (6) When the typical baseline is unknown (kind not in KIND_CADENCE), the row falls back to showing personal status as the primary pill, same as v1.31.2 behavior. <strong>Why this matters strategically</strong>: this is the reward-program substrate. The Recognition Allocation Engine spec (&sect;5.x) keys off &ldquo;eligible per manufacturer reward rules&rdquo; &mdash; that rule check now reads <code>patient.purchasePatterns.byKind.toxin.typicalStatus === &quot;on-cadence&quot;</code> directly. Same for Alle qualifying retreatments, Aspire windows, future FDA-label-keyed reminders. <strong>What this DOESN&rsquo;T fix</strong>: per-product typical override (Voluma 18-24mo vs Restylane 6-9mo &mdash; both currently fall back to filler-kind&rsquo;s 270d typical; per-brand typical map is a v1.31.4 candidate once Karen tags more products). Reward-program-specific windows (Alle requires retreatment within 28-60 days of last to qualify for tier 2, etc. &mdash; v1.31.4+ ships reward-program-specific cadence overlays). Population-norm typical (today&rsquo;s typical comes from <code>KIND_CADENCE</code> defaults; pulling actual Rejuv-tenant population means at scale is a v1.32+ aggregation ship). <strong>Touched</strong>: <code>src/lib/patient-cadence.ts</code> (expectedDays field on CadenceWindow + KIND_CADENCE values updated for toxin / filler / biostim), <code>src/lib/patient-csv.ts</code> (CadenceMetrics extended with typicalExpectedDays + typicalStatus + KIND_CADENCE import + computeCadenceFromDates signature expanded + computePurchasePatterns extended with byMfrKindCounts and byProductKindCounts trackers and dominantKind / typicalForKind helpers), <code>src/routes/app.refill.patients.$patientId.tsx</code> (PatternRowView flips primary pill to typicalStatus + adds personal secondary tag + adds typical retreat reference line + StatusPill takes &lsquo;kind&rsquo; prop with typical / personal labels + personalStatusLabel helper), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.31.2.1",
    date: "June 2026",
    items: [
      "<strong>v1.31.2.1 &mdash; Hotfix: rollupPatientSummary now preserves softTags on re-roll.</strong> Caught immediately during the v1.31.2 dry-run: Grasshopper set Debbie Donaldson&rsquo;s income tier to High with reason &ldquo;profession,&rdquo; saved successfully, then tapped &ldquo;Compute patterns from transactions&rdquo; on the new Purchase patterns card &mdash; and the saved Income tier reset to &ldquo;Not set.&rdquo; <strong>Root cause</strong>: the v1.31.0 soft-tag preservation chain was only half-wired. <code>extractContactSummary</code> correctly pulled <code>softTags</code> out of the prior attachments and into the <code>priorContact</code> object passed to <code>rollupPatientSummary</code> &mdash; but <code>rollupPatientSummary</code>&rsquo;s priorContact-merge block (lines that say &ldquo;if priorContact.X !== undefined then summary.X = priorContact.X&rdquo; for each preserved field) was missing the line for <code>softTags</code>. So extract pulled, but rollup never restored, and the freshly rolled-up summary had no softTags field at all. This means <strong>every QB CSV re-upload since v1.31.0 would have wiped soft tags too</strong>, not just the v1.31.2 recompute path. Patched by adding the single missing merge line, mirroring the <code>vip</code> merge that&rsquo;s been there since v385.2. (The <code>extractContactSummary</code> fix in v1.31.0 also closed the parallel <code>vip</code>-was-missing-from-extract bug, so the two halves of this chain are now in full agreement for both vip and softTags.) <strong>What this fix DOESN&rsquo;T touch</strong>: customTags ride inside softTags so they&rsquo;re covered by the same one-line fix. PurchasePatterns are intentionally not preserved &mdash; they&rsquo;re recomputed fresh on every rollup, that&rsquo;s the whole point. <strong>Re-set required</strong>: any soft tags Grasshopper set between v1.31.0 and v1.31.2.1 deploy that got nuked by a Compute click or QB re-upload need to be re-entered (Debbie Donaldson&rsquo;s Income tier > High > &ldquo;profession&rdquo; specifically &mdash; but worth a sweep across any patient Grasshopper tagged during the testing window). <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (one-line addition inside rollupPatientSummary&rsquo;s priorContact-merge block), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.31.2",
    date: "June 2026",
    items: [
      "<strong>v1.31.2 &mdash; Per-patient purchase patterns: THE differentiator lands.</strong> Per Grasshopper&rsquo;s strategic frame mid-session: tracking each patient&rsquo;s individual purchase cadence across every kind, manufacturer, and specific product IS our moat. The engines downstream (no-show fill, Recognition Allocation, combo upsell, manufacturer-promoted moments, anniversary touch, life-event response) all need this substrate to target the right offer at the right time against THIS patient&rsquo;s real habits &mdash; not population averages. Karen&rsquo;s brain holds this for ~200 patients; the engine holds it for 1,140 (and 11,400). <strong>What ships</strong>: (1) New <code>computePurchasePatterns</code> function in <code>patient-csv.ts</code>: groups a patient&rsquo;s transactions by kind, manufacturer, AND specific product name, then computes per-group cadence metrics. (2) <code>CadenceMetrics</code> per dimension entry: <code>visitCount</code>, <code>firstVisit</code>, <code>lastVisit</code>, <code>lifetimeAvgDays</code> (avg interval across entire history), <code>recentAvgDays</code> (last 730 days, falls back to lifetime when patient tenure &lt;2yr or fewer than 2 recent visits), <code>daysSinceLastVisit</code>, <code>status</code> (on-cadence / overdue / lapsed relative to her OWN norm with 1.2x / 1.5x buffer thresholds), and <code>trend</code> (accelerating / steady / slowing &mdash; recent vs lifetime ratio with 15% threshold to avoid noise). (3) <code>PatientPurchasePatterns</code> struct: <code>byKind</code> + <code>byManufacturer</code> + <code>byProduct</code> branches, each a map from dimension key to <code>CadenceMetrics</code>. Per-product is keyed by raw product name (no normalization) so distinct QB entries stay distinct until Karen consolidates them in catalog. (4) Computed during <code>rollupPatientSummary</code> and stored on <code>knowledge_nodes.attachments.purchasePatterns</code>. Recomputed fresh on every rollup &mdash; pure derivation, never editorial. (5) <code>recomputePatientPatterns</code> server fn for single-patient on-demand recompute without re-uploading the whole QB CSV. Reuses <code>refreshPatientSummary</code> internally; viewAs-honored. (6) New <strong>Purchase patterns</strong> card on <code>/app/refill/patients/$id</code>, slotted right after Soft tags. Three bands: <em>Clinical (per kind)</em> &mdash; tox/filler/biostim/device/service/retail/reward pinned in clinical priority order; <em>By manufacturer</em> &mdash; top 5 with &ldquo;Show all N&rdquo; expander, sorted by visit count; <em>By product</em> &mdash; top 8 with &ldquo;Show all N&rdquo; expander, sorted by visit count. Each row shows label + visit count + lifetime avg cadence + recent avg cadence (when different) + days since last + next-expected projection + status pill + trend chip. (7) Empty-state card with a one-tap &ldquo;Compute patterns from transactions&rdquo; affordance for legacy patient rows that pre-date v1.31.2. (8) Excludes payment / discount / note transaction kinds from cadence computation &mdash; those aren&rsquo;t purchase events. Includes reward redemptions (Alle / Aspire) since they represent visits. <strong>How this collapses the v1.31.3 plan</strong>: per-product cadence makes per-filler-brand cadence (Voluma 18-24mo vs Restylane 6-9mo) fall out automatically from her actual visit data &mdash; no hard-coded brand-cadence map needed. v1.31.3 task closed, folded into this ship. <strong>Strategic impact</strong>: every Profitability Engine scoring factor 1-11 (gross margin / mix breadth / cadence regularity / trend slope / years as patient / referral generator / provider portability / current visit value / combo visit-shape / combo-rebate eligibility) now has a per-patient substrate to read against. Plus factor #4 (cadence regularity) is now directly readable, and factor #5 (trend slope) too. <strong>What this DOESN&rsquo;T fix</strong>: per-category rollup (filler-as-category isn&rsquo;t in <code>ProductKind</code> &mdash; service categories live in the catalog, not transaction kinds; rollup-by-category is a small future ship that joins transactions through the catalog table). Population-norm benchmarking (each patient is measured against her own norm; comparing &ldquo;Sarah&rsquo;s tox cadence vs all spa tox patients&rdquo; is a tenant-level aggregation ship). Backfill across all existing patients in a tenant in one shot (admin button or pg job; today each patient recomputes individually via the card button or a QB re-upload triggers full rollup). Per-injector cadence (when one patient sees two providers; needs provider attribution on transactions). <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (CadenceMetrics + PatientPurchasePatterns types + computeCadenceFromDates + computePurchasePatterns + daysBetween + daysSinceToday helpers + CADENCE_EXCLUDED_KINDS + wired into rollupPatientSummary), <code>src/server/patient-ingest.functions.ts</code> (PatientPurchasePatterns import + extended PatientListRow with purchasePatterns + hydratePatientListRow maps it + new recomputePatientPatterns server fn), <code>src/routes/app.refill.patients.$patientId.tsx</code> (PurchasePatternsCard + PatternBand + PatternRowView + StatusPill + TrendChip + KIND_PRIORITY + kindDisplayLabel + 4 new lucide icons + wired into detail page JSX with optimistic state mirror via onPatientChange), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.31.1",
    date: "June 2026",
    items: [
      "<strong>v1.31.1 &mdash; Custom soft-tags CRUD: Karen-defined tags beyond the seeded six.</strong> v1.31.0 shipped six seeded soft-tags (income tier, negotiator, specials seeker, personality, shopper loyalty, cultural notes) &mdash; the editorial signals Grasshopper&rsquo;s 16-year front-desk observation flagged as universally tracked. But Karen knows things about HER patients that the seed doesn&rsquo;t cover: allergies, pet names, preferred days, family context, treatment sensitivities. v1.31.1 opens the door: a &ldquo;+ Add custom tag&rdquo; affordance at the bottom of the Soft tags card, free-form name + value + optional reason, with the same provenance shape (set-by user + timestamp) as the seeded tags. <strong>What ships</strong>: (1) <code>PatientSoftTags</code> type extended with <code>custom: PatientCustomTag[]</code>. Each row carries stable uuid id (for targeting updates / deletes without name collisions), name (max 80 chars), free-text value (max 2000), optional reason note, set-by + set-at provenance. (2) Three new server fns &mdash; <code>addPatientCustomTag</code>, <code>updatePatientCustomTag</code>, <code>deletePatientCustomTag</code> &mdash; all threaded through <code>resolveEffectiveUserId</code>, so admin viewing-as Karen writes under her <code>user_id</code> with the admin&rsquo;s <code>auth.uid</code> recorded for audit. All three share an internal <code>mutateCustomTags</code> helper that reads attachments, runs the per-fn mutation, writes back &mdash; same JSONB-merge pattern as the seeded-tag fns. (3) New <code>CustomTagsSection</code> at the bottom of the Soft tags card: lists existing custom tags with inline edit affordance per row, &ldquo;Add a custom tag&rdquo; button when nothing is being edited, edit form expands in place with name + value + reason fields + Save / Cancel / Delete. Same chip / pill visual language as the seeded tags. Live tag count in the section header. (4) <code>PatientSoftTagKey</code> tightened to exclude <code>custom</code> &mdash; the seeded-tag fns operate on fixed keys; custom tags use a separate API surface. <strong>Why this matters</strong>: the seeded six are a starting point, not the ceiling. The Profitability Engine spec &sect;3.2 notes the soft-tag layer is intentionally elastic &mdash; what Karen tracks evolves as her practice grows. v1.31.1 lets her grow it without a code change. <strong>What this DOESN&rsquo;T fix</strong>: per-tenant tag catalog (Karen still adds tags per-patient one at a time; a &ldquo;define this tag once, set it on many patients&rdquo; catalog surface is deferred to a future ship when there&rsquo;s enough volume to justify). Enum-typed custom tags (today all custom values are free text; an &ldquo;Allergies&rdquo; tag with a pre-defined enum like &ldquo;lidocaine / latex / nickel&rdquo; isn&rsquo;t available, but Karen can capture the same info as free-text values). Custom-tag history (only most-recent value stored; older values overwritten on edit). <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (PatientCustomTag type + custom field on PatientSoftTags + tightened PatientSoftTagKey), <code>src/server/patient-ingest.functions.ts</code> (PatientCustomTag import + three new server fns sharing the mutateCustomTags helper), <code>src/routes/app.refill.patients.$patientId.tsx</code> (CustomTagsSection + CustomTagRow + CustomTagForm components + Plus icon import + wired into SoftTagsCard below the seeded six), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.31.0",
    date: "June 2026",
    items: [
      "<strong>v1.31.0 &mdash; Patient Soft-Tags: Karen&rsquo;s editorial layer lands on the patient detail page.</strong> First primitive of the Profitability Engine substrate (spec &sect;3.2) after the Catalog primitive shipped end-to-end last sesh (v1.29 &rarr; v1.30.1). Soft-Tags are the editorial signals only Karen can know &mdash; income tier, negotiator stance, specials-seeker flag, personality read, shopper loyalty, cultural / private notes &mdash; that her 16 years at the front desk taught her to track. The engine never infers these; they are Karen-tap-set only. Every entry carries provenance (set-by user + timestamp) plus an optional reason note Karen leaves to remind future-her what tipped her off. <strong>What ships</strong>: (1) Six tags on each patient: <code>incomeTier</code> (high &middot; mid &middot; low &middot; unknown), <code>negotiator</code> (never &middot; occasional &middot; always), <code>specialsSeeker</code> (yes &middot; no), <code>personality</code> (easy &middot; neutral &middot; complainer), <code>shopperLoyalty</code> (loyal &middot; comparison &middot; unknown), <code>culturalNotes</code> (free text, owner-private). (2) New &lsquo;Soft tags&rsquo; card on the patient detail page, slotted between Lifetime and Brand-mix. Inline-edit pattern (click a row, pick a chip, optional reason note, save) &mdash; no modal. Provenance footer shows when set. Clear-tag affordance per row. (3) Two new server fns &mdash; <code>setPatientSoftTag</code> and <code>clearPatientSoftTag</code> &mdash; both threaded through <code>resolveEffectiveUserId</code>, so admin viewing-as a tenant owner writes under the tenant&rsquo;s <code>user_id</code> with the admin&rsquo;s <code>auth.uid</code> recorded as <code>setByUserId</code> for audit. (4) Owner-only visibility is automatic: every read scopes by <code>effectiveUserId</code>, so cross-tenant leakage is structurally impossible. <strong>Zero-migration ship</strong>: tags live on <code>knowledge_nodes.attachments.softTags</code> (a field on the PatientSummary JSONB blob, same pattern as the v385.2 VIP flag). Survives QB re-imports via the rollup-preservation path. <strong>Latent v385.2 bug also fixed</strong>: <code>extractContactSummary</code> was missing <code>vip</code> propagation, meaning Karen&rsquo;s VIP picks were silently wiped on every sales-CSV re-upload since v385.2. The same one-line touch that adds <code>softTags</code> preservation also adds <code>vip</code> preservation &mdash; VIP designations now actually survive re-rolls as v385.2 originally intended. <strong>What this unblocks</strong>: scoring primitives 12-16 of the Profitability Engine &sect;4 (income tier &middot; willingness-to-pay &middot; specials-seeker negative &middot; personality &middot; shopper loyalty) all read directly from this layer. Karen can start tagging her real Rejuv patients as she works through them; the next ship (v1.x.2 Life-Events) and v1.x.3 (17-factor scoring) compose on top. <strong>What this DOESN&rsquo;T fix</strong>: bulk-tagging UI (Karen tags one patient at a time today &mdash; bulk surface comes after the A-list dashboard learns the patterns). Per-role visibility config (the spec calls out staff-practitioner scope as &sect;13 open-question #4; default is owner-only, which is what this ships). Tag change history (only most-recent entry stored; older values overwritten &mdash; if Karen revisits in 3 months, the prior reason note is gone). <strong>Touched</strong>: <code>src/lib/patient-csv.ts</code> (new types: PatientSoftTagEntry generic + PatientSoftTags struct + 4 enum unions + PatientSoftTagKey + extension to PatientContactSummary), <code>src/server/patient-ingest.functions.ts</code> (extended PatientListRow with softTags + hydratePatientListRow maps it through + extractContactSummary preserves vip and softTags through re-rolls + new setPatientSoftTag and clearPatientSoftTag server fns with discriminated-union zod validation), <code>src/routes/app.refill.patients.$patientId.tsx</code> (SoftTagsCard + TagRowShell + EnumTagRow + BoolTagRow + TextTagRow + ChipButton + wired into the detail page JSX with local state mirror for optimistic updates), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.30.1",
    date: "June 2026",
    items: [
      "<strong>v1.30.1 &mdash; Dual-ingest catalog CSV: services + products from one QB drop.</strong> v1.29.4 imported only the Service-typed rows from Karen&rsquo;s QB Item List, leaving 92 Non-inventory rows (her filler / tox / skincare drug inventory: Voluma, Vollure, Belotero Balance, Botox, Daxxify, etc.) sitting un-imported &mdash; the entire Filler bucket on /catalog/services came up nearly empty as a result. v1.30.1 closes that gap: the same QB export now populates BOTH tables in a single drop. Service rows go to <code>services</code>; Non-inventory rows go to <code>products</code> with manufacturer + category + unit_type auto-routed via the canonical brand registry (v1.30.0). <strong>What ships</strong>: extended <code>ingestServicesCsvFn</code> &mdash; same endpoint, dual-table commit. Same parser, same preview-then-commit flow. Server-side now (1) loads canonical brands at the start of every import, (2) for each Non-inventory row, runs <code>lookupCanonicalBrand</code> against name + notes, (3) on match: sets manufacturer + product-category + unit_type from the canonical entry; on miss: defaults to manufacturer=null + category=other + unit_type=other, (4) on commit, services + products write in the same pass with separate upsert-by-name / upsert-by-brand keys. <strong>Category mapping</strong>: canonical brands use the service taxonomy (tox/filler/laser/facial/skincare/other); products have a narrower taxonomy (tox/filler/laser_consumable/skincare/other). Mapping: tox→tox, filler→filler, laser→laser_consumable, facial→other, skincare→skincare. So a canonical &lsquo;BBL&rsquo; (laser) becomes a product with category=laser_consumable when imported via this path. <strong>UI</strong> (<code>/app/refill/catalog/import</code>): summary chip section split into Services / Products / File-totals bands; preview table action chips now distinguish &ldquo;Create svc&rdquo; / &ldquo;Update svc&rdquo; vs. &ldquo;Create prod&rdquo; / &ldquo;Update prod&rdquo; (no more amber &ldquo;Skip (product)&rdquo;); rows show &ldquo;&rarr; <em>Canonical Brand</em>&rdquo; chip when name matched the registry; category column shows &ldquo;(from registry)&rdquo; for matched products and &ldquo;(default)&rdquo; for unmatched ones. Commit button copy: &ldquo;Import N services + M products&rdquo;. Receipt screen lists both counts. Import CSV button added to <code>/app/refill/catalog/products</code> PageHeader. PageHeader title flips from &ldquo;Import services from CSV&rdquo; to &ldquo;Import catalog from CSV.&rdquo; <strong>What this unblocks for Karen-Rejuv specifically</strong>: dropping the SAME 217-row QB export she already used populates her products catalog with the 92 Non-inventory rows: Voluma / Vollure / Volbella / Restylane Lyft / Defyne / Refyne / Kysse / Belotero Balance / Radiesse / Botox / Daxxify / etc. Manufacturer auto-set (Voluma → AbbVie, Restylane line → Galderma, Belotero → Merz, Daxxify → Revance), cost from Purchase price column (margin math hot from day one). Plus the canonical-registry match count surfaces as a summary chip so Karen can see at a glance how many brand drugs were auto-classified. <strong>What this DOESN&rsquo;T fix</strong>: auto-link products to services via service_products linkage on import (queued v1.30.2 candidate &mdash; a &ldquo;link this product to any service whose name contains the same brand&rdquo; sweep). Tenant-level brand overrides. Suggest-new-brand workflow from import preview (admin sees unmatched product rows + promotes them to canonical_brands). <strong>Touched</strong>: <code>src/server/refill-catalog.ts</code> (ImportPreviewRow extended with product fields + matchedBrand; ImportPreview extended with willCreateProduct / willUpdateProduct / matchedToCanonical; ImportReceipt extended with productsCreated / productsUpdated; ingestServicesCsvFn server fn extended with products upsert path + canonical brand lookup integration + canonicalToProductCategory mapper), <code>src/routes/app.refill.catalog.import.tsx</code> (title + description copy + summary chip layout + preview table action chips + brand-match chip + commit button copy + receipt counts), <code>src/routes/app.refill.catalog.products.tsx</code> (Import CSV button in PageHeader), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.30.0.1",
    date: "June 2026",
    items: [
      "<strong>v1.30.0.1 &mdash; Inline persistent banner for Re-categorize result + console diagnostics.</strong> v1.30.0&rsquo;s Re-categorize button worked correctly &mdash; the receipt came back with <code>recategorized: 0</code> because services already had correct categories from v1.29.4 regex inference, and the toast.success briefly flashed then disappeared. Easy to miss. <strong>This ship replaces fleeting toast with a persistent inline emerald banner</strong> above the services list showing scanned / recategorized / unchanged / unmatched counts + expandable changes list. Stays visible until dismissed. Toasts kept as belt-and-suspenders (now 8-10 sec duration). Each toast call wrapped in its own try/catch + console.log/error so a sonner-side failure can&rsquo;t eat the banner. Console diagnostics at every step: <code>[recategorize] start</code>, <code>[recategorize] result</code>, <code>[recategorize] finally</code>, <code>[recategorize] caught error</code>, <code>[recategorize] toast threw</code>. <strong>Self-correction note</strong>: v1.30.0.1 shipped without a changelog entry, leaving the v-pill stuck at v1.30.0 even though the code was deployed. v1.30.0.2 is this entry (changelog-only), so the v-pill now correctly reflects the deployed bundle. <strong>Touched</strong>: <code>src/routes/app.refill.catalog.services.tsx</code> (banner state + JSX + console logs in v1.30.0.1), <code>src/lib/changelog.ts</code> (this entry, retroactively in v1.30.0.2).",
    ],
  },
  {
    version: "v1.30.0",
    date: "June 2026",
    items: [
      "<strong>v1.30.0 &mdash; Canonical brand registry: the first piece of cross-tenant intelligence Refill carries.</strong> Grasshopper&rsquo;s architectural call (2026-06-01): instead of regex pattern-matching on service names (v1.29.4.x), maintain a curated database of aesthetics-industry brands the platform owns. CSV imports + recategorize sweeps match against display name + aliases. Adding a brand propagates to every tenant. Per <code>project_data_intelligence_thesis</code>: per-spa configuration in the foreground, cross-spa knowledge accumulating in the background. <strong>What ships (4 pieces)</strong>: (1) <code>supabase/migrations/20260601030000_v1_30_0_canonical_brands.sql</code> &mdash; new system-wide <code>canonical_brands</code> table with display_name + aliases[] + category + manufacturer + unit_type + notes, RLS read-allowed for authenticated / writes for service_role. (2) <strong>Seed of ~52 brands</strong>: Tox (5: Botox, Dysport, Xeomin, Jeuveau, Daxxify), Filler (22: full Juvederm Voluma/Vollure/Volbella/Ultra/Volux line + full Restylane Refyne/Defyne/Kysse/Lyft/Silk/Contour/Eyelight line + RHA Collection + Belotero + Radiesse + Sculptra + Versa), Laser (9: BBL/Halo/Moxi/Fraxel/CO2 Fractional/Thulium/IPL/Morpheus8/LHR), Skincare (12: SkinCeuticals C E Ferulic + Phloretin + EltaMD UV Clear/Daily/Sport + ZO + Obagi + Neocutis + Alastin + Revision + Anthelios), Facial (4: HydraFacial/Diamond Glow/AquaGold/Dermaplane). Each entry has a rich alias list (e.g. Voluma matches &ldquo;Voluma&rdquo;, &ldquo;Voluma XC&rdquo;, &ldquo;Juvederm Voluma&rdquo;, &ldquo;Juvederm Voluma XC&rdquo;). (3) <strong><code>lookupCanonicalBrand(name, brands)</code> matcher</strong> in <code>src/server/refill-catalog.ts</code>: flattens all aliases into pairs, sorts by alias length descending (most specific wins &mdash; &ldquo;Restylane Refyne&rdquo; beats &ldquo;Restylane&rdquo;), tests each as a case-insensitive word-boundary regex. First match wins. Returns null → caller falls back to existing regex inference. (4) <strong><code>recategorizeServicesFromBrandsFn</code> retroactive sweep</strong>: scans every existing service in the tenant, applies canonical lookup against name + notes, updates category where matched. Surfaced as a <strong>&ldquo;Re-categorize&rdquo; button</strong> on <code>/catalog/services</code> PageHeader (next to Import CSV + Add service). Karen&rsquo;s 85 &ldquo;Other&rdquo; rows get sorted into Tox / Filler / Laser / etc. without re-importing. <strong>Admin CRUD at <code>/app/admin/canonical-brands</code></strong>: full create / edit / delete UI with search box across display name + aliases, grouped by category, comma-separated alias input, manufacturer dropdown, category + unit-type pickers. Server fns gate on <code>app_role = &apos;admin&apos;</code>. <strong>What this unblocks</strong>: v1.30.1 will wire the canonical lookup into <code>ingestServicesCsvFn</code> (currently only applies on the retroactive sweep, not at import time) and bundle the products-CSV import path. v1.30.2 will add the &ldquo;Suggest new brand&rdquo; flow from import preview &mdash; unmatched names get queued for admin promotion. <strong>What this DOESN&rsquo;T fix</strong>: import-time canonical lookup (queued v1.30.1). Suggest-new-brand workflow. Tenant-level brand overrides. Auto-link product rows to canonical brands on products-CSV import. <strong>Touched</strong>: <code>supabase/migrations/20260601030000_v1_30_0_canonical_brands.sql</code> (new, schema + seed), <code>src/server/refill-catalog.ts</code> (CanonicalBrand types + lookupCanonicalBrand + recategorizeServicesFromBrandsFn + listCanonicalBrandsFn + createCanonicalBrandFn + updateCanonicalBrandFn + deleteCanonicalBrandFn + requireAdmin helper), <code>src/routes/app.admin.canonical-brands.tsx</code> (new, ~480 lines admin CRUD with search + category grouping + alias chip rendering), <code>src/routes/app.refill.catalog.services.tsx</code> (Re-categorize button + onRecategorize handler), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.29.4.3",
    date: "June 2026",
    items: [
      "<strong>v1.29.4.3 &mdash; Header-row detection that doesn&rsquo;t get fooled + positive auto-detection UX.</strong> v1.29.4.2&rsquo;s title-row skip was too eager: QB&rsquo;s subtitle row contains &lsquo;Product/Service List&rsquo; which substring-matches the name-column alias &lsquo;product/service&rsquo;, so the parser stopped at row 1 and started data parsing from row 2 onward &mdash; treating the REAL header row (&lsquo;Product/Service full name | Type | Memo/Description | Sales price | Purchase price&rsquo;) as the first data row. Result: 218 rows parsed with name = the literal header strings, every other column &lsquo;not found&rsquo;, every price $0. <strong>Fix #1 &mdash; multi-candidate scoring</strong>: <code>findHeaderRowIndex</code> now scans the first 8 rows and SCORES each by how many cells look like recognized headers across ALL field candidates (name + type + price + cost + category + description + duration). Real header rows score 4-5; title rows score 0-1. Plus a guard: rows with fewer than 2 non-empty cells are skipped entirely (disqualifies single-cell title / subtitle rows even when they substring-match a candidate). Highest score wins. For Karen&rsquo;s QB export: row 0 &lsquo;REJUV SKIN SPA, LLC&rsquo; scores 0, row 1 &lsquo;Product/Service List&rsquo; has 1 non-empty cell &mdash; skipped, row 2 blank &mdash; skipped, row 3 has 5 cells matching 5 candidates &mdash; SELECTED. Preview note now reads &lsquo;Skipped 3 preamble rows&rsquo;. <strong>Fix #2 &mdash; positive auto-detection callout</strong>: when name column IS found, a green-emerald banner replaces the prior &lsquo;not found&rsquo;-everywhere visual: &ldquo;Auto-detected N of 5 columns. Smart-mapping found your QB columns. Review the table below to confirm &mdash; anything marked &lsquo;not found&rsquo; is optional and won&rsquo;t block the import.&rdquo; Failing path retains a red AlertCircle banner. Mapping table rows now show a green CheckCircle next to each found field, and the found-header value renders in emerald-on-emerald-soft instead of plain mono; missing fields render as a quiet em-dash instead of italics &lsquo;not found&rsquo;. UX reads as success when it succeeds. <strong>Touched</strong>: <code>src/lib/catalog-csv.ts</code> (findHeaderRowIndex algorithm rewrite), <code>src/routes/app.refill.catalog.import.tsx</code> (auto-detection banner + per-row checkmarks + emerald success styling on found headers), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.29.4.2",
    date: "June 2026",
    items: [
      "<strong>v1.29.4.2 &mdash; QuickBooks reality check: 4 fixes after the first real Rejuv export.</strong> Per <code>feedback_validate_against_real_exports</code>: shipped v1.29.4 + .1 with educated guesses; the first real Karen-Rejuv QB Item List export surfaced 4 things the parser missed. <strong>(1) Title-row skip</strong>: QB prepends a company-name row (&ldquo;REJUV SKIN SPA, LLC,,,,&rdquo;) above the actual headers. v1.29.4 treated it as the header row, so 219/219 rows parsed as junk. New <code>findHeaderRowIndex</code> searches the first 8 lines for a row containing a recognizable name-column header and starts parsing from there. Preview now reports &ldquo;Skipped N preamble rows&rdquo; when this triggers. <strong>(2) QB-actual column aliases</strong>: <code>Product/Service full name</code> (the QB name column, leading alias now), <code>Purchase price</code> (QB&rsquo;s cost column &mdash; not &ldquo;Cost&rdquo;), <code>Memo/Description</code> (QB&rsquo;s description column). <strong>(3) Type-as-filter, not category</strong>: QB&rsquo;s Type column values are <code>Service</code> / <code>Non-inventory</code> / <code>Inventory</code> &mdash; these classify the ROW (service vs product), not the category. v1.29.4.1 had been (incorrectly) mapping Type into the category enum and falling back to name-inference when the values didn&rsquo;t match. v1.29.4.2 introduces <code>TYPE_HEADER_CANDIDATES</code> separate from <code>CATEGORY_HEADER_CANDIDATES</code> and the parser threads <code>isService</code> through every row. Category continues to come from name-inference (botox &rarr; tox, BBL &rarr; laser, HydraFacial &rarr; facial). <strong>(4) Skip non-service rows on commit</strong>: any row with Type matching Non-inventory / Inventory is now flagged as &ldquo;Skip (product)&rdquo; in the preview (amber chip, dimmed row) and excluded from the commit. These are retail items Karen resells &mdash; they belong in <code>/catalog/products</code> once the products-CSV path ships. Preview summary chip surfaces the skip count. <strong>UI changes</strong>: column mapping table gains a &ldquo;Service / Product type&rdquo; row; preview table gains a Type column; non-service rows render dimmed with a &ldquo;Skip (product)&rdquo; chip; commit button copy + disabled-logic counts only willCreate + willUpdate (not parseable total, since non-service rows are parseable but intentionally skipped). <strong>What this DOESN&rsquo;T fix</strong>: the products-CSV import path (still queued; needs its own preview + commit + manufacturer-detection). Negative-price rows still default to $0 with a warning (Karen-fixable in services UI). Category inference doesn&rsquo;t look up product-affinity (a &ldquo;Belotero&rdquo; row currently maps to filler via the filler regex; if a product name is too generic, it lands in &lsquo;other&rsquo;). <strong>Touched</strong>: <code>src/lib/catalog-csv.ts</code> (findHeaderRowIndex helper + TYPE_HEADER_CANDIDATES + new QB-leading aliases + isService flag + headerRowIndex + nonServiceRows count), <code>src/server/refill-catalog.ts</code> (ImportPreview gains type + headerRowIndex + nonServiceRows + willSkipNonService; commit skips non-service rows), <code>src/routes/app.refill.catalog.import.tsx</code> (Type row in mapping + skip-preamble note + Non-service summary chip + Type column in preview table + Skip chip + dimmed rows + commit count adjusted), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.29.4.1",
    date: "June 2026",
    items: [
      "<strong>v1.29.4.1 &mdash; QuickBooks-first reframe + Cost column &rarr; cogs_per_service.</strong> v1.29.4 led the import parser + copy with Acuity (scheduling platform). Grasshopper called the wrong claim: the typical catalog data source is the ACCOUNTING platform (QuickBooks), not scheduling, because that&rsquo;s where cost-of-goods lives. Margin math is hot on first import only if we read QB&rsquo;s Cost column &mdash; which v1.29.4 missed. <strong>Three corrections</strong>: (1) parser header candidates reordered with QB Item List columns FIRST (<code>Item</code>, <code>Sales Price</code>, <code>Cost</code>, <code>Type</code>, <code>Sales Description</code>), Acuity / Square / Vagaro columns retained as fallback; (2) new <code>COST_HEADER_CANDIDATES</code> + collision-avoidance with the price column (if a small-practice CSV uses &lsquo;Cost&rsquo; as the price column, we don&rsquo;t double-map); (3) Cost column parses into <code>parsedCogs</code> and flows into <code>services.cogs_per_service</code> on insert / update &mdash; on update we honor cogs_source by NOT overwriting cogs on services already in <code>derived</code> mode (auto-derived from product linkage takes priority over CSV-cost). <strong>UI changes</strong>: column-mapping table gains a Cost / COGS row; summary chips add &lsquo;With COGS&rsquo; count; preview table gains a COGS column showing the per-row cost (or &mdash; when blank). Empty-state copy and PageHeader description both lead with &ldquo;QuickBooks Item List&rdquo; first. <strong>Touched</strong>: <code>src/lib/catalog-csv.ts</code> (header candidates reordered + COST_HEADER_CANDIDATES + parsedCogs in ParsedServiceRow + cost-column collision avoidance), <code>src/server/refill-catalog.ts</code> (ImportPreview gains <code>cost</code> field-mapping + <code>withCogs</code> count + commit honors derived-mode on existing rows), <code>src/routes/app.refill.catalog.import.tsx</code> (copy reframe + COGS column in mapping + summary + preview table), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.29.4",
    date: "June 2026",
    items: [
      "<strong>v1.29.4 &mdash; Service-list CSV import: preview-then-commit upsert by name.</strong> Closes the v1.29.x Catalog sprint. Drop your Acuity service-list export (or any CSV with a service-name column) at <code>/app/refill/catalog/import</code> &rarr; client reads the file &rarr; server parses + auto-detects columns + previews what would land &rarr; you eyeball it &rarr; Commit upserts. <strong>Per <code>feedback_validate_against_real_exports</code></strong>: this ships with the parser scaffolded against common Acuity column shapes (Name / Category / Price / Description / Duration variants), but is NOT claimed as &ldquo;Acuity-validated&rdquo; until the first real export has been dropped + iterated to clean. Synthetic fixtures are not validation; that&rsquo;s rule one. <strong>Parser (<code>src/lib/catalog-csv.ts</code>)</strong>: hand-rolled CSV tokenizer (handles quoted fields + escaped quotes + commas inside quotes) mirroring patient-csv style; fuzzy header matching with substring fallback; name-based category inference when the CSV has no category column (botox/dysport/jeuveau/daxxify &rarr; tox; juvederm/restylane/radiesse &rarr; filler; bbl/ipl/moxi/halo/fraxel/laser &rarr; laser; hydrafacial/peel/microderm/dermaplane &rarr; facial; skincare/serum/cleanser &rarr; skincare; default other); price parsing strips currency symbols + commas + whitespace. <strong>Server fn <code>ingestServicesCsvFn</code></strong>: single endpoint with <code>mode: &apos;preview&apos; | &apos;commit&apos;</code> &mdash; preview returns parse stats + field mapping + per-row preview with action (create / update) + warnings; commit re-parses (don&rsquo;t trust client) and upserts by name (case-insensitive within tenant scope). Re-uploading the same file is safe &mdash; no duplicates created. Failed rows surface in the receipt with row index + reason. <strong>UI (<code>app.refill.catalog.import.tsx</code>)</strong>: drag-drop dropzone or file picker; on file drop, automatic preview runs; column mapping table shows which CSV header maps to which field (with required-flag on Name); summary chips (Total / Parseable / Will create / Will update); preview table of first 30 rows with auto-detected category + &lsquo;(from name)&rsquo; / &lsquo;(default)&rsquo; chips when inferred; warnings inline per row; Import button disabled if no name column found (required gate). Receipt screen on commit success: created count, updated count, failed rows with reason, links to View services or Import another file. <strong>Discovery</strong>: &lsquo;Import CSV&rsquo; button added to <code>/app/refill/catalog/services</code> PageHeader next to &lsquo;Add service&rsquo; &mdash; first-class affordance, not buried. <strong>What this DOESN&rsquo;T fix</strong>: products CSV import (queued; needs manufacturer-portal export samples). Bulk re-categorize. Service-duration storage (parser reads the column, server ignores it &mdash; duration isn&rsquo;t stored on services yet). Import undo. Auto-link products to imported services (separate v1.x feature once products-CSV ships). <strong>The real-export validation cycle</strong>: when Grasshopper drops his Karen-Rejuv Acuity export, the column mapping table will show the actual headers in his file; if any are unmapped or misclassified, the parser learns the new alias and re-ships. No row writes until the parser shows green on his real data. <strong>Touched</strong>: <code>src/lib/catalog-csv.ts</code> (new, ~260 lines parser), <code>src/server/refill-catalog.ts</code> (added ImportPreview + ImportReceipt types + ingestServicesCsvFn, ~160 lines), <code>src/routes/app.refill.catalog.import.tsx</code> (new, ~430 lines UI), <code>src/routes/app.refill.catalog.services.tsx</code> (Import CSV button in PageHeader), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change beyond the Import button on Services page.",
    ],
  },
  {
    version: "v1.29.3.1",
    date: "June 2026",
    items: [
      "<strong>v1.29.3.1 &mdash; Free Manual/Auto toggle: both buttons always clickable.</strong> v1.29.3 disabled the Auto button when there were zero linked products and disabled the currently-active mode button. The intent was guardrails; the effect was friction. Grasshopper&rsquo;s feedback: let me A/B freely. <strong>What changes</strong>: both Manual and Auto are now always clickable (subject only to the global busy flag during in-flight requests). Clicking Auto with zero links sets COGS to $0 &mdash; harmless and discoverable; the COGS field shows the explicit value and tooltip explains. <strong>Bonus side effect</strong>: clicking Auto a second time on an already-derived service re-runs <code>computeDerivedCogs</code> and persists the new sum. This is the manual-refresh path for the &ldquo;stale derived COGS when a product cost changes&rdquo; trade-off flagged in v1.29.3 &mdash; no separate sweep job needed; the owner re-clicks Auto after editing product costs and the value reconciles. <strong>Tooltip copy added</strong>: Manual button reads &ldquo;Currently in Manual mode&rdquo; (when active) or &ldquo;Switch to Manual COGS&rdquo; (when not). Auto button reads &ldquo;Click to re-derive COGS from current product costs&rdquo; (when active), &ldquo;Will set COGS to $0 &mdash; link products below to populate&rdquo; (when 0 links), or &ldquo;Switch to Auto COGS from linked products&rdquo; (default). <strong>Touched</strong>: <code>src/routes/app.refill.catalog.services.tsx</code> (LinkageSection toggle buttons), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.29.3",
    date: "June 2026",
    items: [
      "<strong>v1.29.3 &mdash; Service-Products linkage + auto-COGS derive: the structural payoff of the Catalog substrate.</strong> Karen enters her products once + her services once + the math composes itself. Three things ship together: (1) the linkage UI inside the service edit form, (2) the Manual/Auto-COGS toggle, (3) a small UX consistency fix where the Products form now puts Sales price BEFORE Cost (matches the Services form ordering Grasshopper preferred). <strong>Linkage UI (the main ship)</strong>: inline section inside any service&rsquo;s edit card &mdash; &ldquo;Products consumed&rdquo; subsection that lists each linked product as a row (brand + cost/unit + editable quantity + remove). At top: Manual/Auto toggle. At bottom: an Add-product form (dropdown of unlinked products in the tenant&rsquo;s catalog + qty input + Link button). Real-time computed sum line below the list shows <code>SUM(product.cost_per_unit × quantity_per_service)</code> with an &ldquo;Active&rdquo; chip when in Auto mode. <strong>Auto-COGS semantic</strong>: when Karen flips to Auto, server computes the sum and writes it to <code>services.cogs_per_service</code>, sets <code>cogs_source = &apos;derived&apos;</code>. Every subsequent link / unlink / quantity-change on that service triggers a re-sync via <code>syncDerivedCogsIfNeeded</code>. The COGS field in the form is disabled and visually distinct (emerald background) when auto-derive is active &mdash; clear visual signal that &ldquo;this number is being managed for you.&rdquo; Toggle back to Manual leaves the current derived value as the starting point Karen can edit. Pre-flight: Auto button is disabled until at least one product is linked. <strong>UX consistency fix</strong>: Products form now reads Sales price &rarr; Cost per unit (same order as Services form). Both the form inputs AND the card-row display flipped to &ldquo;<code>$X sell &middot; $Y cost</code>&rdquo;. Karen learns one mental order across both surfaces. <strong>New server fns in <code>refill-catalog.ts</code></strong>: <code>listServiceProductsFn</code> (returns service + links bundle), <code>linkProductToServiceFn</code> (upsert on the unique service_id+product_id constraint so re-linking the same product updates quantity), <code>updateServiceProductQuantityFn</code>, <code>unlinkServiceProductFn</code>, <code>setServiceCogsSourceFn</code>. Each mutation that touches linkage on a derived-mode service auto-recomputes and persists the new cogs_per_service. Defense-in-depth: every fn re-validates that both joined service AND product belong to the caller&rsquo;s tenant. <strong>Architectural notes</strong>: (1) Recomputation is app-side, not Postgres-trigger-based &mdash; keeps the migration footprint small and makes the derive logic visible in TypeScript. Trade-off: when a product&rsquo;s <code>cost_per_unit</code> changes via Products CRUD, dependent services in derived mode have stale COGS until the next time Karen touches the linkage. v1.29.4 candidate to add a &ldquo;Refresh derived COGS&rdquo; sweep that re-runs across all derived services on cost change. (2) Linkage UI only renders in edit mode &mdash; a service must exist before it can have linked products (no link-during-create flow). (3) Products list is fetched once on services-page mount, parallel with services fetch, so the dropdown is hot when Karen enters edit mode. <strong>Verification walk</strong>: ensure at least one product exists at <code>/catalog/products</code> (Botox vial $400 cost / $1200 sell) &rarr; navigate to <code>/catalog/services</code> &rarr; add a service &ldquo;Botox / unit&rdquo; price $14 with COGS blank &rarr; click to edit &rarr; Products consumed section renders &ldquo;No products linked yet&rdquo; &rarr; pick Botox vial from the dropdown + qty <code>0.01</code> (assuming the vial is 100 units) + Link &rarr; row appears showing Botox · $400/vial · 0.01 · $4.00 contribution &rarr; flip to Auto &rarr; COGS field disables and shows <code>4.00</code> &rarr; margin updates to <code>$10.00 (71%)</code>. <strong>What this DOESN&rsquo;T fix</strong>: no &ldquo;refresh derived COGS&rdquo; sweep when product costs change (queued). No bulk-link UI. No tenant-customizable categories. No CSV import (v1.29.4). <strong>Rollback</strong>: <code>git revert</code>; no DB damage (service_products table already exists from v1.29.0; derived COGS values are recomputable). <strong>Touched</strong>: <code>src/server/refill-catalog.ts</code> (extended with linkage types + 5 new server fns + computeDerivedCogs + syncDerivedCogsIfNeeded helpers), <code>src/routes/app.refill.catalog.services.tsx</code> (rewritten to include linkage UI inline in edit form + LinkageSection + LinkRow components), <code>src/routes/app.refill.catalog.products.tsx</code> (form field order swap + card row text flip), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.29.2",
    date: "June 2026",
    items: [
      "<strong>v1.29.2 &mdash; Services CRUD + Catalog chip lifted into RefillNav.</strong> Two things land together: the Services UI mirror of v1.29.1 Products, AND the Catalog chip surfaced as the second nav chip (after Patients, before Recovery). <strong>Nav decision reversal</strong>: the v1.29.0/.1 changelogs said catalog stays deep-link-only per <code>project_refill_trojan_horse_thesis</code>. After v1.29.1 walked clean, Grasshopper called it &mdash; the trojan-horse thesis is about PUBLIC-facing positioning (landing / scan / onboard), not the authenticated nav Karen sees after logging in. Hiding daily-use surfaces behind deep links makes her work harder for no positioning benefit. Slot: Patients (WHO data) &rarr; Catalog (WHAT data) &rarr; Recovery (action) &rarr; Inbox (comms) &rarr; Settings (config) &rarr; Billing (back-office). The chip routes to <code>/app/refill/catalog/products</code> by default; the products / services routes are siblings under <code>/catalog</code>. <strong>Services UI (the main ship)</strong>: mirror of v1.29.1 Products against the <code>services</code> table. Surface at <code>/app/refill/catalog/services</code>. <strong>What lands</strong>: extended <code>src/server/refill-catalog.ts</code> with four service server fns &mdash; <code>listServicesFn</code>, <code>createServiceFn</code>, <code>updateServiceFn</code>, <code>deleteServiceFn</code>. Same auth pattern verbatim (verifyAuth &rarr; resolveEffectiveUserId &rarr; getTenantIdForUser). <strong>UI</strong>: <code>src/routes/app.refill.catalog.services.tsx</code> mirrors the products page shape: grouped by category, one card per service, click to edit in place, inline add expander, delete with confirm, empty-state CTA. <strong>Key differences from products</strong>: (1) service categories diverge from product categories per the v1.29.0 migration &mdash; services have <code>laser</code> + <code>facial</code> (treatment categories), products have <code>laser_consumable</code> (product category); (2) <code>cogs_per_service</code> is NULLABLE &mdash; Karen can enter a service with sales price only and leave COGS blank now, v1.29.3 will auto-derive COGS from the product linkage; (3) when COGS is null, the row renders &ldquo;Set COGS for margin&rdquo; instead of the margin number; (4) cogs_source flag stamped to <code>manual</code> on create / update (v1.29.3 introduces the <code>derived</code> path); (5) services have no manufacturer column (manufacturer attribution lives on products only, flows through linkage). <strong>UX detail</strong>: the COGS field placeholder reads &ldquo;Leave blank to auto-derive in v1.29.3&rdquo; &mdash; signals the upcoming feature without forcing immediate entry. <strong>What this unblocks</strong>: v1.29.3 service_products linkage UI + auto-derive toggle that rewrites cogs_per_service from <code>SUM(product.cost_per_unit × quantity_per_service)</code> and flips cogs_source to <code>derived</code>. v1.29.4 Acuity CSV import (validated against Karen&rsquo;s real export). <strong>What this DOESN&rsquo;T fix</strong>: no auto-derive yet (v1.29.3). No linkage UI yet (v1.29.3). No nav-chip widening per <code>project_refill_trojan_horse_thesis</code>. No CSV import. No bulk-edit. <strong>Verification</strong>: log in &rarr; navigate to <code>/app/refill/catalog/services</code> &rarr; empty state renders &rarr; add &ldquo;Botox treatment / unit&rdquo; (Tox category, price $14, leave COGS blank) &rarr; row renders with &ldquo;Set COGS for margin&rdquo; chip &rarr; edit &rarr; set COGS to $6.50 &rarr; save &rarr; row renders with $7.50 (54%) margin. <strong>Rollback</strong>: <code>git revert</code>; no DB damage (services table already exists from v1.29.0). <strong>Touched</strong>: <code>src/server/refill-catalog.ts</code> (extended with services types + four service fns + ServiceRow shape adapter), <code>src/routes/app.refill.catalog.services.tsx</code> (new file, ~450 lines), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.29.1",
    date: "June 2026",
    items: [
      "<strong>v1.29.1 &mdash; Products CRUD: the first UI surface on the Profitability Engine substrate.</strong> v1.29.0 shipped the schema; v1.29.1 lights it up. Surface at <code>/app/refill/catalog/products</code> &mdash; deep-link only per <code>project_refill_trojan_horse_thesis</code> (the 5-chip RefillNav stays narrow forever). <strong>What lands</strong>: <code>src/server/refill-catalog.ts</code> (new module) with four server fns &mdash; <code>listProductsFn</code>, <code>createProductFn</code>, <code>updateProductFn</code>, <code>deleteProductFn</code>. Auth pattern: <code>verifyAuth</code> &rarr; <code>resolveEffectiveUserId</code> (admin viewAs honored) &rarr; <code>getTenantIdForUser</code> &rarr; tenant-scoped query. Matches refill-billing.ts shape verbatim. <strong>UI</strong>: <code>src/routes/app.refill.catalog.products.tsx</code> renders products grouped by category (Tox / Filler / Laser consumable / Skincare / Other) with one card per product. Each card surfaces brand + unit-type chip + manufacturer chip + cost &rarr; sell &rarr; margin (auto-computed dollars + percentage). Click any card to edit in place; inline Add form expander at top; Delete with confirm at the bottom of the edit form. Empty state has a guided &ldquo;Add your first product&rdquo; CTA explaining that cost + sales price unlock margin math for the entire Profitability Engine. Mobile-responsive (Karen on her phone is the design target). <strong>Form details</strong>: brand + manufacturer dropdown (14 options including the AbbVie / former-Allergan label clarification) + category + unit-type + cost + sales-price + notes; margin preview block updates live as you type. Zod validation server-side; client guards numeric parsing. <strong>Architectural notes</strong>: (1) Server fns return camelCase typed <code>Product</code> objects with <code>marginPerUnit</code> + <code>marginPct</code> pre-computed (compute-in-app per spec). (2) tenant_id check on UPDATE and DELETE matches insert-policy pattern &mdash; defense-in-depth against an ID leak. (3) ProductManufacturer enum exported from <code>src/server/refill-catalog.ts</code> matches the 14 CHECK-constraint values from v1.29.0 migration verbatim. <strong>What this unblocks</strong>: v1.29.2 Services CRUD (structurally identical with a different table) and v1.29.3 service_products linkage + auto-margin derive. v1.29.4 Acuity CSV import per <code>feedback_validate_against_real_exports</code>. <strong>What this DOESN&rsquo;T fix</strong>: no nav-chip widening (per trojan-horse). No CSV import yet. No bulk-edit. No price-history (cost_per_unit / sales_price_per_unit are current-state-only). No tenant-customizable categories. <strong>Verification</strong>: log in to production &rarr; navigate to <code>/app/refill/catalog/products</code> &rarr; empty state renders &rarr; add a Botox 100u vial (AbbVie, cost $400, sell $1200) &rarr; row renders in Tox group with $800 margin (67%) &rarr; click to edit &rarr; change sell price to $1250 &rarr; save &rarr; reload &rarr; persisted. Test admin viewAs via PersonaSwitcher. <strong>Rollback</strong>: <code>git revert</code>; no DB damage (tables already exist from v1.29.0; new rows are tenant-scoped and deletable). <strong>Touched</strong>: <code>src/server/refill-catalog.ts</code> (new file, ~250 lines), <code>src/routes/app.refill.catalog.products.tsx</code> (new file, ~430 lines), <code>src/lib/changelog.ts</code> (this entry). No migration; no nav change.",
    ],
  },
  {
    version: "v1.29.0",
    date: "June 2026",
    items: [
      "<strong>v1.29.0 &mdash; Product Catalog schema: the substrate primitive the entire Profitability Engine depends on.</strong> Spec authored 2026-06-01 at <code>Desktop/Refill-Profitability-Engine-Spec-v0_1.html</code> &mdash; this ship implements §3.1 (the catalog primitive) of the consolidated substrate-plus-engines architecture. <strong>Why this comes first</strong>: every downstream piece &mdash; margin-aware patient profitability scoring, brand-affinity matching for the Recognition Allocation Engine, combo-treatment detection, manufacturer-rebate-tier compounding &mdash; depends on knowing per-product cost / sales-price / margin and per-service COGS. Without the catalog, the 17-factor scoring engine cannot compute factor #1 (gross margin per visit) and the Recognition Engine cannot route Allergan rebates to Allergan-product patients. <strong>What shipped</strong>: <code>supabase/migrations/20260601020000_v1_29_0_product_catalog.sql</code> &mdash; three tables: <code>public.products</code> (per-tenant physical product rows with brand / category / unit_type / cost_per_unit / sales_price_per_unit / manufacturer), <code>public.services</code> (per-tenant service rows with name / category / service_price / cogs_per_service / cogs_source), and <code>public.service_products</code> (many-to-many junction with quantity_per_service for combo-treatment math). Plus <code>set_updated_at</code> trigger (idempotent <code>create or replace</code>), indexes on tenant_id / category / manufacturer, RLS enabled with service-role-only policies (matches the refill_pricing_plans / refill_invoices pattern). <strong>Architectural calls (CTO, 2026-06-01)</strong>: (1) text + CHECK constraint over Postgres ENUM &mdash; matches every modern tenant-scoped table; (2) RLS service-role-only &mdash; matches the established Phase-1.6+ pattern; (3) manufacturer values unified with <code>src/lib/product-manufacturer-map.ts</code> TS enum (abbvie / galderma / evolus / merz / skinceuticals / eltamd / neocutis / obagi / revance / rha / sciton / abbvie-coolsculpting) plus <code>generic</code> + <code>in_house</code> for the long tail &mdash; note Allergan → AbbVie because AbbVie owns the brand post-2020 and the existing TS map already encodes it; (4) <code>cogs_per_service</code> nullable + <code>cogs_source</code> flag (default <code>manual</code>) so v1.29.3 auto-derive can rewrite the scalar from linkage sum and flip the flag without breaking the read path. <strong>What this DOESN&rsquo;T fix</strong>: no CRUD UI yet (v1.29.1 Products, v1.29.2 Services, v1.29.3 linkage). No CSV import yet (v1.29.4, validated against Karen&rsquo;s real Acuity service-list export per <code>feedback_validate_against_real_exports</code>). No price-history ledger (deferred &mdash; current state in cost_per_unit / sales_price_per_unit columns only). No tenant-customizable categories (hard-coded at v1.x.0; revisit when spa #2 onboarding surfaces a category gap). No nav-chip widening (per <code>project_refill_trojan_horse_thesis</code> the 5-chip RefillNav stays narrow forever; catalog is deep-link-only at v1.29.0). <strong>Rollback</strong>: drop in reverse FK order &mdash; <code>drop table public.service_products; drop table public.services; drop table public.products;</code> (the <code>set_updated_at</code> function stays in place; harmless if other tables use it). <strong>Touched</strong>: <code>supabase/migrations/20260601020000_v1_29_0_product_catalog.sql</code> (new file, the migration), <code>src/lib/changelog.ts</code> (this entry). Pure data ship; no worker-code change. v1.29.1 Products CRUD lands the first UI surface against this schema.",
    ],
  },
  {
    version: "v1.28.2",
    date: "May 2026",
    items: [
      "<strong>v1.28.2 &mdash; Billing unification Phase 2: decommission the emma-invoice pg_cron job.</strong> Phase 1 (v1.26.23) restored tenant-aware <code>getInvoicePreview</code> in refill-billing and repointed the Recovery page. Phase 2 turns off the legacy monthly cron that has been silently no-op&rsquo;ing since v1.9 (when the UI moved to <code>refill_pricing_plans</code> writes, leaving zero active rows for the emma writer to iterate over). <strong>Why now</strong>: SCHEDULER_SECRET rotation completed 2026-05-31 15:17. This cron was one of the 6 SCHEDULER_SECRET-bearing pg_cron jobs; decommissioning shrinks next quarter&rsquo;s rotation surface from 6 to 5. <strong>What shipped</strong>: <code>supabase/migrations/20260601010000_v1_28_2_decommission_emma_invoice_cron.sql</code> &mdash; single <code>cron.unschedule(&apos;emma-invoice&apos;)</code> wrapped in an <code>EXISTS</code> guard so re-runs are idempotent. <strong>Verification</strong>: post-run <code>SELECT jobname, schedule, active FROM cron.job</code> should show 5 SCHEDULER_SECRET crons (sweep / preshow / reliability / reconcile / recommendations) plus the unrelated refill-invoice ANON-key cron. <strong>What this unblocks</strong>: v1.28.3 deletes <code>src/server/emma-billing.functions.ts</code> + <code>src/routes/api.cron.emma-invoice.ts</code> (zero callers after Phase 2); v1.28.4 RENAMEs <code>emma_invoices</code> + <code>emma_pricing_plans</code> to <code>_archived_*</code>. <strong>What this DOESN&rsquo;T touch</strong>: <code>emma_recovery_events</code> stays put &mdash; 7 live code paths still read it. Phase 5 (tenant_id schema add) gets its own dedicated scout because v1.28.7&rsquo;s full read-path migration touches the live recovery engine. <strong>Rollback</strong>: re-paste <code>supabase/migrations/20260523010000_emma_invoice_cron.sql</code> with the current rotated secret substituted. <strong>Touched</strong>: <code>supabase/migrations/20260601010000_v1_28_2_decommission_emma_invoice_cron.sql</code> (new file), <code>src/lib/changelog.ts</code> (this entry). Pure data ship; no worker-code change.",
    ],
  },
  {
    version: "v1.28.0",
    date: "May 2026",
    items: [
      "<strong>v1.28.0 &mdash; Karen real-tenant rename: dormantspaowner@test.com &rarr; karen.rejuv@gmail.com.</strong> The v1.27 ship arc (shipped + dry-run-verified earlier 2026-05-30, see session_story_2026-05-30_v127_keystone_shipped) proved the waitlist-invite code end-to-end on the dormantspaowner tenant &mdash; but dormantspaowner was the &lsquo;admin&rsquo;s data bucket&rsquo; stand-in, not Karen-as-herself. v1.28.0 closes that gap: rename the auth identity in place so Karen logs in with her real email and inherits the entire data scope (1,140 patients + 5,754 appointments + the v1.27 emma_noshow_policies seed + the 5 emma_waitlist rows from tonight&rsquo;s dry-run) with zero foreign-key churn. <strong>Why rename-in-place vs new-identity-plus-migration</strong>: same user_id (<code>ecf8bcee-24a6-4a4d-a5f4-381166ca57fc</code>) means no UPDATE sweep across the ~15 user-scoped tables (knowledge_nodes, emma_appointments, emma_waitlist, emma_waitlist_tokens, emma_noshow_policies, emma_reliability_status, emma_recovery_events, emma_rescue_attempts/offers, patient_outreach/state, user_prefs, etc.). One line at the auth layer + one line at the display layer, atomic in a single transaction. The alternative would have generated 15&times; more SQL surface for the same end-state, with real risk of orphaning data on a missed table. <strong>What shipped (DB-side, paste into Supabase dashboard)</strong>: <code>supabase/migrations/20260601000000_v128_0_karen_real_tenant_rename.sql</code>. Single transaction: <code>UPDATE auth.users SET email='karen.rejuv@gmail.com', email_confirmed_at=COALESCE(email_confirmed_at, NOW())</code> on the dormantspaowner user_id (the COALESCE keeps magic-link-ready in case the prior email_confirmed_at was NULL); <code>UPDATE public.tenants SET name='Rejuv Skin Spa'</code> on the tenant_id joined through tenant_memberships where role='owner' (drops the &lsquo;[Dormant]&rsquo; tag from v1.26.21). <strong>Side effect</strong>: admin@refill.platform&rsquo;s default-fallback landing tenant&rsquo;s display name flips from &lsquo;[Dormant] Rejuv Skin Spa&rsquo; to &lsquo;Rejuv Skin Spa&rsquo; — same user_id, same data, just Karen-the-real-user owns it now. Admin still sees the bucket when defaulted-landing. <strong>What this DOESN&rsquo;T change</strong>: PERSONA_EMAILS.karen in <code>src/lib/personas.ts</code> still routes the public /personas magic-link mint flow to <code>testspaowner@test.com</code> (the *.test TLD demo tenant, per the v417 safety rail in <code>src/server/v417-persona-mint.ts</code>); Karen&rsquo;s real production tenant is OUTSIDE the public mint flow by design. <code>rescue_proxy_email</code> on Karen&rsquo;s tenant stays at <code>davidfand303@gmail.com</code> for now (Grasshopper&rsquo;s Mac is the iMessage MCP draft processor — concierge-first per <code>project_concierge_first_activation</code>); flipping it to Karen&rsquo;s own email so blue-bubbles originate from her actual Apple ID is the v1.28.1 follow-up once her Mac side is wired with Claude Desktop + imessage-draft MCP. <strong>First-login path for Karen</strong>: magic-link via Supabase dashboard Auth panel (Authentication &rarr; Users &rarr; karen.rejuv@gmail.com &rarr; Send Magic Link). She taps the link in Gmail, the v410.2 cross-host bridge lands her pre-authenticated on app.getrefill.app. No password setup needed. <strong>What this DOESN&rsquo;T fix</strong>: any browser session previously logged in as dormantspaowner@test.com (none expected — admin uses admin@refill.platform) would invalidate on the email rename per Supabase&rsquo;s refresh-token cache behavior. The 4 paused emma_waitlist rows from tonight&rsquo;s dry-run (Stein, Moench, Utter, Yelenick) remain attached to Karen&rsquo;s now-real tenant &mdash; harmless paused state, defer to single pre-launch sweep per <code>feedback_test_data_cleanup_deferred</code>. <strong>Touched</strong>: <code>supabase/migrations/20260601000000_v128_0_karen_real_tenant_rename.sql</code> (new file, the migration), <code>src/lib/changelog.ts</code> (this entry). No worker-code change &mdash; pure data ship.",
    ],
  },
  {
    version: "v1.27.2",
    date: "May 2026",
    items: [
      "<strong>v1.27.2 &mdash; Picker UI at <code>/app/refill/waitlist/invite</code> closes the v1.27.x Karen-go-live arc end-to-end.</strong> Surface where Karen actually does the work: pick A-list patients (default top 5 by lifetime spend, with Top 5 / 10 / 20 quick-selects), review + edit the invite copy with live token preview, hit Send. The button calls v1.27.1&rsquo;s <code>sendOptInInviteBatchFn</code> &mdash; the server pipeline drops one ready-to-send iMessage draft per patient into her inbox, Claude Desktop&rsquo;s MCP cron picks it up at the top of the hour and drafts each blue-bubble message in Messages.app for her to tap Send. Same delivery channel as the rescue dispatcher; different conversation (cold opt-in invite vs. live rescue offer). <strong>New file</strong>: <code>src/routes/app.refill.waitlist.invite.tsx</code>. Bootstrap loads in one parallel round: <code>listPatients</code> (full <code>PatientListRow</code> shape with phone/lifetimeSpend/banned/vip), <code>getMyAListRules</code> (saved A-list thresholds), <code>listReliabilityFlags</code> (cancellation/no-show counts for the exclude-unreliable rule), and the new <code>getInviteCopyTemplateFn</code> from v1.27.1 (per-tenant override or system default). Filters to phone-having patients that pass A-list rules, sorts by lifetime spend descending, auto-selects top 5 on first load. Editable textarea pre-fills from the saved template; live preview renders against the first selected patient with the four supported tokens substituted (<code>{first_name}</code>, <code>{owner_name}</code>, <code>{spa_name}</code>, <code>{opt_in_url}</code>). &lsquo;Save as default&rsquo; calls the existing <code>setSpaProfileLookupKeyFn</code> with <code>lookupKey=invite-copy-template</code> (v1.27.1 wired this through). Per-patient result table after send: Drafted / Already on waitlist / No phone / Banned/opted out / Error chips. <strong>A-list logic deduplication</strong>: <code>matchesRules</code> (12 lines, pure function) lifted out of <code>app.refill.patients.a-list-rules.tsx</code> into <code>src/lib/a-list-rules.ts</code> as <code>matchesAListRules</code>. Both routes now import from the same source &mdash; adding a field to <code>AListRules</code> in the future is a one-place change. <strong>Why no main-nav link</strong>: the four-chip Refill nav (Recovery / Inbox / Settings / Billing) is the &lsquo;narrow forever&rsquo; pillar from <code>project_refill_trojan_horse_thesis</code>. Waitlist surfaces stay deep-link-only for now, accessed via Karen-onboarding instructions or direct URL share. <strong>Why no link from waitlist/bulk to here</strong>: the two pages answer different questions &mdash; bulk = &lsquo;give me URLs to distribute through my own email blast / Mailchimp / patient-comm channel&rsquo;, invite = &lsquo;compose + send blue-bubble iMessages from my Apple ID&rsquo;. Cross-linking them would muddle the mental models. <strong>What this DOESN&rsquo;T ship</strong>: inbound STOP/START keyword routing from iMessage MCP replies (v1.27.3 &mdash; the SMS handler in <code>emma-optout.functions.ts</code> already detects keywords; v1.27.3 wires it to a Claude-Desktop reply-pull bridge). Also doesn&rsquo;t backfill <code>viewAsUserId</code> on the legacy <code>bulkSeedWaitlistIntents</code> or <code>listSeedablePatients</code> (the v374 bulk page works fine when Karen logs in directly; admin viewing-as-Karen on the legacy bulk page is a separate cleanup, low priority since this new invite page covers Karen&rsquo;s production path). <strong>Verification path</strong>: testspaowner magic-link &rarr; <code>/app/refill/waitlist/invite</code> &rarr; confirm A-list candidates load + default top-5 selected + edit template + Save-as-default round-trips through Supabase + Send fires &rarr; check davidfand303@gmail.com for the proxy email &rarr; tap any opt-in URL in the email &rarr; lands on v1.27.0&rsquo;s public route &rarr; tap &lsquo;Yes, count me in&rsquo; &rarr; confirm <code>emma_waitlist.status='active'</code> + <code>opted_in_at</code> stamped via Supabase dashboard. <strong>Touched</strong>: <code>src/routes/app.refill.waitlist.invite.tsx</code> (new file, the picker), <code>src/lib/a-list-rules.ts</code> (new file, the shared matcher), <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (import + rename <code>matchesRules</code> &rarr; <code>matchesAListRules</code>; delete local copy), <code>src/lib/changelog.ts</code> (this entry). No migration; no server-fn additions (all wiring built in v1.27.1).",
    ],
  },
  {
    version: "v1.27.1",
    date: "May 2026",
    items: [
      "<strong>v1.27.1 &mdash; Outbound iMessage-MCP invite pipeline: <code>sendOptInInviteBatchFn</code> server fn + per-tenant editable invite-copy template.</strong> Second leg of the v1.27.x Karen-go-live arc. v1.27.0 closed the patient-side surface (<code>/waitlist/optin/&lt;token&gt;</code>). This ship gives Karen (and the v1.27.2 picker UI) a server pipeline that takes a list of patient_node_ids + an invite body, atomically creates paused <code>emma_waitlist</code> rows + mints tokens + composes a Karen-facing proxy email with per-patient ready-to-send iMessage drafts, then sends it via Resend to her <code>rescue_proxy_email</code> inbox. Claude Desktop&rsquo;s hourly cron picks up the email, calls <code>draft_imessage(recipient_phone, body)</code> via the imessage-draft MCP, drafts blue-bubble iMessages in Messages.app from Karen&rsquo;s Apple ID. Same delivery channel as the rescue dispatcher, but for cold opt-in invites instead of rescue offers. <strong>New file</strong>: <code>src/server/emma-waitlist-invite.functions.ts</code>. Exports <code>DEFAULT_INVITE_TEMPLATE</code> (Karen-toned default with <code>{first_name}</code>, <code>{owner_name}</code>, <code>{spa_name}</code>, <code>{opt_in_url}</code> tokens + TCPA &lsquo;Reply STOP&rsquo; footer), <code>sendOptInInviteBatchFn</code> (the pipeline), <code>getInviteCopyTemplateFn</code> (resolver returning override-or-default for the picker UI). <strong>Pipeline shape</strong>: resolve effective user id (viewAsUserId-aware from day 1 so admin-viewing-as-Karen works), load spa-profile bundle + rescue_proxy_email + patient metadata in parallel, then per-patient: upsert paused waitlist row (skip if status=&lsquo;active&rsquo;, with per-row result codes <code>sent / skipped_already_on_waitlist / skipped_no_phone / skipped_banned_or_opted_out / error</code>) + mint stable token via existing <code>getOrMintWaitlistToken</code> + render body with token substitutions. Compose proxy email mirroring <code>composeProxyEmail</code> from <code>emma-rescue.functions.ts</code>:463-547 (AUTO-SEND directive at top, one section per patient, paste-into-Claude-Desktop framing). Send via Resend with <code>type=refill-waitlist-invite</code> tag for downstream telemetry. If Resend fails, every <code>sent</code> row downgrades to <code>error</code> with the failure message attached &mdash; no silent successes when the email never landed. <strong>Per-tenant invite copy</strong>: extends <code>emma-spa-profile.ts</code>&rsquo;s <code>SpaProfileLookupKey</code> union + <code>SpaProfileBundle</code> shape to include <code>invite-copy-template</code> as a fourth scalar (joins spa-name, owner-display-name, from-number in the same <code>knowledge_nodes</code> bucket). Wired through <code>spa-profile.functions.ts</code>&rsquo;s <code>setInput</code> enum so the existing <code>setSpaProfileLookupKeyFn</code> can write it &mdash; v1.27.2 picker UI gets a free &lsquo;Save as default&rsquo; surface with no new server fn. <strong>Why a new file instead of extending emma-waitlist-seed</strong>: <code>bulkSeedWaitlistIntents</code> (v374) does the upsert + token side already but exposes URLs in a result table for manual distribution. This pipeline ALSO composes + sends the iMessage proxy email; the two functions answer different questions (&lsquo;mint URLs&rsquo; vs. &lsquo;send invites&rsquo;). Folding would&rsquo;ve made bulkSeed&rsquo;s signature schizophrenic. Keeping them separate keeps each call site clear about what it&rsquo;s asking for. <strong>What this DOESN&rsquo;T ship</strong>: the picker UI itself (v1.27.2 &mdash; <code>/app/refill/waitlist/invite</code> with A-list filter + editable textarea + Save-as-default + send button), inbound STOP/START keyword routing from iMessage MCP replies (v1.27.3 &mdash; the <code>detectInboundKeyword</code> handler in <code>emma-optout.functions.ts</code> exists for SMS already; v1.27.3 wires it to a Claude-Desktop reply-pull bridge), <code>viewAsUserId</code> backfill on the legacy <code>bulkSeedWaitlistIntents</code> + <code>listSeedablePatients</code> (v1.27.2 prereq &mdash; rolling into that ship), and audit columns on <code>emma_waitlist</code> for what Karen actually sent (deferred unless v1.27.2 walk surfaces a need). <strong>Touched</strong>: <code>src/server/emma-waitlist-invite.functions.ts</code> (new file, the pipeline), <code>src/server/emma-spa-profile.ts</code> (extend <code>SpaProfileLookupKey</code> + <code>SpaProfileBundle</code> + <code>getSpaProfileBundle</code> for the fourth scalar), <code>src/server/spa-profile.functions.ts</code> (extend <code>setInput.lookupKey</code> enum), <code>src/lib/changelog.ts</code> (this entry). No migration &mdash; <code>emma_waitlist</code>, <code>emma_waitlist_tokens</code>, and the spa-profile partial unique index all exist.",
    ],
  },
  {
    version: "v1.27.0",
    date: "May 2026",
    items: [
      "<strong>v1.27.0 &mdash; Public <code>/waitlist/optin/&lt;token&gt;</code> landing route so the bulk-seed and v1.27.1 invite-composer URLs actually resolve to a page.</strong> Silent latent gap dating back to v374&rsquo;s bulk-seed ship: <code>bulkSeedWaitlistIntents</code> in <code>emma-waitlist-seed.functions.ts</code> has been generating <code>https://getrefill.app/waitlist/optin/&lt;uuid&gt;</code> URLs since 2026-05-18, and the matching public route never existed in the Refill codebase. The May-26 proof-or-nothing milestone verified the <em>rescue claim</em> round-trip (<code>/rescue/claim/&lt;token&gt;</code> is real and live) but the waitlist opt-in flow had never been distributed end-to-end &mdash; no patient had tapped a URL. Karen&rsquo;s production go-live (the v1.27.x keystone arc) needs the patient-side surface working before the v1.27.1 invite-composer can ship anything for her to distribute. <strong>What shipped</strong>: new <code>src/routes/waitlist.optin.$token.tsx</code> mirroring the chrome + OG-meta pattern from <code>rescue.claim.$token.tsx</code>. On mount the page calls the existing public-token-only <code>getWaitlistOptInPayload</code> server fn to fetch the spa name + patient first name + current waitlist status, then renders one of six states: <em>loading</em>, <em>invalid</em> (unknown / mistyped token), <em>already_opted_in</em>, <em>already_revoked</em>, <em>needs_confirm</em> (the happy path &mdash; explicit Confirm button + Decline button), <em>confirmed</em>, <em>declined</em>. Confirm calls <code>optInToWaitlist</code> &mdash; the existing fn flips the paused row to <code>status='active'</code>, stamps <code>opted_in_at</code>, preserves any admin-seeded <code>treatment_types</code>. Decline calls <code>revokeWaitlistOptIn</code> &mdash; row goes to <code>status='revoked'</code>, the engine never targets the patient again. <strong>Why explicit Confirm and not auto-confirm on tap</strong>: TCPA defensibility. An affirmative click on a clearly-labeled Confirm button is a stronger consent record than &lsquo;tapped the URL&rsquo;, and for the cold-invite-to-existing-relationship audience (Karen&rsquo;s real Rejuv patients) the half-second pause to read the spa name + value prop is the right human-first beat. <strong>OG meta</strong>: patient-facing Refill card (<code>refill-og-patient.png</code>) so iMessage / Messages / Mail link previews don&rsquo;t render the spa-owner-pricing homepage card. Same image asset rescue.claim already uses. <strong>What this DOESN&rsquo;T fix</strong>: the v1.27.1 outbound side (the iMessage-MCP composer + per-tenant editable invite copy + send-to-Karen proxy-email pipeline) and v1.27.2&rsquo;s picker UI. This ship is the patient-side surface only; without v1.27.1 the only way to generate a working URL today is the existing <code>/app/refill/waitlist/bulk</code> picker (which dumps URLs into a table for manual distribution, no composed invite copy). v1.27.3 will wire inbound STOP/START iMessage replies through the MCP reply-pull bridge &mdash; until then patients with active opt-ins can only revoke via this page&rsquo;s Decline button or by replying STOP to a future rescue SMS (which goes through the existing <code>emma-optout.functions.ts</code> handler). <strong>Touched</strong>: <code>src/routes/waitlist.optin.$token.tsx</code> (new file, the public route), <code>src/lib/changelog.ts</code> (this entry). No migration; no server-fn changes (calls only existing public token-only fns).",
    ],
  },
  {
    version: "v1.26.23",
    date: "May 2026",
    items: [
      "<strong>v1.26.23 &mdash; Billing unification Phase 1: restore a tenant-aware <code>getInvoicePreview</code> in <code>refill-billing.ts</code> and repoint <code>/app/refill/recovery</code> to it.</strong> The billing-unification scout (Plan-agent run before this ship) materially reframed the &lsquo;heavy migration&rsquo; backlog item: emma billing is already amputated on the write side (no Stripe calls, draft-only rows piling up), so there&rsquo;s no dual-charge risk. <strong>The real bug</strong>: the Recovery dashboard&rsquo;s MTD preview reads <code>emma_pricing_plans</code> via <code>emma-billing.functions.ts:getInvoicePreview</code>, but every modern plan pick (via <code>applyPricingPlan</code> in <code>refill-billing.ts</code>) writes to <code>refill_pricing_plans</code>. Net effect: any tenant on a Refill plan saw &lsquo;no active plan&rsquo; on <code>/app/refill/recovery</code> while <code>/app/billing</code> showed the real plan &mdash; a customer-visible split-brain that&rsquo;s lived since v1.26.14 removed the refill-side variant on a wrong read. Compounding: <code>PLAN_LABEL</code> in the Recovery card still mapped only the legacy emma enum (<code>performance/predictable/hybrid</code>), so even if the data had arrived from refill_pricing_plans the labels would&rsquo;ve rendered the raw enum string for <code>starter</code> and <code>pro</code>. <strong>What shipped</strong>: (1) New shared helper <code>aggregateRecoveryForTenant({sb, tenantId, periodStart, periodEnd})</code> in <code>refill-billing.ts</code> &mdash; pulls memberships fan-out + <code>emma_recovery_events</code> aggregation into one place. <code>generateMonthlyInvoiceForTenant</code> (the cron) refactored to call it instead of inlining the same query. Locks math parity per the scout&rsquo;s Risk #1 (preview drift from the cron once we tweak either path). (2) Restored <code>getInvoicePreview</code> + new <code>RefillInvoicePreview</code> type in <code>refill-billing.ts</code>. Plan lookup uses &lsquo;currently active&rsquo; (<code>plan_ended_at IS NULL</code>) &mdash; the dashboard shows what the spa&rsquo;s CURRENT plan would charge them; the cron uses &lsquo;active AS OF period_end&rsquo; for mid-month plan-switch correctness. Different semantics, deliberately not shared. (3) <code>app.refill.recovery.tsx</code> import flipped from <code>@/server/emma-billing.functions</code> &rarr; <code>@/server/refill-billing</code>; <code>InvoicePreview</code> renamed to <code>RefillInvoicePreview</code> throughout (3 sites). (4) <code>PLAN_LABEL</code> updated to current names: <code>starter/predictable/pro</code> with capitalized labels. (5) Emma-side <code>getInvoicePreview</code> marked DEPRECATED in its docblock; kept live so Phase 3&rsquo;s atomic delete of <code>emma-billing.functions.ts</code> (post-cron-decommission) lands as a single sweep, not piecemeal. Zero remaining callers after this ship. <strong>What this DOESN&rsquo;T fix</strong>: the remaining 4 phases of the unification &mdash; emma cron decommission (Phase 2; gated on rotating the leaked <code>SCHEDULER_SECRET</code> in <code>supabase/migrations/20260523010000_emma_invoice_cron.sql</code> per v391 session story), <code>emma-billing.functions.ts</code> file deletion (Phase 3), emma table archiving (Phase 4 &mdash; rename to <code>_archive_*</code> not drop, per [[feedback-test-data-cleanup-deferred]] default), and adding <code>tenant_id</code> directly to <code>emma_recovery_events</code> to kill the membership fan-out kludge (Phase 5, independently shippable). Those stay queued for the next sesh; Phase 1 alone closes the customer-visible split-brain. <strong>Plan-agent scout output also surfaced</strong>: three-way <code>PLAN_ECONOMICS</code> duplication (refill-billing + emma-billing + Deno stripe-webhook mirror) which Phase 3 collapses; and <code>emma_recovery_events</code> still being user-scoped (Phase 5). Not addressed here. <strong>Touched</strong>: <code>src/server/refill-billing.ts</code> (new <code>aggregateRecoveryForTenant</code> helper + refactored cron path + restored <code>getInvoicePreview</code> + new <code>RefillInvoicePreview</code> type), <code>src/server/emma-billing.functions.ts</code> (DEPRECATED marker on the old variant&rsquo;s docblock), <code>src/routes/app.refill.recovery.tsx</code> (import path flip + type rename + PLAN_LABEL update), <code>src/lib/changelog.ts</code> (this entry). No migration; no behavior change for the cron path (aggregation extracted with identical math).",
    ],
  },
  {
    version: "v1.26.22",
    date: "May 2026",
    items: [
      "<strong>v1.26.22 &mdash; Settings UI for the three knowledge_nodes spa-profile scalars (spa-name, owner-display-name, from-number).</strong> Pre-v1.26.22 these were SQL-paste-only &mdash; no in-app surface for a spa owner (or admin viewing-as) to update them. The v1.26.12 owner-display-name override and the v1.26.18 from-number gap both shipped without editor surfaces, on the bet that v1.26.19&rsquo;s folding-into-shared-helper would make this editor a one-import-target ship. It did. <strong>What shipped</strong>: new route at <code>/app/refill/settings/spa-profile</code>. Three always-visible inputs (spa-name, owner-display-name, from-number) with captions explaining what each scalar drives (spa-name &rarr; SMS/email body customer-facing name; owner-display-name &rarr; v1.26.12 solo-practitioner override for the &lsquo;with X&rsquo; clause; from-number &rarr; outbound SMS sender, blank = engine runs in proxy-email-only mode). Single &lsquo;Save (N)&rsquo; button fires only the keys that changed; per-field save buttons would&rsquo;ve been clearer-but-noisier and these scalars are co-located tenant config, not three independent toggles. Revert button restores server values when there are unsaved changes. <strong>Server fns</strong>: new <code>src/server/spa-profile.functions.ts</code> wraps <code>getSpaProfileBundle</code> + <code>setSpaProfileLookupKey</code> (both newly exported from <code>emma-spa-profile.ts</code>) as <code>createServerFn</code> surfaces with <code>resolveEffectiveUserId</code> for impersonation gating. Admin viewing-as Karen edits Karen&rsquo;s bucket. Non-admin callers passing <code>viewAsUserId</code> get a hard 403 at the resolver layer. <strong>Writer mechanic</strong>: empty/whitespace value DELETEs the row (cleanest representation of &lsquo;unset&rsquo; &mdash; both readers treat null + empty-trim as the same state, so storing an explicit empty row would create future ambiguity). Non-empty value UPSERTs against the partial unique index on <code>(user_id, context, lookup_key) WHERE context IN ('spa-profile', 'services') AND lookup_key IS NOT NULL</code> from migration <code>20260511010000_idempotent_spa_claim.sql</code>. PG matches the partial index for spa-profile inserts so <code>ON CONFLICT</code> resolves correctly. <strong>Why split spa-profile.functions.ts from emma-spa-profile.ts</strong>: the helper module stays pure (no createServerFn imports) so the three Emma surfaces that call it (rescue, preshow, blast) keep clean import shapes. Co-locating the wrappers in a sibling file means the wrappers don&rsquo;t leak into emma-spa-profile.ts&rsquo;s import surface. <strong>What this DOESN&rsquo;T fix</strong>: discoverability. The page is reachable by direct URL only (<code>/app/refill/settings/spa-profile</code>); the Settings chip still lands on <code>/scheduler</code> as the default, no cross-link from there. A proper Settings IA pass with a left-side sub-nav across all five settings pages is a v1.27 candidate &mdash; outside the scope of exposing the editable surface itself. Also doesn&rsquo;t resolve the dual-source spa-name question (this page writes <code>knowledge_nodes</code> spa-name, drives SMS/email composers; the admin viewing-as banner reads <code>public.tenants.name</code>, a separate column). Unifying the two storage points is a v1.27 candidate; for now this editor only writes the SMS-composer half. <strong>Touched</strong>: <code>src/server/emma-spa-profile.ts</code> (new <code>SpaProfileLookupKey</code> + <code>SpaProfileBundle</code> types, new <code>getSpaProfileBundle</code> + <code>setSpaProfileLookupKey</code> helpers), <code>src/server/spa-profile.functions.ts</code> (new file, server-fn wrappers), <code>src/routes/app.refill.settings.spa-profile.tsx</code> (new file, the editor page), <code>src/lib/changelog.ts</code> (this entry). No migration &mdash; uses the existing knowledge_nodes table + partial unique index.",
    ],
  },
  {
    version: "v1.26.21",
    date: "May 2026",
    items: [
      "<strong>v1.26.21 &mdash; Rename the dormant tenant&rsquo;s spa-name so it can&rsquo;t be mistaken for an active one in the v1.26.20 admin banner.</strong> v1.26.20 made the admin defaulted-vs-explicit landing visible: when admin opens getrefill.app with no PersonaSwitcher pick, the amber-bright banner reads &lsquo;Defaulted to <em>tenant.name</em>&rsquo;. For the dormant tenant (auth identity <code>dormantspaowner@test.com</code>, user_id <code>ecf8bcee&hellip;57fc</code>, holds 5,754 historical Acuity appointments + 1,140 QB-uploaded patients but the recovery engine has never fired here) the <code>public.tenants.name</code> column held the bare string &lsquo;Rejuv&rsquo; &mdash; visually indistinguishable from an active spa. The v1.26.16 changelog flagged this as a data hygiene fix; v1.26.20 made it customer-visible (where &lsquo;customer&rsquo; here = Grasshopper-as-admin, the only persona who hits the banner). <strong>What shipped</strong>: a single-row data migration at <code>supabase/migrations/20260621010000_v126_21_dormant_spa_name_hygiene.sql</code> that UPDATEs <code>public.tenants.name</code> for the dormant tenant&rsquo;s row to &lsquo;[Dormant] Rejuv Skin Spa&rsquo;. Tenant resolution joins through <code>tenant_memberships</code> (the tenants table has no <code>owner_user_id</code>; ownership lives in memberships with <code>role='owner'</code>). <strong>Why this name and not &lsquo;dormantspaowner-stub&rsquo;</strong>: the leading <code>[Dormant]</code> tag is unmistakable to an admin glancing at the banner, but preserving the historical &lsquo;Rejuv Skin Spa&rsquo; phrase keeps the connection to Karen Aslakson&rsquo;s original spa readable rather than discarded into a robot identifier. Memory tag for the dormant tenant&rsquo;s history stays load-bearing. <strong>What this DOESN&rsquo;T fix</strong>: the dormant tenant&rsquo;s historical data (5,754 appointments, 1,140 patients, all Acuity-ingested in 2026-Q1 before the v417 routing flip) still sits in the dormant user&rsquo;s rows. Sweeping or archiving that data is a separate pre-launch decision per [[feedback-test-data-cleanup-deferred]] &mdash; defer to a single sweep. Also doesn&rsquo;t address the underlying <code>getFirstTenantForAdmin</code> ordering question (why is the dormant tenant served first when admin defaults? creation-order? alphabetic?) &mdash; that&rsquo;s a separate v1.27 candidate flagged in v1.26.20&rsquo;s notes. <strong>Touched</strong>: <code>supabase/migrations/20260621010000_v126_21_dormant_spa_name_hygiene.sql</code> (new file, the data fix), <code>src/lib/changelog.ts</code> (this entry). Pure data fix; no code change to the running worker. Apply via Supabase dashboard SQL editor.",
    ],
  },
  {
    version: "v1.26.20",
    date: "May 2026",
    items: [
      "<strong>v1.26.20 &mdash; Vary the admin viewing-as banner so &lsquo;explicit pick&rsquo; and &lsquo;defaulted landing&rsquo; don&rsquo;t read identically.</strong> v1.26.16 synced the banner copy to reflect v1.26.7&rsquo;s impersonation reality (CSV ingest + plan writes target the viewed tenant) but couldn&rsquo;t distinguish two distinct states. <strong>State A &mdash; explicit pick</strong>: admin used <code>PersonaSwitcher</code> to choose Karen, <code>localStorage[refill-admin-view-as-user-id]</code> got Karen&rsquo;s user_id, the page reloaded, and the admin-fallback path resolved Karen&rsquo;s tenant. <strong>State B &mdash; defaulted landing</strong>: admin loaded the app with no <code>localStorage</code> pick, the admin-fallback path called <code>getFirstTenantForAdmin</code> with <code>viewAsUserId=undefined</code>, and the server returned whichever tenant happens to be first. Both states rendered the identical &lsquo;Viewing as Karen&rsquo;s Spa&rsquo; soft-yellow banner, so an admin walking up to the laptop couldn&rsquo;t tell whether they were on a deliberate impersonation or had landed there by accident and were about to write plan changes against the wrong tenant. <strong>What shipped</strong>: <code>TenantMembershipState.viewAsExplicit?: boolean</code> &mdash; captured at <em>resolve-time</em> in <code>useTenantMembership</code> by reading <code>getAdminViewAsUserId()</code> right before <code>getFirstTenantForAdmin</code>. <code>true</code> means <code>localStorage</code> held a user_id; <code>false</code> means no pick. Plumbed through <code>RefillShell</code> &rarr; <code>RefillShellChrome</code> as a new prop alongside the existing <code>viewAs</code>. The banner now branches: explicit picks keep the existing soft-yellow &lsquo;Viewing as <em>tenant</em>&rsquo; copy (calm, deliberate); defaulted landings render an amber-bright &lsquo;Defaulted to <em>tenant</em>&rsquo; banner with an explanatory clause (&lsquo;you haven&rsquo;t picked a persona, so the server returned the first tenant available&rsquo;) and the existing data-scope footer. <strong>Why resolve-time not paint-time</strong>: reading <code>getAdminViewAsUserId()</code> at every render would let a stray <code>localStorage</code> edit retroactively reclassify the banner without changing which tenant&rsquo;s data is actually loaded. Snapshot at resolve so the banner&rsquo;s claim matches the data on screen until the next full reload (which is when PersonaSwitcher writes happen anyway). <strong>Why amber-bright for defaulted, not red</strong>: it&rsquo;s not an error state &mdash; admin is allowed to land on a defaulted tenant, just shouldn&rsquo;t mistake it for a deliberate pick. Soft warning, not alarm. <strong>What this DOESN&rsquo;T fix</strong>: the &lsquo;defaulted&rsquo; banner shows the server-picked first tenant by name, but doesn&rsquo;t explain <em>why</em> that specific tenant is first (creation order? alphabetic?) &mdash; that&rsquo;s a <code>getFirstTenantForAdmin</code> ordering surfacing concern, outside this ship. The PersonaSwitcher itself still doesn&rsquo;t show an active-indicator for the currently impersonated persona either, which would be the natural complement &mdash; both ends of the impersonation surface gaining the same explicit/default visibility &mdash; deferred. <strong>Touched</strong>: <code>src/lib/use-tenant-membership.ts</code> (state shape + resolve-time capture), <code>src/components/shells/RefillShell.tsx</code> (prop pass-through), <code>src/components/refill/RefillShellChrome.tsx</code> (prop + banner branching + amber-bright variant), <code>src/lib/changelog.ts</code> (this entry). No migration. Pure client-side UX.",
    ],
  },
  {
    version: "v1.26.19",
    date: "May 2026",
    items: [
      "<strong>v1.26.19 &mdash; Fold the three near-duplicate spa-profile readers into a single shared module.</strong> v1.26.18 closed the <code>phone_numbers</code> schema-vs-code gap by pointing <code>resolveSpaFromNumber</code> at <code>knowledge_nodes</code>, but left the three near-duplicate implementations (one each in <code>emma-rescue.functions.ts</code>, <code>emma-preshow.functions.ts</code>, <code>emma-blast.functions.ts</code>) copy-pasted across the surface area. <code>resolveSpaName</code> was 2-way duplicated (rescue + preshow) on the same shape, and <code>resolveOwnerDisplayName</code> lived solo in rescue. The v1.26.18 entry called this folding out as the &lsquo;future DRY pass&rsquo; &mdash; this is that pass. <strong>What shipped</strong>: new <code>src/server/emma-spa-profile.ts</code> exports <code>resolveSpaName</code>, <code>resolveSpaFromNumber</code>, and <code>resolveOwnerDisplayName</code> as the canonical readers for the <code>knowledge_nodes</code> spa-profile bucket. The three Emma surfaces import from <code>@/server/emma-spa-profile</code> and drop their local copies. Signatures are unchanged at call sites; bodies are bit-for-bit ports. <strong>Why <code>src/server</code> not <code>src/lib</code></strong>: only server functions (cron sweeps, dispatcher fns, scheduler) call these readers; none are reachable from the client bundle. <code>src/lib/personas.ts</code> belongs in lib because <code>PersonaSwitcher.tsx</code> is client code; this helper has no client consumer and therefore lives next to the Emma server fns that use it. <strong>Type shape</strong>: the helper accepts <code>SupabaseClient&lt;Database&gt;</code> (the structural type each surface&rsquo;s local <code>admin()</code> returns), so existing call sites compile unchanged without dragging each file&rsquo;s local <code>SupabaseAdmin</code> alias into a shared definition. <strong>Why <code>resolveOwnerDisplayName</code> moves too (despite being single-source)</strong>: it reads the same <code>spa-profile</code> bucket with the same shape; keeping it inline in <code>emma-rescue.functions.ts</code> while extracting the other two would leave a footgun for the next surface (e.g. preshow gaining a personalized greeting) to re-duplicate. All four spa-profile lookup-key readers (spa-name, from-number, owner-display-name, plus future additions like hours / address / website) now have one canonical home. <strong>Net effect</strong>: next spa-profile <code>lookup_key</code> is one new exported fn in one file, picked up by all three surfaces. The Settings UI for editing these scalars (v1.27 candidate) gets a single import target. <strong>What this DOESN&rsquo;T fix</strong>: the three surfaces each still define their own local <code>admin()</code> helper that mints a service-role <code>SupabaseClient&lt;Database&gt;</code> from the same env vars &mdash; that&rsquo;s a separate copy-paste pattern outside the spa-profile-reader scope and out of scope for this ship. Same for the per-file <code>SupabaseAdmin</code> type aliases (each is <code>ReturnType&lt;typeof admin&gt;</code>; identical resolution, separate declarations). <strong>Touched</strong>: <code>src/server/emma-spa-profile.ts</code> (new file, canonical home), <code>src/server/emma-rescue.functions.ts</code> (import + delete 3 local fns), <code>src/server/emma-preshow.functions.ts</code> (import + delete 2 local fns), <code>src/server/emma-blast.functions.ts</code> (import + delete 1 local fn + docblock), <code>src/lib/changelog.ts</code> (this entry). No migration. No behavior change.",
    ],
  },
  {
    version: "v1.26.18",
    date: "May 2026",
    items: [
      "<strong>v1.26.18 &mdash; Close the <code>phone_numbers</code> schema-vs-code gap by reading the SMS from-number out of <code>knowledge_nodes</code> instead.</strong> Three near-duplicate <code>resolveSpaFromNumber</code> implementations (one each in <code>emma-rescue.functions.ts</code>, <code>emma-preshow.functions.ts</code>, <code>emma-blast.functions.ts</code>) all queried <code>phone_numbers</code> &mdash; a vestigial table from the Liz CRM that Refill cleaved away from and that never existed in this schema. At runtime the Supabase client returned an error on the missing table; <code>.maybeSingle()</code> swallowed it to null. Karen&rsquo;s engine still fired because she&rsquo;s in pure proxy-email mode (<code>rescue_proxy_email = davidfand303@gmail.com</code>, no <code>rescue_proxy_phone</code>), so the <code>willNeedFromNumber</code> gate skipped the requirement entirely. Any spa wanting direct SMS or proxy-SMS-to-owner would have hit &lsquo;Spa has no provisioned SMS number&rsquo; with no working configuration path. <strong>Three paths considered</strong>: (A) read from <code>knowledge_nodes</code> spa-profile pattern (matches the v1.26.12 <code>owner-display-name</code> shape, no migration), (B) create a real <code>phone_numbers</code> table (the original framing &mdash; mirrors a vestigial pattern for one value per tenant with no admin UI), (C) delete the function and lock Refill to proxy-email-only (too lossy). <strong>Shipping A.</strong> spa-profile is the established tenant-config bucket; storing from-number alongside spa-name + hours + address + owner-display-name keeps one mental model + one settings surface. <strong>Code change</strong>: each <code>resolveSpaFromNumber</code> body swaps the table from <code>phone_numbers</code> to <code>knowledge_nodes</code>, filters <code>context = 'spa-profile'</code> + <code>lookup_key = 'from-number'</code>, returns <code>title</code> trimmed (matches the read pattern <code>resolveSpaName</code> and <code>resolveOwnerDisplayName</code> already use in the same files). Signatures unchanged so call sites need no edits. <strong>How a spa wires up direct SMS going forward</strong>: one SQL row in <code>knowledge_nodes</code> with <code>(user_id, context='spa-profile', lookup_key='from-number', title='+12085551234')</code>, same shape as the v1.26.12 owner-display-name override. Settings UI for editing this is a v1.27 candidate alongside the owner-display-name editor. <strong>Why per-tenant scalar in knowledge_nodes, not a new table</strong>: a real <code>phone_numbers</code> table would buy multi-row support (one-spa-many-numbers, is_active flag, sort by created_at) the original Liz CRM had, but Refill&rsquo;s code already collapses to <code>.limit(1).maybeSingle()</code> &mdash; v1 is one number per tenant. New table for one scalar is overengineering relative to the existing spa-profile pattern. <strong>What this DOESN&rsquo;T fix</strong>: the three near-duplicate <code>resolveSpaFromNumber</code> implementations are still copy-pasted across emma-rescue + emma-preshow + emma-blast (same duplication exists for <code>resolveSpaName</code> in 2 files + <code>resolveOwnerDisplayName</code> in 1, all reading spa-profile). Folding into a shared <code>emma-spa-profile.ts</code> helper is a future DRY pass &mdash; out of scope for closing the phone_numbers gap. <strong>Touched</strong>: <code>src/server/emma-rescue.functions.ts</code> (helper body swap + architectural comment), <code>src/server/emma-preshow.functions.ts</code> (helper body swap), <code>src/server/emma-blast.functions.ts</code> (helper body swap + docblock refresh), <code>src/lib/changelog.ts</code> (this entry). No migration.",
    ],
  },
  {
    version: "v1.26.17",
    date: "May 2026",
    items: [
      "<strong>v1.26.17 &mdash; Collapse persona-mint roster and PersonaSwitcher allowlist onto a single source of truth.</strong> Pre-v1.26.17 there were two files defining who the demo personas are, with no enforcement that they stay aligned. <code>src/server/v417-persona-mint.ts</code> held <code>PERSONA_EMAILS</code> = { admin, kelly, maria, karen } — the 4-key map the public /personas page resolves at magic-link mint time. <code>src/lib/demo-personas.ts</code> held <code>DEMO_PERSONA_EMAILS</code> = Set&lt;{karen-email, kelly-email}&gt; — the email allowlist the admin PersonaSwitcher uses to filter <code>listImpersonableUsers</code>. The two overlap on karen + kelly (both consumers see the same person), but the karen + kelly emails were duplicated as string literals across both files. v1.26.13&rsquo;s Karen tenant rename (<code>karen@rejuv-demo.test</code> &rarr; <code>testspaowner@test.com</code>) had to manually edit both files to keep them aligned. Miss one and the public /personas mint and the admin dropdown drift &mdash; admin sees a tenant in the dropdown that can&rsquo;t be minted, or vice versa. The bug was dormant after v1.26.13 (both were edited correctly), but the architecture was a footgun waiting for the next rename. <strong>Compounding</strong>: <code>v417-persona-mint.ts</code>&rsquo;s docblock pointed at <code>src/server/v417-personas.ts</code> as the alignment source-of-truth, but that file doesn&rsquo;t exist (stale comment from a never-built future state). <strong>Fix</strong>: new <code>src/lib/personas.ts</code> &mdash; client-safe pure-const module &mdash; holds <code>PERSONA_EMAILS</code> + <code>PersonaKey</code> + <code>DEMO_PERSONA_KEYS</code> (subset of personas surfaced in the admin dropdown) + <code>DEMO_PERSONA_EMAILS</code> (computed from the previous two; what PersonaSwitcher actually consumes). <code>v417-persona-mint.ts</code> drops its local <code>PERSONA_EMAILS</code> declaration + the long alignment-history docblock and imports the constant from <code>@/lib/personas</code>; re-exports <code>PersonaKey</code> for back-compat with downstream callers that may have been importing it from the server file. <code>PersonaSwitcher.tsx</code> import line flips from <code>@/lib/demo-personas</code> &rarr; <code>@/lib/personas</code>; consumer code unchanged. <code>src/lib/demo-personas.ts</code> deleted entirely (zero remaining consumers after the flip). <strong>Why client-safe lib not server file</strong>: PersonaSwitcher is client code and can&rsquo;t import from a file that uses <code>createServerFn</code> + service-role admin client without dragging server deps into the browser bundle. Pure-const lib module is the canonical shape. <strong>Why DEMO_PERSONA_KEYS separate from DEMO_PERSONA_EMAILS</strong>: the keys are the deliberate product call (per Grasshopper&rsquo;s v1.24.4 three-personas directive: Admin excluded as &lsquo;caller&rsquo;, Maria excluded as &lsquo;not in the canonical walk&rsquo;); the emails are mechanically derived. Adding a 4th visible persona is editing the keys set, not duplicating email strings. <strong>Net effect</strong>: next persona rename touches exactly one place. The footgun is closed. <strong>Touched</strong>: <code>src/lib/personas.ts</code> (new file, the canonical source), <code>src/server/v417-persona-mint.ts</code> (PERSONA_EMAILS removed, import + type re-export added), <code>src/components/admin/PersonaSwitcher.tsx</code> (import flip), <code>src/lib/demo-personas.ts</code> (deleted), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.16",
    date: "May 2026",
    items: [
      "<strong>v1.26.16 &mdash; Update admin viewing-as banner copy to reflect v1.26.7 plan-writes impersonation.</strong> Surfaced during the v1.26.7 verification walk: the yellow banner on the spa-owner shell still read <code>Stripe and plan writes still scope to your own user_id</code> &mdash; written pre-v1.26.7 when that was true, now stale on the plan-writes half (Stripe still intentionally scopes to admin&rsquo;s own id per the v1.26.7 <code>getPaymentMethodStatus</code> carve-out). Walk verified end-to-end: admin impersonating Karen, picked Predictable, plan persisted on Karen&rsquo;s tenant; switched back to Admin, admin&rsquo;s bucket unchanged. The mechanism works; only the banner copy was lying. <strong>New copy</strong>: <code>Viewing as &lt;tenant.name&gt;. Use the persona switcher in the upper-right to change view. Read fetchers return this tenant&rsquo;s data (v1.20). CSV ingest (v1.20.5) and plan writes (v1.26.7) target this tenant. Stripe payment-method writes still scope to your own user_id.</code> The version-pill suffix style preserved on each clause so the banner doubles as an audit trail of which capabilities got impersonation-wired when. <strong>Backlog noted (NOT in this ship)</strong>: (1) when admin picks the &lsquo;Admin&rsquo; persona, <code>setAdminViewAsUserId(null)</code> clears localStorage but the spa-owner shell still falls back to <code>getFirstTenantForAdmin</code> &mdash; admin sees a default tenant&rsquo;s data, not their own. Walk happened to pass because the default fallback tenant (dormant Karen, spa-name &lsquo;Rejuv&rsquo;) has no billing plan either, matching admin&rsquo;s empty bucket by coincidence. Real fix would plumb &lsquo;explicit persona pick vs. defaulted&rsquo; into shell state and either hide the banner or show a different variant when defaulted. v1.27 candidate. (2) The dormant tenant&rsquo;s spa-name reads &lsquo;Rejuv&rsquo; instead of &lsquo;Rejuv Skin Spa&rsquo; or similar, creating a confusing visual when defaulting to it. Data fix, not code &mdash; sweep when the next tenant-hygiene pass happens. <strong>Touched</strong>: <code>src/components/refill/RefillShellChrome.tsx</code> (banner copy text only), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.15",
    date: "May 2026",
    items: [
      "<strong>v1.26.15 &mdash; Strip Acuity&rsquo;s baked-in provider name from rescue treatment labels.</strong> Surfaced during v1.26.12 verification: Karen&rsquo;s Acuity calendar uses treatment strings like <code>Tox w/ Karen</code> and <code>Filler with Karen</code>, where the provider name is welded into the treatment label itself. Every rescue surface (claim page, rescue SMS, proxy SMS, proxy email) <em>also</em> appends its own <code> with &lt;provider&gt;</code> clause via <code>providerClause</code> (composers) and <code>displayProviderName</code> (claim page). Result on Karen&rsquo;s tenant: SMS reads <code>... for Tox w/ Karen with Karen</code>; claim page renders headline <code>an opening just came up with Karen</code> + Treatment row <code>Tox w/ Karen</code> + With row <code>Karen</code> &mdash; three repeats of the same name on one card. Naming the wrong claim: v1.26.12 added the &lsquo;with Karen&rsquo; warmth signal assuming the treatment label was a clean noun phrase. It isn&rsquo;t for solo-practitioner Acuity setups &mdash; the provider name is already part of the label. <strong>Fix</strong>: new <code>stripProviderFromTreatment(treatment, providerName, ownerDisplayName)</code> helper sitting next to <code>providerClause</code>. Strips trailing <code>&lt;connector&gt; &lt;candidate&gt;</code> where connector is one of <code>w/</code>, <code>with</code>, <code>-</code>, <code>&ndash;</code>, <code>&mdash;</code>, <code>/</code>, <code>:</code> and candidate is the provider name or the spa-profile owner-display-name (case-insensitive). Anchored to <code>$</code> so it only catches trailing bake-ins &mdash; <code>Consultation</code> and <code>30 min facial</code> pass through untouched. Returns the original string if stripping would empty it. <strong>Applied at the four sites</strong> that hold treatment + provider + owner together: <code>composeRescueSms</code>, <code>composeProxySms</code>, <code>composeProxyEmail</code>, and <code>getRescueOfferPayload</code> (so the claim-page UI receives a pre-cleaned <code>treatmentType</code> &mdash; Treatment row and SMS body source the same cleaned value). <strong>Effect on Karen&rsquo;s tenant</strong>: SMS becomes <code>... for Tox with Karen</code> (single render). Claim page reads <code>an opening just came up with Karen</code> + Treatment <code>Tox</code> + With <code>Karen</code> &mdash; three slots, three pieces of information, no repeat. <strong>Multi-practitioner spas</strong> (Acuity calendar named after provider, not practice): labels without a trailing provider token pass through unchanged; <code>providerClause</code> render still works the same. <strong>Why strip-on-read, not re-prompt spas</strong>: re-asking the practice to clean their Acuity treatment library is unreasonable &mdash; those labels are what the team has been typing for years and they shouldn&rsquo;t have to re-do data entry to look right in our UI. Strip on render, leave the source-of-truth alone. <strong>Touched</strong>: <code>src/server/emma-rescue.functions.ts</code> (helper + 4 apply sites + 1 <code>resolvedOwnerDisplayName</code> local for reuse in the payload return), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.14",
    date: "May 2026",
    items: [
      "<strong>v1.26.14 &mdash; Kill four dead billing fns flagged in v1.26.7/.8.</strong> The v1.7.2 emma&harr;refill UI unification routed <code>app.billing.tsx</code> to <code>refill-billing.ts</code> for plan + invoice fns, leaving the emma-billing equivalents (<code>applyPricingPlan</code>, <code>getActivePlan</code>, <code>listInvoices</code>) with zero call sites. Mirror situation on the recovery dashboard: <code>app.refill.recovery.tsx</code> imports <code>getInvoicePreview</code> from <code>emma-billing.functions.ts</code>, so the <code>refill-billing.ts</code> variant was the dead one. v1.26.7/.8 audits identified all four and explicitly punted to a separate cleanup ship (the note read &lsquo;dead and intentionally NOT fixed in this sweep &mdash; separate cleanup ship should decide delete-vs-migrate&rsquo;). This is that ship. <strong>Removed from <code>src/server/emma-billing.functions.ts</code></strong>: <code>applyPricingPlan</code> server fn + <code>applyPlanInput</code> zod schema. <code>getActivePlan</code> server fn. <code>listInvoices</code> server fn + <code>accessTokenOnly</code> zod schema (was shared by the two dead fns). <code>ActivePlan</code> + <code>Invoice</code> exported types (orphaned after fn removal; refill-billing&rsquo;s <code>RefillActivePlan</code>/<code>RefillInvoice</code> are the live equivalents). <code>verifyAuth</code> import dropped from the import line (no remaining callers in this file). <code>PricingPlan</code> type kept &mdash; still consumed by <code>getInvoicePreview</code>, <code>getPlanEconomics</code>, <code>PLAN_ECONOMICS</code>. <strong>Removed from <code>src/server/refill-billing.ts</code></strong>: <code>getInvoicePreview</code> server fn + <code>RefillInvoicePreview</code> exported type (dead since v1.7.2). <code>accessTokenOnly</code> + <code>verifyAuth</code> imports retained &mdash; both still have other live consumers in the file. <strong>Top-of-file docblocks updated</strong> in both files to reflect the removals (was listing the dead fns under &lsquo;Surfaces&rsquo; &mdash; misleading). <strong>Verification</strong>: <code>grep -rn 'applyPricingPlan\\|getActivePlan\\|listInvoices' src/</code> across non-changelog files returned only references in <code>app.billing.tsx</code> (which imports from refill-billing &mdash; the live source) and the two billing files (now only refill-billing exports them). <code>getInvoicePreview</code> grep returned only the emma-billing definition + the recovery route&rsquo;s import &mdash; both live. Worker bundle dropped from 795.35 kB &rarr; 794.55 kB confirming dead code actually removed, not just hidden by re-export chains. <strong>What this DOESN&rsquo;T fix</strong>: the broader emma&harr;refill unification is still on the backlog &mdash; <code>emma-billing.functions.ts</code> still owns <code>generateMonthlyInvoiceForUser</code>/<code>generateMonthlyInvoicesForAll</code> writing to the legacy <code>emma_invoices</code> table, and the <code>/api/cron/emma-invoice</code> route still calls them. That&rsquo;s a heavier migration (data table cutover, cron-route swap, idempotency-key bridge) and stays on backlog until a real reason forces it. The naming-collision discovery from v1.26.7 (both files exported identically-named fns) is now resolved structurally &mdash; only one file exports each name. <strong>Touched</strong>: <code>src/server/emma-billing.functions.ts</code> (docblock, 1 import edit, 2 type removals, 2 schema removals, 3 fn removals), <code>src/server/refill-billing.ts</code> (docblock, 1 type removal, 1 fn removal), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.13",
    date: "May 2026",
    items: [
      "<strong>v1.26.13 &mdash; Rename two Karen tenants to test-naming convention.</strong> Surfaced during the v1.26.12 verification: I (Po) repeatedly mis-identified which tenant is LIVE vs dormant because both auth emails embed the human name &lsquo;Karen&rsquo; and the `.test` suffix on `karen@rejuv-demo.test` LOOKS like dev-data when it&rsquo;s actually production-active. The 2026-05-29 diagnostic settled it: <code>karen@rejuv-demo.test</code> holds 11 rescue_attempts + 10 offers + 9 future cancellations + a configured noshow_policy + an active email proxy to <code>davidfand303@gmail.com</code> (the live-fire delivery path); <code>karen.aslak@gmail.com</code> has 5754 historical Acuity appointments + 1140 QB-uploaded patients but zero rescue activity ever. <strong>Convention shift</strong>: separate auth identity (logged-in email, robot-generic, screams &lsquo;test&rsquo;) from display name (UI + customer copy, warm human name). Auth gets <code>test{role}@test.com</code>; display name stays &lsquo;Karen&rsquo; via the v1.26.12 spa-profile <code>owner-display-name</code>. Customers texting still see &lsquo;with Karen&rsquo; — the two layers cleanly separate. <strong>Renamed</strong>: <code>karen@rejuv-demo.test</code> &rarr; <code>testspaowner@test.com</code> (LIVE working tenant — the one v417 persona-mint routes to). <code>karen.aslak@gmail.com</code> &rarr; <code>dormantspaowner@test.com</code> (holds QB roster + historical Acuity data but engine never fires there). user_ids preserved across the rename so every foreign-key reference (knowledge_nodes, emma_appointments, emma_rescue_offers, emma_reliability_status, user_roles, admin_audit_log, etc.) stays intact. <strong>Code changes</strong>: <code>src/server/v417-persona-mint.ts</code> — <code>PERSONA_EMAILS.karen</code> updated to point at <code>testspaowner@test.com</code>; public /personas page magic-link mint now routes through the renamed email. <code>src/lib/demo-personas.ts</code> — <code>DEMO_PERSONA_EMAILS</code> allowlist updated; admin PersonaSwitcher dropdown now shows the LIVE tenant instead of the dormant gmail. The v1.24.5-era choice was correct for &lsquo;where do the patients live&rsquo; but wrong for &lsquo;where does the engine fire&rsquo; — v417 superseded that for the public mint flow; this allowlist now follows for the admin flow too. <strong>SQL rename</strong>: two-statement <code>UPDATE auth.users.email</code>, pasted into Supabase dashboard by Grasshopper after this deploy lands. <strong>Memory rewrites queued</strong>: <code>project_rejuv_demo_test_tenant.md</code> (was &lsquo;active dev/test residue, defer cleanup&rsquo; — wrong on every axis; rewriting as &lsquo;LIVE working tenant, now testspaowner@test.com&rsquo;), <code>feedback_supabase_project_url.md</code> (already references both emails — needs flip), new <code>feedback_test_naming_convention.md</code> (the rule going forward). <strong>2026-06-12 scheduled routine</strong>: prompt references both old emails by name; updating via RemoteTrigger so the agent looks for the renamed tenants. <strong>Side effect</strong>: any active browser session under the old emails will need to re-login via /personas — the auth.users.email rename invalidates the refresh-token cache. Touched: <code>src/server/v417-persona-mint.ts</code>, <code>src/lib/demo-personas.ts</code>, <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.12",
    date: "May 2026",
    items: [
      "<strong>v1.26.12 &mdash; Solo-practitioner override on the &lsquo;with X&rsquo; rescue copy.</strong> Surfaced during the v1.26.0 + v1.26.3 provider-affinity verification: Karen runs Rejuv Skin Spa with three providers (Michelle, Lisa, Karen herself), but in Acuity Karen&rsquo;s calendar is named &lsquo;Rejuv Skin Spa&rsquo; rather than her first name. The v379.2 <code>providerClause</code> suppression treated <code>provider_name == spa_name</code> as &lsquo;calendar named after the practice, suppress&rsquo; &mdash; which is correct for multi-practitioner spas where the calendar aliases the practice, but for Karen&rsquo;s setup it stripped the warmth signal: customers saw &lsquo;Tox at 6:50pm&rsquo; instead of &lsquo;Tox with Karen at 6:50pm.&rsquo; Naming the wrong claim: the original rule baked in &lsquo;calendar-as-practice&rsquo; as the only interpretation when in fact the same condition can mean &lsquo;solo practitioner branding under the practice name.&rsquo; <strong>Fix</strong>: new spa-profile lookup_key <code>owner-display-name</code> (existing knowledge_nodes pattern, no migration). <code>providerClause</code> now takes <code>ownerDisplayName: string | null</code> as a 3rd arg: when <code>provider_name == spa_name</code> AND owner-display-name is set, renders <code>${prefix}${owner}</code>; when null, suppression holds as before. <code>resolveOwnerDisplayName</code> helper added next to <code>resolveSpaName</code>; rescue-send call site fetches once and threads through all 3 composers (<code>composeRescueSms</code>, <code>composeProxySms</code>, <code>composeProxyEmail</code>). <code>getRescueOfferPayload</code> picks up the value via a parallel knowledge_nodes query (same Promise.all gather) and adds <code>ownerDisplayName</code> to <code>RescueOfferPayload</code>. Claim page (<code>rescue.claim.$token.tsx</code>) replaces the binary <code>showProvider</code> with a derived <code>displayProviderName</code>: different-from-spa &rarr; provider as-is; same-as-spa + owner set &rarr; owner; same-as-spa + owner null &rarr; null/suppress. Both &lsquo;claimable&rsquo; and &lsquo;already_claimed_by_you&rsquo; render branches updated; brand-green span (<code>#056048</code>) and Row label both source <code>displayProviderName</code>. <strong>Effect across providers at Rejuv</strong>: Lisa &amp; Michelle&rsquo;s calendars don&rsquo;t match the spa name &rarr; existing path renders &lsquo;with Lisa&rsquo; / &lsquo;with Michelle&rsquo; unchanged. Karen&rsquo;s &lsquo;Rejuv Skin Spa&rsquo; calendar &rarr; new path renders &lsquo;with Karen&rsquo; once the SQL paste in chat sets <code>owner-display-name = &lsquo;Karen&rsquo;</code> in her tenant. Other multi-practitioner spas that don&rsquo;t set the override continue suppressing per v379.2. <strong>Why a per-tenant scalar, not a heuristic</strong>: any inference based on appointment counts or calendar names is fragile (single-provider spas might rename a calendar mid-onboarding, multi-provider spas might have Karen-style calendar conventions, etc.). Explicit tenant config inverts the question from &lsquo;guess the right behavior&rsquo; to &lsquo;the spa told us their convention.&rsquo; The data model already supports this surface (knowledge_nodes <code>spa-profile</code> context, alongside <code>spa-name</code>, <code>hours</code>, <code>address</code>, etc.). No settings UI yet &mdash; for now the value lands via SQL; admin/owner settings surface for editing this is a v1.27 candidate. <strong>Touched</strong>: <code>src/server/emma-rescue.functions.ts</code> (providerClause signature + 3 composer signatures &amp; bodies + RescueOfferPayload type + resolveOwnerDisplayName helper + getRescueOfferPayload parallel-query expansion + 4 call-site thread-throughs), <code>src/routes/rescue.claim.$token.tsx</code> (displayProviderName derivation + 3 JSX swap sites), <code>src/lib/changelog.ts</code> (this entry). No migration. SQL paste in chat sets Karen&rsquo;s override.",
    ],
  },
  {
    version: "v1.26.11",
    date: "May 2026",
    items: [
      "<strong>v1.26.11 &mdash; Baseline-alert suppression on first-sweep.</strong> Karen&rsquo;s Patterns tab opened to <em>200 unread &lsquo;promoted to VIP / Regular&rsquo; alerts</em> all stamped May 28 6:25 PM &mdash; a flood, not signal. Root cause: the bulk-recompute path can&rsquo;t tell &lsquo;brand new patient, first-time tier assignment&rsquo; apart from &lsquo;known patient crossed a threshold.&rsquo; The v1.25.7 RPC at step 1 of <code>recomputeReliabilityForUser</code> does <code>INSERT &hellip; ON CONFLICT</code> to upsert reliability rows, with the column default <code>tier='trusted'</code>. Step 3 reads them back as <code>trusted</code>. Step 4&rsquo;s in-memory diff sees <code>trusted &rarr; vip</code> (or whatever computeTier returns) and queues an &lsquo;update.&rsquo; Step 6 hands that to <code>composeAlert</code>, which <em>does</em> short-circuit when <code>fromTier === null</code> (line ~162, &lsquo;Initial assignment (no prior tier) is not an alert&rsquo;) &mdash; but the bulk path can&rsquo;t surface a null fromTier because the column default already filled it in. composeAlert sees a real-looking promotion and emits the alert. Every first-time-seen patient on a tenant&rsquo;s first-ever sweep got a fake promotion alert. Karen&rsquo;s first sweep: 200 of them in one batch. <strong>Fix</strong>: new Step 0 in <code>recomputeReliabilityForUser</code> snapshots <code>patient_node_id</code>s that already have a reliability row BEFORE the RPC runs. Step 6&rsquo;s alert-emit loop now filters <code>updates</code> through that pre-snapshot &mdash; patients not in the set are first-time tier assignments and don&rsquo;t fire alerts. Real transitions (a known patient crossing a threshold from one sweep to the next) stay in. One-shot SQL to dismiss Karen&rsquo;s existing 200-alert flood pasted to Supabase as part of this ship (system marker on <code>dismissed_by</code>). <strong>Why this is the right shape, not a UX layer band-aid</strong>: a tenant&rsquo;s first sweep is structurally different from every subsequent sweep &mdash; the first sweep populates the baseline; every sweep after compares against the prior baseline and surfaces movement. Putting the suppression at the engine layer means Karen&rsquo;s Patterns tab going forward shows only genuine transitions (someone hit VIP <em>tonight</em>, someone dropped to in_recovery <em>this week</em>) &mdash; signal, not noise. The <code>transitions</code> count on the v1.26.10 freshness pill also now reads &lsquo;real transitions&rsquo; only (was: real transitions + baseline noise). <strong>Anderson canary check</strong> (raised in the v1.26.10 verification but never surfaced cleanly): paste the Anderson lookup SQL on the next chat message into Supabase to read his tier + counts directly. Don&rsquo;t scroll 200 alerts to find him &mdash; that was a bad ask. <strong>Touched</strong>: <code>src/server/emma-reliability.functions.ts</code> (Step 0 pre-RPC snapshot + step 6 filter), <code>src/lib/changelog.ts</code> (this entry). No migration. One-shot SQL paste handles existing Karen-tenant baseline alerts.",
    ],
  },
  {
    version: "v1.26.10",
    date: "May 2026",
    items: [
      "<strong>v1.26.10 &mdash; Persistent last-sweep result on the A-list freshness card.</strong> v1.26.9 shipped the &lsquo;Recompute now&rsquo; button + a toast that briefly read <code>X patients &middot; N tier transitions</code>, but the surface itself only showed <code>Last computed: &lt;time&gt;</code> &mdash; once the toast faded or the page reloaded, the user had no record of <em>what the sweep actually did</em>. Worse, the v1.26.9 freshness query sourced <code>latestRecomputedAt</code> from <code>max(recomputed_at)</code> across <code>emma_reliability_status</code>, but step 5 of <code>recomputeReliabilityForUser</code> only writes <code>recomputed_at</code> for rows whose <em>tier</em> changed &mdash; so a sweep with zero transitions didn&rsquo;t move the timestamp at all, and the card stayed stuck on the last sweep that flipped someone. Confusing for &lsquo;did the cron run last night?&rsquo; <strong>New table</strong>: <code>emma_reliability_runs</code> (migration <code>20260621000000</code>) &mdash; one row per <code>recomputeReliabilityForUser</code> call. Columns: <code>id, user_id, completed_at, patients_recomputed, transitions, trigger ('cron'|'manual'), created_at</code>. RLS: self-read via <code>auth.uid()=user_id</code>, service-role bypass. Composite index on <code>(user_id, completed_at desc)</code> covers the &lsquo;most-recent-run&rsquo; query. <strong>Server changes</strong>: <code>recomputeReliabilityForUser</code> now takes a <code>trigger</code> arg (default <code>'manual'</code>), anchors a single <code>completedAt</code> ISO string used for both <code>recomputed_at</code> stamps on changed rows AND the new runs-log row, and inserts the runs-log row at the end of <em>both</em> code paths (the early-return <code>updates.length === 0</code> branch + the full sweep branch). Returns <code>completedAt</code> alongside <code>patientsRecomputed</code> + <code>transitions</code>. The runs-log insert is best-effort (<code>console.error</code> on failure) matching the existing pattern_alerts batch-insert posture &mdash; a failure here is observability data loss only, the sweep itself succeeded. <code>getReliabilityFreshness</code> now reads from the runs log: three parallel queries (count of reliability rows, most-recent <code>emma_reliability_runs</code> row, fallback <code>max(recomputed_at)</code> on <code>emma_reliability_status</code>). The runs-log timestamp wins; the <code>recomputed_at</code> fallback covers the gap between migration deploy and the first sweep that writes a runs row. Return type expands to include <code>lastRun: { completedAt, patientsRecomputed, transitions, trigger } | null</code>. <code>recomputeReliabilityNow</code> passes <code>trigger: 'manual'</code>; the cron route at <code>/api/cron/emma-reliability-sweep</code> passes <code>trigger: 'cron'</code>. <strong>UI change on the A-list rules page</strong>: the &lsquo;Reliability data&rsquo; section&rsquo;s single <code>Last computed: X &middot; N tracked</code> line becomes a stacked surface &mdash; <code>Last computed: &lt;time&gt; &middot; N tracked</code> followed by three pills: <code>X patients swept</code>, <code>Y tier transitions</code> (green when &gt; 0, neutral when 0 with explicit &lsquo;no tier changes&rsquo; label so success doesn&rsquo;t look broken), and a small <code>Manual</code> / <code>Nightly cron</code> chip telling the user which run they&rsquo;re seeing. Implemented as a new <code>FreshnessLastRun</code> subcomponent inside the route file. <code>handleRecompute</code> also stashes the freshly-returned run into <code>freshness.lastRun</code> so the surface updates immediately on click without waiting for the <code>loadAll()</code> reconcile. <strong>What Grasshopper should see on first deploy</strong>: the section loads showing the existing v1.26.9 <code>Last computed</code> line (from the <code>max(recomputed_at)</code> fallback) but no pills, because no runs row exists yet. First click of <code>Recompute now</code> writes the first runs row + populates the pills. The next morning&rsquo;s 02:00 UTC cron writes another row with <code>trigger='cron'</code> &mdash; if Grasshopper visits the page next day, the chip will read <code>Nightly cron</code>, confirming the cron is alive (which v1.26.9 was the first ship to make believable after the bulk refactor). <strong>Audit history side effect</strong>: with every sweep persisted, debugging &lsquo;why did Anderson&rsquo;s tier flip&rsquo; becomes a runs-log scan instead of an inference from <code>max(recomputed_at)</code>. Not exposed in UI today (no /admin/runs surface); the data is there for next-incident debugging. <strong>Touched</strong>: <code>supabase/migrations/20260621000000_v126_10_reliability_runs.sql</code> (new table + RLS + index + pgrst notify + verify SELECT), <code>src/integrations/supabase/types.ts</code> (table block), <code>src/server/emma-reliability.functions.ts</code> (recomputeReliabilityForUser trigger arg + runs-log insert helper + getReliabilityFreshness 3-query path + ReliabilityFreshness type expansion + recomputeReliabilityNow trigger='manual'), <code>src/routes/api.cron.emma-reliability-sweep.ts</code> (trigger='cron' on the per-spa call), <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (handleRecompute writes lastRun into freshness + new FreshnessLastRun subcomponent replaces the inline line), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.9",
    date: "May 2026",
    items: [
      "<strong>v1.26.9 &mdash; Tier-recompute observability + bulk-mode refactor of the cron sweep path.</strong> Two interlocking fixes triggered by the &lsquo;Anderson David has 11 cancels but tier=trusted&rsquo; observation from the v1.26.4 verification screenshot. <strong>Root cause</strong>: the v1.25.7 RPC fixed the COUNTS-side of the bulk-ingest staleness gap (no_shows_6mo, cancellations_6mo, total_visits, plus v1.26.4&rsquo;s lifetime variants) but explicitly left tier computation in TS &mdash; <em>which means the same per-patient round-trip loop that v1.25.7 was created to eliminate still ran on the tier path, just on a different cadence</em>. The daily cron at 02:00 UTC POSTs to <code>/api/cron/emma-reliability-sweep</code> which iterates every spa and calls <code>recomputeReliabilityForUser</code>. Pre-v1.26.9 that fn looped <code>recomputeReliabilityForPatient</code> per patient: ~4-6 DB round trips each. At Karen-scale (~700 patients with appointment history) that&rsquo;s 2800-4200 RTs &mdash; well past Cloudflare Workers&rsquo; 30s CPU timeout. Cron was almost certainly timing out silently after Karen&rsquo;s 5,754-row Acuity ingest, leaving tiers indefinitely stale. Anderson&rsquo;s &lsquo;trusted&rsquo; tier with 11 cancels was the canary. <strong>Bulk refactor of recomputeReliabilityForUser</strong>: step 1 calls the v1.25.7 RPC for fresh counts (single SQL), step 2 reads policy thresholds (1 RT), step 3 reads ALL reliability rows back (1 RT), step 4 computes new tier per row IN MEMORY via the existing TS <code>computeTier</code> (no DB round trip), step 5 bulk-writes tier changes via chunked Promise.all&rsquo;d UPDATEs (chunk=50, ~1 RT per 50 changes), step 6 batch-inserts pattern_alerts for meaningful transitions (1 RT for patient names + 1 RT for the alerts insert). Total: ~5-15 RTs regardless of patient count. The per-patient TS path (<code>recomputeReliabilityForPatient</code>) is unchanged &mdash; single-patient calls from <code>updateAppointmentStatus</code> have no scale issue. Both paths coexist. The cron route now gets the bulk benefit transparently because it calls the refactored fn. <strong>New observability surface on the A-list rules page</strong>: a &lsquo;Reliability data&rsquo; section at the bottom of the page shows <code>Last computed: X &middot; N patients tracked</code> sourced from <code>max(recomputed_at)</code> across the tenant&rsquo;s reliability rows. Adjacent &lsquo;Recompute now&rsquo; button forces an immediate sweep via the new <code>recomputeReliabilityNow</code> server fn. Toast surfaces <code>X patients recomputed &middot; N tier transitions</code>. Reloads the page data so flagged-count picks up newly in_recovery patients. <strong>New server fns</strong>: <code>getReliabilityFreshness({accessToken, viewAsUserId?}) =&gt; { latestRecomputedAt, patientsTracked }</code> &mdash; parallel max/count queries, single round trip aggregate. <code>recomputeReliabilityNow({accessToken, viewAsUserId?}) =&gt; { patientsRecomputed, transitions, completedAt }</code> &mdash; resolveEffectiveUserId &rarr; recomputeReliabilityForUser (which is now bulk). Both impersonation-aware per the v1.20+ pattern. <strong>What Grasshopper should expect on click</strong>: with 5,754 appointments and 695 matched patients, Anderson David&rsquo;s row should transition from trusted to in_recovery (if noShows6mo &ge; 3) or to regular/vip (if total_visits crosses thresholds). The recap mentioned 11 lifetime cancels &mdash; cancels don&rsquo;t drive tier (no-shows do), so the transition depends on the no_shows_6mo count specifically. Either way: pattern_alerts will fire for any transitions; visible on /app/refill/rescue Patterns tab. The Anderson alert will land too &mdash; that&rsquo;s the system working as designed on real data; not the case for a suppression engine (per the v1.26.7 self/family suppression kill, Anderson is just a real patient record). <strong>Audit-honest note</strong>: <code>composeAlert</code> uses <code>noShows6mo + totalVisits</code> from the in-memory snapshot taken at sweep time, so the alert headline (&lsquo;Pattern: X has N no-shows in the last 6 months&rsquo;) reflects the same numbers that drove the transition decision. No drift between transition condition + alert text. <strong>Pattern lesson banked</strong>: when a denormalization or RPC ship leaves <em>part</em> of the materialized state behind on the TS side (e.g. counts in RPC, tier still in TS), the slow path can stay broken indefinitely under workers timeouts without surfacing &mdash; the cron just silently fails. Future RPC ships should either complete the picture (all dependent columns) or expose the partial-success in observability. Touched: <code>src/server/emma-reliability.functions.ts</code> (recomputeReliabilityForUser bulk refactor + getReliabilityFreshness + recomputeReliabilityNow server fns + types), <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (freshness state + handleRecompute + Reliability data section UI), <code>src/lib/changelog.ts</code> (this entry). No migration &mdash; the v1.25.7 RPC is reused as-is.",
    ],
  },
  {
    version: "v1.26.8",
    date: "May 2026",
    items: [
      "<strong>v1.26.8 &mdash; Impersonation sweep: emma-attribution + emma-deposits, plus recovery dashboard full impersonation correctness.</strong> The recovery dashboard (<code>/app/refill/recovery</code>) was the page that v1.26.7 explicitly left half-done &mdash; <code>getInvoicePreview</code> got swept on the server side, but the route was untouched to avoid the split-brain UI where one card shows tenant data while three others show admin&rsquo;s empty bucket. This ship closes the remaining files and flips all four reads + three writes in one consistent change. <strong>emma-attribution.functions.ts &mdash; 4 gaps closed</strong>: <code>listRecoveryEvents</code> (line 239 read; admin saw their own empty <code>emma_recovery_events</code> bucket instead of Karen&rsquo;s verified events), <code>getRecoveryStats</code> (line 405 read; MoM math + month/lifetime totals + provisional count all came back zero under impersonation), <code>manualConfirmRecovery</code> (line 336 write; admin clicking Confirm on Karen&rsquo;s page would silently no-op because the <code>.eq(&apos;user_id&apos;, userId)</code> filter targeted admin&rsquo;s empty bucket; <code>verified_by</code> would also have logged admin&rsquo;s id if the row existed). <code>unconfirmRecovery</code> (line 383 write; same silent no-op pattern for undo). <strong>emma-deposits.functions.ts &mdash; 2 gaps closed</strong>: <code>listDepositIntents</code> (line 212 read; deposits tab empty under impersonation), <code>markDepositVoided</code> (line 267 write; void silently no-op&rsquo;d while Karen&rsquo;s deposit stayed visible). <strong>Internal helpers explicitly excluded</strong>: <code>recordRecoveryEvent</code>, <code>reconcileRecoveryEventsForUser</code> (attribution), <code>checkDepositEligibility</code>, <code>logDepositIntent</code> (deposits) &mdash; all take <code>userId</code> as a direct arg from caller; not <code>createServerFn</code>, no HTTP surface, caller-validated. <strong>All 6 schemas extended</strong>: <code>listInput</code> (attribution), <code>manualInput</code>, <code>unconfirmInput</code>, <code>statsInput</code> (attribution), <code>listInput</code> (deposits), <code>voidInput</code> (deposits) &mdash; each adds <code>viewAsUserId: z.string().uuid().optional()</code>. <code>verifyAuth</code> import removed from both files (no remaining callers). <strong>Recovery route plumbing</strong>: <code>app.refill.recovery.tsx</code> now imports <code>useTenantMembership</code> + derives <code>viewAsUserId</code> + threads it through <strong>seven</strong> call sites (the 4 attribution reads/writes, 2 deposit reads/writes, plus the previously-swept <code>getInvoicePreview</code>). <code>load</code> useCallback&rsquo;s dep array updated to include <code>viewAsUserId</code> so a mid-session admin switching from one tenant to another triggers a refresh instead of showing stale prior-tenant data. <strong>Audit fields</strong>: <code>verified_by</code> (manualConfirmRecovery) and <code>voided_by</code> (markDepositVoided) now record the <em>effective</em> user_id (impersonated tenant), matching the established v1.25.6 / v1.26.6 / v1.26.7 pattern. Attribution-back-to-admin (&lsquo;who really clicked&rsquo;) would need a separate <code>actor_user_id</code> column &mdash; not in scope. <strong>What this means for Karen</strong>: an admin can now walk Karen&rsquo;s entire recovery dashboard end-to-end via the persona switcher and see her real numbers (MoM math, treatment breakdown, deposit intents, invoice preview) instead of an empty admin bucket. Confirm / undo / void all land in Karen&rsquo;s data, not admin&rsquo;s. The [VERIFIED] math discipline that this page exists to demonstrate now actually works under the surface admin uses to debug it. <strong>Sweep series status after tonight</strong>: 8 ships across the impersonation work (v1.25.5/.6, v1.26.1, v1.26.6, v1.26.7, v1.26.8) closed 17 raw-verifyAuth gaps across 7 files. emma-rescue scouted clean. emma-billing has 3 dead-code fns still on verifyAuth (intentionally left for the emma&harr;refill unification cleanup ship). Still-untouched server files have either no impersonation surface (no createServerFn, internal helpers only) or have not been audited &mdash; the next regression that surfaces &lsquo;zero rows under impersonation&rsquo; will name the next target. Touched: <code>src/server/emma-attribution.functions.ts</code> (4 schemas + 4 handlers + verifyAuth import removal), <code>src/server/emma-deposits.functions.ts</code> (2 schemas + 2 handlers + verifyAuth import removal), <code>src/routes/app.refill.recovery.tsx</code> (useTenantMembership import + viewAsUserId derivation + 7 call sites + load dep array), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.7",
    date: "May 2026",
    items: [
      "<strong>v1.26.7 &mdash; Impersonation sweep: billing files. Plus a naming-collision discovery + scope-discipline call.</strong> Final pair of files in tonight&rsquo;s impersonation-sweep series (emma-appointments v1.25.5/.6, rep-platform v1.26.1, emma-reliability v1.26.6, emma-rescue v1.26.5 scout&rsquo;d clean). Two parallel background Explore scouts catalogued <code>emma-billing.functions.ts</code> + <code>refill-billing.ts</code>. First-pass reports flagged ~6 gaps across both files &mdash; on verification, only <strong>2 were live</strong>; the others were either dead-code (emma&harr;refill fork left legacy exports stranded per v1.7.2), intentional caller-keyed for security (<code>getPaymentMethodStatus</code> &mdash; admin must NOT see tenant&rsquo;s Stripe payment method), or webhook/cron entries (no user-auth surface). <strong>Naming collision caught</strong>: both files export <code>applyPricingPlan</code>, <code>getActivePlan</code>, <code>listInvoices</code>, <code>getInvoicePreview</code> with identical names but different bodies. Each scout assumed its file was the called one; verification against actual import paths in routes resolved the ambiguity: <code>app.billing.tsx</code> imports the four plan/invoice fns from <code>refill-billing</code> (per v1.7.2&rsquo;s emma&rarr;refill migration), and <code>app.refill.recovery.tsx</code> imports <code>getInvoicePreview</code> from <code>emma-billing.functions</code> (the migration only swept billing.tsx, not recovery.tsx). So emma-billing&rsquo;s applyPricingPlan/getActivePlan/listInvoices have zero live call sites &mdash; legacy code from before the refill-billing fork &mdash; and refill-billing&rsquo;s getInvoicePreview is also dead (only emma-billing&rsquo;s is wired). <strong>The two live gaps closed</strong>: <code>refill-billing.ts:applyPricingPlan</code> at line 170 (write-path; admin impersonating Karen would insert the new <code>refill_pricing_plans</code> row keyed to admin&rsquo;s user_id, then update <code>tenants.plan</code> on the WRONG tenant via <code>getTenantIdForUser(admin)</code> &mdash; or fail outright if admin has no tenant). <code>emma-billing.functions.ts:getInvoicePreview</code> at line 382 (read-path; recovery dashboard MTD math would compute off admin&rsquo;s empty <code>emma_pricing_plans</code> + <code>emma_recovery_events</code> buckets). Both schemas extended with <code>viewAsUserId: z.string().uuid().optional()</code>; both handlers swap <code>verifyAuth</code> for <code>resolveEffectiveUserId</code>. <code>app.billing.tsx:231</code> applyPricingPlan call site updated to pass <code>viewAsUserId</code> from the existing <code>useTenantMembership</code> hook (already wired at line 97). <strong>v1.20 framing reversal noted</strong>: v1.20 explicitly excluded applyPricingPlan from the viewAsUserId opt-in (&lsquo;admin shouldn&rsquo;t be inadvertently mutating the impersonated tenant&rsquo;s plan&rsquo;). Subsequent sweeps (v1.25.5 ingestPatientCsv, v1.26.1 ensureMyRepAccount) established that mutation paths under impersonation are SAFE as long as admin&rsquo;s intent is explicit (clicking through the persona-switched UI is intent enough). Closing applyPricingPlan now in alignment with that established pattern. <strong>Recovery route NOT touched &mdash; scope discipline</strong>: <code>app.refill.recovery.tsx</code> calls four server fns &mdash; <code>getInvoicePreview</code> (now impersonation-capable) plus <code>listRecoveryEvents</code> + <code>getRecoveryStats</code> (emma-attribution.functions.ts, not in tonight&rsquo;s scope) + <code>listDepositIntents</code> (emma-deposits.functions.ts, also not in scope). Wiring useTenantMembership + viewAsUserId for ONLY getInvoicePreview would create the split-brain UI that v1.25.6 warned about (invoice math shows Karen&rsquo;s data, recovery events show admin&rsquo;s empty bucket &mdash; harder to debug than complete failure). Leaving recovery.tsx untouched until emma-attribution + emma-deposits get scouted/swept; flip them all at once. Handler-side fix in this ship is still useful: collapses to verifyAuth path when no viewAsUserId is passed, so no regression to current state. <strong>Dead-code annotation</strong>: emma-billing&rsquo;s applyPricingPlan/getActivePlan/listInvoices + refill-billing&rsquo;s getInvoicePreview are dead and intentionally NOT fixed in this sweep &mdash; separate cleanup ship should decide delete-vs-migrate. v1.7.2 changelog already flagged the emma&harr;refill unification as backlog. <strong>verifyAuth import retained in emma-billing.functions</strong>: still used by the three legacy/dead fns. Will be removable when those are deleted or migrated. <strong>Remaining sweep targets after tonight</strong>: emma-attribution.functions.ts (listRecoveryEvents + getRecoveryStats) and emma-deposits.functions.ts (listDepositIntents) &mdash; flagged via the recovery-route split-brain analysis. Closing both will unblock recovery.tsx&rsquo;s full impersonation correctness. Touched: <code>src/server/refill-billing.ts</code> (applyPlanInput + applyPricingPlan handler), <code>src/server/emma-billing.functions.ts</code> (invoicePreviewInput new schema + getInvoicePreview handler + resolveEffectiveUserId import), <code>src/routes/app.billing.tsx</code> (viewAsUserId on applyPricingPlan call site), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.6",
    date: "May 2026",
    items: [
      "<strong>v1.26.6 &mdash; Impersonation sweep: emma-reliability.functions.ts.</strong> Continuing the sweep pattern from v1.25.5/.6 (emma-appointments &mdash; 6 gaps closed), v1.26.1 (rep-platform &mdash; 2 gaps), and tonight&rsquo;s scout of emma-rescue (clean). Background Explore agent audit of <code>emma-reliability.functions.ts</code> found two raw-<code>verifyAuth</code> gaps and three clean fns. <strong>The two gaps</strong>: <code>dismissPatternAlert</code> at line 452 (admin impersonating Karen would update the alert row keyed to admin&rsquo;s own user_id &mdash; the <code>.eq(&apos;user_id&apos;, userId)</code> guard meant the dismiss silently no-op&rsquo;d against admin&rsquo;s empty bucket while Karen&rsquo;s alert stayed undismissed in the UI). <code>getReliabilityCard</code> at line 470 (per-patient detail read, no current call sites &mdash; declared for the future patient profile feature &mdash; but closing the gap now bakes correct semantics in before any UI consumes it, since a UI ship will plausibly land before the next sweep). <strong>The three clean fns confirmed</strong>: <code>listPatternAlerts</code> (already on <code>resolveEffectiveUserId</code> since v1.23.0), <code>recomputeReliabilityForPatient</code> (internal helper taking <code>userId</code> as a direct arg, correctness keyed to caller), <code>listReliabilityFlags</code> (just touched in v1.26.4/.5, already on <code>resolveEffectiveUserId</code>). <strong>Implementation</strong>: both <code>dismissInput</code> + <code>cardInput</code> zod schemas extended with <code>viewAsUserId: z.string().uuid().optional()</code>. Both handlers now thread <code>resolveEffectiveUserId({ accessToken, viewAsUserId })</code> and use <code>effectiveUserId</code> on the table eq + (for dismiss) the <code>dismissed_by</code> audit field. Call site at <code>app.refill.rescue.tsx:163</code> (<code>dismissAlert</code> handler) updated to pass <code>viewAsUserId</code> &mdash; the route already had <code>useTenantMembership</code> wired since v1.23.0 P3 sweep, so the variable was already in scope. <code>verifyAuth</code> is no longer imported from <code>@/server/auth-helpers</code> in this file (only <code>resolveEffectiveUserId</code> remains). <strong>Audit-trail caveat</strong>: <code>dismissed_by</code> now records the <em>effective</em> user_id (the impersonated tenant), not the admin who actually clicked. That matches the established pattern from v1.25.6 (impersonation is meant to be invisible to the tenant&rsquo;s data), but if attribution-back-to-admin ever matters for audit purposes (&lsquo;who really clicked dismiss&rsquo;) a separate <code>actor_user_id</code> column would be the right shape. Not in scope tonight. <strong>Remaining sweep targets</strong>: emma-billing.functions.ts, refill-billing.functions.ts &mdash; both untouched by tonight&rsquo;s scouts. Touched: <code>src/server/emma-reliability.functions.ts</code> (verifyAuth import removed, dismissInput + cardInput extended, both handlers swapped to resolveEffectiveUserId), <code>src/routes/app.refill.rescue.tsx</code> (viewAsUserId threaded at dismissAlert call site), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.5",
    date: "May 2026",
    items: [
      "<strong>v1.26.5 &mdash; Hotfix: v1.26.4 silently changed rule behavior. Naming the wrong claim.</strong> v1.26.4 changelog said &lsquo;Rule behavior unchanged &mdash; still filters on 6mo.&rsquo; <strong>That was wrong.</strong> Widening <code>listReliabilityFlags</code>&rsquo; server WHERE to <code>or(no_shows_6mo.gt.0, cancellations_6mo.gt.0, no_shows_lifetime.gt.0, cancellations_lifetime.gt.0)</code> &mdash; necessary to compute lifetime totals for display &mdash; also widened the result set that the client collapsed into <code>unreliableIds</code> via <code>new Set(flags.map(f =&gt; f.patientNodeId))</code>. Because <code>matchesRules</code> filters with <code>unreliableIds.has(p.id)</code>, the rule silently flipped from 6mo-strict (production semantics since v1.25.4) to lifetime-inclusive. On Karen&rsquo;s data the flagged count jumped from 115 to 358 and the &lsquo;Would be added&rsquo; preview shifted accordingly. Verification screenshot caught it. <strong>Also broken</strong>: toggle copy read &lsquo;(last 6 months &middot; 358 flagged &middot; 172 cancels / 2 no-shows)&rsquo; &mdash; mathematically impossible (358 patients can&rsquo;t have 174 events between them in 6mo) because 358 was the lifetime-inclusive union, not a 6mo number. Per the math-must-be-exact rule, the parenthesized count and the leading window-label must agree. <strong>Fix</strong>: client now filters the reliabilityFlags array down to 6mo-positive patients BEFORE building <code>unreliableIds</code>. Lifetime totals stay informational on the second description line, where they belong. Added a small &lsquo;X more patients with older history outside the 6mo window (not excluded by this rule)&rsquo; clause to the second line when the lifetime-only delta is non-zero &mdash; Karen sees the full picture without the rule silently changing under her. <strong>Karen&rsquo;s original &lsquo;never&rsquo; ask remains real</strong> &mdash; switching the filter from 6mo to lifetime (or adding a window selector) is a conscious UX decision for a separate ship, not a side effect of a data-availability ship. <strong>Pattern lesson</strong>: when a data ship widens a server query, the client read-path needs explicit re-validation against rule semantics. The minute I saw &lsquo;the WHERE got an OR with lifetime,&rsquo; I should have audited every consumer of the response, not just the display path. Caching that pattern for future denormalization ships. Touched: <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (unreliableIds memo filters 6mo-positive only + new lifetimeOnlyCount memo + toggle description second-line addendum), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.4",
    date: "May 2026",
    items: [
      "<strong>v1.26.4 &mdash; Lifetime cancellation / no-show tracking on emma_reliability_status.</strong> Karen&rsquo;s original A-list ask was &lsquo;exclude anyone who&rsquo;s ever cancelled or no-showed&rsquo; &mdash; but v1.25.4 shipped on the 6mo rolling columns (<code>no_shows_6mo</code> + <code>cancellations_6mo</code>) because that&rsquo;s what was materialized. 6mo is correct for tier semantics (the in_recovery tier must be recoverable when the window rolls past; otherwise patients are unforgivable forever and the reliability engine becomes a one-way trap). But the A-list filter is a different decision &mdash; a spa owner who wants &lsquo;never cancelled&rsquo; as a VIP criterion should be able to see lifetime totals. Two new columns added: <code>no_shows_lifetime</code> + <code>cancellations_lifetime</code>, both <code>integer not null default 0</code>. Migration <code>20260528020000_emma_reliability_lifetime_counts.sql</code> adds the columns, backfills from <code>emma_appointments</code> via a single CTE (same shape as the v1.25.7 RPC body, just no 6mo window filter), and updates the <code>refill_recompute_reliability_counts</code> RPC to populate both lifetime + 6mo in one statement going forward. The per-row TS trigger (<code>recomputeReliabilityForPatient</code> in <code>emma-reliability.functions.ts</code>) also updated to materialize both at appointment-status transitions. <strong>Surface change</strong>: A-list rules toggle description now shows a second line &mdash; &lsquo;Lifetime across all patients: X cancels / Y no-shows&rsquo; &mdash; alongside the existing 6mo line. Singular/plural agreement applied. Karen now sees both numbers and can see how much of the cancellation history is recent vs. accumulated. <strong>Behavior unchanged</strong>: the rule (<code>excludeUnreliable</code>) still filters on the 6mo window today. Switching the filter to lifetime (or adding a window selector) is a separate UX decision &mdash; data first, behavior change as a follow-up if Karen asks. <strong>Pattern note</strong>: tier (good/regular/vip/in_recovery) is NOT lifetime-driven and not changing &mdash; that logic is unchanged, still keyed off <code>in_recovery_threshold</code> + <code>in_recovery_window_months</code> per the spa-configurable policy. <strong>Operator step</strong>: paste <code>supabase/migrations/20260528020000_emma_reliability_lifetime_counts.sql</code> into the Supabase SQL editor before the worker is exercised against the new columns (link in the next message). The TS side gracefully renders &lsquo;0 lifetime&rsquo; until the migration lands &mdash; no crash, just a temporary undercount. Touched: <code>supabase/migrations/20260528020000_emma_reliability_lifetime_counts.sql</code> (new), <code>src/server/emma-reliability.functions.ts</code> (per-row trigger + listReliabilityFlags + ReliabilityFlag type + comment), <code>src/integrations/supabase/types.ts</code> (Row/Insert/Update extended), <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (reliabilityTotals memo extended + toggle description second line), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.3",
    date: "May 2026",
    items: [
      "<strong>v1.26.3 &mdash; Rescue claim page: provider promoted to the headline.</strong> v1.26.0 made provider a matching criterion in the rescue dispatcher and the SMS body already carried &lsquo;with Michelle&rsquo; via <code>providerClause</code>. But on the claim landing page (<code>/rescue/claim/&lt;token&gt;</code>) the provider was buried in the offer details card as one of four equal-weight rows (When / Treatment / With / Duration). A patient who normally sees Karen and gets an SMS for a Michelle slot needs to know that <strong>before</strong> they tap Grab &mdash; not after reading down a list. Same fundamental ask as v1.26.0 (provider matters to patients because the relationship is provider-shaped, not spa-shaped), one surface lower in the funnel. <strong>Headline change</strong>: &lsquo;{Name}, an opening just came up&rsquo; becomes &lsquo;{Name}, an opening just came up with <span style=&quot;color:#056048&quot;>Michelle</span>&rsquo; &mdash; provider rendered in brand green (same accent as the CalendarClock icon and the Grab CTA), making it visually the second-strongest element on the screen after the headline itself. <strong>Confirmation state extended</strong>: &lsquo;You&rsquo;re on the books / See you at {when}&rsquo; becomes &lsquo;See you with <strong>Michelle</strong> at {when}&rsquo; for the same reason &mdash; the patient should confirm-back what they grabbed. Both reuse the existing <code>showProvider</code> dedup at line 116 which suppresses the &lsquo;with&rsquo; clause when <code>providerName === spaName</code> (Acuity calendars named after the practice). <strong>What does NOT change</strong>: the offer details row labeled &lsquo;With&rsquo; stays as-is &mdash; repetition of the load-bearing fact across headline + details is intentional, not redundant. The SMS / email body composer is also untouched (already correct since v379.2). The subhead (&lsquo;{spa} just had a cancellation&rsquo;) stays at spa-level because the spa is the relationship-of-record; provider is the human-of-record for THIS slot specifically. Touched: <code>src/routes/rescue.claim.$token.tsx</code> (headline + already_claimed_by_you copy), <code>src/lib/changelog.ts</code> (this entry). No migration, no server change, no schema.",
    ],
  },
  {
    version: "v1.26.2",
    date: "May 2026",
    items: [
      "<strong>v1.26.2 &mdash; A-list rules: honest toggle copy + surface the cancel/no-show split.</strong> v1.25.4 shipped the toggle as &lsquo;Exclude no-show / cancellation history&rsquo; because the underlying reliability engine flags either signal inside the 6mo window. On Karen&rsquo;s real 5,754-row Acuity ingest the split landed at <strong>172 cancellations vs 2 no-shows</strong> &mdash; effectively cancellation-only. Leaving the label dual-named would have implied the toggle was meaningfully filtering on no-shows, which is misleading when 99% of the signal is cancels. Renamed to <strong>&lsquo;Exclude cancellation history&rsquo;</strong> and surfaced the split inline in the description so spa owners see exactly what they&rsquo;re filtering on: &lsquo;last 6 months &middot; 115 flagged &middot; 172 cancels / 2 no-shows&rsquo;. Per the [VERIFIED] convention &mdash; numbers in owner-facing UI must be exact, computed from real data, not approximated. Implementation: <code>listReliabilityFlags</code> already returned per-patient <code>noShows6mo</code> + <code>cancellations6mo</code>; the client was throwing the split away by collapsing the response to a <code>Set&lt;patientId&gt;</code>. Now we hold the full flags array in state, derive <code>unreliableIds</code> + <code>reliabilityTotals</code> via <code>useMemo</code>, and render the totals in the toggle description with singular/plural agreement. <strong>Behavior unchanged</strong> &mdash; the toggle still excludes any patient with either signal in the window. Underlying rule (<code>excludeUnreliable</code> on AListRules) is untouched; this is a copy + transparency ship. Server-side comment on <code>listReliabilityFlags</code> updated to reflect the new label + note the ratio so future readers don&rsquo;t accidentally split the underlying field. Touched: <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (reliabilityFlags state + derived unreliableIds/Totals memos + toggle copy), <code>src/server/emma-reliability.functions.ts</code> (comment), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.1",
    date: "May 2026",
    items: [
      "<strong>v1.26.1 &mdash; Impersonation sweep: rep platform.</strong> Scout of <code>src/server/rep-platform.ts</code> + the rest of the rep server surface (refill-outreach, refill-outreach-send, spa-rep-data-share, referral-tokens, scan-report, admin-reports) surfaced ONLY TWO load-bearing gaps. Most rep server fns either already opt into <code>resolveEffectiveUserId</code> (v1.24.x ground-pounding paid off) or are intentionally scoped to caller via <code>requireRepOrAdmin</code> (outreach sends, recruit stats) or are public (referral slug resolution, scan report HTML). The two real gaps: <code>ensureMyRepAccount</code> at line 135 (admin impersonating a rep was inserting a PHANTOM rep_account row keyed to the admin&rsquo;s own user_id every time the rep page loaded for that rep, instead of provisioning the impersonated rep) and <code>mintMyReferralLink</code> at line 263 (admin impersonating a rep would mint a link whose token encoded the ADMIN as the referring rep, and rep_accounts lookup would fail since admin has no rep_account row &mdash; the mint just errored). Both server fns now extend the file&rsquo;s existing <code>authWithViewAsInput</code> base (line 45) and thread <code>resolveEffectiveUserId</code> instead of raw <code>verifyAuth</code>. Route call sites updated: <code>app.rep.referral-links.tsx</code> handleEnroll + handleMint now derive <code>viewAsUserId</code> from <code>getAdminViewAsUserId()</code> at call time (handlers fire after first render so the useEffect-scoped variable wasn&rsquo;t reachable; computing inline is the right shape). <code>app.rep.integrations.tsx</code> getMyRepAccount call upgraded the same way &mdash; the file had no impersonation hook wired at all, so added the import alongside the existing pattern. <strong>Non-applicable surfaces explicitly excluded from sweep</strong>: <code>resolveReferralSlug</code> (anonymous public), <code>getScanReportHtml</code> (token-gated public), <code>getRecruitInvitePreview</code> (event-id-gated public), the entire <code>spa-rep-data-share.functions.ts</code> surface (consent layer + spa-side keying gates correctly), <code>sendOutreachEmail</code> + <code>listMyOutreachSends</code> + <code>getMyRecruitStats</code> (sender/principal-keyed by design, impersonation would corrupt audit trail). <code>acceptRecruitInvite</code> is similarly caller-scoped (new rep accepts their own invite). Touched: <code>src/server/rep-platform.ts</code> (ensureInput + mintInput swap to authWithViewAsInput; ensure + mint handlers route through resolveEffectiveUserId), <code>src/routes/app.rep.referral-links.tsx</code> (handleEnroll + handleMint inline viewAsUserId derivation), <code>src/routes/app.rep.integrations.tsx</code> (import + load-block inline viewAsUserId), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.26.0",
    date: "May 2026",
    items: [
      "<strong>v1.26.0 &mdash; Provider becomes a matching criterion in the rescue dispatcher.</strong> When a Michelle-booked appointment cancels, the open slot is fundamentally Michelle&rsquo;s &mdash; not Karen&rsquo;s, not anyone else&rsquo;s. The dispatcher should offer that slot to patients whose primary provider is Michelle, not the entire waitlist. Patient who normally sees Karen shouldn&rsquo;t get pinged about a last-minute Michelle opening &mdash; different provider, different relationship, low affinity, wasted send. Implementation lives in <code>selectFitPatients</code> at <code>src/server/emma-rescue.functions.ts:154</code>. After the existing treatment-type filter + patient hydration (phone / banned / opted-out checks), a new provider-affinity pass runs: one bulk query against <code>emma_appointments</code> across all remaining candidate <code>patient_node_id</code>s pulling <code>(patient_node_id, provider_name)</code>, then in-memory aggregation derives each candidate&rsquo;s primary provider as the most-frequent <code>provider_name</code> across their all-time appointment history (ties broken string-asc, stable enough for this scale). The fit pool is then hard-filtered: keep only candidates whose primary provider matches the open appointment&rsquo;s provider (case-insensitive trim, mirroring <code>providerClause</code> at line 245). <strong>Fallback policy</strong>: if the affinity filter produces ZERO candidates, the unfiltered pool is used &mdash; rescue value is too high to lose to an empty match. A structured <code>console.warn</code> with <code>event:rescue_provider_affinity_fallback</code> fires once per fallback so we can observe how often the broader pool kicks in. <strong>Gate at the call site</strong>: <code>providerForMatch</code> is computed in <code>dispatchRescueAttempt</code> as the appointment&rsquo;s provider name UNLESS it&rsquo;s null OR matches the spa name (the same Acuity-calendar-named-after-the-practice case that <code>providerClause</code> suppresses). Null short-circuits the affinity filter inside the matcher &mdash; no behavior change vs. today for spas without provider data or with spa-as-calendar setups. <strong>What does NOT change</strong>: message composition. The body of the rescue SMS / email already includes &ldquo;with Michelle&rdquo; via <code>providerClause</code> (line 245) when provider differs from spa name; that&rsquo;s the existing provider mention in the wire and it stays. No sender-identity override, no display-name refactor &mdash; provider-match is a matching engine criterion, not a sender persona. <strong>Honesty note</strong>: I initially misread &ldquo;another criteria is &lsquo;provider&rsquo;&rdquo; as a sender-identity ask and drafted a plan around <code>overrideFromDisplayName</code> + opener rewrites. Grasshopper corrected pre-implementation. Plan re-scoped to matching engine before any code was written. Touched: <code>src/server/emma-rescue.functions.ts</code> (selectFitPatients + dispatchRescueAttempt call site), <code>src/lib/changelog.ts</code> (this entry). No migration, no UI, no schema.",
    ],
  },
  {
    version: "v1.25.7",
    date: "May 2026",
    items: [
      "<strong>v1.25.7 &mdash; Hotfix: Workers CPU timeout on bulk appointment ingest. The v1.25.5 reliability sweep was the wrong shape.</strong> Karen re-uploaded the 5,754-row Acuity export expecting a clean receipt. UI flashed and went blank instead. Root cause: the post-ingest <code>recomputeReliabilityForUser</code> sweep added in v1.25.5 loops <code>recomputeReliabilityForPatient</code> over every distinct patient (~800-1,000 at Karen-scale), each iteration ~3 DB round trips. 2,400+ round trips blew past Cloudflare Workers&rsquo; 30s CPU budget; the inserts committed (they run first, fast) but the receipt never returned to the client. EXACT antipattern that the file&rsquo;s own line 498 comment warns about for buildPatientIndex (which was fixed in v372.2 by collapsing per-row queries into a single bulk query). I added the warned-against pattern back in. Naming the wrong claim: I shipped a loop-of-DB-queries on a Workers handler 7 days after re-anchoring scout-first / Boris north star and 2 days after caching v372.2&rsquo;s anti-pattern lesson in my own changelog comment. Should have seen it in scout. <strong>Fix</strong>: new Postgres RPC <code>refill_recompute_reliability_counts(p_user_id)</code> via migration <code>20260528010000</code>. Single SQL statement: CTE aggregates no_shows_6mo / cancellations_6mo / total_visits per patient using count(*) filter, INSERT ... ON CONFLICT upserts into <code>emma_reliability_status</code>. Tier stays untouched (per-row trigger + cron still own tier transitions). One round trip from the Worker, server-side aggregation. <code>ingestAppointmentCsv</code> now calls <code>sb.rpc(&apos;refill_recompute_reliability_counts&apos;, { p_user_id })</code> instead of the looping helper. The looping helper still exists for the cron path where Workers timeouts don&rsquo;t apply. <strong>Karen&rsquo;s previous upload likely committed all 5,754 rows</strong> before the timeout. Verification SQL in the next ship message will confirm what landed; no re-upload needed if the count checks out. Touched: <code>supabase/migrations/20260528010000_refill_recompute_reliability_counts.sql</code> (new), <code>src/server/emma-appointments.functions.ts</code> (RPC call replaces sync loop), <code>src/integrations/supabase/types.ts</code> (RPC type), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.25.6",
    date: "May 2026",
    items: [
      "<strong>v1.25.6 &mdash; Impersonation sweep: close the remaining 4 viewing-as gaps in emma-appointments.functions.ts.</strong> v1.25.5 fixed the two highest-impact ones (ingestAppointmentCsv + listAppointments). On post-fix verification, the appointments page still showed a confusing &lsquo;14 of 14 future appointments aren&rsquo;t linked&rsquo; banner while the same page&rsquo;s empty state showed &lsquo;No appointments yet&rsquo; &mdash; classic split-brain. Root cause: <code>getAppointmentMatchCoverage</code> at line 884 was a THIRD impersonation gap I named in the v1.25.5 changelog but didn&rsquo;t close. It was reading the admin&rsquo;s bucket (14 pre-existing rows from earlier test ingests), while <code>listAppointments</code> correctly read Karen&rsquo;s (empty). Fixed it and swept the other three latent gaps in the same file: <code>updateAppointmentStatus</code>, <code>getNoShowPolicy</code>, <code>updateNoShowPolicy</code>. All four now thread <code>resolveEffectiveUserId</code> + accept <code>viewAsUserId</code> on their zod inputs. Call sites updated: <code>app.refill.appointments.tsx</code> for the first three, <code>app.refill.settings.noshow.tsx</code> for the policy pair (which had no <code>useTenantMembership</code> wired at all &mdash; added). Karen&rsquo;s 14-banner should now read 0/0 (her bucket is genuinely empty until the next CSV upload). Lesson: when you find one impersonation gap in a file, close the whole file in one sweep &mdash; whack-a-mole takes longer than a sweep and the partial fix produces split-brain UI states that are harder to debug than complete failure. Touched: <code>src/server/emma-appointments.functions.ts</code> (viewAsUserId on statusInput / policyInput / policyUpdateInput / coverage inline schema; resolveEffectiveUserId on 4 handlers), <code>src/routes/app.refill.appointments.tsx</code> (viewAsUserId on 3 call sites), <code>src/routes/app.refill.settings.noshow.tsx</code> (useTenantMembership added + viewAsUserId on 2 call sites + load deps), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.25.5",
    date: "May 2026",
    items: [
      "<strong>v1.25.5 &mdash; Hotfix: ingestAppointmentCsv + listAppointments now respect admin viewing-as. Also: bulk-ingest reliability sweep.</strong> Karen ran her Acuity schedule export through /app/refill/appointments. Receipt: 5,754 parsed, 5,754 inserted, <strong>0 matched patients</strong>. Root cause: <code>ingestAppointmentCsv</code> at <code>emma-appointments.functions.ts:433</code> called raw <code>verifyAuth</code>, returning the admin's user_id. <code>buildPatientIndex(sb, userId)</code> then loaded the admin's patient list (empty), so every appointment failed name/phone/email match. Worse, the appointments themselves were written under admin's user_id rather than Karen's &mdash; orphaned in a tenant bucket Karen can't see. The pre-existing impersonation gap I flagged for <code>setPatientVip</code> during the v1.25.1 plan but never closed for the appointment ingest path. The yellow banner on the page (&lsquo;CSV ingest writes into this tenant &mdash; v1.20.5&rsquo;) describes <code>ingestPatientCsv</code> ONLY; the appointment surface inherited the gap. Fix: <code>ingestInput</code> + <code>listInput</code> zod schemas now accept <code>viewAsUserId</code>, both handlers thread through <code>resolveEffectiveUserId</code>, route at <code>app.refill.appointments.tsx</code> passes the impersonated user. <strong>Bonus fix</strong>: the per-row reliability recompute hook (line ~705) fires only on individual status flips, not bulk ingest. So even when matching works, bulk-imported cancellations never updated <code>emma_reliability_status</code> &mdash; A-list&rsquo;s <code>excludeUnreliable</code> rule would read 0 forever. Added a sweep call to <code>recomputeReliabilityForUser</code> at end of successful ingest (gated on <code>patientsMatched &gt; 0</code> to avoid pointless work when match rate is zero). <strong>Cleanup</strong>: the 5,754 orphaned admin rows from Karen&rsquo;s first attempt need a one-shot DELETE before re-upload (Supabase dashboard paste, included in the next ship message). Touched: <code>src/server/emma-appointments.functions.ts</code> (viewAsUserId on both inputs + resolveEffectiveUserId on both handlers + post-ingest reliability sweep), <code>src/routes/app.refill.appointments.tsx</code> (viewAsUserId on both calls), <code>src/lib/changelog.ts</code> (this entry). Other emma-appointments fns (lines 637, 734, 764, 851) still use raw verifyAuth &mdash; separate impersonation gaps to close in a v1.25.6 sweep.",
    ],
  },
  {
    version: "v1.25.4",
    date: "May 2026",
    items: [
      "<strong>v1.25.4 &mdash; A-list rules: new toggle to exclude patients with no-show / cancellation history.</strong> Karen ask: A-list shouldn&rsquo;t include patients who&rsquo;ve been unreliable, even if their spend / cadence math otherwise qualifies. Reliability data already lives in <code>emma_reliability_status</code> as <code>no_shows_6mo</code> + <code>cancellations_6mo</code> per patient (denormalized at appointment-status transitions by Emma&rsquo;s reliability engine). New bulk reader <code>listReliabilityFlags</code> in <code>emma-reliability.functions.ts</code> returns one row per patient with any flag in the rolling 6-month window &mdash; fetched once at page mount in parallel with patients + saved rules, materialized into a <code>Set&lt;patientId&gt;</code>, AND-ed into <code>matchesRules()</code> client-side. <strong>The window is 6mo, not lifetime</strong> &mdash; checkbox label calls this out explicitly (&lsquo;last 6 months&rsquo;) so Karen knows what&rsquo;s being checked. Lifetime variant is a follow-up if she asks: would require either a new column on reliability_status or a live count against emma_appointments where status IN no-show/cancelled. Live-count path is straightforward but heavier; defer until Karen says 6mo isn&rsquo;t enough. <strong>Persistence note</strong>: AListRules type extended with <code>excludeUnreliable: boolean</code>, default true. Old persisted rule rows in <code>user_preferences.prefs.aListRules</code> don&rsquo;t have this field, so <code>getMyAListRules</code> now does a sanitize-on-read spread (<code>...DEFAULT_A_LIST_RULES, ...stored</code>) &mdash; missing keys inherit defaults, stored nulls (which are valid for the nullable threshold rules) are preserved correctly. Backward-compat without a data migration. <strong>Flagged-count surfaced inline</strong>: checkbox shows &lsquo;N flagged&rsquo; in the description so Karen knows the rule&rsquo;s effective scope before toggling. Touched: <code>src/server/emma-reliability.functions.ts</code> (listReliabilityFlags + ReliabilityFlag type), <code>src/server/user-prefs.functions.ts</code> (excludeUnreliable on AListRules + DEFAULT + zod + sanitize-on-read spread), <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (unreliableIds state + parallel load + matchesRules signature + checkbox), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.25.3",
    date: "May 2026",
    items: [
      "<strong>v1.25.3 &mdash; A-list rules: Preview list button reveals the prospective A-list before Apply.</strong> Karen ask post-walk of v1.25.2: counts on the preview card are good but she wants to scan NAMES before applying so she can catch anything weird (e.g., a banned-but-not-flagged regular, a former-favorite who churned). New <strong>Preview list</strong> button between Apply + Clear toggles an inline table that shows the full post-Apply A-list &mdash; everyone who would be vip after clicking Apply, sorted by lifetime spend descending. Three status chips: <strong>Already A-list</strong> (green) for currently-vip patients who also match rules; <strong>Would be added</strong> (blue) for the delta Apply would create; <strong>Manual star</strong> (amber) for currently-vip patients who DON&rsquo;T match the rules and are preserved by the additive-only design. Header copy makes the preservation behavior explicit so Karen doesn&rsquo;t have to remember the design call &mdash; the chip distinction tells her at a glance what she&rsquo;s about to mutate vs what survives untouched. Patient names link to <code>/app/refill/patients/$patientId</code> for drill-in. Button-triggered (not live) per spec &mdash; opening on every slider tick would jitter the page. Same-page reveal (not modal) so Karen can keep tuning rules with the list visible. Table columns: Name / Last visit / Visits / Lifetime spend / Status. Touched: <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (previewOpen state + previewRows useMemo + Preview button + inline table section + new PreviewRow subcomponent), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.25.2",
    date: "May 2026",
    items: [
      "<strong>v1.25.2 &mdash; Hotfix: A-list rules page was 500ing on load. <code>listInput</code> zod validator in <code>patient-ingest.functions.ts</code> capped <code>limit</code> at 2000; v1.25.1 route passed 5000 (per Plan-agent recommendation to handle multi-location tenant books). The validator rejected the call before the handler ran &mdash; Karen saw a raw zod error blob instead of the dashboard. Raised the cap to 5000 to match the route's intent. Caught in screen 1 of the dry-run-as-Karen walk (<code>feedback_dry_run_after_major_builds</code> earning its keep again). Pre-flight schema check missed during v1.25.1 plan-to-code translation &mdash; the Plan-agent's call-out was correct in shape but I didn't trace it back to the validator's max. Touched: <code>src/server/patient-ingest.functions.ts</code> (validator cap 2000 → 5000), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.25.1",
    date: "May 2026",
    items: [
      "<strong>v1.25.1 &mdash; A-list automation dashboard. Karen stops starring patients one-at-a-time; she sets rules and the platform does the bulk-mark in one round trip.</strong> Spa-owner-facing power surface at <code>/app/refill/patients/a-list-rules</code>, reachable from a third top-right link on the patients page (next to Contacts + Import CSV). NOT promoted into the chip nav &mdash; per the steering-wheel rule (<code>project_three_agent_architecture</code>) Refill platform defaults stay narrow; A-list automation is a power-user lever, not a configure-me default. <strong>Rules form</strong>: three sliders (lifetime spend &ge; $X, last visit within Y days, total visits &ge; Z) each with an on/off toggle so a rule can be disabled, plus an exclude-banned toggle. Defaults: $1,000 / 365 days / 3 visits / banned excluded. <strong>Live preview card</strong>: pure client-side filter against the already-loaded <code>PatientListRow[]</code> (every field needed &mdash; <code>lifetimeSpendUsd</code>, <code>lastVisit</code>, <code>totalVisits</code>, <code>banned</code> &mdash; was already baked into <code>knowledge_nodes.attachments</code> by the ingest pipeline). Instant slider feedback, no debounce, no RPC per drag. Card surfaces three counts: already A-list (matches AND currently VIP), would be added (matches AND not yet VIP &mdash; what Apply will write), manual stars preserved (currently VIP AND don't match &mdash; explicit reassurance the design-call protects manual work). <strong>Apply is ADDITIVE ONLY</strong>: ships writes vip=true on the addIds delta, never sets vip=false. Manual stars set via the per-row VIP toggle survive every Apply. The Plan-agent pushback on the original spec called this out as the biggest design footgun &mdash; Karen manually stars Mrs. Patel, runs rules tomorrow with spend&ge;$1k excluding Mrs. Patel, the silent-unstar wipes the intent without naming who. Additive-only sidesteps the manual-vs-rule-provenance problem entirely without a data-shape change. <strong>Clear is the separate destructive escape hatch</strong>: typed-confirm modal (must type CLEAR) that bulk-removes ALL vip flags including manual ones. Rules stay saved &mdash; re-apply rebuilds in one click. <strong>Bulk write path</strong>: new Postgres RPC <code>refill_bulk_set_vip(p_user_id, p_add_ids, p_remove_ids)</code> using <code>jsonb_set(attachments, '{vip}', 'true', true)</code> &mdash; merges the vip field without clobbering the rest of the PatientSummary blob (lifetimeSpend, lastVisit, productMix, loyaltyEngagement all live in the same jsonb). One round trip for a 1,140-row apply vs. 1,140 round trips if the per-row <code>setPatientVip</code> were looped. <strong>Persistence</strong>: extended <code>user_preferences</code> with a generic <code>prefs jsonb</code> column (room for future per-user settings without per-feature migrations). Rules stored under <code>prefs.aListRules</code>, lastAppliedAt under <code>prefs.aListLastAppliedAt</code>. <strong>Tenant impersonation safety</strong>: both new server fns (<code>getMyAListRules</code>, <code>saveAListRulesAndMarkApplied</code>) AND the new bulk write (<code>setPatientVipBulk</code>) go through <code>resolveEffectiveUserId</code> so an admin viewing-as Karen writes to Karen's row, not the admin's. The existing per-row <code>setPatientVip</code> (v385.2 legacy) intentionally left as-is &mdash; pre-existing gap, separate ship. <strong>Migration paste path</strong>: <code>supabase/migrations/20260528000000_user_preferences_prefs_jsonb.sql</code> &mdash; ALTER + CREATE FUNCTION + GRANT + NOTIFY in one file with verify queries inlined as comments at the bottom. Touched: <code>supabase/migrations/20260528000000_user_preferences_prefs_jsonb.sql</code> (new), <code>src/integrations/supabase/types.ts</code> (prefs column + refill_bulk_set_vip RPC types), <code>src/server/user-prefs.functions.ts</code> (AListRules type + DEFAULT_A_LIST_RULES + getMyAListRules + saveAListRulesAndMarkApplied), <code>src/server/patient-ingest.functions.ts</code> (setPatientVipBulk + setVipBulkInput), <code>src/routes/app.refill.patients.a-list-rules.tsx</code> (new route + RuleRow + StatChip + apply/clear modals), <code>src/routes/app.refill.patients.index.tsx</code> (third top-right link), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.25.0",
    date: "May 2026",
    items: [
      "<strong>v1.25.0 &mdash; Matching engine upgrade: nickname dictionary + Pass 3 contact-cross-match for surname-change detection.</strong> Two of the gaps Po called out during the matching-engine walkthrough, addressed in one ship. <strong>Nickname dictionary</strong>: <code>src/lib/fuzzy-name-match.ts</code>'s Pass 2 (Levenshtein-on-last-name) gated first-initial equality as a guard against Hennesy/Hennessey-style confidently-wrong-pair surfacing. That blocked legitimate matches where the initial differs but the name is a known nickname pair (Bob ↔ Robert, Bill ↔ William, Liz ↔ Elizabeth, Kate ↔ Katherine, and ~30 other common pairs curated for US med-spa books). v1.25.0 adds a <code>NICKNAME_CANONICAL</code> map; <code>FuzzyTarget</code> gains a <code>firstNameCanonical</code> field; the first-name guard in <code>findFuzzyMatches</code> accepts initial-equal OR canonical-equal. Matches surfaced via nickname resolution carry a <code>matchedViaNickname</code> flag so callers can render a hint chip (UI label change deferred &mdash; the suggestions just show up in the existing EXACT / VERY CLOSE / CLOSE buckets). <strong>Pass 3 — contact cross-match</strong>: new server fn <code>findSameContactDifferentName</code> in <code>patient-ingest.functions.ts</code> detects patient pairs that share a phone OR email + same canonical first name but differ on last name. The classic maiden→married surname-change signal (Sarah Anderson + Sarah Chen same phone) that letter-distance can't catch. First-name guard via nickname canonical (Bob Smith + Robert Smith same phone, same person) prevents household-sharing-phone false positives (mom + daughter would have DIFFERENT first names, filtered out). Results sorted by combined lifetime spend descending so the highest-value duplicates surface first. <strong>UI surface</strong>: new 'Same contact, different name' section on <code>/app/refill/patients/contacts</code>, above the existing 'Suggested matches'. Each pair renders side-by-side patient cards (display name + lifetime spend + visits + last visit), the matched contact value, and a 'via nickname' chip when canonical resolution was required. Cards link into the patient detail page for manual review. NO MERGE primitive yet &mdash; spa owner reviews and reconciles in QuickBooks (or wherever the source data lives); a one-click merge action is queued for a future ship. <strong>The maiden/married case is now caught when both records have contact info</strong>. Pass 3 doesn't help when patients have NO contact info loaded; that's a fundamentally different problem (no signal to bridge by). Touched: <code>src/lib/fuzzy-name-match.ts</code> (nickname dictionary + canonical-form first-name guard), <code>src/server/patient-ingest.functions.ts</code> (findSameContactDifferentName + types), <code>src/routes/app.refill.patients.contacts.tsx</code> (DuplicatesSection + load wiring), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.24.7",
    date: "May 2026",
    items: [
      "<strong>v1.24.7 &mdash; Agentiport cross-DB sister-data pointer on /app/refill/patients/contacts.</strong> Walk-through 2026-05-28 surfaced that Karen's enriched contacts (815 reachable / 92 already-matched suggestions / 6 banned) live in the Agentiport (Emma OS, openagenticv4 codebase) Supabase project &mdash; NOT refill-prod. The CSV upload predated the Refill spinout; refill-prod inherited the 1,140 patient roster via SQL fixup but the contact-candidates + match state stayed on Agentiport. Pragmatic fix: small amber 'See in Agentiport' link in the page header actions slot pointing at <code>agentiport.com/app/refill/patients/contacts</code>. No hunting next session. Memory written to <code>project_agentiport_refill_cross_db_contacts.md</code> with the A/B fallback strategy: re-upload CSV to refill-prod first (Path B, fast), cross-DB copy preserving match state as the fallback (Path A, harder, only if B doesn't match Agentiport's quality). Touched: <code>src/routes/app.refill.patients.contacts.tsx</code> (Agentiport link in actions), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.24.6",
    date: "May 2026",
    items: [
      "<strong>v1.24.6 &mdash; deriveShell priority flip: spa-owner > rep (was rep > spa-owner) so dual-role users land in their primary shell.</strong> Surfaced when admin impersonated real Karen from the PersonaSwitcher: she resolved to RepShell instead of RefillShell because her <code>user_roles</code> contains BOTH <code>spa_owner</code> (from tenant_memberships) AND <code>rep</code> (from a rep_accounts row, likely origin_type='spa_owner' for attribution-chain reasons). Pre-fix deriveShell priority was admin > rep > spa-owner > developer; rep won. Karen's 1,140 patients + recovery feed never rendered because RepShell took over with her (empty) rep-attribution data instead. Fix: deriveShell priority is now admin > spa-owner > rep > developer. Rationale: in Refill's model, owning a spa tenant is the more concrete primary identity &mdash; that's where the patients, recovery events, billing live. Rep attribution is supplementary infrastructure for referral chains. Pure reps (Kelly, no spa_owner role) stay shell='rep' unchanged. A future user who is PRIMARILY a rep AND happens to own a spa would now land in spa-owner shell by default; they can navigate to /app/rep directly &mdash; minor friction trade-off for the more common case being right by default. Touched: <code>src/server/role-helpers.ts</code> (deriveShell priority order), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.24.5",
    date: "May 2026",
    items: [
      "<strong>v1.24.5 &mdash; Rep-platform read fns honor viewAsUserId; Spa Owner persona now points at real Karen for the QuickBooks-uploaded roster.</strong> Two interlocking fixes for the demo walk. <strong>(1) Kelly's rep pages were blank under impersonation</strong> &mdash; <code>useRepProfile</code> + <code>getMyRepAccount</code> were opted into viewAsUserId back in v1.24.0, but the rep-platform's other reads (<code>getMyNetwork</code>, <code>getMyLedger</code>, <code>getMyLiveEarnings</code>, <code>listMyReferralLinks</code>) still resolved by caller and returned admin's empty rep data. Mirror of the v1.23.0 spa-owner long-tail sweep but for the rep platform: each fn now takes an optional <code>viewAsUserId</code> via the shared <code>authWithViewAsInput</code> schema and uses <code>resolveEffectiveUserId</code> for filtering. Page consumers + <code>LiveEarningsCard</code> + <code>RefillRepHome</code> read <code>getAdminViewAsUserId()</code> from localStorage and plumb through. Net effect: when admin impersonates Kelly, RepShell renders chrome AND inner pages (Network / Recruit / Ledger / Referral Links / Economics / Outreach) populate with her 7 downstream, 64 ledger, 19 outreach, 20 recovery events. <strong>(2) Spa Owner persona retargeted at real Karen.</strong> The v1.24.4 allowlist pointed Spa Owner at <code>karen@rejuv-demo.test</code> (demo tenant 'Rejuv Skin Spa'), which has 17 recovery events seeded but only 1 patient. Grasshopper's QuickBooks customer-sales-detail CSV upload landed under <code>karen.aslak@gmail.com</code> (real Karen, production 'Rejuv' tenant) via the post-v1.20.5 admin-impersonation write path. Result: 1,140 real patients existed in DB but the demo persona pointed at the wrong Karen. v1.24.5 fixes the persona allowlist to use <code>karen.aslak@gmail.com</code>. The Spa Owner walk now renders Karen's actual production patient roster, addresses, transaction history &mdash; the real data we already loaded. <strong>Side note</strong>: the v1.24.5 proposal to seed 75 synthetic patients was wrong &mdash; per <em>feedback-real-data-first</em>, real data structure beats synthetic. Migration file deleted before paste. Touched: <code>src/server/rep-platform.ts</code> (4 fns opted in, shared authWithViewAsInput schema), <code>src/routes/app.rep.network.tsx</code> + <code>app.rep.recruit.tsx</code> + <code>app.rep.ledger.tsx</code> + <code>app.rep.referral-links.tsx</code> + <code>app.rep.economics.tsx</code> + <code>app.rep.outreach.tsx</code> (page-side plumbing), <code>src/components/refill/LiveEarningsCard.tsx</code> + <code>RefillRepHome.tsx</code> (component plumbing), <code>src/lib/demo-personas.ts</code> (real Karen allowlist), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.24.4",
    date: "May 2026",
    items: [
      "<strong>v1.24.4 &mdash; PersonaSwitcher restricted to 3 canonical demo personas: Admin (caller) / Spa Owner (Karen Demo) / Rep (Kelly Demo).</strong> Pre-fix, the dropdown listed all 14 users with non-member roles (every spa-owner including production Karen Aslakson, every rep, every developer). Two problems: (1) demo flow diluted &mdash; operator needs THREE personas to walk the system, not fourteen, and (2) production users (real karen.aslak with 1,140 live patients) leaked into the demo switcher when they should be reached only via /app/admin/users for ops work. Per Grasshopper: 'i don't need switcher for users - JUST ROLES'. Fix: new <code>src/lib/demo-personas.ts</code> defines <code>DEMO_PERSONA_EMAILS</code> allowlist (<code>karen@rejuv-demo.test</code> + <code>kelly@refill-demo.test</code>). PersonaSwitcher filters the listImpersonableUsers result client-side to caller + allowlisted emails before grouping. Shell-group labels switched to singular (Admin / Spa Owner / Rep) since each group has exactly one persona now. The server fn + /app/admin/users CRUD continue to surface ALL users — that's the operator surface; the dropdown is the demo surface. Adding a fourth demo persona requires deliberately editing demo-personas.ts (not just a DB row), which keeps the dropdown intentional. <strong>Queued (v1.24.5)</strong>: demo data audit + restore. Rejuv Skin Spa demo tenant has 17 recovery events + $3,925 lifetime but only 1 patient — incoherent for a walkthrough. Patient roster + outreach history + downstream tenants need re-seeding to make the demo flow meaningful end-to-end. Touched: <code>src/lib/demo-personas.ts</code> (new allowlist), <code>src/components/admin/PersonaSwitcher.tsx</code> (filter + singular labels), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.24.3",
    date: "May 2026",
    items: [
      "<strong>v1.24.3 &mdash; Impersonation coherence: generalized smart-routing, sign-out clears impersonation, RepShell viewing-as banner.</strong> Four obvious-default gaps from the walk-through audit, fixed together. <strong>(1) Generalized smart-routing.</strong> v1.24.2 handled /app/admin → non-admin persona by redirecting to /app. But /app/refill → rep persona (admin on a spa-owner URL switching to Kelly the rep) ALSO needed the redirect; the spa-owner home route (RefillHome at /app/refill) doesn't render under rep context, so Grasshopper saw a blank page. New <code>pickRedirectForPersonaSwitch(pathname, targetShell)</code> in <code>src/lib/admin-view-as.ts</code> generalizes the rule: if the current URL's shell-kind (admin / spa-owner / rep) differs from the target persona's shell, redirect to /app for auto-dispatch. Cross-same-shell (cross-spa-owner Patients-as-Karen → Patients-as-Maria; cross-rep) stays in place so admin can compare the same surface across personas. PersonaSwitcher.onChange + Reports' View-as buttons both use the helper now &mdash; same behavior everywhere. <strong>(2) Sign-out clears impersonation.</strong> Pre-fix, signing out while impersonating Karen left <code>refill-admin-view-as-user-id</code> in localStorage; the next sign-in (this user OR anyone on this device) started already mid-impersonation. New <code>clearAdminViewAsUserIdNoReload()</code> called from <code>auth.tsx signOut</code> before <code>supabase.auth.signOut</code>. <strong>(3) RepShell viewing-as banner.</strong> RefillShellChrome had an amber &ldquo;Viewing as &lt;tenant&gt;&rdquo; banner when admin was impersonating a spa-owner; RefillRepShellChrome had no such indicator for rep impersonation. Asymmetric for no reason. RefillRepShellChrome now reads <code>getAdminViewAsUserId()</code> and renders the same banner with the rep's display name when impersonating. <strong>(4) Reports View-as smart-routing.</strong> /app/admin/reports' inline View-as buttons used to call <code>setAdminViewAsUserId(userId)</code> with no redirect; admin stayed on /app/admin/reports. Now passes the right redirect via the same helper as PersonaSwitcher. <strong>The meta-lesson</strong>: persona-switch behavior is obvious-standard-UX, not a judgment call. I shipped the partial v1.24.2 fix when the general rule was already in front of me. Should have generalized the first time. Touched: <code>src/lib/admin-view-as.ts</code> (new helpers), <code>src/components/admin/PersonaSwitcher.tsx</code> (uses helper), <code>src/components/refill/RefillRepShellChrome.tsx</code> (impersonation banner), <code>src/lib/auth.tsx</code> (sign-out clears localStorage), <code>src/routes/app.admin.reports.tsx</code> (View-as buttons smart-route), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.24.2",
    date: "May 2026",
    items: [
      "<strong>v1.24.2 &mdash; PersonaSwitcher: picking a non-admin persona from an admin URL drops to /app instead of reloading the admin page.</strong> Pre-fix, if admin was on /app/admin/users and picked &lsquo;Rejuv Skin Spa&rsquo; from the dropdown, the page reloaded — the persona switcher said &lsquo;Rejuv Skin Spa&rsquo; but the URL stayed /app/admin/users. Two truths fighting: admin keeps full privileges regardless of impersonation (server-side gate is on caller, not impersonated), AND picking a persona should drop into that persona's experience. The reload-in-place behavior preserved admin position but left an incoherent state where you're &ldquo;being&rdquo; Karen on an admin-only surface. Fix: <code>setAdminViewAsUserId(userId, redirectTo?)</code> takes an optional redirect target. <code>PersonaSwitcher.onChange</code> now detects when admin is on an /app/admin/* URL and is picking a non-admin persona → passes <code>redirectTo=&quot;/app&quot;</code> → auto-dispatch routes to the impersonated user's home shell. Other transitions (cross-spa-owner Patients-as-Karen → Patients-as-Maria, cross-rep, reverting to admin) still reload in place so admin can compare the same surface across personas. Touched: <code>src/lib/admin-view-as.ts</code> (redirectTo param), <code>src/components/admin/PersonaSwitcher.tsx</code> (onChange smart routing), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.24.1",
    date: "May 2026",
    items: [
      "<strong>v1.24.1 &mdash; AdminNav chip strip on every /app/admin/* page + feature flags rewritten as a catalog-driven surface.</strong> Two issues surfaced on the v1.24.0 walk-through. <strong>(1) No admin nav</strong>: pre-hotfix, /app/admin had a card grid but once you clicked into Users / Flags / Reports / Audit / Outreach / Trials, there was no way back or sideways without bouncing to /app/admin home. Major basic-UX miss. New <code>src/components/admin/AdminNav.tsx</code> renders a 7-chip strip (Home / Users / Flags / Reports / Audit log / Outreach / Trials) just below the AppShell header on every admin path. app.tsx's isAdminPath branch wraps Outlet in <code>&lt;AdminNav /&gt; + &lt;Outlet /&gt;</code>. <strong>(2) Feature flags UI was engineer-shaped</strong>, not operator-shaped. v1.24.0 shipped a row-driven view (showed raw feature_flags rows, asked admin to type a flag_key + scope UUID + JSON metadata on create). Empty state was 'No flags yet' &mdash; useless because admin doesn't know what flags Refill even supports. Per <em>feedback-human-first-design</em>: computers must think like humans, never the reverse. v1.24.1 inverts the model. New <code>src/lib/feature-flag-catalog.ts</code> defines the curated list of features Refill supports gating: <code>preshow_enabled</code>, <code>rescue_enabled</code>, <code>attribution_enabled</code>, <code>deposit_required</code>, <code>outreach_live</code>, <code>demo_mode</code>. Each entry has a label, description, default state, ON/OFF meanings. <code>/app/admin/flags</code> rewritten to render ONE CARD PER CATALOG ENTRY: title + plain-English description + global ON/OFF toggle + per-tenant/user/rep overrides list + 'Add override' button. The override modal asks scope (tenant/user/rep) + picks the target from a DROPDOWN populated by listTenantsForAdmin or listImpersonableUsers &mdash; no raw UUIDs typed. Uncatalogued rows that don't match the catalog are surfaced separately so admin can clean up. <strong>Lesson</strong>: shipping a CRUD UI for an internal table is not the same as shipping an operator UI. The latter starts from 'what does the operator want to accomplish' and the catalog is the answer. Touched: <code>src/components/admin/AdminNav.tsx</code> (new), <code>src/lib/feature-flag-catalog.ts</code> (new), <code>src/routes/app.admin.flags.tsx</code> (full rewrite, catalog-driven), <code>src/routes/app.tsx</code> (isAdminPath wraps in AdminNav), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.24.0",
    date: "May 2026",
    items: [
      "<strong>v1.24.0 &mdash; Unified admin platform: architectural rebuild (outer AppShell) + P4 user/role management + P5 feature flags + audit log viewer + reports. The 'complete admin' from Grasshopper's original ask, shipped as one.</strong> Pre-rebuild, the chrome (sign-out, PersonaSwitcher, version pill) lived INSIDE RefillShell / RepShell, both of which were conditionally mounted by app.tsx based on tenant + role resolution. When the tenant fallback failed (admin impersonating a non-tenant-owner, schema-cache lag, transient server error), the whole chain collapsed and the user was trapped without sign-out. The v1.23.1 hotfix patched that with a FallbackChrome band-aid; v1.24.0 ships the structural fix Grasshopper actually called for &mdash; the Lovable-style outer always-on chrome above shell selection. <strong>New file: <code>src/components/AppShell.tsx</code></strong> &mdash; outer universal infra (sign-out, PersonaSwitcher, version pill + changelog Popover, theme toggle, email). Renders in EVERY app.tsx branch. Subtitle prop ('no-show recovery' / 'rep platform' / 'admin' / undefined) parameterizes the brand text. NEVER depends on tenant/role state. <strong>Refactored: <code>RefillShellChrome</code> + <code>RefillRepShellChrome</code></strong> &mdash; gutted to inner chrome only. RefillShellChrome now renders just DemoBanner + admin viewing-as banner + RefillNav chip strip. RefillRepShellChrome similarly: DemoBanner + RefillRepNav. No more top header strip in either &mdash; that lives in AppShell. <strong>app.tsx</strong> &mdash; every render branch wrapped in <code>&lt;AppShell subtitle={...}&gt;{innerShell}&lt;/AppShell&gt;</code>. The bare-Outlet branch (formerly the trap) now also includes AppShell. /app/admin path gets a dedicated 'admin' subtitle. Universal chrome reachable in every state. <strong>Deleted</strong>: <code>src/components/AppFallbackChrome.tsx</code> — subsumed by AppShell. <strong>Walk-fix: <code>useRepProfile</code> + <code>getMyRepAccount</code> opt into viewAsUserId</strong>. Pre-fix, when admin impersonated Kelly, RepShell mounted via shell dispatch but useRepProfile fetched the caller's (admin's) profile — null — so RepShell's defensive null-return left a blank page. v1.24.0: useRepProfile reads <code>getAdminViewAsUserId()</code> from localStorage and passes through to <code>getMyRepAccount({viewAsUserId})</code>; server-side <code>resolveEffectiveUserId</code> re-verifies admin role before honoring. Cache key includes viewAsUserId so persona switches re-fetch the impersonated user's profile cleanly. <strong>P4 — admin user/role management</strong> at <code>/app/admin/users</code>. New <code>src/server/admin-users.ts</code> with: <code>listUsers</code> (auth.users JOIN user_roles + rep_accounts + tenant_memberships), <code>createUser</code> (auth.admin.createUser + optional initial role grant), <code>sendPasswordReset</code> (Supabase recovery email), <code>grantRole</code> / <code>revokeRole</code> (user_roles upsert/delete), <code>deleteUser</code> (auth.admin.deleteUser + cascades). Every mutation calls <code>writeAuditEvent</code> to admin_audit_log. UI is a table of users with inline role-toggle chips (click to grant/revoke any of admin/spa_owner/rep/sub_rep/developer), reset + delete action buttons per row, and a 'Create user' modal at top. <strong>P5 — feature flags UI</strong> at <code>/app/admin/flags</code>. New <code>src/server/admin-flags.ts</code> with <code>listFlags</code>, <code>upsertFlag</code>, <code>deleteFlag</code>. Reads/writes public.feature_flags (the v1.22.1 P1 table). UI shows the full flag table with per-row enabled toggle, JSON metadata preview, and a 'New flag' modal that picks scope (global/tenant/user/rep) + scope_id (uuid) + initial enabled state + metadata JSON. Each toggle/create/delete writes to audit log. <strong>P5 — audit log viewer</strong> at <code>/app/admin/audit</code>. New <code>src/server/admin-audit.ts</code> with <code>listAuditEvents</code> (paginated, filter by action). UI shows a chronological table: timestamp, actor email, action chip, target email, payload JSON preview. KNOWN_ACTIONS dropdown lets admin filter to one action class. Auth gate: admin-only server-side. <strong>P5 — reports</strong> at <code>/app/admin/reports</code>. New <code>src/server/admin-reports.ts</code> with <code>getTenantReport</code> (per-tenant: name, plan, owner, patient count, recovery event count + $) and <code>getRepReport</code> (per-rep: display name, status, origin, downstream tenants — zeros for now pending v1.25 rep_attributions table). UI is two-tab (Tenants / Reps) with per-row 'View as' button that writes the row's owner_user_id to localStorage and reloads, dropping admin into that tenant or rep's shell instantly. <strong>/app/admin index updated</strong>: ADMIN_SURFACES now lists all 6 surfaces (Users & roles / Feature flags / Reports / Audit log / Outreach templates / Refill trial lever board) so admin gets a single home for everything. <strong>What's NOT in v1.24.0</strong> (queued): polished analytics dashboards with charts and trend lines; per-sub-rep attribution drilldown (needs a rep_attributions table — that's a v1.25 ship); persona-switch start/stop write to admin_audit_log (decision deferred &mdash; can wire in v1.25 if compliance asks). <strong>Lessons codified</strong>: the v1.20→v1.23 sequence kept treating chrome as 'part of the shell' (per-shell brand + per-shell sign-out). When the shell couldn't mount, the user lost sign-out. v1.24.0's separation &mdash; universal infra outermost, shell-specific brand/nav inside &mdash; matches the Lovable template default and makes user-trapping structurally impossible. Hard rule: a signed-in user must always have a sign-out affordance reachable, no matter what state app.tsx is in. Touched: <code>src/components/AppShell.tsx</code> (new), <code>src/components/refill/RefillShellChrome.tsx</code> (gutted to inner-only), <code>src/components/refill/RefillRepShellChrome.tsx</code> (gutted to inner-only), <code>src/components/AppFallbackChrome.tsx</code> (DELETED), <code>src/routes/app.tsx</code> (wraps every branch in AppShell), <code>src/lib/use-rep-profile.ts</code> (viewAsUserId plumbing), <code>src/server/rep-platform.ts</code> (getMyRepAccount viewAsUserId), <code>src/server/admin-users.ts</code> (new), <code>src/server/admin-flags.ts</code> (new), <code>src/server/admin-audit.ts</code> (new), <code>src/server/admin-reports.ts</code> (new), <code>src/routes/app.admin.users.tsx</code> (new), <code>src/routes/app.admin.flags.tsx</code> (new), <code>src/routes/app.admin.audit.tsx</code> (new), <code>src/routes/app.admin.reports.tsx</code> (new), <code>src/routes/app.admin.index.tsx</code> (ADMIN_SURFACES updated), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.23.1",
    date: "May 2026",
    items: [
      "<strong>v1.23.1 &mdash; Hotfix: FallbackChrome ensures sign-out + PersonaSwitcher are ALWAYS reachable, even when the shell selection can't resolve.</strong> Pre-hotfix design flaw, surfaced in real life by Grasshopper minutes after the v1.23.0 + admin-rename migration landed: <code>app.tsx</code> coupled chrome to shell to tenant. <code>RefillShellChrome</code> (which holds sign-out + version pill + the new PersonaSwitcher) only renders inside <code>RefillShell</code>; <code>RefillShell</code> only mounts when <code>tenantMembership.tenant</code> resolves to a non-null value; the admin-fallback path resolves to null in several legitimate cases (admin impersonating another admin or developer with no tenant_memberships row, transient server error, schema-cache lag immediately after a migration). When that chain collapsed, the user landed on the bare-Outlet branch &mdash; raw page content with no header &mdash; <strong>trapped without a sign-out button</strong>. Recovery required clearing localStorage from DevTools, which a real user couldn't reach. <strong>Fix</strong>: new <code>src/components/AppFallbackChrome.tsx</code> renders a minimal recovery chrome (brand mark + PersonaSwitcher + email + sign-out icon). <code>app.tsx</code>'s bare-Outlet branch now wraps the Outlet in <code>AppFallbackChrome</code>. Universal infra (sign-out / persona-switcher) is now reachable in every shell-selection state, not just when RefillShell or RepShell mounts. <strong>Hard rule going forward</strong>: a signed-in user must ALWAYS have a sign-out affordance reachable, no matter what shell-state app.tsx is in. <strong>Deeper architectural note</strong>: the right long-term fix (v1.24+) is restructuring so universal infra lives in an outer always-on frame ABOVE the shell-selection layer; only brand + nav chips are shell-specific. Lovable templates ship this way by default &mdash; chrome is the outermost layer, content failures never take it down. Refill inherited the per-shell-chrome pattern from openagenticv4 and carried it forward without questioning. The trojan-horse thesis (Refill brand stays narrow) only requires brand + chip nav to be shell-scoped; sign-out + persona-switcher + version pill are universal infrastructure that belongs higher up. <strong>FallbackChrome is the band-aid</strong>; the structural separation lands in v1.24. Touched: <code>src/components/AppFallbackChrome.tsx</code> (new), <code>src/routes/app.tsx</code> (bare-Outlet branch wraps in AppFallbackChrome), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.23.0",
    date: "May 2026",
    items: [
      "<strong>v1.23.0 &mdash; P3 of unified admin platform: cross-shell persona switcher + 12-fn long-tail viewAsUserId sweep + admin identity rename.</strong> The marquee P3 ship from <code>~/.claude/plans/frolicking-stargazing-church.md</code>. P1 laid the schema (v1.22.1), P2 cut consumers over to the unified helper (v1.22.2); v1.23.0 ships the visible feature: admin signs in once and switches view across every shell + every surface without a logout cycle, with the impersonation context flowing through 20 viewAsUserId-aware server fns (8 from v1.20 + 12 new in this ship). <strong>New file: <code>src/lib/admin-view-as.ts</code></strong> &mdash; localStorage helpers keyed by user_id (<code>refill-admin-view-as-user-id</code>). Generalizes the v1.20.6 tenant-id-keyed switcher to a user-id-keyed one, which is the canonical impersonation primitive going forward. setAdminViewAsUserId(userId) writes the key + clears the legacy tenant-id key + triggers <code>window.location.reload()</code> so every module-level cache (useEffectiveRoles, useTenantMembership, useRepProfile, all page-side effects) re-resolves with the new context. Reload-on-change is the same simple-and-correct pattern v1.20.6 used. <strong>New server fn: <code>listImpersonableUsers</code> in <code>src/server/role-helpers.ts</code></strong> &mdash; admin-gated, service-role read. Returns every user with a non-'member' role row joined with auth.users (email), rep_accounts (display name), tenant_memberships+tenants (tenant name). Sorted admin (caller first) &rarr; spa_owners &rarr; reps. Caller's own row included so the &lsquo;My roles (Admin)&rsquo; revert option is always reachable. <strong>Extended: <code>getFirstTenantForAdmin</code></strong> &mdash; new optional <code>viewAsUserId</code> parameter. When set, returns the tenant owned by that user_id (preferring role=owner row), or <code>{ tenant: null }</code> if the user isn't a tenant owner. The null path is the key cross-shell mechanism: useTenantMembership treats it as NOT_A_TENANT &rArr; RefillShell doesn't render &rArr; app.tsx routes to RepShell or AdminShell based on shell from useEffectiveRoles(viewAsUserId). The legacy <code>preferredTenantId</code> parameter stays for back-compat with stale pre-v1.23.0 localStorage; viewAsUserId takes precedence when both are set. <strong>New component: <code>src/components/admin/PersonaSwitcher.tsx</code></strong> &mdash; native &lt;select&gt; in the chrome's upper-right strip, grouped by role (My roles / Spa owners / Reps), compact h-8 to fit alongside the theme toggle / username / sign-out cluster. Renders ONLY when caller (session user, NOT impersonated) is admin &mdash; gated via <code>useEffectiveRoles()</code> without viewAsUserId. Visual state: when impersonating, the select gets an amber-tinted background + border to match the v1.20.6 banner palette as a persistent persona indicator. <strong>Wired into both shell chromes</strong>: <code>RefillShellChrome</code> (spa-owner side + admin's default view) and <code>RefillRepShellChrome</code> (rep side). Admin can switch personas from any shell. <strong>app.tsx cross-shell dispatch</strong> &mdash; reads <code>getAdminViewAsUserId()</code> from localStorage and passes it to <code>useEffectiveRoles(viewAsUserId)</code>. shell resolves to the IMPERSONATED user's shell, not the caller's. Branch on shell to route &mdash; admin impersonating Karen &rarr; shell=\"spa-owner\" &rarr; RefillShell; admin impersonating Kelly &rarr; shell=\"rep\" &rarr; RepShell; admin viewing own &rarr; shell=\"admin\" &rarr; AdminShell (via RefillShell admin-fallback). <strong>useTenantMembership bridging</strong> &mdash; reads the user-id key (preferring it over the legacy tenant-id key when both are set), passes to getFirstTenantForAdmin so spa-owner impersonation lights up the right tenant. Non-spa-owner impersonation (rep) returns null tenant &rArr; NOT_A_TENANT &rArr; RefillShell skipped, RepShell takes over via shell dispatch. <strong>v1.20.6 inline tenant-select REMOVED</strong> from RefillShellChrome banner. The amber &ldquo;Viewing as Rejuv (admin fallback)&rdquo; banner now renders a STATIC tenant name + read/write-scoping callout. The persona-switching mechanic lives in the upper-right header where it can serve all three shells, not just RefillShell. <code>setAdminViewAsTenantId</code> + <code>ADMIN_VIEW_AS_TENANT_ID_KEY</code> stay exported as deprecated for back-compat; remove in v1.24 with the user_preferences.primary_role column. <strong>Long-tail server fn sweep (12 fns)</strong> &mdash; each gets a <code>viewAsUserId: z.string().uuid().optional()</code> input field + a <code>verifyAuth</code> &rarr; <code>resolveEffectiveUserId</code> swap + a <code>userId</code> &rarr; <code>effectiveUserId</code> substitution. Closes the long-tail gap from Audit 2 where admin impersonating Karen would see Karen's patients (v1.20 read fns) but empty rescue activity / pattern alerts / contact-overview / recent reminders / reports / inbox. List: <code>summarizeOverdueCohort</code>, <code>ingestClientListCsv</code>, <code>listContactsOverview</code>, <code>listContactGaps</code> (patient-ingest); <code>listPatientInbox</code>, <code>getInboxThread</code> (emma-inbox); <code>listRescueActivity</code> (emma-rescue); <code>listPatternAlerts</code> (emma-reliability); <code>listRecommendations</code> (emma-intelligence); <code>listRecentReminders</code> (emma-preshow); <code>getEmmaReports</code> (emma-reports); <code>listCsvDialectMappings</code> (emma-csv-mapper). <strong>Page-side plumbing</strong> for the 6 routes that consume them: <code>app.refill.index.tsx</code>, <code>app.refill.patients.contacts.tsx</code>, <code>app.refill.inbox.tsx</code>, <code>app.refill.rescue.tsx</code>, <code>app.refill.appointments.tsx</code>, <code>app.refill.reports.tsx</code>. Each derives viewAsUserId from useTenantMembership state and threads it through every relevant call; effects re-run when the value changes. <strong>/dev/$persona route DELETED</strong>. The 133-line file was replaced by the PersonaSwitcher dropdown. Admin testing flow goes from &ldquo;sign out + sign in to a hardcoded password persona&rdquo; to &ldquo;pick from the upper-right dropdown.&rdquo; <strong>Admin identity rename migration prepped</strong>: <code>supabase/migrations/20260620000000_v123_admin_rename.sql</code> renames the existing <code>admin@refill-demo.test</code> auth.users row (user_id <code>addf1110-...</code> from v417_admin_testing_identity) to <code>admin@refill.platform</code> via a single UPDATE that preserves the UUID, so all FK references in user_roles + admin_audit_log + future P4/P5 surfaces stay valid. Optional revoke-davidfand303-admin statement included but commented out by default. Idempotent + paste-via-Supabase-Dashboard per <em>feedback-migrations-via-dashboard</em>. <code>v417-persona-mint.ts</code>'s PERSONA_EMAILS table updated forward-compatibly to point at the new email; once Grasshopper pastes the rename migration, the /personas admin button continues to authenticate cleanly. Brief minutes-long window between deploy + paste where /personas&rsquo; admin button fails &mdash; acceptable; admin signs in directly via /login. <strong>What this UNBLOCKS</strong>: P4 (admin user/role mgmt UI on /app/admin/users) and P5 (feature flags + reports + analytics) are mechanical adds on top of the substrate now fully in place. The persona switcher mechanic + 20-fn viewAsUserId coverage means admin can walk every surface as any user without logging out. <strong>Risk surface</strong>: useRepProfile + RepShell internal data fetchers (rep-platform.ts) NOT in this sweep &mdash; when admin impersonates Kelly, RepShell chrome renders correctly (via shell dispatch) but inner content fetched via getMyRepAccount / rep-platform reads still scope to caller's user_id. Tracked for v1.23.1 as a focused follow-up. Touched: <code>src/lib/admin-view-as.ts</code> (new), <code>src/components/admin/PersonaSwitcher.tsx</code> (new), <code>src/server/role-helpers.ts</code> (listImpersonableUsers + types added), <code>src/server/refill-tenants.ts</code> (getFirstTenantForAdmin extended), <code>src/components/refill/RefillShellChrome.tsx</code> (PersonaSwitcher wired in + v1.20.6 inline select removed), <code>src/components/refill/RefillRepShellChrome.tsx</code> (PersonaSwitcher wired in), <code>src/routes/app.tsx</code> (viewAsUserId passed to useEffectiveRoles), <code>src/lib/use-tenant-membership.ts</code> (user-id-keyed admin-fallback), <code>src/server/patient-ingest.functions.ts</code> (4 fns swept), <code>src/server/emma-inbox.functions.ts</code> (2 fns swept), <code>src/server/emma-rescue.functions.ts</code> (1 fn swept), <code>src/server/emma-reliability.functions.ts</code> (1 fn swept), <code>src/server/emma-intelligence.functions.ts</code> (1 fn swept), <code>src/server/emma-preshow.functions.ts</code> (1 fn swept), <code>src/server/emma-reports.functions.ts</code> (1 fn swept), <code>src/server/emma-csv-mapper.functions.ts</code> (1 fn swept), <code>src/routes/app.refill.index.tsx</code> + <code>app.refill.patients.contacts.tsx</code> + <code>app.refill.inbox.tsx</code> + <code>app.refill.rescue.tsx</code> + <code>app.refill.appointments.tsx</code> + <code>app.refill.reports.tsx</code> (page plumbing), <code>src/routes/app.admin.index.tsx</code> (docblock refresh), <code>src/routes/dev.$persona.tsx</code> (DELETED), <code>src/server/v417-persona-mint.ts</code> (PERSONA_EMAILS admin update), <code>supabase/migrations/20260620000000_v123_admin_rename.sql</code> (new), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.22.2",
    date: "May 2026",
    items: [
      "<strong>v1.22.2 &mdash; P2 of unified admin platform: consumer cutover from primary_role sibling-concept reads to the unified <code>useEffectiveRoles().shell</code>. Kills the v1.20.4-family dead gate in <code>post-login-redirect.ts</code>. Zero user-visible behavior change for current personas; admin sign-in now correctly routes to /app/admin instead of falling through the dead branch to the spa-owner default.</strong> Per plan in <code>~/.claude/plans/frolicking-stargazing-church.md</code>. Background: v1.22.1 (P1) laid the schema substrate &mdash; <code>app_role</code> enum expanded, <code>user_roles</code> backfilled, <code>getEffectiveRoles</code> + <code>useEffectiveRoles</code> + <code>writeAuditEvent</code> shipped but no consumers yet. v1.22.2 is the mechanical cutover that swaps the three primary_role readers onto the new helper. <strong>app.tsx shell dispatcher</strong> &mdash; replaced the local <code>useState&lt;PrimaryRole&gt;</code> + <code>useEffect</code> that fetched <code>getMyPrimaryRole</code> with a single <code>useEffectiveRoles()</code> hook call. Derives <code>shell</code> as <code>rolesState.status === \"resolved\" ? rolesState.roles.shell : null</code>. Branch comparisons swap from <code>primaryRole === \"rep\"</code> &rarr; <code>shell === \"rep\"</code>, and from the dead <code>primaryRole === \"admin\"</code> + adjacent <code>primaryRole === \"developer\"</code> to a single live <code>shell === \"admin\" || shell === \"developer\"</code> (both route to /app/admin per pre-existing behavior). Same observable dispatch in steady state &mdash; admins still bounce to /app/admin, reps to /app/rep, tenanted spa-owners to /app/refill. Difference: the admin branch is now genuinely reachable. Removed the unused <code>supabase</code> client import (dead since the original cleave) and the local <code>PrimaryRole</code> type shadow. Updated docblock to point at the new dispatch lineage. <strong>login.tsx post-sign-in dispatch</strong> &mdash; replaced the <code>getMyPrimaryRole</code> call with <code>getEffectiveRoles</code> against the same access token; passes <code>roles?.shell ?? null</code> to <code>postLoginTarget</code>. Practical effect: an admin signing in at <code>/login</code> now correctly lands on <code>/app/admin</code> via the live branch instead of falling through to the spa-owner default <code>/app/refill</code> (which would then bounce again via app.tsx's effect anyway, but cleaner to do it right at the source). <strong>post-login-redirect.ts</strong> &mdash; signature changed from <code>postLoginTarget(primaryRole: PrimaryRole | null)</code> to <code>postLoginTarget(shell: Shell | null)</code>. Collapsed the two admin-routed branches (admin + developer) into one. Updated docblock to call out the v1.20.4 family fix: pre-P2 input came from <code>user_preferences.primary_role</code> whose enum never included \"admin\", so the <code>=== \"admin\"</code> branch was dead. Post-P2 input is the unified Shell derived from <code>user_roles</code>, so \"admin\" is a valid value and the branch goes live. Dropped the now-unused <code>PrimaryRole</code> type import in favor of <code>Shell</code> from <code>@/server/role-helpers</code>. <strong>useIsAdmin.ts</strong> &mdash; rewrote from a 50-line direct supabase query against <code>user_roles</code> (with its own module-level cache, its own useEffect fetch, its own cancel-on-unmount handling) to a 4-line thin wrapper over <code>useEffectiveRoles().isAdmin</code>. Single source of truth; eliminates the cache-divergence footgun where the hook's uid-keyed cache could have desynced from a future persona-switch session. The cache benefit is preserved because <code>useEffectiveRoles</code> ships its own module-level promise cache keyed by <code>${accessToken}|${viewAsUserId ?? \"\"}</code> &mdash; same coalescing across components, but session-aware in a way the old hook wasn't. <strong>auth.tsx</strong> &mdash; removed <code>primaryRole</code> from <code>AuthContextValue</code> interface, from <code>useState</code>, from the <code>useEffect</code> that fetched <code>getMyPrimaryRole</code> on every session change, and from the <code>AuthContext.Provider</code> value. Grep confirmed zero remaining live consumers of <code>useAuth().primaryRole</code> across the codebase after the app.tsx + login.tsx cutovers. Removing it saves one round-trip to <code>getMyPrimaryRole</code> per session land + every session refresh, and shrinks AuthContext to its core (user / session / loading / signOut). The <code>user_preferences.primary_role</code> column itself stays on the DB for one release as fallback per D3 lock from the unified admin plan; drops in v1.24. <strong>use-tenant-membership.ts</strong> &mdash; docblock-only update. The admin-fallback path's behavior is unchanged (still gated purely server-side via <code>getFirstTenantForAdmin</code>'s <code>user_roles</code> re-verification). The historical narrative explaining the v1.20.4 dead-gate fix is preserved + extended to note v1.22.2's removal of <code>useAuth().primaryRole</code> entirely, so a future reader sees the full lineage. <strong>What's still in place after P2</strong>: <code>getMyPrimaryRole</code> server fn remains (no callers from this codebase, but might be reachable from external scripts or future audit); the underlying <code>user_preferences.primary_role</code> column remains (D3 lock: drop in v1.24); the v417_admin_testing_identity migration's <code>primary_role='developer'</code> stamp on <code>admin@refill-demo.test</code> is harmless and gets unified into the same admin shell via the user_roles backfill (admin@refill-demo.test now has <code>{admin, developer}</code> per P1's verify SELECT; shell derives \"admin\" because admin takes priority in <code>deriveShell</code>). <strong>What this UNBLOCKS</strong>: P3 (v1.23.0 persona switcher) is now a clean drop-in. The switcher writes a target user_id to localStorage and reloads; <code>useEffectiveRoles</code> already accepts an optional <code>viewAsUserId</code> param that flows through <code>resolveEffectiveUserId</code>'s server-side admin-role re-verification; <code>app.tsx</code> reads <code>shell</code> from the same hook so cross-shell impersonation \"just works\" once the dispatcher sources its viewAsUserId from localStorage instead of (implicitly) the caller. The 13-fn long-tail sweep of remaining read fns into viewAsUserId is mechanical. <strong>Risk surface</strong>: cache invalidation across mid-session role mutations. If an admin grants a user a new role via the future P4 UI, that user's <code>useEffectiveRoles</code> cache is keyed by their access token + viewAsUserId &mdash; the cache lives until the token rotates. Acceptable for v1; if it bites, P5's mutation paths can dispatch a custom event the hook listens for. Touched: <code>src/routes/app.tsx</code> (shell dispatcher cutover), <code>src/routes/login.tsx</code> (post-sign-in dispatch cutover), <code>src/lib/post-login-redirect.ts</code> (signature + dead-gate fix), <code>src/hooks/useIsAdmin.ts</code> (thin wrapper rewrite), <code>src/lib/auth.tsx</code> (primaryRole removal), <code>src/lib/use-tenant-membership.ts</code> (docblock-only), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.22.1",
    date: "May 2026",
    items: [
      "<strong>v1.22.1 &mdash; P1 of the unified admin platform sequence: schema substrate + unified role helper + audit log table. Zero user-visible behavior change.</strong> Per <code>~/.claude/plans/frolicking-stargazing-church.md</code> + the <code>Refill-Unified-Admin-Discovery.html</code> on Desktop. Background: the identity/roles/admin layer had accreted into five sibling concepts (<code>auth.users</code>, <code>user_roles</code>, <code>user_preferences.primary_role</code>, <code>tenant_memberships</code>, <code>rep_accounts</code>) with no central source of truth, plus four partial impersonation mechanisms. The v1.20.4 30-min blank-page outage was a direct symptom &mdash; <code>primaryRole === \"admin\"</code> was an unreachable gate because admin is not a valid <code>primary_role</code> value. v1.22.1 lays the schema substrate so a clean cutover in v1.22.2 (P2) and the v1.23.0 persona switcher (P3) become mechanical drop-ins. <strong>Two migration files</strong> &mdash; SPLIT INTENTIONALLY because Postgres prohibits using a newly-added enum value in the same transaction as the <code>ALTER TYPE</code> that creates it, and the Supabase dashboard SQL editor wraps multi-statement pastes in an implicit transaction. Paste these in order: <code>20260619000000_unified_admin_p1a_app_role_expansion.sql</code> (4 ALTER TYPE statements adding <code>spa_owner</code>, <code>rep</code>, <code>sub_rep</code>, <code>developer</code> to the existing <code>public.app_role</code> enum &mdash; previously just <code>admin</code> / <code>member</code>); then <code>20260619010000_unified_admin_p1b_backfill_and_tables.sql</code> (everything else). <strong>Backfill semantics</strong> in P1b: every row in <code>public.tenant_memberships</code> becomes a <code>spa_owner</code> user_roles row (over-grants are revocable from the future P4 admin UI; <code>tenant_memberships.role</code> is open text with no CHECK so we treat every membership as spa-owner status). Every <code>active</code> row in <code>public.rep_accounts</code> becomes a <code>rep</code> user_roles row (paused / terminated reps intentionally NOT granted). Every row in <code>public.user_preferences</code> with <code>primary_role = 'developer'</code> becomes a <code>developer</code> user_roles row (today: just admin@refill-demo.test per v417_admin_testing_identity). All inserts use <code>ON CONFLICT (user_id, role) DO NOTHING</code> &mdash; migration is fully idempotent and safe to re-run. <strong>Two new tables</strong>: <code>public.feature_flags</code> (id, flag_key, scope = 'global'|'tenant'|'user'|'rep', scope_id, enabled, metadata, updated_at, updated_by) &mdash; the substrate for the P5 admin feature-flag UI; D8 lock means spa-owner Advanced UI and admin feature-flag UI manipulate the SAME rows (spa-owner writes scope=tenant; admin writes scope=global or any). RLS: <code>feature_flags_admin_all</code> via <code>has_role()</code> + <code>feature_flags_service_role</code> escape hatch. <code>public.admin_audit_log</code> (id, actor_user_id, acted_at, action, target_user_id, target_tenant_id, payload jsonb) &mdash; append-only audit record. Records role grants/revokes (P4), feature flag toggles (P5), optional persona-switch start/stop (P3 if compliance demands). RLS: admin-read + service-role full-access. Indexes on acted_at desc, actor, target_user. P1b also writes its own audit-log entry tagged <code>p1.role.backfill</code> with the migration filename in payload &mdash; first audit row in the system, for traceability. <strong>Server helper</strong>: new <code>src/server/role-helpers.ts</code> exports <code>getEffectiveRoles</code> server fn + <code>EffectiveRoles</code>, <code>PlatformRole</code>, <code>Shell</code> types. Reads <code>user_roles</code> rows for the resolved effective user_id (calls <code>resolveEffectiveUserId</code> from auth-helpers first, so admin viewing-as Karen reads Karen's roles, not admin's). Returns <code>{ userId, callerUserId, isViewingAs, isAdmin, isSpaOwner, isRep, isSubRep, isDeveloper, shell, rawRoles }</code>. The <code>shell</code> field derives in priority order admin &gt; rep &gt; spa-owner &gt; developer &mdash; mirrors the existing app.tsx dispatcher exactly so P2 cutover is a one-line consumer swap with no string-key churn. <strong>Client hook</strong>: new <code>src/hooks/useEffectiveRoles.ts</code> wraps the server fn with the same useAuth-driven module-cache pattern as <code>use-tenant-membership.ts</code>. State union: loading / anonymous / resolved / error. Takes optional viewAsUserId so P3 can generalize the v1.20.6 tenant switcher from tenant-id to user-id. P1 ships the hook but no consumers call it yet &mdash; available for P2 cutover. <strong>Audit log writer</strong>: new <code>src/server/audit-log.ts</code> exports <code>writeAuditEvent({ actorUserId, action, targetUserId?, targetTenantId?, payload? })</code>. Service-role insert into admin_audit_log. AuditAction union pre-declares the P1-P5 action taxonomy (role.grant / impersonate.start / flag.toggle / etc.) so typos surface as type errors. Used by SQL in P1b's self-log entry; reserved for P4+ consumers. <strong>What's intentionally NOT in v1.22.1</strong>: no consumer cutover (P2 = v1.22.2), no persona switcher UI (P3 = v1.23.0), no admin user/role management UI (P4), no feature-flag CRUD UI or analytics dashboards (P5), no removal of <code>user_preferences.primary_role</code> column (deferred until P2 verifies zero readers), no removal of <code>/dev/$persona</code> route (P3 deprecates), no removal of v1.20.6 inline tenant switcher (P3 collapses into persona switcher). <strong>Why expand existing user_roles vs new user_platform_roles table</strong>: the discovery doc strawman proposed a new table; the plan refined to reform-in-place (D3 lock). Expanding the enum is half the migration churn for the same end state, and every existing consumer of <code>has_role()</code> + the v1.20 <code>resolveEffectiveUserId</code> admin-role check keeps working verbatim. <strong>Risk surface</strong>: the <code>ALTER TYPE ADD VALUE</code> transaction visibility constraint is the one known failure mode &mdash; mitigated by the two-file split. <code>tenant_memberships.role</code> open text could over-grant spa_owner if a non-owner row exists today (none observed in production but acceptable noise for v1; revocable from P4 admin UI). <strong>Forward sequence locked</strong>: P2 (v1.22.2) = consumer cutover of <code>useAuth().primaryRole</code> + <code>useIsAdmin()</code> + <code>useTenantMembership</code> admin-fallback to <code>useEffectiveRoles().shell</code> / <code>.isAdmin</code>; also kills the dead <code>primaryRole === \"admin\"</code> check in <code>src/lib/post-login-redirect.ts:33</code> (same family as the v1.20.4 bug). P3 (v1.23.0) = persona switcher dropdown upper-right header, generalize v1.20.6 localStorage from tenant-id to user-id, sweep 13 long-tail reads into viewAsUserId, deprecate <code>/dev/$persona</code>, rename <code>admin@refill-demo.test</code> &rarr; <code>admin@refill.platform</code> via Supabase Dashboard, revoke admin role from <code>davidfand303@gmail.com</code>. P4 = <code>/app/admin/users</code> CRUD UI. P5 = <code>/app/admin/flags</code> + reporting + analytics + audit log viewer. Touched: <code>supabase/migrations/20260619000000_unified_admin_p1a_app_role_expansion.sql</code> (new), <code>supabase/migrations/20260619010000_unified_admin_p1b_backfill_and_tables.sql</code> (new), <code>src/server/role-helpers.ts</code> (new), <code>src/hooks/useEffectiveRoles.ts</code> (new), <code>src/server/audit-log.ts</code> (new), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.22",
    date: "May 2026",
    items: [
      "<strong>v1.22 &mdash; Email change reverification flow on /app/refill/settings/account.</strong> v1.18 shipped the password-change form but left email as read-only with a &lsquo;Contact support to change&rsquo; copy because email change requires reverification (Supabase sends a confirmation link to the new address; change isn&rsquo;t applied until clicked). v1.22 wires the full reverification flow. <strong>Email row now editable</strong>: &lsquo;Change email&rsquo; link reveals an inline form with new-email input + Send confirmation button. On submit, calls <code>supabase.auth.updateUser({ email: newEmail })</code> &mdash; Supabase sends a confirmation link to the new address (and, depending on dashboard &lsquo;secure email change&rsquo; setting, also to the OLD address). Email is NOT changed until the user clicks the link. <strong>Pending-state banner</strong>: on every page mount, we read <code>supabase.auth.getUser().data.user.new_email</code> &mdash; populated when a change is pending, undefined otherwise. When present, render an amber banner above the edit button: &lsquo;Change pending: &lt;new_email&gt;. Check your inbox at &lt;new_email&gt; and click the confirmation link to finish the change.&rsquo; Banner survives navigation, page reload, browser close+reopen &mdash; the state lives in auth.users, not React. <strong>Cancel pending change</strong>: X button on the banner. Supabase doesn&rsquo;t expose a direct cancel API, so we re-call <code>updateUser({ email: currentEmail })</code> &mdash; that re-overrides new_email to the current email, which Supabase nulls on confirmation send (effective no-op cancellation). Pending state expires server-side after a configured TTL anyway (default 24h), so failed cancel is non-fatal. UI optimistically clears the banner regardless. <strong>PageHeader description refreshed</strong>: &lsquo;Email change requires reverification flow (coming soon)&rsquo; &rarr; &lsquo;Change your email (with confirmation) or password anytime.&rsquo; <strong>Why no current-password prompt</strong>: same reasoning as v1.18&rsquo;s password change &mdash; the active JWT IS the auth gate; requiring a current-password field would be theater. The double-confirmation requirement (Supabase emails both old + new) is the actual security boundary. <strong>What this finishes</strong>: the full account-lifecycle surface is now in-app &mdash; reset password (forgot-password flow via /reset-password since v1.18), change password (signed-in, no email), change email (signed-in, with reverification). No support tickets needed for any of these. Touched: <code>src/routes/app.refill.settings.account.tsx</code> (email state + form + pending banner + cancel handler + updated docblock), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.21",
    date: "May 2026",
    items: [
      "<strong>v1.21 &mdash; /claim-your-business ported from openagenticv4 with Refill-flavored cleave.</strong> Final P1 item from the <code>Agentiport-Port-Audit.html</code> deliverable. The marketing-landing CTA at <code>src/routes/index.tsx:248</code> has been linking to <code>/claim-your-business</code> for weeks, dead-routing 404. v1.21 lands the page. <strong>Strategic call during port</strong>: the source file (979 lines) was Emma-shaped &mdash; Agentiport header brand, Sparkles logo, &lsquo;Emma handles texts&rsquo; ValueProps, &lsquo;Emma is on standby&rsquo; footer, &lsquo;Hosted Messaging activation&rsquo; concept throughout, <code>/app/karen</code> destination link. Verbatim copy would have shipped 4-5 Emma user-facing violations + a 404 destination link. Instead, cleaved to Refill thesis: scrape&rarr;review&rarr;claim arc preserved (server fns + DB substrate unchanged), but every Emma reference translated to Refill voice anchored to the no-show-recovery loop. <strong>Header</strong>: Agentiport+Sparkles &rarr; Refill+&lsquo;R&rsquo; logo block matching <code>RefillShellChrome</code>, &lsquo;/ no-show recovery&rsquo; subtitle. <strong>H1</strong>: &lsquo;Meet your AI front desk&rsquo; &rarr; &lsquo;Turn your cancellations into recovered revenue.&rsquo; <strong>ValueProps (3 cards)</strong>: completely rewritten &mdash; Calendar/&lsquo;Refill watches your calendar&rsquo;, ShieldCheck/&lsquo;You only pay on what comes back&rsquo;, Sparkles/&lsquo;Your waitlist becomes your safety net&rsquo;. Anchors the pricing-killshot framing (free + % of recovered) directly on the landing surface. <strong>Stage 4 Done copy</strong>: &lsquo;Emma has your services loaded into her sandbox&rsquo; &rarr; &lsquo;Your services, hours, and policies are loaded into your Refill tenant. Recovery starts the moment you wire your scheduler and waitlist.&rsquo; &lsquo;What&rsquo;s next&rsquo; list rewritten: (1) wire scheduler (Acuity, Boulevard, Mindbody), (2) import patient list, (3) watch live recovery feed &mdash; matches the actual Refill onboarding path, not Emma&rsquo;s Hosted Messaging coordination story. <strong>Destination link</strong>: <code>/app/karen</code> &rarr; <code>/app/refill</code>. <strong>Footer</strong>: Agentiport+Emma copy &rarr; &lsquo;Refill · No-show recovery for spas.&rsquo; <strong>Stripped from imports</strong>: Briefcase (unused), MessageCircle (was for &lsquo;Emma handles texts&rsquo; ValueProp, no longer applicable), Plus (unused after rewrite), X (unused after rewrite). <strong>Styling switch</strong>: every Tailwind brand-token class converted to inline <code>style={{ ... }}</code> with hex literals matching <code>styles.css</code> @theme &mdash; defensive against any remaining silently-broken-token edge cases the v1.20.7 audit might have missed (same pattern <code>LiveRecoveryFeed</code> uses). <strong>Auto-save + resume-via-?session=&lt;id&gt; behavior preserved</strong>: 1500ms debounced <code>updateSpaScrape</code>, first-render skip, &lsquo;Saving…&rsquo;/&lsquo;Saved.&rsquo; indicator, full UUID hydration on mount, polling tick for in-flight scrapes. <strong>Components ported</strong>: <code>src/components/PasswordInput.tsx</code> (47 lines, mechanical copy) &mdash; was a transitive dep needed by the sign-up form. <strong>Server-fn deps already in refill-app</strong>: <code>startSpaScrape</code>, <code>getSpaClaimStatus</code>, <code>claimSpa</code>, <code>updateSpaScrape</code> in <code>src/server/spa-claim.functions.ts</code> (1500+ lines, ported earlier). <strong>ServicesEditCard component already ported</strong>. <strong>What this unblocks</strong>: the dead index.tsx CTA now lands somewhere real; an actual spa owner can paste their URL, see what we know, and claim &mdash; converging on the dashboard at <code>/app/refill</code> without ever seeing Agentiport/Emma branding. Funnel-critical surface restored. Touched: <code>src/routes/claim-your-business.tsx</code> (new, ~700 lines), <code>src/components/PasswordInput.tsx</code> (new), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.20.7",
    date: "May 2026",
    items: [
      "<strong>v1.20.7 &mdash; Lane 2 @theme audit: shadcn semantic tokens defined, retroactively unblocking ~700 silently-broken refs across 35 files.</strong> Audit ran the grep against every Tailwind utility class consuming a color name vs. what&rsquo;s actually defined in <code>@theme</code>. Findings: <strong>(a)</strong> primary tokens fully resolved (v1.11&rsquo;s 31-file sweep + v1.20 polish kept it clean — only 4 remaining hits, all inert strings inside changelog HTML or comments); <strong>(b)</strong> <em>massive</em> shadcn-semantic-token gap &mdash; ~700 references to <code>bg-card</code> (102), <code>bg-background</code> (82), <code>text-foreground</code> (160), <code>text-muted-foreground</code> (65), <code>bg-muted</code> + opacity variants (~140), <code>text-destructive</code> (46), <code>divide-border</code> (21), <code>ring-ring</code> (19), <code>border-input</code> (15), <code>bg-destructive/N</code> variants (~36), <code>bg-accent</code> (3), <code>bg-popover</code>, <code>bg-sidebar-accent</code>, <code>text-accent-foreground</code>, <code>text-popover-foreground</code>, etc. ALL emitting zero CSS in Tailwind v4 because none of <code>--color-card</code>, <code>--color-muted</code>, <code>--color-background</code>, <code>--color-foreground</code>, <code>--color-destructive</code>, <code>--color-border</code>, <code>--color-input</code>, <code>--color-ring</code>, <code>--color-accent</code>, <code>--color-popover</code>, <code>--color-sidebar-accent</code> exist in our <code>@theme</code> block. Same class of silently-broken-token bug as the v1.10 primary find + v1.17 rule find &mdash; just at much larger scale. <strong>Three approaches considered</strong>: (A) mechanical sed sweep across 35 files (high blast radius, 700+ replacements, mis-sed risk), (B) sweep only load-bearing routes (leaves long-tail broken), (C) <em>add the missing shadcn semantic role tokens to @theme so they actually render</em> &mdash; same single-styles.css-change pattern v1.17 used to retroactively unblock all rule-* refs. <strong>Picked C</strong> &mdash; it&rsquo;s the same architectural pattern, zero risk of consumer-site mis-sed, single point of truth for what these roles mean visually for the Refill brand. <strong>Implementation</strong>: 16 lines added to <code>src/styles.css</code>&rsquo;s <code>@theme</code> block mapping shadcn semantic roles to Refill brand colors: <code>background</code>&rarr;paper, <code>foreground</code>&rarr;ink, <code>card</code>&rarr;<code>#ffffff</code> (pure white, brighter than warm paper, distinguishes floating surfaces), <code>card-foreground</code>&rarr;ink, <code>popover</code>&rarr;<code>#ffffff</code>, <code>popover-foreground</code>&rarr;ink, <code>muted</code>&rarr;rule-soft (warm off-white for subtle backgrounds), <code>muted-foreground</code>&rarr;ink-soft, <code>accent</code>&rarr;emerald-soft (brand-tinted hover state), <code>accent-foreground</code>&rarr;emerald, <code>destructive</code>&rarr;rose, <code>destructive-foreground</code>&rarr;paper, <code>border</code>&rarr;rule, <code>input</code>&rarr;rule, <code>ring</code>&rarr;emerald (focus matches brand), <code>sidebar-accent</code>&rarr;rule-soft, <code>sidebar-foreground</code>&rarr;ink. <strong>Why this DOESN&rsquo;T conflict with the v1.10 &lsquo;don&rsquo;t add --color-primary&rsquo; decision</strong>: primary was special &mdash; emerald IS the brand primary, so adding the indirection back would have been redundant. card/muted/foreground/destructive/etc. are different &mdash; they&rsquo;re semantic roles (&ldquo;floating surface&rdquo;, &ldquo;less-prominent text&rdquo;, &ldquo;warning state&rdquo;) that need an explicit color mapping per-theme. Defining them just establishes what those roles look like in the Refill brand. <strong>Existing translations stay intact</strong>: every concrete brand-token reference shipped in v1.10/v1.11/v1.14/v1.16/v1.17/v1.20.x (<code>bg-emerald</code>, <code>text-ink</code>, <code>border-rule</code>, etc.) still resolves to the same color &mdash; v1.20.7 only adds NEW token definitions, doesn&rsquo;t modify any existing ones. Visual change is strictly &lsquo;invisible &rarr; visible&rsquo; on previously-broken sites. <strong>Single 16-line styles.css change</strong>: 35-file blast radius eliminated. Touched: <code>src/styles.css</code> (shadcn semantic layer added), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.20.6",
    date: "May 2026",
    items: [
      "<strong>v1.20.6 &mdash; Admin tenant switcher in the viewing-as banner; multi-real-tenant future-proofing.</strong> Pre-v1.20.6, <code>getFirstTenantForAdmin</code> auto-picked the first-by-ordering tenant (real-before-demo per v1.20.3) and there was no way for admin to view a different tenant short of editing localStorage by hand or running SQL. Fine for the current single-real-tenant world (just Rejuv) but breaks the moment we onboard a second pilot. v1.20.6 ships the switcher. <strong>New server fn</strong>: <code>listTenantsForAdmin</code> in <code>src/server/refill-tenants.ts</code> — same admin role re-verification as getFirstTenantForAdmin, returns every tenant in the system with its owner user_id (bulk-fetched in one round-trip from tenant_memberships, prefers role=&lsquo;owner&rsquo; rows, falls back to first membership when role enum is unpopulated). Sorted real-before-demo + created_at ASC within each group so the dropdown matches the default-selection ordering admin already sees. <strong>Extended server fn</strong>: <code>getFirstTenantForAdmin</code> takes optional <code>preferredTenantId</code>. When set + tenant exists, returns that tenant + its owner instead of the default first-by-ordering. Falls through to the v1.20.3 ordering when preferred tenant doesn&rsquo;t exist (defensive against stale localStorage from deleted tenants — won&rsquo;t throw, just picks a sensible default). <strong>Persistence layer</strong>: <code>src/lib/use-tenant-membership.ts</code> exports <code>ADMIN_VIEW_AS_TENANT_ID_KEY</code> + <code>setAdminViewAsTenantId(tenantId)</code>. The hook reads localStorage on the admin-fallback path and passes the value as <code>preferredTenantId</code>. The setter writes the new id and triggers a full <code>window.location.reload()</code> &mdash; not a SPA-style re-render, because invalidating the use-tenant-membership cache + every page-side viewAsUserId effect that&rsquo;s already plumbed would be complex and easy to miss; a reload is one line and unambiguously correct (full re-mount of all components with fresh viewAsUserId). <strong>UI</strong>: <code>RefillShellChrome</code> fetches the tenant list when <code>viewAs === \"admin\"</code> and renders an inline native <code>&lt;select&gt;</code> in the banner replacing the static tenant name. Dropdown shows each tenant name with &lsquo;(demo)&rsquo; suffix on is_demo tenants so admin doesn&rsquo;t accidentally view-as a seed thinking it&rsquo;s real. When there&rsquo;s only one tenant (today&rsquo;s single-Rejuv world ignoring rejuv-demo), the dropdown falls back to a static <code>&lt;strong&gt;</code> — no useless picker. On change, calls <code>setAdminViewAsTenantId</code> which writes localStorage + reloads → entire app re-mounts with the new viewing-as context, including all v1.20 viewAsUserId-plumbed data fetchers. <strong>UX decision rationale</strong>: native select over Popover/custom dropdown because (a) zero extra deps, (b) keyboard navigation + accessibility free, (c) inline-in-text reads naturally (&lsquo;Viewing as [Rejuv ▾] (admin fallback)&rsquo;), (d) reload-on-change is the simplest correct refresh path. Touched: <code>src/server/refill-tenants.ts</code> (listTenantsForAdmin + getFirstTenantForAdmin preferredTenantId), <code>src/lib/use-tenant-membership.ts</code> (localStorage helpers + preferredTenantId plumbing), <code>src/components/refill/RefillShellChrome.tsx</code> (switcher fetch + select markup), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.20.5",
    date: "May 2026",
    items: [
      "<strong>v1.20.5 &mdash; <code>ingestPatientCsv</code> opted into the admin viewing-as write path; closes the SQL-detour footgun.</strong> v1.20 explicitly excluded all write paths from viewAs &lsquo;for safety,&rsquo; but the 2026-05-27 Rejuv pilot upload immediately surfaced the cost of that policy: Grasshopper uploaded the real Rejuv CSV while signed in as admin (viewing-as the tenant in chrome), and the rows landed under his admin user_id instead of the tenant&rsquo;s owner. Required a manual <code>Patient-Data-Move-To-Karen.sql</code> reassign to fix. v1.20.5 narrows the original policy: <strong>ingest IS safe to viewAs</strong> because (a) the use case is explicit (admin pasting a CSV on behalf of a tenant during pilot setup), (b) <code>resolveEffectiveUserId</code> re-verifies admin role server-side before honoring the override, (c) bad ingests are reversible via the same UI. Stripe-mutating writes and plan changes are still intentionally NOT viewAs-aware. <strong>Server change</strong>: <code>ingestInput</code> gains optional <code>viewAsUserId: z.string().uuid().optional()</code>; handler swaps <code>verifyAuth(data.accessToken)</code> for <code>resolveEffectiveUserId({ accessToken, viewAsUserId })</code> and passes <code>effectiveUserId</code> to <code>doIngest</code>. Single 8-line diff; no separate <code>ingestPatientCsvAsTenant</code> fn needed (avoids the code duplication the original sketch implied). <strong>Page-side change</strong>: <code>app.refill.patients.import.tsx</code> imports <code>useTenantMembership</code>, derives <code>viewAsUserId</code> from the state, plumbs it into the <code>ingestPatientCsv</code> call. <strong>Safety surface added</strong>: when admin is viewing-as, the import page renders an amber notice (<code>Uploading into &lt;Tenant&gt;. Patients and transactions will land in this tenant&rsquo;s bucket, not yours.</code>) above the dropzone — eliminates the &lsquo;wait, where did my upload go&rsquo; surprise. <strong>Banner copy refreshed</strong>: <code>RefillShellChrome</code>&rsquo;s admin viewing-as banner updated from &lsquo;Writes still scope to your own user_id&rsquo; (stale post-v1.20.5) to &lsquo;CSV ingest writes into this tenant (v1.20.5). Stripe and plan writes still scope to your own user_id&rsquo; — accurate, narrow callout of which writes are safe to viewAs. <strong>While in the file</strong>: also swept the Emma user-facing strings (eyebrow, description, breadcrumbs) per <em>feedback-emma-never-user-facing</em> — &lsquo;Emma(OS) · Patients&rsquo; &rarr; &lsquo;Refill · Patients&rsquo;, &lsquo;Emma turns it into&rsquo; &rarr; &lsquo;Refill turns it into&rsquo;. Touched: <code>src/server/patient-ingest.functions.ts</code> (zod + handler), <code>src/routes/app.refill.patients.import.tsx</code> (page plumbing + viewing-as notice + Emma sweep), <code>src/components/refill/RefillShellChrome.tsx</code> (banner copy), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.20.4",
    date: "May 2026",
    items: [
      "<strong>v1.20.4 &mdash; Admin-bypass path was dead since v1.19, fixed.</strong> Discovered during the post-v1.20.3 walk-through: the v1.19 <code>useTenantMembership</code> admin-fallback was gated on <code>primaryRole === \"admin\"</code> from <code>useAuth()</code>. But <code>useAuth().primaryRole</code> reads <code>user_preferences.primary_role</code> &mdash; the shell-selection signal whose type is <code>PrimaryRole = \"spa-owner\" | \"rep\" | \"developer\"</code> per <code>src/server/user-prefs.functions.ts:26</code>. <strong>\"admin\" is not a valid PrimaryRole value &mdash; the gate could never be true.</strong> So the v1.19 admin-bypass code path was dead from the day it shipped. It looked like it worked because Grasshopper had a hack tenant_memberships row (inserted via Tenant-Membership-Grant.sql) that made <code>getMyTenant</code> return a real tenant, so the fallback was never reached. The moment that row was dropped (to test v1.20.3 ordering cleanly), <code>useTenantMembership</code> returned NOT_A_TENANT &rArr; <code>src/routes/app.tsx:112</code> failed the <code>isRefillPath &amp;&amp; tenantMembership.tenant</code> guard &rArr; rendered bare <code>&lt;Outlet&gt;</code> &rArr; RefillHome returned null &rArr; <strong>blank pages everywhere</strong>. <strong>Fix</strong>: removed the client-side gate entirely. The server fn <code>getFirstTenantForAdmin</code> already re-verifies admin role against <code>public.user_roles</code> and throws <code>\"Admin only.\"</code> for non-admins &mdash; we catch that and fall through to NOT_A_TENANT, the same end state as before. Cost: one extra POST per non-tenant non-admin sign-in returning 401. Non-tenant users are rare (typically mid-onboarding) so the bandwidth is negligible. Authoritative admin gate stays where it belongs (server-side, against user_roles, with service-role read bypassing RLS). <strong>Three SQL files dropped on Desktop during the diagnostic</strong>: <code>Admin-Drop-Rejuv-Membership.sql</code> (the drop that triggered the blank pages), <code>Admin-Restore-Rejuv-Membership.sql</code> (emergency restore so app loads again pre-fix), <code>Admin-Bypass-Diagnostic.sql</code> (4-row read-only check that cleared suspects 2+3 and pointed at suspect 1). Diagnostic confirmed: admin role row exists (1), is_demo column populated (2 rows: false=1, true=1), v1.20.3 ordering correctly picks <code>rejuv</code> (is_demo=false). <strong>Post-fix flow</strong>: admin can now drop the membership row safely. Hard refresh &rArr; <code>getMyTenant</code> returns null &rArr; v1.20.4 fallback always attempts <code>getFirstTenantForAdmin</code> &rArr; succeeds (admin role + is_demo ordering both valid) &rArr; chrome renders with admin viewing-as banner &rArr; v1.20 viewAsUserId plumbs &rArr; Karen's 1,140 patients render. Touched: <code>src/lib/use-tenant-membership.ts</code> (removed primaryRole gate + updated docblock), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.20.3",
    date: "May 2026",
    items: [
      "<strong>v1.20.3 &mdash; <code>getFirstTenantForAdmin</code> prefers real tenants over demo seeds.</strong> Surfaced during the post-v1.20 admin-upload diagnostic: there are two Rejuv tenants in production &mdash; the synthetic <code>rejuv-demo</code> seed (owner karen@rejuv-demo.test) and the REAL <code>rejuv</code> production tenant (owner karen.aslak@gmail.com, the real Karen Aslakson per <em>project-karen-identity</em>). Pre-v1.20.3, <code>getFirstTenantForAdmin</code> ordered tenants by <code>created_at</code> ASC and returned the oldest &mdash; which is the demo seed because it was migrated in earlier. So when admin viewing-as via the v1.19 admin-bypass shell + v1.20 cross-tenant data-view, the resolved tenant was the demo, not the real one &mdash; and any real production data uploaded against the admin user_id stayed invisible even after v1.20 plumbing was correct. <strong>Fix</strong>: add a leading <code>ORDER BY is_demo ASC NULLS FIRST</code> clause before the existing <code>created_at ASC</code> tiebreaker, so any real tenant (where <code>is_demo</code> is false or null) lands ahead of any demo tenant (<code>is_demo = true</code>). Stable selection within the real-tenant cohort. Single-line server change, no API contract change, no migration. Future v1.20.4+ work: add an explicit tenant switcher in the admin-bypass banner so admin can pick which tenant to view-as (the deterministic-first-tenant rule is a stopgap, not a final answer). Touched: <code>src/server/refill-tenants.ts</code> (single ORDER BY addition), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.20.2",
    date: "May 2026",
    items: [
      "<strong>v1.20.2 &mdash; Patients lifted into the chip nav as the first chip, mirroring the RefillHome quick-actions ordering.</strong> Grasshopper&rsquo;s consistency call: if Patients leads the home grid, it should lead the chip nav too. Chip bar grows from 4 to 5 (Patients / Recovery / Inbox / Settings / Billing). Trojan-horse rule narrative updated &mdash; the bar is still tight and still anchored to the no-show-recovery loop; patients are the underlying data anchor that everything else operates on, so it earns the chip. New chips beyond these 5 still signal platform ambition. <strong>Active-chip detection</strong>: <code>deriveActiveKey</code> in RefillShellChrome.tsx extended to map any path starting with <code>/app/refill/patients</code> &rarr; <code>patients</code> key (covers /patients, /patients/$patientId, /patients/import, /patients/contacts). <code>RefillNavKey</code> union extended to include &lsquo;patients&rsquo;. Touched: <code>src/components/refill/RefillNav.tsx</code> (Patients ITEM + RefillNavKey union + docblock), <code>src/components/refill/RefillShellChrome.tsx</code> (deriveActiveKey patients branch), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.20.1",
    date: "May 2026",
    items: [
      "<strong>v1.20.1 &mdash; Patients surfaced as the lead quick-action card on RefillHome.</strong> Pack v1 outreach templates imported (12 spa templates live in <code>/app/admin/outreach</code>). Next sweep: /app/refill/patients had ZERO discoverability from the rest of the app &mdash; only reachable via direct URL or breadcrumb back-links from /patients/$patientId and /patients/import. Now it leads the RefillHome quick-actions grid (above Recovery / Inbox / Settings / Billing) using the lucide Users icon, subtitle &lsquo;Your roster, cadence, and waitlist&rsquo;. <strong>Why first, not last</strong>: patients are the underlying data anchor &mdash; everything Refill does (recovery, replies, billing splits) flows from the patient roster. Karen&rsquo;s mental model is &lsquo;here are my people, here&rsquo;s what&rsquo;s being done with their cadence.&rsquo; Patients-first ordering matches that. <strong>Trojan-horse rule preserved</strong>: chip nav stays 4-locked (Recovery / Inbox / Settings / Billing). The quick-actions grid is explicitly the soft discoverability surface for routes that exist behind narrow chrome but shouldn&rsquo;t bloat the chip bar. Grid bumped from <code>lg:grid-cols-4</code> to <code>lg:grid-cols-5</code> so 5 cards render symmetrically at desktop; sm:grid-cols-2 stays (5 cards = 2+2+1 with last card wrapping &mdash; acceptable on tablet). <strong>Banner copy refresh</strong>: <code>RefillShellChrome</code>&rsquo;s admin viewing-as banner copy still said &lsquo;Cross-tenant data-view is queued v1.20+.&rsquo; That line shipped 8 hours ago in v1.20 &mdash; updated to reflect what actually works now (read fetchers on patients/waitlist/recovery/billing return the impersonated tenant&rsquo;s data; writes still scope to caller&rsquo;s user_id). Touched: <code>src/components/refill/RefillHome.tsx</code> (Users icon import, Patients QuickAction added as first entry, docblock updated, grid-cols bumped), <code>src/components/refill/RefillShellChrome.tsx</code> (banner copy refresh), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.20",
    date: "May 2026",
    items: [
      "<strong>v1.20 &mdash; Cross-tenant data-view: admin viewing-as a tenant now sees the tenant&rsquo;s actual data, not their own empty set.</strong> Closes the explicit known limitation v1.19 documented: the admin-bypass shell rendered <code>/app/refill/*</code> chrome for the admin without a real tenant_memberships row, BUT every spa-owner server fn still filtered by <code>verifyAuth(accessToken)</code> userId &mdash; so patient lists, recovery feeds, billing, etc. all came back empty. The admin could walk the chrome but couldn&rsquo;t actually observe the impersonated tenant&rsquo;s work. v1.20 fixes the data layer with an opt-in <code>viewAsUserId</code> parameter on the highest-traffic read fns (Approach C hybrid &mdash; helper + selective opt-in, not a full ~20-fn refactor). <strong>New helper</strong>: <code>resolveEffectiveUserId({ accessToken, viewAsUserId })</code> in <code>src/server/auth-helpers.ts</code>. When viewAsUserId is omitted/null/equal-to-caller, collapses to a plain verifyAuth (zero extra DB hit). When viewAsUserId is provided AND differs from caller, re-verifies admin role server-side against <code>public.user_roles</code> (does NOT trust client-side primaryRole signal) and returns the impersonated user_id as <code>effectiveUserId</code>. Non-admin callers passing viewAsUserId get hard 403 &mdash; the security boundary is the server, not the client. Returns <code>{ effectiveUserId, callerUserId, isViewingAs }</code> so audit logging can use callerUserId while data filters use effectiveUserId. <strong>Server fns opted in</strong> (the page-load fns surfaced on /app/refill/* &mdash; ~8 fns total): <code>listPatients</code>, <code>listOverduePatients</code>, <code>getPatientByKey</code>, <code>getPatientById</code> (patient-ingest.functions.ts) &mdash; the patients-dashboard + detail page surface; <code>listWaitlist</code> (emma-waitlist.functions.ts) &mdash; the /app/refill/rescue waitlist tab; <code>getMyTenantRecoveryFeed</code> (refill-tenants.ts) &mdash; the LiveRecoveryFeed hero widget on RefillHome; <code>getActivePlan</code> + <code>listInvoices</code> (refill-billing.ts) &mdash; the /app/billing read surface. Each schema gets an optional <code>viewAsUserId: z.string().uuid().optional()</code> field; each handler swaps <code>verifyAuth(data.accessToken)</code> for <code>resolveEffectiveUserId({ accessToken: data.accessToken, viewAsUserId: data.viewAsUserId })</code> and uses <code>effectiveUserId</code> for all data filtering. <strong>Intentionally NOT opted in</strong>: write paths (markPatientOptedIn, ingestPatientCsv, applyPricingPlan), Stripe-mutating fns (getPaymentMethodStatus, refill-checkout), summarizeOverdueCohort, listRescueActivity, listPatternAlerts, listRecommendations. Read fns first; admin shouldn&rsquo;t be inadvertently mutating the impersonated tenant&rsquo;s payment method, plan, or patient data. <strong>State shape</strong>: <code>getFirstTenantForAdmin</code> extended to also return <code>ownerUserId</code> &mdash; looks up the tenant_memberships rows for the resolved tenant, prefers the &lsquo;owner&rsquo; role row, falls back to any membership. <code>TenantMembershipState</code>&rsquo;s &lsquo;tenant&rsquo; variant gets an optional <code>viewAsUserId?: string</code> alongside <code>viewAs?: \"admin\"</code> so pages can plumb it without re-querying. <strong>Page-side plumbing</strong>: 5 files updated (<code>app.refill.patients.index.tsx</code>, <code>app.refill.patients.$patientId.tsx</code>, <code>app.refill.rescue.tsx</code>, <code>app.refill.index.tsx</code>, <code>app.billing.tsx</code>) &mdash; each imports useTenantMembership, derives <code>viewAsUserId</code> from the membership state, and passes it as part of every relevant server fn call. Effects re-run when viewAsUserId changes so a mid-session admin walking from one surface to another stays consistent. <code>LiveRecoveryFeed</code> component gets an optional <code>viewAsUserId</code> prop; RefillHome passes <code>membership.viewAsUserId</code> through. Effects re-fire when viewAsUserId changes; realtime channel subscriptions stay scoped to the actual tenantUserIds returned by the server (admin observation does not leak the admin&rsquo;s user_id into the realtime channel filter). <strong>Architectural rationale</strong>: per the v1.19 docblock, the &lsquo;viewAsUserId&rsquo; (tenant-owner&rsquo;s user_id) framing was chosen over &lsquo;viewAsTenantId&rsquo; specifically because spa-owner server fns are historically Emma-era user-scoped (.eq(&lsquo;user_id&rsquo;, userId)). Threading effective user_id preserves that shape verbatim &mdash; no fan-out refactor, no migration. The single tenant-scoped layer (refill-billing.getTenantIdForUser) works identically with the impersonated user_id substituted in. <strong>What&rsquo;s NOT in v1.20 (queued)</strong>: long-tail server fns (~15 more fns under user-id filtering &mdash; listRescueActivity, listPatternAlerts, listRecommendations, summarizeOverdueCohort, etc.) that surface on /app/refill/* but weren&rsquo;t in the top-5 page-load surface; per-fn write-path gating; explicit audit log for admin viewing-as actions. Add the long-tail fns when a specific surface surfaces a regression (admin sees zero rows where there should be data); the helper makes the per-fn addition mechanical (~3 lines per fn). Touched: <code>src/server/auth-helpers.ts</code> (resolveEffectiveUserId + EffectiveUserContext type), <code>src/server/refill-tenants.ts</code> (getFirstTenantForAdmin returns ownerUserId; getMyTenantRecoveryFeed opted in), <code>src/server/patient-ingest.functions.ts</code> (4 fns opted in), <code>src/server/emma-waitlist.functions.ts</code> (listWaitlist opted in), <code>src/server/refill-billing.ts</code> (getActivePlan + listInvoices opted in), <code>src/lib/use-tenant-membership.ts</code> (state shape + admin-fallback wiring), <code>src/components/refill/LiveRecoveryFeed.tsx</code> (prop signature), <code>src/components/refill/RefillHome.tsx</code> (pass through), <code>src/routes/app.refill.patients.index.tsx</code> + <code>$patientId.tsx</code> + <code>app.refill.rescue.tsx</code> + <code>app.refill.index.tsx</code> + <code>app.billing.tsx</code> (page-side plumbing), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.19",
    date: "May 2026",
    items: [
      "<strong>v1.19 &mdash; Admin-bypass shell for the Refill spa-owner surfaces (cleans up the Option-A membership-grant hack).</strong> Closes the architectural smell that surfaced during the admin-recovery thread: the only way for the platform admin to walk <code>/app/refill/*</code> pages was to insert a row into <code>public.tenant_memberships</code> linking the admin account to a tenant. Works mechanically but conflates two distinct concerns &mdash; “I&rsquo;m a spa-owner-on-tenant-X” vs “I&rsquo;m the platform admin observing tenant-X for support/walkthrough purposes.” v1.19 separates them. <strong>New server fn</strong>: <code>getFirstTenantForAdmin</code> in <code>src/server/refill-tenants.ts</code> (~60 lines) &mdash; re-verifies admin role server-side against <code>public.user_roles</code> (doesn&rsquo;t trust client-side primaryRole signal), then returns the first tenant by <code>created_at</code> asc. Pure read; no inserts, no membership grant, no side effects. <strong>Modified hook</strong>: <code>useTenantMembership</code> in <code>src/lib/use-tenant-membership.ts</code> gets an admin-fallback path. After the regular <code>getMyTenant</code> lookup returns <code>NOT_A_TENANT</code> AND the user is admin (per <code>useAuth().primaryRole</code>), the hook calls <code>getFirstTenantForAdmin</code> and returns <code>{ status: \"tenant\", tenant, viewAs: \"admin\" }</code>. The new <code>viewAs</code> discriminator on the state type lets consumers know the membership isn&rsquo;t real without breaking the existing <code>status === \"tenant\"</code> check that drives shell selection. <strong>Modified shell</strong>: <code>RefillShell</code> threads <code>viewAs</code> through to <code>RefillShellChrome</code>; chrome renders an amber-soft “Viewing as Rejuv (admin fallback)” banner with a Sparkles icon between the DemoBanner and the chip nav. The banner is invisible for real tenant members and for non-admin users &mdash; only platform admins see it, only when viewing-as. <strong>Explicit known limitation called out in the banner copy + the docblock</strong>: every spa-owner server fn (listPatients, listOverduePatients, listWaitlist, getMyTenantRecoveryFeed, refill-billing surfaces, etc.) still filters by <code>user_id = verifyAuth(accessToken)</code>. The admin viewing-as gets shell chrome rendered but data queries return their OWN data (typically empty). Cross-tenant data-view is a much bigger ship &mdash; every server fn would need to accept an optional <code>viewAsUserId</code> parameter, validate caller is admin, and gate the user_id filter accordingly. Queued as v1.20+ if it ever becomes load-bearing. <strong>Architectural rationale</strong>: per <em>project-refill-trojan-horse-thesis</em>, the chrome stays narrow forever &mdash; this banner is admin-only chrome that never reaches a real spa owner, so the stealth posture holds. The fallback is conservative (gated server-side on admin role, no membership row inserted, no audit trail mutation), so the data-isolation guarantees for tenant data also hold. <strong>Tactical follow-on for Grasshopper</strong>: the existing tenant_memberships row I had you insert via <code>Tenant-Membership-Grant.sql</code> earlier is now structurally optional &mdash; v1.19&rsquo;s admin fallback would have rendered the shell without it. Safe to leave the row in (no harm) OR safe to DELETE it later if you want to test the admin-fallback path cleanly. Touched: <code>src/server/refill-tenants.ts</code> (new server fn), <code>src/lib/use-tenant-membership.ts</code> (full rewrite to thread admin fallback + viewAs discriminator), <code>src/components/shells/RefillShell.tsx</code> (pass viewAs through), <code>src/components/refill/RefillShellChrome.tsx</code> (signature + admin banner), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.18",
    date: "May 2026",
    items: [
      "<strong>v1.18 &mdash; Full password lifecycle: /reset-password (public) + /app/refill/settings/account (gated).</strong> Closes the gap that surfaced during the admin-recovery thread: the /login &lsquo;Forgot password?&rsquo; modal already fires <code>supabase.auth.resetPasswordForEmail({ redirectTo: &quot;https://getrefill.app/reset-password&quot; })</code>, but the redirect target didn&rsquo;t exist as a route &mdash; the recovery email arrived but clicking the link 404&rsquo;d. And there was no in-app change-password UI for already-signed-in users either &mdash; the only path was Supabase Dashboard. v1.18 ships both surfaces. <strong>(a) /reset-password</strong>: public route. Mirrors /login&rsquo;s single-card-centered-paper-bg styling with inline hex colors (same Georgia serif headline, same #056048/#fbfaf7/#e6e2d6 palette). Detects the Supabase auth handoff via <code>supabase.auth.getSession()</code> + listens for <code>PASSWORD_RECOVERY</code> / <code>SIGNED_IN</code> auth events. Four phases: <code>checking</code> (verifying link), <code>invalid</code> (expired/missing tokens &mdash; shows &lsquo;Recovery links expire after one hour&rsquo;), <code>ready</code> (form with new + confirm fields, min 8 chars, eye toggle, real-time mismatch check), <code>done</code> (success card &mdash; signs out the recovery session so the next sign-in is clean, redirects to /login after 1.8s). <strong>(b) /app/refill/settings/account</strong>: auth-gated route. Mirrors the existing settings-page chrome (PageHeader + brand-token Tailwind). Two sections: read-only Email row showing <code>user.email</code> from useAuth() with &lsquo;Contact support to change&rsquo; copy (email change requires reverification flow, deferred), and Change Password card with new + confirm fields + auto &lsquo;Last changed N ago&rsquo; helper text using <code>auth.users.updated_at</code>. On submit calls <code>supabase.auth.updateUser({ password })</code>; success toast + form reset. <strong>UX decision</strong>: no current-password verification field on either page. Supabase&rsquo;s <code>updateUser</code> doesn&rsquo;t require it (the active JWT is the auth gate), and requiring it would be security theater &mdash; if an attacker has the JWT they have the change anyway. Cleaner to skip. <strong>Real-time validation</strong>: both pages show inline &lsquo;Passwords don&rsquo;t match&rsquo; rose text only when confirm has been typed AND mismatches (not on empty); submit button disabled until both fields valid + match. <strong>Eye toggle</strong>: show/hide password using lucide Eye/EyeOff icons, applies to both fields (single toggle controls both). <strong>Surface entry</strong>: /reset-password is reachable from email links (existing flow now works end-to-end) + the &lsquo;Back to sign in&rsquo; link in the page header. /app/refill/settings/account currently reachable via direct URL only; chip nav entry deferred until we decide where it fits per the trojan-horse 4-chip thesis. Touched: <code>src/routes/reset-password.tsx</code> (new file, ~285 lines), <code>src/routes/app.refill.settings.account.tsx</code> (new file, ~220 lines), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.17",
    date: "May 2026",
    items: [
      "<strong>v1.17 &mdash; Waitlist toggle off-state actually visible + bigger find: 3 more silently-broken brand tokens.</strong> Grasshopper&rsquo;s screenshot after v1.16 showed the active emerald toggles working but off-state toggles still invisible. Diagnosis was wrong in v1.16: I translated <code>bg-muted</code> &rarr; <code>bg-rule</code> thinking <code>--color-rule</code> existed in <code>@theme</code>. <strong>It didn&rsquo;t.</strong> The only warm-gray token defined in <code>src/styles.css</code> @theme block was <code>--color-border-soft</code>. Every <code>bg-rule</code>, <code>border-rule</code>, <code>bg-rule-soft</code>, <code>hover:bg-rule-soft</code>, <code>divide-rule</code> reference I shipped across v1.10 (Lane 1 polish), v1.14 (/promos port), and v1.16 (patients sweep) has been emitting <strong>zero CSS</strong>. Same class of bug as the original <code>bg-primary</code> find &mdash; just propagated by me, not inherited from openagenticv4. Per <em>feedback-no-blame-no-glide</em>: I confidently translated to brand tokens that didn&rsquo;t exist. <strong>Fix #1 &mdash; define the missing tokens in @theme</strong>: <code>--color-rule: #e6e2d6</code> (matching the existing border-soft for consistency), <code>--color-rule-soft: #f3f0e7</code> (warm off-white for hover/subtle backgrounds), <code>--color-track: #c8c2b3</code> (darker warm-gray with enough contrast against bg-white for switch tracks). Adding these three lines to styles.css <strong>retroactively unblocks every prior polish ship</strong> &mdash; rule-* tokens that were silently broken now render with intent. <strong>Fix #2 &mdash; waitlist toggle off-state</strong>: changed from <code>bg-rule</code> (was broken; would now be subtle <code>#e6e2d6</code> which is too low-contrast against bg-white card) to <code>bg-track</code> (the new darker <code>#c8c2b3</code>) for an iOS-style switch with visible track contrast matching the agentiport reference Grasshopper screenshot-pinned. <strong>Fix #3 &mdash; A-list star fill</strong>: another silently-broken token found mid-walk &mdash; <code>fill-primary</code> on active VIP stars never rendered (primary undefined per v1.10-v1.11 finding). Translated to <code>fill-emerald</code> so VIP-flagged patients now show a filled emerald star. <strong>Strategic note</strong>: this is the third class of silently-broken tokens (after <code>primary</code> in v1.10 and <code>card</code>/<code>border</code>/<code>muted</code>/<code>foreground</code> in v1.14/v1.16). A wider audit of refill-app&rsquo;s actual <code>@theme</code> contents vs all token references in code is now warranted &mdash; would surface every additional silently-emitted-zero-CSS class name in one pass. Queued as a Lane 2 backlog item. Touched: <code>src/styles.css</code> (3 new @theme lines), <code>src/routes/app.refill.patients.index.tsx</code> (toggle off-state token + star fill token), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.16",
    date: "May 2026",
    items: [
      "<strong>v1.16 &mdash; Patients-page polish: waitlist toggle made visible + silently-broken token sweep on both patient files.</strong> Triggered by Grasshopper&rsquo;s live walk feedback on /app/refill/patients (the &lsquo;amazing dashboard&rsquo; he was remembering from agentiport — confirmed already ported, just visually degraded). <strong>Primary issue</strong>: the WAITLIST column toggle on each row was rendering as a tiny shadow circle with no track, so spa owners couldn&rsquo;t tell the column was interactive (&lsquo;can&rsquo;t tell it exists&rsquo;). Root cause: the toggle&rsquo;s off-state used <code>bg-muted</code> (track) + <code>bg-background</code> (thumb), both of which emit zero CSS in refill-app&rsquo;s <code>@theme</code> block per the v1.10 silently-broken-token find. With no background on either layer, only the thumb&rsquo;s drop-shadow rendered &mdash; appearing as a faint empty circle. <strong>Fix</strong>: <code>bg-muted</code> &rarr; <code>bg-rule</code> (visible warm-gray track), <code>bg-background</code> &rarr; <code>bg-white</code> (visible thumb). Off state now renders as a clear iOS-style switch on a rule-colored track; on state flips to the brand emerald with the white thumb on the right. <strong>Sibling sweep</strong>: same broken-token class across 30 + 33 instances in <code>app.refill.patients.index.tsx</code> and <code>app.refill.patients.\\$patientId.tsx</code>. Mechanical sed sweep: <code>border-border</code> &rarr; <code>border-rule</code>, <code>divide-border</code> &rarr; <code>divide-rule</code>, <code>bg-card</code> &rarr; <code>bg-white</code>, <code>text-foreground</code> &rarr; <code>text-ink</code>, <code>hover:text-foreground</code> &rarr; <code>hover:text-ink</code>, <code>bg-muted/40</code> &rarr; <code>bg-rule-soft/60</code>, <code>bg-muted/30</code> &rarr; <code>bg-rule-soft/60</code>, <code>hover:bg-muted</code> &rarr; <code>hover:bg-rule-soft</code>, <code>bg-background</code> &rarr; <code>bg-white</code>, <code>focus-visible:ring-ring</code> &rarr; <code>focus-visible:ring-emerald</code>, <code>bg-destructive/15 text-destructive</code> &rarr; <code>bg-rose-soft text-rose</code>, <code>bg-destructive/10 text-destructive</code> &rarr; <code>bg-rose-soft text-rose</code>, generic Tailwind amber-700/800/900 used for overdue-pill text &rarr; brand <code>text-amber</code>. <strong>Mid-sweep snag</strong>: my <code>bg-amber-50</code> &rarr; <code>bg-amber-soft</code> pattern accidentally mangled three chip-color-map entries (<code>bg-amber-500/20</code> became <code>bg-amber-soft0/20</code>). Restored those to Tailwind palette (<code>bg-amber-500/20</code> works fine — it&rsquo;s a built-in Tailwind utility, not a broken token). Per <em>feedback-no-blame-no-glide</em>: my sed pattern was insufficiently anchored; future sweeps should match word-boundary or use a more specific token search. <strong>Plus Emma dead-prop strip</strong>: 5 dead <code>eyebrow=&quot;Emma(OS) &middot; Patients&quot;</code> / <code>eyebrow=&quot;Emma(OS) &middot; Patient&quot;</code> props removed (PageHeader v1.4.1 already ignored eyebrow + breadcrumbs, so these weren&rsquo;t user-visible &mdash; but per <em>feedback-emma-never-user-facing</em> hard rule, source-level cleanup honors the rule), and the breadcrumb <code>{ label: &quot;Emma&quot;, to: &ldquo;/app/refill&quot; }</code> on the detail page flipped to <code>{ label: &quot;Refill&quot; }</code>. The <code>* symmetry between Lizzie(OS) and Emma(OS)</code> source comment intentionally stays &mdash; internal architecture context, not user-facing copy. <strong>Critically NOT changed</strong>: any business logic (waitlist toggle behavior, optimistic UI, server-fn signatures, sort comparators, filter chip semantics, manufacturer color map intent). Touched: <code>src/routes/app.refill.patients.index.tsx</code> (~40 surgical edits), <code>src/routes/app.refill.patients.\\$patientId.tsx</code> (~38 surgical edits), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.15",
    date: "May 2026",
    items: [
      "<strong>v1.15 &mdash; /report/&lt;token&gt; ported from openagenticv4 (P1 audit fix, ship 4 of the port sprint).</strong> Per <code>Agentiport-Port-Audit.html</code>, this completes the lead-gen viral loop: token-gated hosted HTML render of <code>csv_scanner_leads.followup_report_html</code>. <strong>Why it matters</strong>: Gmail (and most webmail clients) refuse to inline-render <code>.html</code> attachments for XSS safety &mdash; they show source code instead. v371 shipped follow-up reports as email attachments; v371.1 moved them to hosted URLs so recipients get a clean in-browser render. Without this route live, the Outreach Playbook&rsquo;s V5 variant (reactivation followup for /scan visitors who didn&rsquo;t convert) would dead-link &mdash; the <em>&ldquo;You scanned your spa last week&rdquo;</em> nudge email embeds a <code>getrefill.app/report/&lt;token&gt;</code> link as its proof asset. <strong>What ports</strong>: just the route &mdash; <code>src/routes/report.\\$token.tsx</code>, 130 lines. The server fn <code>getScanReportHtml</code> was already ported (<code>src/server/scan-report.functions.ts</code>) along with the DB columns (<code>csv_scanner_leads.followup_report_html</code> / <code>followup_report_token</code>). <strong>Token IS the capability</strong> &mdash; no auth required, since the report contains only the spa&rsquo;s own self-uploaded math (no cross-spa data leakage risk). <strong>How the page works</strong>: fetches the full polished HTML document from DB, extracts the <code>&lt;body&gt;</code> contents via regex, injects via <code>dangerouslySetInnerHTML</code> into a wrapper div. The body-extraction step is required because React owns <code>document.documentElement.innerHTML</code> &mdash; we can&rsquo;t replace the full document, but we can render the body inside our shell. <strong>Cleave changes during port</strong>: kept the inline-<code>style</code>-prop approach verbatim (NOT translated to Tailwind brand classes) on the loading + error shells because the report body is <code>dangerouslySetInnerHTML</code> and tailwind preflight inside that scope could conflict with the report&rsquo;s own inline styles &mdash; keeping the wrapper as raw style props preserves openagenticv4&rsquo;s isolation guarantee. Hex literals translated to brand-aligned hexes: <code>#f8f6f1</code> (page bg) &rarr; <code>#fbfaf7</code> (brand paper), <code>#64748b</code> (gray text) &rarr; <code>#5a6068</code> (brand ink-soft), <code>#fde68a</code> (amber border) &rarr; <code>#fdf6dc</code> (brand amber-soft), <code>#451a03</code> / <code>#92400e</code> (amber dark text + icon) &rarr; <code>#8a6d0c</code> (brand amber), <code>#0f766e</code> (link teal) &rarr; <code>#056048</code> (brand emerald). <strong>Three render states</strong>: (1) loading spinner with &lsquo;Loading your report&hellip;&rsquo; copy, (2) expired/invalid-token error card with friendly &lsquo;Drop your CSV again at /scan to generate a fresh one&rsquo; recovery copy, (3) rendered report body. <strong>Cross-references that now resolve</strong>: Outreach Playbook V5 reactivation email&rsquo;s embedded report link, any future /scan-driven follow-up email that wants to host its math rather than attach it. Touched: <code>src/routes/report.\\$token.tsx</code> (new file, 130 lines), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.14",
    date: "May 2026",
    items: [
      "<strong>v1.14 &mdash; /app/refill/promos ported from openagenticv4 (P0 audit fix, ship 3 of the port sprint).</strong> Per <code>Agentiport-Port-Audit.html</code>, this was flagged as the possible &lsquo;amazing dashboard&rsquo; Grasshopper remembered: manufacturer promotions surface where the spa owner sees promos for manufacturers she actually carries (matched at claim-your-business via <code>service.attachments.matchedProduct.manufacturer</code>), with Express Interest / Dismiss actions that signal the rep. Now live at <code>/app/refill/promos</code>. <strong>Two files ported, not one</strong>: (a) new server module <code>src/server/refill-promos.ts</code> (~290 lines) containing the 3 spa-side server fns extracted from openagenticv4&rsquo;s 2851-line <code>promotions.functions.ts</code> &mdash; <code>listEligiblePromosForSpa</code>, <code>expressInterestInPromo</code>, <code>dismissPromoForSpa</code> &mdash; plus the helpers they need (<code>normalizeManufacturer</code>, <code>describeTierClauseForVerified</code>, <code>SpaInterestStatus</code>, <code>EligiblePromoForSpa</code> type, local <code>admin()</code> client). Per <em>project-refill-trojan-horse-thesis</em>, the rep-side promo management (<code>listPromotions</code>, <code>draftPromoBlast</code>, <code>sendPromoBlast</code>, <code>listPromoOutreachStates</code>, <code>routeInboundPromoReply</code>, etc. &mdash; ~2560 lines of Liz CRM stack) is intentionally NOT ported &mdash; that stays in openagenticv4. (b) <code>src/routes/app.refill.promos.tsx</code> (~480 lines) with the UI: PageHeader + summary strip + Active/All filter + promo cards with manufacturer chip, status badge (Upcoming/Active/N days left/Ended), [VERIFIED] tier-range box showing entry + top tiers from the manufacturer&rsquo;s ladder, &lsquo;I&rsquo;m interested&rsquo; / &lsquo;Not for me&rsquo; CTAs, optional 500-char rep-note message box. <strong>DB substrate already ported</strong>: <code>spa_promo_interest</code> table (20260515) + <code>knowledge_nodes</code> with <code>node_type=&lsquo;promotion&rsquo;</code> + <code>promotions_engine</code> migration (20260513050000) all already in refill-app. <strong>Cleave changes during port</strong>: removed <code>eyebrow=&ldquo;Emma(OS)&rdquo;</code> per <em>feedback-emma-never-user-facing</em> (Emma is the stealth spa-assistant codename; never appears in user-facing copy); translated all <strong>silently-broken shadcn tokens</strong> to brand tokens (<code>bg-card</code>&rarr;<code>bg-white</code>, <code>border-border</code>&rarr;<code>border-rule</code>, <code>text-foreground</code>&rarr;<code>text-ink</code>, <code>bg-muted</code>&rarr;<code>bg-rule-soft</code>, <code>bg-background</code>&rarr;<code>bg-paper</code>) per the v1.10 find that <code>--color-card</code>/<code>--color-muted</code>/<code>--color-border</code>/<code>--color-foreground</code> aren&rsquo;t defined in refill-app&rsquo;s <code>@theme</code> block; translated all <code>bg-primary</code>/<code>text-primary</code> references (which the v1.11 sweep wouldn&rsquo;t have caught because this file didn&rsquo;t exist yet) to <code>bg-emerald</code>/<code>text-emerald</code>; translated agentiport&rsquo;s <code>bg-clay</code> (expired badge) to <code>bg-rose-soft</code> and <code>bg-sage-soft</code> (active badge) to <code>bg-emerald-soft</code>. <strong>Critically NOT changed</strong>: all server-fn logic (eligibility math, manufacturer normalization, tier sorting, interest signal hydration), all UI state semantics (filter, dismiss-hide-from-active behavior, interest-noted final state), all copy beats. <strong>Cross-references</strong>: page links back to <code>/app/refill</code> dashboard from EmptyState + bottom copy; rep-side &lsquo;N practices interested&rsquo; surface lives in openagenticv4 only (Liz CRM, out of scope for refill-app). Touched: <code>src/server/refill-promos.ts</code> (new), <code>src/routes/app.refill.promos.tsx</code> (new), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.13",
    date: "May 2026",
    items: [
      "<strong>v1.13 &mdash; /start page ported from openagenticv4 (P0 audit fix, ship 2 of the port sprint).</strong> Per the <code>Agentiport-Port-Audit.html</code> finding, <code>/start</code> was the missing P0 landing page for the cold-email funnel: every Karen variant&rsquo;s &lsquo;Set up Refill for my spa&rsquo; CTA points here, and without it the funnel landed at a 404 between /scan and /onboard. Now live: thin manual-intake form capturing the minimum Karen + David need to onboard a new spa within 24 hours &mdash; practice name, email, scheduler dropdown (25 schedulers including Acuity / Boulevard / Mangomint / Mindbody / GlossGenius / etc), plus optional phone + estimated monthly cancels + freeform notes. Why manual not self-serve: shipping a half-baked self-serve flow loses more pilots than it converts; manual onboarding from a real spa-owner couple is the strongest first impression in the segment. /start is the bridge until the /onboard 5-step wizard proves itself. <strong>Server fn already ported</strong>: <code>submitRefillSetupIntent</code> at <code>src/server/refill-setup.functions.ts</code> writes to <code>csv_scanner_leads</code> with intent-attribution (reuses the prior /scan row by email if it exists, attaching the setup-intent fields rather than creating a parallel record &mdash; one lead per spa). <strong>Cleave changes during port</strong> (same pattern as v1.12 /story): dropped <code>useShell()</code> + <code>brandFor()</code> shell-discriminator, inlined static <code>brand</code> import; translated all slate-*/emerald-NNN/amber-NNN/rose-NNN Tailwind generics to brand tokens (ink, ink-soft, ink-faint, emerald, emerald-soft, amber, amber-soft, rose, rule, paper) per v1.10-v1.11 brand-coherence work; flipped dark CTA from bg-slate-900 to bg-emerald matching v1.11 sweep; flipped the success-state checkmark badge from bg-emerald-500 to bg-emerald; softened card radii rounded-3xl &rarr; rounded-xl per v1.10 polish; required-asterisks use <code>text-rose</code> (brand crimson) instead of <code>text-rose-600</code>. <strong>Critically NOT changed</strong>: form fields, scheduler list (25 options preserved verbatim), success copy (&lsquo;Got it. We&rsquo;ll be in touch within 24 hours&rsquo;), pricing footnote (&lsquo;Refill is free until we actually recover revenue for you. 12% of recovered, then.&rsquo;), all messaging beats intact. <strong>Cross-references that now resolve</strong>: every Karen cold-email CTA pointing at <code>getrefill.app/start</code>, /scan&rsquo;s &lsquo;Set up Refill&rsquo; downstream CTA, /story&rsquo;s &lsquo;Ready when you are&rsquo; scan card. Touched: <code>src/routes/start.tsx</code> (new file, ~310 lines), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.12",
    date: "May 2026",
    items: [
      "<strong>v1.12 &mdash; /story page ported from openagenticv4 (first ship of the post-audit port-completeness sprint).</strong> Per the <code>Agentiport-Port-Audit.html</code> finding, <code>/story</code> was a P0 missing route &mdash; every Karen cold-email CTA links to <code>getrefill.app/story</code> for prospects who want the full founders narrative, and without it the cold-email funnel landed at a 404. Now lands at the canonical long-form version: hero (20+ years owning Rejuv, finally built the no-show fix) &rarr; the pain (Monday morning shuffling) &rarr; what we built (any scheduler + honest math + recovery engine) &rarr; the math (empty-slot leak only, defensible against books) &rarr; 3-step CTA card (export CSV &rarr; drop at getrefill.app &rarr; see real number) &rarr; pricing (free + 12% only on recovered, &lsquo;nothing nada zip zilch&rsquo; if no recovery) &rarr; conviction guarantee &rarr; scheduler coverage (24+ platforms) &rarr; closing (&lsquo;Whatever you decide, good luck on your med spa journey&rsquo;) &rarr; Karen + David signature &rarr; bottom CTA. Voice locked to Karen as primary author per <em>real-world-first-then-match</em>. <strong>Cleave changes during port</strong>: dropped <code>useShell()</code> + <code>brandFor()</code> shell-discriminator (refill-app is single-brand post-cleave; brandFor was deleted per <code>src/lib/brand.ts</code> comment block), inlined static <code>brand</code> import; translated 20+ Tailwind generic tokens (<code>slate-900</code>, <code>slate-700</code>, <code>slate-600</code>, <code>slate-500</code>, <code>slate-400</code>, <code>slate-50</code>, <code>slate-100</code>, <code>slate-200</code>, <code>emerald-700</code>, <code>emerald-50</code>, <code>emerald-100</code>, <code>amber-50</code>, <code>amber-200</code>, <code>amber-800</code>, <code>amber-900</code>, <code>bg-[#f8f6f1]</code>, <code>text-white</code>) to Refill brand tokens (<code>ink</code>, <code>ink-soft</code>, <code>ink-faint</code>, <code>emerald</code>, <code>emerald-soft</code>, <code>amber</code>, <code>amber-soft</code>, <code>paper</code>, <code>rule</code>, <code>rule-soft</code>) per v1.10-v1.11 brand-coherence work; flipped dark CTA blocks from <code>bg-slate-900 text-white</code> to <code>bg-emerald text-paper</code> matching the v1.11 sweep; softened card radii from <code>rounded-2xl</code> to <code>rounded-xl</code> per v1.10 polish convention. <strong>Critically NOT changed</strong>: every line of body copy stays verbatim from openagenticv4&rsquo;s v371.3 ship &mdash; voice consistency, pricing (12%), ARV ($250), 3-step CTA, &lsquo;Nothing nada zip zilch&rsquo; guarantee, peer-to-peer closing all preserved exactly. <strong>Cross-references that now resolve</strong>: all Karen cold-email templates referencing <code>getrefill.app/story</code> (Pack v1 + Playbook V1-V5), <code>Refill-Cold-Email.html</code>&rsquo;s P.S. link, future <code>Karen-Onboarding-Loom-Script.html</code> embedded link below founders section, /scan&rsquo;s downstream &lsquo;learn more&rsquo; CTA. Touched: <code>src/routes/story.tsx</code> (new file, 282 lines), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.11",
    date: "May 2026",
    items: [
      "<strong>v1.11 &mdash; Repo-wide bg-primary&rarr;bg-emerald sweep (closes the silently-broken-token find from v1.10).</strong> v1.10 surfaced that <code>bg-primary</code>/<code>text-primary</code>/<code>border-primary</code> generate zero CSS in this codebase &mdash; <code>styles.css</code>&rsquo;s <code>@theme</code> block defines emerald/ink/amber/paper/rose but no <code>--color-primary</code>, so Tailwind v4 emits nothing for any primary-keyed utility. On <code>app.billing.tsx</code> alone there were 6 broken refs. v1.10 swept that one file. v1.11 finishes the job: <strong>31 files, 168 surgical replacements</strong>, two-pass mechanical sed:<br /><br /><code>(text|bg|border|ring)-primary-foreground</code> &rarr; <code>$1-paper</code><br /><code>(text|bg|border|ring)-primary</code> &rarr; <code>$1-emerald</code> (preserving <code>/N</code> opacity modifiers)<br /><br /><strong>Affected surfaces</strong>: <code>src/components/ui/button.tsx</code> (the shared shadcn button default + link variants &mdash; was rendering as default-text), <code>src/routes/index.tsx</code> (landing page logo box + CTA buttons + focus ring + icon container), all 12 <code>app.refill.*</code> route files (campaigns / patients / waitlist / inbox / recovery / rescue / settings / sharing / reports / index), both admin routes, all 7 affected components (OnboardingWizard, ServicesEditCard, NotificationCenter, EmptyState, ErrorBoundary, CohortEditor), and pricing.tsx. <strong>What changes visually</strong>: every CTA button, every focus ring, every active-state highlight, every emphasized icon &mdash; what was rendering as default-text-color or transparent now reads as the sage emerald (#056048) that&rsquo;s supposed to anchor the brand. The landing page logo box (the &lsquo;R&rsquo; in a square) was previously invisible-against-paper-bg; now it&rsquo;s the proper emerald square. <strong>Excluded from sweep</strong>: <code>src/lib/changelog.ts</code> (contains historical HTML text referencing primary tokens by name in earlier changelog entries; not class names). <strong>Build proof</strong>: clean vite build, zero TS errors introduced, deploy succeeded. <strong>Memory record</strong>: <code>project-bg-primary-silently-broken.md</code> documents the find with the translation table for any future reference. <strong>Not done</strong>: the &lsquo;Active&rsquo; chip uppercase tracking-wider stays (chip indicators read genre-correct), table column header uppercase stays (compact scan-friendly), shadcn <code>secondary</code>/<code>accent</code>/<code>destructive</code>/<code>input</code>/<code>muted</code> tokens not audited &mdash; if any are also missing from @theme they&rsquo;ll surface in future polish work. Touched: 31 files (.tsx in src/components/ and src/routes/), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.10",
    date: "May 2026",
    items: [
      "<strong>v1.10 &mdash; Lane 1 polish sweep on /app/billing (brand-coherent + softer rhythm + a silently-broken-token find).</strong> Five Lane 1 backlog items in one ship, all on <code>app.billing.tsx</code>. <strong>(1) Card corner radius</strong>: every <code>rounded-2xl</code> on the page &rarr; <code>rounded-xl</code>. Softer rhythm that matches the RefillHome quick-action cards (the canonical Refill house style) instead of the sharper platform-y radius the page inherited from the emma-era surface. Eight sites touched (active-plan banner, no-active amber banner, plan cards, empty invoice state, invoice table container, card-on-file states 1+2+3). <strong>(2) Brand amber tokens replace Tailwind generic</strong>: the &lsquo;No active plan yet&rsquo; banner + the Hybrid plan-card icon accent + the &lsquo;Sent&rsquo; invoice-status pill + the test-mode footnote on Card-on-file used Tailwind&rsquo;s generic <code>amber-500</code>/<code>amber-700</code> (orange-leaning, doesn&rsquo;t match the muted goldenrod established by Refill brand). Swapped for the brand tokens <code>bg-amber-soft</code> (<code>#fdf6dc</code>) and <code>text-amber</code> (<code>#8a6d0c</code>) defined in <code>@theme</code>. <strong>(3) Refresh button chip-style</strong>: the PageHeader actions-slot refresh button used shadcn <code>border-border</code> + <code>text-ink-soft</code> tokens (generic). Now uses the inactive RefillNav chip pattern (transparent bg, <code>#5a6068</code> ink, <code>#e6e2d6</code> border, 12px semibold) via inline style &mdash; matches the chip row Karen sees up top, reads as &lsquo;sibling not stranger.&rsquo; <strong>(4) Section labels sentence-cased</strong>: &ldquo;PICK YOUR PLAN&rdquo; and &ldquo;INVOICE HISTORY&rdquo; both shed <code>uppercase tracking-wider</code> and the <code>text-sm</code> bump to a softer <code>text-[15px] font-semibold</code>. Refill voice is warmer than enterprise-form-label all-caps; this is the consumer-feel sweep. Table column headers KEEP their all-caps (compact scan-friendly is the right call for dense table heads). The &lsquo;Active&rsquo; chip on plan cards also keeps uppercase &mdash; chip indicators reading uppercase is genre-correct, not the same complaint as section labels. <strong>(5) BIG FIND &mdash; bg-primary is a silently-broken token in this theme</strong>: <code>styles.css</code>&rsquo;s <code>@theme</code> block defines emerald, ink, amber, paper, rose &mdash; but NOT primary. Every <code>bg-primary</code> / <code>text-primary</code> / <code>border-primary</code> / <code>bg-primary/5</code> / <code>bg-primary/15</code> reference in the codebase is a no-op generating no CSS at all (Tailwind v4 only emits utilities for colors in the active theme). On <code>app.billing.tsx</code> alone there were 6 such broken references (active-plan banner border + bg, plan-card active state, primary-tone icon, Active-pill, Pick-Performance button bg, Add-payment-method button bg). All swapped to the brand emerald equivalents: <code>border-emerald</code> + <code>bg-emerald-soft</code>, <code>bg-emerald</code> + <code>text-paper</code>. The visible difference is significant &mdash; what was rendering as default-text-color or transparent now reads as the sage emerald that&rsquo;s supposed to anchor the page. <strong>Repo-wide cleanup of remaining</strong> <code>bg-primary</code> <strong>references is a queued follow-on ship</strong> (many other pages affected; out of v1.10 scope). Also nudged: <code>text-emerald-700</code>/<code>emerald-500/15</code> on card-on-file flipped to brand <code>text-emerald</code>/<code>bg-emerald</code>. Touched: <code>src/routes/app.billing.tsx</code> (~18 surgical edits), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.9",
    date: "May 2026",
    items: [
      "<strong>v1.9 &mdash; emma&harr;refill UI unification + InvoiceRow TZ fix (closes the Frankenstein).</strong> Two small-but-load-bearing cleanups in one ship. <strong>(1) UI unification</strong>: <code>app.billing.tsx</code> finally finishes the emma&rarr;refill migration that v1.7.2 started. Imports for <code>applyPricingPlan</code>, <code>getActivePlan</code>, <code>getPlanEconomics</code>, and the associated types now come from <code>@/server/refill-billing</code> instead of <code>@/server/emma-billing.functions</code>. The page&rsquo;s state types flip from <code>ActivePlan</code> / <code>PricingPlan</code> to <code>RefillActivePlan</code> / <code>RefillPricingPlan</code>, and <code>PLAN_META</code> re-keys from <code>performance</code>/<code>predictable</code>/<code>hybrid</code> to the canonical <code>starter</code>/<code>predictable</code>/<code>pro</code>. <strong>The visible plan labels stay the same</strong> (&ldquo;Performance&rdquo; / &ldquo;Predictable&rdquo; / &ldquo;Hybrid&rdquo;) so Karen&rsquo;s mental model isn&rsquo;t disturbed by an internal refactor &mdash; a future copy-refresh ship can rename if the marketing story shifts. Dropped: the v1.7.2 <code>refillPlanToUiPlan</code> mapper that translated between the two key spaces (no longer needed &mdash; <code>PLAN_META</code> keys are the refill names directly). The chain-break that bit us in tonight&rsquo;s v1.7.x walks &mdash; &ldquo;click Pick Performance &rarr; cron picks up Karen&rdquo; was wrong because the UI wrote to <code>emma_pricing_plans</code> while the cron read <code>refill_pricing_plans</code> &mdash; is now structurally impossible. Plan selection writes to the same table the cron reads, end of story. <strong>(2) InvoiceRow TZ rendering</strong>: the period label on each invoice row was rendering the UTC month boundary through the browser&rsquo;s local timezone, so May 1 UTC (<code>2026-05-01T00:00:00Z</code>) shifted back one day in any negative-offset locale and the label read &ldquo;April 2026&rdquo; in Mountain Time. Fix: added <code>timeZone: &quot;UTC&quot;</code> to <code>toLocaleString</code> so the label matches the period the row actually covers. Three-line fix on the format options object. <strong>(3) Stale copy cleanup</strong>: the active-plan banner&rsquo;s sub-line used to say &ldquo;Stripe linkage in v366.x &middot; invoices stay in draft until then&rdquo; &mdash; that copy was written before v1.6 wired the actual Stripe linkage. Now reads the truthful state via the <code>pm.hasCardOnFile</code> signal already available on the page: &ldquo;Card on file &middot; invoices charge automatically&rdquo; or &ldquo;No card on file yet &middot; invoices stay in draft until added.&rdquo; Touched: <code>src/routes/app.billing.tsx</code> (9 surgical edits across imports, PLAN_META, state types, pickPlan signature, plan iteration array, PlanCard prop type, planSummary signature, InvoiceRow rendering, active-plan banner copy), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.8",
    date: "May 2026",
    items: [
      "<strong>v1.8 &mdash; Stripe webhook handler (closes Phase B-full, the production-reliability backstop).</strong> Phase B-lite shipped in v1.7.2 closed the synchronous card-on-file path: <code>finalizeInvoice</code> &rarr; <code>pay()</code> returns &lsquo;paid&rsquo; in the same call, DB row gets stamped. That covers the happy path. Phase B-full closes the async paths: delayed-confirmation methods (SEPA / ACH / 3DS-required cards) settle hours-to-days later via webhook only; hard payment failures fire <code>invoice.payment_failed</code>; rare upstream Stripe issues fire <code>invoice.finalization_failed</code>. None of these mutate the DB without a webhook listener. v1.8 wires one. <strong>New route</strong>: <code>src/routes/api.webhooks.stripe.ts</code> &rarr; <code>POST /api/webhooks/stripe</code>. Pattern mirrors the existing <code>api.webhooks.scheduler.acuity.$secret.ts</code> handler: raw body read first (HMAC requires exact bytes), signature verified via <code>stripe.webhooks.constructEventAsync</code> (the Web-Crypto variant &mdash; CF Workers don&rsquo;t expose Node&rsquo;s <code>crypto.createHmac</code> sync API), audit row inserted into the pre-existing <code>stripe_webhook_events</code> table (unique on <code>stripe_event_id</code> &mdash; duplicate retries hit Postgres 23505 and return 200 immediately), then routed to the appropriate handler. <strong>Three handlers</strong>: (1) <code>invoice.paid</code> &rarr; <code>refill_invoices.status = &lsquo;paid&rsquo;</code>, <code>paid_at = now()</code>, <code>sent_at = now()</code> if still null. Belt-and-braces idempotency via <code>.is(&quot;paid_at&quot;, null)</code> guard on the update so late-arriving retries can&rsquo;t double-stamp. (2) <code>invoice.payment_failed</code> &rarr; status &lsquo;failed&rsquo;, with an &lsquo;already paid&rsquo; edge-case skip so a stale failure event arriving after a retry&rsquo;s success can&rsquo;t flip a paid row backward. (3) <code>invoice.finalization_failed</code> &rarr; same as payment_failed. <strong>Product gating</strong>: events without <code>metadata.product == &quot;refill&quot;</code> get audited but skip the refill_invoices update &mdash; this matches the contract our cron stamps on invoice creation in <code>generateMonthlyInvoiceForTenant</code>, and keeps the shared <code>stripe_webhook_events</code> table forensically complete without misclassifying legacy emma / WCS events. <strong>Always returns 200</strong> (even on handler errors) so Stripe doesn&rsquo;t exponential-retry-hammer the endpoint &mdash; failures are captured in the audit row&rsquo;s <code>error_message</code> column for replay. The only 4xx is missing signature header (legitimate bad request); the only 5xx is missing env (intentional &mdash; if env is broken we WANT Stripe to retry once it&rsquo;s fixed). <strong>Wiring</strong>: webhook secret lives at <code>STRIPE_WEBHOOK_SECRET</code> in CF Worker secrets (set via <code>wrangler secret put</code>). Endpoint URL <code>https://getrefill.app/api/webhooks/stripe</code> needs to be registered in Stripe Dashboard at <code>https://dashboard.stripe.com/test/webhooks/create</code> with events <code>invoice.paid</code>, <code>invoice.payment_failed</code>, <code>invoice.finalization_failed</code>. Once the secret is bound + the endpoint is live, the entire Phase B loop is webhook-backed. Touched: <code>src/routes/api.webhooks.stripe.ts</code> (new, 230 lines), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.7.2",
    date: "May 2026",
    items: [
      "<strong>v1.7.2 &mdash; auto-charge + Invoice History UI fix (closes the Phase B-lite loop).</strong> The v1.7.1 walk surfaced two remaining gaps that v1.7.2 closes in one ship. <strong>(1) Engine gap &mdash; auto-charge:</strong> v1.7.1 created the Stripe Invoice with the correct $421.80 line item and called <code>finalizeInvoice</code>, but the invoice landed at <code>open</code> + <code>past_due</code> instead of <code>paid</code>. Diagnosis: I set <code>auto_advance:false</code> at invoice creation (correct &mdash; so I could add the InvoiceItem before Stripe raced me on a $0 empty invoice) but never flipped it back, so <code>finalizeInvoice</code> moved status to <code>open</code> without attempting the charge. Fix: explicit <code>stripe.invoices.pay(stripeInvoice.id)</code> call after finalize, returns the invoice with the charge attempted synchronously. The DB stamp now uses <code>paid.status</code> (the truthful post-charge result) instead of <code>finalized.status</code> (which was always non-paid because of the auto_advance flag). Idempotency key suffix bumped to <code>_v3</code> so v1.7.1&rsquo;s cached non-pay responses in Stripe&rsquo;s 24h idempotency window don&rsquo;t replay. <strong>(2) UX gap &mdash; Invoice History visibility:</strong> <code>/app/billing</code>&rsquo;s Invoice History table was empty even after v1.7.1 successfully wrote a <code>refill_invoices</code> row, because <code>app.billing.tsx</code> imported <code>listInvoices</code> from <code>emma-billing.functions.ts</code> (legacy user-scoped, reads <code>emma_invoices</code>) while the cron writes to <code>refill_invoices</code> (newer tenant-scoped). Same emma&harr;refill fork that bit the plans path. Fix: switched <code>listInvoices</code> + the related <code>Invoice</code>/<code>RefillInvoice</code> type usages to import from <code>refill-billing.ts</code>; added <code>refillPlanToUiPlan</code> mapper (<code>starter</code>&rarr;<code>performance</code>, <code>pro</code>&rarr;<code>hybrid</code>, <code>predictable</code> passthrough) at <code>InvoiceRow</code> render time so the plan-label cell uses the existing <code>PLAN_META</code> keyed on the emma-era names. The deeper emma&harr;refill unification (plans + active-plan banner + getActivePlan) stays on the backlog &mdash; not load-bearing for this ship. Touched: <code>src/server/refill-billing.ts</code> (pay() call + key suffix bump), <code>src/routes/app.billing.tsx</code> (imports + type aliases + plan-name mapper).",
    ],
  },
  {
    version: "v1.7.1",
    date: "May 2026",
    items: [
      "<strong>v1.7.1 &mdash; Stripe push line-items bug fix.</strong> v1.7&rsquo;s end-to-end walk surfaced that the Stripe Invoice created by the cron came out with <code>$0.00</code> total / zero line items &mdash; the <code>InvoiceItem</code> with the $421.80 share amount got orphaned on the customer instead of being pulled into the Invoice. Stripe auto-marked the empty $0 invoice as paid (the &lsquo;paid&rsquo; status in the DB row was technically true but misleading &mdash; no actual charge happened). <strong>Root cause</strong>: v1.7 used the &lsquo;pending pool&rsquo; pattern (create <code>InvoiceItem</code> without specifying an invoice &rarr; expect Stripe to auto-pull it into the next <code>Invoice</code>). That pattern&rsquo;s default <code>pending_invoice_items_behavior</code> isn&rsquo;t reliable on the <code>2026-04-22.dahlia</code> API version we&rsquo;re on. <strong>Fix &mdash; approach 2 (invoice-scoped items):</strong> (1) create the <code>Invoice</code> as a draft with <code>auto_advance:false</code> and <code>pending_invoice_items_behavior:&quot;exclude&quot;</code> (the &lsquo;exclude&rsquo; also prevents v1.7&rsquo;s orphaned pending item, still sitting on the customer in test mode, from being double-pulled into future invoices); (2) create the <code>InvoiceItem</code> with <code>invoice: stripeInvoice.id</code> so it&rsquo;s scoped specifically to that invoice rather than going to the pending pool; (3) <code>finalizeInvoice</code> triggers the charge attempt synchronously as before. Idempotency key suffix bumped from no-version to <code>_v2</code> so retries don&rsquo;t hit v1.7&rsquo;s cached (bad) responses within Stripe&rsquo;s 24h idempotency window. <strong>Recovery for previously-failed periods</strong>: the cron skips the Stripe push when the DB row already has <code>stripe_invoice_id IS NOT NULL</code> (idempotency guard). For v1.7&rsquo;s bad-state row, clear it via a one-shot SQL update (<code>set stripe_invoice_id = null, status = &#39;draft&#39;, sent_at = null, paid_at = null</code>) and re-trigger the cron &mdash; v1.7.1&rsquo;s new code path with fresh idempotency keys will produce a proper-amount invoice. Touched: <code>src/server/refill-billing.ts</code> (Stripe push branch refactor).",
    ],
  },
  {
    version: "v1.7",
    date: "May 2026",
    items: [
      "<strong>v1.7 &mdash; Stripe invoice push + auto-charge (Phase B-lite of the $$ loop).</strong> v1.6 captured the card; v1.7 actually uses it. <code>generateMonthlyInvoiceForTenant</code> in <code>src/server/refill-billing.ts</code> now extends past the DB-draft write: when the upserted row has <code>total_due_usd &gt; 0</code> AND the tenant has a <code>stripe_customer_id_{mode}</code> AND a card on file AND no existing <code>stripe_invoice_id</code> on the row, it (1) creates a Stripe <code>InvoiceItem</code> for the total amount, (2) creates a Stripe <code>Invoice</code> with <code>collection_method:&quot;charge_automatically&quot;</code> + <code>default_payment_method</code> set to the saved card + <code>auto_advance:true</code>, (3) calls <code>finalizeInvoice</code> &mdash; which synchronously triggers the charge attempt against the saved card. On success the DB row stamps <code>stripe_invoice_id</code> and flips <code>status</code> to <code>&quot;sent&quot;</code> (or <code>&quot;paid&quot;</code> if Stripe cleared the charge in the same call), plus <code>sent_at</code> / <code>paid_at</code>. Idempotency keys (<code>refill_inv_&lt;tenant&gt;_&lt;period&gt;_item</code> / <code>_invoice</code>) prevent duplicate Stripe Invoices on cron retry within Stripe&rsquo;s 24h window; DB idempotency on <code>(tenant_id, period_start)</code> + the <code>stripe_invoice_id IS NULL</code> guard prevent re-push on long-lag retries. Push failures are caught and logged but non-fatal &mdash; the DB row stays at <code>draft</code> so the next cron run retries; the spa-side state stays correct (Karen sees the draft, knows it didn&rsquo;t charge yet). Webhook-driven sent&rarr;paid flips on <code>invoice.paid</code> / <code>invoice.payment_failed</code> are Phase B-full&rsquo;s job; for now the truthful state lives in Stripe and Karen can verify via the Manage button (Stripe Customer Portal) which always shows the real status. <strong>Manual-trigger / testing</strong>: <code>/api/cron/refill-invoice</code> now accepts <code>?period=YYYY-MM</code> as a query-param override for the prior-month default &mdash; useful for firing against the current month&rsquo;s verified events without waiting for the 1st-of-next-month natural cron. Production cron leaves the param off and gets the prior-month behavior unchanged. Comment block on the route + the doc-block on <code>generateMonthlyInvoicesForAll</code> updated to reflect the new behavior. Touched: <code>src/server/refill-billing.ts</code> (Stripe push branch + periodMonth optional arg + Stripe import already present from v1.6), <code>src/routes/api.cron.refill-invoice.ts</code> (period query param + doc-block).",
    ],
  },
  {
    version: "v1.6",
    date: "May 2026",
    items: [
      "<strong>v1.6 &mdash; Stripe payment-method capture wired end-to-end (Phase A of the $$ loop).</strong> The Refill Stripe surface had been built but never connected: <code>/api/refill-checkout</code> (264 lines, setup-mode + subscription-mode), <code>/api/refill-portal</code> (127 lines, Customer Portal session), mode-aware <code>stripe_customer_id_{test,live}</code> columns on tenants, monthly invoice cron, Stripe webhook handler &mdash; all sitting there, all unreachable from the UI. The Billing page&rsquo;s &lsquo;Pick a plan&rsquo; button called <code>applyPricingPlan</code> (DB-only) and stopped there, so no Stripe Customer was ever created, no card was ever captured, and the footer disclaimer literally read &lsquo;v366.x adds the Stripe customer + auto-pay flow when you&rsquo;re ready.&rsquo; This ship is that v366.x. <strong>Three additions:</strong> <strong>(1)</strong> New <code>getPaymentMethodStatus</code> server fn in <code>src/server/refill-billing.ts</code> &mdash; reads the mode-aware customer id from tenants, calls <code>stripe.customers.listPaymentMethods</code>, returns <code>{ hasCardOnFile, brand, last4, expMonth, expYear, stripeMode }</code>. Best-effort: failures don&rsquo;t take down the rest of the billing page. <strong>(2)</strong> New <code>CardOnFileSection</code> component rendered on <code>/app/billing</code> between the active-plan banner and the plan selector. Three states: loading spinner, emerald-tinted &lsquo;Card on file &middot; Visa &middot;&middot; 4242 &middot; expires MM/YY&rsquo; with a Manage CTA (opens Stripe Customer Portal), or the neutral &lsquo;No card on file yet&rsquo; row with a sage primary &lsquo;Add payment method&rsquo; CTA. <strong>(3)</strong> Wired the CTAs: Add-card POSTs to <code>/api/refill-checkout</code> with <code>plan: &quot;starter&quot;</code> always (setup mode, no charge regardless of which plan the spa is on &mdash; per [[project-trial-first-no-money-asks]], the card just sits on file until the first verified-recovery invoice runs); Manage POSTs to <code>/api/refill-portal</code>. Both redirect via <code>window.location.href = body.url</code>. Return URL handler: <code>?upgrade=success</code> toasts &lsquo;Card on file. You&rsquo;re all set.&rsquo; and refreshes; <code>?upgrade=cancelled</code> toasts the no-pressure message; both strip the search params via <code>replaceState</code> so a refresh doesn&rsquo;t re-trigger the toast. Footer disclaimer rewritten from &lsquo;coming soon&rsquo; to the live-now Stripe-hosted-checkout reassurance. Test-mode badge surfaces under the card row when <code>STRIPE_SECRET_KEY</code> starts with <code>sk_test_</code>. <strong>What this Phase A does NOT do</strong> (queued for next ships): Phase B &mdash; the monthly invoice cron still ships <code>refill_invoices</code> rows as DB drafts; pushing them to Stripe + finalize + auto-charge against the saved card is the next milestone. Phase C &mdash; trial-end + post-recovery drip CTAs that nudge card capture without disrupting onboarding. Touched: <code>src/server/refill-billing.ts</code> (new fn + Stripe import), <code>src/routes/app.billing.tsx</code> (imports, state, two handlers, return-URL effect, section render, component definition).",
    ],
  },
  {
    version: "v1.5.3",
    date: "May 2026",
    items: [
      "<strong>v1.5.3 &mdash; Build Log regenerator ported from openagenticv4 (no more hand-rolled doc).</strong> The <code>Refill-Build-Log.html</code> on Desktop had been hand-maintained since the Refill cleave because the regen script never came across &mdash; minor friction per ship, but the rot was compounding: by v1.5.2 the hand-rolled doc was a 14-version essay-write each time. Ported <code>openagenticv4/scripts/build-history.ts</code> &rarr; <code>refill-app/scripts/build-history.ts</code> with four adaptations: <strong>(1)</strong> Refill&rsquo;s <code>CHANGELOG</code> items are already HTML-formatted (the PageHeader v-pill renders them via raw-HTML), so build-log items pass through verbatim instead of going through the source script&rsquo;s markdown&rarr;HTML escape pipeline that would have double-escaped every <code>&lt;strong&gt;</code> tag. <strong>(2)</strong> Refill uses semver-ish versions (v1.5.2, v1.4.6, v1.5, v1.4) so the sort comparator walks all three numeric segments instead of just the leading int &mdash; the source&rsquo;s <code>parseInt(v[1])</code> would have collapsed every Refill version to <code>1</code> and sorted them in undefined order. <strong>(3)</strong> Replaced the Agentiport-era strategic milestones with 9 Refill-era ones (spinout 5/16 &rarr; pricing killshot 5/16 &rarr; trial-first 5/20 &rarr; single-feature positioning 5/22 &rarr; rejuv-proof-or-nothing 5/26 &rarr; engine proven 5/26 &rarr; iMessage MCP polish 5/26 &rarr; autonomous cron + step 6 proven 5/26). <strong>(4)</strong> Accent palette shifts from Agentiport amber/gold to Refill emerald (#056048) / pale sage (#e8f3ed); Georgia serif headings match RefillHome&rsquo;s &lsquo;Hey {spa}.&rsquo; greeting; paper-tone bg (#faf8f3) preserved. Dropped the Patents section &mdash; not load-bearing for Refill at this stage. New <code>package.json</code> script alias: <code>bun run build-log</code> regenerates the doc after every ship. Touched: <code>scripts/build-history.ts</code> (new), <code>package.json</code> (one-line alias), <code>src/lib/changelog.ts</code> (this entry).",
    ],
  },
  {
    version: "v1.5.2",
    date: "May 2026",
    items: [
      "<strong>v1.5.2 &mdash; Patient-context OG card for rescue claim link previews.</strong> v1.5.1 wired up <code>og:image</code> with the spa-owner marketing card (&ldquo;Free for 30 days. 12% of what we recover&rdquo;) &mdash; great fix for the OpenAgentic-homepage bug, but tone-mismatched for the patient receiving a Tox / filler rescue link. A patient seeing pricing copy on a preview card breaks the &ldquo;your slot just opened up&rdquo; framing. This ship swaps to a patient-receiver-tone card. New asset: <code>public/brand/refill-og-patient.svg</code> (canonical) + <code>refill-og-patient.png</code> (rendered 1200&times;630). Same paper-tone aesthetic + emerald Georgia wordmark per Refill-Standalone-Architecture.html &sect;11.1; copy swaps to &ldquo;Your appointment slot just opened up&rdquo; / &ldquo;Tap to claim &mdash; first-tap-wins.&rdquo; / getrefill.app. <code>__root.tsx</code> keeps the marketing variant (correct for home-page shares + outreach link previews to spa owners); <code>rescue.claim.$token.tsx</code> points at the patient variant. og:image:alt updated to match. Touched: <code>public/brand/refill-og-patient.{svg,png}</code> (new files), <code>src/routes/rescue.claim.$token.tsx</code>.",
    ],
  },
  {
    version: "v1.5.1",
    date: "May 2026",
    items: [
      "<strong>v1.5.1 &mdash; Refill OG image + brand asset migration (kills the OpenAgentic preview in iMessage).</strong> The first end-to-end iMessage-MCP-routine smoke test 2026-05-26 2:11 PM MT surfaced a queue item from v379.1 that had never been worked: rescue claim iMessages were rendering with the generic OpenAgentic homepage preview card (&ldquo;Describe what you want. Deploy with confidence.&rdquo;) because the rescue.claim.$token route's <code>head()</code> set og:title / og:description / og:site_name but never an <code>og:image</code>, and the link-preview scraper fell back to whatever it could find on-page. Sub-issue surfaced: <code>public/brand/</code> didn't exist in this repo at all &mdash; the Refill cleave from openagenticv4 left the brand SVGs behind, so <code>refill-favicon.svg</code> referenced from <code>__root.tsx</code> was silently 404ing on every page (confirmed via curl HEAD against production). Fix: <strong>(1)</strong> Migrated the 3 Refill brand SVGs (<code>refill-favicon.svg</code>, <code>refill-og.svg</code>, <code>refill-wordmark.svg</code>) from <code>openagenticv4/public/brand/</code> &rarr; <code>refill-app/public/brand/</code>. <strong>(2)</strong> Rendered <code>refill-og.png</code> (1200&times;630) from the SVG via <code>@resvg/resvg-js-cli</code> &mdash; iMessage's link-preview scraper requires raster (PNG/JPG); SVG og:image renders inconsistently across iOS / Slack / Twitter. <strong>(3)</strong> Added <code>og:image</code> + <code>og:image:width</code> + <code>og:image:height</code> + <code>og:image:alt</code> + <code>twitter:image</code> meta tags to both <code>rescue.claim.$token.tsx</code> (patient-facing rescue link preview) and <code>__root.tsx</code> (default for all other routes &mdash; home page shares, etc.). <strong>(4)</strong> Upgraded <code>twitter:card</code> from <code>summary</code> &rarr; <code>summary_large_image</code> for the richer hero-image preview. Tone note: the current OG card is the spa-owner marketing variant (&ldquo;Free for 30 days. 12% of what we recover&rdquo;), which is pricing-forward for a patient receiving a Tox rescue link. v1.5.2 will swap to a patient-context variant (&ldquo;Refill &mdash; your appointment slot is waiting&rdquo;). Touched: <code>public/brand/*</code> (3 SVGs + 1 generated PNG), <code>src/routes/rescue.claim.$token.tsx</code>, <code>src/routes/__root.tsx</code>. v1.5.1 is the cosmetic-but-load-bearing finish line on the iMessage MCP workflow.",
    ],
  },
  {
    version: "v1.5",
    date: "May 2026",
    items: [
      "<strong>v1.5 &mdash; Auto-send directive at top of proxy email (Claude Desktop + iMessage MCP).</strong> Karen&rsquo;s &lsquo;Refill rescue drafter&rsquo; Claude Desktop routine runs hourly, watching Gmail for new Refill proxy emails and firing one <code>draft_imessage(recipient_phone, body)</code> call per patient draft block via the iMessage MCP server. Worked end-to-end in v379 (2026-05-18, David Anderson test patient) when Karen typed an explicit prompt; the missing piece for the routine path was an unambiguous directive in the email itself so the routine&rsquo;s prompt parses deterministically without per-fire human intervention. Fix: <code>composeProxyEmail</code> now leads with an AUTO-SEND header (text version: <code>═══ AUTO-SEND ═══</code> block; HTML version: emerald-tinted callout card above the &ldquo;N patients on your waitlist&hellip;&rdquo; summary) naming the MCP tool by name and describing the per-patient call pattern. Bottom-of-email instruction relaxed to the human-only path (&lsquo;Prefer manual? Just forward whichever claim URLs above to those patients yourself.&rsquo;). Touched <code>src/server/emma-rescue.functions.ts</code> (composeProxyEmail text + html bodies). No schema change, no behavioral change to the dispatcher &mdash; pure email-body content tightening. Lane 1 polish item now load-bearing for the hourly automation path.",
    ],
  },
  {
    version: "v1.4.6",
    date: "May 2026",
    items: [
      "<strong>v1.4.6 &mdash; TZ display sweep (renderers compensate for v1.4.3 true-UTC storage).</strong> Karen's successful end-to-end rescue test 2026-05-26 1:18 PM MT exposed the elegant flip-side of v1.4.3: the patient claim page + proxy email rendered the slot as &ldquo;Fri, May 29 at 10:45 PM&rdquo; while Acuity's confirmation said &ldquo;4:45 PM MDT&rdquo; (the real time). A patient would show up 6 hours late or dismiss the offer as a late-night spam. Root cause: 8 renderers across 5 files pinned <code>timeZone: &quot;UTC&quot;</code> per the v379.2 architecture where storage was deliberately TZ-naive (spa-local clock packed into a UTC string &mdash; display was correct by accident because storage was deliberately wrong). v1.4.3 fixed storage to true UTC; renderers were never updated. Sweep: changed all 8 callsites from <code>&quot;UTC&quot;</code> &rarr; <code>&quot;America/Denver&quot;</code> (matches existing pattern in <code>emma-blast.functions.ts:635</code> which already does this for Rejuv). Touched: <code>emma-rescue.functions.ts</code> (composeRescueSms + formatRescueWhen), <code>rescue.claim.$token.tsx</code> (claim page), <code>app.refill.recovery.tsx</code> (Karen's recovery dashboard, 2 callsites), <code>app.refill.patients.$patientId.tsx</code> (patient detail), <code>app.refill.appointments.tsx</code> (appointments view, 2 callsites), <code>app.rep.ledger.tsx</code> (rep ledger). One callsite preserved as UTC: <code>app.refill.patients.index.tsx:1013</code> renders a date-only value constructed from <code>Date.UTC(y, m-1, d)</code>; rendering in local TZ would shift days across midnight. Per-spa TZ schema (lift the hardcoded Rejuv TZ to a per-tenant column) is a Lane 2 backlog item, queued for when spa #2 onboards.",
    ],
  },
  {
    version: "v1.4.5",
    date: "May 2026",
    items: [
      "<strong>v1.4.5 &mdash; Patient rescue-claim URL points at getrefill.app (not stale emma.agentiport.com).</strong> The FIRST successful end-to-end Karen Acuity cancel 2026-05-26 1:02 PM MT (the [[project-rejuv-proof-or-nothing]] moment) landed the Resend email correctly in davidfand303@gmail.com — but the embedded patient claim URL hardcoded <code>https://emma.agentiport.com/rescue/claim/&lt;token&gt;</code>, the legacy openagenticv4 host. The actual /rescue/claim/$token route lives on getrefill.app (src/routes/rescue.claim.$token.tsx) post-Refill-cleave. Fix: <code>buildRescueClaimUrl</code> now reads <code>process.env.REFILL_PUBLIC_ORIGIN</code> with a getrefill.app fallback, matching the established pattern in src/server/rep-platform.ts.",
    ],
  },
  {
    version: "v1.4.4",
    date: "May 2026",
    items: [
      "<strong>v1.4.4 &mdash; Lazy fromNumber check (unblock proxy-email-only rescue mode).</strong> Karen's second live cancel after v1.4.3 surfaced one more chain break: the rescue dispatcher's upfront <code>if (!fromNumber)</code> bail fired regardless of delivery path, marking the rescue attempt closed_unfilled with notes 'Spa has no provisioned SMS number.' But per [[project-carriers-mothballed]] Twilio is OFF — patient outreach happens via iMessage MCP through Karen's machine, with the spa-owner getting a consolidated proxy email from Resend (the email carries patient claim URLs Karen forwards). Karen's setup is proxy-email-only (rescue_proxy_email set, rescue_proxy_phone null) — which doesn't need a fromNumber at all. The fix makes the check path-aware: <code>willNeedFromNumber = !isProxyMode || !!proxyPhone</code>. Pure proxy-email-only spas (= Karen, = every Refill spa post-carrier-mothball) now flow through cleanly; spas configured for proxy-SMS or direct-mode still get the structural guard. Net: the dispatcher's THIRD pre-INSERT early-return path needed a fix to match the post-mothball architecture.",
    ],
  },
  {
    version: "v1.4.3",
    date: "May 2026",
    items: [
      "<strong>v1.4.3 &mdash; Acuity timezone-parsing fix (caught by Karen's first live cancel).</strong> The Acuity &rarr; emma_appointments ingest path had a long-latent timezone bug: both <code>acuityAppointmentToRow</code> (server fn) and <code>acuityAppointmentToInsert</code> (webhook receiver) parsed Acuity&rsquo;s timezone-aware datetime with <code>.replace(/[+-]\\d{4}$/, &quot;Z&quot;)</code> &mdash; a regex that stripped the TZ offset WITHOUT applying it. So &ldquo;2026-05-26T16:00:00-0600&rdquo; (4 PM MT, the spa's local) became &ldquo;2026-05-26T16:00:00Z&rdquo; (4 PM UTC = 10 AM MT), shifting every appointment time 6 hours too early for an MT spa. Karen's first live cancel exposed it: she booked the test for 4 PM MT, cancelled at 12:18 PM MT &mdash; the dispatcher correctly skipped because the (corrupted) scheduled_at said the slot was already 2.5 hours past, when it was actually still 3.5 hours future. Fix: replaced regex with <code>new Date(apt.datetime).toISOString()</code>, which JavaScript's Date constructor handles correctly for every Acuity datetime shape (with or without colon in offset, Z-suffix, etc.). Implications well beyond the dispatcher: Karen&rsquo;s 61-appointment backfill ALL had wrong scheduled_at values; every recovery dashboard time, every preshow reminder, every QBO reconciliation window calculation was 6 hours off. Backfill remediation: Karen clicks &ldquo;Re-sync now&rdquo; on Settings/Scheduler post-deploy &mdash; the upsert overwrites every existing row with corrected UTC. Pinning the lesson per [[feedback-no-blame-no-glide]]: I missed the regex during the original scout because &ldquo;.replace timezone with Z&rdquo; LOOKED correct without thinking through that it's a string-substitution, not a time-zone math operation. Real-data testing > extensive code review; this is exactly what [[project-rejuv-proof-or-nothing]] was designed to surface.",
    ],
  },
  {
    version: "v1.4.2",
    date: "May 2026",
    items: [
      "<strong>v1.4.2 &mdash; Centering sweep (the bug v1.4.1 exposed).</strong> Karen-walk on the v1.4.1 re-skin caught Recovery + Billing rendering their headers and body content left-anchored under the centered chip nav &mdash; a jarring visual inconsistency. Root cause: a pre-existing pattern across 7 refill routes where the page body wrapper used <code>max-w-Xxl px-6 lg:px-10</code> without <code>mx-auto</code>, so the content left-anchored to the screen edge while the chrome (chip nav, top-right strip) centered. The old sticky/backdrop chrome bar masked this; the v1.4.1 re-skin removed the bar and surfaced it. Class-of-bug audit + sweep: added <code>w-full mx-auto</code> to 7 routes (recovery, billing, appointments, reports, rescue, settings/noshow, settings/sender). Inbox + settings/scheduler already had it &mdash; pattern was inconsistent across the codebase, now uniformly centered to match RefillHome + /scan + /onboard.",
    ],
  },
  {
    version: "v1.4.1",
    date: "May 2026",
    items: [
      "<strong>v1.4.1 &mdash; PageHeader re-skin (Refill house style).</strong> The Karen walk + v1.4 Billing fix surfaced a deeper design mismatch: the authed sub-pages (Recovery, Inbox, Settings, Billing, plus 14 other refill.* routes) all wore platform-y chrome &mdash; sticky header bar with backdrop blur, all-caps &lsquo;REFILL&rsquo; eyebrow, sans-serif title + amber version pill, breadcrumbs &mdash; while the rest of getrefill (/scan, /onboard, /login, RefillHome) used the warm brand-forward Refill aesthetic: Georgia serif h1, light paper bg, soft ink-soft lede, sage accents, no eyebrows, no breadcrumbs, no in-body version pill. The handoff between public and authed surfaces was jarring. Rewrote <code>PageHeader.tsx</code> in-place to the Refill house style: Georgia serif h1 (matches RefillHome&rsquo;s &lsquo;Hey {spa}.&rsquo; greeting), soft ink-soft description, kept the actions slot (Refresh buttons etc.) on the right of title, dropped sticky/backdrop chrome and breadcrumb rendering. The <code>eyebrow</code> and <code>breadcrumbs</code> props are kept on the interface as silent back-compat no-ops so the 25 callers don&rsquo;t need touching. Relocated the version-pill + changelog Popover into <code>RefillShellChrome</code>&rsquo;s top-right strip so there&rsquo;s a single v-pill per session rather than one duplicated on every page; restyled in soft sage (#e8f3ed background, #056048 ink) to match brand. Net: 3 files touched (PageHeader.tsx, RefillShellChrome.tsx, changelog.ts), 25 routes inherit the new look automatically.",
    ],
  },
  {
    version: "v1.4",
    date: "May 2026",
    items: [
      "<strong>v1.4 &mdash; Billing-route + brand-CTA cleanup (Karen-walk pinches).</strong> The Karen live walk surfaced three destination-never-built bugs from features that shipped half-wired: (1) Billing chip routed to <code>/app/refill/billing</code> which redirected to <code>/app/billing</code> &mdash; a route speced as the canonical Refill billing surface in v391 but the frontend was never created (only backend + Stripe API routes shipped); (2) the &lsquo;Connect Acuity (30 sec)&rsquo; post-receipt CTA on /scan pointed at <code>/start</code>, also never built; (3) the header &lsquo;Already a customer? Sign in &rarr;&rsquo; link on /scan ALSO pointed at <code>/start</code> &mdash; a hidden third bug exposed by the brand.ctaHref being overloaded across login + signup intents. Fix: built <code>/app/billing</code> proper (lifted the working BillingPage from app.refill.billing.tsx, swapped 5 Emma&rarr;Refill copy leaks, removed the unreachable useShell-gated redirect wrapper); stripped app.refill.billing.tsx to a 5-line back-compat <code>&lt;Navigate to=&quot;/app/billing&quot; replace /&gt;</code> shim for stale email links; rewired 4 internal references (RefillNav chip, RefillHome ActionCard, RefillShellChrome active-chip derive, the &lsquo;Choose a plan&rsquo; CTA on Recovery); split <code>brand.ctaHref</code> into <code>ctaHref</code> (now <code>/onboard</code>) and new <code>loginHref</code> (<code>/login</code>) so signup and signin links route correctly. Same pre-flight gap that hid these bugs from earlier walks: HTTP 200 &ne; destination-route-exists; click-through pre-flight is now the rule.",
    ],
  },
  {
    version: "v1.3",
    date: "May 2026",
    items: [
      "<strong>v1.3 &mdash; /onboard CSV-drop fallback for non-Acuity spas.</strong> Until tonight, Step 2 of the onboard wizard had a hard dead-end for any spa not on Acuity &mdash; the only escape was &lsquo;ask us to enable your platform manually after you finish setup.&rsquo; Added a secondary &lsquo;Upload a client list CSV instead&rsquo; link on Step 2&rsquo;s needs-connect state that navigates to /onboard?step=3&amp;source=csv and skips OAuth entirely. Step 3 reads the source param, dispatches to a new <code>Step3PatientsCsv</code> branch with a drop zone (parsing client-side, posting to existing <code>ingestClientListCsv</code> server fn so we&rsquo;re reusing the battle-tested ingest pipeline behind app.refill.patients.contacts.tsx). State machine: awaiting / ingesting / imported / error. Refactored <code>Step3Patients</code> into a hook-stable dispatcher (renamed the old logic to <code>Step3PatientsAcuity</code>; thin Step3Patients now branches on source). After CSV import, the wizard Continue button advances to Step 4 same as the Acuity path &mdash; the rest of onboarding is source-agnostic. Closes Task #4.",
    ],
  },
  {
    version: "v1.2",
    date: "May 2026",
    items: [
      "<strong>v1.2 &mdash; /scan funnel sharpening (CSV-export polish + Acuity live-mode upgrade).</strong> Original Task #3 was framed as &lsquo;pivot /scan to OAuth-first&rsquo;, but a scout pass surfaced that OAuth-at-the-door conflicts with the trojan-horse stealth-widget posture (asking for OAuth before proving value is exactly what incumbent PMS partners fear). Split the two problems: (1) CSV-export friction &mdash; new inline guide picker beneath the drop zone lets a non-tech spa owner pick their scheduler and see verified export steps (5 platforms with confirmed paths, the other 19 fall through to a generic fallback per the no-fabrication rule); (2) post-receipt OAuth upgrade &mdash; when the parsed dialect resolves to Acuity (deterministic OR AI-mapped), the bottom CTA swaps from generic /start framing to &lsquo;Connect Acuity for live mode&rsquo; with explicit pass-through to /start?detected=acuity. Non-Acuity users get a heads-up line that live-mode for their platform is on the roadmap. Architecture memorialized in project_scan_trojan_horse_architecture.",
    ],
  },
  {
    version: "v1.1",
    date: "May 2026",
    items: [
      "<strong>v1.1 &mdash; RefillShell routing fixes (Karen dry-run prep).</strong> Pre-walk code-audit surfaced 4 hard 404s in the spa-owner shell: Billing nav chip, Billing quick-action card, header gear icon, and the active-chip derive all pointed at top-level routes that don&rsquo;t exist (the nested /app/refill/* convention is the established one). Rewired so a first-time owner click on Billing or Settings lands on the right page.",
    ],
  },
  {
    version: "v1.0",
    date: "May 2026",
    items: [
      "<strong>v1.0 &mdash; Refill cleave landed.</strong> Standalone repo + standalone Supabase + standalone CF Worker on getrefill.app. The 2-week cleave from openagenticv4 collapsed the cross-host auth bridge stack (set Site URL = getrefill.app, single cookie domain, zero shell stamping). 74 migrations ported, 90+ source files lifted, ~50 KLOC of bridge/product-context entanglement deleted by simplification.",
    ],
  },
];

export function currentVersion(): string {
  return CHANGELOG[0]?.version ?? "v1.0";
}
